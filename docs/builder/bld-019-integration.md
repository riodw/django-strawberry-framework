# Build: Cross-slice integration pass — spec-019 (consumer override semantics, scalar fields)

Spec reference: [`docs/SPECS/spec-019-consumer_overrides_scalar-0_0_6.md`][spec-019]
Rationale companion: [`docs/SPECS/appx/spec-019-consumer_overrides_scalar-0_0_6-rationale.md`][spec-019-rationale]
Build plan: [`docs/builder/build-019-consumer_overrides_scalar-0_0_6.md`][build-019]
Prior round artifact (required context, read in full): [`docs/builder/bld-review-1-spec019_rationale.md`][bld-r1]
Status: final-accepted

**Cycle shape.** This is a residual closeout cycle that produced **one documentation round and zero source changes**. [`docs/builder/BUILD.md`][build]'s `## Cross-slice integration pass` is written for a multi-slice code build; several of its mechanical steps have no population here. Each such step is recorded below **with its reason**, per the dispatch: a skip recorded with its reason is a decided answer, silence is not.

Declarations carried from the plan, unchanged and deliberate:

- Ownership partition: `none; single sequential round`.
- Hot-path declaration: `none` (documentation-only cycle; no package source file is written).
- Floor-verification scope: `none` (no Django / Strawberry / channels seam is touched). No floor venv run is owed by this pass or by the final gate.

`HEAD` at the start of this pass is `1b286483`. **It moved during the cycle** — the plan's pre-flight recorded `09003dc2`, and the concurrent spec-018 residual cycle has since committed `b5b2af81` and `1b286483`. Neither commit touches any path this cycle writes; see `### Working-tree audit`.

---

## Integration pass (Worker 1)

### Step-by-step record

**Step 1 — read every prior round artifact in order.** **Run.** One artifact exists: [`bld-review-1-spec019_rationale.md`][bld-r1], read end to end (242 lines), including its `### Dispatched findings checklist`, its `### Spec changes made (Worker 1 only)` table of 25 entries, and all seven items under its `### Notes for Worker 1 (spec reconciliation)`. Two of those notes did not survive re-derivation; see `### Findings`.

**Step 2 — confirm the static inspection helper ran, or was skipped with a reason, for every Python file with review-worthy logic the build touched.** **Skipped, with reason: the build touched no Python file.** The cycle's entire diff is three `.md` files (the spec, the new rationale companion, and one sentence in `CHANGELOG.md`) plus this artifact. `scripts/review_inspect.py` inspects Python source; a pass that writes none has no file to inspect. The plan's pre-flight step 2 recorded a smoke invocation against `django_strawberry_framework/types/definition.py` purely to prove the tool works, and that file is read-only to this cycle.

**Step 3 — compare the `Repeated string literals` sections across every shadow overview.** **Skipped, with reason: no shadow overview was generated by this build, because no slice produced Python code.** The stale overviews under `docs/shadow/` belong to prior cycles and to the concurrent spec-018 cycle; comparing them would describe someone else's diff, not this build's. Fabricating a population to satisfy the step is the failure mode the dispatch names explicitly.

**Step 4 — compare the `Imports` sections across every shadow overview for one-way dependency direction.** **Skipped, same reason as step 3.** No module was added, moved, or re-imported by this cycle, so no import direction changed and there is nothing to confirm one-way.

**Step 5 — walk every accepted artifact's `What looks solid` / `DRY findings` / `Notes for Worker 1` for deferred follow-up that should land here.** **Run.** R1's artifact carries no `## Review (Worker 3)` section (a documentation-only round with no builder, absent by design), so `What looks solid` and `DRY findings` do not exist in it; its `### Notes for Worker 1 (spec reconciliation)` is the whole carry-forward surface and all seven items were walked. Items 1, 2, 5, 6 and 7 were re-derived and confirmed. Items 3 and 4 did not survive re-derivation and are the two findings below. The corrected carry-forward list is under `### Notes for Worker 1 (spec reconciliation)`.

**Step 6 — sweep the whole tree for staged anchors naming this build's spec OR card.** **Run, and this is the one mechanical sweep with a real population to scan.** Both the current and the pre-renumber card number were swept, because this card shipped as `015` and the 2026-07-30 renumber did not rewrite in-tree anchors:

```shell
grep -rEn 'TODO\(spec-019|TODO-(ALPHA|BETA|STABLE)-019' .
grep -rEn 'TODO\(spec-015|TODO-(ALPHA|BETA|STABLE)-015' .
```

**Zero matches for either pattern, anywhere in the tree** — including `KANBAN.md` / `KANBAN.html` / `BACKLOG.md`, which the sweep does not need to exclude here because they carry no `019` or `015` anchor either (both numbers are Done cards, and the `TODO-<MILESTONE>-<NNN>` form legitimately names only unshipped board cards). No anchor belongs to a different card sharing the digits, because no anchor of either shape exists.

The sweep instrument was proved capable of hitting before its zero was accepted: the same grep shapes return 20+ `TODO(spec-NNN` anchors (`spec-029`, `spec-033`, `spec-035`, `spec-036`, `spec-038`, `spec-041`, `spec-050`) and 30+ distinct `TODO-<MILESTONE>-<NNN>` card ids (`TODO-BETA-053` at 56 occurrences down to `TODO-BETA-062` at 5). A zero from an instrument that never fires is not a measurement; this one fires.

**Step 7 — the pass's own check list** (duplicated helpers across slices; inconsistent naming or error handling; repeated ORM/queryset patterns; misplaced responsibilities; missing or too-broad exports; repeated literals / keys / tuple shapes; whether comments tell one coherent story). **Skipped, with reason: every item on this list names a property of Python source across two or more slices.** This cycle has one round and no source. The one item with a documentary analogue — whether the new prose tells one coherent story — is answered by step 8, which is the substantive work of this pass.

**Step 8 — coherence across the spec / rationale pair.** **Run.** See `### Pair coherence` below. This is the only pass that reads the two files as one unit, and it was re-derived from the files themselves rather than from R1's report.

**Step 9 — cross-document consistency for the corrected mechanism.** **Run.** See `### Retired-mechanism carrier sweep`.

### Pair coherence

Re-derived as a fresh reader of R1's output, not verified against its claims.

**Every rationale entry resolves to a spec decision that exists.** The rationale's `## Entries keyed to the spec` carries seven Decision-keyed headings, each written as a reference-style link into the spec's own in-page anchor (ref-ids `spec-019-d1` through `spec-019-d7a`), plus seven headings keyed to named spec sections by title (`## Current state`, `## Implementation plan`, three `## Slice checklist` entries, `## Test strategy`) and three keyed to surfaces outside the spec (the `Status:` line, the verbatim KANBAN body, the `CHANGELOG.md` entry). All seven anchor targets were resolved mechanically against the spec's rendered heading slugs: **0 broken.** The spec's own in-page anchors were checked the same way: **0 broken**, with one benign exception recorded below. The rationale's own in-page anchors: **0 broken.**

The benign exception: the spec contains two `](#scalar-field-override-semantics)` uses, at the Slice 5 GLOSSARY sub-bullet and in the `### Decision 5`-adjacent prose quoting the sentence Slice 5 removes. Both sit **inside quoted `docs/GLOSSARY.md` text** — the anchor belongs to GLOSSARY's own page, and quoting it verbatim is what the sub-bullet is for. Correct in context; not link rot, and not to be "fixed".

Decision 4 (`_build_annotations` body stays unchanged) has no rationale entry. Correct: nothing was moved out of it and it rejected no alternative, so an entry would name no moved deliberation.

**No contract statement was moved out of the spec and lost.** The rationale's `**Kept in the spec deliberately**` list names four passages that exercised [`worker-1.md`][worker-1]'s implementation-relevant-rationale carve-out (Decision 7's guard-placement conclusion, its GraphQL-field-name-not-pk-name prohibition, Decision 7a's enumerated bypass consequences, Decision 5's placement rule); all four were confirmed present in the spec. Read the other way, the rationale carries no normative `must` / `must not` statement that the spec does not also carry — every prohibition in it is written as a **rejected** alternative or a **retired** claim, both of which are historical by construction.

**No deliberation was left behind in the spec.** The dispatched residue grep re-run over the spec returns **0** for `rev[0-9]`, `Rev[0-9]`, `Revision [0-9]`, `Worker 1 picks`, `Worker 1 may`, `option (a)`, `post-rev`, `H1:` / `M1:` / `L1:`. An independent grep for deliberation vocabulary (`rejected|alternative|we considered|previously|no longer permitted|retract`) returns 14 lines, every one of which is either normative (the code *rejects* a shape; the error message names *alternatives*) or one of the two required one-line pointers at the rationale companion (header `Deliberation:` line, and the `## Architectural decisions` preamble). No chronology survives.

**The two files never disagree about what shipped.** Each of the ten rows in the rationale's `## Reconciliation record` table was checked against the spec's current text: all ten "Is" columns match what the spec now says. The one disagreement this pass found was between the rationale and the **working tree**, not between the two files — finding 1 below.

### Retired-mechanism carrier sweep

`get_type_hints`, `fail-soft` / `fail soft`, and `NameError` were swept across the whole tree (`.git` and `.venv` excluded) for surviving descriptions of the mechanism commit `2bcd7f96` retired from `django_strawberry_framework/types/base.py::_id_annotation_is_relay_node_id`.

**(a) Carriers this cycle may fix — one, and it is fixed in this pass.**

- `docs/SPECS/appx/spec-019-consumer_overrides_scalar-0_0_6-rationale.md` — two false or understated descriptions of R1's own findings. Corrected as custodian; see `### Findings` and `### Spec changes made (Worker 1 only)`.

**(b) Out of scope — generated, concurrently-owned, or code.**

- `tests/types/test_definition_order.py::test_consumer_id_resolved_relay_nodeid_with_unresolved_sibling_annotation_is_accepted` #`"the fail-soft annotation walk accepts the"` — inline comment using the retired vocabulary. The behavior it describes is correct; only the word is retired. This cycle writes no code, and the file is baseline-dirty under a concurrent session.
- `tests/types/test_definition_order.py::test_consumer_id_unresolved_non_nodeid_string_on_relay_node_type_raises` #`"raises via the fail-soft regex reject"` — docstring, same class, **on a third test R1 did not enumerate**. Same disposition.
- `tests/types/test_definition_order.py::test_consumer_id_unresolved_nodeid_shaped_string_on_relay_node_type_passes_guard_only` and `tests/types/test_definition_order.py::test_consumer_id_resolved_relay_nodeid_with_unresolved_sibling_annotation_is_accepted` — the two test **names** drawn from the retired "fail-soft sub-case 1 / 2" vocabulary. Recorded in the spec as the landed spelling; deliberately not renamed.
- `docs/builder/build-019-consumer_overrides_scalar-0_0_6.md` (finding 1's text) and `docs/builder/bld-review-1-spec019_rationale.md` — Worker 0's plan and R1's artifact. Both describe the retired mechanism **as retired**, which is their job; the plan is Worker 0's file and the round artifact closes with the cycle.

**(c) Correct in their own context — no action.**

- `django_strawberry_framework/types/base.py::_id_annotation_is_relay_node_id` #`"no ``typing.get_type_hints``"` and #`"handles nested forward references"` — the docstring names `get_type_hints` to say the function does **not** call it, and why. This is the authoritative statement of the landed mechanism.
- `docs/SPECS/spec-019-consumer_overrides_scalar-0_0_6.md` lines reproducing that docstring inside `### Decision 7` — byte-faithful to the source, therefore correct.
- `docs/SPECS/appx/spec-019-consumer_overrides_scalar-0_0_6-rationale.md`'s historical entries (`### The relay.NodeID detection mechanism was rewritten…`, the rev4-rev11 entries, `**Retired vocabulary**`) — a record of a retired mechanism is exactly what a rationale companion is for.
- `django_strawberry_framework/optimizer/hints.py`, `django_strawberry_framework/optimizer/nested_fetch.py`, `django_strawberry_framework/optimizer/nested_planner.py`, `django_strawberry_framework/filters/sets.py`, `tests/optimizer/test_hints.py`, `tests/optimizer/test_nested_index_advisory.py`, `docs/SPECS/spec-027-filters-0_0_8.md`, `docs/SPECS/spec-028-orders-0_0_8.md`, `docs/SPECS/spec-020-list_field-0_0_7.md`, `docs/dry/*` — unrelated uses. `get_type_hints` in the optimizer describes Strawberry's own resolution of `OptimizerHint`; `fail-soft` in the nested-index advisory is a live, current contract of a different subsystem; `NameError` appears in lint and closure discussions. **None is near the Relay guard.** Named here so a future sweep does not re-flag them.

**No other standing doc carries the retired mechanism.** `docs/GLOSSARY.md`, `docs/README.md`, `TODAY.md`, `README.md`, `BACKLOG.md`, `docs/TREE.md` and `CHANGELOG.md` all return zero for `get_type_hints` and `fail-soft`. R1's confirmation that `docs/GLOSSARY.md`'s `Scalar field override semantics` entry names no mechanism is independently confirmed, and its `CHANGELOG.md` correction is confirmed landed (see `### Working-tree audit`).

### Findings

Two, both defects in R1's own output, both correctable in this pass without a re-loop: neither changes a contract, neither touches code, and both live in the one file this pass is authorized to correct as custodian.

**Finding 1 (the load-bearing one) — `KANBAN.md`'s live `DONE-019-0.0.6` body does NOT describe the retired mechanism. R1's claim that it does is false, and it homed a deferred item that has nothing behind it.**

R1 recorded, in its `### Notes for Worker 1` item 4 and at two sites in the rationale companion, that the live card body "still describes the retired mechanism", that "the two copies now disagree", and that the fix is a DB edit plus a regenerate deferred to a future cycle. Re-derived independently, at four levels:

1. `grep -c 'get_type_hints' KANBAN.md` → **0**; `grep -ci 'fail-soft' KANBAN.md` → **0**.
2. Same two counts against `KANBAN.html` → **0** and **0**.
3. Every `TEXT` / `VARCHAR` column of every table in `examples/fakeshop/db.sqlite3`, queried read-only (`file:…?mode=ro`, no write, no schema touch) for `%get_type_hints%` or `%fail-soft%` → **0 rows**, whole database.
4. The card's own rows in `kanban_carditem` for `kanban_card.number = 19`, read in full: fifteen items, each a **single line**. The Relay item is exactly `**`relay.Node` `id` collision rejected at type-creation time.** A consumer` — and stops there.

The mechanism sentences the spec's Slice 5 drop-in carries live on the **continuation lines** of that bullet, which the kanban DB import never carried. The live card names no detection mechanism at all, so `2bcd7f96` falsified nothing in it. Corrected in the rationale companion at both sites, with the measurement recorded so the negative is re-derivable.

Why this matters beyond one sentence: the final gate builds its `### Deferred work catalog` by walking this artifact and R1's, so an un-caught false claim here would have handed the maintainer a DB-edit-plus-regenerate task — the most hazardous class of task in this repo while a concurrent session holds the DB open — for a defect that does not exist.

**Finding 2 — the retired-vocabulary population in `tests/types/test_definition_order.py` was understated: four occurrences across three tests, not three across two.**

R1 enumerated two test names plus one inline comment inside the second of them. The sweep also finds the **docstring** of a third, separate test, `tests/types/test_definition_order.py::test_consumer_id_unresolved_non_nodeid_string_on_relay_node_type_raises` #`"raises via the fail-soft regex reject"`. That test is one of the eleven Relay tests, pins a current correct contract (a non-NodeID string is rejected by `_NODEID_STRING_RE`), and carries retired vocabulary exactly like the other two. The spec's own description of it at `## Test strategy` is clean and needed no edit; only the rationale's enumeration was short. Corrected, with the occurrence count stated as four and the file named as the unit a future rename would open — an undercount is what makes a future "rename them all" pass stop one occurrence early.

**No third finding.** R1's remaining five carry-forward notes were re-derived and hold: finding 9's sweep-population effect (`grep -c 'spec-013'` on this spec is **0** at the working tree, and `KANBAN.md`'s `[spec-011]` bullet still names spec-019 as a member, so the sweep card's population genuinely needs re-deriving); the `spec015_*` identifiers (present, unrenamed, recorded in the spec); the flattened guard control flow (fixed in the spec, recorded in the rationale); `docs/GLOSSARY.md` correct as written; and the `[015-…]` CHANGELOG tracking label left to the renumber-sweep cluster.

### Consolidation check

None recommended, and the [`worker-1.md`][worker-1] delete-before-extract rule had no candidate to apply to: this cycle introduced no constant, helper, module, or shared literal in any language. The one duplication risk the round did carry — the same measurement or contract sentence appearing in both the spec and the rationale — was checked directly in `### Pair coherence` and is absent. The rationale's byte table deliberately points at the round artifact rather than restating a count of a file still being written; that pointer is intact after this pass's edits, and remains correct.

### Working-tree audit

`git status --short` at the end of this pass: **104 paths**, `HEAD` `1b286483`.

**This cycle's own paths — four, all present and all this cycle's:**

- `docs/SPECS/spec-019-consumer_overrides_scalar-0_0_6.md` (` M`) — R1's rewrite. Unchanged by this pass; 104,017 bytes, matching R1's recorded figure exactly.
- `docs/SPECS/appx/spec-019-consumer_overrides_scalar-0_0_6-rationale.md` (`??`) — R1's new companion, plus this pass's three custodial corrections. 47,488 → **48,834** bytes.
- `CHANGELOG.md` (` M`) — `git diff HEAD -- CHANGELOG.md` is **exactly one bullet, one sentence-span replacement**, retiring the `typing.get_type_hints` fail-soft description in the `## [0.0.6]` `Added` Relay-guard entry. No other line in the file is touched. This is R1's one authorized correction, confirmed landed and confirmed minimal.
- `docs/builder/bld-019-integration.md` (`??`) — this artifact. `docs/builder/build-019-consumer_overrides_scalar-0_0_6.md` and `docs/builder/bld-review-1-spec019_rationale.md` are also untracked and are this cycle's.

**New and unexplained relative to the plan's baseline — reported, not reverted** ([`AGENTS.md`][agents] rule 34). `HEAD` advanced two commits during the cycle, both the concurrent spec-018 residual cycle's closeout (`b5b2af81` "docs(specs,builder): complete the spec-018 residual closeout cycle", `1b286483` "docs(kanban): home the spec-018 cycle's deferred board work on card 052"). Consequences visible in the tree, none of them this cycle's:

- Paths the plan listed as baseline-dirty are now **clean**, having been committed by that cycle: `docs/SPECS/spec-018-meta_primary-0_0_6.md`, `docs/SPECS/appx/spec-018-meta_primary-0_0_6-rationale.md`, `docs/GLOSSARY.md`, `docs/builder/bld-integration.md`, `docs/builder/bld-final.md`, `docs/builder/bld-review-1-spec018_rationale.md`.
- A concurrent session created `docs/builder/DONE/` and is moving closed build plans into it: `docs/builder/build-017-deferred_scalars-0_0_6.md` shows ` D` with `docs/builder/DONE/build-017-deferred_scalars-0_0_6.md` untracked. `docs/builder/bld-017-final.md` shows ` D` with **no** counterpart in `DONE/` — deleted outright, not moved.
- `docs/builder/bld-018-final.md` is **staged** for deletion (`D ` in the index) by a concurrent session.
- New dirty package-source and test paths not in the plan's list (`utils/errors.py`, `utils/inputs.py`, `utils/write_transaction.py`, `tests/utils/test_errors.py`, `tests/utils/test_inputs.py`, `tests/mutations/test_write_transaction.py`, `examples/fakeshop/test_query/test_products_visibility_api.py`, ~25 more untracked `docs/review/rev-*.md`) — a concurrent review cycle's in-flight work.

**Naming consequence, re-checked.** The plan chose the NNN-scoped spellings `bld-019-integration.md` / `bld-019-final.md` because the concurrent cycle owned the unscoped names. That cycle has since committed its artifacts as `bld-018-final.md` and no unscoped `bld-integration.md` / `bld-final.md` now exists on disk. **The scoped naming still stands** — it is what the plan's `## Artifact list` and `## Checklist` name, what R1's artifact points at, and what the final gate will look for; renaming now to claim a freed name would only break those pointers.

### Verification owed

| Check | Result |
|---|---|
| `uv run python scripts/check_spec_glossary.py --spec docs/SPECS/spec-019-consumer_overrides_scalar-0_0_6.md` | **exit 0** — `OK: 15 terms - all have glossary entries and at least one spec link.` Re-run after this pass's rationale edits; unchanged. |
| `uv run python scripts/check_trailing_commas.py --check` on the spec, the rationale companion, and both `bld-*` artifacts | **exit 0** — markdown link-def scaffold and all ten canonical group headers present and ordered in every file. |
| Anchor resolution, rationale → spec | **0 broken** of 7 reference-style anchor targets, resolved mechanically against the spec's heading slugs. |
| Anchor resolution, spec → spec and rationale → rationale | **0 broken**, with the two quoted-GLOSSARY anchors recorded above as correct in context. |
| Staged-anchor sweep, `019` and `015`, both anchor shapes | **0 matches**, instrument proved capable of hitting. |
| `git status --short` | 104 paths; this cycle's four accounted for above, everything else pre-existing baseline-dirty or the concurrent cycles' work. Reported, not reverted. |
| Focused tests | Not re-run by this pass. R1 recorded `286 passed` with `--no-cov` on `tests/types/test_definition_order.py tests/types/test_converters.py tests/types/test_base.py`, and this pass changed no code, no test, and no runtime-affecting file. |

No `pytest --cov*` flag was used anywhere in this pass.

### Spec changes made (Worker 1 only)

The active spec, `docs/SPECS/spec-019-consumer_overrides_scalar-0_0_6.md`, was **not** edited by this pass. Its `Status:` line was re-verified per the every-spawn obligation and is accurate: `shipped (0.0.6, 2026-05-19); archived. Card DONE-019-0.0.6.` — the build has falsified nothing in it, and its `Deliberation:` pointer at the rationale companion resolves.

Three custodial corrections were made to the rationale companion, `docs/SPECS/appx/spec-019-consumer_overrides_scalar-0_0_6-rationale.md`, all to R1's own output and all recording a re-derived measurement in place of a claim that did not survive re-derivation:

1. `### The relay.NodeID detection mechanism was rewritten after the card shipped`, the "Two landed test names describe the retired mechanism" bullet — **finding 2**. Rewritten to enumerate four occurrences across three tests, naming the third test's docstring, and to state that the file is the unit a future rename would open.
2. `### DONE-019-0.0.6 — the verbatim KANBAN body`, closing sentence — **finding 1**. The false "its Relay paragraph still describes the retired mechanism" claim replaced with the measured negative and the four levels of evidence, including why the live body is short (the DB carries only the first line of each drop-in bullet).
3. `### What this cycle deliberately did not fix`, two bullets — **findings 1 and 2**. The test-file bullet widened to the four-occurrence population; the `KANBAN.md` bullet removed from the deferred list and replaced with an explicit **Not deferred, because it does not exist** note, so a later reader does not re-open an item with nothing behind it.

Byte effect: the rationale companion moves from 47,488 to 48,834 bytes (+1,346). The spec is unchanged at 104,017. [`docs/builder/BUILD.md`][build]'s corpus ratchet does not bind either file — it binds `BUILD.md`, `ARTIFACT.md`, and the four `worker-*.md` role files, none of which this pass may edit or did edit.

R1's artifact, `docs/builder/bld-review-1-spec019_rationale.md`, is **not** in this pass's writable set and was not edited. Its `### Notes for Worker 1` items 3 and 4 stand on disk as written; findings 1 and 2 above are the correction of record, and the final gate should build its catalog from **this** artifact where the two disagree.

### Notes for Worker 1 (spec reconciliation)

Everything still deferred, corrected against this pass's re-derivation. The final gate's `### Deferred work catalog` is built by walking this list and R1's; where the two disagree, **this list supersedes**, and the two superseding items say so inline.

1. **The `spec015_*` synthetic identifiers in `tests/types/test_definition_order.py`.** Three `app_label`s (`"test_spec015_unsupported"`, `"test_spec015_grouped_choices"`, `"test_spec015_co_resident"`) and one stub-module prefix (`stub_name = f"spec015_unresolved_relay_stub_{uuid.uuid4().hex}"`). Pre-renumber card number baked into test-local synthetic strings with no cross-file consumer. Target: maintainer follow-up, or any future card that opens that file for another reason. Licensed by the plan's finding 10 ("do not rename") and by the spec's own `## Test strategy`, which records the spelling as landed rather than as a recipe. Renaming has no correctness payoff and a real collision risk against the concurrent session's dirty copy of the file.

2. **The retired fail-soft vocabulary in `tests/types/test_definition_order.py` — four occurrences across three tests.** *(Supersedes R1's note 3, which counted three across two.)* Two test names — `::test_consumer_id_unresolved_nodeid_shaped_string_on_relay_node_type_passes_guard_only` and `::test_consumer_id_resolved_relay_nodeid_with_unresolved_sibling_annotation_is_accepted`, named for fail-soft sub-cases 1 and 2; one inline comment inside the second, #`"the fail-soft annotation walk accepts the"`; and one docstring on a third test, `::test_consumer_id_unresolved_non_nodeid_string_on_relay_node_type_raises` #`"raises via the fail-soft regex reject"`. All three tests pin current, correct contracts; only the vocabulary is retired, by commit `2bcd7f96`. Target: the same future card that opens the file. A rename pass that carries the old count of three will stop one occurrence short.

3. **`KANBAN.md`'s live `DONE-019-0.0.6` body — NOT deferred; the item does not exist.** *(Supersedes R1's note 4.)* The live card, the HTML board, and the kanban DB all carry **zero** occurrences of `get_type_hints` and `fail-soft`; the card's Relay item is a single line naming no mechanism, because the DB holds only the first line of each drop-in bullet. No DB edit and no `scripts/build_kanban_md.py` regenerate is owed by this card. Recorded explicitly, and not merely dropped, so the negative is durable: a later reader who finds R1's note without this correction would re-open it.

4. **`KANBAN.md`'s `[spec-011]` renumber-sweep bullet needs its population re-derived; spec-019 has left it.** The bullet (under `TODO-ALPHA-052-0.1.0`) names `docs/SPECS/spec-019-consumer_overrides_scalar-0_0_6.md` as carrying the pre-renumber filenames `spec-013-deferred_scalars` and `spec-014-meta_primary` in its Slice 4 Prior-`0.0.6`-card note. R1 retired both, on the precedent the same bullet records for spec-018. Re-derived this pass: `grep -c 'spec-013'` on the spec is **0** at the working tree. The bullet already warns against re-adding spec-018 from an older reading; the same caution now applies to spec-019. Target: card `TODO-ALPHA-052-0.1.0`, whose owner should re-derive rather than carry the reading forward. DB-backed, so the fix is a DB edit plus a regenerate — genuinely deferred, unlike item 3.

5. **`CHANGELOG.md`'s `[015-consumer_override_semantics_scalar_fields-0.0.6]` tracking label.** A pre-renumber label in prose for a card that is `DONE-019-0.0.6`; its link definition resolves correctly, so the label alone is the artifact. Deliberately left alone: it belongs to the `[spec-011]` / `[spec-013]` renumber cluster `KANBAN.md` tracks, and half-fixing a cluster leaves it divergently rather than uniformly wrong. Target: the same sweep card, `TODO-ALPHA-052-0.1.0`.

6. **No code gap, and none opened by this pass.** R1's independent per-name walk found all 19 Slice-1 tests present at their mandated placements, Slice 2's deletion done, Slice 3's docstring live, Slice 4 long past, and Slice 5's docs half holding end to end. This pass re-derived the parts of that walk that its own sweeps touched — the four `types/base.py` helpers, the three tests named above, the `CHANGELOG.md` and `docs/GLOSSARY.md` states — and confirmed them. **No code round is needed.**

### Summary

The integration pass ran the three steps that had a population and recorded the four that did not, each with the reason no population exists: this cycle wrote three `.md` files and no Python, so the shadow-overview literal and import comparisons, the static-inspection confirmation, and the cross-slice source check list have nothing to scan.

- **Staged-anchor sweep: clean.** Zero `TODO(spec-019` / `TODO-<MILESTONE>-019` and zero `TODO(spec-015` / `TODO-<MILESTONE>-015` anchors anywhere in the tree, from an instrument proved capable of hitting on 20+ other spec anchors and 30+ other card ids.
- **Pair coherence: clean.** All seven rationale→spec Decision anchors resolve; no contract statement was lost out of the spec; no chronology was left in it; all ten rows of the rationale's reconciliation table match the spec's current text.
- **Two findings, both in R1's own output, both corrected here as custodian.** The load-bearing one is a **false** deferred item: `KANBAN.md`'s live card body does not describe the retired mechanism — measured 0 occurrences in the rendered board, the HTML, and every text column of the kanban DB — so no DB edit is owed. The second is an **understated** population: the retired fail-soft vocabulary survives at four places across three tests in one file, not three across two.
- **Working tree: accounted for.** `HEAD` advanced two commits mid-cycle (the concurrent spec-018 closeout); this cycle's four paths are intact and its `CHANGELOG.md` diff is exactly the one authorized sentence. Everything else is reported, not reverted.

Neither finding changes a contract, touches code, or needs a build re-loop.

Final status: **`final-accepted`**.

<!-- LINK DEFINITIONS -->

<!-- Root -->

[agents]: ../../AGENTS.md

<!-- docs/ -->

<!-- docs/SPECS/ -->

[spec-019-rationale]: ../SPECS/appx/spec-019-consumer_overrides_scalar-0_0_6-rationale.md
[spec-019]: ../SPECS/spec-019-consumer_overrides_scalar-0_0_6.md

<!-- docs/builder/ -->

[bld-r1]: bld-review-1-spec019_rationale.md
[build-019]: build-019-consumer_overrides_scalar-0_0_6.md
[build]: BUILD.md
[worker-1]: worker-1.md

<!-- django_strawberry_framework/ -->

<!-- tests/ -->

<!-- examples/ -->

<!-- scripts/ -->

<!-- .venv/ -->

<!-- External -->
