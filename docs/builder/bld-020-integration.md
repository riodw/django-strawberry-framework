# Build: cross-round integration pass — spec-020 (`DjangoListField`) residual closeout

Spec reference: `docs/SPECS/spec-020-list_field-0_0_7.md` (whole file; residual closeout, not a slice)
Rationale companion: `docs/SPECS/appx/spec-020-list_field-0_0_7-rationale.md`
Build plan: `docs/builder/build-020-list_field-0_0_7.md`
Rounds integrated: `docs/builder/bld-review-1-spec020_reconciliation.md` (`final-accepted`), `docs/builder/bld-review-2-docs_completion.md` (`final-accepted`)
Status: final-accepted

This pass and the final test-run gate run in one Worker 1 spawn, per `docs/builder/build-020-list_field-0_0_7.md` `## Integration pass and final gate run in one Worker 1 spawn`. This artifact was written and closed before `docs/builder/bld-020-final.md` was opened; the gate remains the backstop confirming this pass happened, not a second owner of it.

## Spec status-line re-verification (`docs/builder/worker-1.md`)

Read at the top of this spawn. `docs/SPECS/spec-020-list_field-0_0_7.md` lines 1-10: "Target release: `0.0.7`." / "Status: shipped (`0.0.7`, 2026-05-27); archived. Card `DONE-020-0.0.7`." / "Owner: package maintainer." plus the rationale-companion pointer and the pre-renumber provenance note. All accurate at HEAD; nothing this cycle falsifies them. **No spec edit is owed and none was made** — see `### Spec changes made (Worker 1 only)`.

## The six required pre-writing steps, and how each was discharged

`docs/builder/BUILD.md` `## Cross-slice integration pass` lists six steps before the artifact may be written. Three of them presuppose a Python diff; **this cycle's whole diff is documentation — no source file and no test file was modified by any pass** — so those three are discharged by recording that rather than by leaving them unaddressed.

| Step | Disposition |
|---|---|
| 1. Read every prior round artifact in order | **Done, both in full**, not "as needed": `bld-review-1-spec020_reconciliation.md` (819 lines, four passes + final verification) then `bld-review-2-docs_completion.md` (635 lines, plan + build + two Worker 3 legs + final verification). Both are `final-accepted`. |
| 2. Confirm `scripts/review_inspect.py` ran or was explicitly skipped for every `.py` file with review-worthy logic the cycle touched | **Recorded skip: the cycle touched no `.py` file at all.** Re-derived rather than accepted from the rounds: `git diff HEAD --name-only` restricted to this cycle's ownership returns `README.md`, `docs/GLOSSARY.md`, `docs/SPECS/spec-020-list_field-0_0_7.md`, `docs/SPECS/appx/spec-020-list_field-0_0_7-rationale.md` and `examples/fakeshop/db.sqlite3` — zero `.py` paths. The population of files the helper could run on is empty, so the skip is structural and not a sampling gap (`docs/builder/BUILD.md` `### When to run the helper during build`, last paragraph). Both rounds recorded the same skip with the same reason. |
| 3. Compare **Repeated string literals** across every shadow overview | **No comparison exists to make.** The section is emitted per `.py` file by `scripts/review_inspect.py`; with no `.py` in the diff no overview was generated by any pass this cycle, and `docs/shadow/` was cleared at pre-flight. Recorded rather than skipped silently: a cross-file literal is a defect shape a documentation diff cannot carry. |
| 4. Compare **Imports** across every shadow overview for dependency direction | **Same — no overview to compare.** No import statement was added, removed, or moved by this cycle. The one import-graph *claim* the cycle makes (F9: `list_field.py::_validate_relay_djangotype_target` imported by `connection.py` and `relay.py`) was verified at source by R1's builder, its reviewer, and its final verification, and is a statement about shipped code rather than a change to it. |
| 5. Walk both artifacts' `What looks solid` and `Notes for Worker 1` for deferred follow-up that should land here | **Done; the walk's output is `### Deferred follow-up found in the round artifacts` below.** Nothing in it can land in this pass — every item either spans a `.py` surface a no-source cycle cannot own, or belongs to a file whose data is replaced wholesale by a management command. All route to `bld-020-final.md`'s `### Deferred work catalog`. |
| 6. Staged-anchor sweep | **Run here, re-derived rather than cited from the plan.** Command and output below. |

### Step 6 — the staged-anchor sweep, re-derived

```shell
grep -rEn --binary-files=without-match 'TODO\(spec-020|TODO-(ALPHA|BETA|STABLE)-020' . \
  --exclude=KANBAN.md --exclude=KANBAN.html --exclude=BACKLOG.md \
  --exclude-dir=shadow --exclude-dir=.git --exclude-dir=.venv
```

**Four occurrences, none a staged anchor in shipped source or tests.** Verdict: clean; nothing to route back to an owning round.

| Occurrence | Reading |
|---|---|
| `docs/SPECS/appx/spec-017-deferred_scalars-0_0_6-rationale.md:87` | Prose. Rev-9 history of the rejected `TODO-ALPHA-020`-through-`044` renumber. Legitimate; a rationale file's job. |
| `docs/SPECS/appx/spec-017-deferred_scalars-0_0_6-rationale.md:239` | Prose. The same rejected renumber, recorded as the rejected alternative. Legitimate. |
| `docs/SPECS/appx/spec-020-list_field-0_0_7-rationale.md:506` | Prose, **written by R1 this cycle**: the `## Verified against the shipped code` row that records the sweep's own result. Self-referential, not an anchor. |
| `docs/builder/bld-review-2-docs_completion.md:584` | Prose, **written by R2 this cycle**: the round's "staged-anchor sweep not applicable" declaration. Self-referential, not an anchor. |

**Correction to a figure in the build plan, recorded rather than worked around.** `docs/builder/build-020-list_field-0_0_7.md` `### Code-completeness verification` states the sweep "returns only two prose mentions inside `docs/SPECS/appx/spec-017-deferred_scalars-0_0_6-rationale.md`". That was true when Worker 0 ran it at plan time and is **no longer true**: the population is now four, because two of this cycle's own artifacts describe the sweep in prose and therefore match its pattern. The conclusion is unchanged and the two new matches are this cycle's own writing — but the number as stated has been falsified by the cycle that stated it, which is the `## Claims are proven mechanically, never accepted on prose` failure mode in its purest form: **a sweep whose vocabulary appears in the prose that records the sweep counts its own record.** Worker 0 owns the plan file, so this is reported here rather than edited there.

Independently re-confirmed, because a sweep on the card number cannot see it: `grep -n TODO` over the six files the spec's Slice-3 sub-checks name returns exactly one hit, `TODO(spec-035)` in `examples/fakeshop/test_query/test_library_api.py`, which belongs to another card.

## The cross-round question: is anything now stated in more than one place across the finished set?

This is the pass's own work and on this cycle it is real rather than pro forma. R1 found and collapsed a five-fold restatement of the row-bound contract **inside its own diff**; R2's Worker 3 asked the reverse question **inside its own round** and found no deletion candidate. Neither looked across the pair at the finished set, and the two rounds wrote to different surfaces by construction — R1 to the spec and rationale, R2 to `README.md` and the generated glossary — so a divergence between them is exactly the class no per-round pass could see.

Surfaces compared, all six: `docs/SPECS/spec-020-list_field-0_0_7.md`, `docs/SPECS/appx/spec-020-list_field-0_0_7-rationale.md`, `docs/GLOSSARY.md` (`## \`DjangoListField\``), `README.md` (`## Status`), plus the two the cycle deliberately did not touch — `docs/README.md` and `django_strawberry_framework/list_field.py`'s docstrings and comments.

### The existence challenge, asked first

`docs/builder/BUILD.md` `!!IMPORTANT — DRY FIRST!!` puts deletion in scope, and notes the largest DRY win in this repo's history was a deletion. **Asked of every contract this cycle wrote, and answered: no deletion candidate.** The reasoning is not "each surface has a different audience" asserted as a slogan — it is that each surviving copy is reached by a reader the others cannot reach:

- `django_strawberry_framework/list_field.py`'s docstring is a source-local invariant a maintainer reads while editing the factory. Deleting it makes the next editor re-derive the contract from tests.
- `docs/SPECS/spec-020-list_field-0_0_7.md` is an **archived** design record. It is not a doc a consumer reads and is not trimmable as consumer-facing duplication; trimming it would make the archive incomplete for the one reader it exists for.
- `docs/GLOSSARY.md` is the **generated, live, consumer-facing** catalog and is the only one of the three a consumer reaches.

The one place where the challenge had teeth is the row bound, and R1 already resolved it by **decision (a)**: the spec keeps the field-facing surface and defers the policy-composition contract to the glossary entry by pointer. Re-verified here as a cross-round question neither round could close, because R1 verified the deferral target **before** R2 edited that entry: `docs/GLOSSARY.md` `## \`DjangoListField\`` -> `**Row bound (\`0.0.14\`, spec-047).**` still carries all three deferred clauses (`max_list_rows` supplied unconditionally, `max_rows=` narrows, `trusted_max_rows=True` is the only widening, plus the "no unbounded spelling" gloss). **R2's edit left it byte-identical, so (a) still leaves no clause homeless after R2.** The spec's `max_list_rows` occurrence count is **1** and its `trusted_max_rows=True` prose count is **1**, unchanged from R1's close.

Cross-file fragment re-resolved (the N1 lesson: a path that exists is not a link that resolves): the spec's and rationale's `[glossary-djangolistfield]: …GLOSSARY.md#djangolistfield` defs resolve against a heading that survives R2's regenerate — `## \`DjangoListField\`` at `docs/GLOSSARY.md` line 652.

### Finding I1 (cross-round, Low) — the retired `is_async_callable` abbreviation survives on two surfaces R1 could not reach

**This is the pass's one substantive cross-round finding, and it is the finding shape the pass exists for: R1 removed a defective characterisation from six spec sites, R2 edited the same entry on another surface, and neither round was positioned to see that the surface R2 edited still carries the defect.**

R1's M2 established the contract: `django_strawberry_framework/utils/typing.py::is_async_callable` is not a three-shape, one-hop predicate. `::_callable_inspection_target` peels `functools.partial` **and** `staticmethod` in a `while` loop, so the predicate also sees a raw `staticmethod` descriptor and arbitrary nestings of the two — its docstring names the raw-descriptor case as its third motivating shape, and `tests/test_list_field.py::test_djangolistfield_async_staticmethod_resolver_gets_get_queryset_applied` pins it. R1 rewrote all six spec sites and measured `one-hop` in the spec from 3 to **0**.

Measured across the six surfaces in this pass (occurrences, not matching lines):

| Surface | `one-hop` | `staticmethod` | Reading |
|---|---|---|---|
| `docs/SPECS/spec-020-list_field-0_0_7.md` | 0 | 11 | fixed by R1 |
| `docs/SPECS/appx/spec-020-list_field-0_0_7-rationale.md` | 0 | 8 | fixed by R1 |
| `README.md` | 0 | 0 | the `0.0.7` bullet states no predicate; correct for its register |
| `docs/README.md` | 0 | 0 | states no predicate |
| **`docs/GLOSSARY.md`** | **1** | **0** | **stale** |
| **`django_strawberry_framework/list_field.py`** | 0 | 0 | **stale** — the propagation vector itself |

The two stale sites:

1. `docs/GLOSSARY.md` `## \`DjangoListField\``, opening paragraph: async consumer resolvers "are detected at construction time via the partial-aware `is_async_callable` predicate (checked on the resolver, on its `__call__` so callable-instance resolvers with `async def __call__` are covered, and **through a one-hop `functools.partial`**)". Closed at three shapes; omits the `staticmethod` descriptor; and `one-hop` is the qualifier R1 removed from the spec because the peel is a loop, not a hop. It is the file's **only** surviving `one-hop`.
2. `django_strawberry_framework/list_field.py` #"``__call__``/``functools.partial``-aware superset of" — the inline comment above the consumer-wrapper branch. R1's M2 root-cause note named this comment as the vector that propagated the abbreviation into the spec three times. It was never in R1's ownership and is still there.

**Disposition: LEAVE and catalog. Not a builder dispatch, and this pass does not fix it.** The reasoning is the one R2's final verification applied to its own two Lows, and it applies identically here: one of the two sites is a `.py` comment, **this cycle is declared no-source-and-no-test**, so no round in it can execute the whole-population fix; and `worker-0.md`'s no-partial-fix rule forbids correcting one surface of a multi-surface reference while the others diverge — correcting the glossary alone would make the generated consumer catalog disagree with the source comment a maintainer reads while editing the very branch it describes.

Severity Low, not Medium: neither site states a false count (R1's M2 was Medium precisely because `## Edge cases and constraints` asserted "all three async spellings"), the enumeration is a true list of shapes that *are* checked, and the authority both sites name — `is_async_callable` — is correct and its own docstring is complete. The defect is understatement, and the reader most likely to be misled is a maintainer considering a "harmonization", which the spec now guards against explicitly.

### Finding I2 (cross-round, record-only) — the deferred ordering bundle's stated population is three files but five spec sites

R2's final verification routed the `orderBy` recourse and the pk-tiebreaker comparative to the catalog as "one three-surface bundle (spec Boundary line, `::DjangoListField` docstring, glossary `**Ordering.**`)". The **file** count is right. The **site** count inside the spec is not one:

| File | Sites |
|---|---|
| `docs/SPECS/spec-020-list_field-0_0_7.md` | `## Non-goals` #"adds no order tiebreaker"; `## User-facing API` #"**No order guarantee.**" (carries the `orderBy` recourse); `### Decision 8` boundary bullet #"**and no order guarantee**" (carries it again); the sibling `DjangoConnectionField` bullet #"**and a pk tiebreaker appended"** (the unqualified comparative); the asymmetry-is-deliberate bullet. **Five.** `orderBy` occurrences in the spec: **3.** |
| `django_strawberry_framework/list_field.py::DjangoListField` docstring | one passage carrying both halves |
| `docs/GLOSSARY.md` `## \`DjangoListField\`` -> `**Ordering.**` | one paragraph carrying both halves |

Recorded rather than held, and corrected in the catalog entry: a future cycle taking the bundle whole needs the site list, not the file list, or it will fix `### Decision 8` and leave the `## User-facing API` bullet saying the same thing. This is the cycle's own dominant defect class — a stated population that samples its vocabulary — caught one level out, and it costs nothing here because the disposition (LEAVE, take whole or not at all) is unchanged.

### The rest of the cross-round scan — clean

- **The async-iterable arm** is stated in the spec (`### Decision 2` "Three consumer-resolver arms, not two", `## User-facing API`, `## Slice checklist` Slice 1, DoD 1 and 11) and in the glossary (`**Async-iterable resolvers.**`). Compared sentence-by-sentence: **no near-verbatim overlap.** The spec states it as a lettered (a)/(b)/(c) construction-time dispatch with symbol-qualified paths and the `_callable_inspection_target` nesting note; the glossary states the consumer-facing contract in the entry's compressed voice with no symbol path and no test citation. Two registers, not two copies. Confirms R2's within-round answer with independent evidence.
- **The ordering contract's phrasing** between the spec's `## User-facing API` bullet and the glossary's `**Ordering.**` paragraph *is* close — the same "no tiebreaker / database-dependent / unless `orderBy` or `Meta.ordering`" clause set, resequenced. It is not a deletion candidate for the reason the existence challenge gives, and within the spec it is already at the clause-plus-pointer shape R1's D1 fix produced (the `## User-facing API` bullet is one line ending "see [Decision 8]"). No action.
- **`README.md`'s new `0.0.7` bullet against `docs/README.md`'s `DjangoListField` bullet.** Re-checked at the cross-round level rather than accepting R2's within-round answer. Two clauses overlap (the `_default_manager.all()` default resolver, sync/async `get_queryset` cooperation); the overlap is the list's own pre-existing convention — `README.md`'s `0.0.10` bullet overlaps `docs/README.md` by the same measure — and the new bullet carries neither the row bound nor the async-detection rule nor anything from the "Newest shipped" block above it. No new duplication introduced by this cycle.
- **Naming and error-handling consistency between the rounds.** `SyncMisuseError` is now the spelling on every surface this cycle wrote: the spec at every site including `## Goals` item 3 (R1), and the glossary's opening paragraph and `**Async-iterable resolvers.**` paragraph (R2). The looser `ConfigurationError` wording R1 flagged for R2 is gone from the `djangolistfield` entry's rejection clause; the one surviving `ConfigurationError` link in that entry is the **Row bound** paragraph's construction-time `max_rows` error, which is correct and is a different raise. **The two rounds tell one coherent story.**
- **Repeated ORM/queryset patterns, misplaced responsibilities, exports.** Not applicable — no executable line was added, moved, or removed by either round. `git diff HEAD -- django_strawberry_framework/__init__.py` is empty; `__all__` and the re-export list are unchanged, consistent with DoD 19.

### Deferred follow-up found in the round artifacts (step 5's output)

Walked both rounds' `What looks solid`, `Notes for Worker 1 (spec reconciliation)`, `Things I verified and found wrong`, and both `Spec changes made (Worker 1 only)` sections. Everything deferred is enumerated here and **all of it routes to `bld-020-final.md`'s `### Deferred work catalog`; none can land in this pass.**

| Item | Source | Why it cannot land here |
|---|---|---|
| `CHANGELOG.md`'s `0.0.7` section labels every card by its pre-renumber number | build plan `### Escalated to the maintainer`; re-confirmed untouched by both rounds' CHANGELOG-sanity sections | Multi-surface reference cluster escalated by Worker 0, never dispatched. No-partial-fix rule. |
| `KANBAN.md`'s `DONE-020-0.0.7` card lists `apps.py` under `#### Package files` | same | The card-files data is DB-backed and replaced wholesale by `manage.py import_card_files`. |
| The `orderBy` recourse + pk-tiebreaker comparative bundle | R2 Worker 3 Lows 1-2; R2 final verification dispositions 1-2 | Spans a `.py` docstring. Population corrected by I2 above. |
| `docs/GLOSSARY.md` `## \`SyncMisuseError\``'s raising-surface list omits `DjangoListField` | R2 plan note 2; R2 Worker 3 note 3; R2 final verification disposition 3 | Widening it redefines another term's scope; that entry's `**Status:** shipped (\`0.0.5\`)` belongs to another card. |
| The `is_async_callable` abbreviation on two surfaces | **new here — I1** | Spans a `.py` comment. |
| Both baseline gate exceptions | build plan preamble; re-confirmed by both rounds | Neither is repository content this cycle may touch. |
| Build-plan wording fix for the `build_tree_md.py --check` expectation | R2 build report `### Notes for Worker 1`; R2 final verification item 3 | The build plan is Worker 0's file. Already folded in by Worker 0 as "Baseline gate exception 2". |

**Closed rather than carried, and deliberately not re-opened here:** the Slice 5 `KANBAN.md #"## Done"` citation (R2's final verification closed it — the claim is true as written and its only consumer, F13, has landed); the build plan's `### Verified NOT a finding` list; every alternative the rationale records as rejected; and the artifact-record defects both rounds already adjudicated in place (R1's N2 count corrections, R2's `converters.py` label inversion and the `iterdump()` line-count figure).

## Consolidation dispatch

**None required. No Worker 2 consolidation pass and no Worker 3 review pass is requested.** The cross-round scan produced one Low finding (I1) and one record correction (I2), and neither is work a builder can do: I1's population includes a `.py` comment on a cycle declared no-source, and I2 is a correction to a catalog entry this pass writes. `docs/builder/BUILD.md` `## Cross-slice integration pass` dispatches a consolidation loop only when DRY opportunities are found; the existence challenge and the duplication scan both come back with no deletion candidate and no live cross-surface duplication introduced by this cycle.

Per `worker-1.md` `## Integration pass`: before recommending any consolidation, the candidate's readers are grepped so a dead-code deletion is not mistaken for a live duplication. Not exercised — there is no code candidate in this cycle, live or dead.

## Spec changes made (Worker 1 only)

**None.** Neither `docs/SPECS/spec-020-list_field-0_0_7.md` nor `docs/SPECS/appx/spec-020-list_field-0_0_7-rationale.md` was edited by this pass; both are byte-unchanged at the sizes R1's final verification recorded — **100,566** and **109,687** (`wc -c`, re-measured in this pass).

No custodial correction is owed. I1's two stale sites are not in either file (the spec is the surface R1 *fixed*); I2 is a correction to a catalog entry, not to a spec sentence; and the status/header lines are accurate at HEAD. Editing the spec to narrate a defect that lives on other surfaces would be exactly the changelog-inside-the-contract shape `docs/builder/BUILD.md` `## Spec rationale extraction` forbids.

## Integration pass disposition

`Status: final-accepted`.

All six required pre-writing steps are discharged with their dispositions recorded, including the three that a documentation-only diff makes vacuous — recorded as vacuous rather than left silent. The staged-anchor sweep is clean and its command and full output are above, with one falsified plan figure reported. The cross-round DRY scan found no deletion candidate and no live duplication introduced across the round boundary, confirmed the row-bound decision (a) still holds **after** R2's edit to its deferral target, and surfaced one genuine cross-round divergence (I1) plus one population correction (I2), both routed to the deferred catalog rather than fixed, because both span a surface this no-source cycle does not own. No consolidation loop is needed.

`docs/builder/bld-020-final.md` opens next.

<!-- LINK DEFINITIONS -->

<!-- Root -->

<!-- docs/ -->

<!-- docs/SPECS/ -->

<!-- docs/builder/ -->

<!-- django_strawberry_framework/ -->

<!-- tests/ -->

<!-- examples/ -->

<!-- scripts/ -->

<!-- .venv/ -->

<!-- External -->
