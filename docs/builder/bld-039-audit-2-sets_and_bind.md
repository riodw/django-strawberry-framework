# Build: Slice 1b — audit of `spec-039` Slice 2 contracts (`SerializerMutation` base, `Meta`, bind, clear seam, root export)

Spec reference: `docs/SPECS/spec-039-serializer_mutations-0_0_13.md`
Build plan: `docs/builder/build-039-serializer_mutations-0_0_13.md`
Status: review-accepted

## Audit (Worker 3, read-only)

This slice has no build/review split: `spec-039` shipped, nothing was built here, and
no file of any kind was mutated. One combined audit section, per the dispatch.

### Grading baseline

Every file in the grading set is **byte-identical to `HEAD`** (`89ee8ac5`) — verified
per-file with `git diff --quiet HEAD -- <path>`, all clean:

`django_strawberry_framework/rest_framework/{sets.py,__init__.py,inputs.py}`,
`django_strawberry_framework/{__init__.py,registry.py}`,
`django_strawberry_framework/types/finalizer.py`,
`django_strawberry_framework/mutations/{sets.py,operations.py}`,
`django_strawberry_framework/forms/{sets.py,inputs.py}`,
`django_strawberry_framework/utils/inputs.py`,
`tests/rest_framework/{test_sets.py,test_soft_dependency.py}`,
`tests/base/test_init.py`, `tests/types/test_finalizer.py`, `tests/test_registry.py`.

No `git show HEAD:` extraction into a scratch path was therefore needed, and no
`git stash` / `checkout` / `restore` / `worktree` was run. The concurrent session's
dirty files (`conf.py`, `list_field.py`, `orders/sets.py`, `resource_policy.py`,
`utils/querysets.py`, `_strawberry_patches.py`, `apps.py`, and their tests) are
disjoint from this population and were not read as evidence.

The **spec** was read at the working tree, not `HEAD`: the only working-tree change to
`docs/SPECS/spec-039-serializer_mutations-0_0_13.md` is this cycle's own Slice 0
rationale extraction (revision-history prose moved to `docs/SPECS/appx/…-rationale.md`;
`git diff --stat` → 100 insertions / 729 deletions). None of the contract sections in
this population (`## Slice checklist` Slice 2, Decisions 5 / 6 / 10 / 11 / 12,
`## Cross-flavor reuse and DRY obligations`, `## Edge cases and constraints`,
`## Definition of done`) were touched by that extraction.

### Population: 92 contract rows

Enumerated **before** grading, from the sections the dispatch names.

| Group | Source | Rows |
| --- | --- | --- |
| A | `## Slice checklist` → the `Slice 2` block and every nested sub-bullet (incl. the `register_subsystem_clear` sub-block and the `**DRY / reuse**` sub-bullet) | A1–A41 (41) |
| B | `### Decision 5 — Public surface` | B1–B7 (7) |
| C | `### Decision 6 — Base-class strategy` | C1–C9 (9) |
| D | `### Decision 10 — Operations` | D1–D4 (4) |
| E | `### Decision 11 — Write authorization` | E1–E4 (4) |
| F | `### Decision 12` — the `__init__.py` / soft-import half | F1–F12 (12) |
| G | `## Cross-flavor reuse and DRY obligations` rows landing in `rest_framework/sets.py`, plus the `### Import manifest` entry for `sets.py` (P1.2 / P1.6 / P1.7 fold into A33 / A40 / A37–A38, which state the same contract verbatim) | G4–G8 (5) |
| H | `## Definition of done` item 3; item 8's named-lazy-export-not-in-`__all__` clause | H1–H2 (2) |
| I | `## Edge cases and constraints` rows bearing on `Meta` validation, operations, or binding | I1–I8 (8) |

41 + 7 + 9 + 4 + 4 + 12 + 5 + 2 + 8 = **92**.

### Grade counts

| Grade | Count |
| --- | --- |
| BUILT-CONFORMANT | 80 |
| SPEC-STALE | 9 |
| SUPERSEDED | 3 |
| DEVIATED | 0 |
| **DROPPED** | **0** |

80 + 9 + 3 = 92.

### A — `## Slice checklist`, the Slice 2 block (41 rows)

| # | Contract | Grade | Evidence |
| --- | --- | --- | --- |
| A1 | `SerializerMutation` subclasses `DjangoMutation`, overriding `_resolve_model` → `Meta.serializer_class.Meta.model` | BUILT-CONFORMANT | `django_strawberry_framework/rest_framework/sets.py::SerializerMutation`; `::SerializerMutation._resolve_model` #"resolve_meta_model(meta, key=\"serializer_class\", meta_attr=\"Meta\")" |
| A2 | …plus the `_validate_meta` / `build_input` / `input_type_name` / `input_module_path` / `resolve_sync` / `resolve_async` seams — the exact override set `DjangoModelFormMutation` uses | BUILT-CONFORMANT | all six present in `rest_framework/sets.py::SerializerMutation`; the pair is generated by the shared `mutations/sets.py::resolver_seams`, the same factory `forms/sets.py::DjangoModelFormMutation` #"resolve_sync, resolve_async = resolver_seams(" uses |
| A3 | `Meta.serializer_class` required and a `serializers.Serializer` subclass | BUILT-CONFORMANT | `rest_framework/sets.py::SerializerMutation._validate_meta` #"require_backing_class(" → `mutations/sets.py::require_backing_class`; type gate `mutations/sets.py::require_subclass` with `expected=serializers.Serializer` |
| A4 | Must be a `serializers.ModelSerializer` with a resolvable `Meta.model`; otherwise a targeted `ConfigurationError` | BUILT-CONFORMANT | `rest_framework/sets.py::SerializerMutation._validate_meta` #"must be a serializers.ModelSerializer with a concrete Meta.model"; `mutations/sets.py::resolve_backed_model_or_raise` + `::require_model_class` |
| A5 | The check runs **before** `_resolve_model` (clean error, never a raw `AttributeError`) | BUILT-CONFORMANT | ordering in `_validate_meta`: `require_subclass` → `issubclass(..., ModelSerializer)` → `resolve_backed_model_or_raise`; `mutations/sets.py::resolve_meta_model` itself is total (three `getattr(..., None)` links, returns `None`) |
| A6 | `operation` is `create` / `update` only; `"delete"` rejected | BUILT-CONFORMANT | `mutations/sets.py::require_non_delete_operation`; `mutations/operations.py::non_delete_operation_error`; `tests/rest_framework/test_sets.py::test_delete_operation_rejected` (run, passes) |
| A7 | …and its shape-identity operation component is that value | BUILT-CONFORMANT | `rest_framework/inputs.py::SerializerInputShape` #"``serializer_class`` / ``operation_kind`` - the declaration identity"; fed from `NON_DELETE_OPERATION_INPUT_KIND[meta.operation]` in `sets.py::SerializerMutation.build_input` |
| A8 | Allowed-key set **adds** `serializer_class` / `optional_fields`, **keeps** `operation` / `fields` / `exclude` / `permission_classes`, **drops** `model` / `input_class` / `partial_input_class` | **SPEC-STALE** | `rest_framework/sets.py` #"_ALLOWED_SERIALIZER_META_KEYS" = `MODEL_BACKED_WRITE_META_KEYS \| {serializer_class, optional_fields, injected_fields, nested_fields}`. The drops and keeps hold (`mutations/sets.py` #"MODEL_BACKED_WRITE_META_KEYS" = `{fields, exclude, permission_classes, operation, select_for_update}`), but the shipped set also admits `injected_fields` (rev6 #2), `nested_fields` (rev6 #17) and `select_for_update` (rev6 #14). See Notes for Worker 1 #2. |
| A9 | `Meta.fields` / `Meta.exclude` mutually exclusive | BUILT-CONFORMANT | `mutations/sets.py::normalize_meta_field_selection` + `rest_framework/inputs.py::resolve_effective_serializer_fields`; `tests/rest_framework/test_sets.py::test_fields_and_exclude_both_raises` |
| A10 | The whole module is behind the DRF soft-import guard | BUILT-CONFORMANT | `rest_framework/sets.py` #"from rest_framework import serializers" at module top; `rest_framework/__init__.py` #"require_drf()" runs on package import; `tests/rest_framework/test_soft_dependency.py::test_rest_framework_sets_import_raises_install_hint` |
| A11 | No change to `DEFERRED_META_KEYS` / `ALLOWED_META_KEYS` | BUILT-CONFORMANT | `tests/rest_framework/test_sets.py::test_deferred_and_allowed_meta_keys_unchanged` (run, passes) |
| A12 | Rides the existing `bind_mutations()`; **no new bind entry point** | BUILT-CONFORMANT | no `bind_serializer_mutations` / `register_serializer_mutation` symbol exists anywhere in `django_strawberry_framework/` or `tests/` — the only occurrence of the string is the negative statement in `rest_framework/sets.py`'s module docstring #"no ``bind_serializer_mutations()``" |
| A13 | `build_input` materializes into a `rest_framework` input namespace | BUILT-CONFORMANT | `rest_framework/sets.py::SerializerMutation` #"input_module_path: str = SERIALIZER_INPUTS_MODULE_PATH"; `::SerializerMutation.build_input` → `materialize_serializer_input_class` |
| A14 | A `clear_serializer_input_namespace()` ledger-clear exists, on the `forms/inputs.py::clear_form_input_namespace` precedent | BUILT-CONFORMANT | `rest_framework/inputs.py::clear_serializer_input_namespace` |
| A15 | Clear site 1 — the `finalize_django_types` **pre-bind reset block** | BUILT-CONFORMANT | `types/finalizer.py` #"for clear in iter_subsystem_clears(before_bind=True):" immediately before `bind_mutations()`; the serializer row registers with `before_bind=True` (`rest_framework/inputs.py` #"owner=\"rest_framework.input_namespace\"") |
| A16 | Clear site 2 — `TypeRegistry.clear()` | BUILT-CONFORMANT | `registry.py::TypeRegistry.clear` #"for clear in iter_subsystem_clears():" (unfiltered, so it replays the pre-bind rows too) |
| A17 | The clear is wired through the **mandatory** `register_subsystem_clear` seam — one canonical list both sites iterate — **not two hand-edits** | BUILT-CONFORMANT | `registry.py::register_subsystem_clear` writing the single `registry.py` #"_subsystem_clears" dict; both sites read it through `registry.py::iter_subsystem_clears`. **The known trap is confirmed: neither clear is hand-wired in `registry.py`.** The seam is repo-wide — **18 distinct `owner=` identities across 20 registration sites** (measured: `grep -rho "register_subsystem_clear(" django_strawberry_framework/ \| wc -l` → 20; `grep -rn 'owner="' … \| sort -u` → 18), three of them `rest_framework.*`. |
| A18 | The list holds **static `(module_path, attr)` STRING rows only**, resolved lazily by `_clear_if_importable`; no import at registration time | **SUPERSEDED** | Inverted deliberately. `registry.py::register_subsystem_clear` #"register_subsystem_clear() requires a zero-argument callable" — a string row is now **rejected**, pinned by `tests/test_registry.py::test_register_subsystem_clear_rejects_string_references`. `registry.py::_clear_if_importable`'s own docstring records the change: #"clear()'s REGISTRY-WIDE co-clears … do not go through this helper - each subsystem owner registers its own callback". Superseding commit `48f9f65d` (2026-07-11, "Refactor subsystem clear registration and handling", body: "improving clarity and reducing silent failures due to string references"). |
| A19 | The soft-dep asymmetry vanishes **because every entry routes through `_clear_if_importable`** | **SUPERSEDED** | Outcome holds; named mechanism does not. Laziness now comes from registration site, not import guarding: `rest_framework/inputs.py` registers at its own module body, which only executes with DRF present, so a DRF-absent build registers nothing and clears nothing. `registry.py::register_subsystem_clear` #"Optional subsystems remain lazy because only an imported owner can register." Same supersession as A18. |
| A20 | F10 invariant: a subsystem with clearable state has by definition been imported and registered its clear | BUILT-CONFORMANT | holds a fortiori under callable registration — state and callback are created by the same module body execution (`rest_framework/inputs.py` #"register_subsystem_clear(") |
| A21 | A **retry-idempotence test** locks it (materialize input, fail a later type, rerun finalization, assert the ledger was cleared) | BUILT-CONFORMANT | `tests/rest_framework/test_sets.py::test_bind_is_retry_idempotent_after_fixable_later_phase_failure` — monkeypatches `_bind_ordersets` to raise post-bind, asserts `"ItemSerializerInput" in serializer_materialized_names` and `registry.is_finalized() is False`, undoes, re-finalizes clean. **Run: passes.** (It asserts the cleared ledger by its observable consequence — no spurious distinct-class collision on the second pass — rather than by inspecting an empty dict mid-flight. Adequate; noted.) |
| A22 | `__init__.py` exports `SerializerMutation` (**one net-new public symbol**) via a root-level `__getattr__` (PEP 562) | **SPEC-STALE** | The `__getattr__` mechanism is exactly as specified (`django_strawberry_framework/__init__.py::__getattr__`), but the lazy surface is **seven** names, not one: `__init__.py` #"_DRF_SOFT_EXPORTS" carries `SerializerMutation`, `register_serializer_field_converter`, `SerializerFieldConversion`, `describe_serializer_input`, `NestedSerializerConfig`, `SerializerHookContext`, `UploadMetadata`. Six of the seven are spec'd elsewhere in the same document (rev6 #11 line 3287-3291, rev6 #15 line 3409, rev6 #17 lines 3538-3542, and the hook-context block at lines 65-68) — the Slice-2 line and Decision 5 were never reconciled to them. See Notes for Worker 1 #1. |
| A23 | Resolvable by **name** through the shared `require_drf()`; DRF absent → `ImportError` with the install hint | BUILT-CONFORMANT | `__init__.py::__getattr__` #"require_drf()"; `tests/rest_framework/test_soft_dependency.py::test_each_root_serializer_surface_lookup_raises_install_hint` (parametrized over all seven) |
| A24 | **NOT** added to `__all__`; `from … import *` stays DRF-free | BUILT-CONFORMANT | `__init__.py` #"__all__ = (" contains no DRF name; `tests/base/test_init.py::test_public_api_surface_is_pinned` pins the exact 38-entry tuple and `::test_star_import_preserves_namespace_hygiene` asserts `"SerializerMutation" not in ns`. Both run, pass. **Known trap 3 confirmed clean.** |
| A25 | `import django_strawberry_framework` succeeds without DRF; the root never eagerly imports `rest_framework/` | BUILT-CONFORMANT | `tests/rest_framework/test_soft_dependency.py::test_fresh_root_package_import_and_star_import_succeed_without_drf` runs it in a **fresh subprocess** (the only way to catch a newly-introduced eager import) and asserts `"django_strawberry_framework.rest_framework" not in sys.modules` |
| A26 | This is the one root edit; the eager-import + explicit-`__all__` style is otherwise preserved | BUILT-CONFORMANT | `__init__.py` keeps its eager `from .x import Y  # noqa: E402` block and its literal `__all__` tuple; the lazy block is purely additive |
| A27 | `tests/rest_framework/test_sets.py` — the `Meta` validation matrix | BUILT-CONFORMANT | every named cell present: `::test_missing_serializer_class_raises`, `::test_non_serializer_value_rejected`, `::test_plain_serializer_with_no_model_rejected`, `::test_modelserializer_with_no_meta_model_rejected`, `::test_delete_operation_rejected`, `::test_serializer_class_accepted_as_known_key`, `::test_fields_and_exclude_both_raises`, `::test_unknown_meta_key_raises` |
| A28 | …registration | BUILT-CONFORMANT | `::test_serializer_mutation_registers_in_mutation_registry`, `::test_abstract_base_registers_nowhere`, `::test_late_declaration_after_finalize_raises` |
| A29 | …finalizer binding (the `bind_mutations()` path) | BUILT-CONFORMANT | `::test_bind_materializes_serializer_input_into_rest_framework_namespace`, `::test_update_binds_partial_input` |
| A30 | …the no-registered-primary-type error | BUILT-CONFORMANT | `::test_no_registered_primary_type_raises_at_finalize` |
| A31 | …the model-flavor seam defaults unchanged | BUILT-CONFORMANT | `::test_model_flavor_base_unregressed` |
| A32 | DRY: `_validate_meta` reuses `mutations/sets.py::_validate_permission_classes` | BUILT-CONFORMANT | `rest_framework/sets.py` #"_validate_permission_classes as _validate_mutation_permission_classes"; default `(DjangoModelPermission,)` lives once in `mutations/sets.py::_validate_permission_classes` |
| A33 | DRY (P1.2): the shared non-delete ops set — a promoted `NON_DELETE_WRITE_OPERATIONS` both flavors import, **not** a new `_VALID_SERIALIZER_OPERATIONS` | BUILT-CONFORMANT | `_VALID_SERIALIZER_OPERATIONS` and `_VALID_FORM_OPERATIONS` do **not** exist anywhere; `NON_DELETE_WRITE_OPERATIONS` is defined once (`mutations/operations.py`) and reached by both flavors through `mutations/sets.py::require_non_delete_operation`. Module home differs from the spec — see D4. |
| A34 | DRY (P2.7): the promoted `reject_unknown_meta_keys(name, meta, allowed)` called with `_ALLOWED_SERIALIZER_META_KEYS` | BUILT-CONFORMANT | `mutations/sets.py::reject_unknown_meta_keys`; called by serializer (`rest_framework/sets.py::SerializerMutation._validate_meta`) and both form bases (`forms/sets.py`) |
| A35 | DRY (P2.5 / P2.7): returns a `_ValidatedMutationMeta` | BUILT-CONFORMANT | `rest_framework/sets.py::SerializerMutation._validate_meta` #"return _ValidatedMutationMeta(" |
| A36 | DRY (P2.7): `normalize_field_name_sequence(..., flavor="SerializerMutation")` **directly** — no third re-binding wrapper | BUILT-CONFORMANT | called directly for `optional_fields` and `injected_fields`; `fields` / `exclude` route through `mutations/sets.py::normalize_meta_field_selection`, which is the **shared all-flavor** pair-normalizer, not a serializer-specific wrapper. No `_serializer_normalize_*` symbol exists. |
| A37 | DRY (P1.7): the `build_input` / `input_type_name` cluster rides the promoted `build_and_stash_input` core, **not** a byte-parallel `_build_and_stash_serializer_input` | BUILT-CONFORMANT | `mutations/sets.py::build_and_stash_input` called by `rest_framework/sets.py::SerializerMutation.build_input` and `forms/sets.py` (two callers); no `_build_and_stash_serializer_input` exists |
| A38 | DRY (P1.7): does **not** route through `cached_build_input` (the descriptor is only knowable after the build) | BUILT-CONFORMANT | `rest_framework/sets.py::SerializerMutation.build_input` #"does NOT route the cache lookup through"; dedupe goes through `rest_framework/inputs.py::dedupe_serializer_input_shape` **after** `_build()`, with the create-required guard inside `_build` (guard-before-dedupe preserved) |
| A39 | DRY: required-field injection explicit through `Meta.injected_fields` + `get_serializer_injected_data`, never inferred from constructor-hook overrides | BUILT-CONFORMANT | `mutations/sets.py::_hook_overridden` #"Serializer mutations do NOT ride this primitive"; `tests/rest_framework/test_sets.py::test_get_serializer_kwargs_override_no_longer_waives_create_required_guard` pins the removed waiver |
| A40 | DRY (P1.6): the input-ledger clear registers through `register_subsystem_clear` | BUILT-CONFORMANT | `rest_framework/inputs.py` #"owner=\"rest_framework.input_namespace\"" and #"owner=\"rest_framework.shape_cache\"" (both `before_bind=True`); `rest_framework/serializer_converter.py` #"owner=\"rest_framework.choice_enums\"" |
| A41 | The serializer's **only** genuinely-new `_validate_meta` logic is the `serializer_class` is-a-`ModelSerializer` (+ resolvable `Meta.model`) check and `optional_fields` normalization | **SPEC-STALE** | At `HEAD`, `rest_framework/sets.py::SerializerMutation._validate_meta` also owns: `Meta.injected_fields` normalization + the writable-basis and not-still-in-input guards; `Meta.select_for_update` via `validate_select_for_update`; `Meta.nested_fields` via `::_validate_serializer_nested_fields`; the schema-hook field-map validation `::_validate_schema_field_map`; the schema fingerprint capture; and the recursive writable-`source` ownership walk `::_assert_schema_source_ownership`. |

### B — `### Decision 5 — Public surface` (7 rows)

| # | Contract | Grade | Evidence |
| --- | --- | --- | --- |
| B1 | **One net-new public symbol**, re-exported from `__init__.py` as a lazy export via the root `__getattr__` | **SPEC-STALE** | see A22 — seven names in `__init__.py` #"_DRF_SOFT_EXPORTS" |
| B2 | Deliberately **NOT** added to `__all__` | BUILT-CONFORMANT | see A24 |
| B3 | No net-new field factory or error type; exposed through the existing `DjangoMutationField`, returns the frozen `FieldError` envelope | BUILT-CONFORMANT | `mutations/fields.py` unchanged for the serializer flavor; `rest_framework/resolvers.py` #"from ..mutations.inputs import NON_FIELD_ERROR_KEY, FieldError" — the `036` envelope, no serializer-local error type |
| B4 | Target check: `_validate_mutation_target` accepts any family member via the duck-typed `_has_mutation_protocol` | BUILT-CONFORMANT | `mutations/fields.py::_has_mutation_protocol` — `resolve_sync` / `resolve_async` / `input_type_name` callables + non-`None` `input_module_path`; no `issubclass(DjangoMutation)` |
| B5 | Resolver dispatch: `DjangoMutationField._resolve` calls `mutation_cls.resolve_sync` / `resolve_async` | BUILT-CONFORMANT | `mutations/fields.py::DjangoMutationField._resolve` #"return mutation_cls.resolve_async(info, **call_kwargs)" |
| B6 | The `data:` input-ref: `_synthesized_mutation_signature` consults `input_type_name(meta)` + `input_module_path` | BUILT-CONFORMANT | `mutations/fields.py` #"_lazy_ref(mutation_cls.input_type_name(meta), mutation_cls.input_module_path)" |
| B7 | The work owed the factory is a **verification test**, not an edit | BUILT-CONFORMANT | `tests/mutations/test_fields.py` #"`DjangoMutationField` accepts a `SerializerMutation`" — imports `SerializerMutation` by name from the package root, asserts `data: ItemSerializerInput!` and that `resolve_sync` is the serializer override, not `DjangoMutation`'s |

### C — `### Decision 6 — Base-class strategy` (9 rows)

| # | Contract | Grade | Evidence |
| --- | --- | --- | --- |
| C1 | Subclasses `DjangoMutation` exactly as `DjangoModelFormMutation` does | BUILT-CONFORMANT | `rest_framework/sets.py::SerializerMutation` |
| C2 | `_resolve_model` override; reuses the base primary-`DjangoType` payload in the uniform `node` / `result` slot | BUILT-CONFORMANT | `::SerializerMutation._resolve_model`; payload built by the shared `mutations/sets.py::bind_mutation_outputs` path (no serializer payload builder exists) |
| C3 | `DjangoModelPermission` default, authorized for free through the model override | BUILT-CONFORMANT | `mutations/sets.py::_validate_permission_classes` #"unset_default: tuple[Any, ...] = (DjangoModelPermission,)"; `tests/rest_framework/test_sets.py::test_unset_permission_classes_keeps_model_permission_default` |
| C4 | Visibility-scoped `update` locate + the optimizer re-fetch (G2) reused | BUILT-CONFORMANT | `rest_framework/resolvers.py` imports `run_write_pipeline_sync` from `mutations/resolvers.py` and the visibility substrate from `utils/querysets.py`; the pipeline's own detail is Slice 1c's population |
| C5 | `Meta.operation` restricted to `"create"` / `"update"` | BUILT-CONFORMANT | see A6 |
| C6 | Registers in the existing mutation declaration registry and rides `bind_mutations()` at phase 2.5 | BUILT-CONFORMANT | registration is the shared `mutations/sets.py::make_meta_validating_metaclass` + `::register_mutation`; see A12, A28, A29 |
| C7 | `build_input` materializes into a `rest_framework` input namespace | BUILT-CONFORMANT | see A13 |
| C8 | Adds **no** `DjangoType` `Meta` key; `DEFERRED_META_KEYS` / `ALLOWED_META_KEYS` untouched | BUILT-CONFORMANT | see A11 |
| C9 | `ModelSerializer`-driven; a plain model-less `serializers.Serializer` is out of scope and rejected | BUILT-CONFORMANT | `rest_framework/sets.py::SerializerMutation._validate_meta` #"a plain serializers.Serializer has no backing model + no DjangoType to return"; `tests/rest_framework/test_sets.py::test_plain_serializer_with_no_model_rejected` |

### D — `### Decision 10 — Operations` (4 rows)

| # | Contract | Grade | Evidence |
| --- | --- | --- | --- |
| D1 | `"delete"` rejected at class creation with a `ConfigurationError` | BUILT-CONFORMANT | see A6 |
| D2 | The package's per-operation `Meta.operation` is used, not graphene's `model_operations` / `lookup_field` | BUILT-CONFORMANT | neither key is in `_ALLOWED_SERIALIZER_META_KEYS`, so either is an unknown-key `ConfigurationError` via `reject_unknown_meta_keys` |
| D3 | `Meta.operation` stays **mandatory** (no default) | BUILT-CONFORMANT | `mutations/sets.py::require_non_delete_operation` raises on a missing key (`getattr(meta, "operation", None)` → not a `str` → raise); `tests/rest_framework/test_sets.py::test_missing_operation_rejected` |
| D4 | P1.2: a single `NON_DELETE_WRITE_OPERATIONS` constant **promoted to `mutations/sets.py`**, both flavors import it; the "no serializer/form delete" **message** single-sites too | **SPEC-STALE** | Both halves shipped, at a different home: the constant and the message live in `mutations/operations.py` (#"NON_DELETE_WRITE_OPERATIONS: frozenset[str]", `::non_delete_operation_error`) and reach both flavors through `mutations/sets.py::require_non_delete_operation`, which re-exports via its own import. `mutations/operations.py` did not exist when the spec was written. |

### E — `### Decision 11 — Write authorization` (4 rows)

| # | Contract | Grade | Evidence |
| --- | --- | --- | --- |
| E1 | `Meta.permission_classes` defaults to `[DjangoModelPermission]` | BUILT-CONFORMANT | see C3 |
| E2 | Resolves the model via the `_resolve_model` override; enforces Django `add` / `change` | BUILT-CONFORMANT | `mutations/operations.py` #"_OPERATION_PERMISSION_ACTION" maps create→`add`, update→`change`; `mutations/permissions.py::DjangoModelPermission` |
| E3 | An anonymous / under-privileged caller is denied with a top-level `GraphQLError` before any write | BUILT-CONFORMANT | inherited unchanged — `rest_framework/resolvers.py` rides `mutations/resolvers.py::run_write_pipeline_sync`, which owns the authorize step; live proof is Slice 1c's population |
| E4 | `check_permission(self, info, operation, data, instance=None)` is the override point, exactly as for model / form; `Meta.permission_classes` stays a kept serializer `Meta` key | BUILT-CONFORMANT | `mutations/sets.py::DjangoMutation.check_permission` — signature byte-matches the spec, not overridden in `rest_framework/sets.py`; the key is kept via `MODEL_BACKED_WRITE_META_KEYS`, pinned by `tests/rest_framework/test_sets.py::test_permission_classes_accepted_as_known_key` |

### F — `### Decision 12`, the `__init__.py` / soft-import half (12 rows)

| # | Contract | Grade | Evidence |
| --- | --- | --- | --- |
| F1 | The root uses eager imports + an explicit `__all__` tuple, so the export is lazy via a PEP 562 root `__getattr__` | BUILT-CONFORMANT | `django_strawberry_framework/__init__.py::__getattr__` |
| F2 | Table row 1 — `import django_strawberry_framework` **always succeeds** | BUILT-CONFORMANT | see A25 |
| F3 | Table row 2 — `from … import SerializerMutation` triggers `__getattr__`, which imports `rest_framework.sets`; absent → `ImportError` + hint | BUILT-CONFORMANT | `__init__.py` #"_DRF_SOFT_EXPORTS" row `(".rest_framework.sets", "SerializerMutation")`; `tests/base/test_init.py::test_dynamic_drf_soft_exports_via_getattr` and `tests/rest_framework/test_soft_dependency.py::test_each_root_serializer_surface_lookup_raises_install_hint` |
| F4 | Table row 3 — any `rest_framework/` submodule import runs `require_drf()` first | BUILT-CONFORMANT | `rest_framework/__init__.py` #"require_drf()" as the last module-body statement; `::test_rest_framework_package_import_raises_install_hint`, `::test_rest_framework_sets_import_raises_install_hint` |
| F5 | Table row 4 — `from … import *` always succeeds and omits `SerializerMutation` | BUILT-CONFORMANT | `::test_star_import_stays_drf_free_and_binds_no_serializer_surface` (asserts the whole `_DRF_SOFT_EXPORTS` key set is disjoint from the namespace, and a normal eager export still binds) |
| F6 | One shared `require_drf()` in `rest_framework/__init__.py` owns the single install-hint message | BUILT-CONFORMANT | `rest_framework/__init__.py` #"_DRF_INSTALL_HINT" — one string, delegating to `utils/imports.py::require_optional_module`, the same primitive `routers.py::require_channels` uses |
| F7 | F1 — `SerializerMutation` stays out of `__all__` because a star import binds every `__all__` name | BUILT-CONFORMANT | see A24 |
| F8 | The root `__getattr__` does **not** memoize | BUILT-CONFORMANT | `__init__.py::__getattr__` never writes `globals()`; `tests/base/test_init.py::test_dynamic_getattr_non_memoization` (run, passes) and `tests/rest_framework/test_soft_dependency.py::test_successful_lookup_does_not_memoize` (parametrized over all seven) |
| F9 | The absent-DRF test deletes the root `SerializerMutation` attribute (or reloads the root) before forcing the failure | BUILT-CONFORMANT | `tests/rest_framework/test_soft_dependency.py` #"delattr(django_strawberry_framework, \"SerializerMutation\")" in the `_simulate_drf_absent` fixture |
| F10 | The DRF-absent guard path is covered by **simulated absence** — monkeypatching `builtins.__import__` — asserting the hint on all three raising entry points while the root import still succeeds | **SPEC-STALE** | All three entry points are covered and the root import still succeeds, but the simulation mechanism is the importlib-native `sys.modules["rest_framework"] = None` sentinel (`tests/_soft_dependency.py::simulated_absence`), not a `builtins.__import__` monkeypatch. The root-import half additionally runs in a fresh **subprocess**, which the spec does not describe and which is strictly stronger (it can catch a newly-introduced eager import; an in-process re-import cannot). |
| F11 | The test evicts module caches for **both** `rest_framework*` and `django_strawberry_framework.rest_framework*` | BUILT-CONFORMANT | `simulated_absence("rest_framework", "rest_framework", "django_strawberry_framework.rest_framework", parent=…, attr="rest_framework")` — two-sided restore owned by the shared helper |
| F12 | The install hint names the verified floor `djangorestframework>=3.17.0` (place 2 of the three that must agree) | BUILT-CONFORMANT | `rest_framework/__init__.py` #"djangorestframework>=3.17.0"; place 1 is `pyproject.toml` `[dependency-groups].dev` #"djangorestframework>=3.17.0" (**not** `[project].dependencies` — confirmed); place 3 is Decision 12 itself. All three agree. |

### G — cross-flavor reuse rows landing in `rest_framework/sets.py`, plus the import manifest (5 rows)

*(P1.2, P1.6, P1.7 state the same contract as A33, A40, A37–A38 verbatim and are graded there.)*

| # | Contract | Grade | Evidence |
| --- | --- | --- | --- |
| G4 | P2.5 — `_validate_meta` reuses the base sub-validators and returns a `_ValidatedMutationMeta`; **the serializer follows the model flavor's `mutations/sets.py::_normalize_field_sequence` precedent** for field-sequence normalization | **SPEC-STALE** | The reuse and the `_ValidatedMutationMeta` return hold (A32, A34, A35). The named precedent does not exist: `_normalize_field_sequence` has been deleted package-wide. `utils/inputs.py` #"the former per-flavor ``_normalize_field_sequence`` / ``normalize_form_field_sequence``" records the removal; all three flavors now call `utils/inputs.py::normalize_field_name_sequence` with a `flavor=` argument. |
| G5 | P2.7 — promote `reject_unknown_meta_keys`; no third normalize wrapper | BUILT-CONFORMANT | A34 + A36 |
| G6 | P2.6 — the serializer ships **only** the finer `get_serializer_kwargs` hook, has **no** coarse `get_serializer` constructor hook, and its default body sets **neither** `partial` **nor** `context` | BUILT-CONFORMANT | no `def get_serializer(` exists under `rest_framework/`; `rest_framework/sets.py::SerializerMutation.get_serializer_kwargs` body is `return construction_kwargs(data=data)` — shared with the form default via `mutations/sets.py::construction_kwargs`, and it sets neither key |
| G7 | `### Import manifest` row for `sets.py`: imports `mutations/sets.py::{DjangoMutation, _validate_permission_classes, _normalize_field_sequence, _ValidatedMutationMeta, register_mutation}`, the shared non-delete ops constant, `reject_unknown_meta_keys`, `mutations/inputs.py::{CREATE, PARTIAL}` | **SPEC-STALE** | Three entries are wrong and the manifest is far short of the shipped list. Wrong: `_normalize_field_sequence` (deleted, G4); `register_mutation` (**not** imported by `rest_framework/sets.py` — registration is owned by the shared metaclass `mutations/sets.py::make_meta_validating_metaclass`); `PARTIAL` (not imported — reached through `NON_DELETE_OPERATION_INPUT_KIND`; only `CREATE` is imported). Missing from the manifest, all shipped: `MODEL_BACKED_WRITE_META_KEYS`, `NON_DELETE_OPERATION_INPUT_KIND`, `build_and_stash_input`, `construction_kwargs`, `normalize_meta_field_selection`, `require_backing_class`, `require_model_class`, `require_non_delete_operation`, `require_subclass`, `resolve_backed_model_or_raise`, `resolve_meta_model`, `resolver_seams`, `validate_select_for_update`, plus `utils/inputs.py::normalize_field_name_sequence` and sixteen `rest_framework/inputs.py` symbols. Nothing in the shipped list duplicates a manifest symbol's logic — the manifest is under-specified, not violated. |
| G8 | Cross-module: `clear_serializer_input_namespace` **must** register through the mandatory `register_subsystem_clear` seam (M4) rather than be hand-added to both lists; P1.6 touches `types/finalizer.py` and `registry.py` | **SUPERSEDED** | The mandate holds and both files carry the seam; the seam's row shape is the superseded half — see A18 / A19. |

### H — `## Definition of done` (2 rows)

| # | Contract | Grade | Evidence |
| --- | --- | --- | --- |
| H1 | DoD item 3 — the whole Slice-2 completion contract | **SPEC-STALE** | Every clause verified conformant (A1–A12, A22–A26 above) **except** its restatement of the superseded row shape: #"one canonical list of static `(module_path, attr)` string rows both sites iterate via `_clear_if_importable`". The rest of item 3 — the seam being mandatory, both sites clearing, DRF never imported while absent, the serializer not being a third hand-maintained list — all hold. |
| H2 | DoD item 8 — the one net-new public symbol is a **named lazy export** via the root `__getattr__` under the DRF guard, and is **NOT in `__all__`** while DRF is soft | BUILT-CONFORMANT | A22–A24; `tests/base/test_init.py::test_public_api_surface_is_pinned` is the regression pin. (The "one" count is stale — graded at B1, not re-graded here.) |

### I — `## Edge cases and constraints` bearing on `Meta` validation / operations / binding (8 rows)

| # | Contract | Grade | Evidence |
| --- | --- | --- | --- |
| I1 | **DRF not installed** — root import succeeds; any `SerializerMutation` / `rest_framework/` import raises `ImportError` with the hint via `require_drf()` | BUILT-CONFORMANT | F2 / F3 / F4 / F6 |
| I2 | **A plain `serializers.Serializer` (no model)** — rejected at class creation | BUILT-CONFORMANT | C9 |
| I3 | **A `Meta.operation = "delete"`** — rejected at class creation | BUILT-CONFORMANT | A6 / D1 |
| I4 | **No `DjangoType` `Meta` key added** — `DEFERRED_META_KEYS` / `ALLOWED_META_KEYS` byte-unchanged | BUILT-CONFORMANT | A11 |
| I5 | **Create narrowing drops a required serializer field** → bind-time `ConfigurationError` via `guard_create_required_serializer_fields`, run **per declaration**; `Meta.injected_fields` its only subtraction; update inputs unaffected | BUILT-CONFORMANT | `rest_framework/sets.py::SerializerMutation.build_input` #"if operation_kind == CREATE:" inside `_build`, before `dedupe_serializer_input_shape`; `tests/rest_framework/test_sets.py::test_create_required_guard_fires_through_build_input`, `::test_build_input_runs_required_guard_per_declaration`, `::test_meta_injected_fields_lets_narrowing_drop_required_field`, `::test_narrowing_dropping_required_without_injected_still_raises` |
| I6 | **Dynamic / kwargs-requiring serializers** — shape discovered via `get_serializer_for_schema()`; a per-request-varying field set is rejected loudly | BUILT-CONFORMANT | `rest_framework/sets.py::SerializerMutation.get_serializer_for_schema` (default) + `::_validate_schema_field_map` (class creation) + `::_checked_schema_field_map` (bind, fingerprint drift raise); `tests/rest_framework/test_sets.py::test_get_serializer_for_schema_classmethod_override_drives_bind`, `::test_nondeterministic_schema_hook_raises_at_bind` |
| I7 | **Same serializer, same field names, different shape** → distinct `SerializerInputShape` descriptors → distinct deterministic names, never silent reuse | BUILT-CONFORMANT | `rest_framework/inputs.py::SerializerInputShape` — `annotations`, `descriptions`, `required_state`, `optional_fields` all in the identity |
| I8 | **Two distinct generated serializer inputs colliding on one GraphQL name** → finalize-time `ConfigurationError` from the reused `materialize_generated_input_class` ledger | BUILT-CONFORMANT | `rest_framework/inputs.py::materialize_serializer_input_class` wraps the shared ledger raise and re-raises with the registered-shape debug dump |

### DRY findings

- **No new duplication.** Every promotion the spec named for this population landed at a
  single site with ≥ 2 live callers: `require_non_delete_operation`,
  `reject_unknown_meta_keys`, `normalize_meta_field_selection`, `build_and_stash_input`,
  `construction_kwargs`, `resolver_seams`, `require_backing_class` / `require_subclass` /
  `require_model_class` / `resolve_meta_model` / `resolve_backed_model_or_raise`,
  `validate_select_for_update` — all called by both `forms/sets.py` and
  `rest_framework/sets.py`. The forks the spec warned about (`_VALID_SERIALIZER_OPERATIONS`,
  `_build_and_stash_serializer_input`, a third normalize wrapper, a third hand-maintained
  clear list, a serializer-local `_pascalize_token`) do **not** exist.
- **Existence challenge (escalated, not held).** `mutations/sets.py::cached_build_input`
  has exactly **one** real caller — `forms/sets.py` #"return cached_build_input(" — and a
  two-statement body (`guard(); return get_or_store_shape_build(cache, shape_key, build_fn)`).
  Its get-or-store spine is separately shared three ways via
  `utils/inputs.py::get_or_store_shape_build` (`tests/utils/test_inputs.py` #"Model bind,
  form cached_build_input, and serializer dedupe import one get-or-store." pins that),
  and `spec-039` P1.7 explicitly rules the serializer out of it, so the second consumer it
  was promoted for will not arrive from this flavor. What would break if it were inlined
  into `forms/sets.py`: nothing mechanical — the guard-before-lookup **ordering** it
  single-sites would lose its named home, which is the argument for keeping it. Routed to
  Worker 1 below; not a blocker, and outside this cycle's audit scope to act on.
- **One stale comment contradicting its own code** — see Low below.

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` → **empty**; the file is
byte-identical to `HEAD`. This is a read-only audit pass that writes no source, so no
public-surface change was introduced by it.

Separately, as an audit finding rather than a diff check: the shipped `__all__` (38
entries) is DRF-free and pinned by `tests/base/test_init.py::test_public_api_surface_is_pinned`,
which passes. The seven-name lazy surface in `_DRF_SOFT_EXPORTS` is authorized by
`spec-039` Decision 12 for `SerializerMutation` and by rev6 #11 / #15 / #17 and the
hook-context block (spec lines 65-68, 3287-3291, 3409, 3538-3542) for the other six — but
**not** by Decision 5's own list, which still says "one net-new public symbol" (B1 / A22).

### CHANGELOG sanity

Not applicable; this pass modified no file at all, `CHANGELOG.md` included.

### Documentation / release sanity

Not applicable; this pass modified no docs/release/KANBAN/archive surfaces. (The one
artifact it writes is this file.)

### High:

None. No row graded DROPPED or DEVIATED; no spec contract in this population is
unimplemented at `HEAD`.

### Medium:

None. The nine SPEC-STALE and three SUPERSEDED rows are all spec-side debt with correct
code behind them, which `docs/builder/BUILD.md` `## Severity definitions` does not reach —
they are Worker 1's Slice 2 reconciliation work, itemized under Notes for Worker 1.

### Low:

#### A comment asserts the exact shape the code rejects

`django_strawberry_framework/rest_framework/inputs.py`, the comment immediately above the
input-namespace `register_subsystem_clear(...)` call, says:

```django_strawberry_framework/rest_framework/inputs.py:157:161
# Register the serializer input-namespace clear as a canonical PRE-BIND clear
# (the seam centerpiece). The row is a static STRING pair, so
# importing this owner only happens with DRF present, so the callback exists
# exactly when serializer state can exist. A DRF-absent build imports neither
# state nor callback, preserving the soft-dependency boundary.
```

The row is **not** a static string pair — it is a resolved callable plus an `owner=`
string, and `registry.py::register_subsystem_clear` #"requires a zero-argument callable"
raises `TypeError` on a string, pinned by
`tests/test_registry.py::test_register_subsystem_clear_rejects_string_references`. The
sentence is residue of the pre-`48f9f65d` spec text (the same clause A18 grades
SUPERSEDED), and it also mis-states the *reason* the soft-dep boundary holds: it holds
because the owning module only executes with DRF present, not because the row is a string.
Not load-bearing — the code is correct and the second clause of the same sentence is
accurate — but a future reader chasing the seam's contract from this comment will look for
a string list that does not exist.

Recommended change (Slice 3, or folded into Slice 2's `.py` citer sweep, both inside the
cycle fence): replace "The row is a static STRING pair, so" with a statement of the actual
mechanism — the callback is registered from this module's own body, which only runs with
DRF installed. No test expectation changes.

### What looks solid

- **The clear seam is genuinely one list, genuinely read by both sites.** `registry.py`
  #"_subsystem_clears" is written only by `register_subsystem_clear` and read only by
  `iter_subsystem_clears`; `types/finalizer.py` takes the `before_bind=True` slice and
  `TypeRegistry.clear()` takes all of it. Fourteen owners register; none is hand-wired.
  The trap the dispatch warned about (grepping `registry.py` for a subsystem's teardown and
  concluding it is missing) is real and the shipped design is the right answer to it.
- **The soft-dependency contract is tested at the only level that can catch a regression.**
  The root-import half runs in a fresh subprocess, and the absent-path tests are
  parametrized over the whole `_DRF_SOFT_EXPORTS` key set rather than over
  `SerializerMutation` alone — so adding an eighth soft export automatically inherits the
  guard, the non-memoization, and the star-import assertions.
- **`_validate_meta` is fail-closed throughout.** I hunted the `docs/builder/BUILD.md`
  `### Fail-open shapes` list explicitly on this configuration boundary and found none:
  every `getattr(meta, key, None)` default means genuine absence and is preceded by
  `reject_unknown_meta_keys`, so a typo cannot arrive as a silent `None`; every broad
  `except BaseException` (`::_validate_schema_field_map`, `::_validate_serializer_nested_fields`,
  `mutations/sets.py::_safe_frozenset_membership`) converts "the check blew up" into a
  **raise**, never into a permit; there is no clamp and no `or`-fallback on a decision
  value; `_checked_schema_field_map`'s `if meta.schema_fingerprint is not None` correctly
  uses `is not None` rather than truthiness, so an empty-field-map fingerprint `()` still
  arms the drift check.
- **The one place the serializer legitimately diverges from the form flavor is documented
  in code, not just in the spec.** `rest_framework/sets.py::SerializerMutation.build_input`
  states why it rides `build_and_stash_input` but not `cached_build_input`, and the
  guard-before-dedupe ordering is preserved inline — the property `cached_build_input`
  exists to protect is upheld, and `::test_build_input_runs_required_guard_per_declaration`
  pins it.

### Temp test verification

- **No temp test files were created.** `docs/builder/temp-tests/039-audit-2/` was **not**
  created; there is nothing to dispose of. This pass writes exactly one file — this
  artifact — and no other filesystem change of any kind.
- One focused, read-only verification run was made against the working tree (six existing
  permanent tests, all in the population's grading set, all clean vs `HEAD`):

  ```
  uv run pytest -n0 --no-cov -q \
    tests/rest_framework/test_sets.py::test_bind_is_retry_idempotent_after_fixable_later_phase_failure \
    tests/base/test_init.py::test_public_api_surface_is_pinned \
    tests/base/test_init.py::test_star_import_preserves_namespace_hygiene \
    tests/base/test_init.py::test_dynamic_getattr_non_memoization \
    tests/rest_framework/test_sets.py::test_delete_operation_rejected \
    tests/rest_framework/test_sets.py::test_deferred_and_allowed_meta_keys_unchanged
  → 6 passed in 0.11s
  ```

  No `--cov*` flag was used. This confirms A11, A21, A24, D1, F8 and the `__all__` pin are
  live, not merely present in source.
- `scripts/review_inspect.py` was **not** run: `docs/builder/BUILD.md`
  `### When to run the helper during build` scopes it to a build diff, and this pass reviews
  no diff. Repeated-literal evidence was not needed — the DRY question here was whether
  named promotions have two callers, which `grep` over the two flavor modules answers
  directly. Skip recorded per `worker-3.md` "Static helper use".

### Failability proofs

Not applicable, in both directions: this pass introduced no boundary (it mutates nothing),
and there is no Worker 2 build report in this artifact to audit — the cycle's audit slices
have no `built` predecessor. The `worker-3.md` re-run floor is therefore satisfied by an
empty re-run set, legally: "an empty re-run set is legal only when the diff introduces no
boundary that meets the floor," and there is no diff. The source carve-out was **not**
exercised; no production file was touched.

### Hot-path budget

Not applicable; `docs/builder/build-039-serializer_mutations-0_0_13.md`
`## Build-wide declarations` declares hot-path `none` for Slices 0–1d.

### Floor verification

Not applicable; the plan declares floor-verification scope `none` for Slices 0–2.

### Notes for Worker 1 (spec reconciliation)

Nine SPEC-STALE and three SUPERSEDED rows. **The three sentences to rewrite first**, in
order of how badly a reader is misled:

1. **The `register_subsystem_clear` row shape — three places say "static string rows".**
   `## Slice checklist`, the Slice 2 `register_subsystem_clear` sub-block: *"The list holds
   **static `(module_path, attr)` STRING rows only** … the serializer row is the literal
   `("…rest_framework.inputs", "clear_serializer_input_namespace")`, resolved lazily by
   `_clear_if_importable`"*; the same claim in `### Decision 6`'s cross-flavor paragraph
   (*"because every entry routes through `_clear_if_importable`, collapses the
   import-guarded-clear asymmetry"*); and again in `## Definition of done` item 3
   (*"one canonical list of static `(module_path, attr)` string rows both sites iterate via
   `_clear_if_importable`"*), plus the P1.6 table cell in
   `## Cross-flavor reuse and DRY obligations`. **All four must state the corrected contract
   directly, with no chronology** (`BUILD.md` `## Spec rationale extraction`): the seam takes
   a **zero-argument callable plus a stable `owner` string**, registered by the module that
   owns the state; `before_bind=True` selects the finalizer pre-bind subset; laziness comes
   from the fact that only an imported owner can register, so a DRF-absent build registers
   nothing; a string reference is **rejected** (`TypeError`). The "what changed and why"
   half — that string rows were replaced in `48f9f65d` (2026-07-11) to make a rename fail at
   the owner import instead of silently leaving stale state — belongs in
   `docs/SPECS/appx/spec-039-serializer_mutations-0_0_13-rationale.md` under Decision 6,
   never in the spec.

2. **"One net-new public symbol" is seven.** `### Decision 5` opening (*"One net-new public
   symbol, re-exported from `__init__.py` as a lazy export"*), its bullet list (a single
   `SerializerMutation` line), the `## Slice checklist` Slice 2 `__init__.py` sub-bullet
   (*"export `SerializerMutation` (one net-new public symbol)"*), and `## Definition of
   done` item 8 (*"The one net-new public symbol (`SerializerMutation`)"*) all need the full
   list: `SerializerMutation`, `register_serializer_field_converter`,
   `SerializerFieldConversion`, `describe_serializer_input`, `NestedSerializerConfig`,
   `SerializerHookContext`, `UploadMetadata` — **all seven** lazy through the root
   `__getattr__` under `require_drf()`, **all seven** deliberately absent from `__all__`.
   Six are already specified in the same document (rev6 #11, rev6 #15, rev6 #17, and the
   hook-context block at the head of the spec); Decision 5 is simply the one section that
   was never reconciled to them, which is exactly the failure mode this cycle exists to
   catch. Note that `## Definition of done` item 8's *substantive* clause — named lazy
   export, not in `__all__` — is correct and should survive the edit verbatim.

3. **The `_validate_meta` scope sentence understates the shipped validator by six checks.**
   The sentence appears twice, byte-near-identically — `## Slice checklist` Slice 2
   `**DRY / reuse**` sub-bullet and `### Decision 6`'s closing line: *"The serializer's only
   genuinely-new `_validate_meta` logic is the `serializer_class` is-a-`ModelSerializer`
   (+ resolvable `Meta.model`) check and `optional_fields` normalization."* At `HEAD` it also
   owns `Meta.injected_fields` (normalize + writable-basis + not-still-in-input),
   `Meta.select_for_update`, `Meta.nested_fields` (including the create()/update()-override
   requirement), the `get_serializer_for_schema()` field-map validation, the schema
   fingerprint capture, and the recursive writable-`source` ownership walk. The same edit
   should carry the allowed-key set (A8): the shipped
   `_ALLOWED_SERIALIZER_META_KEYS` is `MODEL_BACKED_WRITE_META_KEYS` (`fields`, `exclude`,
   `permission_classes`, `operation`, `select_for_update`) **plus** `serializer_class`,
   `optional_fields`, `injected_fields`, `nested_fields`.

Also owed, lower stakes, all no-code-change:

4. **P1.2's promotion target (D4).** `NON_DELETE_WRITE_OPERATIONS` and the single-sited
   reject message live in `mutations/operations.py`, not `mutations/sets.py`;
   `mutations/operations.py` post-dates the spec. Both the P1.2 table row and
   `### Decision 10`'s cross-flavor paragraph name the wrong module.
5. **P2.5 cites a deleted symbol (G4).** *"The serializer follows the **model** flavor's
   `mutations/sets.py::_normalize_field_sequence` precedent"* — that symbol no longer
   exists, nor does `forms/inputs.py::normalize_form_field_sequence`. All three flavors call
   `utils/inputs.py::normalize_field_name_sequence(..., flavor=…)`. Per `AGENTS.md` rule 27
   a rename must grep-sweep `::OldName`; this is a citer stranded by a rename that already
   happened, and the same sweep should catch `_VALID_FORM_OPERATIONS` (P1.2 table cell) and
   `_form_shape_build_cache` / `_shape_build_cache` (P1.3 table cell) if those are also gone.
6. **The `### Import manifest` row for `sets.py` (G7).** Three wrong entries
   (`_normalize_field_sequence`, `register_mutation`, `PARTIAL`) and thirteen missing shared
   `mutations/sets.py` symbols. It is the spec's *"DoD-checkable summary"*, so an
   under-specified manifest is a gate that cannot fire. Nothing shipped duplicates a listed
   symbol's logic, so this is documentation debt, not a violation.
7. **Decision 12's absence-simulation mechanism (F10).** The spec prescribes
   *"monkeypatching `builtins.__import__`"*; the suite uses the importlib-native
   `sys.modules["rest_framework"] = None` sentinel via `tests/_soft_dependency.py`, plus a
   fresh-subprocess check for the root-import half that the spec does not describe and that
   is strictly stronger. Worth stating the shipped mechanism, since the spec's paragraph is
   otherwise a precise, correct account of the eviction discipline.

8. **`Escalated:` (DRY existence challenge, maintainer decision, not a blocker).**
   `mutations/sets.py::cached_build_input` was promoted by P1.7 as a shared core, and
   `spec-039` itself rules the serializer out of it, so it has and will keep exactly one
   real caller (`forms/sets.py`). Its get-or-store spine is separately shared three ways via
   `utils/inputs.py::get_or_store_shape_build`. Resolution paths: **(a)** keep it — the
   guard-before-lookup ordering deserves a named, documented home even at one caller, and
   `spec-039` P1.7 is the record of that intent; **(b)** inline it into `forms/sets.py` and
   move the ordering rationale into that call site's comment, deleting one indirection.
   I recommend (a) and raise it only because `worker-3.md` `### The existence challenge`
   asks the question when the grounds exist — a promoted helper with one caller. No row is
   held on this.

### Review outcome

`review-accepted`. This pass has no `built` predecessor and hands to Worker 1 for Slice 2
spec reconciliation. **Zero DROPPED rows: no Slice-2 behavior `spec-039` planned is missing
from `HEAD`.** All twelve non-conformant rows are spec-side staleness with correct code
behind them, so no Slice 3 code pass is owed *from this slice's population*; the single
`.py` edit worth making is the Low-severity stale comment above, which Worker 1 may fold
into Slice 2's citer sweep rather than opening `bld-039-slice-3-code_gaps.md` for it alone.
