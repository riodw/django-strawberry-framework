# Build: Final test-run gate — filters / 0.0.8 (027)

Spec reference: `docs/SPECS/spec-027-filters-0_0_8.md` (whole file; the gate pass edited nothing, and one post-final spec correction is recorded under `### Spec changes made (Worker 1 only)`)
Status: final-accepted

## The one thing the maintainer must act on outside this cycle's fence

**`docs/TREE.md` line 859 still renders `(spec-021)` for `examples/fakeshop/apps/library/filters.py`.** That doc is script-rendered from module docstrings; Slice 2 corrected the docstring (it now opens `"""FilterSet declarations for the library acceptance app (spec-027).`, and `grep -rn 'spec-021' --include='*.py' .` returns **0** tree-wide), but the doc was never re-rendered. `spec-021` today names a **different** card — `docs/SPECS/spec-021-apps-0_0_7.md`, the `AppConfig` card — so the rendered row is actively wrong, not merely stale.

The fix is one command, `uv run python scripts/build_tree_md.py`, and it is the maintainer's for two reasons this cycle cannot overrule: `docs/TREE.md` is fenced out of this cycle by the maintainer's scope, and [`START.md`][start] `## Concurrent sessions` warns against regenerating a rendered doc while another session's feature work is mid-flight. **Never hand-edit it** — the next render reverts a hand edit.

Two smaller maintainer items travel with it and are in the catalog below: `KANBAN.md` line 5256 (`Ref: spec-021 pre-merge review M-filters-3 / H-filters-3`, a DB-backed row, so an ORM edit plus a regenerate), and `README.md`'s missing `filter_input_type` (catalog item 10 — the cycle's one genuinely undischarged `## Doc updates` obligation).

## Gate (Worker 1)

### Spec status-line re-verification (Worker 1, every spawn)

Lines 1-9 of `docs/SPECS/spec-027-filters-0_0_8.md` re-read at the start of this pass. `Status: shipped (`0.0.8`)` naming card `DONE-027-0.0.8`, the `Target release` line, `Owner`, the `Predecessors` list (with its `all `shipped (0.0.8)`` glossary clause), and the rationale-companion pointer sentence all still describe the build's current state. **No status/header edit owed, and none made.**

### Baseline

`HEAD` is `5c6fdd71` at the start of this pass and `5c6fdd71` at its close — the fourth consecutive pass during which it did not move. `git status --porcelain` reads **21 ` M` + 7 `??`**, byte-for-byte the roster the prior three passes recorded, and this pass added nothing to it but its own artifact. No `git stash` / `git checkout` / `git restore` / `git worktree` was used anywhere; every `HEAD` reading is `git show HEAD:<path>` read into memory or into a scratch path outside the repository.

### Gate results

| # | Gate | Command | Result |
|---|---|---|---|
| 1 | Full sweep, all three test trees | `uv run pytest --no-cov` | **PASS** — `6179 passed, 40 skipped in 133.77s`, exit **0**. No `--cov*` flag; `--no-cov` is required because `pytest.ini`'s `addopts` auto-applies `--cov`, and no line-coverage figure was inspected or asserted |
| 2a | Django system checks | `uv run python examples/fakeshop/manage.py check` | **PASS** — `System check identified no issues (0 silenced).` exit **0** |
| 2b | Migration drift | `uv run python examples/fakeshop/manage.py makemigrations --check --dry-run` | **PASS** — `No changes detected`, exit **0** |
| 3a | Format, read-only | `uv run ruff format --check .` | **PASS** — `425 files already formatted`, exit **0**. Never `--fix` |
| 3b | Lint, read-only | `uv run ruff check .` | **PASS** — `All checks passed!`, exit **0**. Never `--fix` |
| 3c | Whitespace / conflict markers, whole tree | `git diff --check` | **PASS** — no output, exit **0** |
| 4 | Floor verification | — | `No floor-verification scope declared.` See below |
| 5 | Spec glossary | `uv run python scripts/check_spec_glossary.py --spec docs/SPECS/spec-027-filters-0_0_8.md` | **PASS** — `OK: 48 terms - all have glossary entries and at least one spec link.` exit **0** |
| 6 | Citation gate | `uv run python scripts/check_citations.py` | **PASS** — `OK: 742 citations resolve (665 in 422 .py files, 77 in KANBAN.md).` exit **0** |
| 7 | Source layout / ASCII-only, whole tree | `uv run python scripts/check_trailing_commas.py --check` | **PASS** — exit **0** |

**Nothing failed, so nothing routed back through a slice loop.**

**Gate 6's `.py` half is the half this cycle could move, and it did not.** `665 in 422 .py files` is the identical figure every pass in the consolidation cohort recorded from pass 2 onward. The `KANBAN.md` half reads **77**, up from the 75 the integration pass and Slices 1-3 saw; `KANBAN.md` is fenced out of this cycle and no pass in it wrote a `.py` file after pass 5, so the +2 is a concurrent session's board work, not this cycle's.

### Why gate 1 is the backstop and not a formality

[`BUILD.md`][build] names two staleness classes a focused run structurally cannot see — `### Example-project schema changes must sync every schema-module list` and `### Test staleness a focused run cannot see`. Both hide the same way: the stranded file is not in the diff, so neither per-pass diff review nor any focused scope executes it. This cycle changed no model field and no wire shape, so the prediction was that neither class can bite. **The full parallel sweep is what makes that a measurement rather than an assumption**, and it returns `6179 passed, 40 skipped` with zero collection errors — so the order-dependent fakeshop schema-registry `DuplicatedTypeName` / `LazyType KeyError` class did not appear either.

### Floor verification

`No floor-verification scope declared.`

The reason, stated rather than left blank: [`build-027-filters-0_0_8.md`][plan]'s preamble declares `Floor-verification scope: none`, and the ground for that declaration is that **no slice in this cycle touches a Django / Strawberry / channels integration seam, because no slice changes an executable statement.** Slices 1 and 3 and the integration pass wrote only `.md`; Slice 2 and the consolidation cohort wrote only comment and docstring text. Every pass in the cycle recorded the same literal, no pass owed a floor run, none was run, and this gate builds no floor venv.

### Executable-token identity, all 20 modified `.py` files, re-derived by this pass

The load-bearing claim of the whole cycle is that it changed no executable statement. This pass re-ran it with its own instrument rather than reading a prior pass's verdict: `tokenize` each file, drop `COMMENT` / `NL` / `NEWLINE` / `INDENT` / `DEDENT` / `ENCODING` / `ENDMARKER` and every statement-position `STRING`, and compare the remaining `(type, string)` sequence against `git show HEAD:<path>` read into memory.

**IDENTICAL for all 20; divergent files: 0.** Token counts, for the record: `filters/base.py` 2158, `filters/factories.py` 232, `filters/inputs.py` 2759, `filters/sets.py` 7348, `types/finalizer.py` 5567, `utils/inputs.py` 5397, `examples/fakeshop/apps/kanban/filters.py` 1054, `apps/library/filters.py` 486, `apps/library/filters_genre.py` 67, `apps/products/filters.py` 257, `test_query/test_library_api.py` 32852, `test_query/test_products_api.py` 16685, `tests/filters/test_factories.py` 2818, `test_finalizer.py` 3923, `test_inputs.py` 5894, `test_sets.py` 35233, `tests/types/fixtures/branch_module.py` 67, `fixtures/shelf_module.py` 67, `tests/types/test_base.py` 7905, `test_definition_order.py` 5210.

**The instrument was proved failable before its zero was trusted.** Run unchanged against a mutated `HEAD` copy of `filters/factories.py` (`FilterArgumentsFactory` -> `MUTANT`, one executable token) it reports **DIVERGENT**. So the 20 identical verdicts are a measurement, not a silence.

This is also the attribution evidence: had gate 1 failed, the failure would have pointed away from this cycle by construction, because a diff with no executable token cannot change a test outcome. It did not fail, so the point stands unused — recorded because the honest form of "not ours" is evidence, not a disclaimer.

### What the cycle delivered

Byte and line figures measured with `wc -c -l` at the moment this section was written; the `HEAD` baseline from `git show HEAD:<path> | wc -c -l`.

| File | `HEAD` (`5c6fdd71`) | Now | Delta |
|---|---|---|---|
| `docs/SPECS/spec-027-filters-0_0_8.md` | 324,436 bytes / 1,303 lines | **258,176 bytes / 1,117 lines** | -66,260 bytes / -186 lines |
| `docs/SPECS/appx/spec-027-filters-0_0_8-rationale.md` | did not exist | **149,253 bytes / 772 lines** | +149,253 bytes |

Never `len(text)` — that instrument diverges from `wc -c` by roughly 1,900 bytes on this cycle's em-dash-heavy prose, an error the cycle already paid for once.

In substance:

- **Slice 1** created the archive's last missing `-rationale.md` companion and MOVED the deliberative layer into it: the whole `rev1`-`rev8` inline review history, all twelve `Justification:` blocks, all twelve `Alternatives considered (and rejected):` lists, and the review-round narration welded into the contract prose (248 `rev[0-9]` tokens, 43 `rev-[0-9]`, 101 `adversarial review` at `HEAD` -> 2, 0, 0). A cut, proved by spec-side zero-counts rather than by comparing file sizes.
- **Slice 2** retired every citation and provenance defect in this card's owned `.py` surface across 19 files and ~59 edit sites: 30 pre-renumber card ids, 17 raw spec line numbers deleted rather than renumbered, 20 build-process provenance tokens at 19 sites, and one mangled docstring.
- **Slice 3** rewrote the spec so it states the current contract, closing D2-D11 plus six further defects two whole-population instruments found that no finding had named.
- **The integration pass** found the seam no slice owned — a `#"unique substring"` citation from `.py` into the spec, gated by nothing — and the mirror-direction defect of the spec citing a `GOAL.md` sentence this card's own Slice 5 had removed.
- **The consolidation cohort** (five build passes, four reviews, two Worker 1 final verifications) repaired seven `.py` citation sites and, at its close, four spec edits and three rationale edits.

**The load-bearing verification result: no code gap.** Worker 0 verified every contract-bearing claim the spec makes against `HEAD` before dispatch — every module, primitive, classmethod, named helper, `Meta`-key promotion, finalizer subpass, fakeshop declaration and all 14 contracted live HTTP tests are present in shipped source — and this cycle changed no executable statement on top of that. The whole product of the cycle is documents and comments.

## Deferred work catalog

**The next spec author's reading list.** It inherits the 17-item consolidated list in [`bld-slice-4-027-broken_substring_citations.md`][slice-4] `## Final verification (Worker 1, pass 2)` and **re-derives every item as it transcribes** — a catalog is a claim, not a measurement, and this cycle's catalogs have now been wrong at least nine times. Four items are corrected below in their **subject**, not their count.

Nothing here is `revision-needed` material: every item is a fenced file, another card's tree, an already-carded cluster, or a class whose population no instrument in this repo can close.

1. **A citation split across a line break, invisible to every line-bounded instrument. Two forms.**
   - **Form A, a wrapped `#"substring"`: six sites package-wide.** Re-derived here by a per-line census over all **108** package `.py` files, flagging every `#"` with no closing `"` after it on the same line: `consumers.py` **887**, `filters/factories.py` **15** and **148**, `routers.py` **148**, `types/finalizer.py` **1383**, `types/relay.py` **143**. Independently reproduces the inherited six, site for site. The three inside this cycle's diff are byte-identical to `HEAD` over their line ranges (line-range compare against a `HEAD` copy read into memory, all three IDENTICAL), so none is this cycle's.
   - **Form B, a `spec-NNN` token whose qualifier sits on the next line — carded with an exemplar and no count.** **Correction to the inherited item, which pinned the population at 44:** an instrument matching every line that *ends* in a `spec-NNN` token reports **58** across 31 package files. Two vocabularies, two numbers, and neither is the population — the inherited 44 required a qualifier on the following line, mine does not. Form B is **not a defect**; it is what ordinary comment wrapping at 99 columns produces and every instance reads correctly. It is an **audit hazard**, and the exemplar is `types/finalizer.py:1225-1226`, which reads `...per spec-027` / `Decision 9).` and is why two prior artifacts pinned a box at "line 1226 carries `spec-027 Decision 9`" when `grep -n 'spec-027 Decision 9'` over that file returns **nothing** (re-confirmed here, exit 1).
   - **The audit step is the deliverable, not a number.** Any sweep of a `spec-NNN <qualifier>` pointer must flatten before matching. `scripts/check_citations.py`'s `CITATION_RE` is line-scoped and puts `docs/` out of scope by design, so it reports `OK: 742` with every Form-A wrap live; no existing instrument in this repo measures either form. *Source: [`bld-slice-4-027`][slice-4], Worker 3 pass 1 item 5 / pass 2 item 3 / pass 4 item 5.*
2. **`spec-011 Decision 4` / `Decision 7` cited at eight `.py` sites when `spec-011` has no Decisions — ALREADY CARDED, do not re-card.** Re-derived by `grep -rn 'spec-011' --include='*.py' .`: **8** occurrences across 4 files — `types/base.py` **1041, 1043, 1174, 1778, 1899**, `types/resolvers.py` **558**, `tests/types/test_base.py` **495**, `tests/filters/test_sets.py` **451**. `docs/SPECS/spec-011-stale_placeholder_cleanup-0_0_4.md` is **53** lines with `grep -cE '^### Decision '` -> **0**; the real referent is `spec-015-relay_interfaces-0_0_5.md`. **Homed on two live cards, both re-confirmed on the board here:** `KANBAN.md` line **249** (card `TODO-ALPHA-051-0.0.15`, heading line 211) owns the eight live-code / test occurrences with per-file counts, the retarget target, an explicit "do not widen into a documentation sweep" boundary and a recorded re-derivation trap (`git grep -oh '\[spec-011\]' | wc -l` reports **9**; the ninth row is git's `Binary file examples/fakeshop/db.sqlite3 matches`); `KANBAN.md` line **353** (card `TODO-ALPHA-052-0.1.0`, heading line 315) owns the documentation half, 43 occurrences across 13 files. **This entry exists only to stop a duplicate filing.** *Source: [`bld-slice-4-027`][slice-4], Worker 3 pass 4 L1.*
3. **`django_strawberry_framework/types/base.py:122` names a build **process** phase in shipped source.** Re-derived present: the comment spans lines 120-122 and reads `byte-identical at three compose sites (3rd-copy hoist, spec-032 integration pass)`. A spec Decision pointer is the KEEP class; a build-cycle phase name is the banned class. Another card's file, in no cohort's partition. *Source: [`bld-slice-4-027`][slice-4], Worker 3 pass 4 item 6.*
4. **Card-less prose pointers naming a real section of an unnamed spec: 14 sites, enumerated rather than counted.** Every one re-read here and present: `orders/__init__.py:60`, `orders/factories.py:103`, `orders/inputs.py:222`, `orders/inputs.py:281`, `orders/inputs.py:293`, `orders/sets.py:13`, `orders/sets.py:120`, `orders/sets.py:318`, `orders/sets.py:451`, `orders/sets.py:461`, `rest_framework/__init__.py:13`, `rest_framework/resolvers.py:834`, `rest_framework/sets.py:48`, `routers.py:71`. They spell `Spec Decision 5`, `Spec DoD 4(c)`, `the spec D8 step-4`, `the spec's Risks`. **Every one resolves** to a real section of its own spec, so none is a false citation — the defect is only that the reader must infer which document. Enumerated because a count over a multi-grep union rots the moment either grep's vocabulary changes. *Source: [`bld-slice-4-027`][slice-4], Worker 2 pass 5 item 3.*
5. **Bare `Decision N` references whose card attribution is ambiguous — a class of attribution, not of count.** Most belong to other cards, so the population cannot be swept by number, only resolved site by site. Two exemplars, both re-derived here: `optimizer/extension.py` carries `Decision 11` at **671**, **991**, **1081** and **1499** (the inherited item, before an earlier correction, named line 1053, which carries no such reference); 1081 sits in a docstring whose only `spec-NNN` mention is `spec-035`, which has Decisions 1-9, so a reader following the nearest card lands on a Decision that does not exist. The referent worth writing into the card is `spec-030-connection_field-0_0_9.md` `### Decision 11 — The connection field owns its optimizer cooperation point`, which is what line 1499's sentence is about; the fix is to spell `spec-030 Decision 11` at all four, and it belongs to the connection / optimizer card. Second exemplar: `utils/inputs.py` **1708** and **1733**, both `no operator bag, Spec Decision 8`, meaning **spec-028**'s Decision 8, not this card's. *Source: [`bld-integration-027.md`][integration] item 5; [`bld-slice-4-027`][slice-4], Worker 3 pass 4 item 6.*
6. **Raw source-line and spec-line references, which [`AGENTS.md`][agents] rule 27 allows only in per-cycle scratchpads — and the package-source half is ALREADY CARDED.** **This is the item's load-bearing correction, and it is a disposition rather than a number:** `KANBAN.md` line **357** (card `TODO-ALPHA-052-0.1.0`) already carries "the 8 raw `Decision N line NN` refs in package source violating AGENTS.md rule 27 (the live-code halves coordinate with the DRY-squeeze card's WP batches, which already own this shape)". The inherited item framed the whole class as un-homed. Enumerated here so a future author reconciles against the card rather than filing a duplicate; **no total is asserted**, because the card's figure of 8 and this enumeration are two instruments and the total has now moved for three different reasons.
   - **Spec / doc line refs in package comments (the carded half):** `optimizer/walker.py:836` (`Decision 4 / edge case line 315`), `mutations/sets.py` **503, 508, 671, 1339**, `orders/inputs.py` **167, 227**.
   - **Raw line refs into this repo's own source:** `mutations/fields.py:32` and `mutations/resolvers.py:1150`, both `` (``relay.py`` line 287) ``.
   - **Refs into the external upstream cookbook, not carded above:** `orders/sets.py` **94, 179, 256, 312** (`cookbook lines 30-38`, `265-285`, `279-280`, `115-170`). Expressible as `path::Symbol` against the vendored upstream tree, so rule 27 reaches them, but they are not refs into this repo.
   - **`examples/fakeshop/test_query/test_products_api.py` lines 2948, 2984, 3015, 3051, 3098**, all re-derived present, all spelled as bare `(line 388)` / `(mirror line 493)` inside that file's `036` mirror block — so a sweep by the token `spec-036` finds none of them.
   - **Not a member, and the instrument that manufactured it is worth remembering:** `_strawberry_patches.py:218` is the `#L45-L52` fragment of a **single** GitHub permalink pinned to a commit sha (re-read here), an upstream reference; an `L[0-9]` alternative in a sweep pattern is what turned it into two phantom raw line refs. *Source: [`bld-integration-027.md`][integration] items 6-7.*
7. **The same broken / ungated citation class in other cards' trees.** Confirmed live by reading, in ten files: `orders/factories.py`, `mutations/resolvers.py`, `mutations/inputs.py`, `mutations/sets.py`, `rest_framework/{resolvers,serializer_converter,sets,inputs}.py`, `forms/inputs.py`, `types/base.py`. Two exemplars re-derived here: `rest_framework/serializer_converter.py:740` #"``annotate_queryset_relation`` after" (a bare `M3:` review-finding id) and `rest_framework/inputs.py:1250` #"even when DRF ``required=True``" (a bare `H3`). Every one belongs to another card; the count is instrument-dependent for the same reason as items 4-6. *Source: [`bld-integration-027.md`][integration] item 7, [`bld-slice-2-027`][slice-2] out-of-scope table.*
8. **History-narrating prose in `.py` comments — carded with an exemplar and NO count.** Three instruments over the same file set disagree: ~65 across 15 files, 54 across 11, 46 across 11. All three sweep vocabularies include legitimate contrast prose (a docstring saying what a fixture is *not* is not build provenance), so no number is a population. Confirmed exemplar all three agree on, re-derived here at `django_strawberry_framework/filters/inputs.py:593` — `::_encode_global_id_input` #"The previous implementation eagerly decoded the object". *Source: [`bld-integration-027.md`][integration] item 4.*
9. **`docs/TREE.md` line 859 renders `(spec-021)` and `KANBAN.md` line 5256 carries a second live `spec-021` reference to this card. Both FENCED — maintainer items, never fixes here.** Re-derived: `docs/TREE.md:859` still reads `# FilterSet declarations for the library acceptance app (spec-021).`; the feeding docstring at `examples/fakeshop/apps/library/filters.py:1` reads `spec-027`; `grep -rn 'spec-021' --include='*.py' .` returns **0**. `KANBAN.md:5256` reads `Ref: spec-021 pre-merge review M-filters-3 / H-filters-3.` and is DB-generated, so it is an ORM edit plus a regenerate, not a text fix. Stated in full at the top of this artifact. *Source: [`bld-slice-4-027`][slice-4], Worker 2 pass 1 and Worker 3 pass 1 item 2.*
10. **`README.md` carries zero occurrences of `filter_input_type`. FENCED.** Re-derived: `grep -c 'filter_input_type' README.md` = **0**, against a `## Doc updates` bullet that names it alongside `FilterSet` / `RelatedFilter` / `Meta.filterset_class` (all three present at the `0.0.8` line) and argues explicitly why the helper belongs there. **The cycle's one genuinely undischarged `## Doc updates` obligation**, found only by reading a surface a prior slice had declared unexamined. *Source: [`bld-integration-027.md`][integration] item 10. Licensing spec line: the `## Doc updates` `README.md` bullet — which licenses the work, not the deferral.*
11. **`docs/SPECS/spec-055-search_fields-0_1_2.md` carries three wrong references to this card. FENCED (another card's spec).** Re-derived: lines **29** and **195** both attribute `construct_search` / `LOOKUP_PREFIXES` to "spec-027 Decision 3 Layer 5" — the identical false attribution this cohort repaired at both live `.py` sites, and `spec-055` is the document the next author copies from, so it is the propagation source. Line **200** quotes the staged anchor as `TODO(spec-027-filters-0_0_8 Meta.search_fields card 0.1.2)`, wrong in the id (pass 1 retargeted the real comment to `TODO(spec-055 Slice 1)`, re-confirmed at `filters/sets.py:1369`) **and** already wrong at `HEAD` in the `card 0.1.2` suffix the comment has never carried. *Source: [`bld-slice-4-027`][slice-4], Worker 3 pass 1 item 1.*
12. **Four `test_clear_tolerates_unimportable_*` docstrings describe a retired mechanism.** Re-derived: `tests/test_registry.py` definitions at **1617** (`_filter_submodules`), **1651** (`_order_submodules`), **1687** (`_connection_submodule`), **1721** (`_relay_module`), and `grep -c 'except ImportError' django_strawberry_framework/registry.py` = **0**. All four describe the cycle-safe local-import guards `register_subsystem_clear(...)` replaced; each test still proves something real (poisoning `sys.modules` leaves the registry's own clear undisturbed), so this is docstring rot, not a dead test. **Only the `filter` one belongs to this card**; the others name spec-028 / spec-030 / spec-032. *Source: [`bld-integration-027.md`][integration] item 2.*
13. **The PEP-563 deferred-annotation path for `filter_input_type` has no dedicated test.** Re-derived, **and the claim is scoped to `.py` deliberately**: `grep -rn 'test_filter_input_type_under_future_annotations' --include='*.py' .` returns **0**, while `test_filter_input_type_is_idempotent_under_repeated_calls` — the repeat-safety property PEP 563 depends on — exists. A coverage boundary, not an untested contract; the spec's `## Test plan` says so in as many words. **Correction carried forward and now demonstrated twice:** the original item claimed "zero hits tree-wide", the prior catalog corrected it to 3, and the tree-wide figure is **4** as this sentence is written. The claim is self-falsifying by construction — writing it down populates the grep — so no tree-wide number belongs in it at all. *Source: [`bld-integration-027.md`][integration] item 3. Licensed by the `## Test plan`'s own sentence.*
14. **[`bld-integration-027.md`][integration] `### Citation audit` mis-attributes the second `Partial-finalize recovery` occurrence to Decision 11. Record correction only.** Re-derived at the current spec by `grep -n '^### Decision '` at the moment of use: the occurrence is spec line **839**, under `## Edge cases and constraints` (line **815**); Decision 11 spans **714-787** and Decision 12 opens at **788**, so line 839 is past both and Decision 11 carries zero occurrences. The first occurrence is line **699**, inside Decision 9 (659-703). The disposition that sentence supports — drop the substring from the `types/finalizer.py` citation — is unaffected, since the substring occurs twice either way. A worker may not edit a prior artifact. **A Decision's line bounds are a measurement with a shelf life of one spec edit.**
15. **The `## Non-goals` auto-generation sentence is already carded — do not re-derive it as new rot. Correction: name the right owner.** The inherited item pointed at `TODO-ALPHA-051-0.0.15`. Both cards are involved and the split matters: **`TODO-ALPHA-052-0.1.0` owns the scrub** (`KANBAN.md` line **357**, inside its repo-wide "does any archived spec's `0.0.X` deferral have a card?" sweep, which names "`spec-027`'s 'lands when `DjangoConnectionField` ships in `0.0.9`' auto-generation sentence" explicitly), while **`TODO-ALPHA-051-0.0.15` line 251 carries the WP-D contract question the wording depends on** (are both dynamic-set factories dead code or a deliberately symmetric skeleton?). Acting on either half here would pre-empt a decision this cycle does not own. *Source: [`bld-integration-027.md`][integration] item 9.*
16. **`[fakeshop-test-library-reload]` resolves to two different files across the spec / rationale pair, deliberately.** The spec's def points at `conftest.py` (Slice 3's fix); the rationale's still points at `test_library_api.py`, because its only use there is the verbatim `rev7` entry whose subject is the claim naming that file. Recorded so a future sweep reads it as a decision rather than an unfinished fix. *Source: [`bld-integration-027.md`][integration] item 12.*
17. **Worker 3 pass 4 deleted a stray out-of-tree-basename shadow pair, and the deletion holds — discharged, not deferred.** Re-verified: `ls docs/shadow/` carries the canonical `django_strawberry_framework__filters__factories.{overview.md,stripped.py}` pair and **no** bare `factories.*` duplicate, so a cross-file `## Repeated string literals` comparison cannot double-count that file's literals. (The directory now also holds `__filters__inputs` and `__types__finalizer` overviews the later cohort passes generated, plus the concurrent `scalars` session's; `docs/shadow/` is gitignored and regenerable, so that growth is expected and is not this item.)

**Items 18-21 were added after this artifact first reached `final-accepted`, by the post-final residual cohort of 2026-08-20** ([`build-027-filters-0_0_8.md`][plan] `### Post-final residual cohort`). Items 1-17 are the catalog as the gate closed it; a reader must not take that state as the cycle's complete deferral set. Every item below is re-derived here, not transcribed.

18. **Three `tests/test_registry.py` docstrings describe a retired `TypeRegistry.clear` mechanism, on three other cards' surfaces.** `::test_clear_tolerates_unimportable_order_submodules` (line 1655, spec-028), `::test_clear_tolerates_unimportable_connection_submodule` (1691, spec-030), and `::test_clear_tolerates_unimportable_relay_module` (1725, spec-032) each claim `clear()` carries `except ImportError` guards and "skips that block". All three are stale by one proof: `grep -c 'except ImportError' django_strawberry_framework/registry.py` returns **0**, and an AST walk of `TypeRegistry.clear` (lines 580-607) finds **0** `Import` / `ImportFrom` nodes — `clear()` replays callbacks resolved at each owner's own import time, so there is no block to skip. All three tests pass and each still pins something real (the registry's own state drops either way), so this is prose, not behavior. Fenced out of this cycle (other cards' surfaces). **Target `TODO-ALPHA-051-0.0.15`**, which already collects measured stale test docstrings. *The filter twin at line 1617 is NOT in this population — the sibling cohort repaired it in-fence.*
19. **The order twin's cross-reference now points at a test describing the opposite mechanism — a mechanism divergence, not a dangling name.** `::test_clear_tolerates_unimportable_order_submodules`'s docstring opens "Order twin of ``test_clear_tolerates_unimportable_filter_submodules``" and then describes "cycle-safe local imports per spec-028 Decision 9", while the repaired filter twin now opens "``clear()`` itself imports nothing, so a broken ``sys.modules`` cannot break it". The referenced name still resolves, so no grep or citation gate can see the divergence; only reading both docstrings against each other can. This cycle created the seam knowingly rather than reaching outside its fence. **Target `TODO-ALPHA-051-0.0.15`**, alongside item 18 — the twin's cross-reference must be re-read when its docstring is corrected, not merely renamed.
20. **`tests/orders/test_inputs.py::test_clear_order_input_namespace_tolerates_unimportable_submodules` (docstring lines 904-911) carries FOUR distinct defects, and a reword closes none of them but (b).** *This entry corrects its own first draft in this catalog, which read "Low, stale `from ... import ...` spelling… the split across the four twins is 3 + 1". That description was true only of the retired-mechanism axis and licensed a wording tweak that would leave three defects standing.*
    - **(a) Location misattribution — stated more strongly here than on the filter side.** The docstring claims the poisoning exercises "BOTH ``except ImportError: pass`` guards **in** ``clear_order_input_namespace``". `grep -c 'except ImportError' django_strawberry_framework/orders/inputs.py` = **0** (`filters/inputs.py` = **0** as well). The guards are real but single-sited in the shared substrate — `utils/inputs.py::clear_generated_input_namespace` → `_safe_import` → `utils/imports.py::import_attr_if_importable` — so the premise does fire and the test pins something real; the claim about *where* the guards live is false.
    - **(b) The `from ... import ...` spelling**, which never executes on the poisoned path because the substrate imports by module path via `importlib.import_module`. It appears in the docstring (line 909) **and** the inline comments (lines 918-919), so even the wording half is two sites, not one.
    - **(c) Three raw `path:NN` refs in a `.py` docstring — this cycle's own rule-27 / D13 class — of which none can resolve.** `` Closes ``inputs.py:461-462`` and ``inputs.py:476-477`` `` names a bare basename ambiguous across five files in this repo (`orders/inputs.py` **388** lines, `filters/inputs.py` 1000, `utils/inputs.py` 1735, `tests/orders/test_inputs.py` 929, `tests/filters/test_inputs.py` 1579), and under the only natural reading — the module under test — **both cited ranges sit past EOF**. `scripts/check_citations.py` is `path::Symbol`-only, so it sees none of them.
    - **(d) A rotted cross-file range whose symbol half still resolves.** `(lines 1009-1028)` is cited for `tests/filters/test_inputs.py::test_clear_filter_input_namespace_tolerates_unimportable_submodules`, which is at line **1417**; line 1009 currently lands inside `::test_range_input_type_name_is_scoped_per_filterset`. The resolvable symbol is exactly what hides the range's rot.
    - **Scope the file, not the function.** `tests/orders/test_inputs.py` carries **11** raw refs across **8** docstrings: eight `` Closes ``inputs.py:NN`` `` refs (lines 747, 761, 778, 794, 823, 873, and two at 904), of which **three are past `orders/inputs.py`'s 388-line EOF** (410, 461-462, 476-477); two bare cross-file `(lines NN-MM)` ranges (877, 908), **both wrong** — 877's target `::test_iter_filterset_subclasses_dedupes_diamond_inheritance` is at **1450**, not 1036-1056; and one raw spec-line ref at line 62 (`spec-028 Decision 5 lines 525-532`).
    - **Defects (c) and (d) are NOT shared with the siblings homed above, measured rather than assumed.** An AST walk over the docstrings of all five `tests/test_registry.py` `..._tolerates_unimportable_*` functions returns **0** raw `path:NN` or line-range refs. So the **3 + 1** split holds only on the retired-mechanism axis; by total defect count this site is the heaviest of the four, not the lightest.
    - **Target `TODO-ALPHA-051-0.0.15`.** Spec-028's surface: this cycle describes it and does not touch it.
21. **Two deliberate non-repairs from this cohort's own spec work, stated as claims with their measurements.** (a) Three occurrences of the retracted `(name, filterset_class)` ledger spelling survive in [`spec-027-filters-0_0_8-rationale.md`][spec-027-rationale] — that file's `## Revision history` rev-2 Pass-1 entry, its Decision 6 `### Justification (moved from the spec)` bullet (twice in one sentence), and its Decision 9 `### Changes this Decision underwent` rev-2 H5 entry. All three are moved verbatim review-round text; rewriting them would falsify the history the file exists to hold, so they stay and this entry is the record of that choice. The one non-historical occurrence, Decision 9's `Contract that stays:` summary line, was corrected. (b) No sibling routing is owed for the ledger spelling: `git show HEAD:docs/SPECS/spec-028-orders-0_0_8.md` carries **0** `(name, orderset_class)` occurrences and records the identical correction as its own review finding M1, so the order card closed this class in-cycle. Nothing from the `## Test plan` mis-attribution is left open — all nine symbols and all nine named tests in that bullet were re-measured, one entry of nine was false, and it was moved to the surface that covers it.

**Two items on the residual cohort's own deferral list were re-checked and are DISCHARGED, so they are deliberately absent from 18-21 rather than overlooked.** (i) The cohort's item 5 — spec [`### Decision 9`][spec-027-d9]'s `delattr` bullet, whose stated consequence was that "a reader following either citation lands on the false bullet" — is closed by item 21's own edit: `grep -n delattr docs/SPECS/spec-027-filters-0_0_8.md` now returns exactly **one** hit, line **672**, and that hit is the **negation**, inside the bullet titled "no clear path strips them". (ii) The cohort's item 6 — the plan's post-table sentence "Site 1 pins that `clear()` imports nothing" — is closed at [`build-027-filters-0_0_8.md`][plan] line **185**, which now reads "Site 1 pins that `clear()`'s **own body** imports nothing, so the only route a poisoned `sys.modules` can reach is a replayed callback", with the probe measurement attached. **A catalog carrying a discharged item is the same defect class as one understating a live item** — item 20 above was the second kind, these two would have been the first.

**Closed, so the gate does not carry them forward as open:** the three broken `#"substring"` citations landed in cohort passes 1-2; the `filters/inputs.py` `LOOKUP_PREFIXES` attribution in pass 3; the two `inputs.py` Lows in pass 4; the never-existent-**name** class in pass 5 with `filters/factories.py:95`; the staged anchor was retargeted in pass 1 (its `spec-055` half is item 11); and Decision 3's Layer 5 bullet, Decision 4's two under-enumerations, the seven-site `normalize_input_value` signature spelling and Decision 6 subpass 4's return contract are all discharged by the cohort's closing spec edits.

## Final verification (Worker 1)

- **Build-plan checklist:** the five preceding items are `- [x]` (Slices 1-3, the integration pass, the consolidation loop). This gate is the sixth and last; Worker 0 marks it.
- **DRY check across the whole cycle:** no new duplication is possible. Executable-token identity against `HEAD` holds for all 20 modified `.py` files under this pass's own instrument, proved failable first — a diff with no executable token can introduce no logic, literal, helper, or near-copy.
- **Existing tests still pass:** the full sweep, `6179 passed, 40 skipped`, exit 0. No `--cov*` flag anywhere in this pass.
- **Spec reconciliation:** one owed, and discharged here rather than by the gate's own commands. The gate commands exposed no spec defect and the status/header re-verification found nothing falsified, but "none owed" was a claim about the whole spec that only the gate's instruments could support: the post-final residual cohort measured a false mechanism claim in [`### Decision 9`][spec-027-d9] that no citation, provenance, or `#"substring"` sweep in this cycle could see. It is corrected in the spec and recorded under `### Spec changes made (Worker 1 only)` below. **A gate that finds nothing has measured its own commands, not the document** — the scope of a `none owed` line is the scope of the instrument that produced it.
- **Floor verification:** `No floor-verification scope declared.`
- **Final status: `final-accepted`.**

### Summary

The gate is green on every command: the full three-tree sweep (`6179 passed, 40 skipped`), both Django consistency checks, all three read-only lint/format/diff checks, and the three gates this document-and-comment cycle owes itself (spec glossary 48 terms, citations `742` with the `.py` half unmoved at `665 in 422`, source layout tree-wide). No floor scope was declared and none was owed. The cycle delivered the archive's last missing rationale companion at **149,253 bytes** and a spec rewritten from a chronology into a contract at **258,176 bytes** against a `HEAD` baseline of **324,436**, plus ~59 citation and provenance repairs across 19 `.py` files and seven more in the consolidation cohort — with **no code gap**: the spec's contract was verified present in shipped source before dispatch, and this cycle changed no executable statement, proved by token identity for all 20 modified files. The deferred-work catalog carries **17** items as the gate closed it, and **21** after the post-final residual cohort homed its four (items 18-21); re-deriving them as they were transcribed corrected four in their subject — Form B's population is instrument-dependent (44 under one vocabulary, 58 under another) so the class is now carded with an exemplar and no count; the package-source half of the raw-line-ref class turns out to be **already carded** on `TODO-ALPHA-052-0.1.0`; the `## Non-goals` auto-generation scrub is owned by `052` and only *gated* by `051`'s WP-D; and the PEP-563 item's tree-wide figure moved again (3 -> 4) purely because the catalog keeps writing it down, so the claim is now `.py`-scoped with no tree-wide number at all. The single maintainer action outside this cycle's fence is regenerating `docs/TREE.md`.

### Spec changes made (Worker 1 only)

**The gate pass itself made none** — it edited no spec and no rationale text, and closed `git status --porcelain` at the same 21 ` M` + 7 `??` it opened with, plus this artifact.

**Post-final residual cohort, 2026-08-20 — [`### Decision 9`][spec-027-d9]'s clear-path mechanism claim, three surfaces.** Surfaced by the retired-mechanism docstring cohort ([`build-027-filters-0_0_8.md`][plan] `### Post-final residual cohort`) and re-derived here before editing. The claim: `registry.clear()` "removes every materialized class from the module's global namespace", and `clear_filter_input_namespace()` "walks `_materialized_names.items()` and `delattr(...)` for each". Measured at `HEAD`: `grep -rn delattr django_strawberry_framework/ --include='*.py'` puts no `delattr` on any clear path, `utils/inputs.py::clear_generated_input_namespace` states parking as a deliberate contract with the `strawberry.lazy(...)` LazyType hazard as its reason, and `tests/filters/test_finalizer.py::test_clear_filter_input_namespace_can_be_called_directly` plus `::test_registry_clear_clears_filter_input_namespace_and_helper_set` pin the shipped behavior. Two further surfaces carried the same claim and were corrected in the same pass: the [`## Test plan`][spec-027-test-plan] `tests/filters/test_inputs.py` bullet and the [`## Edge cases and constraints`][spec-027-edge-cases] reload bullet's "No stale globals leak between test runs". The spec now states the parked-globals contract and enumerates what the clear does reset; the retraction, the measurements, and why the code is right are keyed to `### Decision 9` in [`spec-027-filters-0_0_8-rationale.md`][spec-027-rationale] under `## Post-final residual cohort — the clear path`. No `.py` file was touched by this edit; `scripts/check_citations.py` reads `OK: 743 citations resolve (666 in 422 .py files, 77 in KANBAN.md)`, the `.py` half `+1` on this gate's `665 in 422` from the sibling cohort's concurrent docstring work, not from this edit.

**Two further spec corrections followed in the same cohort, rounds 2 and 3.** (a) **The ledger-keying claim, ten sentences across five surfaces** — `## Slice checklist` line 59, `### Decision 9` lines 663/669/670/700, `## Edge cases and constraints` 835/841, `## Test plan` 856/860, and closing summary item 6 at 977 — described the lifecycle ledger as `dict[str, type[FilterSet]]` keyed `class_name → filterset_class`. `filters/inputs.py:156` declares `dict[str, type]` and `utils/inputs.py::materialize_generated_input_class` compares `existing is cls` on the generated **input** class. `(name, filterset_class)` is now **0** occurrences and `(name, input_class)` **8**; the worked example's `cls_a` / `cls_b` became `input_a` / `input_b`; observable behavior is unchanged and the corrected text says why. The checklist's Slice-3 bullet already carried the correct spelling while its Slice-1 bullet carried the false one, so **two bullets of one checklist disagreed and each was internally consistent**. (b) **The `## Test plan` coverage mis-attribution** — `materialize_input_class` and the clear-lifecycle clauses were credited to `tests/filters/test_inputs.py` (**0** occurrences) instead of `tests/filters/test_finalizer.py` (**11**); all four clauses moved and are now named by test, and the bullet's other eight symbols plus all nine named tests were re-measured as correctly placed.

**Cohort outcome, durable record.** [`bld-slice-5-027-retired_mechanism_docstrings.md`][slice-5] is `final-accepted` as of 2026-08-20, set at its `## Final verification (Worker 1)` section. The cohort shipped two `.py` docstring repairs with **no executable-token change**, three rounds of spec correction (above), catalog items **18-21** with two of its own deferrals recorded as discharged, and a rationale entry keyed to `### Decision 9`. Verification re-derived rather than accepted: `TypeRegistry.clear` and `_run_type_teardowns` carry **0** import nodes by AST; **0** of the 19 registered subsystem teardown callbacks contains an `import` statement, so every lookup on the replay path is guarded by `utils/imports.py::import_attr_if_importable`; and a clean-subprocess probe over all four `(filters.factories, filters.sets)` presence states — natural state measured as `factories=False sets=True` — shows `clear_filter_input_namespace()` and `registry.clear()` completing in every one, with neither docstring carrying a count-quantifier that a state change could falsify. Focused run: **6 passed in 2.95s** over the two repaired tests plus the four pinning the corrected spec contracts, no `--cov*` flag. One residual is knowingly left: the `TODO-ALPHA-051-0.0.15` cluster in `tests/orders/test_inputs.py` (catalog item 20), spec-028's surface and outside this cycle's fence.

<!-- LINK DEFINITIONS -->

<!-- Root -->

[agents]: ../../AGENTS.md
[start]: ../../START.md

<!-- docs/ -->

<!-- docs/SPECS/ -->

[spec-027-d9]: ../SPECS/spec-027-filters-0_0_8.md#decision-9--input-class-namespace-vs-typeregistry-and-lifecycle
[spec-027-edge-cases]: ../SPECS/spec-027-filters-0_0_8.md#edge-cases-and-constraints
[spec-027-rationale]: ../SPECS/appx/spec-027-filters-0_0_8-rationale.md
[spec-027-test-plan]: ../SPECS/spec-027-filters-0_0_8.md#test-plan

<!-- docs/builder/ -->

[build]: BUILD.md
[integration]: bld-integration-027.md
[plan]: build-027-filters-0_0_8.md
[slice-2]: bld-slice-2-027-citation_and_provenance_rot.md
[slice-4]: bld-slice-4-027-broken_substring_citations.md
[slice-5]: bld-slice-5-027-retired_mechanism_docstrings.md

<!-- django_strawberry_framework/ -->

<!-- tests/ -->

<!-- examples/ -->

<!-- scripts/ -->

<!-- .venv/ -->

<!-- External -->
