# Build: Final test-run gate

Spec reference: `docs/spec-014-meta_primary-0_0_6.md` (final gate spans all six slices + integration pass of build-014)
Status: final-accepted

## Pre-flight checklist

- **Required reading walked (Worker 1 column).** `AGENTS.md`, `START.md`, `docs/builder/BUILD.md`, `docs/builder/worker-1.md`, `GOAL.md`, `docs/FEATURES.md`, `CHANGELOG.md`, the active build plan (`docs/builder/build-014-meta_primary-0_0_6.md`), the active spec (`docs/spec-014-meta_primary-0_0_6.md`), and `docs/builder/worker-memory/worker-1.md` all re-read at the top of this pass. The `worker-1.md` memory file now records nine entries spanning Slices 1-6 closeout plus the integration pass; size remains under the 50-line consolidation threshold.
- **All six per-slice artifacts read in order (read-only context for the Deferred Work Catalog).** `bld-slice-1-registry_multitype.md` (final-accepted), `bld-slice-2-meta_primary_recognition.md` (final-accepted), `bld-slice-3-ambiguity_audit.md` (final-accepted), `bld-slice-4-consumer_site_updates.md` (final-accepted), `bld-slice-5-version_bump.md` (final-accepted), `bld-slice-6-docs_kanban_archive.md` (final-accepted).
- **Integration artifact read (read-only context).** `docs/builder/bld-integration.md` (final-accepted; six explicit deferrals catalogued under its `## Deferred follow-up catalog`).
- **Build plan checklist state.** `docs/builder/build-014-meta_primary-0_0_6.md` shows every slice and the integration pass checked (`- [x]`); the final-test-run-gate row is the only remaining unchecked item. Worker 0 marks the box after this artifact reaches `final-accepted`.
- **Spec status-line re-verification (per `worker-1.md:42-48`).** Spec lines 1-7 re-read: `Status: draft (revision 6, post-TODO-anchor review).` (line 4) describes revision history, not lifecycle (spec line 260 keeps the spec at its working location with no archival default). Slice 6 final-verification and the integration pass both accepted the `draft` framing intentionally. The predecessor reference at spec line 6 (`KANBAN.md` card `WIP-ALPHA-014-0.0.6`) is historically accurate by design — the card now lives at `DONE-014-0.0.6` in `KANBAN.md`, but the spec records the predecessor identity at authoring time. **No spec edit during this final gate.**
- **Working-tree baseline.** `git status --short` reports the expected build-014 modifications across `CHANGELOG.md`, `KANBAN.md`, `TODAY.md`, `docs/FEATURES.md`, `docs/README.md`, `docs/TREE.md`, the six touched `.py` files under `django_strawberry_framework/`, the six touched test files, `docs/spec-014-meta_primary-0_0_6.md` (Slice 2 + Slice 4 spec edits), and `scripts/check_spec_glossary.py` (pre-Slice-1 ruff-format drift, catalogued as deferred follow-up). Eight new untracked build artifacts under `docs/builder/` are the standard Slice 1-6 + integration + final gate outputs; this artifact is the eighth.
- **Coverage gate honored.** All `pytest` invocations below pass `--no-cov` per BUILD.md "Coverage is the maintainer's gate, not a worker's tool". No `--cov*` flag run during this gate.

## Test results

- **Command:** `uv run pytest --no-cov`
- **Result:** **682 passed, 3 skipped, 3 warnings in 24.38s.**
- **Test trees covered (per `pytest.ini` `testpaths = tests examples/fakeshop/tests examples/fakeshop/test_query`):**
  - `tests/` — the package test tree.
  - `examples/fakeshop/tests/` — the example project's in-process tests.
  - `examples/fakeshop/test_query/` — the live `/graphql/` HTTP acceptance tree.
- **Skips.** Three pre-existing, build-014-unrelated:
  - `tests/types/test_base.py::test_consumer_annotation_overrides_synthesized` (Strawberry-decorator-regenerates-annotations skip).
  - Two additional pre-existing skips in unrelated surfaces.
- **Warnings.** Three pre-existing `RuntimeWarning` / `UserWarning` instances (model-re-registration noise in `tests/types/test_converters.py` x2; `DATABASES` override warning in `examples/fakeshop/tests/test_commands.py::test_seed_shards_command_runs_when_shard_alias_present`). All predate build-014 and are unrelated.
- **Coverage flags.** `--no-cov` passed (the only permitted coverage-shaped flag per BUILD.md "Coverage is the maintainer's gate, not a worker's tool"). No `--cov`, `--cov-report`, or `--cov-config` used.

**Pass.**

## Django check

- **Command:** `uv run python examples/fakeshop/manage.py check`
- **Result:** `System check identified no issues (0 silenced).`

**Pass.**

## Django makemigrations dry-run

- **Command:** `uv run python examples/fakeshop/manage.py makemigrations --check --dry-run`
- **Result:** `No changes detected`

**Pass.**

## CHANGELOG sanity

The build authorized CHANGELOG `[Unreleased]` edits at spec line 254 (overriding `AGENTS.md`'s default prohibition); Slice 6 landed the edits at `CHANGELOG.md:25` (`Added`) and `:30`, `:31`, `:32`, `:33` (four `Changed` bullets). This gate verifies the entries are consistent with shipped behavior:

- **`CHANGELOG.md:25` (Added).** `"\`Meta.primary\` boolean flag. Multiple \`DjangoType\` subclasses per Django model. Registry surface: \`primary_for\`, \`types_for\`, \`models_with_multiple_types\`. Tracked as \`DONE-014-0.0.6\` in [\`KANBAN.md\`](KANBAN.md)."` — matches spec line 255 verbatim modulo the `Tracked as` clause. The three named helpers all exist on `TypeRegistry` per Slice 1's diff (`registry.py:184-206`). The `Meta.primary` boolean is recognized in `_validate_meta` per Slice 2's diff (`types/base.py:399-401`).
- **`CHANGELOG.md:30` (Changed — register signature).** `"\`registry.register\` now returns \`bool\` (whether state was added; was \`None\`). \`registry.register\` and \`registry.register_with_definition\` gained a keyword-only \`primary: bool = False\` parameter. \`registry.get(model)\` semantics: returns the primary if declared; the single type if only one is registered; \`None\` if multiple types are registered with no primary. Tracked as \`DONE-014-0.0.6\` in [\`KANBAN.md\`](KANBAN.md)."` — matches spec line 256 verbatim modulo the `Tracked as` clause. The bool return value lives in `register()` (`registry.py:99-156`). The `primary` keyword is on both signatures. The three-branch `get()` matches the Decision 4 pseudocode at `spec:447-455`.
- **`CHANGELOG.md:31` (Changed — iter_types semantic).** `"\`registry.iter_types()\` now yields once per registered type — a model with multiple types appears multiple times. Consumers iterating to drive a per-model action should explicitly dedupe by model, or use \`models_with_multiple_types()\` + \`types_for(model)\` for an explicit grouping."` — matches spec line 257 verbatim. The new shape is at `registry.py:172-182`.
- **`CHANGELOG.md:32` (Changed — always-defer auto-synthesized relations).** `"\`_build_annotations\` (\`types/base.py\`) always defers **auto-synthesized** relation annotations to \`PendingRelationAnnotation\` + the registry's pending list; the eager-bind shortcut is removed. Consumer-authored relation fields (annotation overrides and assigned \`strawberry.field\`) continue to skip synthesis entirely — the existing \`if field.name in consumer_authored_fields: continue\` short-circuit is preserved."` — matches spec line 258 verbatim. Implementation lives at `types/base.py:617-690` (always-defer with the consumer-authored short-circuit preserved at `:621` for relations, `:644` for scalars).
- **`CHANGELOG.md:33` (Changed — origin-aware plan cache key).** `"optimizer plan cache key includes the resolver's origin Strawberry type alongside the model. Primary-return and secondary-return resolvers on the same model produce distinct cache entries."` — matches spec line 259 verbatim. Implementation lives at `optimizer/extension.py:461-464` (cache annotation) + `:732-735` (five-element tuple construction).

**Cross-verification.** The `iter_types()` semantic-change `Changed` bullet (`CHANGELOG.md:31`) does NOT carry a `Tracked as DONE-014-0.0.6` clause — Slice 6 Worker 2 picked the spec-013 / spec-012 precedent of marking only the first `Added` and first `Changed` bullet with the tracking clause. This is consistent across the build's CHANGELOG: the four spec-014 `Changed` bullets at `:30-:33` share one `Tracked as DONE-014-0.0.6` mention at `:30`, mirroring the pre-existing single-tracking pattern on the `DONE-012-0.0.6` (`CHANGELOG.md:28`) and `DONE-013-0.0.6` (`CHANGELOG.md:21`, `:29`) entries.

**Version-line consistency.** The CHANGELOG `[Unreleased]` block remains at `[Unreleased]` (not flipped to `[0.0.6]`); the package version line in `pyproject.toml`, `django_strawberry_framework/__init__.py`, `tests/base/test_init.py`, `docs/FEATURES.md`, and `uv.lock` all carry `0.0.6` per Slice 5's verification. The `[Unreleased]` framing is consistent with the not-yet-released-but-shipped-behavior status — `0.0.6` will be tagged when the maintainer commits and releases.

**Verdict:** CHANGELOG entries are consistent with shipped behavior end-to-end; no drift between the live source/test surface and the documented contract.

## Deferred work catalog

Walked every per-slice and integration artifact's `Notes for Worker 1 (spec reconciliation)`, `What looks solid`, `DRY findings`, and integration-pass `Recommendations` / `Deferred follow-up catalog` sections. The integration artifact already catalogued six deferrals (per `bld-integration.md:280-289`); this gate reproduces and confirms them.

- **Sibling-formatter envelope at `django_strawberry_framework/types/finalizer.py:21-63` (Slice 3 Worker 3 Low + Slice 3 Worker 1 final-verification deferral + integration pass Observation 1).** Source: `bld-slice-3-ambiguity_audit.md:316-346` (Worker 3 Low finding), `:399` (Worker 1 acceptance), `bld-integration.md:69-80` (Observation 1). Spec line: n/a (DRY discretion item; not a spec deferral). Description: `_format_unresolved_targets_error` (`finalizer.py:21-42`) and `_format_ambiguity_error` (`finalizer.py:45-63`) share a `<header>\n + body + \n\n + <footer>` envelope shape; consolidation deferred unless a third formatter joins the site (then a `_format_finalize_error(header, lines, footer) -> str` helper starts paying off). Slice 4 added zero formatters; Slices 5-6 added zero `.py` lines. N=2 confirmed at integration pass.
- **Plan-cache key test does not strictly pin the `origin` slot as the unique differentiator (Slice 4 Worker 3 Low L1 + Slice 4 Worker 1 final-verification deferral + integration pass Observation 5).** Source: `bld-slice-4-consumer_site_updates.md:415-431` (Worker 3 Low L1 finding), `:517-518` (Worker 1 acceptance), `bld-integration.md:136-148` (Observation 5). Spec line: n/a (DRY discretion item). Description: `test_plan_cache_keys_distinguish_primary_and_secondary_returns_for_same_model` in `tests/optimizer/test_extension.py` asserts two cache entries but the `response_path` slot (`("allItems",)` vs `("allAdminItems",)`) is sufficient to distinguish them on its own; the new `origin` slot is not the unique differentiator in that single test. Three sibling tests at the walker / resolver-helper layers cover the H2 contract cluster-wide. A stricter pin (holding `response_path` constant and varying origin only via per-field return-type annotations) is a future-card hardening candidate.
- **`scripts/check_spec_glossary.py` pre-Slice-1 ruff-format drift (Slice 1 Worker 1 final-verification deferral + integration pass catalog item 3).** Source: `bld-slice-1-registry_multitype.md:255-257` + `:402-409`, `bld-integration.md:284`. Spec line: n/a (out-of-build-scope; pre-existing). Description: Two pre-existing ruff `D301` / `D103` errors plus cosmetic whitespace + COM812 trailing-comma fixes in `scripts/check_spec_glossary.py` are pre-Slice-1 source-tree drift that `uv run ruff format .` re-produces during any worker's validation step. Recorded as out-of-scope at Slice 1; not re-touched through Slices 2-6. The maintainer may fold the drift into the build commit or pull it into a separate "ruff-format byproduct" commit.
- **Forward `WIP-ALPHA-014-0.0.6` cross-references in unrelated KANBAN cards (Slice 6 Worker 1 final-verification deferral + integration pass catalog item 4).** Source: `bld-slice-6-docs_kanban_archive.md:432-433` (Worker 2 notes), `:516` (Worker 3 notes), `:553` (Worker 1 final-verification). Spec line 185-186 (does not authorize rewriting cross-references in unrelated cards). Description: Two `WIP-ALPHA-014-0.0.6` mentions remain at `KANBAN.md:406` and `KANBAN.md:574` inside unrelated TODO cards (`TODO-ALPHA-020-0.0.8` filters input-type namespace; `TODO-ALPHA-026-0.0.11` mutations primary-type resolution); both reference this card by its pre-rename identity for design-context dependency. Worker 1 accepted as historically accurate; rewriting cross-references is a separate docs-hygiene pass at the maintainer's discretion.
- **`docs/FEATURES.md` "Scalar field override semantics" still framed as `planned for 0.0.6` (Slice 6 Worker 3 notes + integration pass catalog item 5).** Source: `bld-slice-6-docs_kanban_archive.md:518` (Worker 3 notes), `bld-integration.md:286`. Spec line: n/a (different card; `WIP-ALPHA-015-0.0.6` territory). Description: `docs/FEATURES.md:101` Index row and `:887` body for `Scalar field override semantics` remain `planned for 0.0.6`. Out of build-014 scope; flagged for maintainer awareness only — the maintainer may want to confirm `0.0.6` is still the right target version for that card before this patch closes.
- **`KANBAN.md:69` parallel stale Snapshot sub-bullet about deferred scalar conversions (Slice 6 Worker 1 closeout memory + integration pass catalog item 6).** Source: `bld-slice-6-docs_kanban_archive.md:52` (Slice 6 Worker 1 closeout memory). Spec line: n/a (out-of-Slice-6-scope; same docs-hygiene class as item 4). Description: The Snapshot section's sibling sub-bullet about deferred scalar conversions is already-shipped (`DONE-013-0.0.6`) but was not in Slice 6's authorization map (spec line 177-185 only authorized the `Meta.primary` / multi-type sub-bullet removal). The Slice 6 Worker 1 final-verification pass removed the `Meta.primary` sub-bullet only; the parallel scalar-conversion stale sub-bullet remains for a future docs-hygiene pass.

**Catalog summary.** Six deferrals total. Two are Low-severity DRY discretion items where prior-slice review accepted the current shape (#1 sibling-formatter envelope, #2 cache-key test). Three are docs-hygiene items not authorized by this card's spec checklist (#3 `scripts/check_spec_glossary.py` ruff drift, #4 forward KANBAN cross-references, #6 KANBAN snapshot stale sub-bullet). One is a separate-card concern flagged for maintainer awareness (#5 scalar field override semantics target version). **No deferral blocks build-014 closeout.**

## Final status

`final-accepted`.

### Summary

Build-014 (`meta_primary / 0.0.6`) ships multiple `DjangoType` subclasses per Django model with `Meta.primary` disambiguation across six slices plus a cross-slice integration pass: Slice 1 reshaped `TypeRegistry._types` to `dict[Model, list[Type]]` with parallel `_primaries` tracking, idempotent same-type re-register, snapshot-conditional rollback in `register_with_definition`, three new public accessors (`primary_for`, `types_for`, `models_with_multiple_types`), and a `bool` return on `register()`; Slice 2 added `Meta.primary` recognition in `_validate_meta` and `__init_subclass__` plus a `primary: bool = False` field on `DjangoTypeDefinition` (store-only per Decision 8 L3); Slice 3 added `audit_primary_ambiguity()` inside `finalize_django_types()` (running once per build between the `is_finalized()` short-circuit and pending-relation resolution); Slice 4 shipped the H1/H2/H3 trifecta (always-defer auto-synthesized relations, `source_type` origin threading through `plan_optimizations -> _walk_selections -> _resolve_field_map` with the `_OriginAndModel` NamedTuple and a five-element plan-cache key, plus schema-audit dedupe via parallel `seen` set); Slice 5 was a no-op verification gate (version was already at `0.0.6` from `spec-013-deferred_scalars-0_0_6.md`); Slice 6 landed the docs/KANBAN/CHANGELOG/archive surface (verbatim KANBAN body drop-in for `DONE-014-0.0.6`, `Meta.primary` flipped to `shipped (0.0.6)` in `docs/FEATURES.md`, alpha-constraint removal on `DjangoType`, one `Added` + four `Changed` bullets in `CHANGELOG.md` under the spec-authorized override of `AGENTS.md`).

Final gate results: **682 tests pass + 3 skipped** under `uv run pytest --no-cov` across all three test trees (`tests/`, `examples/fakeshop/tests/`, `examples/fakeshop/test_query/`). Django's `manage.py check` passes with zero issues; `makemigrations --check --dry-run` reports no model drift. CHANGELOG entries match spec lines 255-259 verbatim and accurately describe shipped behavior. Six deferrals are catalogued (two Low-severity DRY discretion items previously accepted; three docs-hygiene items outside this card's spec authorization; one separate-card target-version concern). No High or Medium findings remain; no consolidation loop required; no failing tests; no failing Django checks. Maintainer follow-up: weigh whether to fold the `scripts/check_spec_glossary.py` ruff-format drift into the build commit or into a separate "ruff-format byproduct" commit (deferred item #3); the build cycle is ready to commit and release `0.0.6`.

**Total slice count:** 6 spec slices + 1 cross-slice integration pass + 1 final test-run gate = 8 build-cycle artifacts.

### Spec changes made (Worker 1 only)

None. The final-verification pass surfaced no spec gap requiring reconciliation. Spec line 4 (`Status: draft (revision 6, post-TODO-anchor review).`) and spec line 6 (predecessor reference to `WIP-ALPHA-014-0.0.6`) remain accurate as documented in Slice 6 final-verification and the integration pass. The two spec edits made during this build (Slice 2 at `spec:319-321` for the bool-guard placement reconciliation; Slice 4 at `spec:153` for the `tests/types/test_base.py` line-number drift) are already recorded in their respective slice artifacts.
