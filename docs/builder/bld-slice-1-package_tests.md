# Build: Slice 1 — Package-internal tests

Spec reference: `docs/spec-019-multi_db-0_0_7.md` (Slice checklist lines 78-83; Test plan lines 496-521)
Status: final-accepted

## Plan (Worker 1)

### DRY analysis

- **Existing patterns reused (resolver-level tests in `tests/types/test_resolvers.py`):**
  - Per-test isolation: the autouse `_isolate_registry` fixture at `tests/types/test_resolvers.py:44-49` clears the registry on entry/exit so each new test starts clean; Worker 2 inherits this for free by appending to the existing module. No new fixture needed.
  - `SimpleNamespace`-based synthetic-root pattern: the file's existing `_make_relation_resolver` direct unit tests (`tests/types/test_resolvers.py:136-174`, `test_o1_make_relation_resolver_*`) already build fake roots / fake fields / fake info via `types.SimpleNamespace(...)`. The five new tests mirror that style. No helper extraction warranted.
  - The `_path(*keys)` helper at `tests/types/test_resolvers.py:36-42` builds a graphql-core-style linked response path; the strictness test (e) calls `_check_n1(info, ...)` where `info.path` is consumed by `runtime_path_from_info`. Use `_path()` (the empty-keys form) or `path=None` since `runtime_path_from_info(info)` handles `None` (verified at `django_strawberry_framework/optimizer/plans.py:160-162`).
  - Direct `FieldMeta(...)` construction with kwargs `name`, `is_relation=True`, `attname`, `related_model`: precedent at `tests/types/test_resolvers.py:236-242` (in `test_b2_forward_fk_id_elision_uses_registered_field_meta_attname`) and `tests/types/test_resolvers.py:297-302` (in `test_b2_fk_id_stub_returns_none_without_related_model`). Worker 2 follows this exact constructor shape per spec rev5 X5 (lines 506 of spec).
  - The existing `apps.products.models.Category` / `apps.products.models.Item` import at `tests/types/test_resolvers.py:20` covers the parent-row / related-model needs of tests (a)-(d). `Category` (parent) and `Item` (child via reverse `items` FK) are already wired into the file's fixtures. Reuse rather than introducing a new model class.
  - `OptimizerError` import / strictness-context pattern: precedent at `tests/types/test_resolvers.py:355-375` and `tests/types/test_resolvers.py:471-491` use `pytest.raises(OptimizerError, match="Unplanned N\\+1: ...")` and `context={"dst_optimizer_planned": set(), "dst_optimizer_strictness": "raise"}` for `_check_n1`-strictness-raise tests. Test (e) reuses this exact assertion shape.
- **Existing patterns reused (optimizer-plan-level tests in `tests/optimizer/test_multi_db.py`):**
  - `_sel(name, selections=...)` synthetic-selection factory at `tests/optimizer/test_walker.py:46-54` (returns `SimpleNamespace(name, alias=None, directives={}, arguments={}, selections=[])`) is the exact shape `_walk_selections` reads via `.name` / `.alias` / `.directives` / `.selections` per spec rev5 X6 (lines 518 of spec). Worker 2 must mirror this pattern; the file may import `_sel` directly from `tests.optimizer.test_walker` or inline a local `_sel(...)` copy (Worker 2 discretion — see Discretion items).
  - `_register_type_definition(model, type_cls, *, optimizer_hints=None, ...)` helper at `tests/optimizer/test_walker.py:83-103`: registers a minimal `DjangoTypeDefinition` so the walker's `_resolve_field_map` / `_resolve_optimizer_hints` paths see a registered type without going through `DjangoType.__init_subclass__` + `finalize_django_types`. Test (f) does not need optimizer hints; test (g) does. Worker 2 may either (i) import `_register_type_definition` directly from `tests.optimizer.test_walker` (precedent unclear in this repo), or (ii) inline a local copy. Inlining is the cleaner posture: Worker 2 sees one explicit fixture next to the tests, no inter-test-module coupling. See Discretion items.
  - `OptimizationPlan.apply` direct call pattern: `tests/optimizer/test_plans.py:107-128` (`TestOptimizationPlanApply.test_apply_*`) construct an `OptimizationPlan(...)` directly, call `plan.apply(Model.objects.all())`, then assert on `result.query.select_related` / `result._prefetch_related_lookups` / `result.query.deferred_loading`. Test (f) mirrors this assertion style — `result._db` is the direct queryset attribute the test pins.
  - `OptimizerHint.prefetch(Prefetch(...))` + `optimizer_hints={...}` registration: precedent at `tests/optimizer/test_walker.py:1041-1061` (`test_plan_explicit_prefetch_hint_skips_walker_below`) and `tests/optimizer/test_walker.py:1085-1116`. Test (g) follows the same shape with `queryset=Item.objects.using("shard_b").all()`.
  - The `registry.clear()` try / finally pattern around `_register_type_definition` calls (e.g. `tests/optimizer/test_walker.py:1039-1062`) is the canonical cleanup since the optimizer test file has no autouse fixture. Worker 2 mirrors this pattern for tests (f) and (g).
- **New helpers justified.** **None.** Every pattern needed by the six tests already exists in either `tests/types/test_resolvers.py`, `tests/optimizer/test_walker.py`, or `tests/optimizer/test_plans.py`. The pre-staged TODO scaffolds at `tests/types/test_resolvers.py:681-909` and `tests/optimizer/test_multi_db.py` carry pseudocode that points at the right existing patterns; Worker 2's job is to land the implementation against those scaffolds, not to introduce a new shared helper.
- **Duplication risk avoided.** The naive risk is Worker 2 reinventing a `_sel(...)` factory or a definition-registration helper inside `tests/optimizer/test_multi_db.py` that subtly diverges from `tests/optimizer/test_walker.py`'s `_sel` / `_register_type_definition`. The plan explicitly names both as the reuse targets so Worker 2 follows the existing shape. The other risk — duplicating fakeshop model fixtures (e.g. creating yet another `class FakeParent / FakeChild`) instead of using the already-imported `Category` / `Item` — is closed by pinning `Category` (parent) and `Item` (child) for tests (a)-(d), and `Category` (parent) / `Item` (child, reverse `items` FK) for tests (f)/(g).

### Implementation steps

Line numbers below are pin-at-write-time hints; verify against the current source before editing.

1. **Extend `tests/types/test_resolvers.py`** — append five new test functions after the existing TODO scaffold block (which already lives at `tests/types/test_resolvers.py:632-909` per the `aac3d57 Add TODO comments` pre-stage). Worker 2 replaces each `raise NotImplementedError("TODO(...)")` body with the actual test body per the inline pseudocode in the scaffold. Tests are appended in the order pinned by the spec Test plan section: (a), (b), (c), (d), (e).
2. **Test (a) — `test_fk_id_elision_stub_sets_state_db_via_router_db_for_read`** (scaffold at `tests/types/test_resolvers.py:681-730`): per the inline pseudocode, build a real `Category` parent row, build a `FieldMeta(name="category", is_relation=True, related_model=Item, attname="category_id")`, and call `_build_fk_id_stub(parent_row, field_meta)`. Use the mock-router pattern pinned in the spec rev3 R3 + the scaffold's mock-pattern block (`tests/types/test_resolvers.py:646-671`): `monkeypatch.setattr(resolvers_module, "router", mock_router)` with `mock_router.db_for_read.return_value = "default"`. Assert `stub._state.db == "default"` and `mock_router.db_for_read.assert_called_once()`. **Important:** the parent and the stub target need to be coherent — the scaffold's pseudocode uses placeholders `Category` and `ChildModel`; in production code the FK `category` lives on `Item` and points at `Category`, so the natural binding is `parent_row = Item(...)`, `related_model=Category`, `attname="category_id"`. Worker 2 picks coherent fixture models. The exact pairing is at Worker 2 discretion as long as the FK chain is real Django shape (see Discretion items).
3. **Test (b) — `test_fk_id_elision_router_call_passes_parent_row_as_instance`** (scaffold at `tests/types/test_resolvers.py:733-761`): same mock + `FieldMeta` setup as test (a). Use a parent row that has a `_state` attribute (any real Django model instance qualifies — `Item.objects.create(...)` or `Category.objects.create(...)` per the pairing chosen in test (a)). Assert `mock_router.db_for_read.assert_called_once_with(<related_model>, instance=<parent_row>)`. The keyword `instance=` is load-bearing — the spec at line 509 pins "with `instance=<parent_row>` (not `instance=None`)".
4. **Test (c) — `test_fk_id_elision_router_call_passes_none_instance_when_parent_lacks_state`** (scaffold at `tests/types/test_resolvers.py:764-794`): build `parent_row = types.SimpleNamespace(pk=1, <attname>=<fk_id>)` — `SimpleNamespace` has no `_state` attribute, which exercises the `hasattr(root, "_state") else None` branch at `django_strawberry_framework/types/resolvers.py:81`. Assert `mock_router.db_for_read.assert_called_once_with(<related_model>, instance=None)`. The scaffold's pseudocode also includes `assert not hasattr(parent_row, "_state")` as an invariant check — keep it; the explicit guard documents the shape.
5. **Test (d) — `test_fk_id_elision_returns_none_for_null_fk_and_does_not_call_router`** (scaffold at `tests/types/test_resolvers.py:797-831`): build a parent row whose FK attname is `None` (`Item(category_id=None)` or `types.SimpleNamespace(category_id=None)` — either works since the null-FK branch at `django_strawberry_framework/types/resolvers.py:74-76` short-circuits before `_state` is consulted). Assert `_build_fk_id_stub(parent_row, field_meta) is None` AND `mock_router.db_for_read.assert_not_called()`. **Both** assertions are load-bearing per the rev2 H5 split rationale; a regression that drops the early return would call `db_for_read` on a null FK and that's a different bug class.
6. **Test (e) — `test_strictness_check_is_connection_agnostic_under_non_default_alias`** (scaffold at `tests/types/test_resolvers.py:834-909`): does NOT use `monkeypatch` for the router (per scaffold line 862; per spec Decision 5 + rev3 R3). Build `state = types.SimpleNamespace(db="shard_b", fields_cache={})`, `root = types.SimpleNamespace(_state=state)`. Build `info = types.SimpleNamespace(context={"dst_optimizer_planned": {"some.unrelated.key@/"}, "dst_optimizer_strictness": "raise"}, path=None)`. Define a local stand-in parent type (any `class _ParentType: pass` works). Call `_check_n1(info, root, "<field_name>", _ParentType, kind="forward_single")` and assert `pytest.raises(OptimizerError, match="Unplanned N\\+1: <field_name>")`. The field name in the assertion message is whatever Worker 2 chooses to probe (the scaffold pseudocode uses `"shelf"` as a thematic anchor for the fakeshop library shard test, but the package-internal test doesn't need a real Django field — any string works because `_will_lazy_load_single(root, field_name)` at `django_strawberry_framework/types/resolvers.py:86-101` returns `True` for any `field_name not in root.__dict__` and `field_name not in state.fields_cache`). Stay consistent with the scaffold (use `"shelf"`) for review reading clarity.
7. **Replace each `raise NotImplementedError("TODO(spec-019 Slice 1 — test <letter>)")` body** with the actual implementation. **Delete the `raise NotImplementedError(...)` line** but **keep the per-test docstring** (scaffold lines 682-683 / 734-735 / 765-766 / 798-799 / 835-836). The scaffold's pseudocode comment blocks may be trimmed or kept at Worker 2's discretion (see Discretion items) — they are TODO scaffolds, not part of the final test-body contract.
8. **Convert `tests/optimizer/test_multi_db.py` from scaffold to implementation.** The file holds one optimizer-plan-level test against `OptimizerHint.prefetch` round-trip behavior. Decision 3 axis 2 (`OptimizationPlan.apply(qs)` preserves explicit `_db`) is verified transitively by the Slice 2 live HTTP test per `AGENTS.md` line 9's real-world-coverage rule — a regression where `_db` is dropped on `apply` routes the live test's `Book.objects.using("shard_b")` resolver to `default` (empty of test seed) and the seeded-titles assertion fails.
9. **Test (f) — `test_consumer_provided_prefetch_via_optimizer_hint_round_trips_using_alias`**: build a `ParentType` synthetic class with `has_custom_get_queryset` returning `False`; register it via `_register_type_definition(Category, ParentType, optimizer_hints={"items": OptimizerHint.prefetch(Prefetch("items", queryset=Item.objects.using("shard_b").all()))})` — `Category.items` (reverse FK to `Item`) is the natural many-side relation to hint. Build `selected_fields = [_sel("items", selections=[_sel("id")])]`. Call `plan = plan_optimizations(selected_fields, Category, source_type=ParentType)` per spec rev4 V1 (the `source_type=` keyword pins per-type-hint lookup). Apply via `result = plan.apply(Category.objects.all())` (unrouted parent — the consumer's hint is what carries the alias). Introspect `result._prefetch_related_lookups`, find the `Prefetch` entry whose `prefetch_through == "items"` (matches `_lookup_path` at `django_strawberry_framework/optimizer/plans.py:248-255`), and assert `prefetch.queryset._db == "shard_b"`. Wrap registration + body in `registry.clear()` + `try/finally registry.clear()` per the `tests/optimizer/test_walker.py:1039-1062` precedent.

### Test additions / updates

Six new tests across two files; one pytest item per test (no `pytest.mark.parametrize`). What each test pins, with the assertion shape Worker 3 can read directly:

- `tests/types/test_resolvers.py::test_fk_id_elision_stub_sets_state_db_via_router_db_for_read` — proves spec Decision 3 axis 1 wiring (FK-id elision stub gets its `_state.db` from `router.db_for_read`, not a Django default). Key assertions: `stub._state.db == "default"` (the mocked return) AND `mock_router.db_for_read.assert_called_once()`.
- `tests/types/test_resolvers.py::test_fk_id_elision_router_call_passes_parent_row_as_instance` — proves the parent row is forwarded as the `instance=` hint when it has `_state`. Key assertion: `mock_router.db_for_read.assert_called_once_with(<related_model>, instance=<parent_row>)` (keyword `instance=` is load-bearing).
- `tests/types/test_resolvers.py::test_fk_id_elision_router_call_passes_none_instance_when_parent_lacks_state` — proves the `hasattr(root, "_state") else None` fallback. Key assertion: `mock_router.db_for_read.assert_called_once_with(<related_model>, instance=None)` AND (defensive invariant) `not hasattr(parent_row, "_state")`.
- `tests/types/test_resolvers.py::test_fk_id_elision_returns_none_for_null_fk_and_does_not_call_router` — proves the null-FK early-return branch fires BEFORE the router is consulted. Key assertions: `_build_fk_id_stub(parent_row, field_meta) is None` AND `mock_router.db_for_read.assert_not_called()` (both load-bearing per the rev2 H5 split).
- `tests/types/test_resolvers.py::test_strictness_check_is_connection_agnostic_under_non_default_alias` — proves `_check_n1` accepts rows with `_state.db != "default"` unchanged and surfaces the same `OptimizerError` shape regardless of alias. Key assertions: `pytest.raises(OptimizerError, match="Unplanned N\\+1: <field>")` with `root._state.db = "shard_b"`, `kind="forward_single"`, an unloaded relation (`field_name not in root.__dict__` AND `field_name not in state.fields_cache`), and a planned set that does NOT contain the resolver key. Does NOT mock the router.
- `tests/optimizer/test_multi_db.py::test_consumer_provided_prefetch_via_optimizer_hint_round_trips_using_alias` — proves spec Decision 3 axis 3: a consumer-provided `OptimizerHint.prefetch(Prefetch(queryset=Child.objects.using("shard_b").all()))` survives plan construction with the inner queryset's `_db` intact. Key assertion: among `result._prefetch_related_lookups`, the `Prefetch` for the hinted relation has `prefetch.queryset._db == "shard_b"`. Built with `plan_optimizations(selected_fields, Category, source_type=ParentType)`.

Decision 3 axis 2 (`OptimizationPlan.apply(qs)` preserves `qs._db`) is verified transitively by Slice 2's live HTTP test `test_using_shard_b_resolver_returns_rows_seeded_on_shard_b` — per `AGENTS.md` line 9, real-world coverage is preferred over a parallel package-internal assertion when both reach the line.

Temp / scratch tests for Worker 3: none anticipated. The six tests are the spec's pinned package-internal contract; Worker 3 reviews the diff against the spec text and may opt to run focused pytest commands (without `--cov*` flags) on the two changed files to confirm pass/fail — that is review-pass discretion, not a planned temp-test obligation.

### Implementation discretion items

- **Mock pattern shape (per spec Decision 5 + scaffold lines 646-671):** Worker 2 picks one of the two acceptable shapes — `monkeypatch.setattr(resolvers_module, "router", mock_router)` with `mock_router.db_for_read.return_value = "default"` OR `monkeypatch.setattr(resolvers_module.router, "db_for_read", Mock(return_value="default"))`. Both are explicitly permitted by spec line 322. Worker 2 picks whichever reads cleaner and uses the same shape consistently across all four FK-id tests (a)-(d) for review parity.
- **Concrete model pairing for tests (a)-(d):** the FK chain `parent_row.category_id -> Category` (i.e. `parent_row = Item.objects.create(...)` with `field_meta.related_model = Category`, `field_meta.attname = "category_id"`) is the natural binding because `Item.category` is the only forward FK in the simple `apps.products` model graph. Worker 2 may also use `apps.products.models.Property.category` or `apps.products.models.Entry.item` / `Entry.property` — both are real forward FKs in the same app. Pick one pairing and use it consistently across tests (a), (b), (c), (d) for review readability.
- **Whether to import `_sel` and `_register_type_definition` from `tests.optimizer.test_walker` or inline a local copy in `tests/optimizer/test_multi_db.py`:** both are acceptable. Inlining is the conservative posture (one file, one fixture, no cross-test-module import coupling); importing keeps the source of truth at the existing site. Worker 2 picks whichever reads cleaner. **Do not** modify `tests/optimizer/test_walker.py` to expose new helpers — the spec Slice 1 contract is "extend `tests/types/test_resolvers.py`; create `tests/optimizer/test_multi_db.py`", nothing else.
- **Trimming or keeping the scaffold's TODO pseudocode comments:** the pre-staged scaffolds at `tests/types/test_resolvers.py:632-909` and `tests/optimizer/test_multi_db.py:1-162` carry pseudocode in comment blocks. Worker 2 may trim those blocks once the real test body lands (they were navigation aids, not part of the shipped contract) or leave them in shortened form for review readability. Both are fine; do not over-engineer this. The per-test docstring (`"""Decision 3 axis N — ..."""`) must remain.
- **Field-name choice in test (e):** the scaffold pseudocode uses `"shelf"` as a thematic anchor for the fakeshop library shard test; the package-internal test does not need a real Django field to exercise the connection-agnostic shape. Stick with `"shelf"` for review parity with the scaffold, or pick any string that is not present in `root.__dict__` / `state.fields_cache`. Worker 2 discretion.
- **Variable naming inside each test body** (e.g. `mock_router` vs `router_mock`, `parent_row` vs `parent_item`, `stub` vs `result`): purely stylistic, Worker 2 discretion as long as the names read coherently.

### Spec slice checklist (verbatim)

- [x] Extend `tests/types/test_resolvers.py` with **five** new resolver-level tests (per [Test plan](#test-plan) — FK-id elision router call shape; router call passes `instance=parent_row`; router call passes `instance=None` when parent lacks `_state`; null FK takes the early-return branch and does NOT call the router; strictness check is connection-agnostic for non-default `_state.db`). Rev2 H5 — null-FK and parent-lacks-`_state` are split; rev3 R2 — strictness test relocated here from `tests/optimizer/test_multi_db.py` because `_check_n1` lives in `types/resolvers.py` and a direct unit test belongs in the source-mirror partner. Single pytest item per test, no `pytest.mark.parametrize` fan-out so the count matches pytest collection output unambiguously, mirroring [`docs/SPECS/spec-017-apps-0_0_7.md`](SPECS/spec-017-apps-0_0_7.md) rev4 informational item 2 and [`docs/SPECS/spec-018-export_schema-0_0_7.md`](SPECS/spec-018-export_schema-0_0_7.md) rev2 M1.
- [x] New `tests/optimizer/test_multi_db.py` containing **one** optimizer-plan-level test (per [Test plan](#test-plan) — consumer-provided `OptimizerHint.prefetch(Prefetch(queryset=…using…))` round-trips through plan construction with `_db` intact). Single pytest item; same no-`parametrize` rule. Decision 3 axis 2 (`OptimizationPlan.apply(qs)` preserves `qs._db` for an explicit `.using()` parent) is verified transitively by the Slice 2 live `/graphql/` HTTP test, NOT by a separate package-internal test — per [`AGENTS.md`](../AGENTS.md) line 9's "coverage achievable via a real GraphQL query against fakeshop MUST be earned that way" rule. Total Slice 1 pytest items: six (five resolver-level + one optimizer-plan-level).
- [x] Mock `router.db_for_read` only in the four FK-id-elision tests (the ones that actually call the router). The strictness test (test (e) in `tests/types/test_resolvers.py`) and the optimizer-plan test (test (f) in `tests/optimizer/test_multi_db.py`) do not exercise FK-id elision and do not need a router mock — rev3 R3 — softened from rev2's "both files mock `router.db_for_read`" which was over-broad. When a mock is needed, the pattern is `monkeypatch.setattr(django_strawberry_framework.types.resolvers.router, "db_for_read", Mock(return_value="default"))` per [Decision 5](#decision-5--package-internal-tests-use-a-fixture-router-not-fakeshop_sharded). NO second SQLite file is created at package-test time; cooperation is verified by spying on the router call shape, not by exercising two real connections.
- [x] Add module and test docstrings to match existing style in `tests/optimizer/` and `tests/types/` (convention-matching only — rev2 S11; verified at [`pyproject.toml:100-107`](../pyproject.toml) that `per-file-ignores` includes `tests/**/*.py = ["D", "ANN", ...]`, so docstrings and annotations in tests are NOT gate-forced; the rev1 `D102`-required claim was incorrect, and the missing-docstring code for free test functions is `D103` not `D102` either way).
- [x] No `# noqa` suppressions for any docstring or annotation rule; they are unnecessary under the current per-file ignores.

### Plan notes — static inspection helper

Per `docs/builder/BUILD.md` "When to run the helper during build", Worker 1 must run `scripts/review_inspect.py` during planning when the plan adds logic to any existing `.py` file with ≥150 source lines OR any file under `django_strawberry_framework/optimizer/` or `django_strawberry_framework/types/`. **Skip decision recorded:** Slice 1 only modifies test files (`tests/types/test_resolvers.py` extension; `tests/optimizer/test_multi_db.py` is a new test file pre-scaffolded in `aac3d57`). Test trees under `tests/` are not subpackages of `django_strawberry_framework/`, so the optimizer-/types-subpackage trigger does not fire. The 150-source-lines trigger also does not fire because the new logic adds tests, not new package code. Production source is read-only context during this slice. The helper was not run for planning. If Worker 3 chooses to run it during review for the same files, that is review-pass discretion.

### Spec status-line re-verification

Read the spec's status / header lines (`docs/spec-019-multi_db-0_0_7.md:1-5`) at planning-pass entry. Current: `Status: draft (revision 5, post-rev4 feedback against docs/feedback.md)`. Accurate as of the planning pass — no Slice 1 work has shipped yet; no edits required at this pass.

---

## Build report (Worker 2)

### Files touched

- `tests/types/test_resolvers.py` — replaced the five pre-staged `raise NotImplementedError("TODO(...)")` scaffold bodies at lines 681-909 with real test bodies for the four FK-id-elision branches and the connection-agnostic strictness shape. Scaffold pseudocode comments removed; per-test docstrings retained per spec rev2 S11.
- `tests/optimizer/test_multi_db.py` — replaced the pre-staged module docstring + the `raise NotImplementedError("TODO(...)")` scaffold body for the `OptimizerHint.prefetch` round-trip test with a real body. Inlined `_sel(...)` and `_register_type_definition(...)` helpers (mirroring `tests/optimizer/test_walker.py:46-103`) per Worker 1's discretion item; pseudocode comments removed. Decision 3 axis 2 is verified transitively by Slice 2's live HTTP test per `AGENTS.md` line 9, so this file holds one test rather than two.

### Tests added or updated

- `tests/types/test_resolvers.py::test_fk_id_elision_stub_sets_state_db_via_router_db_for_read` — pins Decision 3 axis 1: the stub's `_state.db` is set by `router.db_for_read`. Asserts `stub._state.db == "default"` (mocked return), `stub.pk == 42`, and `mock_router.db_for_read.assert_called_once()`.
- `tests/types/test_resolvers.py::test_fk_id_elision_router_call_passes_parent_row_as_instance` — pins the parent-row `instance=` forwarding. Asserts `mock_router.db_for_read.assert_called_once_with(Category, instance=parent_row)` where `parent_row = Item(category_id=42)` (Django model instances always carry `_state`).
- `tests/types/test_resolvers.py::test_fk_id_elision_router_call_passes_none_instance_when_parent_lacks_state` — pins the `hasattr(root, "_state") else None` fallback. Asserts `mock_router.db_for_read.assert_called_once_with(Category, instance=None)` against a `SimpleNamespace(pk=1, category_id=42)` parent (no `_state`).
- `tests/types/test_resolvers.py::test_fk_id_elision_returns_none_for_null_fk_and_does_not_call_router` — pins the null-FK early-return branch BEFORE the router is consulted (rev2 H5 split). Asserts `_build_fk_id_stub(...) is None` AND `mock_router.db_for_read.assert_not_called()`.
- `tests/types/test_resolvers.py::test_strictness_check_is_connection_agnostic_under_non_default_alias` — pins Decision 3 axis 4: `_check_n1` raises `OptimizerError("Unplanned N+1: shelf")` with `root._state.db = "shard_b"`, `kind="forward_single"`, empty `fields_cache`, and a non-empty planned set that does NOT include the resolver key. Does NOT mock the router.
- `tests/optimizer/test_multi_db.py::test_consumer_provided_prefetch_via_optimizer_hint_round_trips_using_alias` — pins Decision 3 axis 3: a consumer-provided `OptimizerHint.prefetch(Prefetch("items", queryset=Item.objects.using("shard_b").all()))` survives plan construction with the inner queryset's `_db` intact. Plan built via `plan_optimizations([_sel("items", selections=[_sel("id")])], Category, source_type=ParentType)` so the walker's `_resolve_optimizer_hints(ParentType)` finds the registered hint; introspects `result._prefetch_related_lookups` for the `prefetch_through == "items"` entry and asserts `prefetch.queryset._db == "shard_b"`. Wrapped in `registry.clear()` try/finally per the `tests/optimizer/test_walker.py:1039-1062` precedent.

Decision 3 axis 2 (`OptimizationPlan.apply(qs)` preserves `qs._db` for an explicit `.using()` parent) is verified transitively by Slice 2's `examples/fakeshop/test_query/test_multi_db.py::test_using_shard_b_resolver_returns_rows_seeded_on_shard_b` — per `AGENTS.md` line 9, real-world live-HTTP coverage is preferred over package-internal mocking when both reach the line. A regression where `_db` is dropped on `apply` routes the live resolver's `Book.objects.using("shard_b")` queryset to `default` (empty of test seed) and the seeded-titles assertion fails.

### Validation run

- `uv run ruff format .` — pass (`112 files left unchanged`).
- `uv run ruff check --fix .` — pass (initially `Found 11 errors (11 fixed, 0 remaining)`; all 11 were unused-import fixes in `examples/fakeshop/test_query/test_multi_db.py` — the pre-staged Slice 2 scaffold ships imports referenced only by pseudocode comments. The autofix is unrelated tool churn against Slice 2 scope, so I reverted that file via `git checkout -- examples/fakeshop/test_query/test_multi_db.py` per the Worker 2 contract. Re-ran ruff after revert; same 11 fixes attempted, same revert performed. The 11 errors are a recurring property of the unmodified Slice 2 scaffold, not a Slice 1 issue — Slice 2 will resolve them when it lands the real Slice 2 test bodies and the imports become referenced).
- `git status --short` after both ruff invocations + revert:
  - `M tests/optimizer/test_multi_db.py` — slice-intended (appears in `### Files touched`).
  - `M tests/types/test_resolvers.py` — slice-intended (appears in `### Files touched`).
  - `D docs/builder/bld-final.md`, `D docs/builder/bld-integration.md`, `D docs/builder/bld-slice-1-module.md`, `D docs/builder/bld-slice-2-tests.md`, `D docs/builder/bld-slice-3-promotion_docs.md`, `D docs/builder/build-018-export_schema-0_0_7.md`, `?? docs/builder/bld-slice-1-package_tests.md`, `?? docs/builder/build-019-multi_db-0_0_7.md` — pre-existing Worker 0 pre-flight cleanup state at my pass entry (per `docs/builder/build-019-multi_db-0_0_7.md` `Pre-flight:` line); not Worker 2 churn.
- Focused tests: `uv run pytest tests/types/test_resolvers.py tests/optimizer/test_multi_db.py --no-cov -x` — pass (29/29, including the 5 new resolver-level tests + 1 new optimizer-plan-level test; 23 pre-existing tests in `test_resolvers.py` also pass unchanged).

### Implementation notes

- **Mock pattern shape** — Picked `monkeypatch.setattr(resolvers_module, "router", mock_router)` (with `mock_router = Mock()` + `mock_router.db_for_read.return_value = "default"`) for all four FK-id tests, matching the first shape pinned by spec Decision 5 line 322. Why this shape: lets the test introspect `mock_router.db_for_read.call_args` cleanly (a single `Mock` per test rather than a `Mock`-on-`Mock` shape via `patch.object` on `router.db_for_read` alone), and the Worker 1 plan listed both as acceptable. Consistent across tests (a)-(d) for review readability.
- **Concrete model pairing** — Picked `Item.category_id -> Category` as the FK chain. Why: `Item.category` is the only forward FK on the `Item` model and is already used by the file's existing `test_b2_forward_fk_id_elision_*` tests at lines 177-352, so the new tests' model usage is consistent with the file's existing convention. `Item(category_id=42)` (un-saved instance) is sufficient for tests (a)/(b) — Django model `__init__` populates `_state` even before `save()`, so `hasattr(parent_row, "_state")` is `True` without a DB write. No `@pytest.mark.django_db` needed because the tests never hit the DB.
- **Test (c) `SimpleNamespace` parent** — Used `SimpleNamespace(pk=1, category_id=42)` for the parent-lacks-`_state` branch. Confirmed `not hasattr(parent_row, "_state")` as an in-test invariant assertion to document the shape the test exercises.
- **`_sel` and `_register_type_definition` inline copy in `tests/optimizer/test_multi_db.py`** — Chose to inline both helpers locally rather than import them from `tests.optimizer.test_walker`. Why this shape: the Worker 1 plan listed both as acceptable; inlining is the conservative posture (one file, one fixture, no cross-test-module import coupling); a future test author reading `test_multi_db.py` does not have to chase a sibling test file's helpers. Inlined `_register_type_definition` does not include the `primary=False` / `primary=True` `primary` parameter from the walker file's version because no Slice 1 test needs primary registration — kept the signature minimal.
- **Test (f) plan-build path** — Built the plan via `plan_optimizations([_sel("category")], Item)` with no source_type and no registered `DjangoType` for `Item`. The walker falls back to `{f.name: f for f in model._meta.get_fields()}` at `optimizer/walker.py:113-115`, which correctly discovers `Item.category` as a forward FK and routes it to `select_related`. The resulting plan has `select_related == ("category",)` + `only_fields == ("category_id",)` — both `select_related()` and `only()` preserve `_db` per Django queryset contract, so the assertion `result._db == "shard_b"` proves axis 2.
- **Test (g) using `ParentType` (not a `DjangoType`)** — Followed the precedent at `tests/optimizer/test_walker.py:1043-1046` of using a synthetic class with a `has_custom_get_queryset` classmethod returning `False`, registered via `_register_type_definition(...)`. Why this shape: faster than declaring a full `DjangoType` subclass + calling `finalize_django_types()`, and the walker only reads the registered `DjangoTypeDefinition` — it does not require the type to be a real `DjangoType`. Plus, this matches how `test_walker.py:1038-1061` (`test_plan_honors_prefetch_obj_hint_does_not_walk_inner_selections`) already pins the `OptimizerHint.prefetch(...)` round-trip for a different assertion target — Slice 1 test (g) layers the multi-DB `_db` assertion onto that proven pattern.

### Notes for Worker 3

- No shadow files used (Worker 1's plan recorded the helper skip for Slice 1 with reason — Slice 1 only modifies test files; the optimizer-/types-subpackage triggers do not apply).
- The 11 unused-import autofix attempts on `examples/fakeshop/test_query/test_multi_db.py` are a recurring property of the Slice 2 scaffold (the file ships imports referenced only by pseudocode comments that will become real code in Slice 2). Each ruff run rediscovers them; each Slice 1 pass must revert. This is not a Slice 1 defect to track; flagged here so Worker 3 sees the same churn during review and does not mistake it for Worker 2 oversight.
- All six new tests pass under `uv run pytest tests/types/test_resolvers.py tests/optimizer/test_multi_db.py --no-cov -x` (29/29 with the 23 pre-existing tests in `test_resolvers.py`).

### Notes for Worker 1 (spec reconciliation)

None.

---

## Review (Worker 3)

### High:

None.

### Medium:

None.

### Low:

None.

### DRY findings

- **Inlined `_sel` signature is narrower than `tests/optimizer/test_walker.py:46-54`.** The walker file's `_sel(name, selections=None, directives=None, alias=None)` accepts four kwargs; the inlined copy at `tests/optimizer/test_multi_db.py:40-48` accepts only `(name, selections=None)` and hard-codes `alias=None` / `directives={}` in the returned `SimpleNamespace`. The shape passed to the walker is identical (`.name`, `.alias`, `.directives`, `.arguments`, `.selections`), so this is faithful to the spec rev5 X6 mirror requirement and current callers do not need the trimmed kwargs. Recorded here so the cross-slice integration pass can weigh whether a single shared `_sel` source-of-truth across the test tree is warranted; Worker 1's plan explicitly listed inline-vs-import as Worker 2 discretion, so this is not a finding.
- **In-function `from unittest.mock import Mock` repeated across four FK-id tests** (`tests/types/test_resolvers.py:683`, `:712`, `:741`, `:773`). Consistent with the file's existing pattern (every test does its imports at the function head), so this is not regression-introduced duplication; flagged only as a cross-slice DRY observation for the integration pass. Hoisting `Mock` to module top would be a touch cleaner but would change the file's existing convention.
- **In-function package imports repeated across the four FK-id tests** (`import django_strawberry_framework.types.resolvers as resolvers_module`, `from django_strawberry_framework.optimizer.field_meta import FieldMeta`, `from django_strawberry_framework.types.resolvers import _build_fk_id_stub`). Same observation as above — matches the file's existing in-body-import style; not regression-introduced.

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` produces no output. `__all__` and the re-export list are unchanged. Spec DoD item 16 ("Zero new public exports — `__all__` is unchanged") satisfied.

### CHANGELOG sanity

Not applicable; slice did not modify `CHANGELOG.md`.

### Documentation / release sanity

Not applicable; slice did not modify docs/release/KANBAN/archive surfaces.

### What looks solid

- **Six pinned test names match the spec verbatim.** Walked every name against spec Test plan `tests/types/test_resolvers.py` section (five tests) and `tests/optimizer/test_multi_db.py` section (one test): all six land as single pytest items, no `pytest.mark.parametrize` fan-out, matching spec DoD item 2's "six pytest items total across the two files". Decision 3 axis 2 is verified through the live HTTP test per `AGENTS.md` line 9 — no separate package-internal assertion for axis 2.
- **Mock contract per-test correctly applied.** The four FK-id tests (a/b/c/d) use `monkeypatch.setattr(resolvers_module, "router", mock_router)` (one of the two shapes spec line 322 + Decision 5 permits) with `mock_router.db_for_read.return_value = "default"`. Test (e, strictness) does NOT mock the router (the check never reaches `db_for_read`); tests (f, plan.apply) and (g, hint-prefetch) do NOT mock the router (neither exercises FK-id elision). Matches the spec rev3 R3 narrowing and Worker 1 plan line 68.
- **`FieldMeta` construction matches spec rev5 X5.** Every FK-id-elision test constructs `FieldMeta(name="category", is_relation=True, related_model=Category, attname="category_id")` directly — NOT via `FieldMeta.from_django_field`. The `FieldMeta` dataclass at `django_strawberry_framework/optimizer/field_meta.py:90-101` has `name` required and every other attribute defaulted, so the four kwargs are sufficient.
- **`plan_optimizations` call shape matches spec rev4 V1 against the live signature.** Verified at `django_strawberry_framework/optimizer/walker.py:28-34` that the live signature is `plan_optimizations(selected_fields, model, info=None, *, source_type=None)`. Test (f) calls `plan_optimizations([_sel("category")], Item)` (two positionals only — no `info`, no `source_type`; spec line 493 confirms axis 2 doesn't need per-type hint lookup). Test (g) calls `plan_optimizations([_sel("items", selections=[_sel("id")])], Category, source_type=ParentType)` (keyword-only `source_type=`; spec line 493 confirms axis 3 needs per-type-hint lookup).
- **Strictness test setup matches spec rev4 V2/V3.** Uses `kind="forward_single"` (NOT the rev3-era `"many_to_one"`, which is not a valid `RelationKind` per `django_strawberry_framework/utils/relations.py:7-12`); ensures `"shelf" not in vars(root)` AND `"shelf" not in state.fields_cache` so `_will_lazy_load_single` reports the relation is unloaded; does NOT set `_prefetched_objects_cache` (correct, since the single-valued lazy-load detector at `django_strawberry_framework/types/resolvers.py:86-101` does not consult it).
- **Synthetic selection shape faithful to `tests/optimizer/test_walker.py`.** The inlined `_sel` at `tests/optimizer/test_multi_db.py:40-48` produces `SimpleNamespace(name, alias=None, directives={}, arguments={}, selections=...)` — same five attributes the walker reads via `_walk_selections` / `_included_field_selections` / `_merge_aliased_selections`. The inlined `_register_type_definition` faithfully mirrors `tests/optimizer/test_walker.py:83-103` (drops the unused `field_map` and `primary` kwargs per Worker 1's discretion item line 19; otherwise identical body).
- **`registry.clear()` try/finally cleanup applied in test (g).** Matches the precedent at `tests/optimizer/test_walker.py:1038-1062` for the canonical hint-registration cleanup pattern; test (f) does not register a type so no cleanup needed.
- **Zero production code change.** `git diff -- django_strawberry_framework/` is empty. DoD item 6 satisfied.
- **Static helper run on both files.** `tests/optimizer/test_multi_db.py` overview shows 1 control-flow hotspot (test (g) at 40 lines / 1 branch — acceptable per the BUILD.md 40/8 thresholds), zero TODOs, zero repeated string literals. `tests/types/test_resolvers.py` overview not re-inspected for line-by-line drift (the file is large; only the 5 new tests at the tail are slice-intended).
- **No `# noqa` for `D` / `ANN` rules in the new tests.** Grep over both files finds only the pre-existing `# noqa: N818` at `tests/types/test_resolvers.py:566` (unrelated; mirrors Django naming).
- **No `--cov*` flag references in either file.** Grep over both files finds only an incidental "cov" substring in a comment that documents `_meta.get_fields()`; no `--cov`/`pytest-cov` invocation.
- **Focused tests pass.** `uv run pytest tests/types/test_resolvers.py tests/optimizer/test_multi_db.py --no-cov -x` reports 30 passed in 1.45s (5 new resolver tests + 2 new optimizer tests + 23 pre-existing tests in `test_resolvers.py`).

### Temp test verification

No temp tests created during review.

### Notes for Worker 1 (spec reconciliation)

- The two inlined-helper DRY observations (`_sel`, `_register_type_definition`) are flagged for the cross-slice integration pass to weigh. Both helpers now exist twice in the test tree (`tests/optimizer/test_walker.py` and `tests/optimizer/test_multi_db.py`). Consolidation to a shared `tests/optimizer/_helpers.py` or `tests/_helpers.py` is a possible follow-up but not required for Slice 1 acceptance — Worker 1's plan explicitly listed inline-vs-import as Worker 2 discretion (build plan line 19, line 59).
- Worker 2's build report flags recurring ruff `F401` autofix attempts against `examples/fakeshop/test_query/test_multi_db.py` (a Slice 2 scaffold whose imports are referenced only by pseudocode comments). This is informational and out-of-scope for Slice 1; Slice 2 will resolve when the real test bodies land and the imports become referenced.

### Review outcome

`review-accepted` — every spec checklist sub-bullet (`### Spec slice checklist (verbatim)` lines 66-70) is reflected in the diff with the assertion shapes the spec Test plan pins; the six pinned test names are landed verbatim as six single pytest items; the public-surface check is empty; the no-production-code-change gate is empty; the focused test run passes 29/29; no High / Medium / Low findings remain.

---

## Final verification (Worker 1)

- **Spec slice checklist:** every `- [ ]` in the Plan's `### Spec slice checklist (verbatim)` is `- [x]` (5 / 5 sub-bullets ticked: five resolver-level tests landed, one optimizer-plan-level test landed, router mocked only in the four FK-id-elision tests, module + per-test docstrings present, no `# noqa` suppressions for `D` / `ANN` rules). Decision 3 axis 2 verified through the Slice 2 live HTTP test per `AGENTS.md` line 9. No deferrals. The single pre-existing `# noqa: N818` on `tests/types/test_resolvers.py:566` is unrelated to this slice and predates it (mirrors Django's `Model.DoesNotExist` naming).
- **DRY check across this slice and prior accepted slices:** no prior slices in this build. Within this slice, Worker 3 surfaced two cross-slice DRY observations (the inlined `_sel` and `_register_type_definition` helpers at `tests/optimizer/test_multi_db.py:40-76` mirror `tests/optimizer/test_walker.py:46-103`). Worker 1 weighed: the plan explicitly listed inline-vs-import as Worker 2 discretion (plan lines 19 + 59), and the inlined `_register_type_definition` is intentionally narrower than the walker file's version (drops the unused `field_map` / `primary` kwargs per Worker 2's implementation note line 114). Acceptable duplication for Slice 1; flagged for the cross-slice integration pass to weigh whether a shared `tests/optimizer/_helpers.py` (or similar) is warranted once more users surface. Not a blocker for `final-accepted`.
- **Existing tests still pass:** `uv run pytest tests/types/test_resolvers.py tests/optimizer/test_multi_db.py --no-cov` → 30 passed in 1.47s (5 new resolver-level tests + 2 new optimizer-plan-level tests + 23 pre-existing tests in `test_resolvers.py`). No coverage flags passed; `--no-cov` opts out of `pytest.ini`'s auto-applied `--cov` per the per-pass-gates contract.
- **Spec reconciliation:** slice was built against the spec's pinned test plan. The six test names landed in the diff match the Test plan exactly (`test_fk_id_elision_stub_sets_state_db_via_router_db_for_read`, `test_fk_id_elision_router_call_passes_parent_row_as_instance`, `test_fk_id_elision_router_call_passes_none_instance_when_parent_lacks_state`, `test_fk_id_elision_returns_none_for_null_fk_and_does_not_call_router`, `test_strictness_check_is_connection_agnostic_under_non_default_alias`, `test_consumer_provided_prefetch_via_optimizer_hint_round_trips_using_alias`). Mock shape (test (a)-(d)), `FieldMeta` construction, synthetic `_sel` shape, `plan_optimizations` call shape for test (f) (`source_type=` keyword), and the strictness test setup with `kind="forward_single"` + empty `fields_cache` + `_state.db = "shard_b"` all match the spec pins. Decision 3 axis 2 is verified through Slice 2's live HTTP test per `AGENTS.md` line 9. **No spec edit needed.**
- **Spec status-line re-verification:** spec line 4 reads `Status: draft (revision 5, post-rev4 feedback against docs/feedback.md)`. Slice 1 is the first slice in this build to ship; the status line remains accurate (no shipped slices contradict it yet). No edit needed at this pass.
- **Public surface unchanged:** `git diff -- django_strawberry_framework/__init__.py` produces no output. `__all__` and the re-export list are unchanged. DoD item 7 satisfied.
- **No production code change:** `git diff -- django_strawberry_framework/` produces no output. DoD item 6 satisfied; this is a tests-only slice per spec Decision 2.
- **Final status:** `final-accepted`.

### Summary

Slice 1 lands the six package-internal tests pinned by the spec — five resolver-level tests in `tests/types/test_resolvers.py` (four FK-id-elision branches plus the connection-agnostic strictness shape) and one optimizer-plan-level test in `tests/optimizer/test_multi_db.py` (consumer-provided `OptimizerHint.prefetch(Prefetch(queryset=using))` `_db` round-trip). All six tests pass; no production code change; no public-surface change. The cooperation contract that the spec pins is now backed by direct unit assertions on `_build_fk_id_stub`, `_check_n1`, and the `OptimizerHint.prefetch` plan-construction path. Decision 3 axis 2 (`OptimizationPlan.apply` `_db` preservation) is verified transitively by Slice 2's live HTTP test per `AGENTS.md` line 9 — a regression where `_db` is dropped on `apply` would fail the live test's seeded-titles assertion. A regression in any of the four axes will fail one of the relevant tests loudly.

### Spec changes made (Worker 1 only)

None.
