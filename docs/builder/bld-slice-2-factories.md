# Build: Slice 2 — Factories — OrderArgumentsFactory BFS + inputs adapters + apply_sync/async

Spec reference: `docs/spec-028-orders-0_0_8.md` (Slice 2 checklist at the spec's `## Slice checklist` section, the two bullets immediately under `- [ ] Slice 2: Factories — OrderArgumentsFactory BFS + inputs adapters` — spec lines 117-119)
Status: final-accepted

## Plan (Worker 1)

### DRY analysis

Slice 2 is a one-for-one mirror of the shipped Filtering subsystem's BFS factory + adapter quartet + resolver-facing classmethod pair, with the cookbook's order-side simplifications layered on top. Every cite below uses the symbol-qualified `path::Symbol` form per `AGENTS.md` #"Source references in docs and code comments" plus inline raw `path:NN` line refs (allowed per `AGENTS.md` since this is a `bld-*.md` scratchpad).

- **Existing patterns reused (verbatim sibling-import / structural mirror, no copy).**
  - `django_strawberry_framework/filters/factories.py::FilterArgumentsFactory` (filters/factories.py:56-182) — the BFS class is ported with the substitutions `FilterSet`→`OrderSet`, `RelatedFilter`→`RelatedOrder`, `related_filters`→`related_orders`, `_type_filterset_registry`→`_type_orderset_registry`, `_input_type_name_for(filterset_class)`→`_input_type_name_for(orderset_class)` (the latter delegates to the same `ClassBasedTypeNameMixin.type_name_for()` on the order side). The cookbook source at `~/projects/django-graphene-filters/django_graphene_filters/order_arguments_factory.py::OrderArgumentsFactory` (lines 35-141) is the original this filter port itself mirrors; the two BFS bodies are structurally identical (queue `pending`, `seen` set, name-collision check at `_type_orderset_registry`, build via `_build_class_type`, enqueue every `related_orders` target). Slice 2 ports the filter side's shape directly (the closer mirror) rather than the cookbook (the upstream) because the filter side already encoded the package-specific decisions (FifO queue order, collision-error wording, the `existing_owner is not None and existing_owner is not fs_class` guard) the order side mirrors per the build-plan's "Filter subsystem is shipped … every divergence is a finding" rule.
  - `django_strawberry_framework/filters/inputs.py::_build_input_fields` (filters/inputs.py:649-780) — the adapter that walks `<Set>.get_fields()` and emits `(python_attr, annotation, field_kwargs)` triples for `build_input_class`. The order side's `_build_input_fields(orderset_cls, owner_definition)` ports the filter shape with three intentional simplifications named in Spec Decision 8: (a) NO per-field operator-bag class build (every leaf annotation is `Ordering | None`, NOT a `<Owner>OrderFooInputType` bag class); (b) NO `_build_logic_fields` (no `and_` / `or_` / `not_` operator bag — the cookbook's `AdvancedOrderSet` has no analogue); (c) NO `HIDE_FLAT_FILTERS` / `is_expanded_child` skip (the order side does not expose flat `*__lookup` paths through `django-filter` expansion — flat shorthand paths come only from `Meta.fields = ["shelf__code"]` per Spec Edge cases "**`Meta.fields = ["shelf__code"]`**" line 980, and the runtime normalizer reconstructs them via `FieldSpec.django_source_path`). The grouping-by-top-level-name step (filters/inputs.py:666-690) reduces to a one-pass walk on the order side because every `get_fields()` entry is either a `RelatedOrder` (related) or `None` (leaf) — no operator-bag flattening. `FieldSpec` (filters/inputs.py:118-160) shape ports verbatim with the same `(python_attr, graphql_name, django_source_path)` fields and the same `(orderset_cls, python_attr)` key shape; the per-field provenance map `_field_specs: dict[tuple[type, str], FieldSpec]` is the runtime path `normalize_input_value` consults at apply time. Spec Decision 9 Test plan line 1010 names this map explicitly ("walks nested `RelatedOrder` input and produces flat `(field_path, Ordering)` tuples (including the `shelf__code` path for `RelatedOrder("ShelfOrder", field_name="shelf")` branches)").
  - `django_strawberry_framework/filters/inputs.py::build_input_class` (filters/inputs.py:590-621) — the `type(name, (), namespace)` + `strawberry.input(cls)` decoration shape is reused verbatim. The same per-field `strawberry.field(default=None, name=<graphql_alias>)` pattern handles the python-attr → GraphQL alias mapping for the order-side flat-shorthand path (`shelf__code` → python attr `shelf_code` → GraphQL `shelfCode`). Slice 2's `orders/inputs.py::build_input_class` is a direct port; no order-specific divergence is justified.
  - `django_strawberry_framework/filters/inputs.py::convert_filter_to_input_annotation` (filters/inputs.py:341-405) — the order-side equivalent `convert_order_field_to_input_annotation(model_field, owner_definition) -> Any` is genuinely simpler than the filter side: the order side has NO operator-bag / branch-by-filter-class shape (no `RangeFilter` / `ListFilter` / `ChoiceFilter` / `GlobalIDFilter` branches). For ordering, the converter ALWAYS returns the union `Ordering | None` regardless of the model field type — Spec Decision 5 + Spec Test plan line 1010 ("`convert_order_field_to_input_annotation` always returns `Ordering | None` for leaf fields (verified table-driven across scalar / choice / FK / PK / `BigIntegerField` types — ordering does NOT differentiate the value type, only the direction)"). The function still takes `(model_field, owner_definition)` for signature symmetry with the filter side AND so future work (e.g., DISTINCT ON in `0.0.9`) can extend it without changing the call sites — but the body has one expression: `return Ordering | None`. The `model_field` / `owner_definition` arguments are unused at the body level today, kept in the signature deliberately. Worker 2 may add a `# noqa: ARG001` if ruff complains; otherwise the docstring spelling out the future-extension reason satisfies the linter's "argument used in docstring" check.
  - `django_strawberry_framework/filters/inputs.py::normalize_input_value` (filters/inputs.py:408-456) — the order-side `normalize_input_value(order_set_class, raw_value)` is again simpler than the filter side: no filter-class dispatch, no `BaseCSVFilter` / `GlobalIDFilter` / `RangeFilter` / `ListFilter` branches. The order side's function walks the Strawberry input dataclass (whose attributes are either `Ordering | None` for leaves or another `<Target>OrderInputType | None` for `RelatedOrder` branches) and produces a flat `list[tuple[str, Ordering]]` of `(field_path, direction)` tuples that the apply pipeline turns into `OrderBy` expressions via `Ordering.resolve(field_path)`. The recursive descent follows the cookbook's `~/projects/django-graphene-filters/django_graphene_filters/orderset.py::AdvancedOrderSet.get_flat_orders` (cookbook lines 115-170) algorithm with two adaptations: (a) input is a Strawberry dataclass instance, NOT a `list[dict]` (the cookbook accepts the raw GraphQL `list[Mapping]` shape; Slice 2's normalizer accepts the already-deserialized Strawberry input); (b) leaf direction is an `Ordering` enum member (NOT a string `"asc"` / `"desc"` like the cookbook). The function also accepts an empty list / `None` direction per Spec Decision 13 / Spec Edge case "Empty `orderBy:` input (`orderBy: []`)" line 987 and "`orderBy: [{ name: null }]`" line 988 — returns an empty list, skipping any leaf whose direction is `None`.
  - `django_strawberry_framework/filters/sets.py::FilterSet.apply_sync` / `apply_async` (filters/sets.py:1543-1619) — the resolver-facing classmethod pair on the order side mirrors the filter side's shape verbatim with the order-side simplifications enumerated in Spec Decision 8 (the 8-step pipeline). Specifically, `OrderSet.apply_sync(input_value, queryset, info)` does the following in the order Spec Decision 8 pins:
    1. `data = cls._normalize_input(input_value)` — calls `orders.inputs.normalize_input_value(cls, input_value)` to produce the flat `[(field_path, Ordering), ...]` list. Empty list returns the queryset unchanged.
    2. `request = cls._request_from_info(info)` — ports `FilterSet._request_from_info` (filters/sets.py:768-790) verbatim with the wording adapted (`"FilterSet.apply"` → `"OrderSet.apply"` in the `ConfigurationError` messages).
    3. `cls._run_permission_checks(input_value, request)` — ports the filter side's `_run_permission_checks` (filters/sets.py:1007-1141) verbatim with the order-side simplification that there are NO `and` / `or` / `not` logical-branch recursion paths (no operator bag on the order side per Spec Decision 8 line 686). The active-branch double-dispatch (parent's `check_<branch>_permission` for active `RelatedOrder` branches AND the child orderset's per-field gates) ports verbatim per Spec Decision 8 step 6 "Active-branch double-dispatch (per H3 of `docs/feedback.md` rev3, mirroring the filter side's `_run_permission_checks`)". The `_fired: dict[type, set[str]]` dedup map ports verbatim — same shape, different module.
    4. `flat_orders = cls.get_flat_orders(data)` — ports `~/projects/django-graphene-filters/django_graphene_filters/orderset.py::AdvancedOrderSet.get_flat_orders` (cookbook lines 115-170) as a classmethod on `OrderSet`. The cookbook returns `(flat_orders: list[str], distinct_fields: list[str])`; Slice 2's port returns ONLY the flat orders (DISTINCT ON is deferred to `0.0.9` per Spec Decision 12; the `distinct_fields` half is dropped). The returned list is `[(field_path, Ordering), ...]` (tuple-form, not the cookbook's string-prefix form) because Slice 2 produces `OrderBy` expressions via `Ordering.resolve(field_path)` rather than bare `"-name"` strings (Spec Decision 5).
    5. `expressions = [direction.resolve(field_path) for field_path, direction in flat_orders if direction is not None]` — the thin post-pass per Spec Decision 8 step 7 "Expression-conversion layering note" pinned in the prose ("the conversion lives in the apply pipeline layer (not inside `get_flat_orders`)"). Skip-when-`direction is None` handles the `orderBy: [{ name: null }]` null-direction edge case.
    6. `return queryset.order_by(*expressions) if expressions else queryset` — the empty-list / all-null no-op contracts per Spec Edge cases lines 987-988.

    `OrderSet.apply_async(input_value, queryset, info) -> QuerySet` is an `async def` mirror; per Spec Decision 8's sync/async-split + N7-of-rev1 caveat AND per the carry-forward "no async-only `get_queryset` re-derivation" — the order side has NO `_derive_related_visibility_querysets_async` step (the filter side's lines 1602-1606 do not port). The order side's `apply_async` body is:
    ```
    data = cls._normalize_input(input_value)
    request = cls._request_from_info(info)
    await sync_to_async(cls._run_permission_checks, thread_sensitive=True)(input_value, request)
    flat_orders = cls.get_flat_orders(data)
    expressions = [direction.resolve(field_path) for field_path, direction in flat_orders if direction is not None]
    return queryset.order_by(*expressions) if expressions else queryset
    ```
    The `sync_to_async(thread_sensitive=True)` wrap around `_run_permission_checks` matches the filter side's lines 1612-1615 verbatim (Spec Decision 8's sync/async-split section quotes the N7-of-rev1 caveat). The `get_flat_orders` and `.order_by(...)` calls do NOT wrap in `sync_to_async` because they are pure-Python parsing + a queryset-method call that does no I/O (Spec Decision 8 step 7 line 670 "the apply pipeline does a thin post-pass that walks `get_flat_orders`'s output" + the N7-of-rev1 docstring caveat names this rationale explicitly).
  - `django_strawberry_framework/sets_mixins.py::LazyRelatedClassMixin` / `ClassBasedTypeNameMixin` — both are already in use by Slice 1's `RelatedOrder` and `OrderSet` respectively. Slice 2 adds NO new sibling-import dependencies; the `orders/factories.py` module imports `from .sets import OrderSet`, `from .base import RelatedOrder`, and `from .inputs import _build_input_fields, _input_type_name_for, build_input_class` — all intra-subpackage imports.
  - `django_strawberry_framework/exceptions.py::ConfigurationError` — Slice 1 already imports this in `orders/inputs.py::materialize_input_class`; Slice 2 imports it in `orders/factories.py` for the `_type_orderset_registry` collision-detection path AND in `orders/inputs.py` for the `_get_concrete_field_names_for_order` model-validation path (if a `Meta.model` declaration references a property instead of a field, the helper raises with the same error wording the cookbook uses).
  - `django_strawberry_framework/filters/__init__.py::_helper_referenced_filtersets` (filters/__init__.py — module scope; the ledger is the set the filter side maintains for `filter_input_type` orphan tracking) — `orders/__init__.py::_helper_referenced_ordersets: set[type[OrderSet]] = set()` is the order-side equivalent. Per Spec Decision 2 N-new-2 of rev2: the ledger is co-located with its only writer (`order_input_type` lives in `__init__.py`). Slice 2 lands BOTH the ledger AND the `order_input_type` helper. Per Spec Decision 11 + Spec Decision 11's code block (spec lines 813-861), the helper signature is `order_input_type(orderset_class: type["OrderSet"]) -> object`; the body validates eagerly (`TypeError` for non-`OrderSet`), records into `_helper_referenced_ordersets`, and returns `Annotated[name, strawberry.lazy(INPUTS_MODULE_PATH)]` (the **element type** per B1 of rev3).
  - `django_strawberry_framework/filters/inputs.py::clear_filter_input_namespace` (filters/inputs.py:882-959) — `orders/inputs.py::clear_order_input_namespace()` ports verbatim per Spec Decision 9 / Revision 4 B2 ("**Exact behavior per B2 of `docs/feedback.md` rev3 (mirrors `clear_filter_input_namespace` verbatim)**"). The five clear steps land identically: `_materialized_names.clear()`, `_field_specs.clear()` (the order-side adapter map populated by `_build_input_fields`), `OrderArgumentsFactory.input_object_types.clear()` + `_type_orderset_registry.clear()` (local-import-guarded), every `OrderSet` subclass's `_owner_definition` / `_expanded_fields` / `_is_expanding_fields` `delattr`-when-in-`__dict__` (local-import-guarded). Module globals stay parked per B2 of rev3 (parking is load-bearing — `materialize_input_class` overwrites via `setattr` on the next finalize). Slice 2 ships `clear_order_input_namespace` AND a Slice-2-local `_iter_orderset_subclasses(root)` helper mirroring `filters/inputs.py::_iter_filterset_subclasses` (filters/inputs.py:962-986) verbatim. The Slice 1 TODO anchor at `orders/inputs.py` bottom names this clear helper as Slice 2's deliverable (the carry-forward to Slice 3 in worker-memory said "Slice 3"; the carry-forward is wrong — Spec Decision 9's lifecycle clause + Spec Test plan line 1010 + Spec Slice 5 DoD item 6 all place `clear_order_input_namespace` in Slice 2 because Slice 3's `registry.clear()` integration NEEDS the helper to exist before the local-import block can call it. Worker 1 final-verification at Slice 2's close should update the Slice 1 worker-memory carry-forward to reflect Slice 2 as the actual home.).

- **New helpers justified (only one — and it lives inside `orders/inputs.py`).**
  - `orders/inputs.py::_get_concrete_field_names_for_order(model)` — returns the list of every column-backed model field name per Spec Revision 4 B4 ("`[f.name for f in model._meta.get_fields() if hasattr(f, "column") and not getattr(f, "many_to_many", False)]`"). Single responsibility: produce the column-leaf field-name list for `OrderSet.get_fields("__all__")` to consume. The helper is a one-line list-comprehension; it could be inlined into `OrderSet._expand_meta_fields`, but extracting it (a) keeps the empirical-vs-documented Django M2M divergence rationale at the single site that needs it (the helper's docstring is where Spec Revision 4 B4's "Empirically against Django 6.0.5" reasoning lives); (b) gives the test suite a single import target for the `__all__` parity tests (the cookbook divergence test asserts `genres` is NOT in the output AND `shelf` IS in the output — easier with one importable helper than with a private `OrderSet` classmethod). Spec Decision 3 line 452 names this helper at this exact path. Worker 3's review-time helper trigger (≥30 lines new logic in `inputs.py`) fires automatically; Worker 3 will run `scripts/review_inspect.py` on the post-Slice-2 `inputs.py`.

- **Duplication risk avoided (three risks named explicitly).**
  - **Risk: copy-paste `_run_permission_checks` body into `orders/sets.py`.** Naive implementation would duplicate the ~140-line `_run_permission_checks` body (filters/sets.py:1007-1141) into `orders/sets.py`, silently bifurcating the dedup contract the first time a maintainer fixes one and forgets the other. **Avoidance:** Slice 2 does NOT call the filter side's `_run_permission_checks` (the filter version walks `and` / `or` / `not` operator-bag branches the order side does not have), but Slice 2's order-side `_run_permission_checks` shares the SAME `_fired: dict[type, set[str]]` shape AND the SAME `_invoke_permission_method` helper shape AND the SAME `_active_permission_field_paths(input_value) -> list[str]` shape AND the SAME `_iter_active_related_branches(input_value)` shape (each adapted to the order side's input dataclass instead of the filter side's `_normalize_input` output dict). The cookbook source at `~/projects/django-graphene-filters/django_graphene_filters/orderset.py::AdvancedOrderSet.check_permissions` (cookbook lines 86-113) is shorter than the filter side's because the cookbook has no `and` / `or` / `not` branches; Slice 2's port matches the cookbook's shape verbatim but uses the filter side's per-field naming convention (`check_<field_path>_permission`) for cross-subsystem consistency. Worker 3 should flag any divergence from the filter side's dedup-map shape or method-name format string as a Medium finding. The duplication is in shape only (the body is shorter and the input format differs); no `from ..filters.sets import _run_permission_checks` import or shared helper is justified — the filter side's helper is keyed on `RelatedFilter` / `_iter_active_related_branches`'s filter-side return type, which is not type-compatible with the order side's `RelatedOrder` / order-side `_iter_active_related_branches`.
  - **Risk: re-implementing the BFS walker in `orders/factories.py` instead of mirroring `FilterArgumentsFactory`.** Naive implementation would write the BFS from first principles (LIFO vs FIFO queue, error message wording, collision check vs duplicate-build skip), silently diverging from the filter side. **Avoidance:** Slice 2's `OrderArgumentsFactory._ensure_built` body is a near-line-for-line port of `filters/factories.py::FilterArgumentsFactory._ensure_built` (filters/factories.py:133-169) with the substitutions named in the DRY analysis above. The collision-error wording reuses the filter side's exact message shape with `FilterSet`→`OrderSet`: `"OrderArgumentsFactory: input type name {target_name!r} is claimed by two distinct OrderSet classes: ..."`. The FIFO queue (`pending.pop(0)` per filters/factories.py:145) is preserved — even though `pending.pop()` (LIFO, the cookbook's choice at order_arguments_factory.py:88) produces the same set of reachable classes for a finite graph, the filter side chose FIFO for predictable build order in nested cycles AND Worker 3 should treat any divergence as a Medium finding. Worker 2 records the choice in build notes either way so Worker 3 doesn't ask.
  - **Risk: parking the `_helper_referenced_ordersets` ledger in `orders/inputs.py` for "convenience" with `clear_order_input_namespace`.** Per Spec Decision 2 N-new-2 of rev2 AND Spec Decision 9's "**`_helper_referenced_ordersets.clear()` stays as a SEPARATE `registry.clear()` block** (lives in `orders/__init__.py` per Decision 2), NOT inside `clear_order_input_namespace()`. Two-block layout matches the filter side..." (spec line 775). The ledger lives in `orders/__init__.py` co-located with its only writer (`order_input_type`); `clear_order_input_namespace()` in `inputs.py` does NOT touch the ledger — Slice 3's `registry.clear()` carries TWO separate blocks per the spec line 775 contract. **Avoidance:** Slice 2's `orders/inputs.py::clear_order_input_namespace` body does NOT import `_helper_referenced_ordersets` and does NOT clear it. The ledger clear lives only in Slice 3's `registry.clear()` block (the second of the two order-side blocks per Spec Decision 9 line 764-769). Worker 3 should flag any cross-coupling between `clear_order_input_namespace` and `_helper_referenced_ordersets` as a High finding (it would entrench duplicated logic at exactly the boundary Spec Decision 9 N-new-2 forbids).

- **`scripts/review_inspect.py` planning-time disposition (record per `BUILD.md`).** Ran the helper on the **current** `orders/sets.py` (199 source lines — over the 150-line trigger) AND `orders/inputs.py` (169 source lines — over the 150-line trigger) on 2026-06-01. Both shadow overviews land at `docs/shadow/django_strawberry_framework__orders__sets.overview.md` and `docs/shadow/django_strawberry_framework__orders__inputs.overview.md`. Hotspots / DRY observations:
  - **`orders/sets.py` shadow scan.** One control-flow hotspot: `OrderSet.get_fields` (51 lines, 6 branch nodes). Slice 2 ADDs `apply_sync` / `apply_async` / `get_flat_orders` / `check_permissions` / `_normalize_input` / `_request_from_info` / `_run_permission_checks` / `_invoke_permission_method` / `_iter_active_related_branches` / `_active_permission_field_paths` — call them ~7 new methods, ~150-200 source lines total (rough estimate against the filter side's 1543-1619 + 1007-1170 = ~230 lines). After Slice 2, `orders/sets.py` is likely ~350-400 source lines AND has 6-8 control-flow hotspots; Worker 3's review-time helper trigger (≥30 lines new logic in `django_strawberry_framework/`) fires automatically. **Repeated string literals from the current shadow:** `"related_orders"` (3x) and `"_expanded_fields"` (2x) — same as the filter side's `"related_filters"` / `"_expanded_filters"` pattern; both are class-attribute names referenced symmetrically across the metaclass walk and the `get_fields` cache gate. Not consolidable; no finding.
  - **`orders/inputs.py` shadow scan.** Zero control-flow hotspots in the Slice 1 surface; Slice 2 ADDs `_build_input_fields` (~50 lines per filter side analogue's shape, simpler), `convert_order_field_to_input_annotation` (~10 lines), `normalize_input_value` (~25 lines per cookbook `get_flat_orders` shape), `build_input_class` (~25 lines, port of filter side verbatim), `_get_concrete_field_names_for_order` (~3 lines), `_iter_orderset_subclasses` (~15 lines, port of filter side verbatim), `clear_order_input_namespace` (~30 lines, port of filter side verbatim), and `FieldSpec` (dataclass + `_field_specs: dict[tuple[type, str], FieldSpec]`, ~25 lines). After Slice 2, `orders/inputs.py` is likely ~350-400 source lines AND has ~3-5 control-flow hotspots; the Worker 3 helper trigger fires. **Zero repeated string literals in the current shadow.** Slice 2 may introduce new repeated literals (`"related_orders"`, `"_owner_definition"`, etc. shared between `_build_input_fields` and `clear_order_input_namespace`); Worker 3's helper rerun at review time catches them.
  - **No `optimizer/` or `types/` files touched.** Slice 2 lands NOTHING under `django_strawberry_framework/optimizer/` or `django_strawberry_framework/types/` — those are Slice 3's domain (`types/finalizer.py` + `types/base.py` + `types/definition.py`). Verified by walking the spec's Slice 2 file list AND the build-plan's per-slice scoping. Worker 1's planning-pass helper trigger ("plan adds logic to any file under `optimizer/` or `types/`") does NOT fire for Slice 2.

### Implementation steps

Slice 2 lands two new responsibilities across THREE files (`orders/factories.py` new + `orders/sets.py` expanded + `orders/inputs.py` expanded) AND the public-helper surface in `orders/__init__.py`. The directory `django_strawberry_framework/orders/` already exists from Slice 1. Line anchors below are pin-at-write-time hints per `BUILD.md` Implementation steps note; Worker 2 verifies against the live source before editing.

1. **Create `django_strawberry_framework/orders/factories.py`.** New file, ~110 source lines (the BFS factory + the `_dynamic_orderset_cache` placeholder per Spec Decision 12 — Layer 6 deferred to `0.0.9` BUT the spec line 911-912 names the symbols `factories.py::get_orderset_class` + `factories.py::_dynamic_orderset_cache` as the forward-compat slot; Slice 2 ships ONLY the BFS factory + the symbol-collision check; Layer 6 / dynamic cache is NOT shipped in this slice per Spec DoD item 5 "`_dynamic_orderset_cache` is **NOT** shipped (Layer 6 deferred per Decision 12)").
   - Module docstring pins responsibility: "Layer 5 of the spec-028 six-layer pipeline (the BFS that builds every reachable Strawberry input class via `_build_input_fields` + `build_input_class` from `orders/inputs.py`). The factory consumes resolved `OrderSet.get_fields()` results — NOT a parallel `OrderSet.Meta.fields` map — so the runtime order shape and the GraphQL input shape stay downstream of one decision site (mirror of `filters/factories.py`'s Layer 5 + Decision 4 H1). The finalizer materializes the built classes as module globals at finalize time; this module owns build-only. Layer 6 (dynamic `OrderSet` generation) is deferred to `0.0.9` per Spec Decision 12; `_dynamic_orderset_cache` is NOT shipped."
   - `from __future__ import annotations`.
   - Imports: `from typing import TYPE_CHECKING`, `from ..exceptions import ConfigurationError`, `from .inputs import _build_input_fields, _input_type_name_for, build_input_class`, `from .sets import OrderSet`. `TYPE_CHECKING` block: `from django.db import models` (matches the filter side's filters/factories.py:30-31 shape, kept for parity even though Slice 2's order-side factory does not directly reference `models.Model`).
   - `class OrderArgumentsFactory:` — port of `filters/factories.py::FilterArgumentsFactory` (filters/factories.py:56-182) with substitutions. Class docstring: "BFS-build every reachable Strawberry input class for an `OrderSet`. Mirror of `django_strawberry_framework/filters/factories.py::FilterArgumentsFactory` AND of the cookbook's `~/projects/django-graphene-filters/django_graphene_filters/order_arguments_factory.py::OrderArgumentsFactory` (lines 35-141). Two class-level caches mirror both prior sources: `input_object_types: dict[str, type]` (class-name → built input class, shared across factory instances) and `_type_orderset_registry: dict[str, type]` (collision detection — strict raise per the cookbook's `raise_on_type_name_collision`). The factory does NOT materialize built classes as module globals; that is Slice 3's finalizer-phase-2.5 contract. The `arguments` property returns the built input class for the root `OrderSet` (parallel to the filter side's `FilterArgumentsFactory.arguments`). Subclassing rejected at class-creation time by `__init_subclass__` for the same reason the filter side rejects it (class-level caches are mutable dicts shared with the base; subclassing would silently cross-contaminate)."
     - `input_object_types: dict[str, type] = {}` — class-level cache.
     - `_type_orderset_registry: dict[str, type] = {}` — collision-detection ledger; spec-028 Decision 9 line 705 names this map's role explicitly (source-class collision detection vs the materialization ledger's `name → input class` keying).
     - `def __init_subclass__(cls) -> None:` — `raise TypeError(...)` per filters/factories.py:92-108 verbatim with `FilterArgumentsFactory` → `OrderArgumentsFactory`. The class-level caches are SHARED with the base via inherited dict references; a subclass would silently cross-contaminate. Same rationale and same message shape.
     - `def __init__(self, orderset_class: type[OrderSet]) -> None:` — stores `self.orderset_class` and `self.order_input_type_name = _input_type_name_for(orderset_class)` (parallel to filters/factories.py:118-119).
     - `@property def arguments(self) -> type:` — calls `self._ensure_built()` then returns `self.input_object_types[self.order_input_type_name]` (mirrors filters/factories.py:121-131; the filter side at line 131 returns the input class itself per Spec Decision 3 Layer 5 line 477 — same shape for the order side).
     - `def _ensure_built(self) -> None:` — BFS walk port of filters/factories.py:133-169 with the substitutions named in the DRY analysis. FIFO queue (`pending.pop(0)`); `seen` set; collision check at `_type_orderset_registry.get(target_name)` raises `ConfigurationError` with the "two distinct OrderSet classes" wording; `if target_name not in self.input_object_types: self._build_class_type(fs_class)`; for each `rel_order` in `getattr(os_class, "related_orders", {}).values()`, enqueue `rel_order.orderset` if `target is not None and target not in seen` (per cookbook order_arguments_factory.py:106-109 + filters/factories.py:164-169).
     - `def _build_class_type(self, os_class: type[OrderSet]) -> None:` — port of filters/factories.py:171-182 with the operator-bag layer removed. Body:
       ```
       type_name = _input_type_name_for(os_class)
       owner_definition = getattr(os_class, "_owner_definition", None)
       input_field_triples = _build_input_fields(os_class, owner_definition)
       # NO _build_logic_fields call — order side has no and/or/not operator bag.
       input_cls = build_input_class(type_name, input_field_triples)
       self.input_object_types[type_name] = input_cls
       self._type_orderset_registry[type_name] = os_class
       ```
   - **No Layer 6 / `_dynamic_orderset_cache` / `get_orderset_class` / `_make_cache_key`.** Per Spec Decision 12 + DoD item 5. Worker 2 may add a single TODO anchor at the bottom of `orders/factories.py` naming spec-028 Decision 12 / `0.0.9` for the deferred dynamic-factory work; the anchor is pseudocode-exempt from `ERA001` per the existing per-file convention. Single multi-line `# TODO(spec-028-orders-0_0_8 Decision 12; deferred to 0.0.9): ...` block at file bottom.

2. **Expand `django_strawberry_framework/orders/sets.py`.** Existing file at 199 source lines; expected post-Slice-2 ~350-400 lines.
   - **Replace the Slice-1 `_expand_meta_fields` `"__all__"` `NotImplementedError` branch** with the `_get_concrete_field_names_for_order(model)` call from `orders/inputs.py`. Specifically, at `orders/sets.py::OrderSet._expand_meta_fields` line 178 (the `if meta_fields == "__all__":` branch), replace the `raise NotImplementedError(...)` block with:
     ```
     if meta_fields == "__all__":
         from .inputs import _get_concrete_field_names_for_order
         model = getattr(meta, "model", None)
         if model is None:
             raise ConfigurationError(
                 f"{cls.__name__}.Meta.fields = '__all__' requires Meta.model.",
             )
         for name in _get_concrete_field_names_for_order(model):
             fields[name] = None
         return fields
     ```
     The local-import `from .inputs import _get_concrete_field_names_for_order` dodges any circular-import risk between `orders/sets.py` and `orders/inputs.py` (today: `orders/sets.py` does not import from `inputs.py`; Slice 2's `_build_input_fields` imports `from .sets import OrderSet` so the cycle is at the import-time level, and the local-import dance keeps it inert at runtime).
     **Update `test_orderset_meta_fields_all_raises_until_slice_2`** — Slice 1 ships this as a positive `NotImplementedError` assertion; Slice 2 either deletes the test outright (the contract changes — `"__all__"` now succeeds) OR rewrites it as the Slice-2 cookbook-parity test (`test_orderset_meta_fields_all_expands_to_column_backed_fields`). Worker 2 picks; the latter is preferred because it pins the new contract at the same call site Slice 1 pinned the placeholder.
   - **Add `from .inputs import normalize_input_value` AT MODULE SCOPE** AND `from graphql import GraphQLError` (per Spec Decision 8's `GraphQLError` import path line 648). Slice 1 deferred both because Slice 1 did not ship `apply_*`.
   - **Add the resolver-facing classmethod pair** at the bottom of `OrderSet` (replacing the Slice 1 TODO anchor at `orders/sets.py` lines 194-199):
     - `@classmethod def _normalize_input(cls, input_value) -> list[tuple[str, Ordering]]:` — calls `normalize_input_value(cls, input_value)` from `orders/inputs.py`; thin delegate that returns the flat `[(field_path, Ordering), ...]` list. Body is one line: `return normalize_input_value(cls, input_value)`. Kept as a classmethod for parity with `FilterSet._normalize_input` (the filter side's classmethod returns a dict — different return type, same name + same role + same call site discipline so future maintainers see one shape).
     - `@classmethod def _request_from_info(cls, info: Any) -> Any:` — port of `filters/sets.py::FilterSet._request_from_info` (filters/sets.py:768-790) with the `"FilterSet.apply"` strings → `"OrderSet.apply"`. Raises `ConfigurationError` for missing context / non-HttpRequest context. Identical body shape.
     - `@classmethod def _active_permission_field_paths(cls, input_value) -> list[str]:` — port of filters/sets.py:1169-1213 adapted to the order-side input dataclass walk. Returns one entry per active top-level field's `django_source_path` (from `_field_specs[(cls, python_attr)].django_source_path`); excludes `RelatedOrder` field names (those fire through the active-branch dispatch, not the per-field gate).
     - `@classmethod def _iter_active_related_branches(cls, input_value) -> list[tuple[str, RelatedOrder, Any]]:` — port of filters/sets.py:792-825 adapted to walk `cls.related_orders` against the input dataclass's attributes. Returns `(field_name, related_order, child_input)` triples for every `RelatedOrder` whose corresponding python-attr on the input is non-None.
     - `@staticmethod def _extract_branch_value(input_value, field_name: str) -> Any:` — port of filters/sets.py:828-846 adapted to read `getattr(input_value, field_name, None)` from the Strawberry dataclass.
     - `@staticmethod def _invoke_permission_method(bare_instance, field_path: str, request, *, fired: set[str] | None = None) -> None:` — port of filters/sets.py:1144-1167 verbatim (same `method_name = f"check_{field_path.replace('__', '_')}_permission"` derivation).
     - `@classmethod def _run_permission_checks(cls, input_value, request, *, _fired: dict[type, set[str]] | None = None, _bare: Any = None) -> None:` — port of filters/sets.py:1007-1141 WITHOUT the `_depth` recursion guard AND WITHOUT the `and` / `or` / `not` branch walks (order side has no operator bag). Body shape:
       ```
       if input_value is None: return
       if _fired is None: _fired = {}
       class_fired = _fired.setdefault(cls, set())
       bare = _bare if _bare is not None else object.__new__(cls)
       for field_path in cls._active_permission_field_paths(input_value):
           cls._invoke_permission_method(bare, field_path, request, fired=class_fired)
       for field_name, related_order, child_input in cls._iter_active_related_branches(input_value):
           child_orderset = related_order.orderset
           if child_orderset is not None and hasattr(child_orderset, "_run_permission_checks"):
               child_orderset._run_permission_checks(child_input, request, _fired=_fired)
           cls._invoke_permission_method(bare, field_name, request, fired=class_fired)
       ```
       The same dedup map AND same active-branch double-dispatch contract as the filter side per Spec Decision 8 step 6 + H3 of rev3.
     - `def check_permissions(self, request: Any) -> None:` — the cookbook-shape instance method per Spec Test plan line 1008 ("`check_permissions` recurses through `RelatedOrder`s with active-input-only scope"). Cookbook source at orderset.py:86-113. Slice 2's port: ALSO calls `self.__class__._run_permission_checks(self._input_value, request)` (a thin wrapper around the classmethod pipeline so a consumer who explicitly instantiates an `OrderSet` instance with `self._input_value = <input>` and calls `instance.check_permissions(request)` still hits the same dispatch). Cookbook compatibility shape; Worker 2 may choose to ship `check_permissions` as a classmethod-only API (matching the filter side, which has BOTH `FilterSet.check_permissions(self, request, requested_fields=...)` at filters/sets.py:1215 AND the classmethod `_run_permission_checks`). Recommended: ship BOTH for cookbook compatibility AND for the filter-side symmetry. Worker 2 picks the exact shape.
     - `@classmethod def get_flat_orders(cls, order_data: list, prefix: str = "") -> list[tuple[str, Ordering]]:` — port of `~/projects/django-graphene-filters/django_graphene_filters/orderset.py::AdvancedOrderSet.get_flat_orders` (cookbook lines 115-170) with the DISTINCT ON tuple-half dropped (Spec Decision 12 — DISTINCT ON deferred to `0.0.9`) AND the return shape changed from `list[str]` (cookbook's `"-name"` bare-string shape) to `list[tuple[str, Ordering]]` (Spec Decision 5's `OrderBy`-via-`Ordering.resolve` discipline). Body: walk `order_data` (already normalized to a list of nested Strawberry-dataclass-or-dict-with-Ordering-leaves), for each entry: if the entry's value is another nested input, recurse with `prefix + field_name + "__"`; if the entry's value is an `Ordering` member (or `None`), emit `(f"{prefix}{field_path}", direction)` and append. `None` directions are emitted (preserved as `(field_path, None)`) AND the caller (`apply_sync` / `apply_async`) skips them in the `direction.resolve(...)` step — keeps `get_flat_orders` shape-pure and pushes the null-direction edge case into one place. (Alternative: skip `None` directions inside `get_flat_orders` itself; Worker 2 picks the layering. Recommended: emit-then-filter — keeps `get_flat_orders` symmetric across all input shapes and pushes the filter to `apply_sync`'s comprehension.)
     - `@classmethod def apply_sync(cls, input_value, queryset, info) -> QuerySet:` — the resolver-facing classmethod per Spec Decision 8 step 7 + DoD item 4(b). 6-step body per the DRY analysis bullet on `FilterSet.apply_sync` adapted to the order side. Pseudocode (Worker 2 fleshes out):
       ```
       request = cls._request_from_info(info)
       cls._run_permission_checks(input_value, request)
       data = cls._normalize_input(input_value)  # flat [(field_path, Ordering), ...] list
       if not data: return queryset
       expressions = [direction.resolve(field_path) for field_path, direction in data if direction is not None]
       if not expressions: return queryset
       return queryset.order_by(*expressions)
       ```
       The ordering of `_request_from_info` → `_run_permission_checks` → `_normalize_input` matches Spec Decision 8 step 5 / 6 / 7 verbatim. Permission checks fire BEFORE any `order_by(...)` call per Spec Decision 8 step 6 line 657 ("denial gates raise … before any `order_by(...)` clause touches the queryset").
     - `@classmethod async def apply_async(cls, input_value, queryset, info) -> QuerySet:` — `async def` mirror per Spec Decision 8 sync/async-split. Body:
       ```
       from asgiref.sync import sync_to_async
       request = cls._request_from_info(info)
       await sync_to_async(cls._run_permission_checks, thread_sensitive=True)(input_value, request)
       data = cls._normalize_input(input_value)
       if not data: return queryset
       expressions = [direction.resolve(field_path) for field_path, direction in data if direction is not None]
       if not expressions: return queryset
       return queryset.order_by(*expressions)
       ```
       Local `from asgiref.sync import sync_to_async` import per the filter side's filters/sets.py — kept local to avoid forcing the `asgiref` dep into Slice 1's module-load path even though `asgiref` is already a Django dep (cosmetic discipline only). The `sync_to_async(thread_sensitive=True)` wrap around `_run_permission_checks` matches the filter side's filters/sets.py:1612-1615 verbatim. **No `OrderSet.apply(...)` dispatcher** — Spec Decision 8 line 676 + Spec DoD item 4(c) explicit: "No `OrderSet.apply(...)` dispatcher. Per H1 of `docs/feedback.md` rev1, the filter side's `apply(...)` exists to detect and rewrap a sync-misuse `RuntimeError` raised when a `RelatedFilter` target declares an `async def get_queryset`; the order side has no equivalent code path."
   - **TODO anchor cleanup.** The Slice 1 TODO anchor at `orders/sets.py` lines 194-199 (naming `apply_sync` / `apply_async` / `check_permissions` / `get_flat_orders`) is FULLY consumed by Slice 2's expansion. Worker 2 removes the anchor.

3. **Expand `django_strawberry_framework/orders/inputs.py`.** Existing file at 169 source lines; expected post-Slice-2 ~350-400 lines.
   - **Add `FieldSpec` dataclass** (port of `filters/inputs.py::FieldSpec` filters/inputs.py:118-160 verbatim). `@dataclass(frozen=True, slots=True) class FieldSpec:` with three fields: `python_attr: str`, `graphql_name: str`, `django_source_path: str`. Same shape, no order-specific divergence.
   - **Add `_field_specs: dict[tuple[type, str], FieldSpec] = {}` module-scope ledger** — port of `filters/inputs.py::_field_specs` filters/inputs.py:159 verbatim. Keyed on `(OrderSet class, python_attr)`. Populated by `_build_input_fields`; consulted by `normalize_input_value`; cleared by `clear_order_input_namespace`.
   - **Add `def _get_concrete_field_names_for_order(model) -> list[str]:`** — the helper Spec Decision 3 line 452 names. Body:
     ```
     return [
         f.name
         for f in model._meta.get_fields()
         if hasattr(f, "column") and not getattr(f, "many_to_many", False)
     ]
     ```
     Docstring captures the Spec Revision 4 B4 rationale: the cookbook's `get_concrete_field_names` uses `hasattr(f, "column")` alone, but empirically against Django 6.0.5 `ManyToManyField` exposes `.column = None` so `hasattr` returns `True` and M2M would slip in. The extra `not f.many_to_many` clause is a deliberate divergence from the cookbook's empirical code that aligns with the cookbook's documented intent ("excludes reverse relations, many-to-many managers, and other virtual fields"). The helper returns column-backed fields only — includes forward FK / OneToOne columns (their `<field>_id` column is on the model's own table); excludes reverse FKs (no `column` attribute) AND M2M managers (the `not f.many_to_many` clause).
   - **Add `def _build_input_fields(orderset_cls: type[OrderSet], owner_definition: DjangoTypeDefinition | None = None) -> list[tuple[str, Any, dict[str, Any]]]:`** — port of `filters/inputs.py::_build_input_fields` (filters/inputs.py:649-780) with the three simplifications named in the DRY analysis:
     - NO grouping-by-top-level-name loop (the order side's `get_fields()` returns either `RelatedOrder` instances or `None`-marked leaves; no operator-bag flattening).
     - NO per-field operator-bag class build (every leaf is `Ordering | None`).
     - NO `HIDE_FLAT_FILTERS` / `is_expanded_child` skip.
     Body shape:
     ```
     all_fields = orderset_cls.get_fields()  # OrderedDict[str, RelatedOrder | None]
     related_orders = getattr(orderset_cls, "related_orders", OrderedDict())
     triples: list[tuple[str, Any, dict[str, Any]]] = []
     for top_name, related_or_none in all_fields.items():
         python_attr = top_name.replace("__", "_")
         graphql_name = _camel_case(python_attr)
         field_kwargs: dict[str, Any] = {}
         if python_attr != graphql_name:
             field_kwargs["name"] = graphql_name
         if related_or_none is not None:
             target_os = related_or_none.orderset
             if target_os is None:
                 continue  # Placeholder RelatedOrder(None, ...) — skip silently (cookbook lines 124-130).
             target_name = _input_type_name_for(target_os)
             inner = Annotated[target_name, strawberry.lazy(INPUTS_MODULE_PATH)]
             triples.append((python_attr, inner | None, field_kwargs))
             _field_specs[(orderset_cls, python_attr)] = FieldSpec(
                 python_attr=python_attr,
                 graphql_name=graphql_name,
                 django_source_path=related_or_none.field_name or top_name,
             )
         else:
             model_field = _model_field_for_order(orderset_cls, top_name)
             annotation = convert_order_field_to_input_annotation(model_field, owner_definition)
             triples.append((python_attr, annotation, field_kwargs))
             _field_specs[(orderset_cls, python_attr)] = FieldSpec(
                 python_attr=python_attr,
                 graphql_name=graphql_name,
                 django_source_path=top_name,
             )
     return triples
     ```
     The `_camel_case` helper ports verbatim from filters/inputs.py:783-791. `_model_field_for_order` is a thin helper that resolves the Django model field for a given orderset's `Meta.model` and a field-name path (`shelf__code` resolves to `Shelf.code`); ports the filter side's `_model_field_for_filter` (filters/inputs.py:792-825) shape with the `Filter`-specific `field_name` / `parent_lookup_expr` walking removed (order side has no filter-instance to consult). Worker 2 may choose to skip `_model_field_for_order` entirely and pass `None` to `convert_order_field_to_input_annotation` — the converter discards `model_field` today (Spec Decision 5 — converter always returns `Ordering | None` regardless of model field type); the argument is signature-only. Recommended: pass `None` and document the future-extension reason inline.
   - **Add `def convert_order_field_to_input_annotation(model_field, owner_definition=None) -> Any:`** — the named scalar-field-to-Strawberry-input converter per Spec Decision 5. Body is one line: `return Ordering | None`. The `model_field` / `owner_definition` arguments are unused at the body level today, kept in the signature for future-extension symmetry with the filter side. Add `# noqa: ARG001` if ruff complains, or wire the unused-argument warning inline into the docstring.
   - **Add `def normalize_input_value(orderset_cls: type[OrderSet], input_value) -> list[tuple[str, Ordering | None]]:`** — the runtime symmetric per Spec Decision 8 step 3 + Spec Test plan line 1010. The function consumes the Strawberry input dataclass (post-deserialization, post-Strawberry-type-coercion) AND produces a flat `[(field_path, Ordering | None), ...]` list. Recursive walk over the dataclass's `__dataclass_fields__`-like attributes per the cookbook's `get_flat_orders` algorithm (cookbook lines 115-170), with the cookbook adaptations named in the DRY analysis:
     ```
     if input_value is None: return []
     # Per Spec Decision 13 / Edge case "Empty orderBy:" — list[Strawberry dataclass]
     # at the top level (the resolver receives list[<TypeName>OrderInputType] | None).
     if isinstance(input_value, list):
         result: list[tuple[str, Ordering | None]] = []
         for element in input_value:
             result.extend(normalize_input_value(orderset_cls, element))
         return result
     # Single dataclass element. Walk its fields against the orderset's _field_specs.
     result = []
     related_orders = getattr(orderset_cls, "related_orders", OrderedDict())
     for python_attr in _iter_python_attrs_in_dataclass(input_value):
         value = getattr(input_value, python_attr, None)
         if value is None: continue
         spec = _field_specs.get((orderset_cls, python_attr))
         if spec is None: continue  # Defensive — should be impossible after Slice 3's bind.
         if python_attr in related_orders or <some other related-branch check>:
             # Recurse into the nested orderset.
             child_orderset = related_orders[python_attr].orderset
             prefix = spec.django_source_path + "__"
             for child_path, child_dir in normalize_input_value(child_orderset, value):
                 result.append((prefix + child_path, child_dir))
         else:
             # Leaf — value is an `Ordering | None`.
             result.append((spec.django_source_path, value))
     return result
     ```
     The recursion uses `spec.django_source_path` as the prefix so the flat-shorthand path (`Meta.fields = ["shelf__code"]` → python attr `shelf_code` → `spec.django_source_path == "shelf__code"`) reconstructs the cookbook ORM path correctly. Worker 2 verifies the `_iter_python_attrs_in_dataclass` shape against the actual Strawberry input dataclass post-decoration (likely `cls.__dataclass_fields__.keys()` or a similar Strawberry-specific attribute).
   - **Add `def build_input_class(name: str, field_specs: list[tuple[str, Any, dict[str, Any] | None]]) -> type:`** — port of `filters/inputs.py::build_input_class` (filters/inputs.py:590-621) verbatim. Same `type(name, (), namespace)` + `strawberry.input(cls)` shape. Same per-field `strawberry.field(default=None, name=<graphql_alias>)` handling.
   - **Add `def _iter_orderset_subclasses(root: type) -> list[type]:`** — port of `filters/inputs.py::_iter_filterset_subclasses` (filters/inputs.py:962-986) verbatim with `FilterSet` → `OrderSet`. Same `type.__subclasses__()` walk shape.
   - **Add `def clear_order_input_namespace() -> None:`** — port of `filters/inputs.py::clear_filter_input_namespace` (filters/inputs.py:882-959) verbatim with the filter-side substitutions. Body:
     ```
     _materialized_names.clear()
     _field_specs.clear()
     try:
         from .factories import OrderArgumentsFactory
     except ImportError:
         pass
     else:
         OrderArgumentsFactory.input_object_types.clear()
         OrderArgumentsFactory._type_orderset_registry.clear()
     try:
         from .sets import OrderSet
     except ImportError:
         pass
     else:
         for subclass in _iter_orderset_subclasses(OrderSet):
             for attr in ("_owner_definition", "_expanded_fields", "_is_expanding_fields"):
                 if attr in subclass.__dict__:
                     delattr(subclass, attr)
     ```
     **Module globals stay parked** per Spec Revision 4 B2 — no `delattr` of materialized class objects from `sys.modules[INPUTS_MODULE_PATH].__dict__`. Same parking-is-load-bearing rationale as the filter side: `materialize_input_class` overwrites via `setattr` on the next finalize, and stripping the global would break any held `strawberry.lazy(...)` LazyType in a consumer module whose autouse-reload fixture did NOT also reload the holder. Worker 3 must verify NO `delattr` against the module's `__dict__`.
   - **TODO anchor cleanup.** The Slice 1 TODO anchors at `orders/inputs.py` lines 158-169 (naming the Slice 2 input-data adapters AND the Slice 3 `clear_order_input_namespace`) are FULLY consumed by Slice 2's expansion. **NOTE the Slice 1 TODO anchor's phrasing assigned `clear_order_input_namespace` to Slice 3 BUT the actual spec contract places it in Slice 2** — see the carry-forward note in the DRY analysis. Worker 2 removes both anchors; Slice 3 adds its own anchor inside `types/finalizer.py` for the `_bind_ordersets` work.

4. **Expand `django_strawberry_framework/orders/__init__.py`.** Existing file at 27 source lines; expected post-Slice-2 ~50 source lines.
   - **Add the orphan-tracking ledger:** `_helper_referenced_ordersets: set[type[OrderSet]] = set()` at module scope. Per Spec Decision 2 N-new-2 of rev2: the ledger is co-located with its only writer (`order_input_type`).
   - **Add the `order_input_type` consumer helper** per Spec Decision 11 (spec lines 813-861, including the code block). Signature: `def order_input_type(orderset_class: type[OrderSet]) -> object:`. Body:
     ```
     from typing import Annotated  # module-scope import is fine too
     from .sets import OrderSet  # local import inside the function per Spec Decision 11 line 842
     if not (isinstance(orderset_class, type) and issubclass(orderset_class, OrderSet)):
         raise TypeError(
             "order_input_type() requires an OrderSet subclass; got "
             f"{orderset_class!r}",
         )
     _helper_referenced_ordersets.add(orderset_class)
     name = _input_type_name_for(orderset_class)
     return Annotated[name, strawberry.lazy(INPUTS_MODULE_PATH)]
     ```
     Per B1 of Spec Revision 4: the helper returns the **element type** (NOT a list type); consumers wrap as `list[order_input_type(MyOrder)] | None` at the resolver site to match the `orderBy: [<T>OrderInputType!]` GraphQL argument shape. Per Spec Decision 11's evaluation-timing note (spec line 878): repeated calls with the same `OrderSet` are idempotent on `_helper_referenced_ordersets` (set semantics).
   - **Extend `__all__`** to include `"order_input_type"`. Final `__all__`: `("Ordering", "OrderSet", "OrderSetMetaclass", "RelatedOrder", "order_input_type")`. `_helper_referenced_ordersets` stays out of `__all__` (the `_` prefix marks it private to consumers — Spec Decision 11 line 882 names it as a module-level set that the finalizer reads but consumers do not).
   - **Add `from .factories import OrderArgumentsFactory`** if the spec's Decision 2 line 408 wording about "(for advanced uses) the internal symbols `OrderSetMetaclass`, `OrderArgumentsFactory`" requires it as an importable-but-not-public symbol. Spec Decision 2 line 408 names `OrderArgumentsFactory` as part of the re-export contract; Worker 2 adds the import to `__init__.py`. Whether `OrderArgumentsFactory` is in `__all__` is a Worker 2 discretion call — recommended: NOT in `__all__` (advanced-use symbols stay module-importable without the public surface widening). Worker 2 records the choice in build notes.

5. **Run `uv run ruff format .` and `uv run ruff check --fix .` as the apply-changes step closes.** Standard close per `AGENTS.md` #"Run uv run ruff format . and uv run ruff check --fix . after every edit". Slice 2 lands `.py` only under `django_strawberry_framework/orders/` and `tests/orders/` (the test edits land in step 7 below). Worker 2's build report records the `git status --short` after both ruff invocations and classifies every modified path as slice-intended or reverted-tool-churn per `BUILD.md` #"### Validation run".

6. **Verify against the shipped filter subsystem before pinning line layouts.** Worker 2 reads the following filter-side sites before writing any line of `orders/factories.py` or expanding `orders/inputs.py` / `orders/sets.py`:
   - `django_strawberry_framework/filters/factories.py::FilterArgumentsFactory` (filters/factories.py:56-182) — for the BFS port.
   - `django_strawberry_framework/filters/inputs.py::_build_input_fields` (filters/inputs.py:649-780) — for the adapter shape.
   - `django_strawberry_framework/filters/inputs.py::build_input_class` (filters/inputs.py:590-621) — for the `strawberry.input` decoration shape.
   - `django_strawberry_framework/filters/inputs.py::clear_filter_input_namespace` (filters/inputs.py:882-959) — for the clear lifecycle shape.
   - `django_strawberry_framework/filters/inputs.py::_iter_filterset_subclasses` (filters/inputs.py:962-986) — for the subclass walk.
   - `django_strawberry_framework/filters/sets.py::FilterSet.apply_sync` (filters/sets.py:1543-1565) AND `FilterSet.apply_async` (filters/sets.py:1568-1619).
   - `django_strawberry_framework/filters/sets.py::FilterSet._run_permission_checks` (filters/sets.py:1007-1141) AND `_invoke_permission_method` (filters/sets.py:1144-1167) AND `_request_from_info` (filters/sets.py:768-790) AND `_iter_active_related_branches` (filters/sets.py:792-825).
   - `~/projects/django-graphene-filters/django_graphene_filters/orderset.py::AdvancedOrderSet.get_flat_orders` (cookbook lines 115-170) — for the cookbook recursive parse shape.
   - `~/projects/django-graphene-filters/django_graphene_filters/orderset.py::AdvancedOrderSet.check_permissions` (cookbook lines 86-113) — for the cookbook permission-check shape.
   - `~/projects/django-graphene-filters/django_graphene_filters/order_arguments_factory.py::OrderArgumentsFactory._ensure_built` (cookbook lines 78-109) AND `_build_class_type` (cookbook lines 111-141) — for the cookbook BFS shape.
   Every divergence from the filter-side shape (other than the four enumerated simplifications: no operator bag, no `_build_logic_fields`, no `_derive_*_visibility_querysets_*`, no `_apply_related_constraints`) is a finding. Worker 2 records each in the build report's `### Implementation notes` so Worker 3 doesn't have to re-derive the rationale.

7. **Write the test additions/updates.** See "Test additions / updates" below — Slice 2 expands `tests/orders/test_factories.py`, `tests/orders/test_sets.py` (additive — does NOT rewrite Slice 1 assertions), `tests/orders/test_inputs.py` (additive), and updates the Slice 1 `test_orderset_meta_fields_all_raises_until_slice_2` to assert the new `"__all__"` cookbook-parity contract.

8. **`scripts/review_inspect.py` planning-time disposition.** RAN — see the DRY analysis bullet's recorded hotspots/observations. Helper output lands under `docs/shadow/django_strawberry_framework__orders__sets.overview.md` and `docs/shadow/django_strawberry_framework__orders__inputs.overview.md`. Worker 3's review-time obligation to re-run on the post-Slice-2 expanded files stays in force per `BUILD.md` #"Worker 3 must run the helper during review when … slice adds 30 or more lines of new logic to any file under `django_strawberry_framework/`".

### Test additions / updates

Slice 2 lands the test bodies for the Slice 2-scoped surface only. The existing test files at `tests/orders/test_base.py` and `tests/orders/test_sets.py` already carry Slice 1's tests; Slice 2's `tests/orders/test_sets.py` edits are **append-only** (no Slice 1 assertion is rewritten) EXCEPT for `test_orderset_meta_fields_all_raises_until_slice_2` which IS rewritten to assert the new Slice 2 cookbook-parity contract (the test's name should also change to reflect the new contract; Worker 2 picks the new name — recommended `test_orderset_meta_fields_all_expands_to_column_backed_fields_per_cookbook`). `tests/orders/test_inputs.py` already carries Slice 1's tests; Slice 2's edits are append-only. `tests/orders/test_factories.py` exists today only as a TODO-anchored stub; Slice 2 lands real test bodies. `tests/orders/test_base.py` is NOT touched in Slice 2 (Slice 2 doesn't expand `base.py`).

Test names below use `path::test_name` per `BUILD.md` Implementation steps note.

- **`tests/orders/test_base.py`** — NOT touched in Slice 2. No expansion expected per the build prompt.

- **`tests/orders/test_sets.py`** — append-only edits + ONE rewrite. Slice 2 adds:
  - **Rewrite of `test_orderset_meta_fields_all_raises_until_slice_2`** → `test_orderset_meta_fields_all_expands_to_column_backed_fields_per_cookbook` — declare `class BookOrder(OrderSet): class Meta: model = Book; fields = "__all__"`; call `BookOrder.get_fields()`; assert the returned `OrderedDict`'s keys include `"id"`, `"title"`, `"subtitle"`, `"circulation_status"`, AND `"shelf"` (forward FK column); assert `"genres"` NOT in the keys (M2M excluded per Spec Revision 4 B4); assert `"loans"` NOT in the keys (reverse FK excluded — no `column` attr).
  - **`test_orderset_meta_fields_all_raises_configurationerror_without_meta_model`** — declare `class FooOrder(OrderSet): class Meta: fields = "__all__"` (no `model`); call `FooOrder.get_fields()`; assert `ConfigurationError` is raised with a message naming the missing `Meta.model`.
  - **`test_orderset_get_fields_all_with_explicit_relatedorder_override_replaces_column_leaf`** — declare `BookOrder` with `Meta.fields = "__all__"` AND `shelf = RelatedOrder(ShelfOrder, field_name="shelf")`; call `BookOrder.get_fields()`; assert `"shelf"` is in the keys AND `fields["shelf"]` is the `RelatedOrder` instance (NOT `None` — the explicit override replaces the column leaf per Spec Edge case line 979).
  - **`test_orderset_apply_sync_applies_order_by_to_queryset`** — declare `BookOrder(Meta.model=Book, fields=["title"])`; build a minimal Strawberry input shape (e.g. `[BookOrderInputType(title=Ordering.ASC)]` after Slice 2's factory has materialized the class via `OrderArgumentsFactory(BookOrder).arguments`); call `BookOrder.apply_sync(input_value, Book.objects.all(), <stub info>)`; assert the returned queryset's `query.order_by` carries `("title",)` (Django stores the ascending bare-string form). The stub `info` carries `info.context.request` as a `django.http.HttpRequest`-shaped mock.
  - **`test_orderset_apply_sync_returns_unmodified_queryset_for_empty_input`** — `BookOrder.apply_sync([], Book.objects.all(), <stub info>)`; assert the returned queryset's `query.order_by` is empty.
  - **`test_orderset_apply_sync_returns_unmodified_queryset_for_all_null_directions`** — `BookOrder.apply_sync([BookOrderInputType(title=None)], Book.objects.all(), <stub info>)`; assert empty `query.order_by`.
  - **`test_orderset_apply_sync_emits_multi_field_priority`** — `BookOrder.apply_sync([BookOrderInputType(title=Ordering.ASC), BookOrderInputType(subtitle=Ordering.DESC_NULLS_LAST)], ...)`; assert the queryset's `order_by` contains `OrderBy` expressions with `expression=F("title")` first then `expression=F("subtitle")` second (list element order is the tie-breaker mechanism per Spec Decision 5).
  - **`test_orderset_apply_sync_propagates_graphqlerror_from_check_permission`** — declare `BookOrder` with `check_title_permission(self, request)` that raises `from graphql import GraphQLError; GraphQLError("staff only", extensions={"code": "ORDER_PERMISSION_DENIED"})` for `request.user.is_anonymous`; build an anonymous-user stub `info`; call `BookOrder.apply_sync([BookOrderInputType(title=Ordering.ASC)], qs, info)`; assert `GraphQLError` is raised AND no `order_by` is applied to the queryset (verified by asserting the queryset reference is the original unmodified queryset, not a re-cloned ordered one).
  - **`test_orderset_apply_async_via_asyncio_run`** — same fixture as `apply_sync` but via `asyncio.run(BookOrder.apply_async([BookOrderInputType(title=Ordering.ASC)], qs, info))`; assert identical post-`order_by` contract. Pins the async/sync parity contract Spec Decision 8 sync/async-split names.
  - **`test_orderset_apply_async_runs_check_permission_in_sync_to_async`** — same `check_title_permission` fixture as the sync test; call via `asyncio.run(BookOrder.apply_async([BookOrderInputType(title=Ordering.ASC)], qs, info))`; assert `GraphQLError` is propagated through. This pins the `sync_to_async` wrap correctly routes the exception through the event-loop boundary.
  - **`test_orderset_check_permission_denies_for_active_field`** (per Spec H3 of rev3 + M6 of rev1 split-pair) — declare `BookOrder` with `check_title_permission` that raises `GraphQLError`; call `apply_sync([BookOrderInputType(title=Ordering.ASC)], qs, info)`; assert raise.
  - **`test_orderset_check_permission_quiet_for_inactive_field`** (split-pair sibling) — same `BookOrder` declaration; call `apply_sync([BookOrderInputType(subtitle=Ordering.ASC)], qs, info)`; assert NO `GraphQLError` raised (`title` is absent from the input — active-input-only scope means `check_title_permission` does not fire).
  - **`test_orderset_check_permission_active_relatedorder_branch_fires_parent_gate`** (per Spec H3 of rev3 — active-branch double-dispatch contract) — declare `BookOrder` with `shelf = RelatedOrder(ShelfOrder, field_name="shelf")` AND `check_shelf_permission(self, request)` that raises `GraphQLError`; call `apply_sync([BookOrderInputType(shelf=ShelfOrderInputType(code=Ordering.ASC))], qs, info)`; assert raise.
  - **`test_orderset_check_permission_active_relatedorder_branch_fires_child_gate`** — declare `ShelfOrder` with `check_code_permission(self, request)` raising; same active-branch input; assert raise. Verifies the child orderset's gate fires via the recursive `_run_permission_checks` call.
  - **`test_orderset_check_permission_dedups_repeated_list_entries`** — declare `BookOrder.check_shelf_permission` AND `ShelfOrder.check_code_permission` that BOTH count their invocations (counter attribute on the bare instance); call `apply_sync([BookOrderInputType(shelf=ShelfOrderInputType(code=Ordering.ASC)), BookOrderInputType(shelf=ShelfOrderInputType(code=Ordering.DESC))], qs, info)`; assert each counter is incremented exactly ONCE (the `_fired` dedup map per `(OrderSet class, method name)` ensures one fire per class across the entire input).
  - **`test_orderset_request_from_info_reads_context_request_attribute`** — call `OrderSet._request_from_info(<info with info.context.request=stub_request>)`; assert returns the `stub_request`.
  - **`test_orderset_request_from_info_reads_bare_httprequest_context`** — call with `info.context` being an `HttpRequest` instance directly (no `.request` attribute); assert returns the HttpRequest itself.
  - **`test_orderset_request_from_info_raises_on_unrecognized_context_shape`** — call with `info.context` being a plain object that has neither `.request` NOR is an `HttpRequest`; assert `ConfigurationError`.
  - **`test_orderset_get_flat_orders_walks_nested_relatedorder_into_prefixed_paths`** — declare `BookOrder` with `shelf = RelatedOrder(ShelfOrder, field_name="shelf")`; build a normalized data tuple `[(<shelf+code path tuple>)]`; call `BookOrder.get_flat_orders(...)`; assert the returned list contains `("shelf__code", Ordering.ASC)`. Pins the cookbook recursion shape.
  - **Cookbook `__all__` cookbook-parity tests** (per Spec Revision 4 B4) — three test shapes:
    - `test_orderset_meta_fields_all_includes_forward_fk_column_leaf` — assert `"shelf"` IS in `BookOrder.get_fields("__all__")` keys when no `shelf = RelatedOrder(...)` is declared.
    - `test_orderset_meta_fields_all_excludes_many_to_many_managers` — assert `"genres"` NOT in `BookOrder.get_fields("__all__")` keys.
    - `test_orderset_meta_fields_all_excludes_reverse_fk` — assert `"loans"` NOT in `BookOrder.get_fields("__all__")` keys.

- **`tests/orders/test_factories.py`** — replace the TODO-anchored stub with real test bodies:
  - `test_factory_visits_every_reachable_relatedorder_target_via_bfs` — declare cycle-free graph `BookOrder → ShelfOrder` AND `BookOrder → GenreOrder`; call `OrderArgumentsFactory(BookOrder).arguments`; assert `_type_orderset_registry` contains entries for `BookOrderInputType`, `ShelfOrderInputType`, AND `GenreOrderInputType`.
  - `test_factory_handles_cycles_via_seen_set` — declare `AOrder.b = RelatedOrder(BOrder)` AND `BOrder.a = RelatedOrder(AOrder)`; call `OrderArgumentsFactory(AOrder).arguments`; assert BOTH classes built exactly ONCE (no infinite loop, no double-build, no `ConfigurationError`).
  - `test_factory_builds_leaf_fields_with_ordering_or_none_annotation` — declare `BookOrder(Meta.fields=["title"])`; call `OrderArgumentsFactory(BookOrder).arguments`; introspect the built class's `__annotations__["title"]`; assert it's `Ordering | None` (a `typing.Union` of `Ordering` and `NoneType`).
  - `test_factory_builds_relatedorder_fields_with_annotated_strawberry_lazy_forward_reference` — declare `BookOrder.shelf = RelatedOrder(ShelfOrder)`; call `OrderArgumentsFactory(BookOrder).arguments`; introspect the built class's `__annotations__["shelf"]`; assert it's a `Union[Annotated[ForwardRef("ShelfOrderInputType"), strawberry.lazy(INPUTS_MODULE_PATH)], None]`-shaped annotation. Use `typing.get_args` to walk the union.
  - `test_factory_raises_on_two_distinct_ordersets_sharing_classname` — declare two distinct `BookOrder` classes (in different test modules or via dynamic `type(...)`); both pass through the factory; assert `ConfigurationError` is raised with both qualified class names AND `"OrderArgumentsFactory"` AND the duplicate name in the message.
  - `test_factory_arguments_is_idempotent` — call `OrderArgumentsFactory(BookOrder).arguments` twice; assert both calls return the SAME class instance (cache hit on the second call).
  - `test_factory_input_object_types_shared_across_factory_instances` — call `OrderArgumentsFactory(BookOrder).arguments` then `OrderArgumentsFactory(ShelfOrder).arguments` (two separate factory instances against the SAME shared `input_object_types` dict); after both, assert `"BookOrderInputType" in OrderArgumentsFactory.input_object_types` AND `"ShelfOrderInputType" in OrderArgumentsFactory.input_object_types`. Same shape the filter side ships.
  - `test_factory_subclass_rejected_at_class_creation_time` — attempt `class MyFactory(OrderArgumentsFactory): pass`; assert `TypeError` at class-definition time per the `__init_subclass__` guard.

- **`tests/orders/test_inputs.py`** — append-only edits. Slice 2 adds:
  - `test_get_concrete_field_names_for_order_includes_forward_fk_column` — call `_get_concrete_field_names_for_order(Book)`; assert `"shelf"` is in the list (forward FK column).
  - `test_get_concrete_field_names_for_order_excludes_m2m_managers` — assert `"genres"` NOT in the list.
  - `test_get_concrete_field_names_for_order_excludes_reverse_fk` — assert `"loans"` NOT in the list.
  - `test_convert_order_field_to_input_annotation_always_returns_ordering_or_none` — table-driven across `IntegerField` / `TextField` / `BooleanField` / `DateTimeField` / `ForeignKey` model fields; for each, assert `convert_order_field_to_input_annotation(field, None)` returns the `Ordering | None` union (`typing.Union[Ordering, NoneType]`).
  - `test_normalize_input_value_walks_nested_relatedorder_into_flat_field_paths` — set up `_field_specs` for `BookOrder` and `ShelfOrder` (manually, or via a `_build_input_fields(BookOrder)` call to populate); build an input dataclass shape `BookOrderInputType(shelf=ShelfOrderInputType(code=Ordering.ASC))`; call `normalize_input_value(BookOrder, input_value)`; assert returns `[("shelf__code", Ordering.ASC)]`.
  - `test_normalize_input_value_passes_through_empty_list` (Spec Decision 13 / M7 of rev1 — empty-list pass-through) — call `normalize_input_value(BookOrder, [])`; assert returns `[]`.
  - `test_normalize_input_value_emits_null_direction_as_field_path_with_none` (Spec Decision 13 — null-direction handling) — call `normalize_input_value(BookOrder, [BookOrderInputType(title=None)])`; assert returns `[("title", None)]` (the apply pipeline then filters out the `None` direction at the comprehension step).
  - `test_normalize_input_value_handles_top_level_list_of_dataclass_elements` — call `normalize_input_value(BookOrder, [BookOrderInputType(title=Ordering.ASC), BookOrderInputType(subtitle=Ordering.DESC_NULLS_LAST)])`; assert returns `[("title", Ordering.ASC), ("subtitle", Ordering.DESC_NULLS_LAST)]` in list-element order (pins the multi-element tie-breaker mechanism).
  - `test_build_input_class_decorates_with_strawberry_input` — call `build_input_class("FooOrderInputType", [("title", Ordering | None, None)])`; introspect the returned class; assert it carries the Strawberry `_type_definition` (or whichever attribute Strawberry uses to mark `@strawberry.input` classes) AND the `title` annotation.
  - `test_build_input_class_handles_python_attr_to_graphql_alias_mapping` — call `build_input_class("FooOrderInputType", [("shelf_code", Ordering | None, {"name": "shelfCode"})])`; assert the class's `shelf_code` annotation is `Ordering | None` AND the Strawberry field for `shelf_code` carries the `name="shelfCode"` alias.
  - `test_field_specs_populated_by_build_input_fields_for_leaf` — call `_build_input_fields(BookOrder, None)` after declaring `BookOrder(Meta.fields=["title"])`; assert `_field_specs[(BookOrder, "title")]` is a `FieldSpec` with `python_attr="title"`, `graphql_name="title"`, `django_source_path="title"`.
  - `test_field_specs_populated_by_build_input_fields_for_flat_shorthand` (Spec Edge case `Meta.fields = ["shelf__code"]` → `shelfCode`) — declare `BookOrder(Meta.fields=["shelf__code"])`; call `_build_input_fields(BookOrder, None)`; assert `_field_specs[(BookOrder, "shelf_code")]` carries `python_attr="shelf_code"`, `graphql_name="shelfCode"`, `django_source_path="shelf__code"`.
  - `test_field_specs_populated_by_build_input_fields_for_relatedorder` — declare `BookOrder.shelf = RelatedOrder(ShelfOrder, field_name="shelf")`; call `_build_input_fields(BookOrder, None)`; assert `_field_specs[(BookOrder, "shelf")]` carries `django_source_path="shelf"`.
  - `test_clear_order_input_namespace_resets_materialized_names_ledger` — call `materialize_input_class("FooOrderInputType", Foo)`; call `clear_order_input_namespace()`; assert `_materialized_names` is empty.
  - `test_clear_order_input_namespace_leaves_module_globals_parked` (Spec Revision 4 B2 — parking is load-bearing) — call `materialize_input_class("FooOrderInputType", Foo)`; call `clear_order_input_namespace()`; assert `getattr(sys.modules[INPUTS_MODULE_PATH], "FooOrderInputType") is Foo` STILL (the global is NOT delattr'd).
  - `test_clear_order_input_namespace_clears_factory_class_level_caches` — populate `OrderArgumentsFactory.input_object_types["FooOrderInputType"] = Foo` AND `_type_orderset_registry["FooOrderInputType"] = FooOrder`; call `clear_order_input_namespace()`; assert both dicts are empty.
  - `test_clear_order_input_namespace_resets_orderset_subclass_binding_state` — declare an `OrderSet` subclass with `_owner_definition = <stub>`, `_expanded_fields = OrderedDict(...)`, `_is_expanding_fields = True` all SET DIRECTLY on the subclass's `__dict__`; call `clear_order_input_namespace()`; assert none of the three attrs remain in the subclass's `__dict__` (delattr'd) AND `Subclass._owner_definition is None` (inherited default from `OrderSet` base restored).
  - `test_clear_order_input_namespace_tolerates_unimported_factory_module` — directly call `clear_order_input_namespace()` BEFORE any test has imported `orders.factories`; assert no `ImportError` raises (the local-import-with-pass-on-ImportError shape works as designed).
  - `test_order_input_type_returns_element_annotation_for_orderset_subclass` (Spec B1 of rev3) — call `order_input_type(BookOrder)`; assert the return is an `Annotated[...]` shape AND `typing.get_args(result)[0]` is a `typing.ForwardRef("BookOrderInputType")` AND `typing.get_args(result)[1]` is a `strawberry.LazyType` (or whatever Strawberry exposes for `strawberry.lazy(...)`'s wrapper).
  - `test_order_input_type_raises_typeerror_for_non_orderset` — call `order_input_type(int)` (or any class that is NOT an `OrderSet` subclass); assert `TypeError`.
  - `test_order_input_type_records_orderset_into_helper_referenced_set` — call `order_input_type(BookOrder)`; assert `BookOrder in _helper_referenced_ordersets`.
  - `test_order_input_type_is_idempotent_under_repeated_calls` — call three times; assert `_helper_referenced_ordersets` size remains `1` (set semantics).
  - `test_order_input_type_resolver_wraps_as_list_under_strawberry_schema` (Spec B1 of rev3 — pin the list-wrap discipline) — declare a resolver that uses `order_by: list[order_input_type(BookOrder)] | None = None`; build a minimal `strawberry.Schema`; assert the SDL exposes `orderBy: [BookOrderInputType!]` (list-of-non-null per Spec Decision 5).

- **Coverage for the `_is_expanding_fields` reentry branch** (per the carry-forward #3 from worker-1 memory). The Slice 1 review flagged this branch as structurally unreachable in Slice 1. Slice 2's analysis:
  - **Does `apply_sync` / `apply_async` produce the firing call path?** NO. The apply pipeline calls `cls._normalize_input(input_value)` → `normalize_input_value(cls, input_value)` → walks the `_field_specs` map; it does NOT call `cls.get_fields()` directly. The factory's `_build_input_fields` calls `orderset_cls.get_fields()` — but that runs at finalize time (Slice 3's domain), not at apply time.
  - **Does `_build_input_fields` (via the BFS) produce the firing call path?** YES — under the right cycle shape. The factory's `_ensure_built` enqueues every reachable `RelatedOrder` target; each call to `_build_input_fields(target)` invokes `target.get_fields()`. If two ordersets `AOrder` and `BOrder` reference each other (cycle `A → B → A`), the BFS visits `AOrder.get_fields()` first (which sets `AOrder._is_expanding_fields = True` and walks into `BOrder` via the `related_orders` map). The cookbook's BFS at order_arguments_factory.py:78-109 does NOT call `get_fields()` recursively on the *related target* inside the in-flight expansion — the BFS enqueues targets and processes them in separate iterations of the outer `while pending:` loop, with the in-flight `_is_expanding_fields` flag already reset to `False` by the time the next iteration runs. So the BFS-level recursion does NOT trigger the reentry branch.
  - **Does `get_fields()` produce its own reentry?** ONLY when the BFS itself re-enters `get_fields()` on the SAME class within the `_is_expanding_fields = True` scope. The Slice 1 implementation at `orders/sets.py::OrderSet.get_fields #"if cls.__dict__.get(\"_is_expanding_fields\", False):"` (sets.py:132-136) fires only when `cls.get_fields()` is called recursively on the same class before the `try/finally` has reset the flag. The cookbook orderset.py:265-285 does NOT walk into `related_order.orderset.get_fields()` inside `get_fields()` — Slice 1's port preserves this. So the reentry branch is genuinely unreachable through the Slice 2 surface as planned.
  - **Disposition for Slice 2.** Slice 2's `apply_sync` / `apply_async` / `get_flat_orders` / `_normalize_input` / `_build_input_fields` do NOT produce the firing call path. The branch remains structurally unreachable through the planned Slice 2 implementation. **Restructure recommendation (preferred per `AGENTS.md`):** Slice 2's plan removes the reentry branch entirely. The branch is dead defensive code — its removal is the cleaner abstraction. The full `_expand_meta_fields` body becomes:
    ```
    if cls.__dict__.get("_expanded_fields") is not None:
        return cls.__dict__["_expanded_fields"]
    cls._is_expanding_fields = True
    try:
        fields = cls._expand_meta_fields()
        for k, v in getattr(cls, "related_orders", {}).items():
            fields[k] = v
        if "related_orders" in cls.__dict__ and all(
            not isinstance(f._orderset, str) for f in cls.related_orders.values()
        ):
            cls._expanded_fields = fields
        return fields
    finally:
        cls._is_expanding_fields = False
    ```
    The `_is_expanding_fields` slot itself stays (it's the load-bearing protection AGAINST a future recursive call from a yet-unanticipated path), but the explicit reentry-branch test is removed. Worker 2 records this as a deliberate Slice-2 refactor. **Worker 3 will need to verify the cookbook source's reentry shape**: per cookbook orderset.py:265-285 (the `get_fields` body), the cookbook DOES NOT have an explicit reentry check either — the `_is_expanding_fields` flag is used elsewhere (e.g., in DRF-style `replace_csv_filters` integration not applicable here) but `get_fields` itself doesn't gate on it. So removing the branch BRINGS Slice 1's code CLOSER to the cookbook, not further away. **Test coverage for the removed branch:** the Slice 1 `test_orderset_get_fields_caches_on_resolved_related_orders` + `test_orderset_get_fields_does_not_cache_with_unresolved_string_target` still cover the cache-write-gate AND the cache-skip-on-string-target shapes; no new test is needed for the removal (the existing tests pass against the simpler body). **Alternative (rejected):** keep the branch AND add a Slice-2 test that fires it artificially (e.g., via `cls._is_expanding_fields = True` set before calling `get_fields()`). Rejected because the artificial test pins dead code rather than removing it — violates `AGENTS.md` "always recommend the root-cause fix over the surface patch".

No temp / scratch tests proposed for Slice 2 — the permanent tests above are sufficient to pin every Slice 2 sub-check. Worker 3 may add `docs/builder/temp-tests/slice-2/...` probes during review if it wants to test edge cases; that's Worker 3's discretion.

The Slice 2 tests are package tests under `tests/orders/`. No `examples/fakeshop/test_query/` tests in Slice 2 — live HTTP coverage is Slice 4's deliverable per the build plan's checklist. No `examples/fakeshop/tests/` tests either — the live order resolvers don't exist until Slice 4.

### Implementation discretion items

The items below are at Worker 2's discretion because Worker 1 has assessed each and concluded the choice is between equally valid shapes, or that the choice is a minor style preference that the spec does not pin.

1. **BFS queue discipline in `OrderArgumentsFactory._ensure_built`** — FIFO (`pending.pop(0)`) vs LIFO (`pending.pop()`). The cookbook uses LIFO at order_arguments_factory.py:88; the shipped filter side uses FIFO at filters/factories.py:145. Worker 1 recommends FIFO for filter-side symmetry (every divergence is a finding per the build-plan rule); Worker 2 may use LIFO if it has a strong reason. Build report records the choice. Worker 3 should NOT flag the FIFO-vs-LIFO choice as a finding IF the build report records it.
2. **`check_permissions` instance-method vs classmethod-only API.** The cookbook ships `check_permissions(self, request, fields=None)` as an instance method (orderset.py:86); the filter side ships BOTH `FilterSet.check_permissions(self, request, requested_fields=None)` (filters/sets.py:1215) AND the classmethod `_run_permission_checks(cls, ...)`. Slice 2's `OrderSet` ships the classmethod pipeline either way; Worker 2 picks whether to ALSO ship a thin instance-method `check_permissions(self, request)` for cookbook compatibility. Recommended: ship both (instance method delegates to classmethod via `self.__class__._run_permission_checks(self._input_value, request)`). Worker 2 records the choice.
3. **`_model_field_for_order` helper presence.** The Spec Decision 5 / converter `convert_order_field_to_input_annotation` ignores the `model_field` argument today (always returns `Ordering | None`). Worker 2 may choose to pass `None` everywhere (drops `_model_field_for_order`) OR to populate the `model_field` argument by walking the model's `_meta.get_field(field_name)` (keeps signature-symmetric with the filter side). Recommended: pass `None` AND document the future-extension reason inline in the converter's docstring. The signature accepts `model_field` for forward-compatibility with potential future DISTINCT ON or type-specific direction enums (0.0.9 cohort), but the current body discards it.
4. **`_normalize_input` and `normalize_input_value` exact recursion shape over the Strawberry dataclass.** Strawberry input dataclasses expose their fields via either `__dataclass_fields__` (the underlying `dataclasses.fields(...)` shape) OR via `cls.__strawberry_type__.fields` (the Strawberry-specific reflection); Worker 2 picks whichever is more reliable across Strawberry versions in use. Recommended: use `dataclasses.fields(type(input_value))` since the Strawberry-decorated classes ARE dataclasses; verify against the actual Slice 2 build that the iteration order matches the declaration order (the list-element-order tie-breaker mechanism requires deterministic iteration). Build report records the chosen mechanism.
5. **`get_flat_orders` return-type — preserve nulls or filter at source.** `apply_sync` filters out `None` directions at the comprehension step. Worker 2 may EITHER keep `get_flat_orders` shape-pure (emit `(field_path, None)` entries and filter at apply time) OR filter inside `get_flat_orders` (only emit non-None entries). Recommended: shape-pure (preserves the cookbook's algorithmic intent AND lets future DISTINCT ON work add separate distinct-field tracking against the same flat list without re-deriving). Worker 2 records the choice.
6. **`OrderArgumentsFactory` in `orders/__init__.py::__all__`.** Spec Decision 2 line 408 names `OrderArgumentsFactory` as part of the re-export contract "(for advanced uses)" — Worker 2 picks whether to include it in `__all__` (public surface widens) OR keep it module-importable but not in `__all__` (consumer must `from django_strawberry_framework.orders.factories import OrderArgumentsFactory`). Recommended: NOT in `__all__` per the filter side's precedent at filters/__init__.py (the filter side keeps `FilterArgumentsFactory` importable but not in `__all__`). Build report records the choice.

Items NOT at discretion (escalation triggers if discovered ambiguous):

- The `_helper_referenced_ordersets` ledger MUST live in `orders/__init__.py` co-located with `order_input_type` per Spec Decision 2 N-new-2 of rev2. NOT discretion.
- `materialize_input_class` collision error wording stays `"OrderSet input classes"` (subsystem noun retained per the Slice 1 final-verification's deferral to integration pass). NOT discretion — Worker 2 must NOT align with the filter side's wording at Slice 2.
- The `apply_async` body MUST wrap `_run_permission_checks` in `sync_to_async(thread_sensitive=True)` AND MUST NOT wrap `get_flat_orders` / `.order_by(...)` (Spec Decision 8 N7 of rev1 caveat). NOT discretion.
- `OrderArgumentsFactory._type_orderset_registry` keys on the materialized input-type NAME (string), value is the source `OrderSet` class — NOT the inverted shape. NOT discretion (Spec Decision 9 + the filter side's `_type_filterset_registry` shape).
- **NO `OrderSet.apply(...)` dispatcher** per Spec DoD item 4(c). NOT discretion.
- `clear_order_input_namespace` MUST NOT `delattr` materialized module globals per Spec Revision 4 B2 (parking is load-bearing). NOT discretion.
- `_helper_referenced_ordersets.clear()` lives in `registry.clear()` as a SEPARATE block (Slice 3's domain) — NOT inside `clear_order_input_namespace`. NOT discretion.

### Spec slice checklist (verbatim)

The two sub-bullets below are copied verbatim from `docs/spec-028-orders-0_0_8.md`'s `## Slice checklist` section, the `- [ ] Slice 2: Factories — OrderArgumentsFactory BFS + inputs adapters` block (spec lines 117-119). Worker 1's final-verification pass ticks each box `- [x]` as the contract lands; an un-ticked box at final verification is either deferred with a one-line reason under `### Spec changes made (Worker 1 only)` or the slice goes `revision-needed`. Slice 2's final verification also re-walks the Slice 1 sub-bullets per the carry-forwards from worker-memory: Slice 1 sub-bullet 1 (the 5-file directory-creation contract) ticks at Slice 2's final verification when `factories.py` lands; Slice 1 sub-bullet 3's resolver-API half ticks here when `apply_sync` / `apply_async` / `check_<field>_permission` dispatch land.

- [x] `factories.py` ships `OrderArgumentsFactory` (port from [`django_graphene_filters/order_arguments_factory.py::OrderArgumentsFactory`][upstream-cookbook-order-arguments-factory]) — Layer 5 of [Decision 3](#decision-3--five-layer-port-plus-a-deferred-layer-6). BFS walk that builds every reachable `strawberry.input` type; `_build_class_type` emits `@strawberry.input`-decorated classes via `django_strawberry_framework.orders.inputs.build_input_class(name, field_specs)` (the Strawberry-adapted analogue of Graphene's `type(name, (graphene.InputObjectType,), fields)`). Leaf fields use the `Ordering` enum from `inputs.py`; `RelatedOrder` fields emit `Annotated["<TargetOrderSet>InputType", strawberry.lazy("django_strawberry_framework.orders.inputs")] | None` references (the same Layer-5 cycle-safe forward-reference shape the filter subsystem ships per [`docs/SPECS/spec-027-filters-0_0_8.md`][spec-027] Decision 3).
- [x] `inputs.py` ships the input-data adapter functions: `_build_input_fields` (target-orderset references via `strawberry.lazy(...)` over module globals; leaf fields typed `Ordering | None`), `convert_order_field_to_input_annotation(model_field, owner_definition)` (the named scalar-field-to-Strawberry-input converter — for ordering, the converter always emits `Ordering | None` because the only legal input value for a leaf field is a direction, NOT the field's value), `normalize_input_value(order_set_class, raw_value)` (the runtime symmetric — walks the nested `RelatedOrder` input tree and produces a flat list of `(field_path, Ordering)` tuples that the apply pipeline turns into Django `order_by(...)` arguments).

**Slice 1 carry-forwards re-walked at Slice 2 final verification:**

- Slice 1 sub-bullet 1 (the 5-file directory-creation contract) — ticks `- [x]` when `factories.py` lands per the Slice 1 artifact's deferral note.
- Slice 1 sub-bullet 3's resolver-API half — ticks fully when `apply_sync` / `apply_async` / `check_<field>_permission` dispatch land per the Slice 1 artifact's partial-tick deferral.

**Note on the Decision 5 `Ordering` enum content.** The Slice 2 contract above names `Ordering` as the leaf-field type produced by `_build_class_type`; the enum itself shipped in Slice 1. Slice 2's `_build_input_fields` / `convert_order_field_to_input_annotation` import `Ordering` from `orders/inputs.py` (the same module that hosts the new adapters).

---

## Build report (Worker 2)

### Files touched

- `django_strawberry_framework/orders/factories.py` — replaced the Slice 1 TODO-anchored placeholder with the BFS `OrderArgumentsFactory` (class-level `input_object_types` + `_type_orderset_registry` caches, `__init_subclass__` rejection, `__init` + `arguments` property + `_ensure_built` BFS + `_build_class_type`). A bottom-of-file TODO anchor names spec-028 Decision 12's deferred Layer 6 (`_dynamic_orderset_cache` / `get_orderset_class`) for `0.0.9`.
- `django_strawberry_framework/orders/inputs.py` — replaced the two Slice 1 TODO anchors with the full Slice 2 adapter surface: `FieldSpec` dataclass + `_field_specs` provenance ledger, `_get_concrete_field_names_for_order` (`hasattr(f, "column") and not f.many_to_many` per Spec Revision 4 B4), `_camel_case` helper, `convert_order_field_to_input_annotation` (one-liner returning `Ordering | None`), `build_input_class` (verbatim port of filter side), `_build_input_fields` (operator-bag-free walk of `orderset_cls.get_fields()`), `normalize_input_value` (recursive descent over the Strawberry dataclass), `_iter_orderset_subclasses` (verbatim port), and `clear_order_input_namespace` (mirror of filter side; modules-stay-parked per Revision 4 B2; does NOT touch `_helper_referenced_ordersets` per Spec line 775).
- `django_strawberry_framework/orders/sets.py` — replaced the Slice 1 `NotImplementedError` placeholder in `_expand_meta_fields` with the `_get_concrete_field_names_for_order` walk (raises `ConfigurationError` when `Meta.model` is missing); REMOVED the structurally-unreachable `_is_expanding_fields` reentry-branch test per the Plan's disposition (the `_is_expanding_fields` slot itself stays for future defensive use, only the explicit test is dropped). Added the resolver-facing classmethod pair `apply_sync` / `apply_async` (no `apply(...)` dispatcher per Spec DoD 4(c)), the `check_permissions` instance method (cookbook compatibility), `_normalize_input` delegate, `_request_from_info`, `_extract_branch_value`, `_iter_active_related_branches` (active-input-only walk that flattens top-level lists), `_active_permission_field_paths`, `_invoke_permission_method`, `_run_permission_checks` (active-branch double-dispatch + dedup-map shape mirrored from `FilterSet._run_permission_checks` minus the operator-bag recursion), and `get_flat_orders` (prefix-applying pass-through over normalized data).
- `django_strawberry_framework/orders/__init__.py` — added the `_helper_referenced_ordersets: set[type[OrderSet]] = set()` ledger (co-located with its only writer per Spec Decision 2 N-new-2 of rev2) and the `order_input_type(orderset_class)` Decision-11 consumer helper (eager `TypeError` for non-`OrderSet` arguments; idempotent set semantics; returns `Annotated[name, strawberry.lazy(INPUTS_MODULE_PATH)]` per Spec Revision 4 B1 — element type, not list type). Re-exported `OrderArgumentsFactory` at module scope (advanced-use; kept OUT of `__all__` per filter-side precedent at `filters/__init__.py`). Extended `__all__` to include `"order_input_type"`.
- `tests/orders/test_factories.py` — replaced the TODO-anchored stub with 9 tests covering the BFS factory: reachable-via-BFS, cycle-via-seen-set, leaf-annotation shape, related-branch lazy-Annotated shape, collision raise, `.arguments` idempotency, shared class-level cache across instances, subclass-rejection at class-creation time, and a `RelatedOrder(None, ...)` placeholder skip.
- `tests/orders/test_sets.py` — REWROTE the obsolete `test_orderset_meta_fields_all_raises_until_slice_2` test as `test_orderset_meta_fields_all_expands_to_column_backed_fields_per_cookbook` (positive contract). Appended 26 Slice 2 tests covering: `__all__` without `Meta.model` raise, explicit-`RelatedOrder`-overrides-column-leaf, the resolver-facing surface (`apply_sync` order_by emission / empty-input / all-null / multi-field priority / `GraphQLError`-propagation; `apply_async` parity + `sync_to_async` permission wrap), active-input-only per-field gates (denies-for-active / quiet-for-inactive), active-branch double-dispatch (parent gate / child gate / dedup-across-list-elements), `_request_from_info` shapes (canonical / bare HttpRequest / unrecognized / None context), `get_flat_orders` walker (pass-through + prefix-application), `_normalize_input` delegate smoke, and `check_permissions` instance-method cookbook-compat (delegates / no-op when no input parked).
- `tests/orders/test_inputs.py` — APPEND-ONLY; added 22 Slice 2 tests covering `_get_concrete_field_names_for_order` (forward FK / M2M / reverse FK), `convert_order_field_to_input_annotation` (Ordering | None regardless of model field), `normalize_input_value` (nested-RelatedOrder flat-path / empty list / null-direction skip / multi-element / None input), `build_input_class` (Strawberry decoration + python_attr -> graphql alias mapping), `_build_input_fields` populates `_field_specs` (leaf / flat-shorthand / RelatedOrder shapes), `clear_order_input_namespace` (ledger reset / module-globals stay parked / factory caches cleared / subclass binding slots reset), and `order_input_type` (Annotated element-type / TypeError for non-OrderSet / ledger record / idempotency).

### Tests added or updated

- `tests/orders/test_factories.py::test_factory_visits_every_reachable_relatedorder_target_via_bfs` — BFS visits Book / Shelf / Genre ordersets.
- `tests/orders/test_factories.py::test_factory_handles_cycles_via_seen_set` — `A -> B -> A` mutual cycle terminates; both built exactly once.
- `tests/orders/test_factories.py::test_factory_builds_leaf_fields_with_ordering_or_none_annotation` — leaf annotation is `Ordering | None`.
- `tests/orders/test_factories.py::test_factory_builds_relatedorder_fields_with_annotated_strawberry_lazy_forward_reference` — `RelatedOrder` field carries `Annotated[ForwardRef("...InputType"), strawberry.lazy(...)] | None`.
- `tests/orders/test_factories.py::test_factory_raises_on_two_distinct_ordersets_sharing_classname` — collision raise names both qualnames + `OrderArgumentsFactory`.
- `tests/orders/test_factories.py::test_factory_arguments_is_idempotent` — repeated `.arguments` reads return the same class instance.
- `tests/orders/test_factories.py::test_factory_input_object_types_shared_across_factory_instances` — class-level dict shared across `OrderArgumentsFactory(A)` + `OrderArgumentsFactory(B)`.
- `tests/orders/test_factories.py::test_factory_subclass_rejected_at_class_creation_time` — `TypeError` at class-definition time.
- `tests/orders/test_factories.py::test_factory_skips_related_order_with_none_target` — placeholder `RelatedOrder(None, ...)` does not raise.
- `tests/orders/test_sets.py::test_orderset_meta_fields_all_expands_to_column_backed_fields_per_cookbook` — REWRITTEN; positive `"__all__"` contract.
- `tests/orders/test_sets.py::test_orderset_meta_fields_all_raises_configurationerror_without_meta_model` — missing `Meta.model` -> `ConfigurationError`.
- `tests/orders/test_sets.py::test_orderset_get_fields_all_with_explicit_relatedorder_override_replaces_column_leaf` — `Meta.fields = "__all__"` + explicit `RelatedOrder("shelf")` -> `fields["shelf"]` is the related order, not None.
- `tests/orders/test_sets.py::test_orderset_apply_sync_applies_order_by_to_queryset` — single `Ordering.ASC` leaf -> non-empty `query.order_by`.
- `tests/orders/test_sets.py::test_orderset_apply_sync_returns_unmodified_queryset_for_empty_input` — `[]` -> identity queryset reference.
- `tests/orders/test_sets.py::test_orderset_apply_sync_returns_unmodified_queryset_for_all_null_directions` — all-None directions -> identity queryset.
- `tests/orders/test_sets.py::test_orderset_apply_sync_emits_multi_field_priority` — two leaves -> two `query.order_by` entries in declaration order.
- `tests/orders/test_sets.py::test_orderset_apply_sync_propagates_graphqlerror_from_check_permission` — `GraphQLError` from `check_*_permission` propagates pre-`order_by`.
- `tests/orders/test_sets.py::test_orderset_apply_async_via_asyncio_run` — async path produces equivalent `query.order_by`.
- `tests/orders/test_sets.py::test_orderset_apply_async_runs_check_permission_in_sync_to_async` — `GraphQLError` propagates through `sync_to_async` boundary.
- `tests/orders/test_sets.py::test_orderset_check_permission_denies_for_active_field` — active leaf fires gate.
- `tests/orders/test_sets.py::test_orderset_check_permission_quiet_for_inactive_field` — None leaf does NOT fire gate.
- `tests/orders/test_sets.py::test_orderset_check_permission_active_relatedorder_branch_fires_parent_gate` — parent's `check_shelf_permission` fires for active branch.
- `tests/orders/test_sets.py::test_orderset_check_permission_active_relatedorder_branch_fires_child_gate` — child orderset's gate fires via recursion.
- `tests/orders/test_sets.py::test_orderset_check_permission_dedups_repeated_list_entries` — same field across two list elements fires each gate exactly once.
- `tests/orders/test_sets.py::test_orderset_request_from_info_reads_context_request_attribute` — canonical `info.context.request`.
- `tests/orders/test_sets.py::test_orderset_request_from_info_reads_bare_httprequest_context` — `info.context = HttpRequest()`.
- `tests/orders/test_sets.py::test_orderset_request_from_info_raises_on_unrecognized_context_shape` — random object context -> `ConfigurationError`.
- `tests/orders/test_sets.py::test_orderset_request_from_info_raises_when_info_context_is_none` — `info.context = None` -> raise.
- `tests/orders/test_sets.py::test_orderset_get_flat_orders_walks_normalized_pairs` — pass-through with no prefix.
- `tests/orders/test_sets.py::test_orderset_get_flat_orders_applies_prefix` — prefix concatenation.
- `tests/orders/test_sets.py::test_orderset_normalize_input_delegates_to_module_helper` — classmethod delegates to `normalize_input_value`.
- `tests/orders/test_sets.py::test_orderset_check_permissions_instance_method_delegates` — bound-method cookbook compat routes through classmethod pipeline.
- `tests/orders/test_sets.py::test_orderset_check_permissions_instance_tolerates_no_input_value` — bare instance without parked input is a no-op.
- `tests/orders/test_inputs.py::test_get_concrete_field_names_for_order_includes_forward_fk_column` — forward FK present.
- `tests/orders/test_inputs.py::test_get_concrete_field_names_for_order_excludes_m2m_managers` — M2M excluded.
- `tests/orders/test_inputs.py::test_get_concrete_field_names_for_order_excludes_reverse_fk` — reverse FK excluded.
- `tests/orders/test_inputs.py::test_convert_order_field_to_input_annotation_returns_ordering_or_none` — `Ordering | None` regardless of `model_field`.
- `tests/orders/test_inputs.py::test_normalize_input_value_walks_nested_relatedorder_into_flat_field_paths` — `shelf -> code` -> `shelf__code` flat path.
- `tests/orders/test_inputs.py::test_normalize_input_value_passes_through_empty_list` — `[]` -> `[]`.
- `tests/orders/test_inputs.py::test_normalize_input_value_skips_null_direction_leaves` — `direction=None` leaves are skipped.
- `tests/orders/test_inputs.py::test_normalize_input_value_handles_top_level_list_of_dataclass_elements` — multi-element list preserves declaration order.
- `tests/orders/test_inputs.py::test_normalize_input_value_returns_empty_for_none_input` — `None` -> `[]`.
- `tests/orders/test_inputs.py::test_build_input_class_decorates_with_strawberry_input` — `__strawberry_definition__` is present.
- `tests/orders/test_inputs.py::test_build_input_class_handles_python_attr_to_graphql_alias_mapping` — `name=` alias survives the decorator.
- `tests/orders/test_inputs.py::test_field_specs_populated_by_build_input_fields_for_leaf` — leaf `FieldSpec(python=title, graphql=title, django=title)`.
- `tests/orders/test_inputs.py::test_field_specs_populated_by_build_input_fields_for_flat_shorthand` — `Meta.fields = ["shelf__code"]` -> `FieldSpec(python=shelf_code, graphql=shelfCode, django=shelf__code)`.
- `tests/orders/test_inputs.py::test_field_specs_populated_by_build_input_fields_for_relatedorder` — `FieldSpec(django=shelf)` for the related field.
- `tests/orders/test_inputs.py::test_clear_order_input_namespace_resets_materialized_names_ledger` — ledger emptied after clear.
- `tests/orders/test_inputs.py::test_clear_order_input_namespace_leaves_module_globals_parked` — class object stays at `sys.modules[INPUTS_MODULE_PATH]` (parking-is-load-bearing per B2).
- `tests/orders/test_inputs.py::test_clear_order_input_namespace_clears_factory_class_level_caches` — `OrderArgumentsFactory.input_object_types` + `_type_orderset_registry` emptied.
- `tests/orders/test_inputs.py::test_clear_order_input_namespace_resets_orderset_subclass_binding_state` — `_owner_definition` / `_expanded_fields` / `_is_expanding_fields` delattr'd; inherited defaults restored.
- `tests/orders/test_inputs.py::test_order_input_type_returns_element_annotation_for_orderset_subclass` — `Annotated[ForwardRef("...InputType"), strawberry.lazy(...)]` element shape.
- `tests/orders/test_inputs.py::test_order_input_type_raises_typeerror_for_non_orderset` — non-`OrderSet` argument -> `TypeError`.
- `tests/orders/test_inputs.py::test_order_input_type_records_orderset_into_helper_referenced_set` — ledger writes happen.
- `tests/orders/test_inputs.py::test_order_input_type_is_idempotent_under_repeated_calls` — set semantics dedup repeat calls.

### Validation run

- `uv run ruff format .` — pass; 213 files left unchanged (no formatter rewrites of the new / expanded files).
- `uv run ruff check --fix .` — pass; 2 auto-fixed (unused `Union` import + `if`-`else`-block -> ternary in `tests/orders/test_inputs.py`); both fixes landed in the test file under "Files touched"; one hidden unsafe-fix declined (`_ = Union[int, str]` artifact removed manually). Final state: `All checks passed!`.
- `git status --short` after both ruff invocations:
  - **Slice-intended (in the diff; appears in `### Files touched`):**
    - `M django_strawberry_framework/orders/__init__.py`
    - `M django_strawberry_framework/orders/factories.py`
    - `M django_strawberry_framework/orders/inputs.py`
    - `M django_strawberry_framework/orders/sets.py`
    - `M tests/orders/test_factories.py`
    - `M tests/orders/test_inputs.py`
    - `M tests/orders/test_sets.py`
  - **Slice-1 carry-forward (in working-tree from the prior slice not yet committed; left untouched in Slice 2):** `M django_strawberry_framework/orders/base.py`, `M tests/orders/test_base.py` — both reflect Slice 1's committed-but-uncommitted state; Slice 2 does not edit either.
  - **Baseline-dirty out-of-scope (untouched per build-plan rule):** `TODAY.md`, `django_strawberry_framework/management/commands/export_schema.py`, `django_strawberry_framework/optimizer/_context.py`, `django_strawberry_framework/optimizer/extension.py`, `django_strawberry_framework/optimizer/field_meta.py`, `django_strawberry_framework/optimizer/hints.py`, `docs/GLOSSARY.md`, `docs/TREE.md`, `docs/review/rev-filters.md`, `docs/review/review-0_0_7.md`, `docs/review/rev-management.md` (untracked), `docs/review/rev-management__commands.md` (untracked), `docs/review/rev-management__commands__export_schema.md` (untracked), `docs/review/rev-optimizer___context.md` (untracked), `docs/review/rev-optimizer__extension.md` (untracked), `docs/review/rev-optimizer__field_meta.md` (untracked), `docs/review/rev-optimizer__hints.md` (untracked), `docs/GLOSSARY2.md` (untracked), `scripts/build_glossary_md.py`, `examples/fakeshop/README.md`, `examples/fakeshop/apps/glossary/migrations/0001_initial.py`, `examples/fakeshop/db.sqlite3`, `scripts/import_glossary_md.py`.
  - **Maintainer concurrent work surfaced mid-build (treat as baseline-dirty per AGENTS.md "Unexpected file modifications"; flagged for Worker 1):** `M django_strawberry_framework/optimizer/plans.py`, `M django_strawberry_framework/optimizer/walker.py`, `M tests/optimizer/test_walker.py`, `M docs/spec-028-orders-0_0_8.md` (Worker 1's exclusive surface — left untouched), `M examples/fakeshop/apps/glossary/admin.py`, `M examples/fakeshop/apps/glossary/filters.py`, `M examples/fakeshop/apps/glossary/models.py`, `M examples/fakeshop/apps/glossary/schema.py`, `M examples/fakeshop/apps/kanban/admin.py`, `M examples/fakeshop/apps/kanban/filters.py`, `M examples/fakeshop/apps/kanban/models.py`, `M examples/fakeshop/apps/kanban/schema.py`, `M examples/fakeshop/test_query/test_glossary_api.py`, `?? docs/review/rev-optimizer__plans.md`, `?? docs/review/rev-optimizer__walker.md`, `?? examples/fakeshop/apps/glossary/migrations/0002_remove_glossarydocument_glossary_document_key_required_and_more.py`, `?? examples/fakeshop/apps/kanban/migrations/0012_alter_boarddoc_options_boarddoc_include_heading_and_more.py`.
  - **Expected pre-flight deletion (recorded in the build plan preamble):** `D docs/builder/build-021-filters-0_0_8.md`.
  - **Build artifact (in-scope for this cycle):** `?? docs/builder/bld-slice-2-factories.md` (this file).
  - No tool-induced drift to revert; every modified file is either slice-intended, a Slice 1 carry-forward, pre-recorded as baseline-dirty, or maintainer concurrent work surfaced mid-build (flagged for Worker 1 reconciliation).
- Focused test run (used for sanity only, no `--cov*` flags per BUILD.md): `uv run pytest tests/orders/ --no-cov -q` — **87 passed in 0.69s**. Confirms every Slice 2 assertion lands and Slice 1 assertions still pass.

### Implementation notes

- **`get_flat_orders` is shape-pure (preserves null directions); the `apply_sync` / `apply_async` comprehension filters `None` at the layer boundary.** Per Implementation-discretion item 5 — shape-pure keeps the cookbook's algorithmic intent and lets future DISTINCT ON tracking (deferred to `0.0.9`) extend the helper without re-deriving the null-direction handling. The skip lives at `OrderSet.apply_sync #"if direction is not None"` and the symmetric place in `apply_async`.
- **`_normalize_input` deduplicates by walking the **dataclass** fields, not the related-orders keys.** The recursion in `normalize_input_value` consults `__dataclass_fields__` then cross-references `related_orders` to decide leaf-vs-branch. Strawberry's `@strawberry.input` decoration keeps Python's `dataclasses.fields()` contract intact, so the `__dataclass_fields__` walk produces stable deterministic ordering -- the foundation of the spec's "list element order is the multi-field tie-breaker" contract (Spec Decision 5).
- **`_extract_branch_value` does NOT collapse `UNSET` to `None` (divergence from filter side documented in the docstring).** Order inputs never see an UNSET sentinel because `order_input_type` produces `list[<T>!] | None` -- the per-attribute defaults inside the dataclass are `None` (set by `build_input_class`'s `default=None` per-field), not UNSET. If a future spec extends the order argument shape with optional struct-shaped wrappers, this method may need the UNSET collapse the filter side has.
- **BFS FIFO discipline (`pending.pop(0)`).** Per Implementation-discretion item 1 -- chose FIFO to match the filter side's `FilterArgumentsFactory._ensure_built` shape. The cookbook uses LIFO at order_arguments_factory.py:88; both yield the same set of reachable classes, but the filter-side symmetry makes cross-slice review trivial. Recorded so Worker 3 has the rationale.
- **`check_permissions` shipped as BOTH the classmethod pipeline (`_run_permission_checks`) AND the instance method (`check_permissions`).** Per Implementation-discretion item 2 -- instance method delegates to `type(self)._run_permission_checks(self._input_value, request)` for cookbook compat. The classmethod is the canonical pipeline `apply_sync` / `apply_async` call.
- **`_model_field_for_order` helper NOT shipped (Implementation-discretion item 3).** The converter `convert_order_field_to_input_annotation` ignores `model_field` today (always returns `Ordering | None`); `_build_input_fields` passes `None` rather than walking `model._meta.get_field(...)` per the planner's recommendation. The argument remains in the signature for future-extension (Spec Decision 12 / `0.0.9`).
- **`OrderArgumentsFactory` kept OUT of `__all__` in `orders/__init__.py` per Implementation-discretion item 6.** Matches the filter side's precedent at `filters/__init__.py` (`FilterArgumentsFactory` is module-importable but not in `__all__`). Consumers needing advanced access write `from django_strawberry_framework.orders.factories import OrderArgumentsFactory`.
- **`_is_expanding_fields` reentry-branch test REMOVED from `_expand_meta_fields` per the Plan's "Restructure recommendation".** The `_is_expanding_fields` slot itself stays in place (load-bearing for future defensive use); the explicit early-return branch was structurally unreachable through Slice 2's planned surface (the BFS produces no recursive `get_fields()` call on the same class within an in-flight expansion). The existing `test_orderset_get_fields_caches_on_resolved_related_orders` + `test_orderset_get_fields_does_not_cache_with_unresolved_string_target` tests still cover the cache-write-gate paths.

### Notes for Worker 3

- **No shadow file consulted during build.** The Plan's helper-disposition entry notes that Worker 3's review-time helper trigger fires for both `orders/sets.py` (now ~390 source lines) and `orders/inputs.py` (now ~390 source lines); both cross the 150-line threshold AND `orders/sets.py` adds ≥30 lines of new logic. Worker 3 should run `scripts/review_inspect.py --output-dir docs/shadow` against these two files at review time per BUILD.md.
- **Active-input-only / double-dispatch / dedup map shape mirrors the filter side `FilterSet._run_permission_checks` exactly minus the operator-bag recursion.** The dedup map key is the OrderSet class; the value is the set of method names that have already fired. Worker 3 should verify the cross-class behavior (parent's gate fires once across N list elements per-`(parent_class, method_name)`; child's gate fires once per `(child_class, method_name)`).
- **`normalize_input_value` does NOT preserve dict-vs-dataclass distinction symmetrically.** The Strawberry input dataclass is the canonical input shape; a plain `dict` short-circuits through `__dataclass_fields__` is None -> empty list. If a future Slice 4 live-HTTP test needs to drive `apply_sync` with a raw dict, that path needs a separate code path (today the resolver always receives the decoded dataclass).
- **`_helper_referenced_ordersets` lives in `orders/__init__.py`, NOT `orders/inputs.py`.** Spec Decision 9 line 775 + Spec Decision 2 N-new-2 of rev2 pin this placement; `clear_order_input_namespace()` in `inputs.py` does NOT touch the ledger. Worker 3 should verify by grep: `grep -n "_helper_referenced_ordersets" django_strawberry_framework/orders/inputs.py` returns empty.
- **`order_input_type` returns the ELEMENT type, not a list type.** Per Spec Revision 4 B1, consumers wrap as `list[order_input_type(MyOrder)] | None`. Tests pin the element shape via `typing.get_args(result)[0]` returning a `ForwardRef("...InputType")`.

### Notes for Worker 1 (spec reconciliation)

- **Maintainer concurrent work mid-build (`docs/spec-028-orders-0_0_8.md`).** The active spec shows as `M` in `git status --short`. Worker 2 left it untouched per the slice scoping ("Worker 1 is the only worker authorized to mutate the spec"). Worker 1's final-verification pass should reconcile the spec edits with Slice 2's contract — if the spec edits reflect status-header updates from the Slice 1 close, no action needed; if they touch Slice 2 contract surface, a sync-pass against the build is warranted.
- **Slice 1 worker-memory carry-forward correction (per Plan DRY analysis).** The Slice 1 TODO anchor at `orders/inputs.py` bottom (now removed in this slice) named `clear_order_input_namespace` as a Slice 3 deliverable; spec-028 Decision 9's lifecycle clause + Slice 5 DoD item 6 + the Plan's DRY analysis confirm the helper is a Slice 2 deliverable (Slice 3 only wires `registry.clear()` to call it). Slice 2 shipped the helper as planned; recording here so the Slice 1 worker-memory carry-forward note is captured for Worker 1's spec-reconciliation sweep.
- **Maintainer concurrent work in `docs/builder/build-028-orders-0_0_8.md` baseline-dirty list (additions).** The `git status --short` mid-build now shows `M django_strawberry_framework/optimizer/plans.py`, `M django_strawberry_framework/optimizer/walker.py`, `M tests/optimizer/test_walker.py`, `?? docs/review/rev-optimizer__plans.md`, `?? docs/review/rev-optimizer__walker.md`, and two new migration files (`?? examples/fakeshop/apps/glossary/migrations/0002_remove_glossarydocument_glossary_document_key_required_and_more.py`, `?? examples/fakeshop/apps/kanban/migrations/0012_alter_boarddoc_options_boarddoc_include_heading_and_more.py`). Suggest adding these to the build plan's baseline-dirty list (Worker 0 edit) so Slice 3+ doesn't re-flag them.
- **`OrderSet.check_permissions` instance-method shape decision.** Worker 2 chose to ship BOTH the classmethod pipeline AND a thin instance-method delegate (cookbook compat per Implementation-discretion item 2). Spec Decision 8 mentions only the classmethod pipeline by name; the cookbook compat layer is a defensive nicety. Worker 1 may consider whether to record the dual-shape contract in the spec body or leave it as a planning-pass implementation detail.

---

## Review (Worker 3)

### High:

None.

### Medium:

None.

### Low:

#### `_iter_active_related_branches` recurses into nested lists, but lists are top-level-only by spec

`OrderSet._iter_active_related_branches` (django_strawberry_framework/orders/sets.py lines 297-300) handles `isinstance(input_value, list)` by recursing into each element. Same shape appears in `_active_permission_field_paths` (lines 322-329). This is correct for the top-level `list[<T>OrderInputType] | None` argument shape per Spec Decision 5, but the recursion is unbounded — if a future spec extends `RelatedOrder` to accept a `list[...]` child input, the existing helper handles it naturally. The current implementation is correct for `0.0.8`; the recursion shape is defensive future-proofing that matches the cookbook's `get_flat_orders` algorithm. No change needed; flagging as Low so a future cycle that adds nested-list semantics doesn't double-implement the dispatch.

```django_strawberry_framework/orders/sets.py:297
        if isinstance(input_value, list):
            for element in input_value:
                active.extend(cls._iter_active_related_branches(element))
            return active
```

#### Missing test: `clear_order_input_namespace` tolerates unimported factory module

Plan step 7 / Slice 2 test list named `test_clear_order_input_namespace_tolerates_unimported_factory_module` (Plan line 354) — a test that calls `clear_order_input_namespace()` before `orders.factories` has been imported and asserts no `ImportError` propagates. Worker 2 did not land this test. The local-import-with-`pass`-on-ImportError code path at `orders/inputs.py` lines 459-465 + 474-477 exists but is not directly exercised. The contract is symmetric with the filter side's `clear_filter_input_namespace`. Low because both `try` blocks are visibly defensive code that mirrors the shipped filter side verbatim; a follow-up cycle can add the regression test if a partial-load environment ever surfaces.

### DRY findings

None. Every divergence from the filter subsystem is spec-justified:
- No `_build_logic_fields` call in `OrderArgumentsFactory._build_class_type` — Spec Decision 8 line 686 ("no operator-bag").
- No `HIDE_FLAT_FILTERS` skip in `_build_input_fields` — order side has no `*__lookup` flat-path expansion.
- No `_derive_*_visibility_querysets_async` in `apply_async` — Spec Decision 8 step 4 line 652 ("re-derivation has no parallel for order side").
- No `OrderSet.apply(...)` dispatcher — Spec DoD 4(c) + Decision 8 line 676.
- BFS uses FIFO (`pending.pop(0)`) matching the filter side at `filters/factories.py::FilterArgumentsFactory._ensure_built` rather than the cookbook's LIFO (`pending.pop()`). Recorded in Implementation Notes (build report). Both yield the same set of reachable classes; FIFO is the filter-side symmetry choice.
- Collision-error wording at `orders/factories.py::OrderArgumentsFactory._ensure_built` substitutes `OrderArgumentsFactory` / `OrderSet` for filter-side `FilterArgumentsFactory` / `FilterSet` (parallel shape, parallel error).
- Materialize-collision wording at `orders/inputs.py::materialize_input_class` says "two distinct OrderSet input classes" (subsystem-noun divergence retained per the Slice 1 final-verification deferral to integration pass).
- `_iter_active_related_branches` adds a `list[...]` recursive branch the filter side does not have, because the order side accepts a top-level list at the resolver per Spec Decision 5.

The `_run_permission_checks` body (orders/sets.py lines 381-452) is structurally a parallel of `filters/sets.py::FilterSet._run_permission_checks` minus the operator-bag recursion — same `_fired: dict[type, set[str]]` dedup map shape, same active-branch double-dispatch (parent + child gates fire once each per class), same `object.__new__(cls)` bare-instance pattern. Verified by reading both bodies side-by-side. The simplification (no `and`/`or`/`not` recursion) is justified by Spec Decision 8 line 686. The bodies are intentionally not factored into a shared helper because the parent class — `FilterSet` vs `OrderSet` — is the binding the dedup key needs and the call patterns through `_iter_active_related_branches` are subsystem-specific.

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` returns empty. `__all__` and the re-export list at the package root are unchanged. Inside `orders/__init__.py`, `__all__` expanded to `("Ordering", "OrderSet", "OrderSetMetaclass", "RelatedOrder", "order_input_type")` per Spec Decision 11 + Decision 2 — authorized growth at the subsystem boundary, NOT the package-level public surface. `OrderArgumentsFactory` is module-importable but kept out of `__all__` per the filter-side precedent (Implementation discretion item 6 of the Plan). Conforms with the spec contract.

### CHANGELOG sanity

Not applicable; slice did not modify CHANGELOG.md.

### Documentation / release sanity

Not applicable; slice did not modify docs/release/KANBAN/archive surfaces.

### What looks solid

- Spec slice checklist (verbatim) sub-bullets BOTH addressed by the diff. `factories.py` ships `OrderArgumentsFactory` with the BFS walk, the `_build_class_type` Strawberry-decoration shape, the leaf-`Ordering | None` annotation, and the `Annotated[<TargetOrderSet>InputType, strawberry.lazy(...)] | None` related-branch annotation. `inputs.py` ships `_build_input_fields`, `convert_order_field_to_input_annotation`, `normalize_input_value`, AND the supporting `build_input_class` + `FieldSpec` + `_field_specs` provenance ledger + `_get_concrete_field_names_for_order` + `_camel_case` + `_iter_orderset_subclasses` + `clear_order_input_namespace`.
- Slice 1 carry-forward sub-bullets (Plan reconciliation): the directory-creation bullet's `factories.py` deliverable lands here as promised; the resolver-facing API surface (`apply_sync` / `apply_async` / `check_<field>_permission` dispatch) ships in `orders/sets.py`.
- Spec Decision 5: `Ordering.resolve` semantics preserved — bare `ASC`/`DESC` leave `nulls_first` and `nulls_last` as `None` (Django sentinel for "no NULLS clause"). Slice 2 does not regress the Slice 1 contract.
- Spec Decision 8 step 6: `_run_permission_checks` body fires gates active-input-only AND double-dispatches on active `RelatedOrder` branches (parent's `check_<branch>_permission` + child orderset's `check_<field>_permission`). The `_fired: dict[type, set[str]]` dedup map fires each gate at most once per `(class, method-name)` pair across the entire input. Verified by `test_orderset_check_permission_dedups_repeated_list_entries`.
- Spec Decision 8 step 6: active-input-only scope — `test_orderset_check_permission_quiet_for_inactive_field` pins that an absent (None) leaf does NOT fire its gate. Worker 3 probe at `docs/builder/temp-tests/slice-2/test_review_probes.py::test_inactive_relatedorder_branch_quiet_for_both_gates` extends the contract to verify a RelatedOrder branch absence is quiet for BOTH parent and child gates.
- Spec Decision 8 step 7: expression conversion lives in `apply_sync`/`apply_async`, NOT inside `get_flat_orders`. `get_flat_orders` is shape-pure (preserves null directions); the apply pipeline filters at the comprehension step. Matches the layering note in Spec Decision 8 step 7 verbatim.
- Spec Decision 8 sync/async split: `apply_async` is `async def` with return annotation `-> models.QuerySet` (NOT `Awaitable[QuerySet]`) per Spec Revision 4 N2. `sync_to_async(cls._run_permission_checks, thread_sensitive=True)` wraps ONLY the permission step; `get_flat_orders` and `.order_by(...)` are NOT wrapped per Spec Decision 8 N7. No `OrderSet.apply(...)` dispatcher (Spec DoD 4(c)) — verified by `docs/builder/temp-tests/slice-2/test_review_probes.py::test_orderset_has_no_apply_dispatcher_method`.
- Spec Decision 9 line 775 + Decision 2 N-new-2: `_helper_referenced_ordersets` ledger lives in `orders/__init__.py` co-located with its only writer (`order_input_type`). `clear_order_input_namespace()` in `inputs.py` does NOT touch the ledger — verified by `docs/builder/temp-tests/slice-2/test_review_probes.py::test_clear_order_input_namespace_does_not_touch_helper_ledger`. The two-block layout for Slice 3's `registry.clear()` integration is set up correctly.
- Spec Decision 9 / Revision 4 B2: `clear_order_input_namespace` leaves materialized module globals parked in `sys.modules[INPUTS_MODULE_PATH].__dict__` — verified by `tests/orders/test_inputs.py::test_clear_order_input_namespace_leaves_module_globals_parked`. No `delattr` against the module dict — confirmed by reading `orders/inputs.py::clear_order_input_namespace` lines 452-488.
- Spec Decision 11 / Revision 4 B1: `order_input_type(orderset_class)` returns the ELEMENT type (`Annotated[<Name>OrderInputType, strawberry.lazy(...)]`), NOT a list type. Consumers wrap as `list[order_input_type(MyOrder)] | None`. Verified by `tests/orders/test_inputs.py::test_order_input_type_returns_element_annotation_for_orderset_subclass` AND `docs/builder/temp-tests/slice-2/test_review_probes.py::test_order_input_type_return_is_not_a_list_type`.
- Spec Revision 4 B4: `_get_concrete_field_names_for_order(model)` uses `hasattr(f, "column") and not getattr(f, "many_to_many", False)`. Forward FK columns are included; M2M and reverse FK are excluded. Verified by three tests: `test_get_concrete_field_names_for_order_includes_forward_fk_column`, `test_get_concrete_field_names_for_order_excludes_m2m_managers`, `test_get_concrete_field_names_for_order_excludes_reverse_fk`. The empirical-vs-documented Django M2M rationale lives in the helper's docstring.
- `_is_expanding_fields` slot stays in place at `orders/sets.py` line 124; the explicit reentry-branch TEST was removed from `_expand_meta_fields` per the Plan's "Restructure recommendation". The slot is load-bearing for future defensive use; Slice 1's tests `test_orderset_get_fields_caches_on_resolved_related_orders` and `test_orderset_get_fields_does_not_cache_with_unresolved_string_target` still cover the cache-write-gate paths.
- `clear_order_input_namespace` ships in Slice 2 (NOT Slice 3) per the Worker 1 plan's reconciliation of the Slice 1 carry-forward note.
- `OrderArgumentsFactory.__init_subclass__` rejects subclassing at class-creation time (verified by `test_factory_subclass_rejected_at_class_creation_time`). Mirrors the filter side's pattern verbatim.
- Cycle handling: `RelatedOrder(None, ...)` placeholders are skipped silently in `_build_input_fields` (line 271-274) AND in `_ensure_built` (line 159) — verified by `test_factory_skips_related_order_with_none_target` AND `test_factory_handles_cycles_via_seen_set`.
- `_field_specs` provenance map keyed on `(OrderSet class, python_attr)` populated correctly for leaf / flat-shorthand / RelatedOrder shapes (three dedicated tests).
- Inspection helper output (`docs/shadow/django_strawberry_framework__orders__sets.overview.md`, `..__inputs.overview.md`, `..__factories.overview.md`, `..____init__.overview.md`) walked. Control-flow hotspots in `sets.py`: `get_fields` (41 lines, 5 branches), `_active_permission_field_paths` (44 lines, 11 branches), `_run_permission_checks` (72 lines, 7 branches), `apply_async` (42 lines, 2 branches). Each hotspot has spec backing and dedicated tests. No unexpected ORM-marker patterns. Repeated-literal scan: `"_owner_definition"`, `"_expanded_fields"`, `"_is_expanding_fields"` each appear 2-3 times across `orders/sets.py` and `orders/inputs.py` — load-bearing identifiers tied to the binding seam contract; consolidation would not improve readability.

### Temp test verification

- `docs/builder/temp-tests/slice-2/test_review_probes.py` — 5 probes covering:
  1. `test_inactive_relatedorder_branch_quiet_for_both_gates` — confirms inactive `RelatedOrder` branch is quiet for BOTH parent and child gates (extends the Slice 2 active-branch coverage). All 5 probes pass.
  2. `test_orderset_has_no_apply_dispatcher_method` — confirms no `apply()` exists on `OrderSet` (Spec DoD 4(c)).
  3. `test_clear_order_input_namespace_does_not_touch_helper_ledger` — confirms the ledger survives the clear (Spec Decision 9 two-block contract).
  4. `test_order_input_type_return_is_not_a_list_type` — confirms `order_input_type` returns the element type (Spec Revision 4 B1).
  5. `test_materialize_input_class_collision_uses_orderset_wording` — confirms order side keeps `"OrderSet"` wording at the collision error.

Disposition: KEPT under `docs/builder/temp-tests/slice-2/` as scratch; permanent suite already exercises these contracts via `tests/orders/test_sets.py::test_orderset_check_permission_quiet_for_inactive_field` AND `tests/orders/test_inputs.py::test_order_input_type_returns_element_annotation_for_orderset_subclass`. Probe 1 specifically (`test_inactive_relatedorder_branch_quiet_for_both_gates`) is a stricter contract than the existing `test_orderset_check_permission_quiet_for_inactive_field` (which only exercises an inactive leaf, not an inactive related-branch). Worker 2 may promote this probe to `tests/orders/test_sets.py` in a follow-up cycle; recorded as a Low optional enhancement, not a finding.

### Focused test run

`uv run pytest tests/orders/ --no-cov -q` — **87 passed in 0.66s**.
`uv run pytest docs/builder/temp-tests/slice-2/test_review_probes.py --no-cov -q` — **5 passed in 0.55s**.

### Notes for Worker 1 (spec reconciliation)

- **`OrderSet.check_permissions` instance method shipped as dual surface** — Worker 2 ships BOTH the classmethod pipeline (`_run_permission_checks`) AND the instance method delegate (`check_permissions`). Spec Decision 8 step 6 mentions only the classmethod pipeline by name. The filter side ALSO ships both shapes (`filters/sets.py::FilterSet.check_permissions` line 1215 + `_run_permission_checks` line 1007), so the dual-surface choice is symmetric with the filter side, NOT divergent. Worker 3 considered escalating this to Worker 1 as the prompt's check #14 suggested, but reading the filter source side-by-side resolved the question: the order side mirrors the filter side at the surface level. **NOT escalating.** Recording so Worker 1 has the cross-side comparison if the spec body grows a `check_permissions` instance-method reference in a future revision.
- **`_iter_active_related_branches`'s top-level-list recursion is order-side-specific** (the filter side has no analogue because filter input is a single object, not a list). Spec Decision 5 says the resolver wraps as `list[order_input_type(MyOrder)] | None`, which means the input value at `apply_sync` time is a list of dataclass elements. The helper's `isinstance(input_value, list)` branch is the order-side adaptation. No spec edit needed; flagging in case Worker 1's spec-reconciliation sweep wants to add a one-line note to Spec Decision 8 step 6 about the order-side list-flatten dispatch (would close a small spec-body ambiguity that the filter side does not face).

### Review outcome

`review-accepted` — all spec contracts satisfied; no High or Medium findings; two Low findings (recursive list dispatch as future-proofing, one missing planned test that is defensively code-side-equivalent to the shipped filter helper) are recorded but do not block acceptance. Slice 1 carry-forward sub-bullets (1: 5-file directory creation; 3: resolver-facing API surface) are addressed by this slice and ready for Worker 1's final-verification box-ticks.

---

## Final verification (Worker 1)

- Spec slice checklist: both Slice 2 sub-bullets (`factories.py ships OrderArgumentsFactory` / `inputs.py ships the input-data adapter functions`) ticked `- [x]` after walking the diff against the spec contract — the BFS factory + class-level caches + `__init_subclass__` rejection are all present at `django_strawberry_framework/orders/factories.py`; the four named adapter functions (`_build_input_fields`, `convert_order_field_to_input_annotation`, `normalize_input_value`, plus the supporting `build_input_class` / `FieldSpec` / `_field_specs` / `_get_concrete_field_names_for_order` / `_iter_orderset_subclasses` / `clear_order_input_namespace`) are all present at `django_strawberry_framework/orders/inputs.py`. Slice 1 carry-forwards ticked at the Slice 1 artifact in this pass: sub-bullet 1 (5-file directory-creation contract) now `- [x]` — `factories.py` landed in Slice 2; sub-bullet 3's resolver-API half (`apply_sync` / `apply_async` / `check_<field>_permission` dispatch) noted as completed in the Slice 1 artifact's parenthetical (the box itself was already `- [x]` for the Slice-1-scoped half — see `### Spec changes made (Worker 1 only)` below).
- DRY check across this slice and prior accepted slices: clean. Worker 1 ran `uv run python scripts/review_inspect.py` against `orders/sets.py`, `orders/inputs.py`, AND `orders/factories.py` (`--output-dir docs/shadow`). The **Repeated string literals** sections report only the load-bearing class-attribute names mirrored from the filter side: `"related_orders"` (5x in `sets.py`) + `"_expanded_fields"` (2x in `sets.py`) — same shape as the shipped filter side's `"related_filters"` / `"_expanded_filters"` pattern, not consolidable; `"description"` (3x in `inputs.py`) and `"__annotations__"` (2x in `inputs.py`) — Python attribute / docstring keyword tokens, not duplicate string literals across files; `None.` in `factories.py`. No unexpected cross-`orders/` duplication. The Worker 3 review-pass already walked the filter-side parallel side-by-side; no new DRY opportunity surfaces at this final-verification pass.
- Existing tests still pass: `uv run pytest tests/orders/ examples/fakeshop/apps/library/tests/ --no-cov` → **91 passed in 1.03s** (89 in `tests/orders/`, 2 in `examples/fakeshop/apps/library/tests/`). The full `tests/orders/` surface includes the Slice 1 + Slice 2 contract pins; the library acceptance tests confirm no regression on the shipped DjangoType surface.
- Spec reconciliation: ONE spec edit landed in this pass — the status header on line 4 was updated to reflect that Slice 2 has shipped (4 of 5 files → 5 of 5 files, `factories.py` now on disk). Two Worker-3-surfaced advisory items resolved without spec edits: (a) `_iter_active_related_branches`'s top-level-list recursion is the natural consequence of Spec Decision 5's `orderBy: [<T>OrderInputType!]` list-shaped argument (inputs at `apply_sync` time ARE lists by spec); adding a spec-body note would be cosmetic. (b) The dual-shape `OrderSet.check_permissions` (instance method + classmethod pipeline) matches the shipped filter side's `FilterSet.check_permissions` (`filters/sets.py:1215`) + `FilterSet._run_permission_checks` (`filters/sets.py:1007`) pattern exactly — Spec Decision 8 step 6 names the classmethod pipeline by name; the instance method is the cookbook-compat surface (`AdvancedOrderSet.check_permissions` parallel) that the spec body already references implicitly via Decision 2's `sets.py` bullet ("`check_permissions` active-input-only scope"). Worker 3's `Notes for Worker 1` explicitly noted "NOT escalating" because the filter side carries the same dual-surface. No spec edit needed. Implementation verified clean: the instance method at `django_strawberry_framework/orders/sets.py::OrderSet.check_permissions` is a one-line delegate to `type(self)._run_permission_checks(getattr(self, "_input_value", None), request)` — no recursion, no double-fire risk (the classmethod allocates its own bare instance via `object.__new__(cls)`; the instance-method's `self` is not threaded through and the classmethod walks the parked input via the shared `_fired: dict[type, set[str]]` dedup map). The Slice 1 worker-memory carry-forward (W2's flag that `clear_order_input_namespace` was mis-categorized as Slice 3 in the Slice 1 anchor wording) is resolved: the helper landed in Slice 2 as the spec (DoD item 6) always required; the Slice 1 TODO anchor that named it for Slice 3 was removed by Slice 2's expansion. No spec edit needed.
- Final status: `final-accepted`.

### Summary

Slice 2 shipped the Layer-5 BFS factory + the adapter quartet + the resolver-facing classmethod pair end-to-end. `django_strawberry_framework/orders/factories.py::OrderArgumentsFactory` walks every reachable `RelatedOrder` target via a FIFO BFS, builds each `@strawberry.input` class through `orders/inputs.py::build_input_class`, populates the class-level `input_object_types` cache, and rejects subclassing at `__init_subclass__` time. `django_strawberry_framework/orders/inputs.py` grew the four spec-named adapters — `_build_input_fields` (operator-bag-free walk of `orderset_cls.get_fields()`), `convert_order_field_to_input_annotation` (always `Ordering | None`), `normalize_input_value` (recursive descent over the Strawberry dataclass into flat `(field_path, Ordering)` tuples), plus the supporting `build_input_class` / `FieldSpec` / `_field_specs` / `_get_concrete_field_names_for_order` / `_camel_case` / `_iter_orderset_subclasses` / `clear_order_input_namespace`. `django_strawberry_framework/orders/sets.py` grew the resolver-facing `apply_sync` / `apply_async` classmethod pair (no `apply(...)` dispatcher per Spec DoD 4(c)), the cookbook-compat `check_permissions` instance method that delegates to the classmethod pipeline, and the full `_run_permission_checks` body with active-input-only scope + active-branch double-dispatch + `_fired: dict[type, set[str]]` dedup mirrored from the filter side minus the operator-bag recursion. `django_strawberry_framework/orders/__init__.py` grew the `_helper_referenced_ordersets` ledger + the `order_input_type(OrderSet)` consumer helper (returns the element type per Spec Revision 4 B1). 87 package tests pass in `tests/orders/` (Slice 1 + Slice 2 combined surface).

### Spec changes made (Worker 1 only)

- `docs/spec-028-orders-0_0_8.md` line 4 (Status header): updated from "in-progress — Slice 1 (Foundation) shipped (2026-06-01); Slices 2-6 still pending. The `django_strawberry_framework/orders/` subpackage now exists on disk with four of the five planned files (`__init__.py`, `base.py`, `inputs.py`, `sets.py`); `factories.py` lands in Slice 2." to "in-progress — Slices 1 (Foundation) and 2 (Factories) shipped (2026-06-01); Slices 3-6 still pending. The `django_strawberry_framework/orders/` subpackage now exists on disk with all five planned files (`__init__.py`, `base.py`, `factories.py`, `inputs.py`, `sets.py`)." Reason: per the spec status-line re-verification rule, the header must describe the post-slice state truthfully; Slice 2 lands `factories.py` and resolves the four-of-five → five-of-five count. Slice-triggering: Slice 2 final verification.
- `docs/builder/bld-slice-1-foundation.md` Spec slice checklist (sub-bullet 1): box ticked `- [x]`; the parenthetical updated to record that the deferred-from-Slice-1 contract for `factories.py` landed at Slice 2's final verification (2026-06-01). Worker 0's KANBAN tick at the Slice 1 build-plan row was already in place; this is the artifact-internal final-verification record per the Slice 1 plan's "box ticks at Slice 2's final verification" deferral note.
- `docs/builder/bld-slice-1-foundation.md` Spec slice checklist (sub-bullet 3, resolver-API half): parenthetical updated to record that the deferred-from-Slice-1 contract for `apply_sync` / `apply_async` / `check_<field>_permission` dispatch landed at Slice 2's final verification (2026-06-01). The box was already `- [x]` for the Slice-1-scoped class-skeleton half per the Slice 1 final-verification pass; this updates the deferral note to reflect completion.

### Notes for Slice 3 planning (Worker 0 / Worker 1 carry-forward)

- `clear_order_input_namespace` is **shipped** in `orders/inputs.py`. Slice 3's job is to wire `registry.clear()` to call it via a local-import-with-`pass`-on-ImportError block (mirror of the shipped filter side's pattern at `registry.py`). The helper does NOT touch `_helper_referenced_ordersets`; Slice 3 must add a **second, separate** `registry.clear()` block that imports `_helper_referenced_ordersets` from `orders/__init__.py` and calls `.clear()` on it (Spec Decision 9 line 775 two-block contract).
- `OrderSet.apply_sync` / `apply_async` are shipped; Slice 4's fakeshop resolvers can wire them directly via the chain `<Type>.get_queryset(qs, info)` → `<Type>Filter.apply_sync(filter, qs, info)` → `<Type>Order.apply_sync(order_by, qs, info)`. The resolver-pattern is the canonical SQL shape; Slice 4's tests pin the cooperation.
- `order_input_type(OrderSet)` returns the **element type** (not list type). Slice 4 resolvers must wrap as `order_by: list[order_input_type(MyOrder)] | None = None` to expose `orderBy: [<T>OrderInputType!]` in the SDL.
- `OrderArgumentsFactory` is module-importable from `orders.factories` but NOT in `orders/__init__.py::__all__` (Worker 2 chose this per Implementation discretion item 6, matching the filter-side precedent). Slice 5's docs surface should mirror this — list `OrderArgumentsFactory` as an "advanced use" symbol, not in the public-export bullet list alongside `OrderSet` / `RelatedOrder` / `Ordering` / `order_input_type`.
- The `_is_expanding_fields` slot stays in `orders/sets.py` for defensive purposes (the explicit reentry-branch TEST was removed in Slice 2 as dead code). If Slice 3's finalizer-phase-2.5 wiring introduces any recursive `get_fields()` call path, the slot is already in place to support a re-introduced guard test without re-introducing it speculatively now.

---

<!-- LINK DEFINITIONS -->

<!-- Root -->

<!-- docs/ -->
[spec-027]: ../SPECS/spec-027-filters-0_0_8.md

<!-- docs/SPECS/ -->

<!-- docs/builder/ -->

<!-- django_strawberry_framework/ -->

<!-- tests/ -->

<!-- examples/ -->

<!-- scripts/ -->

<!-- .venv/ -->

<!-- External -->
[upstream-cookbook-order-arguments-factory]: file:///Users/riordenweber/projects/django-graphene-filters/django_graphene_filters/order_arguments_factory.py
