# Build: Slice 2 — root `node(id:)` / `nodes(ids:)` — `DjangoNodeField` / `DjangoNodesField`

Spec reference: `docs/spec-032-full_relay-0_0_9.md` (Slice-2 checklist lines 88-94; Decision 3 at 297-313; Decision 4 at 314-334; Decision 5 at 335-356; Decision 8's no-Node-types check at 413, 419; Error shapes 253-265; Edge cases 514-532; Test plan Slice 2 at 551-563; DoD items 3-4 at 660-663; Current state 130-145)
Status: final-accepted

## Plan (Worker 1)

Spec status-line re-verification (worker-1.md per-spawn rule): spec line 5 reads "in build — Slice 1 implemented (final-accepted in the build cycle, uncommitted); Slices 2–7 not started" — accurate at the start of this planning pass; no edit needed.

Spec-vs-codebase verification: every symbol the spec names for this slice exists on disk — `types/relay.py::decode_global_id` (relay.py:596, typed `relay.GlobalID | str`, uniform-`ConfigurationError` contract), `list_field.py::_validate_djangotype_target` (list_field.py:93, docstring already names "card 032's `DjangoNodeField`"), `types/base.py::_is_relay_shaped` (base.py:410, imported by connection.py:58), the `resolve_node` / `resolve_nodes` classmethods installed by `types/relay.py::install_relay_node_resolvers` (relay.py:897, `required=False` defaults at 759/816), `types/relay.py::_model_for` (relay.py:297), `strawberry.utils.inspect.in_async_context` (imported at types/relay.py:34 and list_field.py:17), and the `registry.clear()` co-clear precedent blocks (registry.py:462-507). No spec-vs-codebase gap; no escalation needed.

### DRY analysis

- **Existing patterns reused** (cite file:line):
  - **Decode seam:** `django_strawberry_framework/types/relay.py::decode_global_id` (types/relay.py:596-709). Already accepts the raw string (`relay.GlobalID | str`), already wraps Strawberry's `GlobalIDValueError` superset, already runs the resolve-then-enforce strategy dispatch. The new module calls it; zero decode logic is reimplemented.
  - **Constructor guards:** `list_field.py::_validate_djangotype_target` (list_field.py:93-139) — the four shared target guards, parameterized on the factory's public name via `field=`; its docstring pre-authorizes this exact reuse. The fifth (Relay-Node-shaped) guard reuses `types/base.py::_is_relay_shaped` (base.py:410) exactly as `connection.py::DjangoConnectionField` does (connection.py:663, 683-687) — including the construction-time-tuple rationale documented there (Meta-declared `relay.Node` is in `definition.interfaces` before Phase 2.5 injects it into `__bases__`).
  - **Factory mechanism:** `strawberry.field(resolver=...)` return value picked up by the class-body walk with the consumer's class-attribute annotation driving SDL nullability — `list_field.py::DjangoListField` (list_field.py:214-219); same keyword surface (`description` / `deprecation_reason` / `directives: Sequence[object] = ()`).
  - **Dispatch targets:** the resolved type's `resolve_node` / `resolve_nodes` **classmethods** (Decision 3) — installed defaults `_resolve_node_default` (types/relay.py:759-792) and `_resolve_nodes_default` (types/relay.py:816-857) run `get_queryset` on both color branches with `required=False`, and calling the classmethod (not the underscore default) preserves consumer overrides for free.
  - **Order preservation + duplicates:** `types/relay.py::_order_nodes` (types/relay.py:722-756) already emits one entry per requested key (duplicates each get their row) and `None` for missing keys under `required=False`. The `nodes` resolver passes each type's pk list (input order, duplicates included) to `resolve_nodes` and gets the 1:1 positional list back — **no per-position mapping is reimplemented in `relay.py`**; only the cross-type interleave is new.
  - **Per-call async dispatch:** `list_field.py::DjangoListField._default` (list_field.py:178-186) — `if in_async_context(): return <coroutine>` returned directly (no redundant wrapper layer; rev6 H1 note there). The single-node resolver goes one step simpler (pure pass-through of `resolve_node`'s value, which is already color-dispatched); the `nodes` resolver uses the same per-call check to return one gathering coroutine (spec Edge cases "Async end-to-end").
  - **pk lookup for pre-coercion:** `types/relay.py::_model_for` (types/relay.py:297-306) — reuse instead of re-deriving `__django_strawberry_definition__.model` (the staged pseudocode at relay.py:48 re-derives it; the real implementation imports `_model_for`, the same private-sibling import precedent as list_field.py:21).
  - **Error-conversion precedent:** `filters/sets.py` `FILTER_INVALID` raise (sets.py:1305-1311) — `GraphQLError(<message>, extensions={"code": ...})`; `GLOBALID_INVALID` follows that exact shape (`from graphql import GraphQLError`).
  - **Ledger + co-clear precedent:** `registry.py::TypeRegistry.clear` blocks (registry.py:462-507) — try/except-ImportError/else, one block per cleared cache; the staged anchor at registry.py:509-514 pins the same shape for `_node_fields_declared`. Best-effort-guard coverage precedent: the monkeypatch-sys.modules tests at tests/test_registry.py:1420-1510.
  - **Finalize-time check home:** the staged anchor at `types/finalizer.py:375-384` sits after the Phase-2.5 per-type loop (finalizer.py:360-373) and before the Slice-3 synthesis anchor; `implements_relay_node` (types/relay.py:64) is the predicate, already imported by the finalizer.
  - **Test fixtures:** `tests/types/test_relay_interfaces.py` — `_isolate_registry` autouse (test_relay_interfaces.py:41), inline `CategoryNode(DjangoType)` + `finalize_django_types()` pattern, `services.seed_data(N)` first line for catalog-model tests (e.g. test_relay_interfaces.py:494), the decode fixture family (test_relay_interfaces.py:2094-2330: model-label / type-name / strategy-rejection shapes), and the async-`get_queryset` sync-misuse fixture (test_relay_interfaces.py:891-920). `tests/test_connection.py` — `_make_node_type` / `_schema_for` schema-building helpers (test_connection.py:73-115) and the `original_error` unwrap pattern (tests/test_list_field.py:373-376 for `SyncMisuseError` through `schema.execute_sync`). `pytest.ini` `asyncio_mode = auto` means plain `async def test_` for the async-context tests.
- **New helpers justified** (all module-local to the new `django_strawberry_framework/relay.py`; each has ≥2 call sites inside the module):
  - `_node_fields_declared: list[str]` — the module-level ledger; appended by both factories, read by the finalizer check, cleared by `registry.clear()`. Single responsibility: "has any root node field been declared".
  - `_decode_or_graphql_error(gid)` — the narrow `ConfigurationError`→`GraphQLError("Invalid GlobalID: ...", extensions={"code": "GLOBALID_INVALID"})` boundary, wrapping the `decode_global_id` call ONLY (Revision 7 P2 — `SyncMisuseError` IS-A `ConfigurationError`; dispatch runs outside this try). Call sites: the single-node resolver, the batch resolver.
  - `_coerce_pk_or_none(resolved_type, node_id)` — `_model_for(resolved_type)._meta.pk.to_python(node_id)` under `try`/`except (ValueError, ValidationError)` → `None` (Revision 7 P2; the shipped `_coerce_node_id` at types/relay.py:266 only unwraps a `GlobalID` wrapper and must not be extended — pk-type coercion is the root field's responsibility per spec Edge cases line 522). Call sites: both resolvers.
  - `_check_typed_match(target_type, resolved)` (name at Worker 2's discretion) — the typed-form wrong-type `GraphQLError` naming expected and received types. Call sites: both resolvers (the batch form runs it per decoded id and any mismatch fails the whole field).
  - A small shared `_validate_node_target(target_type, *, field)` wrapper running `_validate_djangotype_target(target_type, None, field=field)` + the `_is_relay_shaped` fifth guard — so the two factories do not copy-paste the guard pair and the fifth-guard raise text exists once. The raise message follows connection.py:684-687's wording shape but names the node-field factory (e.g. "`<field>` requires a Relay-Node-shaped DjangoType target; add `relay.Node` to `Meta.interfaces` (or inherit `relay.Node` directly)").
  - `_interleave(positions, per_type_results)` (shape at Worker 2's discretion) — one reassembly used by both the sync path and the gathering coroutine, so the null-hole interleave logic exists once.
- **Duplication risk avoided:**
  - The naive shape would copy decode/typed-check/coerce/ledger logic between the two factories — avoided by the five module-local helpers above; the two resolvers reduce to short orchestration bodies.
  - The naive batch resolver would reimplement per-position row mapping and duplicate handling — avoided by delegating to the shipped `_order_nodes` via `resolve_nodes` (only the cross-type interleave is new code).
  - The fifth-guard raise now exists at **three** sites package-wide (connection.py:684 + the two node factories via the shared local wrapper, which keeps it at two literal occurrences). Deliberately NOT hoisting a generic helper into `list_field.py` / `types/base.py` this slice: connection.py's message text is shipped wording and the parameterization would be a one-word noun swap. **Recorded as a cross-slice integration-pass watch item** (alongside Slice 1's deferred 2x `".Meta.interfaces entry "` prefix): if the integration pass finds the two messages have converged, consolidate there.
  - `GLOBALID_INVALID` is a new literal; it appears exactly once (inside `_decode_or_graphql_error`). Test files may assert it as a literal — acceptable (test-side literals pin the wire contract).

### Implementation steps

Line numbers are pin-at-write-time navigational hints. Verify against the current source before editing — another worker's pass may have shifted the file since this plan was written.

1. **`django_strawberry_framework/relay.py` — replace the staged module wholesale** (currently 114 lines of docstring + four `TODO(spec-032-full_relay-0_0_9 Slice 2)` pseudocode anchors; all are removed in this change per the AGENTS.md anchor discipline). New module contents, in order:
   - Module docstring: the consumer-facing root refetch surface (Decisions 3/4/5/11), the nullable-by-contract note (`required=False` unconditionally; the optional annotation spelling is the supported shape), and the interface-only-schema reachability note (spec Edge cases line 526: a schema whose only root field is interface-typed `node` must pass concrete types via `strawberry.Schema(types=[...])` — engine behavior, documented here per the spec's "Documented in the root-field docstring").
   - Imports: `from collections.abc import Sequence`; `from typing import Any`; `import strawberry`; `from django.core.exceptions import ValidationError`; `from graphql import GraphQLError`; `from strawberry.types import Info`; `from strawberry.utils.inspect import in_async_context`; `from .exceptions import ConfigurationError`; `from .list_field import _validate_djangotype_target`; `from .types.base import _is_relay_shaped`; `from .types.relay import _model_for, decode_global_id`. (No import cycle: nothing under `types/` imports top-level `relay.py` at module load — the finalizer's read is function-local, step 4.) `__all__ = ("DjangoNodeField", "DjangoNodesField")`.
   - `_node_fields_declared: list[str] = []` — appended by **both** factories on every call (bare and typed), entries are the factory-name string (the staged-pseudocode shape; only emptiness is load-bearing, the string aids debugging).
   - `_decode_or_graphql_error`, `_coerce_pk_or_none`, `_check_typed_match`, `_validate_node_target`, `_interleave` per the DRY analysis. `_check_typed_match` raises `GraphQLError` naming the **expected** and **received** types by their `graphql_type_name` (read from each side's `__django_strawberry_definition__` — the consumer-facing SDL names, honoring `Meta.name`); identity comparison is `resolved is not target_type`. No `extensions` code on the mismatch error — the spec assigns a code only to `GLOBALID_INVALID` (Error shapes line 263 names no code for the mismatch), and inventing one would be an accidental API.
2. **`DjangoNodeField(target_type=None, *, description=None, deprecation_reason=None, directives=())`** (`# noqa: N802` PascalCase-parity comment, the list_field.py:142 / connection.py:633 precedent; no `resolver=` kwarg — the spec signature has none and there is no consumer-resolver seam on a refetch field):
   - If `target_type is not None`: `_validate_node_target(target_type, field="DjangoNodeField")`.
   - Append to the ledger.
   - Inner resolver `def _resolve(root: Any, info: Info, id: strawberry.ID) -> Any:` — the argument MUST be named `id` (the Relay-spec signature) and annotated `strawberry.ID` (Revision 7 P1 — never `relay.GlobalID`); ruff's flake8-builtins is enabled (`pyproject.toml` select "A"), so the parameter carries `# noqa: A002` with a one-line Relay-spec-signature comment. Body: `resolved, node_id = _decode_or_graphql_error(id)` → typed check → `pk = _coerce_pk_or_none(resolved, node_id)`; `if pk is None: return None` (uncoercible literal → null, no query) → `return resolved.resolve_node(pk, info=info, required=False)` (pass-through: a coroutine in async context is awaited by Strawberry as a plain-field return; everything after the decode call runs OUTSIDE the try so `SyncMisuseError` surfaces as itself).
   - `return strawberry.field(resolver=_resolve, description=description, deprecation_reason=deprecation_reason, directives=directives)`.
3. **`DjangoNodesField(target_type=None, *, description=None, deprecation_reason=None, directives=())`** — same guards + ledger append. Inner resolver `def _resolve(root: Any, info: Info, ids: list[strawberry.ID]) -> Any:`:
   - `if not ids: return []` — zero database access (spec Edge cases line 520).
   - Decode every id via `_decode_or_graphql_error` **before any query** — a malformed id anywhere fails the whole field (Decision 5 "Batch decode failures fail the whole field").
   - Typed form: run `_check_typed_match` per decoded entry — a wrong-type id anywhere fails the whole field (Decision 4).
   - Per-id pk pre-coercion: uncoercible positions become reserved `null` holes (never poisoning the `pk__in` for the rest of the batch — Revision 7 P2).
   - Group coercible `(type, pk)` by decoded type (a plain dict keyed on the type class — insertion-ordered, types are hashable), keeping each type's pks in input order with duplicates preserved; record a positions map (input index → `(type, within-group index)` or null-hole marker).
   - `if in_async_context():` return ONE gathering coroutine (`async def _gather()`) that awaits each distinct type's `resolved.resolve_nodes(info=info, node_ids=pks, required=False)` **sequentially** (sequential awaits, not `asyncio.gather` — Django async-ORM connection safety, and the spec requires only "a single gathering coroutine"; each call is itself the coroutine `_resolve_nodes_async` returns under per-call dispatch, types/relay.py:849-850) and then `_interleave`s into input order with `null` holes.
   - Sync branch: call each type's `resolve_nodes(info=info, node_ids=pks, required=False)` once (one query per distinct type) and `_interleave`. Missing/hidden positions are already `None` in each per-type list (`_order_nodes` `required=False`); uncoercible positions come from the holes map.
4. **`django_strawberry_framework/types/finalizer.py`** — replace the Slice-2 anchor (finalizer.py:375-384) with the no-Node-types check at exactly that position (after the Phase-2.5 per-type loop ending at finalizer.py:373, before the untouched Slice-3 anchor at finalizer.py:386): function-local `from ..relay import _node_fields_declared` (cycle-safe local import — plain import, no try/except: unlike `registry.clear()`'s best-effort teardown, this is a contract check that must never be silently skipped); `if _node_fields_declared and not any(implements_relay_node(tc) for tc, _ in registry.iter_definitions()): raise ConfigurationError("node lookup configured but no Node types registered.")` — message verbatim from spec line 92 / Decision 8 line 413.
5. **`django_strawberry_framework/registry.py`** — replace the Slice-2 anchor (registry.py:509-514) with the co-clear block in the established try/except-ImportError/else shape (the `_helper_referenced_filtersets` precedent directly above it): `from .relay import _node_fields_declared` / `pass` / `_node_fields_declared.clear()`, with the same one-block-per-cache comment style.
6. **`django_strawberry_framework/__init__.py`** — replace the Slice-2 anchor (lines 27-32) with `from .relay import DjangoNodeField, DjangoNodesField  # noqa: E402` (isort places it between the `.optimizer.hints` and `.scalars` imports) and add `"DjangoNodeField"`, `"DjangoNodesField"` to `__all__` alphabetically between `"DjangoListField"` and `"DjangoOptimizerExtension"` (the staged anchor's stated position — verified: `DjangoList…` < `DjangoNode…` < `DjangoNodes…` < `DjangoOptimizer…`). Spec-authorized public-surface change (slice checklist line 93; DoD item 4) — Worker 3's public-surface check should cite it.
7. **`tests/test_relay_node_field.py`** — replace the Slice-2 TODO block (test file lines 10-61) with the real tests (see Test additions); rewrite the module docstring to describe the shipped Slice-2 surface while noting the Slice-4 additions are staged; the Slice-4 TODO block (lines 63-69) stays untouched.
8. **`tests/test_registry.py`** — add one sibling best-effort-guard test for the new co-clear block, mirroring the connection-cache guard test at tests/test_registry.py:1490-1510 (monkeypatch `sys.modules` so `from .relay import ...` raises `ImportError`; `clear()` still succeeds). Required by `fail_under = 100` (the `except ImportError: pass` line is unreachable otherwise); the positive co-clear path is pinned by `test_node_field_without_node_types_raises_at_finalize` in the new file.
9. Run `uv run ruff format .` and `uv run ruff check --fix .`; reconcile `git status --short` per the Worker 2 validation-run contract. Trailing-comma rule applies to the factory signatures (5 parameters ≥ threshold 4 → trailing comma, one per line).

### Test additions / updates

All package-internal by spec mandate (Test plan lines 535-543: the live copies land with Slice 6's activation; these are the "package twins that keep the per-slice `fail_under = 100` gate green"). Home: `tests/test_relay_node_field.py` (new — Decision 11's card-named file). Conventions: `_isolate_registry`-style autouse fixture (copy the 5-line shape from tests/test_connection.py:59-64 or tests/types/test_relay_interfaces.py:41-45), `services.seed_data(N)` as the first line of every catalog-model test (AGENTS.md rule; the fixture types are built over `apps.products.models` Category/Item like the sibling suites), inline `class XNode(DjangoType)` + `finalize_django_types()` + `strawberry.Schema(...)` and `schema.execute_sync` / `await schema.execute` execution (real-usage posture; `pytest.ini` `asyncio_mode = auto`). The spec-named tests (Test plan lines 551-563):

- `test_bare_node_field_resolves_model_label_id` / `test_bare_node_field_resolves_type_name_id` — bare `node: relay.Node | None = DjangoNodeField()` on a Query; one fixture type on the default `model` strategy, one on `Meta.globalid_strategy = "type"`; each emitted-payload shape refetches to the right concrete type (assert via `__typename` — the shipped `is_type_of` dispatch). The bare-field schema passes `types=[...]` (the documented engine constraint).
- `test_typed_node_field_resolves_target` — `genre_like: CategoryNode | None = DjangoNodeField(CategoryNode)` returns the row for a matching id.
- `test_typed_node_field_mismatch_raises` — an ItemNode id at the CategoryNode-typed field → `result.errors` non-empty, message names both `graphql_type_name`s (expected and received), `data` carries the field as `None`/absent per nullability.
- `test_typed_nodes_field_resolves_targets` — `DjangoNodesField(CategoryNode)` resolves a list of matching ids in order.
- `test_typed_nodes_field_mismatch_raises` — one ItemNode id mid-batch fails the **whole field** with the expected/received `GraphQLError` (assert the enclosing `data` is nulled — the `[Node]!` non-null propagation), never a per-position `null`.
- `test_node_hidden_row_returns_null` / `test_node_missing_row_returns_null` — a `get_queryset`-filtered fixture type (the test_relay_interfaces.py:533-554 pattern); hidden id → `data["node"] is None` with NO error; missing id (max pk + 1) → same shape.
- `test_node_null_paths_issue_equal_queries` — the no-existence-oracle pin: hidden vs missing refetch issue the same query count (`django_assert_num_queries` or `CaptureQueriesContext` — Worker 2's choice).
- `test_node_malformed_id_graphql_error` — parametrized over malformed base64 / unresolvable model label / strategy-forbidden shape (reuse the decode fixture shapes from test_relay_interfaces.py:2165-2315); each surfaces `extensions["code"] == "GLOBALID_INVALID"` and message prefix `"Invalid GlobalID: "`, never a raw `ConfigurationError`. The malformed-base64 case is the Revision 7 P1 reachability proof (assert the package's code, not Strawberry's engine error text).
- `test_node_uncoercible_pk_returns_null` — well-formed `<label>:abc` over an integer-pk type → `null`, no error, and zero queries issued (query-count 0 for the resolve — the "no query is issued" pin).
- `test_nodes_uncoercible_pk_null_hole` — the same garbage pk mid-batch → positional `null` hole; the other positions still resolve (the batch's `pk__in` is not poisoned).
- `test_nodes_preserves_input_order_with_null_holes` — visible/hidden/missing ids interleaved; nulls land at the right indexes.
- `test_nodes_batches_per_type` — ids spanning two types: query-count pin of one query per distinct type.
- `test_nodes_duplicate_ids` — the same id twice resolves per position (two equal entries).
- `test_nodes_empty_list` — `ids: []` → `[]` with zero queries.
- `test_nodes_malformed_id_mid_batch` — one malformed id among well-formed ones → whole-field `GLOBALID_INVALID` (enclosing `data` nulled), not a hole.
- `test_node_field_without_node_types_raises_at_finalize` — declare `DjangoNodeField()` (ledger append), register no Relay-Node-shaped type, `finalize_django_types()` raises `ConfigurationError` with the verbatim message; then `registry.clear()` + a Node-shaped registration finalizes cleanly (the ledger-reset half).
- `test_node_async_context` / `test_nodes_async_context` — `await schema.execute(...)` happy paths (single pass-through coroutine; batch returns ONE gathering coroutine — assert correct results across ≥2 types so the gather + interleave is exercised).
- `test_node_sync_async_get_queryset_raises_sync_misuse` — a type with `async def get_queryset`, executed via `schema.execute_sync`. **Discriminating assertions (Revision 7 P2):** `result.errors[0].original_error` is a `SyncMisuseError` (the tests/test_list_field.py:373-376 unwrap pattern), AND the error is NOT `GLOBALID_INVALID`-coded (extensions lack the code / message lacks "Invalid GlobalID") — proving the catch-convert boundary scopes the decode call only.
- `test_public_exports` — `from django_strawberry_framework import DjangoNodeField, DjangoNodesField` succeeds and both are in `__all__`.

Plan-added beyond the spec-named list (coverage-required; flagged for Worker 3):

- `test_node_field_target_guards` (+ a `DjangoNodesField` sibling or parametrize over both factories) — the typed-form construction guards: a non-class / non-DjangoType / unregistered target raises the `_validate_djangotype_target` errors naming the factory, and a registered-but-non-Relay-shaped target raises the fifth-guard error naming the factory. Covers the new call sites and the shared `_validate_node_target` raise; mirrors tests/test_connection.py:540-560.
- `tests/test_registry.py::test_clear_skips_missing_relay_module` (name to mirror the existing best-effort siblings at test_registry.py:1420/1454/1490) — the `except ImportError` branch of the new co-clear block.

Temp/scratch tests: none required for development. Worker 3 may probe under `docs/builder/temp-tests/slice-2/` if it wants a throwaway check that a `nodes` batch mixing an uncoercible pk with a malformed id still fails whole-field (decode runs first, before coercion — by construction in the planned ordering).

### Implementation discretion items

Assessed and decided as Worker 2's choice — equally valid shapes, no architectural weight:

- Private helper names in `relay.py` (`_decode_or_graphql_error` / `_coerce_pk_or_none` are the staged names and fine; `_check_typed_match` / `_validate_node_target` / `_interleave` are placeholders) and whether `_interleave` consumes a positions list of `(type, index)` tuples vs an index-into-per-type-iterator scheme — as long as one interleave implementation serves both sync and async paths.
- Whether the two factories share their common prologue (guards + ledger append) via a third tiny local helper or repeat the two-line pair inline — two lines twice is acceptable; a helper is fine if it stays obvious.
- The query-count assertion mechanism (`django_assert_num_queries` fixture vs `CaptureQueriesContext`) and whether the malformed-id cases use `pytest.mark.parametrize` or three inline assertions in one test.
- Whether the fixture node types are module-level (the tests/test_connection.py `_make_node_type` builder style) or per-test inline classes (the test_relay_interfaces style) — pick whichever keeps the ~20 tests shortest; the autouse registry isolation makes both safe.
- `# noqa: A002` placement/wording for the `id` resolver argument (line-level noqa with a one-line comment is the house shape).

### Spec slice checklist (verbatim)

- [x] New top-level [`django_strawberry_framework/relay.py`][relay-toplevel]: `DjangoNodeField(target_type=None, *, description=, deprecation_reason=, directives=)` and `DjangoNodesField(target_type=None, *, ...)` factory functions returning `strawberry.field(resolver=...)` values picked up by Strawberry's class-body walk (the [`DjangoListField`][glossary-djangolistfield] mechanism). The synthesized resolvers declare `id: strawberry.ID` / `ids: list[strawberry.ID]` arguments — the **raw-string** form, deliberately **not** `relay.GlobalID` ([Decision 4](#decision-4--djangonodefield--djangonodesfield-a-bare-interface-form-and-a-typed-form) / [Decision 5](#decision-5--null-for-invisible-rows-graphqlerror-for-malformed-ids): a `relay.GlobalID`-annotated argument is parsed by Strawberry's `convert_argument` *before* the resolver runs, so malformed ids would never reach the package) — decode **server-side** via [`types/relay.py::decode_global_id`][types-relay] (typed `relay.GlobalID | str`; never trusting the client's claim of which type the id belongs to), pre-coerce the decoded `node_id` to the target's pk type, dispatch to the resolved type's `resolve_node` / `resolve_nodes` (which honor [`get_queryset`][glossary-get_queryset-visibility-hook]), and return `null` (or a `null` list entry) for hidden, missing, and uncoercible-pk ids. `nodes` preserves input order, resolves per-type **batched** (`resolve_nodes` once per distinct type), and supports duplicate ids.
- [x] The typed form (`DjangoNodeField(GenreType)`) runs the shared [`list_field.py::_validate_djangotype_target`][list-field] guards plus the connection-style Relay-Node-shaped guard at construction time, and at request time rejects an id that decodes to a different type with a `GraphQLError` naming the expected and received types ([Decision 4](#decision-4--djangonodefield--djangonodesfield-a-bare-interface-form-and-a-typed-form)). `DjangoNodesField(GenreType)` is the typed batch sibling running the same per-id check; a wrong-type id anywhere in the batch — like a malformed id ([Decision 5](#decision-5--null-for-invisible-rows-graphqlerror-for-malformed-ids)) — fails the **whole field** (the `[Node]!` non-null nulls the enclosing `data`), never a per-position `null`.
- [x] Decode failures ([`ConfigurationError`][glossary-configurationerror] from `decode_global_id`) are caught at the root-field boundary and re-raised as `GraphQLError("Invalid GlobalID: ...", extensions={"code": "GLOBALID_INVALID"})` — the [`FilterSet`][glossary-filterset] `FILTER_INVALID` precedent ([Decision 5](#decision-5--null-for-invisible-rows-graphqlerror-for-malformed-ids)).
- [x] A module-level `_node_fields_declared` ledger (appended by both factories, co-cleared by `registry.clear()` — the `_helper_referenced_filtersets` precedent) backs a [`finalize_django_types`][glossary-finalize_django_types] check: a declared node field on a registry with **no** Relay-Node-shaped [`DjangoType`][glossary-djangotype]s raises `ConfigurationError("node lookup configured but no Node types registered.")`.
- [x] `DjangoNodeField` and `DjangoNodesField` are exported from [`django_strawberry_framework/__init__.py`][package-init] (the card's DoD names both).
- [x] Package coverage: `tests/test_relay_node_field.py` (new) per the [Test plan](#test-plan).

### Static inspection (BUILD.md-mandated)

`uv run python scripts/review_inspect.py <file> --output-dir docs/shadow` was run during this planning pass for `django_strawberry_framework/types/relay.py`, `types/finalizer.py`, `list_field.py`, `registry.py`, and `connection.py` (the ≥150-line seams this slice touches or cites; `types/base.py`'s shadow persists from Slice 1). The staged `relay.py` itself (114 lines, fully replaced) and the staged test stub were read directly. Findings used:

- `types/relay.py` overview: 32 symbols, 4 repeated string literals — none in the seams this slice consumes; the imports table confirms the `from ..registry import registry` cycle-dodge is function-local (line 643), the precedent for the finalizer's and registry's function-local `..relay` imports in steps 4-5.
- `connection.py` overview: `DjangoConnectionField` is 62 lines / 1 branch node — the factory-body complexity budget the two new factories should match (guards + ledger + one inner resolver + one return); the batch resolver is the only function expected to approach hotspot territory (decode loop + grouping + two color branches) and should stay under the 40-line/8-branch thresholds by delegating to the module helpers.
- `registry.py` overview confirms the four existing co-clear blocks are structurally identical — the new block is the fifth of the same shape.
- Shadow line numbers are not canonical; all line refs in this artifact are original-source pins (allowed in this per-cycle scratchpad).

### Notes for Worker 1 (spec reconciliation)

- No spec-vs-codebase gaps found at planning time; all named symbols exist (see the verification note at the top of this plan). One micro-nit not worth a spec edit: the staged `__init__.py` anchor's alphabetical-position hint and the spec's export wording are consistent with plain alphabetical `__all__` ordering; ruff/mechanical sorting settles it.
- The plan adds two tests beyond the spec-named list (`test_node_field_target_guards` and the `tests/test_registry.py` best-effort-guard sibling) — both are coverage-forced by `fail_under = 100`, not scope creep; final verification should treat them as in-contract.

---

## Build report (Worker 2)

### Files touched

Grounded in `git status --short` (prior-slice / pre-flight churn — `types/base.py`, `tests/types/test_base.py`, the spec, the eight tracked `docs/builder` deletions, the three new build artifacts — left untouched):

- `django_strawberry_framework/relay.py` — staged pseudocode module replaced wholesale with the real implementation: `_node_fields_declared` ledger, `_decode_or_graphql_error`, `_coerce_pk_or_none`, `_check_typed_match`, `_validate_node_target`, `_interleave`, and the `DjangoNodeField` / `DjangoNodesField` factories (all four Slice-2 TODO anchors removed; module docstring carries the nullable-by-contract note and the `strawberry.Schema(types=[...])` engine note).
- `django_strawberry_framework/types/finalizer.py` — Slice-2 anchor replaced with the no-Node-types check at the planned position (after the Phase-2.5 loop, before the untouched Slice-3 anchor); plain function-local `from ..relay import _node_fields_declared` (no try/except, per the plan's contract-check rationale); verbatim message.
- `django_strawberry_framework/registry.py` — Slice-2 anchor replaced with the fifth co-clear block in the established try/except-ImportError/else shape.
- `django_strawberry_framework/__init__.py` — TODO anchor removed; `from .relay import DjangoNodeField, DjangoNodesField  # noqa: E402` in isort position (between `.optimizer.hints` and `.scalars`); both names added to `__all__` alphabetically.
- `tests/test_relay_node_field.py` — Slice-2 TODO block replaced with 26 real tests (22 test functions, two parametrized); module docstring rewritten; the Slice-4 TODO block stays.
- `tests/test_registry.py` — `test_clear_tolerates_unimportable_relay_module` appended (mirrors the connection-guard sibling; covers the new `except ImportError` branch).
- `tests/base/test_init.py` — `test_public_api_surface_is_pinned`'s expected `__all__` tuple gains the two new names. **Plan addition** (the plan did not list this file): the pinned-surface test fails otherwise; the export is the spec-authorized public-surface change (slice checklist item 5 / DoD item 4), and `AGENTS.md` allows `tests/base/test_init.py` to grow. See spec-reconciliation notes.

### Tests added or updated

- `tests/test_relay_node_field.py` (new, 26 collected): every spec-named Slice-2 test (bare model-label/type-name refetch; typed happy + mismatch; typed batch happy + mismatch-whole-field; hidden/missing null + equal-query-count pin; malformed-id `GLOBALID_INVALID` parametrized over malformed-base64 / unresolvable-label / strategy-forbidden; uncoercible-pk null (zero queries) + batch null hole; order/batch-per-type/duplicates/empty-list/mid-batch-malformed; finalize ledger check + `registry.clear()` reset half; async node/nodes contexts (the nodes case spans two types, exercising gather + interleave); discriminating `SyncMisuseError` pass-through; public exports) plus the plan-added `test_node_field_target_guards` (parametrized over both factories, covering all five construction guards).
- `tests/test_registry.py::test_clear_tolerates_unimportable_relay_module` (plan-added, coverage-forced).
- `tests/base/test_init.py::test_public_api_surface_is_pinned` updated (see above).

### Validation run

- `uv run ruff format .` then `uv run ruff check --fix .` — clean (one E501 wrap and import-sort fix auto-applied in the new test file); `scripts/check_trailing_commas.py` run additionally (the pre-commit/CI gate) — fixed one layout in the new code; a final `ruff format .` / `ruff check .` pass confirms a stable fixpoint (`241 files left unchanged` / `All checks passed!`).
- Focused tests (all `--no-cov`): `tests/test_relay_node_field.py` 26 passed; `tests/test_registry.py` 69 passed; sibling suites importing the changed surfaces — `tests/types/test_relay_interfaces.py` + `tests/test_connection.py` + `tests/test_list_field.py` + `tests/base/test_init.py` — 199 passed (the one pre-update `test_public_api_surface_is_pinned` failure was the pinned-surface tuple, fixed as described; re-run green).
- `git status --short` reconciled: every modified file is slice-intended; no tool churn to revert.

### Implementation notes

- `_check_typed_match(target_type, resolved)` takes the possibly-`None` target and no-ops for the bare form, so both resolvers call it unconditionally (no `if target_type is not None` duplication at the call sites). Message shape: `"Wrong node type: expected a {expected} id, received a {received} id."` — names both `graphql_type_name`s, carries no `extensions` (plan-pinned).
- `_interleave` consumes a `positions: list[tuple[type, int] | None]` map (`None` = uncoercible hole, else `(decoded_type, within-group index)`) built in the same loop that groups coercible pks — one pass, duplicates preserved by appending to the group list before recording the index.
- The gathering coroutine is an inner `async def _gather()` closure (single call site, per the plan's helper-arity rule), awaiting each distinct type's `resolve_nodes` sequentially.
- The single-node resolver's signature is deliberately multi-line with per-line `# noqa: ARG001` (unused `root`) and `# noqa: A002` (the Relay-spec `id` argument) plus the Revision-7-P1 comment; the interleaved comments also keep the comma-fixer from collapsing it.
- Test fixture style: module-level builders (`_make_node_type`, `_make_hidden_category_node`, `_schema_with` using the `tests/test_connection.py` namespace-dict pattern) rather than per-test inline classes — `_schema_with(field_name, annotation, field_value, *, extra_types=())` keeps each of the 26 tests to one schema-building call. Query-count mechanism: `django_assert_num_queries` (fixed counts: 0 for the no-query pins, 1+1 for the equal-paths pin, 2 for batch-per-type).
- Async tests follow the `tests/test_connection.py` shape: `@pytest.mark.django_db(transaction=True)`, `await sync_to_async(services.seed_data)(1)`, `await Model.objects.order_by("pk").afirst()`, `await schema.execute(...)`.
- The malformed-id parametrize includes the strategy-forbidden case as a type-name payload (`CategoryNode:<pk>`) against a default-`model`-strategy type — exercising decode Step 2 through the root field.

### Intentionally skipped plan items

None. All nine implementation steps and both plan-added tests landed.

### Notes for Worker 3

- No shadow files were used during the build pass (the plan's static-inspection findings were sufficient); all source reading was direct.
- The spec-authorized public-surface change is `__init__.py`'s two new exports — `tests/base/test_init.py::test_public_api_surface_is_pinned` now pins the widened tuple; please verify the widening is exactly the two spec-named symbols.
- The plan's optional throwaway probe (uncoercible pk + malformed id mixed in one batch failing whole-field) holds by construction — decode runs over every id before any coercion; `test_nodes_malformed_id_mid_batch` pins the decode-first whole-field half.
- The mismatch-error wording (`"Wrong node type: expected a ... id, received a ... id."`) is new literal text (the spec pins only "naming the expected and received types"); flag if you want different phrasing before Slice 6 makes it live.

### Notes for Worker 1 (spec reconciliation)

- **Small, mechanically obvious drift:** `tests/base/test_init.py` was touched although neither the spec's Slice-2 checklist nor the plan's file list names it — the pinned-public-surface test (`test_public_api_surface_is_pinned`) hard-codes `__all__` and fails on the spec-mandated export addition. The two-line tuple update is the only possible reconciliation of slice checklist item 5 with that pin; recorded here per the drift protocol. Consider whether future specs' export items should name the pin test explicitly.
- No other deviations: helper names, message texts, error shapes, dispatch mechanics, and test placement all follow the plan as written.

---

## Review (Worker 3)

Reviewed against `docs/spec-032-full_relay-0_0_9.md` (Slice-2 checklist 88-94, Decisions 3/4/5 at 297-356, Decision 8's no-Node-types check at 413/419, Error shapes 253-265, Edge cases 514-532, Test plan 551-563, DoD items 3-4 at 660-663) and the artifact plan. Diff scope per the cumulative-diff rule: only the seven Slice-2 files (`relay.py`, `types/finalizer.py`, `registry.py`, `__init__.py`, `tests/test_relay_node_field.py`, `tests/test_registry.py`, `tests/base/test_init.py`); the Slice-1 churn (`types/base.py`, `tests/types/test_base.py`, the spec's Worker-1 status/NodeID edits) and the pre-flight `docs/builder` deletions were excluded as out-of-scope baseline.

Static inspection: `scripts/review_inspect.py --output-dir docs/shadow` run for `django_strawberry_framework/relay.py` (new module), `types/finalizer.py` (existing file under `types/`), and `tests/test_relay_node_field.py` (~620 new lines outside the package). **Skipped for `registry.py`** (the new co-clear block is ~12 lines, below the 30-line in-package threshold and not under `optimizer/`/`types/`; the block is a structural copy of the four siblings directly above it). Shadow walk results:

- Hotspots: `DjangoNodesField._resolve` (52 lines / 5 branches, original-source `relay.py::DjangoNodesField` inner `_resolve`) exceeds the 40-line default but ~15 of those lines are contract comments and the branch count stays well under 8; the decode/typed-check/group/dispatch body matches the plan's "short orchestration via module helpers" budget. Justified.
- Django/ORM markers: exactly one — `_model_for(resolved_type)._meta.pk.to_python(node_id)` in `_coerce_pk_or_none` — the spec-mandated pk pre-coercion (Decision 5, Revision 7 P2, Edge cases line 522). Justified; the `(ValueError, ValidationError)` catch matches the spec's exact exception pair.
- Calls of interest: 1x `to_python` (above), 1x `len(pks)` (the within-group index for `_interleave`'s positions map). Both load-bearing.
- Repeated string literals in `relay.py`: 3x `DjangoNodeField` / 3x `DjangoNodesField` (`__all__` + ledger append + `field=` kwarg) — public-name literals, acceptable.

### High:

None.

### Medium:

None.

### Low:

#### Build-report test-function count is off by one

`## Build report (Worker 2)` says "26 real tests (22 test functions, two parametrized)"; the file carries **23** test functions (26 collected: 21 plain + 3-way malformed parametrize + 2-way guards parametrize). Artifact-text inaccuracy only — no code action; recorded so Worker 1's final verification doesn't chase a phantom missing test. Addressed by this note (no revision required).

### DRY findings

- **Fifth-guard raise text now exists at two literal sites** — `connection.py::DjangoConnectionField #"a connection field requires a Relay-Node-shaped DjangoType"` and `relay.py::_validate_node_target #"requires a Relay-Node-shaped DjangoType target"`. The plan pre-justified this (shipped wording vs. a one-word parameterization) and recorded it as a cross-slice integration-pass watch item alongside Slice 1's 2x `".Meta.interfaces entry "` prefix; confirmed the relay-side wording exists exactly once in the module (shared by both factories via `_validate_node_target`). Deferred follow-up for Worker 1's integration pass — no action this slice.
- **Test-file repeated GraphQL documents**: `"query ($ids: [ID!]!) { categories(ids: $ids) { name } }"` x7 and `"query ($id: ID!) { category(id: $id) { name } }"` x6 in `tests/test_relay_node_field.py`, while the file already hoists `_NODE_QUERY` / `_NODES_QUERY` constants for the two interface-typed shapes. Two more module constants would complete the pattern. Test-side, readability-neutral either way — recorded as a deferred follow-up for the integration pass, not a revision blocker.
- No duplicated decode/coerce/typed-check/interleave logic between the two factories: the five module-local helpers from the plan all landed with ≥2 call sites each; the batch resolver delegates per-position mapping to the shipped `_order_nodes` via `resolve_nodes` exactly as planned (no reimplementation).
- `GLOBALID_INVALID` appears exactly once in source (inside `_decode_or_graphql_error`); the 3x test-side occurrences pin the wire contract per the plan's explicit allowance.

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` adds `from .relay import DjangoNodeField, DjangoNodesField  # noqa: E402` and the two `__all__` entries (alphabetical, between `"DjangoListField"` and `"DjangoOptimizerExtension"`). **This change IS spec-authorized**: slice checklist line 93 ("`DjangoNodeField` and `DjangoNodesField` are exported from `django_strawberry_framework/__init__.py` (the card's DoD names both)") and DoD item 4 (spec line 663). No other export or `__all__` change is present. The widening is exactly the two spec-named symbols (verified per Worker 2's note), and `tests/base/test_init.py::test_public_api_surface_is_pinned` now pins the widened tuple — the two-line test update is the only possible reconciliation of the pinned-surface test with the mandated export and is in-contract.

### CHANGELOG sanity

Not applicable; slice did not modify CHANGELOG.md.

### Documentation / release sanity

Not applicable; slice did not modify docs/release/KANBAN/archive surfaces. (The working tree's `M docs/spec-032-full_relay-0_0_9.md` is Worker 1's Slice-1 status-line + NodeID-branch reconciliation, outside this slice's diff per the cumulative-diff rule.)

### What looks solid

- **The catch-convert boundary is exactly as narrow as the spec demands** (Decision 5, Revision 7 P2): the `try/except ConfigurationError` lives inside `_decode_or_graphql_error` and wraps only `decode_global_id`; both resolvers run the typed check, pk coercion, and `resolve_node`/`resolve_nodes` dispatch outside it. `test_node_sync_async_get_queryset_raises_sync_misuse` discriminates correctly (asserts `original_error` IS-A `SyncMisuseError`, extensions lack `GLOBALID_INVALID`, message lacks "Invalid GlobalID") — the IS-A-`ConfigurationError` trap is test-locked.
- **Whole-field vs positional-null semantics land precisely**: the batch resolver decodes every id before any query (malformed-anywhere → whole field, `test_nodes_malformed_id_mid_batch` asserts `result.data is None`), typed mismatch anywhere fails whole-field (`test_typed_nodes_field_mismatch_raises` likewise), while uncoercible/hidden/missing ids each get positional `null` (`test_nodes_uncoercible_pk_null_hole`, `test_nodes_preserves_input_order_with_null_holes`). My temp probe confirmed the decode-first ordering on a mixed uncoercible+malformed batch (whole-field error, zero queries).
- **No existence oracle**: hidden and missing both traverse `resolve_node(..., required=False)` → the one filtered-queryset `qs.first()` path; `test_node_null_paths_issue_equal_queries` pins 1 query each. Uncoercible ids short-circuit pre-query (`django_assert_num_queries(0)` pinned in two tests).
- **Per-type batching with order + duplicates**: insertion-ordered `dict` grouping + positions map + the shipped `_order_nodes` (verified: its index emits one entry per requested key, so duplicates each get their row — `test_nodes_duplicate_ids`, `test_nodes_batches_per_type` pins one query per distinct type). `_interleave` is single-sited and serves both color branches.
- **Async shape matches the spec's Edge cases**: single-node pass-through (coroutine awaited as plain-field return); `nodes` returns ONE gathering coroutine with sequential awaits (Django async-ORM connection safety noted in-source); both async tests exercise it, the `nodes` case across two types (gather + interleave).
- **Finalize check and ledger**: plain (non-best-effort) function-local import in the finalizer with the documented rationale; verbatim spec message `"node lookup configured but no Node types registered."`; the fifth co-clear block in `registry.clear()` is byte-shaped like its four siblings; `test_clear_tolerates_unimportable_relay_module` covers the `except ImportError` branch and `test_node_field_without_node_types_raises_at_finalize` covers both the raise and the ledger-reset half.
- **Decode seam intact**: zero decode logic reimplemented; `decode_global_id`'s uniform-`ConfigurationError` contract (verified at `types/relay.py::decode_global_id`, including the `definition_for_graphql_name` miss raising `ConfigurationError`, not `AttributeError`) means the boundary conversion is exhaustive for the malformed family.
- **Spec slice checklist (verbatim)**: all six boxes ticked by Worker 2 and all six verified landed in the diff — no over-ticked box, no silently-unaddressed sub-check. Every spec-named Slice-2 test (Test plan lines 553-563) is present by name plus the two plan-added coverage tests.
- Focused runs green: `tests/test_relay_node_field.py` + `tests/test_registry.py` + `tests/base/test_init.py` — 99 passed (`--no-cov`).

### Temp test verification

- `docs/builder/temp-tests/slice-2/test_probe_mixed_failures.py` — two probes: (1) a batch mixing a valid id, an uncoercible-pk id, and a malformed id fails whole-field with `GLOBALID_INVALID` at zero queries (decode-first ordering); (2) duplicate hidden-row ids each land their own positional `null`. Both passed.
- Disposition: **deleted** — both behaviors hold by construction and are adjacent to permanent pins (`test_nodes_malformed_id_mid_batch`, `test_nodes_duplicate_ids`, `test_nodes_preserves_input_order_with_null_holes`); no new branch found, so no promotion needed.

### Notes for Worker 1 (spec reconciliation)

- The spec's status line (line 5) still reads "Slice 1 implemented …; Slices 2–7 not started" — stale once this slice is accepted; the usual Worker 1 status-line refresh applies at final verification.
- Worker 2's drift note on `tests/base/test_init.py` is correct and mechanically forced (the pinned-surface test cannot coexist with the mandated export otherwise); I verified the widening is exactly the two spec-named symbols. Worth weighing Worker 2's suggestion that future specs' export items name the pin test explicitly.
- The mismatch-error wording `"Wrong node type: expected a {expected} id, received a {received} id."` is new literal text the spec leaves open ("naming the expected and received types"). It reads well and carries no extensions code per the plan's pin; no objection — flagging only because Slice 6 makes it live wire text, after which it hardens into the de-facto API.
- Two deferred DRY follow-ups for the integration pass (recorded above): the two-site fifth-guard wording (pre-planned watch item) and the two unhoisted GraphQL document literals in the new test file.

### Review outcome

`review-accepted` — no High or Medium findings; the single Low is an artifact-text count nit addressed in-place; DRY findings are deferred follow-ups recorded for Worker 1's integration pass. Artifact `Status:` updated to `review-accepted`.

---

## Final verification (Worker 1)

- **Spec slice checklist audit:** all six `- [x]` boxes in the Plan's `### Spec slice checklist (verbatim)` independently verified against the working-tree Slice-2 diff (`relay.py`, `types/finalizer.py`, `registry.py`, `__init__.py`, `tests/test_relay_node_field.py`, `tests/test_registry.py`, `tests/base/test_init.py`) — **no over-ticks, no un-ticked landed boxes, no deferrals.** (1) The new top-level `django_strawberry_framework/relay.py` carries both factories returning `strawberry.field(resolver=...)`; resolvers declare raw-string `id: strawberry.ID` / `ids: list[strawberry.ID]` (never `relay.GlobalID`), decode server-side via `types/relay.py::decode_global_id`, pre-coerce the pk (`_coerce_pk_or_none` → `_model_for(...)._meta.pk.to_python` under the spec's exact `(ValueError, ValidationError)` pair), dispatch to the resolved type's `resolve_node` / `resolve_nodes` classmethods with `required=False`, and return `null` / positional `null` for hidden, missing, and uncoercible ids; `nodes` preserves input order via the positions map + `_interleave`, batches per distinct type (insertion-ordered `dict` grouping, one `resolve_nodes` each), and preserves duplicates (within-group index recorded before append). (2) Typed form: `_validate_node_target` runs the four shared `_validate_djangotype_target` guards plus the `_is_relay_shaped` fifth guard at construction; request-time mismatch raises the expected/received `GraphQLError` (`_check_typed_match`, identity comparison, `graphql_type_name`s, no extensions code); the batch sibling runs the per-id check and any mismatch — like any malformed id (decode-all-before-any-query ordering verified in `_resolve`) — fails the whole field. (3) `_decode_or_graphql_error` wraps the decode call ONLY and converts `ConfigurationError` → `GraphQLError("Invalid GlobalID: ...", extensions={"code": "GLOBALID_INVALID"})`; the narrow scope keeps `SyncMisuseError` (IS-A `ConfigurationError`) surfacing as itself, test-locked by `test_node_sync_async_get_queryset_raises_sync_misuse`'s discriminating assertions. (4) `_node_fields_declared` ledger appended by both factories; fifth co-clear block in `registry.clear()` (try/except-ImportError/else, byte-shaped like its four siblings); finalize-time check at the planned position with a plain (deliberately non-best-effort) function-local import and the verbatim message `"node lookup configured but no Node types registered."`. (5) `__init__.py` exports exactly the two spec-named symbols (import in isort position; `__all__` alphabetical). (6) `tests/test_relay_node_field.py` carries every spec-named Slice-2 test by name (23 test functions, 26 collected — Worker 3's count correction confirmed) plus the two plan-added coverage tests, which I treat as in-contract per the plan's pre-clearance (coverage-forced, not scope creep). All four staged Slice-2 TODO anchors removed in the shipping change; the Slice-3/Slice-4 anchors untouched.
- **DRY check (Slice 2 + accepted Slice 1):** no new cross-slice duplication — Slice 1's domain (`_validate_interfaces` named rejections in `types/base.py`) is untouched by this slice, and Slice 1's deferred 2x `".Meta.interfaces entry "` prefix gains no third occurrence here (revisit trigger remains Slice 3). Within/across-module findings re-verified by grep: the Relay-Node-shaped fifth-guard raise text exists at exactly **two** literal sites (`connection.py::DjangoConnectionField #"a connection field requires a Relay-Node-shaped DjangoType"` and `relay.py::_validate_node_target #"requires a Relay-Node-shaped DjangoType target"` — single-sited in the module, shared by both factories); **deferral to the integration pass confirmed appropriate**: the plan pre-justified it (connection.py's text is shipped wording; consolidation is a one-noun parameterization), and fixing now would touch a prior-shipped surface outside this slice's diff scope. The test-file GraphQL document literals (7x `categories(ids: $ids)` / 6x `category(id: $id)` while `_NODE_QUERY` / `_NODES_QUERY` are already hoisted) — **deferral likewise confirmed**: test-side, readability-neutral, and the integration pass already owns a pass over this file's sibling watch item; recorded as the second integration-pass entry rather than a revision blocker. `GLOBALID_INVALID` appears exactly once in source; the five module-local helpers each have ≥2 call sites (the inner `_gather` closure is single-sited by design); no decode/order/duplicate logic reimplemented — `_order_nodes` reached via `resolve_nodes` exactly as planned.
- **Tests:** `uv run pytest tests/test_relay_node_field.py tests/test_registry.py tests/types/test_base.py tests/base/test_init.py --no-cov` — **198 passed** (no regressions in the Slice-1 suite or the registry suite). No coverage flags used.
- **Spec reconciliation:** the one recorded drift — `tests/base/test_init.py::test_public_api_surface_is_pinned`'s two-line tuple widening, plan-unlisted — is accepted as mechanically forced by the spec-mandated export (slice checklist item 5 / DoD item 4): the pinned-surface test cannot coexist with the export otherwise, AGENTS.md explicitly allows `tests/base/test_init.py` to grow, and Worker 3 verified the widening is exactly the two spec-named symbols. **No spec edit needed for it** — the spec's export bullet already implies the pin-test reconciliation; naming the pin test in export bullets is a future-spec authoring convention, carried forward in worker memory rather than retro-edited into this card's contract record. The mismatch-error wording (`"Wrong node type: expected a {expected} id, received a {received} id."`) is within the spec's open phrasing ("naming the expected and received types") — accepted as-shipped; it hardens into wire text at Slice 6. Status line refreshed (below).
- **Final status:** `final-accepted`.

### Summary

Slice 2 ships the consumer-facing root Relay refetch surface (spec-032 Decisions 3/4/5): `DjangoNodeField` / `DjangoNodesField` in the new top-level `django_strawberry_framework/relay.py`, both exported from the package `__init__`. The factories return `strawberry.field(resolver=...)` values whose resolvers take raw-string ids, decode server-side through the shipped `decode_global_id` seam, pre-coerce pks, and dispatch to each type's `resolve_node` / `resolve_nodes` classmethods with `required=False` — null for hidden/missing/uncoercible ids, `GLOBALID_INVALID`-coded `GraphQLError` for malformed ids (narrowly scoped so `SyncMisuseError` surfaces as itself), whole-field failure for malformed or wrong-type ids anywhere in a batch, per-type batching with input order and duplicates preserved, and a single sequential-await gathering coroutine in async context. A `_node_fields_declared` ledger (co-cleared by `registry.clear()`) backs the finalize-time no-Node-types `ConfigurationError`. 23 new test functions in `tests/test_relay_node_field.py` plus a registry ImportError-guard sibling and the pinned-`__all__` widening.

### Spec changes made (Worker 1 only)

- `docs/spec-032-full_relay-0_0_9.md` line 5 (status line) — "Slice 1 implemented …; Slices 2–7 not started" updated to "Slices 1–2 implemented (final-accepted in the build cycle, uncommitted); Slices 3–7 not started", per the worker-1.md per-spawn status-line rule and the Slice-1 precedent. Triggered by Slice 2 final verification. `scripts/check_spec_glossary.py --spec docs/spec-032-full_relay-0_0_9.md` re-run after the edit: OK (38 terms).
- No other spec edits: the `tests/base/test_init.py` drift needs no contract change (see Spec reconciliation above), and no checklist box required a deferral entry.
