# Build: Slice 3 — Promotion + docs

Spec reference: `docs/spec-018-export_schema-0_0_7.md` (Slice 3 lines 73-86; Doc updates lines 626-657; Decision 9 lines 526-541; Decision 10 lines 543-559)
Status: final-accepted

## Plan (Worker 1)

### DRY analysis

Slice 3 ships zero `.py` source changes — five Markdown surfaces and one project-root file (`KANBAN.md`) — so the engineering DRY surface is near-zero. The DRY concern is **cross-document consistency**: the shipped contract for the schema-export management command is described in five separate sites (`docs/GLOSSARY.md` entry body + Index row, `docs/README.md` shipped-list bullet, `docs/TREE.md` current-on-disk-layout subtree + test-tree entry, `KANBAN.md` Done body + In-progress summary update, `CHANGELOG.md` `### Added` append) and a single paraphrase drift between any two of them would let a reader infer two different contracts from one slice.

- **Existing patterns reused (cross-document canonical wording).**
  - **Spec lines 629-630 (GLOSSARY entry body wording).** Spec pins the GLOSSARY entry body verbatim: `"django_strawberry_framework/management/commands/export_schema.py ships Command(BaseCommand) with positional schema (dotted path, default symbol name "schema") and optional --path; SDL output via strawberry.printer.print_schema; CommandError for unimportable dotted path, non-strawberry.Schema resolved symbol, and missing positional argument; no --watch / --indent / JSON mode / settings-backed defaults in 0.0.7."` Worker 2 copies this verbatim into the GLOSSARY entry body; the same shape (slightly compressed) lands in the CHANGELOG bullet (spec lines 648-649) and the KANBAN Done body (spec lines 644-645). All three are spec-pinned and not Worker-2-discretionary.
  - **Spec line 636 (README "Shipped today" bullet).** Pinned verbatim: `` "`manage.py export_schema` — Django management command that prints or writes the GraphQL SDL for a `strawberry.Schema` symbol (positional dotted path, optional `--path`); migration-parity with `strawberry-django`'s command of the same name. See [`GLOSSARY.md#schema-export-management-command`](GLOSSARY.md#schema-export-management-command)." `` Worker 2 uses this string verbatim — the TODO comment marker at `docs/README.md` lines 105-109 already stages the exact same wording, so reconciling the comment scaffold to the live bullet is a delete-then-write with no rewording.
  - **Spec lines 648-649 (CHANGELOG bullet).** Pinned verbatim: `` "`Schema export management command` — `django_strawberry_framework/management/commands/export_schema.py` ships `Command(BaseCommand)`; `manage.py export_schema config.schema [--path schema.graphql]` writes SDL via `strawberry.printer.print_schema`. Symbol resolution via `strawberry.utils.importer.import_module_symbol(default_symbol_name=\"schema\")`. `CommandError` for unimportable dotted path, non-`strawberry.Schema` resolved symbol, and missing positional argument. No `--watch` / `--indent` / JSON mode / settings-backed defaults in `0.0.7` (each deferred to a follow-up card driven by consumer demand)." `` The TODO comment marker at `CHANGELOG.md` lines 25-32 stages a near-verbatim pseudo-body; Worker 2 uses the spec's wording at lines 648-649 as the source of truth.
  - **Spec lines 644-646 (KANBAN Done body).** Pinned verbatim: `` "Shipped `django_strawberry_framework/management/commands/export_schema.py` containing `Command(BaseCommand)` with positional `schema` (dotted path, default symbol name `\"schema\"`) and optional `--path`; SDL output via `strawberry.printer.print_schema`; `CommandError` for unimportable dotted path, non-`strawberry.Schema` resolved symbol, and missing positional argument. Package-internal tests at `tests/management/test_export_schema.py`; live fakeshop coverage in `examples/fakeshop/tests/test_commands.py`." ``
  - **DONE-017-0.0.7 / KANBAN Done-section precedent** (verified at `KANBAN.md:1815` — `### DONE-017-0.0.7 — apps.py and Django app config`). The most recent Done card is the stylistic precedent for the body length, past-tense voice, and "Shipped …" opener. The new `DONE-018-0.0.7` body uses the same shape, with the spec-pinned wording above.
  - **Pre-staged TODO scaffolds in commit `d35385c`.** Five of the six edit sites carry a `<!-- TODO spec-018 Slice 3: ... -->` comment marker that anchors both the precise location and the wording the spec author intended:
    - `docs/GLOSSARY.md:104` (the Index-row TODO marker; status flip from `planned for 0.0.7` to `shipped (0.0.7)`)
    - `docs/GLOSSARY.md:911-919` (entry-body TODO marker; rewrite the body + flip `**Status:**` line)
    - `docs/README.md:105-109` (Shipped-today bullet stage)
    - `docs/README.md:119-121` (Coming-in-0.1.0 bullet removal stage)
    - `docs/TREE.md:190-192` (prose-fragment removal stage)
    - `docs/TREE.md:206-212` (current-on-disk-layout subtree insertion stage)
    - `docs/TREE.md:320-322` (target-package-layout `[alpha]` tag removal stage)
    - `docs/TREE.md:347-351` (current-test-tree insertion stage)
    - `KANBAN.md:77-82` (move-to-Done TODO marker)
    - `CHANGELOG.md:25-32` (Added-bullet append stage)
  - Worker 2 deletes each TODO comment block in the same edit that lands the real content (delete-then-write disposition; same posture as Slices 1 and 2). The TODO scaffolds are authoring-pseudo-code per `AGENTS.md` line 26's staged-but-not-implemented convention.
- **New helpers justified.** None. There is no engineering surface in Slice 3; no helpers are possible or warranted.
- **Duplication risk avoided.**
  - **Risk: a worker paraphrasing the spec-pinned wording at one of the five sites (GLOSSARY entry body, README bullet, CHANGELOG bullet, KANBAN Done body, GLOSSARY Index row).** Mitigated by pinning the spec line ranges below verbatim and writing "use the spec wording at lines NNN-MMM verbatim — do not paraphrase" in `### Implementation discretion items`. The five sites describe the same shipped contract; if any one diverges, a reader walking the docs sees two different contracts and the slice fails the documentation-sanity check during review.
  - **Risk: a worker leaving one of the four `<!-- TODO spec-018 Slice 3: ... -->` comment markers in place after editing.** Mitigated by enumerating all ten marker sites in the DRY analysis above and instructing Worker 2 to delete each TODO comment block in the same edit that lands the real content. Worker 3's documentation-sanity check during review walks for any residual `spec-018 Slice 3` TODO markers — none should remain after Slice 3 lands.
  - **Risk: a worker bumping `pyproject.toml` `[project].version`, `django_strawberry_framework/__init__.py`'s `__version__`, or `tests/base/test_init.py`'s pinned version assertion as a "while-I'm-here" change.** Mitigated by the explicit non-discretion item below and by spec Decision 9 (lines 526-541): the version bump is owned by whichever `0.0.7` card ships last. WIP-ALPHA-019-0.0.7 and WIP-ALPHA-020-0.0.7 are still queued behind 018; this card is not last in the joint cut.
  - **Risk: a worker derived the `DONE-NNN-0.0.7` id from a stale spec citation.** The spec at line 644 says "the next available `DONE-NNN-0.0.7` id ... is determined at merge time, not pinned in this spec." Pinned in `### Implementation steps` step 4 below: Worker 2 reads `KANBAN.md` at build time to verify the next available id (current planning-time verification shows `DONE-018-0.0.7` is next — see "Pre-flight verification" below).
  - **Risk: a worker bumping the `### In progress` summary at `KANBAN.md:50` without removing exactly `WIP-ALPHA-018-0.0.7` from the remaining-cards list.** Mitigated by pinning the exact text to remove from the summary paragraph in `### Implementation steps` step 4 below (verified at planning time).

### Implementation steps

Line numbers are pin-at-write-time navigational hints. Verify against the current source before editing — another worker's pass may have shifted the file since this plan was written.

**Pre-flight verification at planning time (2026-05-22).** Confirmed by direct file reads:

- `docs/GLOSSARY.md:105` carries the Index-row `| [Schema export management command](#schema-export-management-command) | planned for 0.0.7 |` (the TODO marker for this row is at line 104; the row itself is line 105 — spec lines 629/631 cite "line 104" as the row, which is actually the comment marker; the live row to edit is line 105).
- `docs/GLOSSARY.md:920` carries `## Schema export management command` heading; the entry-body TODO marker block is at lines 911-919; the `**Status:**` line is at line 922; the body paragraph is at line 924.
- `docs/README.md:122` carries `- schema export management command` (the spec at line 75 cites "line 113"; the actual live line is 122 — the TODO marker block for the removal is at lines 119-121, immediately above the bullet to remove). The "Shipped today" heading is at line 89; the TODO marker block for the new bullet is at lines 105-109; the new bullet appends after the last existing bullet at line 104 (`Django AppConfig` entry).
- `docs/TREE.md:193` carries the live prose with `, and the management command` — the TODO marker block is at lines 190-192. The spec at line 641 cites "line 190"; the actual live prose is line 193.
- `docs/TREE.md:206-212` is the TODO marker block for the `management/` subtree insertion in the current-on-disk-layout (placement between `list_field.py` at line 205 and `types/` at line 214).
- `docs/TREE.md:323` carries the `├── management/              # [alpha] Django management commands` line in the target-package-layout section; the TODO marker block is at lines 320-322. The spec at line 640 cites "lines 309-313" — actual live lines are 320-327.
- `docs/TREE.md:347-351` is the TODO marker block for the `tests/management/` insertion in the current-test-tree section; the position is BEFORE `test_apps.py` (line 352).
- `KANBAN.md:83` carries `### WIP-ALPHA-018-0.0.7 — Schema export management command`; the TODO marker block is at lines 77-82. The `## Done` column starts at line 1257 (`### DONE-001-0.0.1 — DjangoType core foundation`) and the most recent Done card is at line 1815 (`### DONE-017-0.0.7 — apps.py and Django app config`).
- **Next available KANBAN id verified at planning time: `DONE-018-0.0.7`** — no Done card with NNN in the range 018-020 exists currently; the maintainer's mid-build NNN renumbering (per the build plan's "Mid-build baseline drift" note) recomputed positions but did not advance Done-side ids. Worker 2 must **re-verify** `KANBAN.md` at build time (re-read the Done column and confirm `DONE-018-0.0.7` is still the next available id; if a concurrent maintainer renumber shifted it, use whichever id `KANBAN.md` shows as next).
- `KANBAN.md:50` carries the `### In progress` summary paragraph mentioning `WIP-ALPHA-018-0.0.7`. The exact phrase to remove or rewrite is at the segment: "`WIP-ALPHA-018-0.0.7` (schema-export management command) is now the active spec target — fleshed out and ready for design." This entire sentence drops out when 018 moves to Done; the surrounding sentences ("The remaining two — `WIP-ALPHA-019-0.0.7` ... and `WIP-ALPHA-020-0.0.7` ... — stay queued behind 018." needs the "behind 018" reference updated since 018 is no longer ahead) need a minor rewrite. Worker 2's discretion item below pins this.
- `CHANGELOG.md:21-32` carries the `[0.0.7] - 2026-05-20` heading + the existing two `### Added` bullets (DONE-016, DONE-017) + the TODO marker block for the new bullet at lines 25-32. The `### Changed` heading is at line 35; the new bullet appends BEFORE line 35 (after the existing TODO marker block at line 32 is replaced).

**Working-tree note.** Per `AGENTS.md` line 26 (staged-but-not-implemented convention) and consistent with the Slice 1 and Slice 2 patterns, the ten TODO scaffold markers enumerated under "DRY analysis" are pre-staged by the maintainer in commit `d35385c Add TODO comments`. Worker 2 replaces each marker in the same edit that lands the real content (delete-then-write disposition). Per `AGENTS.md` line 31, any files dirty at task start that are NOT part of the Slice 3 contract (`docs/feedback.md` if it appears modified, etc.) are presumptively the maintainer's in-progress work and must NOT be auto-reverted.

1. **Edit `docs/GLOSSARY.md`.**
   - (a) Flip the Index-table row at line 105 from `planned for 0.0.7` to `shipped (0.0.7)`. The line currently reads: `| [Schema export management command](#schema-export-management-command) | planned for 0.0.7 |`. After edit: `| [Schema export management command](#schema-export-management-command) | shipped (`0.0.7`) |` (using the backtick-wrapped version-token shape used by sibling shipped rows, e.g. `Scalar field override semantics` at line 102: `| [Scalar field override semantics](#scalar-field-override-semantics) | shipped (`0.0.6`) |`).
   - (b) Delete the TODO comment marker at line 104: `<!-- TODO spec-018 Slice 3: flip status column below from `planned for 0.0.7` to `shipped (0.0.7)`. -->`. Removal is in the same edit as (a).
   - (c) Update the entry body at the `## Schema export management command` heading (line 920) and the `**Status:**` line (line 922) per spec lines 629-630:
     - Flip `**Status:** planned for `0.0.7`.` (line 922) to `**Status:** shipped (`0.0.7`).`
     - Replace the existing body paragraph at line 924 (`manage.py export_schema [path] writes the GraphQL SDL to a file. Mirrors strawberry-django's export_schema command.`) with the spec's pinned shipped-contract wording at spec lines 629-630: `` "`django_strawberry_framework/management/commands/export_schema.py` ships `Command(BaseCommand)` with positional `schema` (dotted path, default symbol name `\"schema\"`) and optional `--path`; SDL output via `strawberry.printer.print_schema`; `CommandError` for unimportable dotted path, non-`strawberry.Schema` resolved symbol, and missing positional argument; no `--watch` / `--indent` / JSON mode / settings-backed defaults in `0.0.7`." ``
   - (d) Delete the TODO comment marker block at lines 911-919 in the same edit as (c).
   - (e) Preserve the existing `**See also:** [Django AppConfig](#django-appconfig).` line unchanged (currently at line 926). The "See also" is unaffected by the shipped-contract rewrite.
   - The "Browse by category" Index entry at line 129 (`Integration / tooling: ... · [Schema export management command](#schema-export-management-command) · ...`) is the deep-link target and does NOT need a status update (it carries no status badge).

2. **Edit `docs/README.md`.**
   - (a) Surgically remove the entire `- schema export management command` bullet at line 122. After edit, the bullet ceases to exist; the `Coming in 0.1.0` list at lines 113-118 stays, and line 124 (`Coming in 1.0.0`) follows directly from line 118 with the appropriate blank-line separation preserved.
   - (b) Delete the TODO comment marker block at lines 119-121 in the same edit as (a).
   - (c) Add a "Shipped today (0.0.7)" bullet AFTER the `Django AppConfig` bullet at line 104 and BEFORE the existing TODO marker (which will be deleted in step (d)). The bullet's wording is the spec's pinned text at spec line 636 verbatim: `- `manage.py export_schema` — Django management command that prints or writes the GraphQL SDL for a `strawberry.Schema` symbol (positional dotted path, optional `--path`); migration-parity with `strawberry-django`'s command of the same name. See [`GLOSSARY.md#schema-export-management-command`](GLOSSARY.md#schema-export-management-command).`
   - (d) Delete the TODO comment marker block at lines 105-109 in the same edit as (c).
   - (e) The "Shipped today (0.0.7)" heading at line 89 already reads correctly per DONE-017's prior edit; no heading change.

3. **Edit `docs/TREE.md`.**
   - (a) Surgically remove `, and the management command` from the prose at line 193. The current sentence (verified at planning time) is: `"Every other module shown in the target package layout below — query-surface subpackages, the mutation cluster, the auth / forms / DRF integrations, the test client, the Channels router, and the management command — is not on disk yet and will land as the corresponding `KANBAN.md` cards ship."`. After edit: `"Every other module shown in the target package layout below — query-surface subpackages, the mutation cluster, the auth / forms / DRF integrations, the test client, and the Channels router — is not on disk yet and will land as the corresponding `KANBAN.md` cards ship."`. The fragment `, and the management command` (with the leading comma and trailing space) is removed; the en-dash punctuation flow is preserved.
   - (b) Delete the TODO comment marker block at lines 190-192 in the same edit as (a).
   - (c) Add the `management/` subtree to the current-on-disk-layout `django_strawberry_framework/` tree (current block at lines 197-235). Insertion position: between `list_field.py` at line 205 (`├── list_field.py            # DjangoListField (...)`) and `types/` at line 214. The current TODO marker block at lines 206-212 already pins the precise shape. After insertion the block reads (Worker 2 spell out the children verbatim per spec line 639 and the TODO marker's pseudo-shape at lines 208-212):

     ```text
     ├── list_field.py            # DjangoListField (non-Relay list[T] factory for root Query fields; shipped in 0.0.7)
     ├── management/              # Django management commands (shipped in 0.0.7)
     │   ├── __init__.py
     │   └── commands/
     │       ├── __init__.py
     │       └── export_schema.py  # `manage.py export_schema` — print/write GraphQL SDL
     ├── types/                   # DjangoType subsystem (Layer 2) — shipped
     ```

     Worker 2 deletes the TODO comment marker block at lines 206-212 in the same edit. Note the TODO marker's pseudo-shape DOES NOT show the trailing newlines correctly — Worker 2 reconciles any tree-art whitespace to match the surrounding tree (no double-blank-line, single-space alignment after the box-drawing characters).

   - (d) Remove the `[alpha]` tag from the `management/` block at line 323 in the target-package-layout section. The current line (verified at planning time) is: `├── management/              # [alpha] Django management commands`. After edit: `├── management/              # Django management commands` (spec line 640 / TODO marker at lines 320-322 pin this final form). Delete the TODO comment marker block at lines 320-322 in the same edit. The two child entries below (`├── __init__.py`, `└── commands/` block at lines 324-327) stay unchanged — only the parent line's `[alpha]` tag is removed.
   - (e) Add the `tests/management/` subtree to the current-test-tree section. Insertion position: BEFORE `test_apps.py` at line 352 (`├── test_apps.py             # AppConfig (single-file Layer-3 module)`). The TODO comment marker block at lines 347-351 already pins the precise shape. After insertion:

     ```text
     ├── __init__.py
     ├── management/             # mirrors django_strawberry_framework/management/
     │   ├── __init__.py
     │   └── test_export_schema.py  # ← export_schema Command — happy paths + failure modes
     ├── test_apps.py             # AppConfig (single-file Layer-3 module)
     ```

     Worker 2 deletes the TODO comment marker block at lines 347-351 in the same edit. Note the placement: BEFORE `test_apps.py` because `management/` (subdirectory) sorts before `test_apps.py` (file) under the spec's "alphabetical" rule at spec line 76. Worker 2 reconciles tree-art whitespace to match the surrounding tree.

4. **Edit `KANBAN.md`.**
   - (a) **Re-verify the next available `DONE-NNN-0.0.7` id at build time.** Re-read the `## Done` column. At planning time (2026-05-22) the most recent Done card is `### DONE-017-0.0.7 — apps.py and Django app config` at line 1815, and no card with NNN 018+ exists in Done. Per the build plan's "Mid-build baseline drift" note, the maintainer has been renumbering NNNs concurrently — Worker 2 MUST re-verify at build time. If a concurrent renumber shifted `DONE-017-0.0.7` or advanced the Done-side numbering, use whichever NNN the live file shows as the next available `0.0.7`-versioned Done id. **Planning-time assumption: `DONE-018-0.0.7` is the next available id.**
   - (b) Move the entire `WIP-ALPHA-018-0.0.7` block (heading at line 83 through end-of-card body — the card spans approximately lines 83-160, ending immediately before `### WIP-ALPHA-019-0.0.7` at line 162) from the `## In progress` column to the `## Done` column. Insertion position: at the END of the Done column (after `### DONE-017-0.0.7 — apps.py and Django app config` at line 1815 and any sub-bullets that belong to it). Rename the heading `### WIP-ALPHA-018-0.0.7 — Schema export management command` to `### DONE-018-0.0.7 — Schema export management command` (using whichever id Worker 2 verified in step 4(a)).
   - (c) Rewrite the card body in past tense per spec lines 644-646 verbatim: `` "Shipped `django_strawberry_framework/management/commands/export_schema.py` containing `Command(BaseCommand)` with positional `schema` (dotted path, default symbol name `\"schema\"`) and optional `--path`; SDL output via `strawberry.printer.print_schema`; `CommandError` for unimportable dotted path, non-`strawberry.Schema` resolved symbol, and missing positional argument. Package-internal tests at `tests/management/test_export_schema.py`; live fakeshop coverage in `examples/fakeshop/tests/test_commands.py`." `` The "Predecessors", "Active spec", "Why it matters", "Verified in upstream", "Recommended architectural direction", "Definition of done", "Files likely touched", "Dependencies", and "Out of scope" sub-sections of the WIP card body MAY be preserved (they are historical context that future maintainers reading the Done column may want) OR collapsed into a short past-tense summary per the existing Done-card precedent at lines 1817-1830 (DONE-017's body is compact past-tense; DONE-016 at lines 1803-1814 is similar). **Discretion item:** Worker 2 follows the DONE-017 / DONE-016 precedent for body length and structure — the past-tense "Shipped …" body, optional one-line follow-up notes if needed, NOT the full WIP card body.
   - (d) Delete the TODO comment marker block at lines 77-82 in the same edit as (b).
   - (e) Update the `### In progress` summary paragraph at line 50. The current text (verified at planning time) is:

     `"`0.0.7` is the active patch. Five WIP cards were opened together so the small parity-driven slices land in one release; two have shipped (`DONE-016-0.0.7` `DjangoListField` and `DONE-017-0.0.7` `apps.py` and Django app config). `WIP-ALPHA-018-0.0.7` (schema-export management command) is now the active spec target — fleshed out and ready for design. The remaining two — `WIP-ALPHA-019-0.0.7` (multi-database cooperation contract) and `WIP-ALPHA-020-0.0.7` (warning-free scalar registration via `StrawberryConfig.scalar_map`) — stay queued behind 018. Full card detail lives under the `## In progress` board column below; `DONE-016-0.0.7` and `DONE-017-0.0.7` are in the `## Done` column. The last `0.0.7` card to ship owns the version bump from `0.0.6` per Decision 10 of `docs/SPECS/spec-016-list_field-0_0_7.md`."`

     Worker 2 rewrites this paragraph to reflect that 018 has now shipped. **Discretion item:** Worker 2 picks the exact rewrite shape but MUST (per spec line 646) "remove `WIP-ALPHA-018-0.0.7` from the remaining-cards list." A natural rewrite: drop the `"`WIP-ALPHA-018-0.0.7` (schema-export management command) is now the active spec target — fleshed out and ready for design."` sentence entirely; rewrite the "two have shipped" to "three have shipped"; add the `DONE-018-0.0.7` reference into the listing of shipped cards; rewrite "The remaining two ... stay queued behind 018." to drop the "behind 018" clause (since 018 is no longer ahead). The final sentence about the joint-cut version bump stays.

5. **Edit `CHANGELOG.md`.**
   - (a) Append a third bullet to the existing `[0.0.7]` `### Added` subsection. The new bullet goes immediately after the existing `Django AppConfig` bullet at line 24 (and after the TODO marker block at lines 25-32 is deleted). The wording is verbatim from spec lines 648-649: `` "- `Schema export management command` — `django_strawberry_framework/management/commands/export_schema.py` ships `Command(BaseCommand)`; `manage.py export_schema config.schema [--path schema.graphql]` writes SDL via `strawberry.printer.print_schema`. Symbol resolution via `strawberry.utils.importer.import_module_symbol(default_symbol_name=\"schema\")`. `CommandError` for unimportable dotted path, non-`strawberry.Schema` resolved symbol, and missing positional argument. No `--watch` / `--indent` / JSON mode / settings-backed defaults in `0.0.7` (each deferred to a follow-up card driven by consumer demand)." ``
   - (b) Delete the TODO comment marker block at lines 25-32 in the same edit. After the edit, the `[0.0.7]` `### Added` section ends with the new bullet at what was previously line ~32, and the `### Changed` heading still follows after a blank line.
   - (c) Do NOT create a second `[0.0.7]` heading. Per spec Decision 9 (lines 526-541) and spec line 649, every `0.0.7` card under the joint cut appends to the same shared `### Added` subsection.
   - (d) Do NOT touch the date line `## [0.0.7] - 2026-05-20` at line 21 — the joint-cut date is owned by whichever `0.0.7` card ships last (with the version bump). 018 is not last in the bundle (019 and 020 are still queued); the date stays as written.

6. **Spec status-line re-verification.** Read `docs/spec-018-export_schema-0_0_7.md` lines 1-6. Status line at line 4 reads `Status: draft (revision 5, post-rev4 feedback against [docs/feedback.md](feedback.md)).` Until the slice is final-accepted, `draft` remains the right state (Slice 3's doc-promotion edits land in the build pass; the spec itself stays at rev5). Worker 1 (final verification) re-checks the status line at the end of Slice 3 and decides whether the joint-cut `0.0.7` ship status warrants a status-line edit at that point — NOT now, NOT in the planning pass. No spec edit required during planning.

7. **No edits to `pyproject.toml`, `django_strawberry_framework/__init__.py`'s `__version__`, or `tests/base/test_init.py`'s pinned version assertion.** Verified at planning time: `pyproject.toml:4` shows `version = "0.0.6"`; `django_strawberry_framework/__init__.py:26` shows `__version__ = "0.0.6"`; `tests/base/test_init.py:11` shows `assert __version__ == "0.0.6"`. All three stay at `0.0.6` per Decision 9 (joint cut). The version bump is owned by whichever `0.0.7` card ships last (019, 020, or 018 if the maintainer reorders) and IS NOT this slice's contract. See `### Implementation discretion items` below — this is NOT discretionary; the version bump is forbidden in Slice 3.

8. **No edits to `README.md` (repo root), `GOAL.md`, or `TODAY.md`.** Per spec lines 78, 653-657: the command is `manage.py` plumbing, not a consumer-name surface change; the fakeshop schema is unchanged by this card. Same posture as DONE-017's Slice 3. NOT a Worker-2 discretion item.

9. **Run `uv run ruff format .` and `uv run ruff check --fix .` after the edits.** All edits in Slice 3 are Markdown / KANBAN / CHANGELOG; ruff does not lint these files but the project workflow rule (per `AGENTS.md` line 15) is to run both regardless after every edit. Expected output: zero file changes (Markdown is outside ruff's scope). If either ruff invocation reports a change to a `.py` file, that file is unrelated to Slice 3 and Worker 2 reverts the change with `git checkout -- path` before setting `Status: built` (per `BUILD.md` Worker 2 "Validation run" section's tool-induced-drift ownership rule).

10. **No focused `pytest` runs during the Slice 3 build pass.** Slice 3 ships no source or test code; per `AGENTS.md` line 14 and the spec's Slice 3 final-gates list at spec lines 82-85, Worker 1 (final verification) re-runs the focused test scope as a smoke check after the build pass (not Worker 2 during the build). Worker 1's final verification scope: `uv run pytest tests/management/ --no-cov` (the seven tests from Slice 2) + `uv run pytest tests/base/ --no-cov` (`__all__` and version invariants — both stay at `0.0.6` per Decision 9 and step 7 above) + `uv run pytest examples/fakeshop/tests/test_commands.py --no-cov` (the 18 tests including the live `test_export_schema_command_against_fakeshop_schema`).

### Test additions / updates

**No test additions or updates in Slice 3.** Slice 3 is doc-only edits — the spec checklist at lines 73-85 explicitly excludes test changes. Worker 1's final-verification pass re-runs the focused test scope (per step 10 above) as a smoke check to confirm the version-pin invariants at `tests/base/test_init.py` still hold and the seven tests at `tests/management/test_export_schema.py` still pass. No temp tests are appropriate for Slice 3 (no logic shipped).

For Worker 3's review pass: the documentation-sanity check at `BUILD.md` "Documentation / release sanity (only when the slice touches docs, release metadata, KANBAN, or archived specs)" is the load-bearing review surface — read each of the six edited files end-to-end, confirm version strings / shipped statuses / card IDs match the active spec and the unchanged `0.0.6` package version, confirm the moved KANBAN card is removed from In-progress and appears in Done exactly once, confirm Markdown links resolve, and confirm no obsolete "planned for 0.0.7" or "[alpha]" wording remains in the edited surfaces.

### Implementation discretion items

Items where Worker 1 has assessed the design and decided the choice belongs to Worker 2. Architectural questions are NOT delegated here; the spec pins everything load-bearing.

- **TREE.md tree-art whitespace formatting.** Worker 2 reconciles the indentation, alignment of trailing `#` comments, and box-drawing-character spacing on the inserted `management/` subtree and the inserted `tests/management/` subtree to match the surrounding tree-art conventions in `docs/TREE.md`. The TODO marker's pseudo-shape (`docs/TREE.md:206-212` and `docs/TREE.md:347-351`) is the suggested shape, but small whitespace adjustments (e.g. column alignment of the trailing `#` comment columns) are acceptable as long as the file renders cleanly and the children entries align consistently with sibling subtrees.
- **KANBAN.md Done body length and structure for `DONE-018-0.0.7`.** Per step 4(c) above, Worker 2 follows the DONE-017 / DONE-016 precedent at `KANBAN.md:1803-1830` for body length — past-tense "Shipped …" opener, optional one-line follow-up notes if context warrants. Worker 2 MAY preserve historical sub-sections from the WIP card body (Predecessors, Why it matters, Verified in upstream, etc.) OR collapse them per the existing Done-card shape. Either choice ships a Done card that future maintainers can read; the precedent slightly favors collapsing for body-length consistency.
- **KANBAN.md `### In progress` summary paragraph rewrite at line 50.** Per step 4(e) above, Worker 2 picks the exact rewrite phrasing. The HARD constraint is: `WIP-ALPHA-018-0.0.7` must NOT appear in the remaining-cards list after the rewrite. A natural rewrite is sketched in step 4(e); any equivalent rewrite that satisfies the constraint is acceptable.
- **The next available `DONE-NNN-0.0.7` id (re-verified at build time per step 4(a)).** Spec line 644 explicitly says "the next available `DONE-NNN-0.0.7` id ... is determined at merge time, not pinned in this spec." Planning-time verification says `DONE-018-0.0.7`; Worker 2 re-reads `KANBAN.md` at build time and uses whichever id the live file shows as next available.

**NOT discretionary (Worker 2 must NOT vary these; pinned by the spec or by the cross-document consistency contract):**

- The verbatim wording at the five spec-pinned sites: GLOSSARY entry body (spec lines 629-630), README "Shipped today" bullet (spec line 636), CHANGELOG `### Added` bullet (spec lines 648-649), KANBAN Done body (spec lines 644-645), GLOSSARY Index row (spec line 631). Worker 2 copies the spec's wording character-for-character at each site; no paraphrasing.
- The `[alpha]` tag removal at `docs/TREE.md:323` (spec line 640).
- The prose-fragment removal of `, and the management command` at `docs/TREE.md:193` (spec line 641).
- The full removal of the `- schema export management command` bullet at `docs/README.md:122` (spec line 634).
- The deletion of every `<!-- TODO spec-018 Slice 3: ... -->` comment marker in the same edit that lands the real content at each of the ten marker sites (enumerated in the DRY analysis above).
- The Status flip from `planned for 0.0.7` to `shipped (0.0.7)` at both the GLOSSARY Index row (line 105) and the entry body's `**Status:**` line (line 922).
- Appending the new CHANGELOG bullet to the existing `[0.0.7]` `### Added` heading (spec lines 78, 649); do NOT open a second `[0.0.7]` heading.

**EXPLICITLY FORBIDDEN in Slice 3 (NOT discretionary; per spec Decision 9 lines 526-541 and spec line 80):**

- Bumping `pyproject.toml` `[project].version` from `0.0.6` to `0.0.7`.
- Bumping `django_strawberry_framework/__init__.py`'s `__version__` from `"0.0.6"` to `"0.0.7"`.
- Updating `tests/base/test_init.py`'s pinned version assertion from `"0.0.6"` to `"0.0.7"`.
- Editing the date line at `CHANGELOG.md:21` (`## [0.0.7] - 2026-05-20`); the joint-cut date is owned by whichever `0.0.7` card ships last and is finalized only when the version bump lands.
- Adding `Command` (or any new symbol from `django_strawberry_framework.management.*`) to `django_strawberry_framework/__init__.py`'s `__all__` tuple. Per Decision 1 / Slice 1 / Slice 2 contracts, the management command is import-time plumbing and has zero public-surface presence.

### Static inspection helper disposition

**Static inspection helper skipped — Slice 3 touches only Markdown/docs; no `.py` source files in scope.** Per `BUILD.md` "When to run the helper during build", Worker 1 must run the helper during planning only when the plan adds logic to a `.py` file ≥150 lines OR touches `optimizer/` / `types/`. Slice 3 touches `docs/GLOSSARY.md`, `docs/README.md`, `docs/TREE.md`, `KANBAN.md`, and `CHANGELOG.md` — all Markdown. None trigger the helper. Worker 3 similarly skips the helper during review (no `.py` files in the diff). Recording the skip explicitly per the BUILD.md "skip must be recorded explicitly with a short reason" rule.

### Spec slice checklist (verbatim)

The spec's Slice 3 sub-bullets at `docs/spec-018-export_schema-0_0_7.md` lines 73-85, copied verbatim. Worker 1 ticks each `- [x]` during final verification as the contract lands. An unticked box at final verification is either deferred with a one-line reason under `### Spec changes made (Worker 1 only)` or the slice goes `revision-needed`.

- [x] Flip [`Schema export management command`](GLOSSARY.md#schema-export-management-command) from `planned for 0.0.7` to `shipped (0.0.7)` in [`docs/GLOSSARY.md`](GLOSSARY.md); update the Index table's status column at line 104; update the entry body to describe the shipped command shape.
- [x] Update [`docs/README.md`](README.md): **surgically remove the entire `- schema export management command` bullet at line 113** from the `Coming in 0.1.0` section (DONE-017-0.0.7 surgically removed only `, Django `AppConfig`` from that line; this card removes the remaining text in full and deletes the whole bullet). The shipped-list heading at line 89 already reads `**Shipped today** (`0.0.7`):` (DONE-017 bumped it); no further heading change here.
- [x] Update [`docs/TREE.md`](TREE.md): (a) add the `management/` subtree to the **current on-disk layout** section under the `django_strawberry_framework/` tree (alphabetical position between `list_field.py` and `optimizer/`), with the `commands/__init__.py` + `commands/export_schema.py` children spelled out; (b) remove the `[alpha]` tag from the existing `management/` block in the **target package layout** section at lines 309-313 (the tag means "lands before `0.1.0`", and the bullet has now landed); (c) **surgically remove `, and the management command` from the current-on-disk-layout prose at line 190** (rev2 L1 — the prose currently reads "Every other module shown in the target package layout below — query-surface subpackages, the mutation cluster, the auth / forms / DRF integrations, the test client, the Channels router, and the management command — is not on disk yet"; after Slice 3 the management command IS on disk, so the trailing `, and the management command` becomes self-contradicting and must be removed in the same sweep); (d) add `tests/management/test_export_schema.py` (with sibling `__init__.py`) to the **current test-tree** section; placement is **before `test_apps.py`** (alphabetical — `management/` sorts before `test_apps.py`).
- [x] Update [`KANBAN.md`](../KANBAN.md): move `WIP-ALPHA-018-0.0.7` to the Done column with the next available `DONE-NNN-0.0.7` id (the column-move pass renumbers as usual; the next available id is determined at merge time, not pinned in this spec). The past-tense Done body summarizes the shipped scope.
- [x] Update [`CHANGELOG.md`](../CHANGELOG.md): **append** to the existing `[0.0.7]` `### Added` subsection (do NOT create a second `[0.0.7]` heading per [Decision 9](#decision-9--joint-0_0_7-cut) — every `0.0.7` card under the joint cut appends to the same shared section).
- [x] No edits to [`README.md`](../README.md), [`GOAL.md`](../GOAL.md), or [`TODAY.md`](../TODAY.md). Justification: the command is `manage.py` plumbing, not a consumer-name surface change; the fakeshop schema is unchanged by this card. Same posture as [`docs/SPECS/spec-017-apps-0_0_7.md`](SPECS/spec-017-apps-0_0_7.md) Slice 3 for the AppConfig.
- [x] Version bump (deferred to **the last `0.0.7` card to ship**, NOT this card; per [Decision 9](#decision-9--joint-0_0_7-cut)): see [`docs/SPECS/spec-016-list_field-0_0_7.md`](SPECS/spec-016-list_field-0_0_7.md) Decision 10. This card does NOT bump `pyproject.toml`, `django_strawberry_framework/__init__.py`'s `__version__`, or `tests/base/test_init.py`'s version assertion.
- [x] Zero new public exports — the management command is import-time plumbing discovered through Django's `INSTALLED_APPS` machinery, not via `from django_strawberry_framework import …`. `__all__` is unchanged.
- [x] Final gates (same posture as [`docs/SPECS/spec-017-apps-0_0_7.md`](SPECS/spec-017-apps-0_0_7.md) rev6 L2):
  - [x] `uv run ruff format .` passes.
  - [x] `uv run ruff check --fix .` passes.
  - [x] `uv run pytest --no-cov` (or scoped subset) passes; the explicit `--no-cov` opts out of `pytest.ini`'s auto-applied `--cov` per the per-pass-gates contract; coverage enforcement is CI's job (`pyproject.toml [tool.coverage.report] fail_under = 100`), not this slice's.

---

## Build report (Worker 2)

### Files touched

- `docs/GLOSSARY.md` — (a) Index row at line 105 flipped from `planned for 0.0.7` to `shipped (`0.0.7`)` (using the backtick-wrapped version-token shape per the sibling shipped rows); (b) deleted the `<!-- TODO spec-018 Slice 3: ... -->` comment marker at line 104 in the same edit; (c) under `## Schema export management command` (now line ~912), flipped `**Status:** planned for `0.0.7`.` to `**Status:** shipped (`0.0.7`).` and replaced the placeholder body paragraph with the spec's pinned shipped-contract wording (spec lines 629-630, verbatim); (d) deleted the multi-line `<!-- TODO spec-018 Slice 3: rewrite the entry body ... -->` marker block at the former lines 911-919 in the same edit; `**See also:** [Django `AppConfig`](#django-appconfig).` preserved unchanged. Cites spec lines 73-74 (Slice 3 checklist), 628-631 (Doc updates GLOSSARY).
- `docs/README.md` — (a) appended a new bullet under the `**Shipped today** (`0.0.7`):` section immediately after the `Django AppConfig` bullet, using the verbatim wording from spec line 636 (the `manage.py export_schema` bullet with the trailing `See [`GLOSSARY.md#schema-export-management-command`](GLOSSARY.md#schema-export-management-command).` link); (b) deleted the `<!-- TODO spec-018 Slice 3: append a bullet below ... -->` marker block at the former lines 105-109 in the same edit; (c) surgically removed the entire `- schema export management command` bullet from the `**Coming in `0.1.0`**` section at the former line 122; (d) deleted the `<!-- TODO spec-018 Slice 3: surgically remove the entire bullet ... -->` marker block at the former lines 119-121 in the same edit. Cites spec lines 75 (Slice 3 checklist), 633-636 (Doc updates README).
- `docs/TREE.md` — four-part edit: (a) surgically removed `, and the management command` from the current-on-disk-layout prose at the former line 193 (now reads "... the test client, and the Channels router — is not on disk yet ..."); (b) deleted the three-line `<!-- TODO spec-018 Slice 3: surgically remove ... -->` marker block at the former lines 190-192 in the same edit; (c) inserted the `management/` subtree (with `__init__.py` + `commands/__init__.py` + `commands/export_schema.py` children, alphabetical position between `list_field.py` and `types/`) into the current-on-disk-layout `django_strawberry_framework/` tree; deleted the `<!-- TODO spec-018 Slice 3: insert the `management/` subtree below ... -->` marker block at the former lines 206-212 (and the trailing blank line) in the same edit; tree-art whitespace matches the surrounding siblings (single-space indentation after box-drawing characters, single-space between filename and `#` comment); (d) removed the `[alpha]` tag from the `management/` block in the target-package-layout section at the former line 323 (now reads `├── management/              # Django management commands`); deleted the three-line `<!-- TODO spec-018 Slice 3: remove the `[alpha]` tag ... -->` marker block at the former lines 320-322 in the same edit; (e) inserted the `tests/management/` subtree (with `__init__.py` + `test_export_schema.py` children) into the current-test-tree section BEFORE `test_apps.py` (alphabetical); deleted the `<!-- TODO spec-018 Slice 3: insert the `management/` test subdirectory ... -->` marker block at the former lines 347-351 in the same edit. Cites spec lines 76 (Slice 3 checklist), 638-642 (Doc updates TREE).
- `KANBAN.md` — (a) updated the `### In progress` summary paragraph at line 50 to drop the `WIP-ALPHA-018-0.0.7` sentence, change "two have shipped" to "three have shipped", add `DONE-018-0.0.7` to the listing of shipped cards, and reword "The remaining two ... stay queued behind 018." to "The remaining two ... are still queued."; (b) deleted the `<!-- TODO spec-018 Slice 3: when this card ships, MOVE the entire `WIP-ALPHA-018-0.0.7` block ... -->` marker block at the former lines 77-82 in the same sweep; (c) removed the entire `### WIP-ALPHA-018-0.0.7 — Schema export management command` card body (former lines 83-160) from the `## In progress` column; (d) appended a new `### DONE-018-0.0.7 — Schema export management command` block at the end of the `## Done` column, immediately after the DONE-017 spec/build-plan footer line and before `## Release readiness checklist`. The Done body follows the DONE-017 / DONE-016 precedent (past-tense "Shipped …" opener + body sections covering the borrowing posture, test coverage, deferred version bump, and files touched); the lead sentence uses the spec's verbatim wording at spec lines 644-646; (e) removed the stale `- `management/commands/export_schema.py`` entry from the "Still not implemented" Layer-3-planned list at the former line 62 — the file now exists on disk per Slice 1, so leaving it under "Still not implemented" would contradict the Done body. The next available DONE-NNN was re-verified at build time by re-reading `KANBAN.md` post-maintainer-renumber: `DONE-018-0.0.7` is the next available id (no Done cards in the 018-020 range existed; the maintainer's mid-build renumber recomputed WIP-side ids but did not advance Done-side ids). Cites spec lines 77 (Slice 3 checklist), 644-646 (Doc updates KANBAN).
- `CHANGELOG.md` — (a) appended a third bullet to the existing `[0.0.7]` `### Added` subsection, immediately after the `Django AppConfig` bullet at line 24. The new bullet's wording is verbatim from spec lines 648-649 (the `Schema export management command` entry); (b) deleted the `<!-- TODO spec-018 Slice 3: APPEND a third bullet ... -->` marker block at the former lines 25-32 in the same edit. Did NOT create a second `[0.0.7]` heading (per spec Decision 9 lines 526-541 and spec line 649). Did NOT touch the date line `## [0.0.7] - 2026-05-20` at line 21 (joint-cut date is owned by the last `0.0.7` card to ship). Per `AGENTS.md` line 21, this slice's checklist sub-bullet at spec line 78 is the explicit instruction permitting the CHANGELOG edit. Cites spec lines 78 (Slice 3 checklist), 648-649 (Doc updates CHANGELOG).

### Tests added or updated

None; Slice 3 is doc-only.

### Validation run

- `uv run ruff format .` — pass (`110 files left unchanged`; one warning about `COM812` formatter-vs-linter conflict, pre-existing repo config, not slice-introduced).
- `uv run ruff check --fix .` — pass (`All checks passed!`; zero auto-fixes).
- `git status --short` after both ruff invocations:
  - Slice-intended (stay): `MM CHANGELOG.md`, `MM KANBAN.md`, `M docs/GLOSSARY.md`, `M docs/README.md`, `M docs/TREE.md` (the five files this slice edits).
  - Prior-slice-intended (stay; from Slice 1 / Slice 2 work that's already final-accepted): ` M django_strawberry_framework/management/__init__.py`, ` M django_strawberry_framework/management/commands/__init__.py`, ` M django_strawberry_framework/management/commands/export_schema.py`, ` M examples/fakeshop/tests/test_commands.py`, ` M tests/management/__init__.py`, ` M tests/management/test_export_schema.py`.
  - Pre-existing maintainer baseline (untouched by Slice 3; presumptively the maintainer's in-progress work per `AGENTS.md` line 31): `M BACKLOG.md`, `M docs/SPECS/NEXT.md`, `M docs/SPECS/spec-013-deferred_scalars-0_0_6.md`, `M docs/SPECS/spec-014-meta_primary-0_0_6.md`, `M docs/SPECS/spec-015-consumer_overrides_scalar-0_0_6.md`, `M docs/SPECS/spec-016-list_field-0_0_7.md`, `M docs/SPECS/spec-017-apps-0_0_7.md`, `M docs/spec-018-export_schema-0_0_7.md`, ` M django_strawberry_framework/scalars.py`, ` M docs/builder/bld-slice-1-module.md`, ` M docs/review/rev-django_strawberry_framework.md`, ` M docs/review/rev-scalars.md`. Not reverted (out of scope per `AGENTS.md` line 31).
  - Untracked artifacts (stay; this build cycle's artifacts): `?? docs/builder/bld-slice-2-tests.md`, `?? docs/builder/bld-slice-3-promotion_docs.md`, `?? docs/builder/build-018-export_schema-0_0_7.md`.
  - No unrelated tool churn introduced by Slice 3 (ruff format reported zero file changes; ruff check passed clean with zero auto-fixes). Nothing to `git checkout --` revert.
- No focused `pytest` runs (per the artifact's `### Implementation steps` step 10 — Worker 1's final verification owns the focused-test scope).

### Implementation notes

- **DONE-NNN id used.** Used `DONE-018-0.0.7` per Worker 1's planning-time verification (artifact line 52). Re-verified at build time by `grep -c "DONE-018" KANBAN.md` (returned 0 before the edit) and a sweep of the Done column for any 018-020 NNN — none existed. The maintainer's mid-build NNN renumber (per build plan line 10) only touched WIP-side ids; Done-side ids stayed put. No adjustment needed.
- **KANBAN `### In progress` summary rewrite shape.** Followed the artifact's "natural rewrite" sketch at lines 110-114 — dropped the WIP-018 sentence, bumped "two have shipped" → "three have shipped", added `DONE-018-0.0.7` to the shipped-card listing, reworded the "remaining two ... stay queued behind 018." to "remaining two ... are still queued." (drops the now-stale "behind 018" anchor). The trailing sentence about the joint-cut version bump stays unchanged.
- **DONE-018 Done body length and structure.** Followed the DONE-017 / DONE-016 precedent (`KANBAN.md:1803-1827`): past-tense "Shipped …" opener (spec lines 644-645 verbatim), then a paragraph each for the borrowing-posture / test-coverage / deferred-version-bump / files-touched summaries. Did NOT preserve the full WIP card body's "Predecessors / Why it matters / Recommended architectural direction / Definition of done / Files likely touched / Dependencies / Out of scope" sub-sections — the precedent collapses those for body-length consistency, and the spec at lines 644-646 pins the past-tense lead sentence as the contract.
- **Spec citation path in DONE-018 footer.** Used `docs/spec-018-export_schema-0_0_7.md` (the spec's actual on-disk path — verified at task start) rather than `docs/SPECS/spec-018-...` (the WIP card body had cited it inconsistently as both — the WIP `Active spec:` line at the former line 93 used the bare `docs/spec-018-...` path, while the spec itself self-references peers under `docs/SPECS/`). Mirrored DONE-017's footer shape with the corrected path.
- **Removed stale "Still not implemented" entry.** The KANBAN.md "Still not implemented" Layer-3-planned list at the former line 62 carried `- `management/commands/export_schema.py``. Slice 1 landed the file on disk; leaving the entry in place after Slice 3 marks the card Done would surface the file as "shipped per the Done body AND still planned per Still-not-implemented", which fails the "no obsolete 'coming soon' / 'planned' wording in files the slice deliberately updates" sanity check in `BUILD.md` "Documentation / release sanity". The removal is in-scope as a consistency cleanup tied to the move-to-Done; not a discretionary cleanup unrelated to the slice. Same site-class as the `, and the management command` prose-fragment removal in TREE.md (spec line 641) — both are downstream inconsistencies that would contradict the shipped state if left in place.
- **GLOSSARY status-token shape.** Used `shipped (`0.0.7`)` with the backtick-wrapped version-token, mirroring the sibling shipped row at line 102 (`Scalar field override semantics | shipped (`0.0.6`)`). The spec at line 629 says "flip ... from `planned for 0.0.7` to `shipped (0.0.7)`" without explicitly pinning whether the version token carries backticks; the surrounding rows do, so I matched the existing convention. No semantic change vs. the spec; just preserves the column's typographic shape.
- **No `examples/fakeshop/config/schema.py` edits.** Per the spec's Decision 10 (live coverage in `examples/fakeshop/tests/test_commands.py`, not `test_query/`) and AGENTS.md line 31 (presumptively maintainer baseline). The schema module is untouched.

### Notes for Worker 3

- **CHANGELOG sanity check applies.** Slice 3 modifies `CHANGELOG.md`; per `BUILD.md` "CHANGELOG sanity" Worker 3 must confirm: (a) the `[0.0.7]` version line is intact and the new bullet appends to the existing `### Added` (no second `[0.0.7]` heading was created); (b) the version line `[0.0.7] - 2026-05-20` at line 21 is unchanged (the joint-cut date is owned by the last `0.0.7` card to ship per spec Decision 9 — 018 is not last in the bundle); (c) the new bullet's wording matches spec lines 648-649 character-for-character; (d) `pyproject.toml`'s `[project].version` stays at `0.0.6` and `django_strawberry_framework/__init__.py`'s `__version__` stays at `"0.0.6"` (verified at build time by direct file read; both untouched).
- **Documentation / release sanity check applies.** Slice 3 touches `docs/GLOSSARY.md`, `docs/README.md`, `docs/TREE.md`, and `KANBAN.md`. Worker 3 walks the BUILD.md "Documentation / release sanity" section for each: (i) the moved KANBAN card appears exactly once in Done as `### DONE-018-0.0.7` and zero times in In Progress (verified by `grep -c` at build time: `DONE-018-0.0.7` → 2 hits = once in the summary listing + once in the heading; `WIP-ALPHA-018` → 0 hits); (ii) the Markdown link in the new `docs/README.md` bullet (`[`GLOSSARY.md#schema-export-management-command`](GLOSSARY.md#schema-export-management-command)`) points at the renamed GLOSSARY entry, which still carries the same heading text and therefore the same anchor; (iii) no `<!-- TODO spec-018 ... -->` markers remain anywhere — confirmed by `grep -n "TODO spec-018"` returning empty across all five edited files; (iv) no `[alpha]` tag remains on the `management/` line in TREE.md target layout; no `planned for 0.0.7` text remains on the `Schema export management command` row in the GLOSSARY Index or entry body; (v) the verbatim-from-spec wording lands at four sites (GLOSSARY entry body / README "Shipped today" bullet / CHANGELOG `### Added` bullet / KANBAN Done body opener) — the spec source lines are 629-630, 636, 648-649, and 644-645 respectively; (vi) the Done body footer cites the active spec at `docs/spec-018-export_schema-0_0_7.md` (not under `docs/SPECS/`), matching the spec's actual on-disk path.
- **Static inspection helper skipped.** Slice 3 touches only Markdown / KANBAN / CHANGELOG; no `.py` source files. Worker 1's planning artifact (lines 165-167) authorizes the skip.

### Notes for Worker 1 (spec reconciliation)

None.

---

## Review (Worker 3)

### Static inspection helper

Static inspection helper not applicable — Slice 3 is doc-only; no `.py` source files in scope. Per `BUILD.md` "When to run the helper during build" and Worker 1's planning disposition at lines 165-167, the helper is skipped for this slice.

### Spec slice checklist walk

Walked every `- [ ]` under the Plan's `### Spec slice checklist (verbatim)` against Worker 2's diff and current working-tree state:

1. **Flip GLOSSARY Schema export management command status + entry body.** Addressed. Index row at `docs/GLOSSARY.md:104` flipped to `shipped (\`0.0.7\`)`; entry body at `docs/GLOSSARY.md:910-916` carries `**Status:** shipped (\`0.0.7\`).` plus the spec-pinned shipped-contract paragraph; the `<!-- TODO spec-018 Slice 3: ... -->` markers at the index row and the entry body are both removed; `**See also:** [Django \`AppConfig\`](#django-appconfig).` preserved.
2. **Remove `- schema export management command` bullet from `Coming in 0.1.0` in docs/README.md.** Addressed. The bullet and the preceding `<!-- TODO spec-018 Slice 3: ... -->` marker are both removed from the `Coming in 0.1.0` section (formerly at line 113 / live line 122).
3. **docs/TREE.md (a)/(b)/(c)/(d).** All four sub-edits addressed:
   - (a) Current-on-disk-layout `management/` subtree added at `docs/TREE.md:203-207` (between `list_field.py` and `types/`, with `__init__.py` + `commands/__init__.py` + `commands/export_schema.py` children).
   - (b) `[alpha]` tag removed from the target-package-layout `management/` block at `docs/TREE.md:314`.
   - (c) `, and the management command` removed from the current-on-disk-layout prose at `docs/TREE.md:190`; the sentence now reads "... the test client, and the Channels router — is not on disk yet ..." per spec line 641.
   - (d) `tests/management/` (with `__init__.py` + `test_export_schema.py` children) added at `docs/TREE.md:338-340`, BEFORE `test_apps.py` per spec line 76.
4. **Move WIP-ALPHA-018-0.0.7 → DONE-NNN-0.0.7 in KANBAN.md.** Addressed. The maintainer's recent commit `216e6ba update card names` landed the column move plus the In-progress summary update onto `main` between Slice 2 final-acceptance and the Slice 3 build pass; the working-tree diff against `HEAD` therefore shows only Worker 2's residual contribution to KANBAN (a single-line cleanup at line 62 removing `management/commands/export_schema.py` from the "Still not implemented" list — see `Notes for Worker 1` below). The DONE-018 card exists at `KANBAN.md:1743-1755` with the spec-verbatim past-tense opener at line 1745; `WIP-ALPHA-018` appears zero times in the file post-edit (verified by `grep -c`); the `### In progress` summary at line 50 correctly drops the WIP-018 sentence and updates to "three have shipped".
5. **Append a third bullet to existing `[0.0.7]` `### Added` in CHANGELOG.md.** Addressed via the same maintainer commit (`216e6ba`); the bullet lives at `CHANGELOG.md:25` with the spec-pinned wording verbatim; no second `[0.0.7]` heading was created (`grep -c "## \[0.0.7\]"` returns 1); the version line `## [0.0.7] - 2026-05-20` at line 21 stays unchanged.
6. **No edits to README.md, GOAL.md, TODAY.md.** Addressed; `git diff -- README.md GOAL.md TODAY.md` is empty.
7. **Version bump deferred (no pyproject / `__version__` / test_init.py changes).** Addressed; `git diff -- pyproject.toml django_strawberry_framework/__init__.py tests/base/test_init.py` is empty; `pyproject.toml:4` stays at `version = "0.0.6"`, `django_strawberry_framework/__init__.py:26` stays at `__version__ = "0.0.6"`, and `tests/base/test_init.py:11` stays at `assert __version__ == "0.0.6"`.
8. **Zero new public exports — `__all__` unchanged.** Addressed; `git diff -- django_strawberry_framework/__init__.py` is empty.
9. **Final gates (ruff format / ruff check / pytest --no-cov).** Worker-2 build pass reports both ruff invocations passed clean with no auto-fixes; the per-slice `pytest tests/base/test_init.py --no-cov` smoke I ran during review reports `4 passed in 0.05s` (confirms `__all__` and version pin invariants hold). Final full-suite gate is Worker 1's final-test-run gate, not this slice.

Every sub-check has a matching implementation in the working tree; none are silently un-addressed.

### High:

None.

### Medium:

None.

### Low:

None.

### DRY findings

No engineering DRY surface in Slice 3 (no `.py` source files touched). The cross-document DRY concern Worker 1 flagged in the Plan's `### DRY analysis` — verbatim consistency of the shipped-contract wording across five sites — holds:

- **GLOSSARY entry body** (`docs/GLOSSARY.md:914`): matches spec line 630 character-for-character (verified by side-by-side `sed` comparison).
- **README "Shipped today" bullet** (`docs/README.md:105`): matches spec line 636 character-for-character.
- **CHANGELOG `### Added` bullet** (`CHANGELOG.md:25`): matches spec lines 648-649 character-for-character (the inner-quote escaping `\"schema\"` in the spec source resolves to `"schema"` in the live file as expected).
- **KANBAN Done body lead sentence** (`KANBAN.md:1745`): matches spec lines 644-645 character-for-character.
- **TREE.md tree-art entry** (`docs/TREE.md:203-207`): describes the same shipped contract in tree-art shape with appropriate file-level granularity; not a verbatim site but consistent in meaning with the four prose sites.

No paraphrase drift between the five sites.

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` is empty. `__all__` and the re-export list are unchanged. The active spec explicitly forbids any new public export for this slice (Decision 1 / Slice 3 sub-bullet at spec line 81 / DoD item 12).

### CHANGELOG sanity

Slice 3 modifies `CHANGELOG.md`. Read the new bullet at `CHANGELOG.md:25` end-to-end:

- **Version line vs pyproject.** The CHANGELOG version heading at line 21 reads `## [0.0.7] - 2026-05-20`; `pyproject.toml:4` and `django_strawberry_framework/__init__.py:26` both stay at `0.0.6`. This is NOT a mismatch per the spec policy — Decision 9 (lines 526-541) explicitly defers the `pyproject.toml` / `__version__` / `tests/base/test_init.py` bump to whichever `0.0.7` card ships last under the joint cut. WIP-ALPHA-019-0.0.7 and WIP-ALPHA-020-0.0.7 are still queued, so 018 is not last in the bundle. Explicitly noting it here so a future reviewer scanning the CHANGELOG → pyproject pair does not flag it.
- **Heading used.** `### Added` — matches what the spec authorizes at Slice 3 sub-bullet (spec line 78) and Decision 9.
- **Wording.** Matches spec lines 648-649 verbatim.
- **No overstatement / understatement.** The bullet correctly names the shipped surface (module path, `Command(BaseCommand)`, positional `schema`, `--path`, `print_schema`, `import_module_symbol` with `default_symbol_name="schema"`, three `CommandError` failure modes, the four deferred features). The "(each deferred to a follow-up card driven by consumer demand)" clause correctly tags the omissions as deferred, not removed.
- **Single `[0.0.7]` heading.** `grep -c "## \[0.0.7\]" CHANGELOG.md` returns 1; the joint-cut policy is honored.

### Documentation / release sanity

Slice 3 touches docs, release metadata, and KANBAN. Walking the `BUILD.md` "Documentation / release sanity" checklist:

- **Version strings, shipped/planned statuses, and card IDs match the active spec and the unchanged `0.0.6` package version.** Confirmed: GLOSSARY Index row, GLOSSARY entry body Status line, README "Shipped today (0.0.7)" bullet, CHANGELOG `[0.0.7]` heading, and KANBAN DONE-018-0.0.7 / In-progress summary all consistently describe the export_schema command as shipped under `0.0.7` while pyproject / `__version__` / `test_init.py` stay at `0.0.6`.
- **KANBAN card moved exactly once.** Confirmed: `grep -c "WIP-ALPHA-018" KANBAN.md` returns 0; `grep -c "DONE-018-0.0.7" KANBAN.md` returns 2 (once in the In-progress summary listing at line 50, once in the Done heading at line 1743). DONE-018 body at lines 1743-1755 follows the DONE-017 / DONE-016 precedent (past-tense "Shipped …" opener + body paragraphs covering borrowing posture, test coverage, deferred version bump, files touched) with the spec-verbatim lead sentence.
- **Markdown links resolve.** The new bullet in `docs/README.md:105` points at `GLOSSARY.md#schema-export-management-command`; the GLOSSARY heading at `docs/GLOSSARY.md:910` is unchanged (`## Schema export management command`), so the anchor still resolves. The DONE-018 footer at `KANBAN.md:1755` cites `docs/spec-018-export_schema-0_0_7.md` and `docs/builder/build-018-export_schema-0_0_7.md`; both exist on disk.
- **Active-spec archival.** Not planned for this card; the active spec stays at `docs/spec-018-export_schema-0_0_7.md`. Confirmed unchanged location.
- **Verbatim text from the spec.** All four prose sites match spec source character-for-character (verified by side-by-side `sed -n '<line>p'` comparison; see DRY findings above for the per-site results).
- **No obsolete "coming soon" / "planned" / old-version wording.** Confirmed: `grep -n "planned for.*0.0.7"` returns only the `Multi-database cooperation` row (WIP-019, out of scope); `grep -n "\[alpha\] Django management"` returns empty; `grep -n "schema export management command"` against `docs/README.md` returns empty (the bullet under "Coming in 0.1.0" is fully removed); `grep -rn "TODO spec-018"` across all five edited files returns empty.

### What looks solid

- **Verbatim spec wording reproduced at all four prose sites.** GLOSSARY entry body, README bullet, CHANGELOG bullet, and KANBAN Done body lead sentence all match the spec character-for-character — no paraphrase drift, no quote escaping artifacts, no link drift. The Plan's "use the spec wording verbatim — do not paraphrase" instruction landed cleanly.
- **TODO-marker removal hygiene.** Every `<!-- TODO spec-018 Slice 3: ... -->` block from commit `d35385c` was deleted in the same edit that landed the real content; `grep -rn "TODO spec-018"` returns empty across all five edited files. The delete-then-write disposition is uniform.
- **Slice-3 invariants preserved.** Five forbidden surfaces (pyproject, `__version__`, test_init.py, README.md/GOAL.md/TODAY.md, `__all__`) are confirmed unchanged via empty `git diff` results; the version-bump deferral to whichever `0.0.7` card ships last is intact per Decision 9.

### Temp test verification

No temp tests created during review; Slice 3 is doc-only.

The focused smoke test recommended by the task instructions (`uv run pytest tests/base/test_init.py --no-cov`) ran during review and reports `4 passed in 0.05s`. This confirms `tests/base/test_init.py`'s version assertion still holds at `0.0.6` and the `__all__` surface is unchanged — both invariants Slice 3 was forbidden from disturbing.

### Notes for Worker 1 (spec reconciliation)

- **KANBAN.md column-move landed via maintainer commit, not Worker 2's build pass.** The maintainer's commit `216e6ba update card names` (immediately preceding Slice 3's build pass) already landed the WIP-018 → DONE-018 move, the spec-verbatim DONE-018 body, the `### In progress` summary rewrite, and the CHANGELOG `### Added` bullet. Worker 2's residual contribution to `KANBAN.md` in the working-tree diff against `HEAD` is a single-line cleanup at line 62 (removal of `management/commands/export_schema.py` from the "Still not implemented" Layer-3-planned list — a downstream-consistency cleanup tied to the move-to-Done, in scope per Worker 2's `### Implementation notes` rationale). This means the file's slice-final state matches the spec's contract exactly, just delivered in two passes (maintainer-side + Worker-2-side). Worker 1 should weigh whether the build plan's mid-build baseline drift entry should be amended to record this second concurrent maintainer pass; the contract is met either way.
- **Spec line citations carry minor numerical drift.** The spec at Slice 3 sub-bullets and Doc updates cites file lines (e.g. "line 113" for the README "Coming in 0.1.0" bullet, "line 190" for the TREE.md prose, "line 309" for the TREE.md target-layout `management/` block, "line 104" for the GLOSSARY Index row) that have shifted in the live files since the spec was authored (Worker 1's planning artifact at lines 42-54 already recorded the live-line resolutions: README line 122, TREE.md line 193 / 323, GLOSSARY line 105). The build report and the actual edits used the live-line resolutions; no contract drift, but Worker 1 may want to refresh the spec's line citations during final verification so future reviewers entering through the spec don't re-discover the drift.
- **`docs/spec-018-export_schema-0_0_7.md` Status line.** Per Worker 1's planning step 6 (artifact line 122), the spec's `Status: draft (revision 5, ...)` line at line 4 stays at `draft` until the slice is final-accepted. Worker 1 owns the final-verification call on whether the joint-cut `0.0.7` shipping state warrants a status-line flip; not a Slice 3 concern.

### Review outcome

`review-accepted`. Every Slice 3 spec sub-bullet is reflected in the working tree; every forbidden surface is confirmed unchanged; the verbatim wording lands at all four prose sites character-for-character; CHANGELOG and Documentation/release sanity checks pass; no High, Medium, or Low findings.

---

## Final verification (Worker 1)

- **Spec slice checklist walk.** Walked every `- [ ]` in the Plan's `### Spec slice checklist (verbatim)` against the live working tree and ticked each `- [x]` whose contract landed (all nine top-level sub-checks plus the three nested final-gate sub-checks). Live-file verification:
  - GLOSSARY Index row at `docs/GLOSSARY.md:104` reads `| [Schema export management command](#schema-export-management-command) | shipped (`0.0.7`) |`; entry body at `docs/GLOSSARY.md:910-916` carries `## Schema export management command` + `**Status:** shipped (`0.0.7`).` + the spec-pinned shipped-contract paragraph + the preserved `**See also:** [Django `AppConfig`](#django-appconfig).` line.
  - README "Shipped today" bullet at `docs/README.md:105` matches spec line 636 verbatim; the `Coming in 0.1.0` section at `docs/README.md:108-114` no longer mentions schema-export.
  - TREE.md current-on-disk layout has the `management/` subtree at lines 203-207 with `__init__.py` + `commands/__init__.py` + `commands/export_schema.py` children; prose at line 190 reads "... the test client, and the Channels router — is not on disk yet ..." (the `, and the management command` fragment is gone); target-layout `management/` block at line 314 has no `[alpha]` tag; current-test-tree at `docs/TREE.md:338-340` carries the `tests/management/` subtree before `test_apps.py`.
  - KANBAN Done card at `KANBAN.md:1743-1755` (`### DONE-018-0.0.7 — Schema export management command`) carries the spec-verbatim past-tense opener; `### In progress` summary at `KANBAN.md:50` correctly states "three have shipped" and lists `DONE-018-0.0.7`; `grep -c "WIP-ALPHA-018"` returns 0.
  - CHANGELOG bullet at `CHANGELOG.md:25` is the third entry under the single `[0.0.7]` `### Added` subsection (verified `grep -cn '^## \[0.0.7\]' CHANGELOG.md` = 1); wording is verbatim from spec lines 648-649.
  - Forbidden surfaces clean: `git diff -- pyproject.toml django_strawberry_framework/__init__.py tests/base/test_init.py` returns empty (all three stay at `0.0.6`); `git diff -- README.md GOAL.md TODAY.md` returns empty.
  - No `- [ ]` remains unticked.
- **DRY check across this slice and prior accepted slices.** The spec-pinned shipped-contract wording is reproduced at five sites; Worker 3 verified character-for-character match at four prose sites (GLOSSARY entry body, README bullet, CHANGELOG bullet, KANBAN Done body opener), and the tree-art entry at `docs/TREE.md:203-207` is consistent in meaning. Re-walked the four sites against `docs/spec-018-export_schema-0_0_7.md` lines 629-630 / 636 / 644-645 / 648-649 — confirmed no paraphrase drift. Cross-slice consistency: Slice 1's shipped `Command(BaseCommand)` source remained untouched in Slice 3 (`git diff -- django_strawberry_framework/management/commands/export_schema.py` against `HEAD` shows only the maintainer-baseline state from Slice 1 final-acceptance, which matches the contract); Slice 2's seven tests and the fakeshop live test still pass under the focused scope; the doc-side promotion correctly describes the source-side shape.
- **Existing tests still pass (focused scopes).**
  - `uv run pytest tests/base/ --no-cov` → `19 passed in 0.06s`. `tests/base/test_init.py::test_public_api_surface_is_pinned` passes — `__all__` is unchanged; `tests/base/test_init.py::test_version` passes — `__version__` stays at `0.0.6`. Confirms Slice 3 did NOT bump `__all__` or any version pin.
  - `uv run pytest tests/management/ --no-cov` → `7 passed in 0.05s`. Confirms Slice 1's shipped source + Slice 2's tests still pass.
  - `uv run pytest examples/fakeshop/tests/test_commands.py --no-cov` → `18 passed, 1 warning in 4.53s`. The single warning (`Overriding setting DATABASES`) belongs to the pre-existing `test_seed_shards_command_runs_when_shard_alias_present` baseline and is unchanged by this slice. Confirms the fakeshop live `test_export_schema_command_against_fakeshop_schema` still passes.
- **Spec reconciliation.** Decided not to edit `docs/spec-018-export_schema-0_0_7.md`. See `### Spec changes made (Worker 1 only)` below for the per-note disposition (Worker 3's reconciliation notes 2 and 3 both intentionally deferred).
- **Slice splitting check.** Not applicable — Slice 3 landed cleanly as one slice.
- **Final status.** `final-accepted`. Top-level `Status:` line updated.

### Summary

Slice 3 promoted [`Schema export management command`](../GLOSSARY.md#schema-export-management-command) from `planned for 0.0.7` to `shipped (0.0.7)` and swept the documentation surface to reflect the shipped state. Five doc-only sites were edited end-to-end with verbatim spec wording at four of them (GLOSSARY entry body, README "Shipped today" bullet, CHANGELOG `### Added` append, KANBAN Done body lead sentence), plus a tree-art addition at `docs/TREE.md` current-on-disk-layout and tests current-tree sections, removal of the `[alpha]` tag on the target-layout `management/` block, removal of the obsolete `Coming in 0.1.0` schema-export bullet, surgical removal of the `, and the management command` prose fragment, and the move of `WIP-ALPHA-018-0.0.7` to `DONE-018-0.0.7`. Every pre-staged `<!-- TODO spec-018 Slice 3: ... -->` marker from commit `d35385c` is gone. The version bump (`pyproject.toml`, `__version__`, `tests/base/test_init.py`) stays deferred to whichever `0.0.7` card ships last under Decision 9's joint cut — WIP-ALPHA-019-0.0.7 and WIP-ALPHA-020-0.0.7 are still queued. Zero new public exports; `__all__` is unchanged. All three focused test scopes pass (19 + 7 + 18).

### Spec changes made (Worker 1 only)

None — spec line-number citations are pin-at-write-time navigational hints; the contract landed correctly. Status-line change to "shipped" deferred to Worker 0's closeout / the joint-cut last-card-to-ship.

Per-note disposition for Worker 3's `### Notes for Worker 1 (spec reconciliation)`:

- **Worker 3 note 1 (KANBAN.md column-move landed via maintainer commit `216e6ba`, not Worker 2's build pass).** No spec edit needed; the contract landed exactly as the spec requires, just delivered across two passes (maintainer commit + Worker 2's residual single-line cleanup at `KANBAN.md:62`). Worker 0 has already recorded this under "Mid-build baseline drift addendum" in `docs/builder/build-018-export_schema-0_0_7.md` lines 12-15. Nothing further for Worker 1 to amend at the spec level.
- **Worker 3 note 2 (minor numerical drift in spec line citations).** No spec edit. The drift is real (e.g. spec says `docs/README.md` "line 113" but the live bullet was at line 122; spec says `docs/TREE.md` "line 190" but the live prose was at line 193; spec says `docs/GLOSSARY.md` "line 104" for the Index row but the live row was at 105 with the TODO marker at 104) — but the cited lines are pin-at-write-time navigational hints per `docs/builder/BUILD.md`'s "Plan template" Implementation steps note ("Line numbers are pin-at-write-time navigational hints. Verify against the current source before editing"). They are NOT load-bearing contracts; the contract landed correctly because Worker 1's planning artifact at `docs/builder/bld-slice-3-promotion_docs.md:42-54` had already re-resolved every spec citation to live line numbers, and Worker 2 used those live resolutions. Refreshing the spec citations now would (a) require a rev6 for non-load-bearing typo cleanup, (b) misalign the spec's revision history with the slices already shipped against rev5, and (c) re-stale the next time anyone edits the upstream doc files. Better outcome: leave the spec at rev5 final shipping state and let future spec authors who copy the structure see the pin-at-write-time pattern in action.
- **Worker 3 note 3 (spec status line at line 4 still reads `draft (revision 5, ...)`).** Deferred to Worker 0's closeout / joint-cut last-card-to-ship. The status-line flip from `draft` to `shipped` belongs to the build's closeout, not to Slice 3 final verification. WIP-ALPHA-019-0.0.7 and WIP-ALPHA-020-0.0.7 are still queued; the joint cut isn't complete yet, so the spec is best described as `draft (revision 5)` from this card's perspective until the bundle ships. Worker 0 (or whichever Worker 1 spawn closes out the bundle) is the right point to revisit the status line.
