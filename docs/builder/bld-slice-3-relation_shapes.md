# Build: Slice 3 — relation-as-Connection upgrade — `Meta.relation_shapes` + Phase-2.5 synthesis

Spec reference: `docs/spec-032-full_relay-0_0_9.md` (Slice-3 checklist lines 95-98; Decision 6 at 358-381; Decision 7 at 382-401; Error shapes 259-260; Edge cases 527-529 + 531; Test plan Slice 3 at 565-576; Current state 139-140, 143; pre-`033` posture: Decision 12 / Non-goals / build-plan context flag)
Status: final-accepted

## Plan (Worker 1)

Spec status-line re-verification (worker-1.md per-spawn rule): spec line 5 reads "in build — Slices 1–2 implemented (final-accepted in the build cycle, uncommitted); Slices 3–7 not started" — accurate at the start of this planning pass; no edit needed.

Spec-vs-codebase verification: every symbol the spec names for this slice exists on disk — `types/base.py::_validate_connection` (base.py:167, the structural model), `types/base.py::_is_relay_shaped` (base.py:410) and the precomputed `relay_shaped` bool in `_validate_meta` (base.py:983), `consumer_authored_fields` (base.py:475-482), `DjangoTypeDefinition` with the staged `relation_shapes` anchor (definition.py:125-130), `types/finalizer.py::implements_relay_node` Phase-2.5 loop (finalizer.py:360-373) with the Slice-3 anchor at finalizer.py:392-417, and the spec-030 machinery `connection.py::_connection_type_for` (connection.py:298), `_synthesized_signature` (connection.py:477), `_pipeline_sync` (connection.py:422), `_build_connection_resolver` (connection.py:542) with the Slice-3 anchor at connection.py:610-630. Strawberry's default-camel-case rule is `strawberry.utils.str_converters.to_camel_case` (the exact function `strawberry/schema/name_converter.py::NameConverter` applies under `auto_camel_case=True`). No spec-vs-codebase gap requiring escalation; two **intra-spec wording tensions are resolved in-plan** (see "Notes for Worker 1 (spec reconciliation)" below) per the controlling Decision-7 / Error-shapes text and the staged anchors.

Staged `TODO(spec-032 … Slice 3)` anchors this slice ships and removes (grep-verified):

- `django_strawberry_framework/types/base.py:79-100` (the `ALLOWED_META_KEYS` + `_validate_relation_shapes` contract) and `types/base.py:549-551` (the `relation_shapes=validated.relation_shapes` threading).
- `django_strawberry_framework/types/definition.py:125-130` (the `relation_shapes` slot).
- `django_strawberry_framework/types/finalizer.py:392-417` (the Phase-2.5 synthesis pseudocode).
- `django_strawberry_framework/connection.py:610-630` (the relation-seeded resolver variant pseudocode).
- `tests/types/test_base.py:346-356` (the key-validation test list).
- `tests/test_relay_connection.py:11-31` (the Slice-3 synthesis test list — the file's Slice-4 anchor at lines 33+ stays untouched; the module docstring's "Staged lists below are removed by the change that ships each slice" sentence stays accurate for the remaining Slice-4 block).

### DRY analysis

- **Existing patterns reused** (cite file:line):
  - **Validator model:** `types/base.py::_validate_connection` (base.py:167-209) — `None` short-circuit, dict-shape check, unknown-entry typo guard, Relay-Node gate taking the precomputed `relay_shaped` bool. `_validate_relation_shapes` is its structural sibling, called from `_validate_meta` in the same block (base.py:986-991) and threaded through `_ValidatedMeta` (base.py:879-899) exactly like `connection` / `globalid_strategy`.
  - **Two-stage validation precedent:** the spec-029 `nullable_overrides` flow — shape stage inside `_validate_meta` (base.py:992-1012), target stage in `__init_subclass__` after `_select_fields` + `consumer_authored_fields` exist (`_validate_nullability_override_targets`, base.py:1092-1187, called at base.py:484-491). The field-level `relation_shapes` checks (unknown / excluded / non-relation / single-valued / consumer-authored) need the selected fields and the consumer-override union, which do not exist at `_validate_meta` time — the new `_validate_relation_shape_targets` mirrors that helper's signature shape, check order (unknown → excluded → per-name checks), and placement.
  - **Unknown-field error shape:** `types/base.py::_format_unknown_fields_error` (base.py:709-723) — the unknown-key path routes through it with `attr="relation_shapes"`, matching the `Meta.fields` / `Meta.exclude` / `Meta.optimizer_hints` / overrides typo guards.
  - **Vocabulary-constant precedent:** `STRING_GLOBALID_STRATEGIES` / `DEFAULT_GLOBALID_STRATEGY` (base.py:109-110) — the valid-shape set and the `"both"` default land as module constants (`RELATION_SHAPE_VALUES`, `DEFAULT_RELATION_SHAPE`) so the validator's typo-guard text and the finalizer's default read share one source instead of re-typed literals.
  - **Many-sidedness classifier:** `utils/relations.py::relation_kind` / `is_many_side_relation_kind` (relations.py:35-86) via `FieldMeta.is_many_side` read from `definition.field_map[snake_case(name)]` (field_meta.py:108-115) — the same single-source classifier `types/resolvers.py::_make_relation_resolver` keys on. No new cardinality logic.
  - **Definition slot precedent:** `DjangoTypeDefinition.connection` (definition.py:124) + its invariants-docstring bullet (definition.py:64-70) — `relation_shapes` lands beside it with the same populate-in-`__init_subclass__`, read-by-Phase-2.5 contract.
  - **Synthesis-step home:** the finalizer Phase-2.5 loop conventions — `registry.iter_definitions()` + per-entry `if definition.finalized: continue` (finalizer.py:360-362), `implements_relay_node` as the Relay predicate (correct at this phase: `apply_interfaces` has already injected the bases), `registry.get(related_model)` for target resolution (the same lookup Phase 1 uses at finalizer.py:322, honoring `Meta.primary`), and the function-local plain-import precedent for cycle-safe cross-module reads (`from ..relay import _node_fields_declared`, finalizer.py:385 — the new `from ..connection import …` follows it: a contract step, never try/except-guarded).
  - **Connection machinery reused wholesale** (spec-030; zero reimplementation): `connection.py::_connection_type_for` (connection.py:298-319 — per-target class + `totalCount` opt-in + cache), `_synthesized_signature` (connection.py:477-539 — sidecar-derived `filter:` / `order_by:` args whose `filter_input_type` / `order_input_type` calls self-register on the orphan ledgers), `_pipeline_sync` (connection.py:422-450 — visibility → filter → orderBy → deterministic order → optimizer seam), and the default-branch resolver shape of `_build_connection_resolver` (connection.py:570-579 — sync resolver returning a LAZY queryset; the committed-at-construction rationale at connection.py:542-568 holds verbatim for the synthesized relation resolver).
  - **Camel-case rule:** `strawberry.utils.str_converters.to_camel_case` — imported, not hand-rolled; it IS the default `NameConverter` rule, so the guard cannot drift from Strawberry's actual default naming.
  - **Test fixtures:** `tests/test_connection.py::_make_node_type` / `_schema_for` (test_connection.py:73-115) and its `_isolate_global_registry` autouse (clears `registry` + `_connection_type_cache`, test_connection.py:59-71) — the new suite copies that fixture shape. Cardinality fixtures: products `Category.items` reverse FK (`seed_data(N)` first line per AGENTS.md) and library `Book.genres` / `Genre.books` forward/reverse M2M (inline `Model.objects.create` — the library fixture rule). Sidecar/`get_queryset` fixture patterns: tests/test_connection.py's inline FilterSet/OrderSet types and the `get_queryset`-filtered node-type pattern in tests/types/test_relay_interfaces.py.
- **New helpers justified** (each single-responsibility, ≥1 well-defined call surface):
  - `types/base.py::_validate_relation_shapes(meta, value, relay_shaped)` — stage-1 shape + gate validator (the spec-named helper). Call site: `_validate_meta`.
  - `types/base.py::_validate_relation_shape_targets(...)` — stage-2 field-level validator (unknown / excluded / non-relation / single-valued / consumer-authored). Call site: `__init_subclass__`, beside `_validate_nullability_override_targets`. (Why two functions, not the anchor's one: the four-param single function literally cannot run from `_validate_meta` — `consumer_authored_fields` is computed after it returns — and splitting follows the established spec-029 two-stage pattern. See the reconciliation note.)
  - `types/base.py::_RELAY_NODE_GATE_LEAD` (name at Worker 2's discretion) — module constant for the shared gate fragment `"requires a Relay-Node-shaped type; add \`relay.Node\` to \`Meta.interfaces\`"`. Today that fragment exists twice verbatim in base.py (`_validate_connection` base.py:204-208; `_validate_globalid_strategy` base.py:277-281, whose comment already says "mirrors `_validate_connection`'s gate and remediation text"); the relation_shapes gate adds a third occurrence with a spec-pinned different tail ("… or remove the key", Decision 7 line 388). Hoist the lead-in to the constant (the `_INTERFACES_SHAPE_ERROR_LEAD_IN` precedent, base.py:726-729) and compose all three sites from it — byte-identical output at the two existing sites (their pinned tests keep passing unmodified).
  - `connection.py::_build_relation_connection_resolver(target_type, accessor_name)` — the staged anchor's helper: the relation-manager-seeded resolver (`getattr(root, accessor_name).all()` → `_pipeline_sync` → `_synthesized_signature` attach). Call site: the finalizer synthesis step.
  - `types/finalizer.py::_synthesize_relation_connections()` (+ a small collision-guard local, shape at Worker 2's discretion) — the Phase-2.5 step. Call site: `finalize_django_types()` at the anchor position.
- **Duplication risk avoided:**
  - The naive synthesis would re-derive the connection class, the sidecar signature, or the pipeline — avoided: the only new runtime code in `connection.py` is the 6-line relation-seeded source; everything downstream is the shipped pipeline (`_pipeline_sync` + `_synthesized_signature` + `_connection_type_for` called as-is).
  - The naive validator would re-type the `{"list", "connection", "both"}` literal in the typo-guard message, the finalizer default, and tests — avoided by `RELATION_SHAPE_VALUES` / `DEFAULT_RELATION_SHAPE` (finalizer imports the default; the typo-guard message renders `sorted(RELATION_SHAPE_VALUES)` like the `STRING_GLOBALID_STRATEGIES` message at base.py:261-263).
  - The Relay-gate text would otherwise reach a third same-file near-copy — consolidated via the lead-in constant (above), which also retires half of the standing integration-pass concern about that family.
  - The accessor used by the synthesized resolver is the same `field.name` the shipped many-side list resolver reads (`types/resolvers.py::many_resolver`, resolvers.py "`getattr(root, field_name).all()`") — pinned so the list field and its connection sibling can never disagree about which manager they traverse.
  - **Integration-pass watch items re-checked:** (1) the 2x `".Meta.interfaces entry "` prefix in base.py — this slice does NOT touch `_validate_interfaces` and adds no third occurrence (the relation_shapes messages use the `"{model}.Meta.relation_shapes …"` subject shape); the Slice-1 deferral stands unchanged. (2) The fifth-guard raise text at 2 literal sites (connection.py:684-687 + relay.py::`_validate_node_target`) — the synthesis's explicit-shape-on-non-Node-target error is a *different* message (it names the relation, the target, and the per-relation remediation; it is not a field-construction guard), so this slice adds no third occurrence there either; the Slice-2 deferral stands unchanged.

### Implementation steps

Line numbers are pin-at-write-time navigational hints. Verify against the current source before editing — another worker's pass may have shifted the file since this plan was written.

1. **`django_strawberry_framework/types/base.py` — constants + key.**
   - Add `RELATION_SHAPE_VALUES: frozenset[str] = frozenset({"list", "connection", "both"})` and `DEFAULT_RELATION_SHAPE = "both"` beside `STRING_GLOBALID_STRATEGIES` (base.py:109-110), with a short comment naming them the single source of the shape vocabulary for the validator, the finalizer synthesis, and the error text (spec-032 Decision 7).
   - `ALLOWED_META_KEYS` (base.py:55-72) grows `"relation_shapes"` (alphabetical: between `"primary"` and `"required_overrides"`). Extend the net-new-key comment block (base.py:73-77) with one clause naming `relation_shapes` (spec-032 Decision 7) — NOT a `DEFERRED_META_KEYS` promotion; `DEFERRED_META_KEYS` unchanged.
   - Add the `_RELAY_NODE_GATE_LEAD` constant and recompose the two existing gate raises (`_validate_connection` base.py:204-208, `_validate_globalid_strategy` base.py:277-281) from it — output byte-identical to today's messages (the pinned tests `test_connection_key_requires_relay_node` and the globalid-strategy gate tests must pass unmodified; that is the refactor's correctness check).
   - Remove the staged anchor block at base.py:79-100.
2. **`types/base.py::_validate_relation_shapes(meta, value, relay_shaped)`** — stage 1, modeled on `_validate_connection`, placed beside it:
   - `value is None` → `None` (absent key: every eligible relation defaults to `"both"` at synthesis — Decision 7).
   - Not a `dict` → `ConfigurationError(f"{meta.model.__name__}.Meta.relation_shapes must be a dict mapping relation field names to shapes; got {value!r}")` (exact wording Worker 2's discretion; must name the model, the key, and the offending value — the `_validate_connection` non-dict shape).
   - Per entry: a non-`str` key, or a value not in `RELATION_SHAPE_VALUES` → `ConfigurationError` naming the offending entry and listing `sorted(RELATION_SHAPE_VALUES)` (the `STRING_GLOBALID_STRATEGIES` typo-guard message shape, base.py:261-263).
   - `not relay_shaped` → `ConfigurationError` with the spec-pinned remediation: `f"{meta.model.__name__}.Meta.relation_shapes {_RELAY_NODE_GATE_LEAD} or remove the key."` (Decision 7 line 388 — note the tail differs from the connection/globalid gates' "or inherit `relay.Node` directly.").
   - Return the normalized `dict(value)`.
   Call it from `_validate_meta` after `_validate_globalid_strategy` (base.py:987-991); add `relation_shapes: dict[str, str] | None` to `_ValidatedMeta` (base.py:879-899) and the return construction (base.py:1014-1026); extend the `_validate_meta` docstring's validation-order list with one numbered line.
3. **`types/base.py::_validate_relation_shape_targets(*, model, relation_shapes, selected_fields, field_map, consumer_authored_fields)`** — stage 2, placed beside `_validate_nullability_override_targets` and called from `__init_subclass__` right after it (base.py:484-491), i.e. after `consumer_authored_fields` (base.py:475-482) and `field_map` (base.py:452) exist. Early-return on `not relation_shapes`. Checks, in the sibling's order, iterating keys `sorted(...)` for deterministic messages:
   - **Unknown** — keys not in `{f.name for f in model._meta.get_fields()}` → `_format_unknown_fields_error(model=model, attr="relation_shapes", unknown=…, available=…)`.
   - **Excluded / not selected** — keys not in `{f.name for f in selected_fields}` → `ConfigurationError` naming the field and that it is excluded from the selected set (the `_validate_nullability_override_targets` excluded-path message shape, base.py:1159-1166, adapted to `Meta.relation_shapes`).
   - **Non-relation** — `selected_by_name[name].is_relation` is False → `ConfigurationError` naming the field and the reason ("names non-relation field … only many-side relations can take a connection shape" — wording at Worker 2's discretion; must name the field and the reason per Decision 7).
   - **Single-valued relation** — `not field_map[snake_case(name)].is_many_side` (forward FK / OneToOne / reverse OneToOne) → `ConfigurationError` naming the field and "there is nothing to paginate" (Decision 7 line 389's stated reason).
   - **Consumer-authored** — `name in consumer_authored_fields` → `ConfigurationError` at type creation explaining that consumer overrides own the field's shape (Decision 7 / Revision 3 P2; Error shapes line 259 places this failure at type creation). The implicit `"both"` default is unaffected — only an explicit key fails loud.
   All five run at type creation per Decision 7 / Error shapes / the staged base.py anchor; ONLY the target-is-Node-shaped check runs at finalization (step 6).
4. **`types/base.py::__init_subclass__`** — thread `relation_shapes=validated.relation_shapes` into the `DjangoTypeDefinition(...)` construction (base.py:527-552), removing the staged anchor at base.py:549-551.
5. **`django_strawberry_framework/types/definition.py`** — replace the anchor (definition.py:125-130) with the slot `relation_shapes: dict[str, str] | None = None` (after `connection`), and add an invariants-docstring bullet mirroring the `connection` bullet (definition.py:64-70): normalized by `_validate_relation_shapes` (+ the stage-2 target validator), populated in `__init_subclass__`, read by the Phase-2.5 synthesis (spec-032 Decision 7).
6. **`django_strawberry_framework/types/finalizer.py`** — replace the anchor (finalizer.py:392-417) with the synthesis call `_synthesize_relation_connections()` at exactly that position: after the no-Node-types check (finalizer.py:385-390), before `_audit_model_label_routing` (finalizer.py:424) — which satisfies Decision 6's "after `install_globalid_typename_resolver`, before Phase 3" AND runs before `_bind_filtersets` / `_bind_ordersets`, so the sidecar-helper registrations made by `_synthesized_signature` are orphan-validated and materialized in the same finalize (the same ordering `DjangoConnectionField`-at-class-body-time enjoys). The new module-level function:
   - Function-local `from ..connection import _build_relation_connection_resolver, _connection_type_for` and `from strawberry.utils.str_converters import to_camel_case` (cycle-safe plain imports — a synthesis step must never be silently skipped, so no try/except, the finalizer.py:375-385 precedent; `from strawberry import relay` may live at module top, Worker 2's call).
   - Loop: `for type_cls, definition in registry.iter_definitions():` — skip `definition.finalized`; skip `not implements_relay_node(type_cls)`. `shapes = definition.relation_shapes or {}`.
   - Per selected field (`definition.selected_fields`, stable Django order): skip non-relations; skip non-many-side (`definition.field_map[snake_case(field.name)].is_many_side` — the spec's reverse-FK / forward- / reverse-M2M set); `name = field.name`.
   - **Consumer-authored relation** → `continue` (implicit-default skip only; an explicit key already raised at type creation — Decision 6 / Edge cases line 527).
   - **Target gate:** `target_type = registry.get(field.related_model)`; if `target_type is None or not implements_relay_node(target_type)`: when `shapes.get(name)` is `"connection"` or `"both"` (an explicit request) → `ConfigurationError` at finalization naming the relation, the declaring type, the non-Node target, and the remediation (make the target Relay-Node-shaped or drop/narrow the entry); else `continue` — silent list-only under the implicit default (Decision 6's degrade-silently / fail-loud-on-explicit split; the live `GenreType.books`-over-non-Node-`BookType` case must keep building).
   - `shape = shapes.get(name, DEFAULT_RELATION_SHAPE)` (import the constant from `.base`); `if shape == "list": continue` (synthesize nothing — today's shipped shape).
   - `generated = f"{name}_connection"`. **Re-entrancy guard** (the module's documented partial-finalize rerun contract, finalizer.py:271-280): if `type_cls.__dict__.get(generated)` carries the synthesis marker (a private attribute, e.g. `_dst_synthesized_relation_connection = True`, stamped on the field object at attach time — name at Worker 2's discretion), `continue` — the field was attached by a prior partial run; without this, a finalize that raises later (e.g. an orphan-sidecar failure in `_bind_filtersets`, which runs after synthesis) would re-enter here and misread our own field as a collision.
   - **Collision guard (Revision 3 P3 — BOTH name surfaces):** build the existing-name set `existing = set(type_cls.__annotations__) | {f.name for f in definition.selected_fields} | {k for k, v in vars(type_cls).items() if isinstance(v, StrawberryField)}` (annotations cover synthesized scalars, resolved relations, and consumer annotations; selected-field names cover the Relay-suppressed pk and any model field outside the annotation dict; the class-dict StrawberryField scan covers consumer `strawberry.field(...)` assignments AND connections synthesized earlier in this same pass). Collision iff `generated in existing` **or** `to_camel_case(generated) in {to_camel_case(n) for n in existing}` (the second check subsumes camel-authored names like `booksConnection`, whose camel form is itself). On collision → `ConfigurationError` naming the declaring type, the generated name, the colliding existing name (when it differs, i.e. the camel-surface case), and the `relation_shapes = {"<field>": "list"}` opt-out (Error shapes line 260). A non-default `name_converter` / `auto_camel_case=False` is unknowable at finalization (the `StrawberryConfig` does not exist yet) and falls through to Strawberry's own schema-build duplicate-field error — record this documented constraint in the function docstring (Decision 6 / Edge cases line 528).
   - **Attach:** `field_obj = relay.connection(_connection_type_for(target_type), resolver=_build_relation_connection_resolver(target_type, name))`; stamp the marker; `setattr(type_cls, generated, field_obj)`. Prefer the annotation-free attach (the `relay.connection`-as-decorator shape: the connection type is passed explicitly as `graphql_type`, so no class annotation is required); the SDL assertion in `test_default_both_synthesizes_connection_sibling` is the proof — if Strawberry 0.316.0 drops an un-annotated class-dict ConnectionField from the type (it should not), fall back to also writing `type_cls.__annotations__[generated] = <connection type>` and note it in the build report.
   - **`shape == "connection"` list suppression:** after attaching the sibling, remove the generated list form — `type_cls.__annotations__.pop(name)` (the Phase-1 `resolved_relation_annotation` entry) and `delattr(type_cls, name)` (the Phase-2 `_attach_relation_resolvers` strawberry.field) — both before Phase 3 freezes the annotation set, so the SDL never carries the list field (Edge cases line 527). On the re-entrancy path the pops must tolerate already-removed entries (`dict.pop(name, None)` / a guarded delattr).
   - **Strictness note in the docstring:** the synthesized resolver consults NO `DST_OPTIMIZER_*` sentinels and derives an empty optimizer plan pre-`033` — wiring strictness/planning into the connection pipeline is `WIP-ALPHA-033-0.0.9`'s scope (Decision 12 / Non-goals; the build plan's standing context flag). Tests assert behavior, never SQL shape.
   - Update the module docstring's Phase-2.5 description (finalizer.py:19-30) with one clause naming the relation-as-Connection synthesis.
7. **`django_strawberry_framework/connection.py`** — replace the anchor (connection.py:610-630) with `_build_relation_connection_resolver(target_type, accessor_name)` per the staged pseudocode: a sync `def _resolve(root, info, **kwargs)` seeding `getattr(root, accessor_name).all()` (prefetch-cache-reachable — the `033` cooperation seam) into `_pipeline_sync(target_type, qs, info, filter_input=kwargs.get("filter"), order_by_input=kwargs.get("order_by"))`, then attaching `_synthesized_signature(target_type)` exactly as `_build_connection_resolver` does (connection.py:604-607). Sync-only by design (the committed-at-construction contract: a lazy queryset works under both `execute_sync` and `await execute`); deliberately NO strictness-sentinel consultation (the anchor's pinned note carries into the docstring). Docstring cites spec-032 Decision 6.
8. **Tests** — `tests/types/test_base.py` (replace the anchor at test_base.py:346-356) and `tests/test_relay_connection.py` (replace the Slice-3 anchor block at lines 11-31; keep the Slice-4 block; rewrite the module docstring to describe the shipped Slice-3 surface while noting the Slice-4 additions stay staged). See Test additions.
9. Run `uv run ruff format .` and `uv run ruff check --fix .`; reconcile `git status --short` per the Worker 2 validation-run contract. Trailing-comma rule: the stage-2 validator's keyword-only signature and the new constants/dicts at ≥4 items carry trailing commas.

### Test additions / updates

All package-internal by spec mandate (Test plan lines 565-576 + the package-only rationale at 535-539: type-creation / finalization `ConfigurationError`s cannot appear in the example schema; the synthesized-relation shape variants need cardinality fixtures the fakeshop graph lacks until Slice 6 promotes `BookType`). These are the "package twins" that keep the per-slice `fail_under = 100` gate green; the live nested-connection proofs land in Slice 6. Run focused tests only with `--no-cov` (coverage flags are forbidden; coverage is the maintainer's gate).

`tests/types/test_base.py` (extend — the Meta-key validation home, beside the other Meta validation):

- `test_meta_relation_shapes_in_allowed_meta_keys` — `"relation_shapes"` in `ALLOWED_META_KEYS`, not in `DEFERRED_META_KEYS` (mirror `test_interfaces_is_shipped_not_deferred`, test_base.py:360-373).
- `test_relation_shapes_validation_matrix` — each raises `ConfigurationError` at type creation (parametrize or split at Worker 2's discretion; suggested fixtures in parens, all real models per AGENTS.md): non-dict (`relation_shapes = "books"`); bad value (`{"items": "paginated"}` — message lists `sorted(RELATION_SHAPE_VALUES)`); unknown field (`{"nope": "both"}` — `_format_unknown_fields_error` shape); non-relation field (`{"name": "both"}` on a Category type); single-valued relation (`{"category": "both"}` on an Item type — forward FK, "nothing to paginate"); excluded field (`{"items": "both"}` with `fields` omitting `items` on a Category type); non-Relay declaring type (no `relay.Node` — assert the gate lead-in + the spec-pinned "or remove the key" tail).
- `test_relation_shapes_on_consumer_authored_relation_raises` — a Relay-Node-shaped Category type with a consumer annotation or `strawberry.field` override on `items` AND `relation_shapes = {"items": "connection"}` → `ConfigurationError` at type creation with the overrides-own-the-shape message (Revision 3 P2; the silent-accept-then-skip path must not exist). Cover both the annotation and the assigned-`strawberry.field` corner, or one of each across two cases.

`tests/test_relay_connection.py` (Slice-3 block — the spec-named eight, lines 569-576, plus plan-added coverage tests). Conventions: an `_isolate_global_registry`-style autouse clearing `registry` + `_connection_type_cache` (copy tests/test_connection.py:59-71); inline `DjangoType` declarations + `finalize_django_types()` + `strawberry.Schema(..., config=strawberry_config())`; catalog-model tests open with `services.seed_data(N)`, library-model tests use inline `Model.objects.create`; SDL assertions via `str(schema)`; execution assertions via `schema.execute_sync` asserting **behavior (rows / pagination / argument presence), never SQL shape** (pre-`033` posture — the synthesized connections derive an empty optimizer plan and strictness does not flag their nested lazy loads):

- `test_default_both_synthesizes_connection_sibling` — Relay-Node-shaped declaring + target types with NO `relation_shapes` key; assert the SDL carries the list field AND the `<field>Connection` sibling for each of the three eligible kinds: reverse FK (`Category.items` over a Node-shaped ItemType), forward M2M (`Book.genres` over a Node-shaped GenreType-like fixture), reverse M2M (`Genre.books` over a Node-shaped BookType-like fixture). One test or three — Worker 2's split, but all three kinds must be pinned (the spec names all three).
- `test_shape_connection_suppresses_list` — `relation_shapes = {"<field>": "connection"}`: SDL carries `<field>Connection`, NOT the `list[T]` field (and a query against the connection resolves rows).
- `test_shape_list_suppresses_connection` — `relation_shapes = {"<field>": "list"}`: SDL carries the list field only; no `<field>Connection`.
- `test_non_node_target_silently_list_only` — implicit default over a non-Node target (e.g. `Book.loans`-like: a Node-shaped declaring type whose many-side target type is registered but not Relay-shaped): builds cleanly, list-only SDL, no error.
- `test_non_node_target_explicit_raises` — the same graph with an explicit `{"<field>": "connection"}` (and the `"both"` variant) → `ConfigurationError` at `finalize_django_types()` naming the relation and the non-Node target.
- `test_consumer_overridden_relation_skipped` — a consumer-authored relation (annotation or assigned field) with NO `relation_shapes` entry is never upgraded under the implicit default: SDL carries the consumer's field shape, no `<field>Connection`.
- `test_generated_name_collision_raises` — a consumer-declared attribute named `<field>_connection` (e.g. annotation `items_connection: str`) on the declaring type → `ConfigurationError` at finalization naming the collision AND the `relation_shapes = {"<field>": "list"}` opt-out text.
- `test_generated_name_graphql_camel_collision_raises` — a consumer-authored camelCase attribute (e.g. an `itemsConnection` annotation / `strawberry.field`) collides with the generated `items_connection` on the default-camel-cased GraphQL surface despite distinct Python names (Revision 3 P3).
- `test_synthesized_connection_carries_sidecar_args_and_total_count` — the TARGET type declares `Meta.filterset_class` / `Meta.orderset_class` / `Meta.connection = {"total_count": True}`; assert the synthesized field's SDL carries `filter:` / `orderBy:` arguments and `totalCount`, and a counter-fixture target WITHOUT the opt-in yields a connection without `totalCount` (the type-level target-driven contract).
- `test_synthesized_connection_runs_target_get_queryset` — the target type's `get_queryset` hides rows; a nested `<field>Connection { edges { node … } }` query returns only visible rows (visibility inside the nested connection).

Plan-added beyond the spec-named list (coverage-forced; flagged for Worker 3 — treat as in-contract):

- `test_synthesized_connection_paginates` (or fold into the default-both test) — `first:`/`after` behavior through the synthesized field, proving the relation-manager-seeded source flows the shipped pipeline end-to-end (rows + `pageInfo`, behavior only).
- `test_synthesis_skips_already_attached_on_refinalize` — the re-entrancy marker path: drive a finalize that raises AFTER synthesis (e.g. an orphan FilterSet referenced via `filter_input_type(...)` but unwired — `_bind_filtersets` subpass 3 runs after the synthesis step), then wire the orphan via a newly declared type and re-call `finalize_django_types()`; the rerun must succeed (no self-collision) and the SDL carries exactly one `<field>Connection`. Required for the marker-skip branch under `fail_under = 100`.
- `test_relation_shapes_unrelated_to_unknown_meta_key_guard` is NOT needed — the existing unknown-key typo-guard tests already pin that surface; do not duplicate.

Temp/scratch tests: none required for development. Worker 3 may probe under `docs/builder/temp-tests/slice-3/` if it wants a throwaway check that a `"connection"`-shaped relation's suppressed list field is also absent from `type_cls.__annotations__` post-finalize (asserted indirectly by the SDL test by construction).

### Implementation discretion items

Assessed and decided as Worker 2's choice — equally valid shapes, no architectural weight:

- Private names: `_synthesize_relation_connections`, the collision-guard local (inline vs `_guard_relation_connection_name`), the marker attribute name, `_RELAY_NODE_GATE_LEAD`'s exact identifier — as long as the marker is private, the guard compares both name surfaces, and the gate lead-in is one literal composed at three sites.
- Whether the stage-2 validator receives `field_map` (preferred: `FieldMeta.is_many_side` is the canonical classifier) or classifies via `utils.relations.relation_kind(field)` directly — both single-source the cardinality rule; pick one and say why in the build report.
- `pytest.parametrize` vs discrete tests for the validation matrix and for the three relation kinds in the default-both proof — all enumerated cases must be visibly pinned either way.
- Exact non-spec-pinned error wordings (non-dict, non-relation, single-valued, collision, explicit-non-Node-target) — must name the model/type, the offending field/entry, and the reason/remediation; the spec-pinned fragments (gate tail "or remove the key", the `"list"` opt-out mention, overrides-own-the-shape) are NOT discretionary.
- The annotation-free attach vs annotation+attach fallback (step 6) — constrained by the SDL proof; document whichever lands in the build report's Implementation notes.

### Notes for Worker 1 (spec reconciliation)

Two intra-spec wording tensions, resolved in-plan (no spec edit during planning — the Slice-1 precedent); consider one-line spec edits at final verification under `### Spec changes made (Worker 1 only)`:

- **Slice-3 checklist line 97** says "Field-name validation (relation-ness, many-side-ness, membership in the selected set) … run at finalization", but Decision 7 (line 389), Error shapes (line 259: "shape, key-name, and consumer-authored failures at type creation; target-shape failures at finalization"), the Test plan (line 567: the matrix raises at type creation), and the staged base.py anchor all place every name-level check at **type creation**, reserving only the target-is-Node-shaped check for finalization. The plan follows the controlling Decision-7 / Error-shapes contract; the checklist sentence should be tightened to match.
- **Decision 7 line 384** says `_validate_relation_shapes(meta, value, relay_shaped)` is "called from `_validate_meta`", while the staged base.py anchor names a four-param form including `consumer_authored_fields` — which cannot exist at `_validate_meta` time (it is computed after `_validate_meta` returns, from `_select_fields`' output). The plan resolves this with the established spec-029 two-stage split (`_validate_relation_shapes` in `_validate_meta` + `_validate_relation_shape_targets` in `__init_subclass__`), preserving every behavioral contract (all five name-level checks at type creation). A clarifying clause in Decision 7 would close the gap.

### Spec slice checklist (verbatim)

- [x] [`django_strawberry_framework/types/base.py::ALLOWED_META_KEYS`][base] grows `"relation_shapes"` (net-new public key — NOT a `DEFERRED_META_KEYS` promotion, the [`spec-031`][spec-031] Decision 6 / [`spec-030`][spec-030] Decision 8 rule). `_validate_relation_shapes` (modeled on `_validate_connection`) accepts a `dict[str, str]` with values in `{"list", "connection", "both"}`, gated to Relay-Node-shaped declaring types; unknown keys / values / shapes — and a key naming a **consumer-authored** relation, whose shape the override owns ([Decision 7](#decision-7--metarelation_shapes-is-a-net-new-allowed_meta_keys-key-stored-on-the-definition), Revision 3) — raise [`ConfigurationError`][glossary-configurationerror]. The normalized value is stored on [`DjangoTypeDefinition`][definition] (a new `relation_shapes` slot).
- [x] [`django_strawberry_framework/types/finalizer.py`][finalizer] Phase 2.5 gains the synthesis step: for every Relay-Node-shaped type, each selected many-side relation (reverse FK, forward / reverse M2M) whose **target** type is also Relay-Node-shaped gets a `<field>_connection` sibling (rendered `<field>Connection` by Strawberry's camel-casing) per the resolved shape — `"both"` (default) keeps the `list[T]` field and adds the connection; `"connection"` suppresses the list field; `"list"` suppresses the connection. The synthesized field reuses the [`spec-030`][spec-030] machinery (`_connection_type_for`, the synthesized sidecar signature, the pipeline tail) with a relation-manager-seeded source. Field-name validation (relation-ness, many-side-ness, membership in the selected set) and the explicit-shape-on-non-Node-target rejection run at finalization, where relation targets are settled.
- [x] Package coverage: `tests/test_relay_connection.py` (new) + [`tests/types/test_base.py`][test-types-base] (key validation) per the [Test plan](#test-plan).

### Static inspection (BUILD.md-mandated)

`uv run python scripts/review_inspect.py <target> --output-dir docs/shadow` was run during this planning pass for `django_strawberry_framework/types/base.py` (1423 lines, under `types/`), `django_strawberry_framework/types/finalizer.py` (1033 lines, under `types/`), `django_strawberry_framework/connection.py` (695 lines, ≥150), and `django_strawberry_framework/types/definition.py` (231 lines, under `types/`). Findings used:

- base.py repeated-literals: the 2x `".Meta.interfaces entry "` prefix (untouched by this slice — the Slice-1 integration deferral stands); the Relay-gate text appears at 2 composed sites today and would reach 3 — hence the `_RELAY_NODE_GATE_LEAD` consolidation in this plan.
- finalizer.py: `finalize_django_types` is already a hotspot; the synthesis lands as a separate module function (`_synthesize_relation_connections`) called once, keeping the orchestrator body one line longer rather than +40 inline lines.
- connection.py: no repeated literal this slice would re-repeat; `_build_relation_connection_resolver` adds one symbol beside `_build_connection_resolver` with no new ORM markers beyond the `getattr(root, accessor).all()` seed.
- Shadow line numbers are not canonical; all line refs in this artifact are original-source pins (allowed in this per-cycle scratchpad).

## Build report (Worker 2)

### Files touched

Grounded in `git status --short`; prior slices' diffs (`__init__.py`, `registry.py`, `relay.py`, `tests/base/test_init.py`, `tests/test_registry.py`, `tests/test_relay_node_field.py`, the spec, the pre-flight `D docs/builder/*.md` entries, bld-slice-1/2 artifacts) left intact.

- `django_strawberry_framework/types/base.py` — `ALLOWED_META_KEYS` grows `"relation_shapes"` (alphabetical, comment block extended); `RELATION_SHAPE_VALUES` / `DEFAULT_RELATION_SHAPE` constants; `_RELAY_NODE_GATE_LEAD` hoisted and the `_validate_connection` / `_validate_globalid_strategy` gate raises recomposed from it (byte-identical output — the pinned `test_connection_key_requires_relay_node` and globalid gate tests pass unmodified); `_validate_relation_shapes` (stage 1, beside `_validate_connection`, called from `_validate_meta` after `_validate_globalid_strategy`; docstring validation-order item 8 added); `_ValidatedMeta.relation_shapes` slot + return construction; `_validate_relation_shape_targets` (stage 2, beside `_validate_nullability_override_targets`, called from `__init_subclass__` right after it); `relation_shapes=validated.relation_shapes` threaded into `DjangoTypeDefinition(...)`. Both Slice-3 anchors removed.
- `django_strawberry_framework/types/definition.py` — `relation_shapes: dict[str, str] | None = None` slot after `connection` (anchor removed) + invariants-docstring bullet mirroring the `connection` bullet.
- `django_strawberry_framework/types/finalizer.py` — `_SYNTHESIZED_RELATION_CONNECTION_MARKER` constant + `_synthesize_relation_connections()` module function (placed before `finalize_django_types`); called at exactly the anchor position (after the no-Node-types check, before `_audit_model_label_routing`, so synthesis precedes `_bind_filtersets` / `_bind_ordersets` and the sidecar registrations from `_synthesized_signature` are orphan-validated in the same finalize); module docstring Phase-2.5 description extended; module-top imports `relay` / `StrawberryField` / `to_camel_case` added (third-party, cycle-free); `..connection` + `.base` reads are function-local plain imports per the plan. Anchor removed.
- `django_strawberry_framework/connection.py` — `_build_relation_connection_resolver(target_type, accessor_name)` replacing the anchor: sync resolver seeding `getattr(root, accessor_name).all()` into `_pipeline_sync`, `_synthesized_signature` attached exactly as `_build_connection_resolver` does; docstring carries the Decision-6 citation, the prefetch-cache/033 seam note, and the strictness-blind pre-033 posture.
- `tests/types/test_base.py` — anchor replaced with the key-validation tests (see below).
- `tests/test_relay_connection.py` — Slice-3 anchor block replaced with the synthesis suite; Slice-4 staged block kept verbatim; module docstring rewritten to describe the shipped Slice-3 surface and note the staged Slice-4 list.

### Tests added or updated

`tests/types/test_base.py`: `test_meta_relation_shapes_in_allowed_meta_keys`; `test_relation_shapes_validation_matrix` (parametrized 8 ways: non-dict, non-str key, bad value listing `sorted(RELATION_SHAPE_VALUES)`, unknown field via the `_format_unknown_fields_error` shape, non-relation field, single-valued relation "nothing to paginate", excluded field, non-Relay declaring type asserting the gate lead-in + the spec-pinned "or remove the key." tail); `test_relation_shapes_on_consumer_annotated_relation_raises` + `test_relation_shapes_on_consumer_assigned_relation_raises` (both override corners, one each). Shared `_declare_relation_shapes_type` local helper.

`tests/test_relay_connection.py` (Slice-3 block): `test_default_both_synthesizes_reverse_fk_connection_sibling` + `test_default_both_synthesizes_m2m_connection_siblings` (all three spec-named kinds pinned: `Category.items` reverse FK, `Book.genres` forward M2M, `Genre.books` reverse M2M; the reverse-FK test doubles as the no-`totalCount` counter-fixture); `test_shape_connection_suppresses_list` (SDL + execution rows); `test_shape_list_suppresses_connection`; `test_non_node_target_silently_list_only` (`Book.loans` over non-Node `LoanType`); `test_non_node_target_explicit_raises` (parametrized `"connection"` / `"both"`); `test_consumer_overridden_relation_skipped`; `test_generated_name_collision_raises`; `test_generated_name_graphql_camel_collision_raises`; `test_synthesized_connection_carries_sidecar_args_and_total_count` (ItemType with both sidecars + `total_count` vs PropertyType with neither, in one schema; `sdl.count("totalCount") == 1`); `test_synthesized_connection_runs_target_get_queryset` (library fixture, title-filtered visibility); `test_synthesized_connection_paginates` (plan-added: `first: 2` + `hasNextPage`, behavior only); `test_synthesis_skips_already_attached_on_refinalize` (plan-added: orphan `filter_input_type` raise after synthesis, wire + rerun, `sdl.count("itemsConnection(") == 1`). Fixtures per AGENTS: `seed_data(N)` first line for catalog tests, inline `Model.objects.create` for library; `_isolate_global_registry` autouse copied from `tests/test_connection.py`.

### Validation run

- `uv run ruff format .` — pass (no reformats beyond my own files at final run).
- `uv run ruff check --fix .` — pass ("All checks passed").
- `git status --short` reconciled — every modified file classified slice-intended or prior-slice (listed above); no tool churn reverted.
- Focused tests (no `--cov*`): `tests/test_relay_connection.py tests/types/test_base.py tests/test_connection.py` — 179 passed. Siblings importing changed surfaces: `tests/types/test_relay_interfaces.py tests/test_registry.py tests/test_relay_node_field.py tests/base/test_init.py tests/types/test_definition_order.py tests/types/test_definition_order_schema.py tests/test_list_field.py` — 278 passed, exit 0.
- Pre-existing-claim verification: the sibling run prints a post-summary `PytestUnraisableExceptionWarning` (sqlite connection GC at interpreter shutdown), exit 0. Verified pre-existing: `git stash push -u -m "slice3-preexist-check"` → same pytest command at the stashed tree → same warning tail printed (exit 0) → `git stash pop`. (The stashed-tree run also failed `tests/test_list_field.py::test_djangolistfield_async_consumer_resolver_python_list_return_passes_through` — expected, since the stash removed the uncommitted Slice-1/2 diffs too; the working-tree run passes it.)

### Implementation notes

- **Stage-1 entry checks split into two messages** (non-str key vs unknown value) rather than the plan's single "offending entry" raise: a `keys must be relation field name strings; got 1` message is clearer than rendering a non-str key through the value-typo message, and the value message mirrors `_validate_globalid_strategy`'s "got unknown strategy ... valid strategies are ..." shape (`got unknown shape {shape!r}; valid shapes are {sorted(RELATION_SHAPE_VALUES)}`). Both raise `ConfigurationError` at type creation; the matrix pins both.
- **Stage-2 cardinality via `field_map[snake_case(name)].is_many_side`** (the plan's preferred option) — `FieldMeta.is_many_side` is the same single-source classifier `types/resolvers.py::_make_relation_resolver` and the synthesis loop key on, so validator and synthesis can never disagree.
- **Marker is stamped on the field object** (`field_obj._dst_synthesized_relation_connection = True` via `setattr` with the module constant) — `relay.connection(graphql_type, resolver=...)` returns a plain `StrawberryField` instance that accepts attribute stamping (verified against installed Strawberry).
- **Annotation-free attach landed** (the plan's preferred shape): `setattr(type_cls, generated, relay.connection(_connection_type_for(target), resolver=...))` with NO `__annotations__` entry — Strawberry picks up the resolver-backed class-dict field and the SDL tests prove it; the fallback annotation write was not needed.
- **Collision guard inlined** (no `_guard_relation_connection_name` helper): one `existing` set + one camel comparison + one raise site reads fine inline; extracting it would pass five locals for no reuse.
- **Collision message** names the declaring type, the generated name, every colliding existing name (sorted, covers the camel-surface case where the names differ), the camel-cased GraphQL surface, and the spec-pinned `relation_shapes = {"<field>": "list"}` opt-out.
- **Explicit-non-Node-target message** reads `{Type}.Meta.relation_shapes[{name!r}] explicitly requests a connection, but the relation's target type {Target} is not Relay-Node-shaped` + add-`relay.Node`-to-the-target / narrow-to-`"list"` / drop remediation. Distinct from the fifth-guard field-construction message, as the plan's integration-watch note requires.
- **`target_type is None` arm of the target gate is defensive**: a selected, non-consumer-authored relation with no registered target raises in Phase 1 (unresolved targets) before synthesis runs, so the arm is unreachable from real flow today; kept per the plan's pinned condition (coverage.py does not branch-track boolean operators, so it costs nothing).
- **Third-party imports (`relay`, `StrawberryField`, `to_camel_case`) live at finalizer module top**; only the cycle-relevant package reads (`..connection`, `.base`) are function-local — the `_node_fields_declared` precedent, plain (no try/except).
- **SDL arg assertions use a `_field_args_block` slicer**, not single-line substring checks: the Relay pagination args carry descriptions, so graphql-core prints connection-field arguments one per line.
- **The synthesis loop's `if definition.finalized: continue` branch** is covered by the existing `tests/test_registry.py` manual-`definition.finalized = True` partial-finalize test (test_registry.py:408), which now flows through the new step.

### Notes for Worker 3

- No shadow files used for implementation (the plan's static-inspection findings were sufficient); all line refs in this report are original-source.
- No temp tests promoted (none existed). The plan's optional throwaway probe (suppressed list absent from `__annotations__` post-finalize) is asserted indirectly by `test_shape_connection_suppresses_list`'s SDL assertion, by construction.
- The two plan-added tests (`test_synthesized_connection_paginates`, `test_synthesis_skips_already_attached_on_refinalize`) are flagged in-contract by the plan ("treat as in-contract").
- The re-finalize test drives the marker-skip branch through the real orphan-FilterSet failure path (orphan raise happens in `_bind_filtersets` subpass 3, which runs after synthesis) — no monkeypatching.
- The byte-identity claim for the two recomposed gate raises is checkable from the diff: lead constant + per-site tails concatenate to exactly the prior literals; the pinned tests passed unmodified.

### Notes for Worker 1 (spec reconciliation)

No new drift beyond the two intra-spec wording tensions the plan already resolved (Slice-3 checklist line 97's "at finalization" wording vs the controlling Decision-7/Error-shapes type-creation contract; Decision 7 line 384's three-vs-four-param `_validate_relation_shapes` signature — implemented as the plan's two-stage split). Small in-plan-discretion deltas worth a glance: the stage-1 non-str-key/unknown-value message split, and the inline (non-helper) collision guard. The Slice-3 checklist box 2 is ticked on the plan's resolved reading; if Worker 1 prefers, the checklist sentence edit lands under `### Spec changes made (Worker 1 only)` at final verification.

---

## Review (Worker 3)

Reviewed the Slice-3 contribution only (cumulative-diff rule): `types/base.py` (relation_shapes additions + `_RELAY_NODE_GATE_LEAD` hoist; the `_RELAY_NON_INTERFACE_HELPERS` block is accepted Slice-1), `types/definition.py`, `types/finalizer.py` (`_synthesize_relation_connections`; the no-Node-types check is accepted Slice-2), `connection.py` (`_build_relation_connection_resolver`), `tests/types/test_base.py` (Slice-3 block), `tests/test_relay_connection.py`. The `__init__.py` / `registry.py` / `relay.py` / `tests/base/test_init.py` / `tests/test_registry.py` / `tests/test_relay_node_field.py` / spec diffs are prior accepted slices and Worker-1-owned spec edits — out of scope, untouched by this slice as the build report claims (verified against the per-file diffs).

### High:

None.

### Medium:

None.

### Low:

#### One-off unreproduced sibling-test failure (tests/types/test_definition_order_schema.py)

`test_relay_declared_type_emits_node_interface_and_global_id` FAILED exactly once in my first run of the build report's sibling set (`tests/types/test_relay_interfaces.py tests/test_registry.py tests/test_relay_node_field.py tests/base/test_init.py tests/types/test_definition_order.py tests/types/test_definition_order_schema.py tests/test_list_field.py --no-cov`: 1 failed, 277 passed). It then passed in isolation and in TEN subsequent identical re-runs (including a back-to-back mimic of my original two-invocation sequence and a 5-seed `PYTHONHASHSEED` probe of the file pair), all 278 passed; the same files also pass at pristine HEAD. The traceback was not captured (my first invocation tailed the summary only). The failing test declares a relation-free Relay type — the Slice-3 synthesis loop body never runs for it — so I have no causal path from this diff to the failure and no reproduction. Recorded transparently rather than suppressed; no source change requested. Worker 1's final test-run gate (full `--no-cov` sweep) is the natural watchpoint: if it recurs there, treat it as a real flake and bisect; if it never recurs, write it off as transient.

#### `_build_relation_connection_resolver` near-copies `_build_connection_resolver`'s sync branch (connection.py:619-633 vs connection.py:570-579, 604-607)

The new resolver body (`_pipeline_sync(target_type, <seed>, info, filter_input=..., order_by_input=...)`) plus the 3-line `_synthesized_signature` attach tail is structurally identical to `_build_connection_resolver`'s sync-consumer branch; a 2-line delegation — `return _build_connection_resolver(target_type, lambda root, info: getattr(root, accessor_name).all())` — would be behaviorally identical. **Assessed and intentionally accepted as-is** (recorded rejection reason): the spec's staged anchor and the plan both pinned the standalone-helper shape; the function's docstring carries a distinct load-bearing contract (the 033 prefetch-cache seam, the accessor-identity pin against `many_resolver`, the strictness-blind posture) that delegation through the "consumer resolver" branch would mislabel semantically (the synthesized resolver is not consumer-authored, and `_is_async_callable` would run pointlessly). Net duplication is ~10 mechanical lines. Logged for the cross-slice integration pass to weigh with fresh eyes (see DRY findings).

### DRY findings

- **Resolved this slice (verified):** the Relay-Node-gate lead-in now composes from one `_RELAY_NODE_GATE_LEAD` constant at three sites (base.py:170-176, `_validate_connection` base.py:201-204, `_validate_globalid_strategy` base.py:320-322, `_validate_relation_shapes` base.py:248-251). Byte-identity of the two pre-existing messages confirmed two ways: programmatic concatenation against the HEAD literals (`git show HEAD:...base.py` lines 206-207 / 279-280) compares `True`, and the pinned tests (`test_connection_key_requires_relay_node`, the globalid gate tests) pass unmodified.
- **Resolved this slice (verified):** `RELATION_SHAPE_VALUES` / `DEFAULT_RELATION_SHAPE` single-source the shape vocabulary — the validator's typo-guard renders `sorted(RELATION_SHAPE_VALUES)`, the finalizer imports `DEFAULT_RELATION_SHAPE` (function-local from `.base`), no re-typed `{"list","connection","both"}` literal anywhere in the diff.
- **Resolved this slice (verified):** zero parallel connection pipeline. The only new runtime code in `connection.py` is the relation-manager seed; `_connection_type_for`, `_synthesized_signature`, `_pipeline_sync` are called as-is. Cardinality is single-sourced through `FieldMeta.is_many_side` at both the stage-2 validator (base.py:1310) and the synthesis loop (finalizer.py:332), so validator and synthesis cannot disagree.
- **Remaining, deferred to integration pass:** (1) the Low finding above (relation-resolver delegation question); (2) the 2x tail literal `` "or inherit `relay.Node` directly." `` left in base.py after the lead hoist (shadow overview confirms 2x) — genuinely per-gate remediation text since the third site's tail differs by spec pin ("or remove the key."), so hoisting it buys nothing today; (3) the standing Slice-1/2 watch items (2x `".Meta.interfaces entry "` prefix — this slice adds NO third occurrence, verified: the relation_shapes messages use the `{Model}.Meta.relation_shapes` subject shape; the two-site fifth-guard wording — the explicit-non-Node-target message is a distinct finalization-time message naming relation + target + per-relation remediation, NOT a third occurrence, verified).
- Minor: `tests/test_relay_connection.py::_make_type` and `tests/types/test_base.py::_declare_relation_shapes_type` are sibling dynamic-`type()` declarers. Different trees, different concerns (one builds usable types, one exists to raise at creation); merging them would couple two test files across directories for ~10 lines. No action.

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` shows exactly the Slice-2 change: `DjangoNodeField` / `DjangoNodesField` imported and added to `__all__` — spec-authorized (Slice-2 checklist box "`DjangoNodeField` and `DjangoNodesField` are exported from `django_strawberry_framework/__init__.py` (the card's DoD names both)") and accepted in the Slice-2 review. **Slice 3 adds no public export**: `RELATION_SHAPE_VALUES` / `DEFAULT_RELATION_SHAPE` / `_validate_relation_shapes` / `_validate_relation_shape_targets` / `_build_relation_connection_resolver` / `_synthesize_relation_connections` are module-level but not re-exported from the package root; the consumer surface is the `Meta.relation_shapes` key only, exactly as Decision 7 specifies.

### CHANGELOG sanity

Not applicable; slice did not modify CHANGELOG.md (the build plan reserves the CHANGELOG grant for Slice 7).

### Documentation / release sanity

Not applicable; slice did not modify docs/release/KANBAN/archive surfaces. (The working-tree `docs/spec-032-*.md` diff is Worker 1's prior-slice status-line and Current-state/NodeID wording edits, recorded in earlier artifacts; this slice did not touch the spec. The `definition.py` / `finalizer.py` docstring updates are code documentation reviewed above.)

### What looks solid

- **The `_RELAY_NODE_GATE_LEAD` hoist is provably byte-identical** — verified programmatically against the HEAD literals, not just by eye, and the pinned tests pass unmodified. This is the refactor's stated correctness check and it holds.
- **The two-stage validation split is the right call** and follows the spec-029 `nullable_overrides` precedent exactly: every name-level check fires at type creation (matrix-pinned 8 ways including the non-str-key corner the plan's single-message shape would have rendered confusingly), only the target-shape check waits for finalization. The intra-spec tension (checklist line 97 vs Decision 7/Error shapes) was escalated, not silently absorbed.
- **The synthesis loop's ordering is exactly right**: after the relay loop (so `implements_relay_node` sees injected bases), before `_audit_model_label_routing` / `_bind_filtersets` / `_bind_ordersets` (so `_synthesized_signature`'s sidecar registrations are orphan-validated in the same finalize — proven by `test_synthesis_skips_already_attached_on_refinalize`, which drives the marker branch through the REAL orphan-raise path with no monkeypatching).
- **The collision guard covers both name surfaces** with one comparison set; `to_camel_case` verified idempotent on already-camel input (`to_camel_case("itemsConnection") == "itemsConnection"`), so the camel-authored corner cannot slip through. The non-default-NameConverter constraint is honestly documented in the docstring rather than papered over.
- **The `"connection"` suppression is tolerant on the re-entrancy path** (`pop(name, None)` + guarded `delattr`) and my temp probe confirmed the suppressed field is gone from BOTH `__annotations__` and the class dict post-finalize, not merely absent from the SDL.
- **Test fixtures follow AGENTS.md placement rules**: catalog tests open with `services.seed_data(N)`, library tests use inline `Model.objects.create`, all package-internal per the spec's Test-plan mandate with the live proofs correctly deferred to Slice 6. All three eligible relation kinds (reverse FK, forward M2M, reverse M2M) are SDL-pinned.
- **Checklist walk: all 3 boxes ticked, all 3 landed, no over-tick.** Box 1 (key + validator + consumer-authored raise + definition slot): in diff. Box 2 (Phase-2.5 synthesis, three shapes, machinery reuse, finalization target-rejection): in diff, with the "field-name validation at finalization" wording divergence pre-recorded as a spec-reconciliation note rather than silently re-read. Box 3 (two test files): in diff.
- Focused runs (no `--cov*`): `tests/test_relay_connection.py tests/types/test_base.py tests/test_connection.py` — 179 passed. Sibling set — 278 passed (after the one-off recorded under Low). Read-only `ruff format --check` / `ruff check` both clean.
- **Pre-existing-claim verification (worker-3.md rule):** Worker 2's claim that the post-summary `PytestUnraisableExceptionWarning` (sqlite GC at shutdown) is pre-existing — re-verified myself via `git stash push -u` → DB-touching suites at pristine HEAD (`tests/test_connection.py` 55 passed, exit 0; `tests/test_registry.py tests/types/test_definition_order_schema.py` 72 passed, exit 0) → `git stash pop` (clean restore, 25 status entries before and after). The warning is an intermittent interpreter-shutdown artifact on both trees, never affects exit status. Claim accepted.

### Temp test verification

- `docs/builder/temp-tests/slice-3/test_review_probe.py` — two probes: (1) the plan's optional check that a `"connection"`-shaped relation's suppressed list field is absent from `type_cls.__annotations__` AND the class dict post-finalize; (2) an explicit `"list"` entry over a non-Node target builds cleanly (the permitted narrowing is legal anywhere, not just over Node targets). Both passed (2 passed).
- Disposition: **deleted** — neither caught a new branch or bug; probe 1's behavior is asserted indirectly by `test_shape_connection_suppresses_list`'s SDL assertion by construction (as the build report predicted), probe 2's path is the same target-gate `continue` the implicit-default test exercises.

### Static inspection (review pass)

`uv run python scripts/review_inspect.py <file> --output-dir docs/shadow` run for all four touched source files: `types/base.py`, `types/finalizer.py`, `connection.py`, `types/definition.py`. No skips. Hotspots walked: `_synthesize_relation_connections` (131 lines / 18 branches — every branch traced to a pinning test or a recorded disposition: the `definition.finalized` skip is exercised by `tests/test_registry.py`'s manual-finalized partial-finalize test as the build report claims, verified by reading test_registry.py:393-414; the defensive `target_type is None` arm is recorded in the build report as unreachable-from-real-flow with the boolean-operator coverage rationale — acceptable), `_validate_relation_shapes` (6 branches, matrix-pinned), `_validate_relation_shape_targets` (7 branches, matrix-pinned). Django/ORM marker tables: no new marker lines in the touched regions of finalizer.py or base.py; connection.py's only new executable ORM surface is the `getattr(root, accessor_name).all()` seed (manager → queryset, lazy, prefetch-cache-reachable — exactly the Decision-6 contract). Repeated-literal tables consulted for the DRY findings above. Shadow line numbers not canonical; all line refs in this section are original-source pins.

### Notes for Worker 1 (spec reconciliation)

- Endorsing both plan-recorded intra-spec tensions for a one-line spec edit each at final verification: (1) Slice-3 checklist line 97's "field-name validation … run at finalization" should be tightened to the controlling Decision-7 / Error-shapes contract (name-level checks at type creation; only target-shape at finalization — what shipped); (2) Decision 7 line 384's three-param `_validate_relation_shapes` vs the anchor's four-param form — a clarifying clause naming the two-stage split would close it.
- The Low one-off `test_relay_declared_type_emits_node_interface_and_global_id` failure: unreproduced over ten re-runs; please watch the final full-sweep gate. No action needed if clean there.
- DRY deferrals for the integration pass: the `_build_relation_connection_resolver` delegation question (Low above, intentionally accepted this slice), the 2x gate-tail literal in base.py, plus the unchanged Slice-1/2 standing items (2x `".Meta.interfaces entry "`; two-site fifth-guard wording; the two unhoisted GraphQL document literals in tests/test_relay_node_field.py).

### Review outcome

`review-accepted` — no High or Medium findings; both Low findings carry recorded dispositions (transparent flake record with a final-gate watchpoint; DRY shape intentionally accepted with reason and an integration-pass deferral). All three checklist boxes verified landed; public-surface, CHANGELOG, and doc-sanity checks performed; temp tests deleted with disposition recorded.

---

## Final verification (Worker 1)

Spec status-line re-verification (worker-1.md per-spawn rule): spec line 5 read "Slices 1–2 implemented … Slices 3–7 not started" — stale at the start of this pass; updated to "Slices 1–3 implemented (final-accepted in the build cycle, uncommitted); Slices 4–7 not started" (recorded under Spec changes below).

- **Spec slice checklist audit: all 3 boxes verified `- [x]`, all 3 landed, no over-ticks.** Box 1: `ALLOWED_META_KEYS` grows `"relation_shapes"` (alphabetical, net-new comment block extended, NOT a deferred promotion — confirmed `DEFERRED_META_KEYS` untouched in the diff); `_validate_relation_shapes` modeled on `_validate_connection` (None short-circuit / dict-shape / per-entry typo guard listing `sorted(RELATION_SHAPE_VALUES)` / Relay-Node gate with the spec-pinned "or remove the key." tail); the field-level rejections (unknown via `_format_unknown_fields_error`, excluded, non-relation, single-valued "nothing to paginate", consumer-authored) land in the stage-2 `_validate_relation_shape_targets` called from `__init_subclass__` after `consumer_authored_fields` exists; the normalized dict threads through `_ValidatedMeta` into the new `DjangoTypeDefinition.relation_shapes` slot (invariants-docstring bullet present). Box 2: `_synthesize_relation_connections()` called at exactly the Decision-6 position (after the no-Node-types check / `install_globalid_typename_resolver`, before `_audit_model_label_routing` and the sidecar binding, so `_synthesized_signature` ledger registrations are orphan-validated in the same finalize — the planning-pass carry-forward ordering check holds); all three shapes (`"both"` default keeps list + adds sibling, `"connection"` suppresses the list annotation AND the Phase-2 resolver with re-entrancy-tolerant removals, `"list"` synthesizes nothing); machinery reused wholesale (`_connection_type_for`, `_synthesized_signature` via `_build_relation_connection_resolver`, `_pipeline_sync`) with the relation-manager seed `getattr(root, accessor_name).all()`; explicit-shape-on-non-Node-target raises at finalization, implicit default degrades silently; both-surface collision guard (`to_camel_case`) with the spec-pinned `"list"` opt-out text; re-entrancy marker. Box 3: `tests/test_relay_connection.py` carries the spec-named eight plus the two plan-added in-contract tests (pagination, re-finalize marker path); `tests/types/test_base.py` carries the key-membership test, the 8-way validation matrix, and both consumer-authored corners. All six staged Slice-3 `TODO(spec-032 … Slice 3)` anchors removed (grep: zero `Slice 3` anchors remain; Slice-4 anchors in `tests/test_relay_connection.py` and other future-slice anchors intact).
- **DRY check across Slices 1–3:** no new cross-slice duplication. The `_RELAY_NODE_GATE_LEAD` hoist is verified in the diff: one constant, three compose sites (`_validate_connection`, `_validate_globalid_strategy`, `_validate_relation_shapes`); the two pre-existing messages recompose byte-identically (lead + per-site tails concatenate to the prior literals; the pinned gate tests pass unmodified) — this RESOLVES the would-be third copy and improves the standing base.py gate-text picture rather than worsening it. Slice 3 adds NO third `".Meta.interfaces entry "` occurrence (relation_shapes messages use the `{Model}.Meta.relation_shapes` subject shape) and NO third fifth-guard occurrence (the explicit-non-Node-target message is a distinct finalization-time message naming relation + target + per-relation remediation) — both Slice-1/2 standing watch items stand unchanged for the integration pass. Shape vocabulary single-sourced (`RELATION_SHAPE_VALUES` / `DEFAULT_RELATION_SHAPE`; no re-typed shape-set literal in the diff); cardinality single-sourced through `FieldMeta.is_many_side` at both the stage-2 validator and the synthesis loop.
- **DRY ruling on Worker 3's accepted-with-reason Low finding (`_build_relation_connection_resolver` near-copies `_build_connection_resolver`'s sync branch + signature tail): DEFER to the integration pass — no Worker 2 consolidation pass now.** Reasons: (1) the duplication is ~10 mechanical lines, behaviorally inert, and both the spec's staged anchor and Decision 6 ("the spec-030 factory pieces verbatim, with one difference: the resolver body seeds … from the parent's relation manager") pin the standalone-helper shape; (2) the 2-line seed-lambda delegation (`_build_connection_resolver(target_type, lambda root, info: getattr(root, accessor_name).all())`) would route the synthesized resolver through the *consumer-resolver* branch — a semantic mislabel (it is framework-synthesized, not consumer-authored), would run `_is_async_callable` pointlessly, and would strand the helper's load-bearing docstring contract (the 033 prefetch-cache seam, the `many_resolver` accessor-identity pin, the strictness-blind posture); (3) `WIP-ALPHA-033-0.0.9` wires strictness/planning into the connection pipeline next — the two builders may genuinely diverge there, so consolidating now risks an immediate un-consolidation; the integration pass (which runs before 033 but with all seven slices' evidence) is the right altitude to weigh it. Recorded as a standing integration-pass item alongside the prior deferrals.
- **Focused tests pass:** `uv run pytest tests/test_relay_connection.py tests/types/test_base.py tests/test_connection.py tests/types/test_definition_order_schema.py --no-cov` — **183 passed, exit 0**. The Worker-3 Low watchpoint `tests/types/test_definition_order_schema.py::test_relay_declared_type_emits_node_interface_and_global_id` **passed** in this run (now unreproduced in 11+ re-runs across two workers); disposition unchanged — watch the final full-sweep gate, write off as transient if clean there. No coverage flags used or inspected.
- **Spec reconciliation:** both plan-recorded intra-spec wording tensions resolved by spec edit to match the as-built (and Decision-7-controlled) contract — see below. Glossary consistency re-ran clean after the edits (`scripts/check_spec_glossary.py` — OK, 38 terms).
- Final status: **`final-accepted`**.

### Summary

Slice 3 ships the relation-as-Connection upgrade: the net-new `Meta.relation_shapes` key (`{"list", "connection", "both"}`, default `"both"`), validated in two stages at type creation (shape + Relay-Node gate in `_validate_meta`; unknown / excluded / non-relation / single-valued / consumer-authored field checks in `__init_subclass__`) and stored on `DjangoTypeDefinition.relation_shapes`; plus the finalizer Phase-2.5 `_synthesize_relation_connections()` step attaching `<field>_connection` siblings for every eligible many-side relation over a Relay-Node-shaped target, reusing the spec-030 connection machinery wholesale with a relation-manager-seeded `_build_relation_connection_resolver`. Explicit requests fail loud (non-Node target at finalization, consumer-authored key at type creation, both-surface name collisions); the implicit default degrades silently to list-only. 25 new tests across `tests/test_relay_connection.py` and `tests/types/test_base.py`; the `_RELAY_NODE_GATE_LEAD` hoist consolidates the Relay-gate text to one constant at three sites byte-identically.

### Spec changes made (Worker 1 only)

All three edits in `docs/spec-032-full_relay-0_0_9.md`, triggered by Slice 3; `scripts/check_spec_glossary.py` exits 0 after the edits:

- **Line 5 (status line):** "Slices 1–2 implemented … Slices 3–7 not started" → "Slices 1–3 implemented (final-accepted in the build cycle, uncommitted); Slices 4–7 not started" — the established per-slice precedent; the prior wording was stale once this pass accepted the slice.
- **Line 97 (Slice-3 checklist, box 2):** "Field-name validation (relation-ness, many-side-ness, membership in the selected set) and the explicit-shape-on-non-Node-target rejection run at finalization …" → field-name validation (now also naming consumer-authored ownership) runs **at type creation** per Decision 7 / Error shapes; **only** the explicit-shape-on-non-Node-target rejection runs at finalization — the checklist sentence contradicted the controlling Decision-7 / Error-shapes (line 259) / Test-plan contract the build followed; tightened to the as-built timing.
- **Line 384 (Decision 7 lead-in):** clarified that `_validate_relation_shapes(meta, value, relay_shaped)` owns the shape checks + Relay-Node gate while the field-level name checks run in the stage-2 `_validate_relation_shape_targets` from `__init_subclass__` (the spec-029 `nullable_overrides` two-stage precedent) — the prior wording implied one `_validate_meta`-called function performing checks that need `consumer_authored_fields`, which does not exist at `_validate_meta` time (the staged anchor's four-param signature was unrunnable as written).
