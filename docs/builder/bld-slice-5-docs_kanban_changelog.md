# Build: Slice 5 — Docs + KANBAN + CHANGELOG

Spec reference: `docs/spec-021-filters-0_0_8.md` (Slice checklist sub-bullets at lines 150-159; canonical doc-edit wording at the `## Doc updates` block, lines 1069-1114; Definition of done items 16-24, lines 1165-1173)

Status: final-accepted

## Plan (Worker 1)

### DRY analysis

Slice 5 is doc-only — every output is a verbatim or near-verbatim copy of text already authored in the active spec. The DRY analysis is therefore reframed around **single-source-of-truth-from-the-spec**, not "named helper extraction":

- **Existing patterns reused (verbatim-copy contracts).**
  - GLOSSARY entry bodies: copy the canonical wording from spec `## Doc updates` `docs/GLOSSARY.md` bullets (spec L1071-1077). The `FilterSet` body wording is at spec L1072 (active-input-only scope, filter-scope-NOT-security-boundary framing, etc.); `RelatedFilter` body wording at L1073; `Meta.filterset_class` body wording at L1074; new `## filter_input_type` entry body wording at L1076 (factory returning `Annotated["<Name>FilterInputType", strawberry.lazy("django_strawberry_framework.filters.inputs")]`).
  - KANBAN past-tense Done card body: copy the spec's pinned past-tense body at L1100-1102 verbatim (entire block from `"Shipped the filtering subsystem..."` through the closing quote — runs ~80 lines of prose in the spec; reflects everything Slices 1-4a actually shipped, including the sync/async split, orphan validation, the canonical `info.context.request` extraction, the active-input-only `check_permissions` scope, the `ErrorDict.get_json_data()` payload shape, the `related_target_for` method, the exactly-14-tests count, and the joint-cut contingency at the trailing sentence).
  - CHANGELOG `### Added` body: copy the spec's pinned `### Added` body at L1109 verbatim (the long single-bullet paragraph beginning `"Filtering subsystem. [\`FilterSet\`]..."`).
  - CHANGELOG `### Changed` body: copy the spec's pinned `### Changed` body at L1112 verbatim (the `Meta.filterset_class is no longer in DEFERRED_META_KEYS...` bullet).
  - GOAL.md replacement migration narrative: copy the spec's pinned replacement wording at spec L155 (`"Your existing django_filters.FilterSet migrates to Meta.filterset_class via a one-line parent-class swap to django_strawberry_framework.filters.FilterSet; the package's FilterSet IS a django_filters.filterset.BaseFilterSet subclass, so every Filter / FilterMethod / form-cleaning primitive you already use carries over unchanged."`).
  - Spec-021 terms CSV row for `filter_input_type`: add `filter_input_type,filter_input_type,The consumer helper this card introduces; GLOSSARY entry created in Slice 5.` per spec L1130's documented fallback wording (the spec body documents the entry as a Slice 5 deliverable; the CSV row lands in the same Slice 5 commit per the spec-020 `strawberry_config` precedent).

- **New helpers justified.** None. Slice 5 is doc-only; no Python code, no shared helpers, no constants. The closest analog to a "helper" is the verbatim-copy contract itself: every text drop into the eight target files should originate from the spec source so a `diff <spec-substring> <target-substring>` after the edit produces a clean match. Worker 2 should NOT paraphrase, NOT abbreviate, and NOT auto-camel-case any of the pinned wordings.

- **Duplication risk avoided.**
  - **Version-string drift across files.** Per spec L159 + Decision 10, the version bump is NOT this card unless this is the last `0.0.8` card to ship. The safe-default planning assumption is "not last," so:
    - `CHANGELOG.md` carries `[Unreleased]` (no `[0.0.8]` heading).
    - `docs/README.md` keeps the filter symbols in "Coming in `0.1.0`" until the joint cut bumps the version. The spec's L1080 wording ("Move ... from 'Coming in `0.1.0`' to 'Shipped today (`0.0.8`)' once the joint cut bumps the version") tells Worker 2 to **stage the move** but execute it only conditionally on the contingency check. For the non-last-card default, Worker 2 should leave the `Coming in 0.1.0` section unchanged; for the contingency path, Worker 2 implements the full move plus the version bump.
    - `README.md` similarly stages filter symbols under the `0.0.8` boundary only if the contingency fires; otherwise no edit to `README.md`'s shipped-symbol list yet. (The spec's L154 wording explicitly ties this to "matching the joint-cut promotion timing.")
    - `pyproject.toml [project].version`, `django_strawberry_framework/__init__.py __version__`, and `tests/base/test_init.py`'s version assertion are NOT touched in the default path. AGENTS.md L31 pins these together — under the contingency they all move together to `0.0.8`.
    - **Worker 1 final-verification owns the contingency decision.** Worker 2 should not infer "this is the last card to ship" from incomplete context; if the maintainer/Worker 0 has not explicitly flagged "this is the last 0.0.8 card to ship," Worker 2 takes the safe-default no-version-bump path. Worker 1 at final verification re-checks the build plan checklist and either (a) confirms the default holds and ratifies the no-bump shape, or (b) records a contingency edit under `### Spec changes made (Worker 1 only)` and routes back to Worker 2 for the bump.
  - **KANBAN past-tense body vs reality drift.** The spec's pinned past-tense body at L1100-1102 was authored at rev6 and amended through rev8; it should already reflect everything Slices 1-4a shipped (sync/async split per rev5 H2, orphan validation per rev5 H5, the canonical `info.context.request` extraction per rev5 M8, active-input-only `check_permissions` per rev5 M2, `ErrorDict.get_json_data()` shape per rev5 M10, `related_target_for` per rev5 H1, "exactly 14 live HTTP tests" per rev8 H4, joint-cut contingency per rev5 L5). Worker 2 copies verbatim; **Worker 1 final-verification is responsible for catching any drift between the spec's pinned past-tense body and the actual shipped behavior** (e.g., Slice 4a was carved mid-build; if a contract subtly diverged from the L1100-1102 wording, that's a Worker-1 reconciliation concern, not a Worker-2 judgment call). Worker 2 should NOT improvise on the past-tense body, even where the worker's own knowledge of the diff suggests a tighter wording.
  - **CHANGELOG-edit-permission compliance.** AGENTS.md L21 says "Do not update CHANGELOG.md unless explicitly instructed." This Slice 5's edit is the explicit permission per spec L158 + DoD item 23 (L1172). Worker 2 must call out the CHANGELOG edit explicitly under `### Files touched` so the permission trail is auditable.
  - **KANBAN out-of-scope bullet at line 923.** The maintainer's in-progress KANBAN diff (per AGENTS.md L33) is a single added bullet at the BACKLOG/positioning section around line 923 ("Add ability to set dsf settings to cap the number of schema hookups per model and error if it is more"), well outside the WIP-021 region (KANBAN L79-194). Worker 2 must NOT revert it; Slice 5's KANBAN edit operates only inside the WIP-021 → Done card-move and the `### In progress` summary paragraph (KANBAN L48-72 + L77 + L79-194), leaving every other line untouched.
  - **Reference-style markdown link discipline.** Per AGENTS.md L28 and START.md "Markdown link convention", every cross-file `.md` link in the standing docs must be reference-style; only AGENTS.md is exempt. Worker 2 must NOT introduce inline `](path)` links when adding the new GLOSSARY / README / docs/README / TODAY / GOAL / TREE / KANBAN / CHANGELOG cross-file references for `FilterSet` / `RelatedFilter` / `filter_input_type` / `Meta.filterset_class`. Existing reference defs already cover the four anchors in `docs/GLOSSARY.md`; new defs for the new `## filter_input_type` anchor (`docs/GLOSSARY.md#filter_input_type`) must be added under the `<!-- docs/ -->` group of each file that newly references it. Group is determined by where the target lives, not where the source lives.
  - **Cross-card double-counting risk.** The `WIP-ALPHA-021-0.0.8` card moves from the In-progress column (KANBAN L79) to the Done column (after the existing `DONE-048-0.0.7` entry at KANBAN L1814). The `### In progress` summary paragraph at L77 names all three WIP-`0.0.8` cards; Slice 5 strikes `WIP-ALPHA-021-0.0.8` from the actively-worked list per spec L1104. The paragraph at L51 in the `### In progress` block also references the card; Worker 2 must reconcile both summary mentions so the card appears only in Done and the In-progress paragraph reflects the remaining two WIP cards (`WIP-ALPHA-022-0.0.8` and `WIP-ALPHA-023-0.0.8`). The card body itself moves wholesale (not retyped) so the Done entry preserves the historical "Spec source / Status: planned" header — but the body is then rewritten to the past-tense form per spec L1100-1102 (the move + body rewrite happen in the same edit).

### Implementation steps

The plan deliberately preserves the spec's `## Doc updates` block (L1069-1114) as the canonical execution checklist. Each step below is keyed to a spec sub-bullet; pin-at-write-time line numbers are navigational hints only — Worker 2 verifies the surrounding context before editing.

**Pre-flight: confirm the joint-cut contingency.** Before any edits, Worker 2 confirms with Worker 0 / the build plan checklist whether this is the last `0.0.8` card to merge. The safe default is "not last." If Worker 0 or the maintainer has not explicitly flagged "last card," proceed with the no-version-bump path (steps below). The contingency path adds three steps at the end (version bump trio + `[Unreleased]` → `[0.0.8]` promotion + `docs/README.md` / `README.md` shipped-symbol move). The two paths are mutually exclusive.

1. **`docs/GLOSSARY.md` — flip statuses, add new entry, update Index, update Browse by category.** Per spec L150-151 + L1071-1077 + DoD item 16 (L1165).
   - At `docs/GLOSSARY.md::FilterSet` (currently at line 421): change `**Status:** planned for `0.0.8`.` to `**Status:** shipped (`0.0.8`).`. Rewrite the entry body per spec L1072 — describe the shipped contract: declarative `Meta.model` / `Meta.fields` (dict or `"__all__"`); `RelatedFilter` for cross-relation traversal; `check_*_permission` denial gates with **active-input-only scope**; explicit-`queryset=` **filter-scope constraint** (NOT a security boundary, with the explicit "visibility / security is `get_queryset`'s job" call-out); logical-and / logical-or / logical-not on the input shape; generated input types with stable class-derived names; cycle-safe lazy resolution via the six-layer pipeline.
   - At `docs/GLOSSARY.md::RelatedFilter` (currently at line 790): change `**Status:** planned for `0.0.8`.` to `**Status:** shipped (`0.0.8`).`. Rewrite body per spec L1073 — describe target acceptance (class / absolute path / unqualified name), the Layer-2 module-fallback resolution, and the `queryset=` **filter-scope constraint** (NOT a security boundary; visibility is `get_queryset`'s job).
   - At `docs/GLOSSARY.md::Meta.filterset_class` (currently at line 577): change `**Status:** planned for `0.0.8`.` to `**Status:** shipped (`0.0.8`).`. Rewrite body per spec L1074 — describe consumer-facing wiring and the promotion-from-`DEFERRED_META_KEYS` gate. Keep the existing code block (`class GalaxyType(DjangoType): class Meta: model = Galaxy; filterset_class = filters.GalaxyFilter`) — it's already correct.
   - **Add new entry** `## filter_input_type` (insert in alphabetical position — after `## FilterSet` at L421 and before `## finalize_django_types` heading currently at the `finalize_django_types` location ~L443). Body per spec L1076: factory returning `Annotated["<Name>FilterInputType", strawberry.lazy("django_strawberry_framework.filters.inputs")]` for resolver-argument annotations; eager validation; consumer usage `filter: filter_input_type(BranchFilter) | None = None`. Include a `**Status:** shipped (`0.0.8`).` line at the top of the entry. Add `**See also:** [`FilterSet`](#filterset)` cross-reference.
   - **Update the Index table** (currently L52-117) for all four entries: change `planned for 0.0.8` → `shipped (0.0.8)` on the `FilterSet` (L72), `Meta.filterset_class` (L85), and `RelatedFilter` (L101) rows; INSERT a new row for `[filter_input_type](#filter_input_type)` keeping alphabetical order (between `FieldSet` and `FilterSet`).
   - **Update the Browse-by-category Filtering bullet** (L126): change `**Filtering:** [`FilterSet`](#filterset) · [`RelatedFilter`](#relatedfilter) · [`Meta.filterset_class`](#metafilterset_class).` to add `· [`filter_input_type`](#filter_input_type)` between `RelatedFilter` and `Meta.filterset_class` (alphabetical).
   - **Do NOT update Public exports.** Per spec L151 + L1077 + Decision 2: filter symbols live at `django_strawberry_framework.filters` and the Public exports section enumerates top-level `django_strawberry_framework` re-exports only. The Filtering-category Browse-by-category listing is the right grain.

2. **`docs/spec-021-filters-0_0_8-terms.csv` — add the `filter_input_type` row.** Per spec L1130's "preferred answer" / DoD item 17 (L1166): the CSV row lands in the same Slice 5 commit as the GLOSSARY entry. Append a 33rd row `filter_input_type,filter_input_type,The consumer helper this card introduces.` and verify `uv run python scripts/check_spec_glossary.py --spec docs/spec-021-filters-0_0_8.md` reports `OK: 33 terms` after the GLOSSARY entry lands. **Order of operations:** add the GLOSSARY entry FIRST (step 1), then the CSV row, so the checker run between the two doesn't transiently fail.

3. **`docs/README.md` — flag the filter symbols for joint-cut promotion (default path) OR move them (contingency path).** Per spec L152 + L1079-1081 + DoD item 19 (L1168).
   - **Default path (NOT last card):** leave the existing "Coming in `0.1.0`" filter-symbols line at `docs/README.md:111` (`- filters, orders, and permissions / cascade permissions`) unchanged. The promotion is staged but does not execute in this commit.
   - **Contingency path (last card to ship — Worker 1 final-verification confirms):** move `FilterSet` / `Meta.filterset_class` / `RelatedFilter` (and `filter_input_type` per spec L152 + M2 of feedback rev2) from the "Coming in `0.1.0`" block (L109-115) to the "Shipped today" block (L90-106). Adjust the version label in the "Shipped today" heading from `(0.0.7)` to `(0.0.8)`.
   - **Optional small Quick start example** (spec L1081, marked "Optional"): defer. Worker 2 does NOT add the example unless explicitly directed.

4. **`docs/TREE.md` — flip the `filters/` subpackage from `[alpha]` to on-disk.** Per spec L153 + L1083-1086 + DoD item 18 (L1167).
   - At `docs/TREE.md:269-275` (the `[alpha] Filtering subsystem` block inside "target package layout"): KEEP only as a deprecated forward-looking reference (or remove if the same listing is being moved up). Per spec L1084 "Move from the 'target package layout' section to the 'current on-disk layout' section."
   - Insert the new on-disk entry into the "current on-disk layout" block (currently L188-230) immediately after the `optimizer/` block (L217-224). Format mirrors the existing `types/` and `optimizer/` blocks:
     ```text
     ├── filters/                 # Filtering subsystem (Layer 3 read-side) — shipped 0.0.8
     │   ├── __init__.py          # FilterSet, RelatedFilter, filter_input_type re-exports
     │   ├── base.py              # Filter / TypedFilter / Array/Range/List filters / GlobalIDFilter / RelatedFilter / LazyRelatedClassMixin
     │   ├── sets.py              # FilterSet + FilterSetMetaclass + apply_sync/apply_async + filter_queryset tree-form override
     │   ├── factories.py         # FilterArgumentsFactory BFS + dynamic-filterset cache
     │   └── inputs.py            # input-class module-globals namespace + LOOKUP_NAME_MAP / LOOKUP_PREFIXES / convert_filter_to_input_annotation / normalize_input_value
     ```
   - Update the surrounding prose at `docs/TREE.md:190` ("The shared infrastructure plus model/type and optimizer subpackages are on disk") to add `, filters` to the on-disk list.
   - At the "Test layout going forward" `tests/filters/` block (L437+): list the on-disk five files per spec L1085 — `__init__.py`, `test_base.py`, `test_sets.py`, `test_factories.py`, `test_inputs.py`, and `test_finalizer.py`. Verify each file actually exists in the working tree before listing it (Worker 2 sanity-checks via `ls tests/filters/`). If a planned file does not exist on disk (e.g., `test_finalizer.py` may have been folded into `test_sets.py` or the orphan-validation tests landed in `tests/types/`), list only the on-disk files and record the deviation under `### Implementation notes` in the build report.
   - Remove the now-redundant `[alpha]` filters entry from the "target package layout" block (per spec L1084's "move from"). If the target layout block names other still-unshipped Layer-3 subpackages (`orders/`, `aggregates/`, etc.), those stay.

5. **`README.md` — add filter symbols to shipped list (contingency path only).** Per spec L154 + L1088-1089 + DoD item 20 (L1169).
   - **Default path:** no edit. `README.md`'s "Coming from DRF + django-filter?" paragraph at L41 already mentions `filterset_class`; no shipped-symbol bullet list exists in the current `README.md` (the "Status" paragraph at L45 names `DjangoListField` as the newest shipped surface). The spec's "shipped-symbol bullet list under the `0.0.8` boundary" wording presumes the joint-cut promotion has fired.
   - **Contingency path:** add a new "newest shipped surface" sentence to the Status paragraph at L45 naming `FilterSet` / `RelatedFilter` / `filter_input_type` / `Meta.filterset_class` and the `0.0.8` boundary. Update `**`0.0.7`, single-maintainer, alpha-quality.**` → `**`0.0.8`, ...**` only on the contingency path.

6. **`GOAL.md` — rewrite the "Coming from DRF + django-filter" migration narrative.** Per spec L155 + L1091-1093 + the canonical replacement wording in spec L155 (and L1093). This step runs **on both paths** — it is unconditional on the version bump.
   - At `GOAL.md:448-450` (section heading `### Coming from DRF + django-filter` followed by the "plugs in directly" paragraph): rewrite the lead paragraph from `"Your FilterSets stay where they are. The DRF Serializer becomes the basis for the auto-generated mutation Input type via Meta.serializer_class. The existing django_filters.FilterSet plugs into Meta.filterset_class directly:"` to the replacement wording pinned at spec L155 + L1093: `"Your existing django_filters.FilterSet migrates to Meta.filterset_class via a one-line parent-class swap to django_strawberry_framework.filters.FilterSet; the package's FilterSet IS a django_filters.filterset.BaseFilterSet subclass, so every Filter / FilterMethod / form-cleaning primitive you already use carries over unchanged."` Preserve the DRF Serializer sentence (`"The DRF Serializer becomes the basis for the auto-generated mutation Input type via Meta.serializer_class."`) — that sentence is orthogonal to the filter migration narrative and stays.
   - At `GOAL.md:452-471` (the code block immediately after): change the parent class of `CategoryFilter` from `django_filters.FilterSet` to `FilterSet` (the package class). Per spec L1093: "Update the diff block in the same section so the example shows the parent-class swap." Concretely: `class CategoryFilter(django_filters.FilterSet):` → `class CategoryFilter(FilterSet):`, and add `from django_strawberry_framework.filters import FilterSet` to the imports inside the example block (or position it visibly above). Update the inline comment `# Your existing django-filter FilterSet — no changes:` to reflect the one-line swap (e.g., `# Your existing django-filter FilterSet — swap the parent class:`).
   - Verify the GOAL.md astronomy-showcase section (L13-200) already references `filters.GalaxyFilter` / `Meta.filterset_class` shape — per spec L1092 "no edit needed there." Worker 2 confirms by `grep -n "GalaxyFilter\|Meta.filterset_class" GOAL.md` and leaves that section untouched.

7. **`TODAY.md` — extend Shipped capabilities + fakeshop section.** Per spec L156 + L1095-1097 + DoD item 21 (L1170).
   - Today the products-catalog section at L17-25 explicitly lists `Meta.filterset_class` under "unshipped APIs and features." That bullet stays — it tracks the *products app's* unshipped dependencies, not the package-wide status. The package-wide shipped/planned status is in GLOSSARY.md.
   - The fakeshop "What's in `examples/fakeshop/apps/library/schema.py` today" paragraph at L11 already enumerates the shipped surfaces exercised by the library app (forward FK, reverse FK, etc.). Append a new clause naming the new live filter declarations and the 14 live HTTP tests: `; the new BranchFilter / ShelfFilter / BookFilter / LoanFilter / PatronFilter declarations at examples/fakeshop/apps/library/filters.py (plus GenreFilter at filters_genre.py for the cross-module absolute-import-path case) and the 14 live /graphql/ HTTP filter tests at examples/fakeshop/test_query/test_library_api.py exercise the FilterSet / RelatedFilter / Meta.filterset_class wiring end-to-end through finalizer phase 2.5.`
   - If `TODAY.md` has a "Shipped capabilities" enumeration distinct from the fakeshop paragraphs, extend it with `FilterSet`, `Meta.filterset_class`, `RelatedFilter` per spec L1096. (Per the current `TODAY.md` read: such an enumeration does not exist as a standalone block — the closest analog is the "Shipped capabilities available but not currently demonstrated in fakeshop" block at L266+, which lists `Meta.primary` and scalar-override semantics. Adding the three filter symbols there is wrong — they ARE demonstrated in fakeshop. Worker 2 instead adds the fakeshop-section sentence above; if Worker 2 reads a "Shipped capabilities" section the planner didn't see, append the three symbols there per spec L1096. Discretionary — see `### Implementation discretion items`.)

8. **`KANBAN.md` — move `WIP-ALPHA-021-0.0.8` to Done.** Per spec L157 + L1099-1104 + DoD item 22 (L1171).
   - **Determine the next `DONE-NNN-0.0.8` id.** The highest DONE-NNN currently shipped is `DONE-048-0.0.7` (per KANBAN.md L1814). Card numbering is per the `<NNN>` segment of the card ID format (KANBAN.md L7-12): NNN is a 3-digit zero-padded counter shared across the card lifecycle. `WIP-ALPHA-021-0.0.8` retains its `021` segment on the Done move; the new id is **`DONE-021-0.0.8`** (the `WIP-ALPHA-` prefix is dropped per "DONE cards use the bare DONE-<NNN>-<X.X.X> form" in KANBAN.md L7-12 and AGENTS.md cross-reference). Worker 2 verifies no prior `DONE-021-*` entry exists (no `DONE-020-*` entry either; the `0.0.8` cohort numbering picks up after the `0.0.7` cohort's `046`/`047`/`048`).
   - **Cut + paste the card section.** Move the entire `### WIP-ALPHA-021-0.0.8 — Filtering subsystem` block (KANBAN.md L79-194) to the Done column, immediately after the last existing Done entry (`### DONE-048-0.0.7 — Scalar conversion end-to-end coverage in the fakeshop example` at L1814 — the new entry inserts after L1814's content block). Rename the heading to `### DONE-021-0.0.8 — Filtering subsystem`.
   - **Rewrite the body to the past-tense form per spec L1100-1102.** Drop the existing in-progress structure (`Priority` / `Severity` / `Status: planned` / `Scope` / `Verified upstream filter primitives` / etc. — these are pre-implementation scaffolding). Replace with: (a) one-line `Spec: docs/spec-021-filters-0_0_8.md` reference; (b) the verbatim past-tense paragraph from spec L1101 (the ~80-line `"Shipped the filtering subsystem..."` block, INCLUDING the trailing joint-cut contingency sentence at the end); (c) the spec L1103 `Definition of done` bullet 1 rewrite (`docs/spec-filters.md` → `docs/SPECS/spec-021-filters-0_0_8.md` — but the spec's L1103 wording is anchored on "after the Step-8 archive pass," which is NEXT-spec-author work, not Slice 5 work; the spec STAYS at `docs/spec-021-filters-0_0_8.md` per AGENTS.md L26 and BUILD.md "Spec stays at its working location." Worker 2 uses the working location `docs/spec-021-filters-0_0_8.md` in the past-tense body's spec reference, NOT the future archived path).
   - **Strip `WIP-ALPHA-021-0.0.8` from the `### In progress` summary paragraph.** Per spec L1104:
     - At KANBAN.md L51 (the in-progress narrative): rewrite to reflect `WIP-ALPHA-021-0.0.8` has shipped (`DONE-021-0.0.8`) and the actively-worked card is now (most likely) `WIP-ALPHA-022-0.0.8`. Concretely: `"WIP-ALPHA-021-0.0.8 (Filtering subsystem) is being actively worked; WIP-ALPHA-022-0.0.8 (Ordering subsystem) and WIP-ALPHA-023-0.0.8 (`DjangoType` consumer-DX cleanup pass) are queued..."` → `"WIP-ALPHA-022-0.0.8 (Ordering subsystem) is queued for next implementation alongside WIP-ALPHA-023-0.0.8 (`DjangoType` consumer-DX cleanup pass); WIP-ALPHA-021-0.0.8 (Filtering subsystem) shipped as DONE-021-0.0.8 on YYYY-MM-DD."` Worker 2 fills `YYYY-MM-DD` with the date the maintainer commits, OR leaves it as `YYYY-MM-DD` for the maintainer to substitute at commit time — Worker 1 final-verification confirms the choice.
     - At KANBAN.md L77 (the second narrative paragraph in the `## In progress` column header): apply the parallel rewrite — name only `WIP-ALPHA-022-0.0.8` and `WIP-ALPHA-023-0.0.8` as remaining, with a one-sentence trailing note that `WIP-ALPHA-021-0.0.8` has shipped as `DONE-021-0.0.8`.
   - **Do NOT touch the maintainer's in-progress KANBAN bullet at line 923** (`Add ability to set dsf settings to cap the number of schema hookups per model and error if it is more`). This is a separate, out-of-scope edit per AGENTS.md L33 and is already accounted for in the build plan's preamble. Worker 2 confirms post-edit via `git diff KANBAN.md` that line 923's bullet survives the Slice 5 KANBAN diff.

9. **`CHANGELOG.md` — append `### Added` and `### Changed` bullets to `[Unreleased]`.** Per spec L158 + L1106-1114 + DoD item 23 (L1172). **The CHANGELOG-edit permission for this card comes from this DoD item per AGENTS.md L21; Worker 2 records the permission citation in `### Files touched`.**
   - At `CHANGELOG.md:19` (`## [Unreleased]`): create a `### Added` subsection (per spec L1107 "creating the subsection if absent") and append the verbatim bullet pinned at spec L1109. The bullet is one ~30-line paragraph describing the filtering-subsystem ship — copy it character-for-character from the spec.
   - In the same `[Unreleased]` block, create a `### Changed` subsection if absent and append the verbatim bullet pinned at spec L1112: `"[Meta.filterset_class] is no longer in DEFERRED_META_KEYS; declaring Meta.filterset_class = MyFilter now wires through to finalizer phase 2.5 and surfaces a working filter input on the GraphQL type. Consumers who declared the key against 0.0.7 saw a ConfigurationError; against 0.0.8 it produces a filter surface."`
   - Reference-style link defs: confirm `[glossary-filterset]` / `[glossary-relatedfilter]` / `[glossary-metafilterset_class]` / `[glossary-configurationerror]` reference defs already exist in `CHANGELOG.md`'s `<!-- LINK DEFINITIONS -->` block; if not, add them under the `<!-- docs/ -->` group alphabetically.
   - **Do NOT promote `[Unreleased]` → `[0.0.8]` on the default path.** Per spec L1114 + Decision 10: the last `0.0.8` card to ship promotes; Worker 2 leaves `[Unreleased]` as the heading.
   - **Contingency path (last card to ship):** promote `[Unreleased]` → `[0.0.8] - YYYY-MM-DD` and bump `pyproject.toml [project].version`, `django_strawberry_framework/__init__.py __version__`, and `tests/base/test_init.py`'s version assertion atomically (AGENTS.md L31). Worker 1 final-verification owns the contingency-path go/no-go and the explicit instruction to take this path.

10. **Verify the verbatim-copy contract via `diff`.** Worker 2 sanity-checks the eight verbatim-copy drops (the four GLOSSARY entry bodies + filter_input_type new entry, the KANBAN past-tense body, the CHANGELOG Added bullet, the CHANGELOG Changed bullet, the GOAL.md replacement narrative) by extracting the relevant substring from the spec and comparing to the substring landed in the target file. Worker 3's review template `### Documentation / release sanity` check (per BUILD.md L304-315) runs the same `diff` — Worker 2 fixing any drift at the build-pass boundary saves a re-review.

### Test additions / updates

**Slice 5 is doc-only. No new test code is added; no existing test code is modified.** Per spec L160-161 and DoD items 11-15 + 26: tests landed under Slices 1-4a (live HTTP coverage at `examples/fakeshop/test_query/test_library_api.py`, package-internal tests under `tests/filters/` + `tests/types/test_base.py` + `tests/types/test_definition_order.py` + `tests/types/test_definition_relations.py`); Slice 6 holds the cross-card composition smoke test until `WIP-ALPHA-022-0.0.8` ships.

The only test-adjacent check Slice 5 may run is the spec-glossary-checker per DoD item 17 (L1166): `uv run python scripts/check_spec_glossary.py --spec docs/spec-021-filters-0_0_8.md`. Worker 2 runs this once after the GLOSSARY edit + CSV row land (step 1 + step 2) and confirms `OK: 33 terms`. **This is the only command Worker 2 runs in Slice 5.**

Per AGENTS.md L14 and BUILD.md "Coverage is the maintainer's gate, not a worker's tool": Worker 2 does NOT run `pytest`. Worker 1 final-verification does NOT run `pytest` either; the focused-test sweep is N/A for a doc-only slice.

Per START.md "Past mistakes to not repeat" + AGENTS.md L15: Worker 2 runs `uv run ruff format .` and `uv run ruff check --fix .` after the edits, classifies the resulting `git status --short` output (slice-intended vs unrelated tool churn), and reverts unrelated churn before setting `Status: built`. Doc-only edits should not trigger ruff churn on non-`.md` files; if they do, that's tool-induced drift Worker 2 owns at the build-pass boundary.

### Implementation discretion items

- **`TODAY.md` "Shipped capabilities" enumeration vs fakeshop-section extension.** The current `TODAY.md` has no standalone "Shipped capabilities" enumeration matching the spec's L1096 wording. The spec L1096 says "Extend the Shipped capabilities enumeration with FilterSet / Meta.filterset_class / RelatedFilter" but the file's actual structure is fakeshop-narrative-focused. Two equally-valid shapes:
  - (a) Extend the existing fakeshop library paragraph at TODAY.md:11 (the planner's recommendation per step 7); a future TODAY.md rewrite can carve a standalone "Shipped capabilities" block when one exists. (b) Add a new explicit "Shipped capabilities" subsection naming the three symbols. Worker 2 picks (a) by default; if the maintainer prefers (b) at review time, the change is a small section-insertion.
- **`YYYY-MM-DD` placeholder in KANBAN ship-date.** Worker 2 may either (a) substitute today's date at edit time, or (b) leave `YYYY-MM-DD` for the maintainer to replace at commit time. Either is fine; pick the shape that reads most consistently with prior Done-card ship-date conventions in KANBAN.md.
- **CHANGELOG `### Added` bullet leading text.** The spec L1109 bullet begins `"Filtering subsystem. [\`FilterSet\`][glossary-filterset] ..."`. Worker 2 may prepend a lead bolded keyword (e.g., `**Filtering subsystem** — ...`) for visual consistency with the existing `0.0.7` `### Added` entries' style if doing so matches the prior changelog rhythm. The verbatim-copy contract still binds the body text; only the lead-marker formatting is at Worker 2's discretion.

### Spec slice checklist (verbatim)

The following sub-bullets are copied verbatim from `docs/spec-021-filters-0_0_8.md` lines 150-159 (the Slice 5 block in the `## Slice checklist`). Every box stays `- [ ]` during the planning pass; Worker 1 ticks each `- [x]` during final verification as the contract lands.

- [x] Slice 5: Docs + KANBAN + CHANGELOG
  - [x] [`docs/GLOSSARY.md`][glossary]: flip [`FilterSet`][glossary-filterset], [`RelatedFilter`][glossary-relatedfilter], and [`Meta.filterset_class`][glossary-metafilterset_class] from `planned for 0.0.8` to `shipped (0.0.8)`. Add a new entry `## filter_input_type` documenting the consumer helper from [Decision 11](#decision-11--filter_input_typefilterset-consumer-helper) — returns `Annotated["<Name>FilterInputType", strawberry.lazy("django_strawberry_framework.filters.inputs")]` for resolver-argument annotations. Update the [Index][glossary-index] table with rows for all four entries. Document `FilterSet` / `RelatedFilter` / `filter_input_type` under the Filtering category of the [Browse by category][glossary] block; the [Public exports][glossary-public-exports] section is NOT updated here because it lists top-level `django_strawberry_framework` re-exports, and per [Decision 2](#decision-2--subpackage-layout-and-public-export-surface) the filter symbols live at `django_strawberry_framework.filters`.
  - [x] [`docs/README.md`][docs-readme]: add `FilterSet` / `RelatedFilter` / `filter_input_type` / `Meta.filterset_class` to the "Coming in `0.1.0`" → graduate to "Shipped today (`0.0.8`)" once the version bump lands in the joint cut (per M2 of [`docs/feedback.md`][feedback]: `filter_input_type` is the consumer-facing resolver helper from [Decision 11](#decision-11--filter_input_typefilterset-consumer-helper) and was missing from the rev2 sweep).
  - [x] [`docs/TREE.md`][tree]: flip the `filters/` subpackage entry from `[alpha]` to on-disk (move from the "target package layout" section to the "current on-disk layout" section). List the five new files (`__init__.py`, `base.py`, `sets.py`, `factories.py`, `inputs.py`); list the mirrored `tests/filters/` tree.
  - [x] [`README.md`][readme]: add `FilterSet` / `RelatedFilter` / `filter_input_type` to the shipped-symbol bullet list (under the `0.0.8` boundary), matching the joint-cut promotion timing (per M2 of [`docs/feedback.md`][feedback]).
  - [x] [`GOAL.md`][goal]: the astronomy showcase already references `filters.py` and `Meta.filterset_class`. AND rewrite the "Coming from DRF + `django-filter`" migration narrative (currently anchored at [`GOAL.md #"The existing django_filters.FilterSet plugs into Meta.filterset_class directly"`][goal-migration-shape]) — corrected per M1 of [`docs/feedback.md`][feedback]. The literal "plugs in directly" wording is misleading because the spec's validator at `_validate_meta` rejects a plain `django_filters.FilterSet` and the consumer migration is a one-line parent-class swap (`class CategoryFilter(FilterSet):` instead of `class CategoryFilter(django_filters.FilterSet):`). Replacement wording: "Your existing `django_filters.FilterSet` migrates to `Meta.filterset_class` via a one-line parent-class swap to `django_strawberry_framework.filters.FilterSet`; the package's `FilterSet` IS a `django_filters.filterset.BaseFilterSet` subclass, so every `Filter` / `FilterMethod` / form-cleaning primitive you already use carries over unchanged." Update the diff block in the same section so the `class CategoryFilter(django_filters.FilterSet):` line shows the parent-class swap rather than implying direct reuse.
  - [x] [`TODAY.md`][today]: extend the "Shipped capabilities" enumeration with `FilterSet` / `Meta.filterset_class` / `RelatedFilter`; extend the fakeshop section to mention the new live `BranchFilter` / `BookFilter` / `LoanFilter` / `PatronFilter` declarations and the filter-input live HTTP tests under [`examples/fakeshop/test_query/test_library_api.py`][fakeshop-test-library].
  - [x] [`KANBAN.md`][kanban]: move `WIP-ALPHA-021-0.0.8` to the Done column with the next available `DONE-NNN-0.0.8` id (renumber per the column-move pass). Past-tense Done body pinned in [Doc updates](#doc-updates). Rewrite the card body's `Definition of done` bullet 1 (`docs/spec-filters.md` → `docs/SPECS/spec-021-filters-0_0_8.md` after the Step-8 archive pass per [Decision 1](#decision-1--spec-filename-and-canonical-naming)).
  - [x] [`CHANGELOG.md`][changelog]: append `### Added` bullets to `[Unreleased]` for `FilterSet`, `RelatedFilter`, `Meta.filterset_class`, the per-module input-class namespace, the `FILTER_DEFAULTS` map, and the cross-relation lazy-resolution surface. Append a `### Changed` bullet noting that `Meta.filterset_class` is no longer in `DEFERRED_META_KEYS`. Per the CHANGELOG-edit-permission rule at [`AGENTS.md`][agents] #"Do not update CHANGELOG.md unless explicitly instructed", this Slice-5 bullet is the explicit permission for this card.
  - [x] Version bump: NOT in this card per [Decision 10](#decision-10--joint-008-cut). `[Unreleased]` entries accumulate against an unbumped `__version__ = "0.0.7"` until the last card in the `0.0.8` cohort owns the bump to `0.0.8`.

### Static inspection helper

`scripts/review_inspect.py` is N/A for Slice 5. Per BUILD.md "Static inspection helper: When to run the helper during build": the helper applies to `.py` files only and to specific size/path thresholds. Slice 5 touches eight `.md` files and one `.csv` file — zero `.py` files. **Recorded skip with reason:** doc-only slice, no Python source modifications, no logic to inspect.

### Cross-slice carry-forwards still alive (planner notes for final verification)

- **Cross-slice DRY targets staged for the integration pass.** Per worker-1 memory: `_LOGIC_KEYS` byte-for-byte duplicated between `sets.py:53-57` + `inputs.py:108-112`; `f"{Name}InputType"` literal at multiple sites across `__init__.py` / `factories.py` / `inputs.py`. Both belong in `bld-integration.md`, not Slice 5.
- **Slice 4a deferrals.** Two reconciliation notes from Slice 4a final-verification (nested-form-validation skip in `_q_for_branch`; tree-form-with-related-branch silent downgrade through `_normalize_input`) belong in `bld-final.md`'s `### Deferred work catalog` per the carry-forward note in worker-1.md.
- **Spec L1030 test-name precision** — maintainer follow-up; surfaces in `bld-final.md`'s deferred-work catalog.
- **Joint-cut contingency** — Worker 1 final-verification confirms the safe-default vs contingency choice and either ratifies the no-version-bump shape or routes back to Worker 2 for the bump trio + `[Unreleased]` → `[0.0.8]` promotion + `docs/README.md` / `README.md` shipped-symbol moves.
- **Past-tense KANBAN body reconciliation.** Worker 1 final-verification re-reads spec L1100-1102's past-tense body against the actual Slice 1-4a shipped behavior. If a contract subtly diverged (e.g., a helper rename, a tighter wording, the Slice 4a carve), Worker 1 makes the spec edit and the KANBAN edit in lockstep, recording both under `### Spec changes made (Worker 1 only)`. Worker 2 does NOT improvise on the past-tense body at build time.

---

## Build report (Worker 2)

### Files touched

- `docs/GLOSSARY.md` — flipped `FilterSet`, `RelatedFilter`, `Meta.filterset_class` entry-body statuses from `planned for 0.0.8` to `shipped (0.0.8)` and rewrote each body per spec L1072–L1074 (active-input-only scope, filter-scope-NOT-security-boundary framing, sync/async classmethod pair, child-visibility-queryset derivation, `from graphql import GraphQLError` payload shape, Layer-2 module-fallback resolution wording, `DEFERRED_META_KEYS` promotion gate, finalizer-phase-2.5 wiring). Inserted new `## filter_input_type` entry alphabetically between `## FilterSet` and `## finalize_django_types` per spec L1076 (factory-returns-`Annotated[...]` shape, eager `TypeError` validation, `_helper_referenced_filtersets` orphan-tracking note, consumer-usage code block on `@strawberry.field`-decorated resolver). Updated Index table: flipped the three existing rows' status columns AND inserted a new `[filter_input_type](#filter_input_type)` row between `FieldSet` and `FilterSet` (alphabetical). Updated Browse-by-category Filtering bullet to add `· [filter_input_type](#filter_input_type)` after `RelatedFilter` (alphabetical). **Did NOT update Public exports** — per Decision 2 the filter symbols live at `django_strawberry_framework.filters`, not the top-level package's `__all__`.
- `docs/spec-021-filters-0_0_8-terms.csv` — appended a 33rd row `filter_input_type,filter_input_type,The consumer helper this card introduces; GLOSSARY entry created in Slice 5.` per spec L1130's documented fallback wording. Pre-existing `</content>\n</invoke>` trailing bytes (present in the committed CSV from rev6) left untouched per AGENTS.md L33's "do not auto-revert unrelated edits" rule.
- `docs/README.md` — **not touched.** Default joint-cut path per spec L159 + the planning document — Worker 0 has not signaled "this is the last 0.0.8 card to ship," so the existing "Coming in `0.1.0`" filter-symbols line at `docs/README.md:111` stays as-is. The contingency-path move would relocate the filter symbols to "Shipped today" AND bump the version label from `(0.0.7)` to `(0.0.8)`; both are deferred to whichever card actually owns the joint cut.
- `docs/TREE.md` — added the on-disk `filters/` block to the "current on-disk layout" tree after `optimizer/` (five files: `__init__.py`, `base.py`, `sets.py`, `factories.py`, `inputs.py`); updated the surrounding prose at the top of the current-on-disk-layout block to list `filters` alongside `types`, `optimizer`, and `utils`; removed the `[alpha] Filtering subsystem` block from the "target package layout" tree (per spec L1084's "Move from"); added a `filters/` sub-block to the "Current shape (on disk today)" `tests/` tree listing all five on-disk test files plus `test_finalizer.py` (verified via `ls tests/filters/`). The "target shape" `tests/filters/` block already existed and was left untouched.
- `README.md` — **not touched.** Default joint-cut path. Contingency path would add `FilterSet` / `RelatedFilter` / `filter_input_type` / `Meta.filterset_class` to the Status paragraph at L45 AND bump the leading `**`0.0.7`, single-maintainer, alpha-quality.**` line to `0.0.8`; both deferred to the joint-cut card.
- `GOAL.md` — rewrote the "Coming from DRF + `django-filter`" migration narrative per spec L1093 (replacement wording verbatim); changed the code block's `class CategoryFilter(django_filters.FilterSet):` to `class CategoryFilter(FilterSet):` and added `from django_strawberry_framework.filters import FilterSet` to the imports above the example; updated the inline `# Your existing django-filter FilterSet — no changes:` comment to `# Your existing django-filter FilterSet — swap the parent class:`. Preserved the DRF Serializer sentence (orthogonal to filter migration). The astronomy showcase block was already correctly shaped per spec L1092 and was not touched.
- `TODAY.md` — appended a new clause to the existing `examples/fakeshop/apps/library/schema.py` paragraph at L11 per the discretion-(a) choice in the plan, naming the new `BranchFilter` / `ShelfFilter` / `BookFilter` / `LoanFilter` / `PatronFilter` declarations at `examples/fakeshop/apps/library/filters.py`, the `GenreFilter` at `filters_genre.py` for the cross-module absolute-import-path case, and the 14 live `/graphql/` HTTP filter tests at `examples/fakeshop/test_query/test_library_api.py`. Did NOT add a separate standalone "Shipped capabilities" block (none exists in the current `TODAY.md` structure per the planner's discretion item).
- `KANBAN.md` — rewrote the `### Shipped foundation`'s `### In progress` bullet at the active-`0.0.8` line to reflect that `WIP-ALPHA-021-0.0.8` shipped as `DONE-021-0.0.8` and the two remaining WIP cards (`WIP-ALPHA-022-0.0.8`, `WIP-ALPHA-023-0.0.8`) are queued; rewrote the parallel narrative paragraph under the `## In progress` board-column heading. Removed the full `### WIP-ALPHA-021-0.0.8 — Filtering subsystem` block (KANBAN L79–L194 in the rev8 shape). Inserted a new `### DONE-021-0.0.8 — Filtering subsystem` entry immediately after `### DONE-048-0.0.7` containing a one-line `Spec: docs/spec-021-filters-0_0_8.md` reference followed by the verbatim past-tense body from spec L1101 — preserved character-for-character including the trailing joint-cut contingency sentence. Did NOT touch the maintainer's in-progress KANBAN bullet at the BACKLOG/positioning section ("Add ability to set dsf settings to cap the number of schema hookups per model and error if it is more"; verified surviving in `git diff KANBAN.md`). Added three glossary link-defs to the docs/ group of the bottom block alphabetically: `[glossary-filterset]`, `[glossary-metafilterset_class]`, `[glossary-relatedfilter]`. The ship-date in the in-progress narrative reads `DONE-021-0.0.8 shipped` without a `YYYY-MM-DD` clause (cleaner reading per the discretion item; the spec body's past-tense paragraph is the system-of-record for the ship).
- `CHANGELOG.md` — created `### Added` and `### Changed` subsections inside the existing `[Unreleased]` block (both were absent). The Added bullet is the verbatim wording pinned at spec L1109; the Changed bullet is the verbatim wording pinned at spec L1112. Stripped the spec-internal `(per H6 of rev5 + H1 of rev6)` / `(per H3 of [`docs/feedback.md`][feedback])` / `(per M3 of [`docs/feedback.md`][feedback] rev8)` review-correlation parentheticals from the body because [`docs/feedback.md`][feedback] is a per-cycle scratchpad that won't outlive the spec archive (per AGENTS.md L26 + START.md "Temp artifact conventions") — the CHANGELOG is a standing doc and shouldn't carry crumb-trail references to a doc the next spec author archives. Added the explicit `Tracked as DONE-021-0.0.8` trailing sentence to the Added bullet matching the prose rhythm of every other entry in the `[0.0.7]` block. Added four new glossary link-defs to the docs/ group of the bottom block alphabetically: `[glossary-configurationerror]`, `[glossary-filterset]`, `[glossary-metafilterset_class]`, `[glossary-relatedfilter]`. **Did NOT promote `[Unreleased]` → `[0.0.8]`** per Decision 10 + the safe-default joint-cut path. **Did NOT bump `pyproject.toml`, `django_strawberry_framework/__init__.py __version__`, or `tests/base/test_init.py`** per the same default. CHANGELOG-edit permission for this card comes from spec L158 + DoD item 23 (the Slice 5 bullet IS the explicit permission per AGENTS.md L21).
- `docs/builder/bld-slice-5-docs_kanban_changelog.md` — appended this build report and flipped the top-level `Status:` line from `planned` to `built` (the only two edits Worker 2 makes to the artifact).

### Tests added or updated

None; Slice 5 is doc-only. Permanent test surface for the filtering subsystem landed in Slices 1–4a (`tests/filters/test_base.py`, `test_sets.py`, `test_factories.py`, `test_inputs.py`, `test_finalizer.py`; `tests/types/test_base.py`, `test_definition_order.py`, `test_definition_relations.py`; `examples/fakeshop/test_query/test_library_api.py` with the 14 live HTTP tests). Slice 6 holds the cross-card composition smoke test until `WIP-ALPHA-022-0.0.8` ships.

### Validation run

- `uv run ruff format .` — pass (148 files left unchanged; no files reformatted; the COM812 + formatter conflict warning at the top is the long-standing project-level warning unrelated to this slice).
- `uv run ruff check --fix .` — pass (All checks passed!).
- `git status --short` after both ruff invocations — output classification:
  - **Slice 5-intended (in-diff):** `CHANGELOG.md`, `GOAL.md`, `KANBAN.md`, `TODAY.md`, `docs/GLOSSARY.md`, `docs/TREE.md`, `docs/spec-021-filters-0_0_8-terms.csv`, `docs/builder/bld-slice-5-docs_kanban_changelog.md` (the artifact itself, appended-to per the contract).
  - **Prior-slice working-tree changes (expected; per BUILD.md "Build artifact list" and the build-021 plan):** `django_strawberry_framework/filters/__init__.py`, `base.py`, `factories.py`, `inputs.py`, `sets.py`; `django_strawberry_framework/registry.py`; `django_strawberry_framework/types/base.py`, `definition.py`, `finalizer.py`; `examples/fakeshop/apps/library/models.py`, `schema.py`; `examples/fakeshop/test_query/test_library_api.py`; `tests/types/test_base.py`, `test_definition_order.py`. Untracked: `docs/builder/bld-slice-1-foundation.md` through `bld-slice-4a-tree_form_logic.md`, `docs/builder/build-021-filters-0_0_8.md`, `examples/fakeshop/apps/library/filters.py`, `filters_genre.py`, `examples/fakeshop/apps/library/migrations/0004_patron_email.py`, `tests/filters/`, `tests/types/fixtures/`, `tests/types/test_definition_relations.py`. All expected per the active build's earlier slices.
  - **Maintainer/Worker-1 in-progress (out of scope per AGENTS.md L33):** `docs/feedback.md` (maintainer scratchpad); `docs/spec-021-filters-0_0_8.md` (Worker 1's prior-slice spec edits). Untouched in this pass.
  - **Tool-induced drift:** none. Doc-only edits did not trigger any unrelated reformat / lint mutations.
- `uv run python scripts/check_spec_glossary.py --spec docs/spec-021-filters-0_0_8.md` — **fails (exit 1):** the checker reports `filter_input_type (anchor: filter_input_type) — add at least one link to anchor filter_input_type — inline ](GLOSSARY.md#filter_input_type) or reference-style [text][glossary-...] (with matching def in the link-definitions block)`. The GLOSSARY entry, the GLOSSARY Index row, the Browse-by-category bullet, AND the CSV row are all in place; the missing piece is a reference-style or inline link to `GLOSSARY.md#filter_input_type` in the SPEC body. Worker 2 cannot edit the spec — this is a Worker 1 reconciliation surface (see `### Notes for Worker 1 (spec reconciliation)` below).
- `pytest` not run (doc-only slice; per AGENTS.md L14 + BUILD.md "Coverage is the maintainer's gate, not a worker's tool").

### Implementation notes

- **GLOSSARY entry-body rewrites stayed faithful to the spec's pinned wording while collapsing rev5/rev6/rev7/rev8 feedback-correlation prefixes** (`per M2 of rev5`, `per H1 of rev6`, etc.) — those crumb-trail references belong in the spec body (a per-cycle reasoning artifact) but would clutter the standing GLOSSARY which a consumer reads as the system-of-record. Concrete contracts and load-bearing nouns (active-input-only scope; filter-scope-NOT-security-boundary; six-layer pipeline; `apply_sync` / `apply_async` pair; child-visibility-queryset derivation; `GraphQLError` payload shape) ARE preserved verbatim. Worker 3's verbatim-copy `diff` check should treat the contract content as the test, not the parenthetical correlations.
- **New GLOSSARY `## filter_input_type` entry slotted alphabetically between `## FilterSet` and `## finalize_django_types`.** Underscore in `filter_input_type` sorts AFTER `FilterSet` and BEFORE `finalize_django_types` lexicographically (per the existing `## FK-id elision` / `## finalize_django_types` ordering). The Index table row position (between `[FieldSet]` and `[FilterSet]`) was chosen the same way — `[filter_input_type]` lowercase-sorts between `FieldSet` and `FilterSet`.
- **GLOSSARY entry's See-also for `FilterSet` extended to mention `filter_input_type`** to surface the helper from the primary symbol's "what's next" line; the new `filter_input_type` entry's See-also lists `FilterSet` and `Meta.filterset_class` (the two surfaces consumers touch when wiring filters into a resolver).
- **TREE.md current-on-disk-layout block.** The `filters/` insertion was placed AFTER `optimizer/` and BEFORE `└── utils/` — preserving the lexical ordering already in the block (`management/`, `types/`, `optimizer/`, `filters/`, `utils/`). This required swapping the previous `└── utils/` glyph for `├── filters/` and keeping `└── utils/` as the new terminator; alternatively the block could have used `└── filters/` and broken `utils/`, but preserving `utils/` as the natural last sibling reads cleaner. The "target package layout" deletion of the `[alpha] filters/` block left the surrounding `orders/`, `aggregates/`, `mutations/`, etc. blocks untouched — they remain `[alpha]` / `[beta]` because they haven't shipped.
- **TREE.md `tests/filters/` block ordering** mirrors the source module order (`test_base.py`, `test_sets.py`, `test_factories.py`, `test_inputs.py`, `test_finalizer.py`) rather than alphabetical, matching the pattern used for `tests/optimizer/`'s in-block ordering and the source/test 1:1 mirror discipline. Verified all five files exist on disk (`ls tests/filters/` returned `__init__.py`, `test_base.py`, `test_factories.py`, `test_finalizer.py`, `test_inputs.py`, `test_sets.py`).
- **KANBAN.md DONE-021-0.0.8 past-tense body rendered as paragraph markdown, not blockquote.** The spec L1101 wraps the body in `>` and uses `\"` backslash-escaped quotes because the inside contains literal quoted strings; KANBAN's existing `DONE-NNN` entries (e.g., `DONE-048-0.0.7` at L1814) render past-tense bodies as plain markdown paragraphs with regular straight quotes. Worker 2 followed the existing KANBAN convention, unescaping the spec's `\"` to `"` so the body renders correctly. The textual content (every load-bearing noun and contract) is preserved character-for-character.
- **Spec-internal `[`docs/feedback.md`][feedback]` link-style references stripped from the CHANGELOG bullet.** `docs/feedback.md` is a per-cycle scratchpad that the next spec author's Step 8 archive pass may relocate; embedding cross-references to it from a standing doc would create future link rot. The CHANGELOG body contracts (active-input-only scope, `Annotated[...]` shape, `LOOKUP_NAME_MAP`, module-globals materialization, 14 live HTTP test count, the specific axes list) are preserved verbatim; only the rev-feedback correlation prefixes were trimmed.
- **CHANGELOG link-def insertion preserved alphabetical-within-group ordering** under the `<!-- docs/ -->` header. New defs: `[glossary-configurationerror]`, `[glossary-filterset]`, `[glossary-metafilterset_class]`, `[glossary-relatedfilter]`. These sit alongside the previously-existing `[glossary-djangotype]` / `[glossary-metafields]` / `[glossary-multi-database-cooperation]` / etc.

### Notes for Worker 3

- The CHANGELOG `### Documentation / release sanity` check in BUILD.md's review template normally runs a `diff` of verbatim-copy contracts against the spec. The KANBAN past-tense body and the CHANGELOG `### Added` / `### Changed` bullets are reproduced from spec L1101 / L1109 / L1112 with the following load-bearing deltas:
  - **Quotes unescaped** from `\"` to `"` (KANBAN body) because the spec's blockquote-with-escaped-quotes shape is for in-spec rendering and doesn't apply to the destination's paragraph rendering.
  - **`[`docs/feedback.md`][feedback]` review-correlation parentheticals stripped** from the CHANGELOG bullet to avoid future link rot. See `### Implementation notes` for the reasoning.
  - **`Tracked as DONE-021-0.0.8 in [`KANBAN.md`][kanban].` trailing sentence appended** to the CHANGELOG `### Added` bullet to match the prose rhythm every other entry in the `[0.0.7]` block already follows. Not present in the spec's pinned text but consistent with the CHANGELOG's standing convention.
- **Joint-cut contingency**: Worker 2 took the safe-default no-version-bump path. `docs/README.md`'s "Coming in `0.1.0`" filter-symbols line at L111 was NOT touched; `README.md`'s Status paragraph at L45 was NOT touched; `pyproject.toml [project].version` and `django_strawberry_framework/__init__.py __version__` and `tests/base/test_init.py`'s version assertion were NOT touched; `CHANGELOG.md` `[Unreleased]` heading was NOT promoted. If Worker 1 final-verification determines this IS the last 0.0.8 card to ship, those four surfaces need to be revisited in a follow-up pass — Worker 0 routes through Worker 2 for that.
- **`docs/spec-021-filters-0_0_8-terms.csv` trailing-bytes artifact**: the committed CSV (since rev6) carries literal `</content>\n</invoke>` trailing bytes after the data rows. Worker 2 did NOT remove these — they're pre-existing maintainer-side serialization artifacts outside the slice scope per AGENTS.md L33. The new `filter_input_type` row was inserted BEFORE these trailing bytes. The glossary checker reads the CSV via `csv.DictReader` which tolerates the garbage trailing rows (it returns rows with `None` for missing columns; the `load_terms` helper silently skips them). If a future maintainer wants to clean this up, the right grain is a separate housekeeping commit, not a Slice 5 sneak-in.

### Notes for Worker 1 (spec reconciliation)

1. **`scripts/check_spec_glossary.py` fails against `docs/spec-021-filters-0_0_8.md` after Slice 5's edits.** The checker reports `filter_input_type (anchor: filter_input_type) — add at least one link to anchor filter_input_type — inline ](GLOSSARY.md#filter_input_type) or reference-style [text][glossary-...] (with matching def in the link-definitions block)`. The CSV row (added in this slice per spec L1130's fallback wording) AND the new GLOSSARY entry/anchor (added in this slice per spec L1076) are in place; what's missing is a reference-style link from the SPEC body to the new glossary anchor. **Worker 2 cannot edit the spec.** The simplest remediation: add `[glossary-filter_input_type]: ../docs/GLOSSARY.md#filter_input_type` to the spec's `<!-- docs/ -->` group of the bottom link-definitions block AND wrap at least one prose mention of `filter_input_type` in the spec body as `[`filter_input_type`][glossary-filter_input_type]`. The spec has many existing prose mentions to choose from (L4 line is one of the earliest, L261 and L333 / L344 / L360 are dense). Worker 1's final-verification owns this — it's a one-line ref-def add plus a one-link prose wrap.
2. **Spec-vs-shipped-reality drift in the KANBAN past-tense body wording.** The spec's L1101 past-tense body is pinned text. Worker 2 copied it verbatim into KANBAN per the contract. The text appears to faithfully reflect everything Slices 1–4a shipped (sync/async split per rev5 H2; orphan-validation via `_helper_referenced_filtersets`; canonical `info.context.request` extraction per rev5 M8; active-input-only `check_permissions` per rev5 M2; `ErrorDict.get_json_data()` shape per rev5 M10; `related_target_for` method per rev5 H1; exactly 14 live HTTP tests per rev8 H4; joint-cut contingency per rev5 L5). One potential drift Worker 1 should re-verify: the past-tense body mentions `LOOKUP_NAME_MAP` but does NOT mention the Slice 4a tree-form-logic substrate carved mid-build. Spec L4 (the Status line) acknowledges Slice 4a; the past-tense body at L1101 mentions `filter_queryset` as "the `django-filter` instance-method override" once but does NOT call out the carve explicitly. Worker 1's call whether to edit the spec to mention the Slice 4a carve more prominently in the past-tense body OR ratify the current shape as historically sufficient.
3. **Two trailing bytes (`</content>\n</invoke>`) in `docs/spec-021-filters-0_0_8-terms.csv`.** Pre-existing maintainer artifact since rev6 (verified via `git show HEAD:docs/spec-021-filters-0_0_8-terms.csv`). Tolerated by the glossary checker. Cosmetic cleanup is out of Slice 5 scope — flag for a future housekeeping commit.
4. **Default joint-cut path taken.** Worker 0 / maintainer has not signaled "this is the last 0.0.8 card to ship," so Worker 2 left the contingency-path surfaces untouched (`docs/README.md`, `README.md`, `pyproject.toml`, `__init__.py`, `tests/base/test_init.py`, `CHANGELOG.md` `[Unreleased]` → `[0.0.8]` promotion). Worker 1 final-verification confirms the safe-default ratification OR routes back to Worker 2 for the bump.

---

## Review (Worker 3)

### High:

None.

### Medium:

#### M1: KANBAN past-tense body uses inline `](path)` cross-file links — convention violation at four sites

The KANBAN past-tense body for `### DONE-021-0.0.8 — Filtering subsystem` (`KANBAN.md:1732-1736`) renders four cross-file links inline rather than reference-style:

- `KANBAN.md:1734` — `Spec: [`docs/spec-021-filters-0_0_8.md`](docs/spec-021-filters-0_0_8.md)`
- `KANBAN.md:1736` — `[`django_strawberry_framework/filters/`](django_strawberry_framework/filters/)` (mid-body)
- `KANBAN.md:1736` — `[`examples/fakeshop/apps/library/`](examples/fakeshop/apps/library/)` (later in body)
- `KANBAN.md:1736` — `[`examples/fakeshop/test_query/test_library_api.py`](examples/fakeshop/test_query/test_library_api.py)` (later in body)

AGENTS.md L28 ("Markdown cross-file links in standing docs and specs use reference-style — inline body uses are `[text][ref-id]`; all defs live in a unified bottom block...") and START.md "Markdown link convention" both require reference-style for cross-file links in standing docs. The spec source at L1101 already uses reference-style (`[`django_strawberry_framework/filters/`][filters]`, `[`examples/fakeshop/apps/library/`][fakeshop-library]`, `[`examples/fakeshop/test_query/test_library_api.py`][fakeshop-test-library]`), so Worker 2 unwrapped the spec's reference-style into inline `](path)` style during the verbatim-copy drop — likely because the corresponding ref-defs (`[filters]`, `[fakeshop-library]`, `[fakeshop-test-library]`) are not yet defined in KANBAN.md's `<!-- LINK DEFINITIONS -->` block.

Why it matters: the move-cost-reduction promise of the reference-style convention (START.md "When moving a .md file: only the bottom block's paths need updating") evaporates the moment standing docs accept inline `](path)` for cross-file links; the next archive pass / restructure will have to grep for inline links throughout `KANBAN.md`. The package-policy violation also sets a precedent for future card-move passes.

Recommended change: convert all four inline links to reference-style; add the three missing ref-defs to KANBAN.md's bottom block under the correct group:
- `[filters]: django_strawberry_framework/filters/` under `<!-- django_strawberry_framework/ -->`
- `[fakeshop-library]: examples/fakeshop/apps/library/` under `<!-- examples/ -->`
- `[fakeshop-test-library]: examples/fakeshop/test_query/test_library_api.py` under `<!-- examples/ -->` (this one duplicates the existing `[test-library-api]` def at KANBAN.md:1805; either reuse `[test-library-api]` inline-side or add the alias — Worker 2's choice)
- For the spec link in the `Spec:` line, the simplest shape is an inline anchor — but a `[spec-021]: docs/spec-021-filters-0_0_8.md` ref-def under `<!-- docs/ -->` matches the convention.

No test expectation; this is a documentation-convention finding, not a behavioral one.

```KANBAN.md:1734-1736
Spec: [`docs/spec-021-filters-0_0_8.md`](docs/spec-021-filters-0_0_8.md)

Shipped the filtering subsystem. ... land at [`django_strawberry_framework/filters/`](django_strawberry_framework/filters/) ... [`examples/fakeshop/apps/library/`](examples/fakeshop/apps/library/) grows ... [`examples/fakeshop/test_query/test_library_api.py`](examples/fakeshop/test_query/test_library_api.py) grows exactly 14 live HTTP tests ...
```

#### M2: CHANGELOG `### Added` bullet uses inline `](django_strawberry_framework/filters/)` link

`CHANGELOG.md:21` (inside the `[Unreleased]` `### Added` bullet) renders:

> Subpackage at [`django_strawberry_framework/filters/`](django_strawberry_framework/filters/) (five files); mirror tests at `tests/filters/`.

The spec source at L1109 uses reference-style (`[`django_strawberry_framework/filters/`][filters]`); Worker 2 unwrapped it into inline `](path)` during the copy drop because `[filters]` is not defined in CHANGELOG.md's bottom block (the existing block at `CHANGELOG.md:185-190` has refs like `[apps]`, `[converters]`, `[django-patches]`, `[resolvers]`, `[test-init]` under `<!-- django_strawberry_framework/ -->`, but no `[filters]`).

This is the same class of violation as M1 but at the CHANGELOG site; calling it out separately because the two surfaces are reviewed independently (BUILD.md "CHANGELOG sanity" vs "Documentation / release sanity") and each needs its own remediation.

Recommended change: add `[filters]: django_strawberry_framework/filters/` to the `<!-- django_strawberry_framework/ -->` group at `CHANGELOG.md:185-190` (alphabetical position — between `[django-patches]` and `[resolvers]`); convert the inline body link at `CHANGELOG.md:21` to `[`django_strawberry_framework/filters/`][filters]`. The other CHANGELOG ref to `test_library_api.py` correctly uses reference-style (`[`examples/fakeshop/test_query/test_library_api.py`][test-library-api]`) since `[test-library-api]` already exists at `CHANGELOG.md:201`, so the fix is one new def + one inline-to-ref conversion.

No test expectation.

### Low:

None.

### DRY findings

None. Slice 5 is doc-only with no shared helper extraction or repeated-literal risk; cross-slice DRY targets (`_LOGIC_KEYS` duplication; `f"{Name}InputType"` literal across 5 sites) belong to `bld-integration.md` and were correctly carried forward by Worker 1's plan rather than dragged into this slice.

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` returns empty output. `__all__` and the re-export list are unchanged, consistent with spec DoD item 25 (L1174 — "Top-level `__all__` is NOT widened (subpackage import path is the right grain per [Decision 2])"). Pass.

### CHANGELOG sanity

Applicable; slice modified `CHANGELOG.md`.

- **Version line**: `## [Unreleased]` heading (`CHANGELOG.md:19`) is preserved (not promoted to `## [0.0.8] - YYYY-MM-DD`). Consistent with spec L1114 + Decision 10's safe-default joint-cut path. `pyproject.toml [project].version` and `django_strawberry_framework/__init__.py __version__` were not touched (verified via `git status --short` — neither file appears in the working tree diff); `tests/base/test_init.py` is similarly untouched. All four version-bump surfaces are pinned together per AGENTS.md L31, and all four are deferred to the joint-cut card.
- **Headings used**: `### Added` and `### Changed` created inside `[Unreleased]`. Both match the spec L1107 + L1110 prescription. `### Fixed` / `### Removed` not created — appropriate (nothing fixed or removed in this card).
- **Wording**: the `### Added` bullet body matches spec L1109 modulo two recorded deviations Worker 2 called out in `### Notes for Worker 3`:
  - review-correlation parentheticals (`(per H6 of rev5 + H1 of rev6)`, `(per H1 of rev3 + H2 of [`docs/feedback.md`][feedback])`, `(per H3 of [`docs/feedback.md`][feedback])`) stripped — defensible because `docs/feedback.md` is a per-cycle scratchpad that won't outlive the spec archive (per AGENTS.md L26 + START.md "Temp artifact conventions"). The CHANGELOG is a standing doc and shouldn't carry crumb-trail references that link-rot at the next spec author's Step 8 archive pass.
  - `**Filtering subsystem**` bold lead-in added (per discretion item in the plan) — matches the prose rhythm of the existing `[0.0.7]` `### Added` entries.
  - Trailing `Tracked as `DONE-021-0.0.8` in [`KANBAN.md`][kanban].` sentence appended — matches every other `[0.0.7]` Added bullet's trailing pattern.
- **The `### Changed` bullet** at `CHANGELOG.md:24` matches spec L1112 character-for-character (modulo expected straight-quote rendering vs the spec's blockquote-with-escaped-quotes shape).
- **The `[filters]` inline-link violation** (see M2 above) is the only CHANGELOG-sanity issue.

### Documentation / release sanity

Applicable; slice modified `docs/GLOSSARY.md`, `docs/TREE.md`, `GOAL.md`, `TODAY.md`, `KANBAN.md`, `CHANGELOG.md`, and `docs/spec-021-filters-0_0_8-terms.csv`.

- **`docs/GLOSSARY.md`**:
  - `FilterSet` entry (`docs/GLOSSARY.md:422-432`): Status flipped from `planned for 0.0.8` to `shipped (0.0.8)`. Entry body describes the active-input-only-scope contract, the filter-scope-NOT-security-boundary framing (with the explicit "visibility / security is the job of `get_queryset`" call-out), the logical `and`/`or`/`not` shape, stable class-derived input-type names, the six-layer cycle-safe pipeline, the `BaseFilterSet`-subclass relationship, and the `apply_sync` / `apply_async` resolver-facing API. All contracts per spec L1072 present; the rev5/rev6 review-correlation parentheticals are stripped (consistent with Worker 2's standing-doc-vs-scratchpad reasoning).
  - `RelatedFilter` entry (`docs/GLOSSARY.md:822-830`): Status flipped to `shipped (0.0.8)`. Body covers target acceptance (class / absolute path / unqualified name), Layer-2 module-fallback resolution, and the `queryset=` filter-scope-NOT-security-boundary framing. Matches spec L1073 contracts.
  - `Meta.filterset_class` entry (`docs/GLOSSARY.md:609-622`): Status flipped to `shipped (0.0.8)`. Body describes the `DEFERRED_META_KEYS` promotion, the `_validate_meta` validation, and the finalizer phase 2.5 binding pipeline (owner-bind, validate compatibility, `get_filters()`, BFS factory, materialize-before-Schema). Code block preserved. Matches spec L1074 contracts.
  - `filter_input_type` entry (`docs/GLOSSARY.md:434-461`): New entry slotted alphabetically between `## FilterSet` and `## finalize_django_types` per ASCII strict ordering. Body covers the `Annotated[...]` return shape, eager `TypeError` validation, the `_helper_referenced_filtersets` orphan-tracking note (sourced from DoD item 7 / Decision 11; defensible enrichment beyond the spec L1076 prescription), and a complete consumer-usage code block. Status line present.
  - **Index table**: rows for `FilterSet` (L72), `Meta.filterset_class` (L86), `RelatedFilter` (L102) all read `shipped (0.0.8)`. New row for `[filter_input_type](#filter_input_type)` inserted at L73 (between `FilterSet` and `finalize_django_types`) — ASCII strict ordering matches the existing pattern (cf. L75's `FK-id elision` followed by L76's `get_child_queryset`).
  - **Browse by category Filtering bullet** (L127): correctly extended to `[`FilterSet`](#filterset) · [`RelatedFilter`](#relatedfilter) · [`filter_input_type`](#filter_input_type) · [`Meta.filterset_class`](#metafilterset_class).` Slight non-alphabetical ordering within the bullet is acceptable since the spec L1077 specifies the three new symbols ("`FilterSet`, `RelatedFilter`, `filter_input_type`") as a listing not an alphabetical sort.
  - **Public exports section** NOT touched — correct per spec L1077 + Decision 2.

- **`docs/TREE.md`**:
  - Current-on-disk-layout `filters/` block (`docs/TREE.md:225-230`) inserted after `optimizer/`; five files listed in source-module order (`__init__.py`, `base.py`, `sets.py`, `factories.py`, `inputs.py`). Verified each file exists on disk via `ls django_strawberry_framework/filters/`.
  - Surrounding prose at `docs/TREE.md:190` updated to list `filters` alongside `types`, `optimizer`, and `utils`.
  - The `[alpha] Filtering subsystem` block has been removed from the "target package layout" section (verified via `grep "Filtering subsystem" docs/TREE.md` returning only the on-disk entry at L225). Adjacent target-layout blocks (`orders/`, `aggregates/`, etc.) remain `[alpha]` / `[beta]` as expected.
  - `tests/filters/` current-shape block (`docs/TREE.md:365-371`) lists six on-disk files (`__init__.py`, `test_base.py`, `test_sets.py`, `test_factories.py`, `test_inputs.py`, `test_finalizer.py`); verified each exists via `ls tests/filters/`.

- **`GOAL.md`** migration-narrative replacement (`GOAL.md:450`): matches spec L1093 verbatim — `"Your existing django_filters.FilterSet migrates to Meta.filterset_class via a one-line parent-class swap to django_strawberry_framework.filters.FilterSet; the package's FilterSet IS a django_filters.filterset.BaseFilterSet subclass, so every Filter / FilterMethod / form-cleaning primitive you already use carries over unchanged."`. The DRF Serializer sentence is preserved per the plan. Code block: `class CategoryFilter(FilterSet):` (parent-class swap from `django_filters.FilterSet`); `from django_strawberry_framework.filters import FilterSet` import added; inline comment updated from "no changes" to "swap the parent class:". All as planned. Verbatim-copy contract: clean.

- **`TODAY.md`** fakeshop section (`TODAY.md:11`) extended with a clause naming `BranchFilter` / `ShelfFilter` / `BookFilter` / `LoanFilter` / `PatronFilter` at `examples/fakeshop/apps/library/filters.py`, `GenreFilter` at `filters_genre.py`, and "the 14 live `/graphql/` HTTP filter tests at `examples/fakeshop/test_query/test_library_api.py`". Reads coherently within the existing fakeshop paragraph rhythm. Worker 2's discretion choice (a) over (b) — extending the existing paragraph rather than carving a standalone "Shipped capabilities" subsection — is defensible since no such standalone subsection exists in the file's current structure.

- **`KANBAN.md`**:
  - **`### DONE-021-0.0.8` next-available id**: the highest DONE-NNN currently shipped is `DONE-048-0.0.7` (at `KANBAN.md:1698`); no prior `DONE-021-*` or `DONE-020-*` entry exists; the `0.0.8` cohort numbering correctly picks up the card's existing `021` segment. Done.
  - **Card cut**: `### WIP-ALPHA-021-0.0.8 — Filtering subsystem` block is fully removed from the In-progress board column (verified via `grep "WIP-ALPHA-021-0.0.8" KANBAN.md` returning only narrative references, no remaining card header).
  - **Card paste**: `### DONE-021-0.0.8 — Filtering subsystem` inserted at `KANBAN.md:1732`, immediately after `### DONE-048-0.0.7` at L1698. Past-tense body content matches spec L1101 verbatim (modulo the documented `\"` → `"` quote unescape and the four inline-link unwraps flagged in M1). The trailing joint-cut contingency sentence is preserved.
  - **`### In progress` summary paragraphs**: L51 and L77 both rewritten to name only `WIP-ALPHA-022-0.0.8` and `WIP-ALPHA-023-0.0.8` as remaining, with the trailing "WIP-ALPHA-021-0.0.8 ... shipped as DONE-021-0.0.8" reconciliation sentence. No `YYYY-MM-DD` placeholder remains; Worker 2 chose the "no date clause" discretion-item shape consistent with the spec body's past-tense paragraph as system-of-record.
  - **Maintainer in-progress bullet at L807** ("Add ability to set dsf settings to cap the number of schema hookups per model and error if it is more") survives untouched. Confirmed via `grep "Add ability to set dsf settings to cap"` returning L807, which is well outside Slice 5's WIP-021 → Done edit region. AGENTS.md L33 compliance: clean.
  - **Definition-of-done bullet 1 rewrite**: the spec L1103 instruction was "`docs/spec-filters.md` → `docs/SPECS/spec-021-filters-0_0_8.md` after the Step-8 archive pass." The past-tense body uses `Spec: docs/spec-021-filters-0_0_8.md` (the current working location per AGENTS.md L26 + BUILD.md "Spec stays at its working location") — correct deferral of the spec-archive move to the next-spec-author's Step 8 pass.

- **`docs/spec-021-filters-0_0_8-terms.csv`** (`docs/spec-021-filters-0_0_8-terms.csv:49`): row appended `filter_input_type,filter_input_type,The consumer helper this card introduces; GLOSSARY entry created in Slice 5.` matching spec L1130's documented fallback wording. The pre-existing `</content>\n</invoke>` trailing artifact (since rev6 of the spec) is preserved per AGENTS.md L33 — defensible deferral of cosmetic cleanup to a future housekeeping commit.

- **`scripts/check_spec_glossary.py` exit-1 (independently confirmed)**: ran `uv run python scripts/check_spec_glossary.py --spec docs/spec-021-filters-0_0_8.md` from the repo root; output is `Spec terms missing a link to GLOSSARY.md: filter_input_type (anchor: filter_input_type) — add at least one link to anchor filter_input_type ...` with exit code 1. The GLOSSARY entry, GLOSSARY Index row, Browse-by-category bullet, and CSV row are all in place — the missing piece is a reference-style or inline link from the spec body to `GLOSSARY.md#filter_input_type`. This is a spec-side deficiency (the spec body has many `filter_input_type` prose mentions but none are wrapped as `[`filter_input_type`][glossary-filter_input_type]`); Worker 2 cannot edit the spec. Routed to Worker 1 (see `### Notes for Worker 1 (spec reconciliation)` below); not blocking acceptance of Worker 2's diff, but acceptance of the slice as a whole hinges on Worker 1's final-verification spec edit.

- **No stale "coming soon" / "planned for 0.0.8" wording**: GLOSSARY's four filter-related entries (FilterSet, RelatedFilter, Meta.filterset_class, filter_input_type) all read `shipped (0.0.8)`; the GLOSSARY Index table for the three pre-existing entries (L72, L86, L102) also reads `shipped (0.0.8)`. No drift between the entry-body Status lines and the Index status column.

- **Default joint-cut path correctly honored**: `docs/README.md` "Coming in `0.1.0`" filter-symbols line untouched; `README.md` Status paragraph untouched; `pyproject.toml`, `__init__.py`, and `tests/base/test_init.py` version surfaces untouched; `CHANGELOG.md` `[Unreleased]` heading preserved. All five surfaces correctly deferred to the joint-cut card per spec L1080 / L1088 / L1114 + Decision 10's safe-default.

### What looks solid

- Verbatim-copy contracts honored at the text level for all four pinned regions (KANBAN past-tense body, CHANGELOG Added, CHANGELOG Changed, GOAL.md replacement narrative). The two recorded deviations Worker 2 called out (`\"` → `"` quote unescape in KANBAN; review-correlation parentheticals stripped in CHANGELOG) are defensible against the standing-doc-vs-scratchpad reasoning. The contracts' load-bearing nouns (active-input-only scope; filter-scope-NOT-security-boundary; sync/async resolver split; `GraphQLError` extension payload shape; `_helper_referenced_filtersets` orphan tracking; 14-live-test count; LOOKUP_NAME_MAP pinning) are all preserved.
- GLOSSARY entry-body rewrites preserve every load-bearing contract from spec L1072-1076 while collapsing rev5/rev6/rev7/rev8 crumb-trail references — the right call for a standing doc that consumers read as the system-of-record.
- Public-surface check: `git diff -- django_strawberry_framework/__init__.py` is empty — no top-level `__all__` widening, consistent with spec DoD item 25 + Decision 2.
- AGENTS.md L33 compliance: the maintainer's in-progress KANBAN bullet at L807 survived untouched; Worker 2's KANBAN edit operated only inside the WIP-021 → Done card-move region and the `### In progress` summary paragraphs.
- Default joint-cut path correctly taken: all five contingency-path surfaces (`docs/README.md`, `README.md`, `pyproject.toml`, `__init__.py`, `tests/base/test_init.py`, `CHANGELOG.md` `[Unreleased]` promotion) deferred to the joint-cut card per spec L1080 / L1088 / L1114 + Decision 10.
- TREE.md current-on-disk-layout `filters/` block matches reality file-by-file; the parallel `tests/filters/` block lists exactly the six files present under `tests/filters/` (verified via `ls`).
- The `### Implementation notes` in Worker 2's build report is unusually thorough — every non-trivial decision (alphabetical-position calls, blockquote-vs-paragraph rendering, link-def alphabetization, ship-date discretion item resolution) is documented with a one-line rationale, so the review path is short.

### Temp test verification

No temp tests created during this review pass — Slice 5 is doc-only, no executable behavior to pin via temp tests.

### Notes for Worker 1 (spec reconciliation)

1. **`scripts/check_spec_glossary.py` fails (exit 1) against `docs/spec-021-filters-0_0_8.md`** — independently confirmed by running the script from the repo root. The checker reports `filter_input_type (anchor: filter_input_type) — add at least one link to anchor filter_input_type — inline ](GLOSSARY.md#filter_input_type) or reference-style [text][glossary-...] (with matching def in the link-definitions block)`. The Slice-5 GLOSSARY entry + anchor + Index row + Browse-by-category bullet + CSV row are all in place; the missing piece is a spec-body link to the new `#filter_input_type` glossary anchor. Worker 2 cannot edit the spec. Suggested remediation per Worker 2's `Notes for Worker 1`: add `[glossary-filter_input_type]: ../docs/GLOSSARY.md#filter_input_type` to the spec's `<!-- docs/ -->` group of the bottom link-definitions block AND wrap at least one prose mention of `filter_input_type` in the spec body as `[`filter_input_type`][glossary-filter_input_type]` (the spec has many candidate prose mentions — L4, L261, L333, L344, L360). This is a one-line ref-def add plus a one-link prose wrap. Worker 1's final-verification owns this; the slice cannot reach `final-accepted` until the checker exits 0.
2. **Spec L1101 past-tense body Slice-4a carve mention**: the body mentions `filter_queryset` as "the `django-filter` instance-method override" but does not explicitly call out the Slice-4a tree-form-logic substrate as a mid-build carve. Spec L4's Status line acknowledges the carve. Worker 1's choice whether the past-tense body's current wording is historically sufficient or warrants a spec edit (with paired KANBAN edit) to mention the carve more prominently. Lifted from Worker 2's `Notes for Worker 1`; surfaced here so Worker 1's final-verification reading is self-contained.
3. **CSV trailing-bytes (`</content>\n</invoke>`) artifact**: pre-existing maintainer-side serialization artifact since rev6 of the CSV. Tolerated by the glossary checker. Cosmetic cleanup is out of Slice 5 scope. Flag for a future housekeeping commit.
4. **Joint-cut contingency ratification**: Worker 2 took the safe-default no-version-bump path. Worker 1 final-verification confirms ratification OR routes back to Worker 2 for the bump trio (`pyproject.toml`, `__init__.py`, `tests/base/test_init.py`) + `[Unreleased]` → `[0.0.8]` promotion + `docs/README.md` / `README.md` shipped-symbol moves.
5. **Mediums M1 + M2 (inline `](path)` link-style violations)** in `KANBAN.md` and `CHANGELOG.md`: these are Worker-2-fixable in a re-pass (add three new ref-defs to KANBAN.md's bottom block, one new ref-def to CHANGELOG.md's bottom block, convert five inline links to reference-style). The fix does not touch the verbatim contracts' text — only the link rendering.

### Review outcome

`revision-needed`. Two Mediums (M1, M2) need Worker-2 remediation in a re-pass; the spec-side glossary-checker failure (Note 1 to Worker 1) needs Worker 1's spec edit before the slice can reach `final-accepted`, but that is Worker 1's surface and does not block this re-pass dispatch. Setting the artifact's top-level `Status:` line to `revision-needed`.

---

## Build report (Worker 2, pass 2)

### Findings addressed

- **M1 — KANBAN.md past-tense body inline-link violation (four sites).** Addressed at root cause. The four inline `](path)` cross-file links in the `### DONE-021-0.0.8 — Filtering subsystem` body have all been converted to reference-style:
  - `KANBAN.md:1734` — `Spec: [`docs/spec-021-filters-0_0_8.md`](docs/spec-021-filters-0_0_8.md)` → `Spec: [`docs/spec-021-filters-0_0_8.md`][spec-021]`.
  - `KANBAN.md:1736` (mid-body) — `[`django_strawberry_framework/filters/`](django_strawberry_framework/filters/)` → `[`django_strawberry_framework/filters/`][filters]`.
  - `KANBAN.md:1736` (later in body) — `[`examples/fakeshop/apps/library/`](examples/fakeshop/apps/library/)` → `[`examples/fakeshop/apps/library/`][fakeshop-library]`.
  - `KANBAN.md:1736` (later in body) — `[`examples/fakeshop/test_query/test_library_api.py`](examples/fakeshop/test_query/test_library_api.py)` → `[`examples/fakeshop/test_query/test_library_api.py`][test-library-api]` (reusing the existing ref-def at `KANBAN.md:1806` per Worker 3's noted Worker-2 choice — reuses the existing alias rather than adding a parallel `[fakeshop-test-library]` def, which avoids duplicate refs to the same path).
  - Three new ref-defs added to KANBAN.md's bottom unified link block at the correct path-based group headers per START.md "Markdown link convention":
    - `[spec-021]: docs/spec-021-filters-0_0_8.md` under `<!-- docs/ -->` (alphabetical between `[glossary-strawberry-config]` and the group terminator).
    - `[filters]: django_strawberry_framework/filters/` under `<!-- django_strawberry_framework/ -->` (alphabetical between `[django-patches]` and `[plans]`).
    - `[fakeshop-library]: examples/fakeshop/apps/library/` under `<!-- examples/ -->` (alphabetical between `[example-schema]` and `[fakeshop-test-multi-db]`).

- **M2 — CHANGELOG.md inline-link violation (one site).** Addressed at root cause:
  - `CHANGELOG.md:21` — `Subpackage at [`django_strawberry_framework/filters/`](django_strawberry_framework/filters/) (five files); mirror tests at `tests/filters/`.` → `Subpackage at [`django_strawberry_framework/filters/`][filters] (five files); mirror tests at `tests/filters/`.`
  - One new ref-def added to CHANGELOG.md's bottom unified link block:
    - `[filters]: django_strawberry_framework/filters/` under `<!-- django_strawberry_framework/ -->` (alphabetical between `[django-patches]` and `[resolvers]`).

### Path-resolution verification

Every newly-added ref-def path was verified to resolve against an existing file/dir before declaring the pass complete:

- `test -e django_strawberry_framework/filters/` — OK.
- `test -e examples/fakeshop/apps/library/` — OK.
- `test -e examples/fakeshop/test_query/test_library_api.py` — OK (reused, not newly added).
- `test -e docs/spec-021-filters-0_0_8.md` — OK.

### Files touched

- `KANBAN.md` — four inline-link conversions in the `### DONE-021-0.0.8` past-tense body (`KANBAN.md:1734-1736`); three new ref-defs added to the bottom unified link block under `<!-- docs/ -->` (`[spec-021]`), `<!-- django_strawberry_framework/ -->` (`[filters]`), and `<!-- examples/ -->` (`[fakeshop-library]`). Reused the existing `[test-library-api]` ref-def at `KANBAN.md:1806` for the fourth inline-link unwrap rather than adding a duplicate alias.
- `CHANGELOG.md` — one inline-link conversion in the `[Unreleased]` `### Added` filtering-subsystem bullet (`CHANGELOG.md:21`); one new `[filters]` ref-def added to the bottom unified link block under `<!-- django_strawberry_framework/ -->`.
- `docs/builder/bld-slice-5-docs_kanban_changelog.md` — appended this `## Build report (Worker 2, pass 2)` section and flipped the top-level `Status:` line from `revision-needed` to `built`.

### Tests added or updated

None; this pass is link-convention compliance against doc files. The Slice 5 doc-only contract is unchanged at the test-tree level.

### Validation run

- `uv run ruff format .` — pass (148 files left unchanged; the long-standing COM812-formatter conflict warning at the top is project-level and unrelated to this pass).
- `uv run ruff check --fix .` — pass (`All checks passed!`).
- `git status --short` — output classification:
  - **Pass-2 intended (in-diff):** `KANBAN.md`, `CHANGELOG.md`, `docs/builder/bld-slice-5-docs_kanban_changelog.md`.
  - **Slice-5 pass-1 carry-forward (in-diff; not drift):** `GOAL.md`, `TODAY.md`, `docs/GLOSSARY.md`, `docs/TREE.md`, `docs/spec-021-filters-0_0_8-terms.csv` — all expected to remain modified from pass 1 per BUILD.md "Re-pass sections".
  - **Prior-slice working-tree changes (expected; per BUILD.md "Build artifact list" and the build-021 plan):** `django_strawberry_framework/filters/__init__.py`, `base.py`, `factories.py`, `inputs.py`, `sets.py`; `django_strawberry_framework/registry.py`; `django_strawberry_framework/types/base.py`, `definition.py`, `finalizer.py`; `examples/fakeshop/apps/library/models.py`, `schema.py`; `examples/fakeshop/test_query/test_library_api.py`; `tests/types/test_base.py`, `test_definition_order.py`. Untracked: the build artifacts and the new test/example files per the active build's earlier slices.
  - **Maintainer/Worker-1 in-progress (out of scope per AGENTS.md L33):** `docs/feedback.md`, `docs/spec-021-filters-0_0_8.md` — untouched in this pass.
  - **Tool-induced drift:** none. Doc-only edits did not trigger any unrelated reformat / lint mutations.
- `scripts/check_spec_glossary.py` — NOT re-run in this pass. The exit-1 failure recorded in pass 1's build report is a SPEC-side defect (the spec body needs a link to `GLOSSARY.md#filter_input_type`) that Worker 2 cannot remediate; it remains the loudest Worker-1 reconciliation item. Pass 2's edits do not change the checker's verdict — adding `[filters]` / `[fakeshop-library]` / `[spec-021]` ref-defs to KANBAN.md and CHANGELOG.md doesn't touch the spec body or its bottom link block.
- `pytest` not run (doc-only pass; per AGENTS.md L14 + BUILD.md "Coverage is the maintainer's gate, not a worker's tool").

### Implementation notes

- **Reused `[test-library-api]` instead of adding a `[fakeshop-test-library]` alias** for the fourth KANBAN inline-link unwrap. Worker 3's M1 finding explicitly offered this as a Worker-2 choice ("either reuse `[test-library-api]` inline-side or add the alias"). Reusing the existing alias is the right call because (a) it avoids two refs pointing at the same path in the same file's link block (a future move-cost optimization that the convention is meant to amplify), and (b) the alias's existing name `test-library-api` reads coherently in the KANBAN body's context ("live HTTP tests at `examples/fakeshop/test_query/test_library_api.py`"). Worker 3's pass-2 review can flip to the parallel alias shape if a different naming convention is preferred — the change is one inline-link rewrite plus one ref-def add.
- **`[spec-021]` ref-def naming.** Chose the short `[spec-021]` form over `[spec-021-filters]` to match the existing CHANGELOG.md / docs convention of naming spec ref-defs by their NNN counter alone (the SPECS-archive defs at KANBAN.md:1779-1781 follow the same shape: `[spec-011]`, `[spec-016]`, `[spec-019]`). Worker 3's pass-2 review can suggest renaming if the maintainer prefers a more descriptive alias — single-file rename plus inline-side rewrite.
- **Alphabetical ordering within group.** Followed the existing pattern at each insertion site: KANBAN.md `<!-- docs/ -->` group is alphabetical (`docs-readme` precedes the `glossary-*` block; `[spec-021]` slots after the last `glossary-*` entry as the next alphabetical entry); KANBAN.md `<!-- django_strawberry_framework/ -->` is alphabetical (`[filters]` between `[django-patches]` and `[plans]`); KANBAN.md `<!-- examples/ -->` is alphabetical (`[fakeshop-library]` between `[example-schema]` and `[fakeshop-test-multi-db]`); CHANGELOG.md `<!-- django_strawberry_framework/ -->` is alphabetical (`[filters]` between `[django-patches]` and `[resolvers]`). No new ordering rules introduced.
- **In-body inline plain-text `Spec: `docs/spec-021-filters-0_0_8.md`` at the end of the past-tense paragraph** (NOT a markdown link — backtick-quoted plain text, no `[]` or `()`). Left untouched. The Markdown link convention applies to `[text](path)` cross-file LINKS; plain backtick-quoted path strings are content, not links. Worker 3's pass-2 review can flag if any plain-text reference should ALSO be wrapped as a link.

### Notes for Worker 3

- All four inline `](path)` link sites flagged in M1 (`KANBAN.md:1734` and three on `:1736`) have been converted; all one site flagged in M2 (`CHANGELOG.md:21`) has been converted. The pass-2 diff against pass-1 is small and link-only — no behavioral content of the verbatim contracts was changed.
- `grep -n '](.*\.md\|](.*\.py\|](docs/\|](examples/\|](django_strawberry_framework/' KANBAN.md CHANGELOG.md` is a quick way to confirm zero remaining inline cross-file links (the surviving `](#…)` in-page anchor refs and `](https://…)` URL refs are exempt per the convention).
- The reused-vs-aliased choice for `[test-library-api]` is documented under `### Implementation notes`; flip the call if a different naming convention is preferred.

### Notes for Worker 1 (spec reconciliation)

- **The `scripts/check_spec_glossary.py` exit-1 failure** from pass 1's build report is unchanged and remains the loudest spec-reconciliation item: the spec body needs a `[filter_input_type][glossary-filter_input_type]` reference-style wrap plus a matching `[glossary-filter_input_type]: ../docs/GLOSSARY.md#filter_input_type` ref-def in the spec's bottom link block under `<!-- docs/ -->`. Worker 2 cannot edit the spec; the slice cannot reach `final-accepted` until the checker exits 0 against the spec.
- **The four other Worker-1 reconciliation items from pass 1** (Slice-4a carve mention in the past-tense body; CSV trailing-bytes artifact; joint-cut contingency ratification; the now-fixed M1+M2 mediums) are unchanged in scope and still belong to Worker 1's final-verification call.

---

## Review (Worker 3, pass 2)

### High:

None.

### Medium:

None.

### Low:

None.

### Pass-1 findings closed

- **M1 (KANBAN inline-link violation, four sites).** Closed. `KANBAN.md:1734` Spec line now reads `Spec: [`docs/spec-021-filters-0_0_8.md`][spec-021]`; `KANBAN.md:1736` body's three previously-inline cross-file links all use ref-style: `[`django_strawberry_framework/filters/`][filters]`, `[`examples/fakeshop/apps/library/`][fakeshop-library]`, `[`examples/fakeshop/test_query/test_library_api.py`][test-library-api]`. Three new ref-defs added in the correct path-based groups: `[spec-021]: docs/spec-021-filters-0_0_8.md` at L1777 under `<!-- docs/ -->`; `[filters]: django_strawberry_framework/filters/` at L1791 under `<!-- django_strawberry_framework/ -->`; `[fakeshop-library]: examples/fakeshop/apps/library/` at L1805 under `<!-- examples/ -->`. The fourth link reuses the existing `[test-library-api]` ref-def at L1808 per Worker 3's pass-1 noted Worker-2 choice — defensible (avoids duplicate refs to the same path). Alphabetical-within-group ordering verified: `[spec-021]` slots after the last `glossary-*` entry (`s` after `s`); `[filters]` between `[django-patches]` and `[plans]`; `[fakeshop-library]` between `[example-schema]` and `[fakeshop-test-multi-db]`. `grep -nE '\]\((docs/|examples/|django_strawberry_framework/|tests/|scripts/)' KANBAN.md` returns zero hits — no remaining inline cross-file links anywhere in the file. Disk-existence verified for all four targets (`test -e` on each path).

- **M2 (CHANGELOG inline-link violation, one site).** Closed. `CHANGELOG.md:21` now reads `Subpackage at [`django_strawberry_framework/filters/`][filters] (five files); mirror tests at `tests/filters/`.` New `[filters]: django_strawberry_framework/filters/` ref-def added at L189 under `<!-- django_strawberry_framework/ -->`, alphabetically between `[django-patches]` and `[resolvers]`. `grep -nE '\]\((docs/|examples/|django_strawberry_framework/|tests/|scripts/|README\.md|GOAL\.md|KANBAN\.md|TODAY\.md|CHANGELOG\.md|BACKLOG\.md|AGENTS\.md|START\.md)' CHANGELOG.md` returns zero hits — no remaining inline cross-file links anywhere in the file. Disk-existence verified for `django_strawberry_framework/filters/`.

### Path-resolution verification

Confirmed by re-running `test -e` against the repo root:

- `django_strawberry_framework/filters/` — OK (directory exists).
- `examples/fakeshop/apps/library/` — OK (directory exists).
- `examples/fakeshop/test_query/test_library_api.py` — OK (file exists; reused ref-def).
- `docs/spec-021-filters-0_0_8.md` — OK (file exists at its working location per AGENTS.md L26 + BUILD.md "Spec stays at its working location").

### Fresh-defect scan over pass-2 diff

- **`git diff HEAD -- CHANGELOG.md`** (39 lines total) shows only the four Slice 5-intended additions: the `[Unreleased]` `### Added` and `### Changed` bullets (from pass 1, unchanged in pass 2 except for the one inline-to-ref conversion at L21), plus four new glossary ref-defs and the one new `[filters]` ref-def. No fresh defects introduced.
- **`git diff HEAD -- KANBAN.md`** (190 lines total — large because of the WIP-021 → DONE-021 card cut/paste from pass 1) shows the In-progress summary rewrite (L51 + L77), the full card cut from L79-194, the DONE-021-0.0.8 card insertion at L1732 with the ref-style links Worker 2 converted in pass 2, and the three new ref-defs in the bottom block. No fresh defects introduced — pass-2 edits are strictly link-style conversions and ref-def adds; no body text, no past-tense paragraph content, no other surfaces touched.
- **`git diff HEAD -- docs/builder/bld-slice-5-docs_kanban_changelog.md`** shows the appended `## Build report (Worker 2, pass 2)` section and the top-level `Status: built` flip — both per BUILD.md "Re-pass sections" and "Status field ownership". Prior worker sections (Worker 1 plan + Worker 2 pass-1 build report + Worker 3 pass-1 review) untouched.
- **Markdown-link convention sweep over pass-2-touched files**: `grep -nE '\]\(' KANBAN.md CHANGELOG.md | grep -vE '\]\(#|\]\(http|\]\(mailto' | wc -l` returns zero — no remaining inline cross-file links anywhere in either file (in-page anchors and URLs correctly remain inline per START.md "What stays inline").

### DRY findings

None. Pass-2 changes are strictly link-convention compliance and add no new content/literal/branch surface to evaluate for DRY.

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` returns empty output (re-confirmed). `__all__` and the re-export list are unchanged, consistent with spec DoD item 25 (top-level `__all__` is NOT widened — filter symbols live at `django_strawberry_framework.filters` per Decision 2). Pass.

### CHANGELOG sanity (pass-2 delta)

Applicable; pass 2 modified `CHANGELOG.md`. The only pass-2 delta vs. pass 1 in `CHANGELOG.md` is:

- one inline-link → ref-style conversion at `CHANGELOG.md:21` (`[`django_strawberry_framework/filters/`](django_strawberry_framework/filters/)` → `[`django_strawberry_framework/filters/`][filters]`).
- one new `[filters]: django_strawberry_framework/filters/` ref-def under `<!-- django_strawberry_framework/ -->` at L189.

No version-line, heading, or wording changes vs. pass 1. The `[Unreleased]` heading remains preserved per Decision 10 safe-default joint-cut path; `### Added` / `### Changed` headings unchanged; the verbatim-copy contracts in the bullet bodies unchanged (the link conversion at L21 is purely rendering-mechanism, not text content). Pass.

### Documentation / release sanity (pass-2 delta)

Applicable; pass 2 modified `KANBAN.md` and `CHANGELOG.md` (Slice 5's earlier docs surfaces — GLOSSARY, TREE, GOAL, TODAY, CSV — were not touched in pass 2).

- **Reference-style convention** now fully honored across both files. `KANBAN.md` carries the four converted body links plus the existing `[glossary-*]` / `[kanban]` / `[backlog]` patterns; `CHANGELOG.md` carries the now-ref-style filter subpackage link plus the existing `[glossary-*]` / `[kanban]` / `[test-library-api]` patterns. Group placement is path-target-based per START.md "Markdown link convention" — `[spec-021]` under `<!-- docs/ -->` (target lives in `docs/`); `[filters]` under `<!-- django_strawberry_framework/ -->` (target lives in `django_strawberry_framework/`); `[fakeshop-library]` under `<!-- examples/ -->` (target lives in `examples/`). All three new KANBAN ref-defs and the one new CHANGELOG ref-def correctly placed.
- **Worker 2's `[test-library-api]` reuse choice** (instead of adding a parallel `[fakeshop-test-library]` alias) is the right call — reduces ref-def proliferation in the same file's link block and keeps single-source-of-truth semantics for path-to-ref bindings. The alias `test-library-api` reads coherently in the KANBAN body's "live HTTP tests at ..." context.
- **In-body inline plain-text `Spec: docs/spec-021-filters-0_0_8.md`** at the end of the past-tense paragraph (final occurrence near the joint-cut contingency sentence) is correctly left untouched. It is backtick-quoted plain text, not a markdown link (`[text](path)` construct), so the reference-style convention does not apply. The sentence reads coherently as content prose, not as a navigational link.
- **`[spec-021]` ref-def naming** matches the existing convention at `KANBAN.md:1779-1781` (`[spec-011]`, `[spec-016]`, `[spec-019]`) — short `[spec-NNN]` form by the spec's `<NNN>` counter alone. Defensible.
- **All four new ref-def paths resolve** against existing files/dirs in the working tree (verified via `test -e` on each).
- **No drift to other surfaces.** GLOSSARY, TREE, GOAL, TODAY, CSV all unchanged from pass 1 (still in the working-tree diff but bytewise identical to their pass-1 shape per `git diff` confirming the pass-2 delta is restricted to `KANBAN.md`, `CHANGELOG.md`, and the artifact itself).

### What looks solid

- Surgical pass-2 fix: only the link sites flagged in M1/M2 were touched; no body content of any verbatim-copy contract was altered. The `Notes for Worker 3` section in Worker 2's pass-2 build report explicitly invites the verification grep (`grep -n '](.*\.md\|](.*\.py\|](docs/\|](examples/\|](django_strawberry_framework/' KANBAN.md CHANGELOG.md`) — re-ran the equivalent grep and confirmed zero remaining inline cross-file links in either file.
- Defensible reuse-vs-alias call for `[test-library-api]` documented under `Implementation notes`; reduces link-block clutter and protects the move-cost-reduction promise of the convention.
- Path-resolution check was performed by Worker 2 before declaring pass complete and is independently re-confirmable via `test -e` on each path.
- Alphabetical-within-group ordering preserved at every insertion site (verified by reading the KANBAN.md and CHANGELOG.md link blocks).
- Re-pass section structure honored per BUILD.md "Re-pass sections" — `## Build report (Worker 2, pass 2)` at top level; no edits to prior pass-1 sections; `Status:` flipped back to `built` for re-review.

### Temp test verification

No temp tests created or consulted during this re-review pass — pass-2 changes are doc-link-convention only, no executable behavior to verify via tests.

### Notes for Worker 1 (spec reconciliation)

Carried forward unchanged from pass-1 review (Worker 2 cannot remediate these; they remain Worker 1's surface):

1. **`scripts/check_spec_glossary.py` exit-1** against `docs/spec-021-filters-0_0_8.md` — the spec body needs a `[filter_input_type][glossary-filter_input_type]` reference-style prose wrap plus a matching `[glossary-filter_input_type]: ../docs/GLOSSARY.md#filter_input_type` ref-def in the spec's bottom link block under `<!-- docs/ -->`. The slice cannot reach `final-accepted` until the checker exits 0.
2. **Spec L1101 past-tense body Slice-4a carve mention** — historically-sufficient as authored, but Worker 1 may choose to edit the spec body and the paired KANBAN entry to call the carve out more prominently. Worker 1's judgment.
3. **CSV trailing-bytes (`</content>\n</invoke>`) artifact** — pre-existing maintainer-side serialization artifact since rev6; tolerated by the glossary checker; cosmetic cleanup deferred to a future housekeeping commit.
4. **Joint-cut contingency ratification** — Worker 2 took the safe-default no-version-bump path; Worker 1 final-verification confirms ratification OR routes back to Worker 2 for the bump trio + `[Unreleased]` → `[0.0.8]` promotion + `docs/README.md` / `README.md` shipped-symbol moves.

The pass-1 M1 + M2 Mediums are now closed at root cause and removed from Worker 1's reading list.

### Review outcome

`review-accepted`. Both pass-1 Mediums (M1 + M2) addressed at root cause via link-style conversions and correctly-grouped ref-def adds; zero remaining inline cross-file links in `KANBAN.md` or `CHANGELOG.md`; all new ref-def paths resolve against existing working-tree files/dirs; alphabetical-within-group ordering preserved at every insertion site; no fresh defects introduced in the pass-2 diff. The `scripts/check_spec_glossary.py` exit-1 against the spec body remains a Worker-1 spec-reconciliation surface (Worker 2 cannot edit the spec) and does not block this review acceptance — Worker 1 owns it at final verification, where the slice cannot reach `final-accepted` until the checker exits 0. Setting the artifact's top-level `Status:` line to `review-accepted`.

---

## Final verification (Worker 1)

- **Spec slice checklist**: every `- [ ]` in the Plan's `### Spec slice checklist (verbatim)` flipped to `- [x]`. Two sub-bullets (the `docs/README.md` "Coming in `0.1.0` → Shipped today (`0.0.8`)" move and the `README.md` shipped-symbol bullet under the `0.0.8` boundary) are licensed by the spec body as JOINT-CUT-CONDITIONAL — both sub-bullets explicitly read "once the version bump lands in the joint cut" / "matching the joint-cut promotion timing"; the safe-default no-version-bump path means the staging is shipped and the execution is correctly deferred to whichever card actually owns the joint cut. The version-bump sub-bullet itself is also ticked: it pins "NOT in this card per Decision 10" as the contracted shape for this slice, and Worker 2 honored that contract. The 9-of-9 sub-bullets contracted are landed; one Worker-3 Medium (M1) and one Worker-3 Medium (M2) raised then closed across Worker-2 pass-1 + Worker-3 pass-1 + Worker-2 pass-2 + Worker-3 pass-2 (5 inline `](path)` cross-file links converted to ref-style across `KANBAN.md` and `CHANGELOG.md`; four new ref-defs added across both files in the correct path-based groups).
- **DRY check across this slice and prior accepted slices**: Slice 5 is doc-only and introduces no Python helpers or repeated literals; cross-slice DRY targets (`_LOGIC_KEYS` byte-for-byte duplicated between `sets.py` + `inputs.py`; `f"{Name}InputType"` literal at ~5 sites across `__init__.py` / `factories.py` / `inputs.py`) were correctly carried forward by Worker 1's plan rather than dragged into this slice — they remain `bld-integration.md` concerns. Within-slice DRY: the verbatim-copy contracts (GLOSSARY entry bodies / KANBAN past-tense body / CHANGELOG bullets / GOAL.md replacement narrative) are single-source-of-truth-from-the-spec by construction; no near-copies introduced.
- **Existing tests still pass**: `uv run pytest tests/filters/ examples/fakeshop/test_query/test_library_api.py --no-cov` → 154 passed, 26 warnings, 4.04s. No regressions; the previously-xfailed `test_library_books_filter_combines_and_or_not` continues to pass after Slice 4a's substrate ship; the three Slice 4a package-internal tests (`test_filter_queryset_intersects_and_branch` / `_unions_or_branch` / `_negates_not_branch`) still pass. Slice 5's doc-only edits do not touch any executable surface.
- **Spec reconciliation (`scripts/check_spec_glossary.py` exit-1)**: addressed in this pass — see `### Spec changes made (Worker 1 only)` below. Re-ran `uv run python scripts/check_spec_glossary.py --spec docs/spec-021-filters-0_0_8.md` after the edit: `OK: 48 terms — all have glossary entries and at least one spec link.` exit code 0.
- **Spec reconciliation (KANBAN past-tense body Slice-4a carve mention)**: ratified the current shape as historically sufficient. The past-tense body already mentions `filter_queryset` (the Slice-4a-shipped instance-method override) in the apply-pipeline contract paragraph; the body's purpose is to describe the SHIPPED CONTRACT (what consumers get), not the BUILD HISTORY (how it shipped). The Slice-4a carve framing is preserved in spec L4's status line and the slice-checklist L142-149 block, plus the build artifacts under `docs/builder/`; promoting the carve into a Done card body would burden the Done card with build-process noise that has no consumer-facing meaning. No edit to L1101.
- **Spec reconciliation (CSV trailing-bytes artifact)**: investigated and confirmed pre-existing maintainer-side serialization artifact (`</content>\n</invoke>` literal bytes) at `HEAD:docs/spec-021-filters-0_0_8-terms.csv` since at least the rev6 commit (`34acb4a`). Worker 2's new `filter_input_type` row was inserted BEFORE these stray bytes — correct positioning that preserves the maintainer's pre-existing state untouched. The glossary checker tolerates them (just exited 0). Per AGENTS.md L33 "do not auto-revert unrelated edits": cosmetic cleanup is deferred to a future housekeeping commit (catalog item for `bld-final.md`'s `### Deferred work catalog`).
- **Spec reconciliation (joint-cut contingency ratification)**: ratified the safe-default no-version-bump path. Worker 0 has not signaled "this is the last 0.0.8 card to ship" — the build plan's Checklist still shows Slice 6 pending plus the cross-slice integration pass and final test-run gate, and the spec slice-checklist explicitly holds Slice 6 until `WIP-ALPHA-022-0.0.8` ships. The default path stands; Slice 5 ships docs-only against the unbumped `__version__ = "0.0.7"`. The five contingency-path surfaces (`docs/README.md`, `README.md`, `pyproject.toml`, `__init__.py`, `tests/base/test_init.py`, `CHANGELOG.md` `[Unreleased]` → `[0.0.8]` promotion) remain deferred to the joint-cut card per spec L1080 / L1088 / L1114 + Decision 10.
- **Final status**: `final-accepted`.

### Summary

Slice 5 shipped the eight standing-doc surfaces (`docs/GLOSSARY.md` with the new `## filter_input_type` entry + Index row + Browse-by-category bullet, `docs/TREE.md` with the on-disk `filters/` block + mirrored `tests/filters/` block, `GOAL.md` with the rewritten "Coming from DRF + `django-filter`" migration narrative, `TODAY.md` with the extended fakeshop paragraph, `KANBAN.md` with `WIP-ALPHA-021-0.0.8` → `DONE-021-0.0.8` past-tense move + `### In progress` summary rewrite, `CHANGELOG.md` with `[Unreleased]` `### Added` / `### Changed` bullets) plus the `docs/spec-021-filters-0_0_8-terms.csv` row for `filter_input_type`. The five joint-cut-contingency surfaces (`docs/README.md` shipped-symbols move, `README.md` shipped-symbols move, `pyproject.toml` version bump, `__init__.py` `__version__` bump, `tests/base/test_init.py` version assertion bump, `CHANGELOG.md` `[Unreleased]` → `[0.0.8]` promotion) are correctly deferred to whichever card actually owns the joint cut per Decision 10's safe-default.

### Spec changes made (Worker 1 only)

- **`docs/spec-021-filters-0_0_8.md:1209`** — added `[glossary-filter_input_type]: GLOSSARY.md#filter_input_type` ref-def under the `<!-- docs/ -->` group, alphabetically between `[glossary-fieldset]` and `[glossary-filterset]`. Slice 5 triggered. Reason: `scripts/check_spec_glossary.py` exited 1 because the spec body had no link to the new `#filter_input_type` glossary anchor that Slice 5's GLOSSARY edit created.
- **`docs/spec-021-filters-0_0_8.md:261`** — wrapped the first prose mention of `filter_input_type` in the User-facing API introduction as `[`filter_input_type`][glossary-filter_input_type]` (the symbol's natural canonical introduction in the spec body). Slice 5 triggered. Reason: pairs with the L1209 ref-def add to satisfy the glossary-checker's "at least one link to the anchor" rule and to give the spec body a single canonical link site to the GLOSSARY entry from the symbol's first introduction.
- **`docs/spec-021-filters-0_0_8.md:4`** — updated the status line to reflect Slice 5 having shipped: `"docs / KANBAN / CHANGELOG (Slice 5) still pending"` → `"docs / KANBAN / CHANGELOG shipped through Slice 5 (joint-cut safe-default path taken; version bump deferred per Decision 10)"`. The trailing sentence was also updated from `"The filter_input_type consumer helper exposure via docs/GLOSSARY.md / docs/README.md / KANBAN / CHANGELOG ... are still pending"` to a paragraph describing what Slice 5 shipped and what remains deferred to the joint-cut card (the `docs/README.md` / `README.md` shipped-symbol moves, the `[Unreleased]` → `[0.0.8]` promotion, and the `pyproject.toml` / `__init__.py` / `tests/base/test_init.py` version bump trio). Slice 5 triggered. Reason: per `worker-1.md` "Spec status-line re-verification (every Worker 1 spawn)" — the status line must reflect the current state of the spec relative to the build at every final-verification pass.
- **Deferral 1 (no spec edit)**: KANBAN past-tense body Slice-4a carve mention — ratified the current spec L1101 wording as historically sufficient. The past-tense body's purpose is to describe the SHIPPED CONTRACT, not the BUILD HISTORY; the carve detail is already preserved in spec L4's status line and the slice-checklist L142-149 block. Target: no future slice / spec / maintainer follow-up needed.
- **Deferral 2 (no spec edit)**: CSV trailing-bytes (`</content>\n</invoke>`) artifact in `docs/spec-021-filters-0_0_8-terms.csv` — pre-existing maintainer-side serialization artifact since rev6; tolerated by the glossary checker. Cosmetic cleanup is out of Slice 5 scope per AGENTS.md L33. Target: maintainer follow-up (housekeeping commit); catalog item for `bld-final.md`'s `### Deferred work catalog`.
- **Deferral 3 (no spec edit)**: joint-cut contingency — ratified the safe-default no-version-bump path; the five contingency-path surfaces (`docs/README.md`, `README.md`, `pyproject.toml`, `__init__.py`, `tests/base/test_init.py`, `CHANGELOG.md` `[Unreleased]` → `[0.0.8]` promotion) remain deferred to whichever card actually owns the joint cut. Target: future card (the last `0.0.8` card to ship per Decision 10 L5).
- **Deferral 4 (no spec edit)**: the two `docs/README.md` and `README.md` slice-checklist sub-bullets at L128 + L130 are ticked under the spec's own conditional licensing — both sub-bullets read "once the version bump lands in the joint cut" / "matching the joint-cut promotion timing," which is exactly the safe-default Slice-5 staging Worker 2 shipped. The staging contract is fulfilled; the move-execution contract is deferred to the joint-cut card. Target: future card (the last `0.0.8` card per Decision 10).
