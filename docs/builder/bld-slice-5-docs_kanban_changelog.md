# Build: Slice 5 — Docs + KANBAN + CHANGELOG status updates (no version bump)

Spec reference: `docs/spec-028-orders-0_0_8.md` (Slice 5 checklist at line 128-137; Doc updates section at lines 1048-1092; Decision 10 — Version bumps are maintainer-commanded at lines 790-808; KANBAN past-tense Done body pinned verbatim at line 1080; Slice 5 promotion-and-no-version-bump contract from Revision 5 at lines 60-65)
Status: final-accepted

## Plan (Worker 1)

### DRY analysis

Slice 5 is docs-only — five `.md` files plus `KANBAN.md` plus `CHANGELOG.md`. The DRY risk is the same risk the spec's M10 of rev1 already named: status strings, headline sentences, and the past-tense narrative get copy-pasted across `docs/GLOSSARY.md` entry bodies, the `docs/README.md` shipped-bullet list, the `README.md` shipped-bullet list, the `KANBAN.md` Done card body, the `CHANGELOG.md` Added bullet, and the `TODAY.md` shipped-capabilities enumeration — and the moment any two of them drift, a future reader cannot tell which is canonical. The spec's M10 resolution (the CHANGELOG bullet references the KANBAN past-tense body as the single source of truth and ships only a one-line headline summary) is the load-bearing DRY rule for this slice; every other content edit is **either** verbatim from the spec's Doc updates section (lines 1048-1092) **or** a small structural change (Index table cell, Browse-by-category line, TREE.md `[alpha]` → on-disk move) that has only one site by construction.

- **Existing patterns reused (verbatim sibling-pattern carry-overs from DONE-027-0.0.8's Slice 5).**
  - `CHANGELOG.md` `[Unreleased]` `### Added` shape — the Filtering subsystem bullet at `CHANGELOG.md` line 21 IS the template the Ordering bullet sibling-mirrors. Slice 5 ships ONE Ordering bullet under the same `### Added` heading (the heading already exists; the Filtering subsystem bullet from `DONE-027-0.0.8` already lives there) and ONE `### Changed` bullet under the existing `### Changed` heading (line 23). The `[Unreleased]` heading at line 19 stays as `[Unreleased]` — promotion to `## [0.0.8]` is Spec Decision 10 territory and is explicitly forbidden in this card.
  - `docs/GLOSSARY.md` "Flip status" pattern — verbatim mirror of `DONE-027-0.0.8`'s Slice 5 flip of `FilterSet` / `RelatedFilter` / `filter_input_type` / `Meta.filterset_class` from `planned for 0.0.8` to `shipped (0.0.8)`. Five GLOSSARY entries (`OrderSet`, `Ordering`, `order_input_type`, `RelatedOrder`, `Meta.orderset_class`) take the same flip; the bodies update per the Spec Doc updates section's pinned text at lines 1050-1057.
  - `docs/GLOSSARY.md` Index table status column — Filtering subsystem rows already flipped to `shipped (0.0.8)` in DONE-027-0.0.8. Slice 5 flips the five Ordering rows at lines 91 / 97 / 98 / 99 / 105 of the current `docs/GLOSSARY.md`.
  - `docs/GLOSSARY.md` Browse-by-category Ordering line at line 130 already enumerates the five symbols — no body edit needed there beyond verifying every entry's anchor still resolves after the body edits (the anchors `#orderset` / `#ordering` / `#relatedorder` / `#order_input_type` / `#metaorderset_class` are stable; only the bodies change).
  - `docs/README.md` shipped-bullet list (line 97 onwards) — mirror of DONE-027-0.0.8's Slice 5 addition (the filter bullet at line 114 IS the template). Slice 5 adds one Ordering bullet alongside it; Filtering bullet stays untouched.
  - `README.md` Status paragraph (line 45-52) — DONE-027-0.0.8 left "Newest shipped surface: the filtering subsystem (`0.0.8`)" as the canonical phrasing. Slice 5 updates the paragraph to name Ordering as the newest shipped surface and demotes Filtering to "alongside the filter symbols promoted in DONE-027-0.0.8" (the spec's pinned wording at line 1068 carries this verbatim).
  - `docs/TREE.md` `[alpha]` → on-disk move — DONE-027-0.0.8 already moved `filters/` from the target-layout block to the current-on-disk block; Slice 5 does the same for `orders/`. The `orders/` directory listing in the target-layout block (lines 278-283 of the current `docs/TREE.md`) has a Slice 5 TODO anchor at line 283 that is removed by this slice; the matching `tests/orders/` listing in the target-shape block (lines 461-464) is already pinned and just needs to be promoted to the current-shape block at the same time the source listing moves.
  - `KANBAN.md` column-move pattern — every prior `DONE-NNN-X.Y.Z` card ships the same Done-column shape: a single past-tense bullet body, no future-tense / "planned" language anywhere in the card body. Slice 5 follows the same shape for the Ordering card; the spec pins the body text verbatim at line 1080.
  - `TODAY.md` "library" current-state paragraph at line 19 already carries the filter sibling-narration ("The new `BranchFilter` / `ShelfFilter` / `BookFilter` / `LoanFilter` / `PatronFilter` declarations at `examples/fakeshop/apps/library/filters.py` (plus `GenreFilter` at `filters_genre.py` for the cross-module absolute-import-path case) and the 14 live `/graphql/` HTTP filter tests at `examples/fakeshop/test_query/test_library_api.py` exercise the `FilterSet` / `RelatedFilter` / `Meta.filterset_class` wiring end-to-end through finalizer phase 2.5."). Slice 5 appends a parallel sentence naming the five orderset classes + `GenreOrder` cross-module fixture + 14 live order HTTP tests. The TODO anchor at lines 11-17 of `TODAY.md` is removed in the same edit.

- **New helpers justified (none).** Slice 5 ships zero net-new shared helpers. No `.py` files added or modified. Every doc edit is a single-site change to a `.md` file (or its source-of-truth SQLite row, for `KANBAN.md`).

- **Duplication risk avoided (four risks named explicitly).**
  - **Risk: drift between the KANBAN past-tense body and the CHANGELOG bullet.** Naive implementation would copy-paste the full ~600-word past-tense narrative into both the `KANBAN.md` Done card AND the `CHANGELOG.md` `### Added` bullet, exactly the duplication the spec's M10 of rev1 already named. **Avoidance:** the spec's pinned CHANGELOG bullet at line 1087 is **explicitly** a one-line headline that references the KANBAN body as the single source of truth — `"Ordering subsystem — see the KANBAN past-tense body above for the canonical narrative … Headline: OrderSet / RelatedOrder / Meta.orderset_class shipped … 14 live HTTP tests in examples/fakeshop/test_query/test_library_api.py. Spec: docs/spec-028-orders-0_0_8.md."`. Worker 2 ships exactly that one-line headline; the long-form narrative lives only in `KANBAN.md`'s Done card. Worker 3's `diff` against the spec catches verbatim drift.
  - **Risk: docs/GLOSSARY.md entry body drift from the spec's pinned text.** The spec lines 1050-1056 each pin one entry-body update sentence with specific clauses (e.g., line 1051's `OrderSet` body mentions "active-input-only scope plus active-branch double-dispatch for `RelatedOrder` branches"; line 1052's `RelatedOrder` body mentions "sibling import from `sets_mixins.LazyRelatedClassMixin`" — wording that is load-bearing for the H1 of rev3 retraction). **Avoidance:** Worker 2 quotes each spec body sentence verbatim into the GLOSSARY entry, then layers ordering-specific detail beyond the pinned sentence only as the entry's overflow text. Worker 3 diffs the new GLOSSARY entry bodies against the spec's pinned text at lines 1050-1056 and flags any verbatim drift.
  - **Risk: shipped-bullet-list drift between `docs/README.md` and `README.md`.** The spec's Doc updates section at lines 1060 + 1068 names the same five symbols for both shipped lists (`OrderSet` / `RelatedOrder` / `order_input_type` / `Ordering` / `Meta.orderset_class` in `docs/README.md`; `OrderSet` / `RelatedOrder` / `order_input_type` / `Ordering` in `README.md`). M3 of rev3 explicitly requires `Ordering` in both lists. **Avoidance:** the plan pins both lists to enumerate the same four symbols in the same order (`OrderSet` first, then `RelatedOrder`, then `Ordering`, then `order_input_type`); `Meta.orderset_class` appears only in `docs/README.md` (per Spec line 1060 — the deeper `docs/README.md` shipped list is the only one to enumerate `Meta` keys; the top-level `README.md` status paragraph stays one tier higher and does not enumerate `Meta` keys). Worker 3 confirms `Ordering` appears in both shipped lists per M3 of rev3.
  - **Risk: TODO-anchor leakage post-Slice-5.** The spec's Slice 5 sub-bullets (lines 129-137) implicitly remove every `TODO(spec-028-orders-0_0_8 Slice 5)` anchor in the standing docs: `docs/GLOSSARY.md` line 705 (Meta.orderset_class entry TODO comment block), `docs/README.md` line 90 (shipped-list TODO comment block), `README.md` line 45 (Status paragraph TODO comment block), `docs/TREE.md` line 283 (orders/inputs.py target-layout TODO comment), `TODAY.md` line 11 (library current-state TODO comment block). Five anchors total. Worker 2 deletes all five during the Slice 5 doc-flip; Worker 3 verifies via `grep -rE "TODO\(spec-028-orders-0_0_8 Slice 5\)" docs/ README.md TODAY.md KANBAN.md CHANGELOG.md` returning empty.

### Implementation steps

Slice 5 edits eight files: `docs/GLOSSARY.md`, `docs/README.md`, `docs/TREE.md`, `README.md`, `TODAY.md`, `KANBAN.md`, `CHANGELOG.md`, plus the kanban SQLite source-of-truth at `examples/fakeshop/db.sqlite3` (per the KANBAN.md preamble line 7: KANBAN.md is a rendered artifact; the source is the `kanban` Django app's database). `GOAL.md` is explicitly NOT edited — the spec's Slice 5 sub-bullet (line 133) confirms the astronomy showcase already references `Meta.orderset_class` and the per-app `orders.py` shape, so no edit is needed beyond confirming the references resolve (which they do — `Meta.orderset_class` resolves to `docs/GLOSSARY.md#metaorderset_class` after the flip; `orders.py` is the new on-disk file under `examples/fakeshop/apps/library/`).

**Additive-layering discipline for baseline-dirty files (load-bearing for Worker 2).** Three files (`docs/GLOSSARY.md`, `docs/TREE.md`, `TODAY.md`) are listed in the build plan's baseline-dirty out-of-scope list (lines 11-46 of `docs/builder/build-028-orders-0_0_8.md`), BUT the build plan explicitly carves out Slice 5 (line 51): "Slice 5 must still edit `docs/GLOSSARY.md`, `docs/TREE.md`, and `TODAY.md` (per the spec's Slice 5 contract). Worker 1 / Worker 2 / Worker 3 should treat those three files as in-scope for Slice 5 only and additive (do not revert maintainer changes already in the working tree at plan time; layer Slice 5's diffs on top). The other baseline-dirty paths are strictly out of scope for every slice." Worker 2's discipline:
- (a) Read the CURRENT state of `docs/GLOSSARY.md`, `docs/TREE.md`, and `TODAY.md` (the live working tree as of Slice 5 build time — NOT the pre-build state). The maintainer has been editing those files concurrently (e.g., `docs/GLOSSARY.md` already carries new `SyncMisuseError` entry and a few `convert_relation` → `resolved_relation_annotation` reference fixes; `docs/TREE.md` carries the same `convert_relation` → `resolved_relation_annotation` fix).
- (b) Layer Slice 5's diffs ON TOP of the maintainer's in-progress changes. Do NOT revert any line the maintainer has touched (the `SyncMisuseError` entry stays, the `convert_relation` → `resolved_relation_annotation` reference stays, etc.).
- (c) Only edit the lines Slice 5's contract explicitly names: the five Ordering Index table rows in `docs/GLOSSARY.md`; the five Ordering entry bodies plus the TODO anchor block at line 705; the `orders/` block in `docs/TREE.md` plus its `tests/orders/` mirror plus the line-283 TODO anchor; the TODAY.md library current-state paragraph + Shipped-capabilities enumeration + the line-11 TODO anchor block.

**Pre-plan grep sanity (per the dispatch prompt).** Both stale-count probes return clean for standing docs:
- `grep -nE "15.*live HTTP" docs/ README.md TODAY.md KANBAN.md CHANGELOG.md` returns only one hit at `docs/spec-028-orders-0_0_8.md:38` (the revision-history line that LITERALLY says "Updated to 'Exactly 13 new live HTTP tests'" — a historical correction breadcrumb, not an executable count claim; ALSO it says "13" not "15", and the rev4 contract pass corrected the count to 14, so this revision-history line is itself accurate as a historical record). NO straggler "15 live HTTP" appears in any standing doc that Slice 5 must edit.
- `grep -nE "two M7" docs/ README.md TODAY.md KANBAN.md CHANGELOG.md` returns ONLY artifact hits (in `docs/builder/worker-memory/worker-1.md` line 47 and the bld-slice-4 artifact lines 251 / 345 / 489 / 516). These are per-cycle scratchpad artifacts exempt from standing-doc cleanup per `AGENTS.md` line 27 + `START.md` "Temp artifact conventions". No standing doc carries a stale "two M7" reference.

**Result:** Slice 5 has no count-harmonization work beyond what the spec's Doc updates section already pins. The "14 live HTTP tests" count is canonical across the spec body, Decision 13, Implementation-plan row, KANBAN past-tense body (spec line 1080), and CHANGELOG headline (spec line 1087) — Slice 5 carries the same count verbatim into the docs surface.

**File-by-file edits.** Line anchors below are pin-at-write-time navigational hints per BUILD.md Implementation steps note; Worker 2 verifies against the live source before editing.

1. **`docs/GLOSSARY.md`** — five entry flips + Index table updates + TODO-anchor removal.
   - **Index table (lines 91 / 97 / 98 / 99 / 105).** Flip the status column for `Meta.orderset_class` (line 91), `Ordering` (line 97), `OrderSet` (line 98), `order_input_type` (line 99), `RelatedOrder` (line 105) from `planned for 0.0.8` to `shipped (0.0.8)`. Five one-cell edits. Mirror exactly the cell-shape used for the Filtering subsystem rows (e.g., `[`FilterSet`](#filterset) | shipped (`0.0.8`)` at line 72).
   - **Browse-by-category Ordering line (line 130).** No edit needed — the line already enumerates `OrderSet`, `RelatedOrder`, `Ordering`, `order_input_type`, `Meta.orderset_class` in the expected shape; the anchors all resolve unchanged. Verify the line still reads as `- **Ordering:** [`OrderSet`](#orderset) · [`RelatedOrder`](#relatedorder) · [`Ordering`](#ordering) · [`order_input_type`](#order_input_type) · [`Meta.orderset_class`](#metaorderset_class).` after the body edits land. No body edit; this is a verification step.
   - **`Meta.orderset_class` entry (lines 697-712).** Flip the `**Status:**` line from `planned for 0.0.8` to `shipped (`0.0.8`)`. Update entry body per Spec line 1053: "describe the consumer-facing wiring and the promotion-from-`DEFERRED_META_KEYS` gate." Slice 5 body ships ~3 sentences: (a) what the key does (references an `OrderSet` subclass that defines ordering input for this `DjangoType`; surfaces as the `orderBy: [<T>OrderInputType!]` argument on plain `@strawberry.field` resolvers in `0.0.8`, and on `DjangoConnectionField` when it ships in `0.0.9`); (b) the promotion gate (no longer in `DEFERRED_META_KEYS` since `0.0.8`; finalizer phase 2.5 owns the binding); (c) the binding shape (`Meta.orderset_class = MyOrder` wires `MyOrder._owner_definition` to the owning `DjangoType` and materializes the input class as a `django_strawberry_framework.orders.inputs` module global). Delete the TODO anchor block at lines 705-712 (10 lines including the trailing blank line).
   - **`Ordering` entry (lines 790-796).** Flip the `**Status:**` line from `planned for 0.0.8` to `shipped (`0.0.8`)`. Update body per Spec line 1055: "the implemented direction enum (six members: `ASC` / `DESC` / `ASC_NULLS_FIRST` / `ASC_NULLS_LAST` / `DESC_NULLS_FIRST` / `DESC_NULLS_LAST`); `resolve(field_path)` method returning Django `OrderBy` expressions." The current body already covers the six members and the `resolve()` shape; Slice 5 confirms the existing prose remains accurate, adds the implementation-time detail that `resolve()` returns `F(field_path).asc(nulls_first=...)` / `desc(nulls_last=...)` per the `Ordering.resolve` contract (rev4 M4 of rev1).
   - **`OrderSet` entry (lines 798-804).** Flip the `**Status:**` line from `planned for 0.0.8` to `shipped (`0.0.8`)`. Update body per Spec line 1051: "declarative `Meta.model` / `Meta.fields` (list form or `"__all__"` shorthand for every column-backed model field — includes forward FK / OneToOne columns; excludes reverse relations and M2M managers per B4 of `docs/feedback.md` rev3); `RelatedOrder` for cross-relation traversal; `check_*_permission` denial gates with **active-input-only scope** plus active-branch double-dispatch for `RelatedOrder` branches (parent's `check_<branch>_permission` fires alongside child orderset's field gates, deduped per `(OrderSet class, method name)` per H3 of rev3); list-shaped `orderBy:` argument with list-element-order tie-breaker mechanism; six-member `Ordering` enum with NULLS positioning; cycle-safe lazy resolution via the five-layer port + Layer 6 deferred to `0.0.9`."
   - **`order_input_type` entry (lines 806-812).** Flip the `**Status:**` line from `planned for 0.0.8` to `shipped (`0.0.8`)`. Update body per Spec line 1054: "factory returning the **element type** `Annotated["<Name>OrderInputType", strawberry.lazy("django_strawberry_framework.orders.inputs")]` for resolver-argument annotations; eager validation; consumer usage `order_by: list[order_input_type(BranchOrder)] | None = None` (the list wrap matches the `orderBy: [<T>OrderInputType!]` list-shaped GraphQL argument); orphan validation at finalize." The current body already names the element-type contract and the list-wrap pattern; verify the prose remains accurate against the shipped helper signature.
   - **`RelatedOrder` entry (lines 880-886).** Flip the `**Status:**` line from `planned for 0.0.8` to `shipped (`0.0.8`)`. Update body per Spec line 1052: "describe target acceptance (class / absolute path / unqualified name), the shared Layer-2 module-fallback resolution (sibling import from `sets_mixins.LazyRelatedClassMixin` — the neutral shared module per H1 of `docs/feedback.md` rev3, not `filters.base` as stated in earlier revisions)." Match the shape of the shipped `RelatedFilter` entry at lines 870-878 (target acceptance + Layer-2 resolution + lazy-resolution pattern + final sentence on `check_*_permission` composition). Per Spec Rev4 H4 (position-side-channel leak), the entry body should ALSO carry a one-sentence note that ordering by a hidden related column changes the *position* of visible parent rows based on data the user cannot read, and consumers' defense is the parent-side `check_<branch>_permission` gate on the `RelatedOrder` branch (rev3 H3 active-branch double-dispatch).
   - **Verbatim-copy contract.** Per BUILD.md "Documentation / release sanity" review trigger AND the dispatch-prompt verbatim-copy contract: each of the five entry-body updates above should be character-for-character checked against the spec's Doc updates section (lines 1050-1056). Worker 3's review `diff`s the post-edit GLOSSARY entries against the spec.

2. **`docs/README.md`** — shipped-bullet-list addition + TODO-anchor removal.
   - **Shipped-bullet list (line 97 onwards).** Add ONE bullet per Spec line 1060 ("Move `OrderSet` / `Meta.orderset_class` / `RelatedOrder` / `order_input_type` / `Ordering` from `Coming in 0.1.0` to `Shipped today (0.0.8)`"). Bullet body mirrors the shape of the filter sibling at line 114 (`- filtering subsystem (new in 0.0.8) — ...`). The Ordering bullet text: `- ordering subsystem (new in 0.0.8) — `OrderSet` declarative ordering classes with `Meta.fields` (list form or `"__all__"` shorthand for every column-backed model field); `RelatedOrder` cross-relation ordering traversal (class / absolute import path / unqualified-name); `Meta.orderset_class` consumer wiring (promoted out of `DEFERRED_META_KEYS`); the public `Ordering` enum (six members with NULLS positioning); the `order_input_type` resolver-argument helper; finalizer phase-2.5 binding with orphan validation; per-field `check_*_permission` denial gates with active-input-only scope plus active-branch dispatch on `RelatedOrder` branches; clean composition with the shipped filter subsystem and the optimizer. See [`GLOSSARY.md#orderset`][glossary-orderset].`
   - **"Coming in 0.1.0" block (lines 117-123).** Remove "orders" from line 119 (`- orders and permissions / cascade permissions` becomes `- permissions / cascade permissions`). Verify no other "orders" or "ordering" reference appears in the `Coming in 0.1.0` block.
   - **Add new link def for `glossary-orderset`** in the bottom `<!-- LINK DEFINITIONS -->` block under the `<!-- docs/ -->` group (alphabetical insertion alongside the existing `[glossary-filterset]` at line 263). Path: `glossary-orderset]: GLOSSARY.md#orderset`. Worker 2 may also add `[glossary-ordering]`, `[glossary-relatedorder]`, `[glossary-order_input_type]`, `[glossary-metaorderset_class]` if the new bullet body references them; Slice 5 leaves that micro-decision as a Worker-2 discretion item (the spec doesn't pin which subset of anchors get ref-defs).
   - **TODO anchor block at lines 90-95.** Delete the entire 6-line HTML comment block.

3. **`docs/TREE.md`** — `[alpha]` → on-disk move for `orders/` + `tests/orders/` mirror promotion + TODO-anchor removal.
   - **Current on-disk layout block (lines 188-239).** Insert the `orders/` subpackage entry between the existing `filters/` block (lines 228-233) and the trailing `utils/` block at line 234. The five files: `__init__.py`, `base.py`, `sets.py`, `factories.py`, `inputs.py`. Per-file comments mirror the shape of the `filters/` block (subsystem name + per-file responsibility). Suggested shape:
     ```text
     ├── orders/                  # Ordering subsystem (Layer 3 read-side) — shipped 0.0.8
     │   ├── __init__.py          # OrderSet, RelatedOrder, Ordering, order_input_type re-exports + _helper_referenced_ordersets ledger
     │   ├── base.py              # RelatedOrder primitive; LazyRelatedClassMixin re-imported from ..sets_mixins
     │   ├── sets.py              # OrderSet + OrderSetMetaclass + apply_sync / apply_async + check_permissions active-input-only scope
     │   ├── factories.py         # OrderArgumentsFactory BFS + _type_orderset_registry (no _dynamic_orderset_cache — Layer 6 deferred to 0.0.9)
     │   └── inputs.py            # input-class module-globals namespace + Ordering enum + materialize_input_class + clear_order_input_namespace + normalize_input_value
     ```
   - **Target package layout block (lines 241-328).** Remove the existing `orders/` block at lines 278-283 (six lines including the trailing TODO-anchor line). The block name + bracket-tag `[alpha]` carry the responsibility annotation that no longer applies post-Slice-5 promotion.
   - **Current shape `tests/` block (lines 338-402).** Insert the `orders/` mirror entry between the existing `filters/` block (lines 372-378) and the existing `utils/` block at line 379. Per Spec line 1064 + DoD item 11: seven files (`__init__.py`, `test_base.py`, `test_sets.py`, `test_factories.py`, `test_inputs.py`, `test_finalizer.py`, `test_composition.py`). Suggested shape:
     ```text
     ├── orders/                  # mirrors django_strawberry_framework/orders/
     │   ├── __init__.py
     │   ├── test_base.py         # ← RelatedOrder + LazyRelatedClassMixin sibling-import behaviour
     │   ├── test_sets.py         # ← OrderSet + OrderSetMetaclass + apply_sync / apply_async + active-input-only check_permissions
     │   ├── test_factories.py    # ← OrderArgumentsFactory BFS + per-module input-class namespace
     │   ├── test_inputs.py       # ← Ordering enum + materialize_input_class + clear_order_input_namespace + normalize_input_value + order_input_type
     │   ├── test_finalizer.py    # ← phase 2.5 binding + Meta.orderset_class promotion + orphan validation
     │   └── test_composition.py  # ← filter + order composition smoke (Slice 6)
     ```
   - **Target shape `tests/orders/` block (lines 461-465).** Existing target-shape block already enumerates three files (`test_base.py`, `test_sets.py`, `test_factories.py`). Update to match the seven-file on-disk reality named above, OR (alternate disposition — Worker-2 discretion) simply remove the target-shape `tests/orders/` block since the subsystem has graduated to on-disk; the target-shape block is for not-yet-on-disk subsystems. Slice 5 plan recommends the latter (clean removal) for symmetry with how `filters/` is handled — but Worker 2 may choose either shape so long as the on-disk reality is the canonical listing.
   - **TODO anchor at line 283** is removed as part of the orders/ block removal in the target-layout section.

4. **`README.md`** — Status paragraph update + TODO-anchor removal.
   - **Status paragraph (lines 45-52).** Rewrite to name Ordering as the newest shipped surface. Pinned wording shape (drawn from the spec's revision history at line 60 + DoD item 14):
     > `**`0.0.8`, single-maintainer, alpha-quality.** Fine for internal tools and prototypes; not production. The public names are stable; correctness and edge-case behavior are still hardening. Newest shipped surface: the ordering subsystem (`0.0.8`) — `OrderSet` declarative ordering classes, `RelatedOrder` cross-relation ordering traversal, the public `Ordering` enum (six members with NULLS positioning), the `order_input_type` consumer helper, `Meta.orderset_class` wiring, and finalizer phase-2.5 binding — alongside the filter symbols promoted in `DONE-027-0.0.8`'s Slice 5 (`FilterSet`, `RelatedFilter`, `filter_input_type`, `Meta.filterset_class`).`
   - **TODO anchor block at lines 45-50.** Delete the 6-line HTML comment block (NOTE: the line range overlaps with the rewritten Status paragraph; the rewrite naturally subsumes the deletion). Per M3 of rev3, `Ordering` MUST be listed — the pinned wording above carries it.

5. **`TODAY.md`** — Shipped-capabilities extension + library current-state extension + TODO-anchor removal.
   - **TODO anchor block at lines 11-17.** Delete the 7-line HTML comment block.
   - **Library current-state paragraph (line 19).** Append a parallel sentence after the existing filter sentence. Wording shape (pinned by the spec's Doc updates section at line 1075):
     > `The new `BranchOrder` / `ShelfOrder` / `BookOrder` / `LoanOrder` / `PatronOrder` declarations at `examples/fakeshop/apps/library/orders.py` (plus `GenreOrder` at `orders_genre.py` for the cross-module absolute-import-path case) and the 14 live `/graphql/` HTTP order tests at `examples/fakeshop/test_query/test_library_api.py` exercise the `OrderSet` / `RelatedOrder` / `Meta.orderset_class` / `Ordering` / `order_input_type` wiring end-to-end through finalizer phase 2.5, including filter+order composition on the `allLibraryBooks` resolver.`
   - **"What fakeshop model fields work today" or sibling "Shipped capabilities available but not currently demonstrated in fakeshop" section.** The spec's Doc updates section at line 1074 says: "Extend the 'Shipped capabilities' enumeration with `OrderSet` / `Meta.orderset_class` / `RelatedOrder` / `Ordering` / `order_input_type`." The current `TODAY.md` does NOT have a literal heading called "Shipped capabilities" — the closest sections are "What fakeshop model fields work today" (line 156) and "Shipped capabilities available but not currently demonstrated in fakeshop" (line 276). The library current-state paragraph at line 19 already enumerates filter capabilities inline; the parallel order narration above is the canonical extension. **Worker 2 discretion:** also append a short bullet to the "Shipped capabilities available but not currently demonstrated in fakeshop" section ONLY IF an Ordering capability genuinely is shipped-but-not-demonstrated — which it isn't (every shipped Ordering symbol IS demonstrated via the 14 live HTTP tests). So Slice 5's "Shipped capabilities" extension lands as the library current-state paragraph appendage only. Worker 3 confirms.
   - **Products section dependency list (lines 26-33).** Remove `- `Meta.orderset_class`` from the unshipped-APIs list at line 30. The other four unshipped APIs (`DjangoConnectionField` / `apply_cascade_permissions` / `Meta.aggregate_class` / `Meta.fields_class` / `Meta.search_fields`) stay.
   - **"What the fakeshop example should wait for" section (lines 268-274).** Remove `- ordering, aggregates, and fieldsets` line, replace with `- aggregates and fieldsets` (or `- aggregate sets and field sets` to match the surrounding voice). Ordering is no longer unshipped.

6. **`KANBAN.md`** + SQLite source-of-truth at `examples/fakeshop/db.sqlite3`.
   - Per `KANBAN.md` line 7: KANBAN.md is a rendered artifact, not a source. Edits go through the `kanban` Django app's `Card` / `CardItem` rows in the SQLite database, then `scripts/build_kanban_md.py` + `scripts/build_kanban_html.py` regenerate `KANBAN.md` and `KANBAN.html`.
   - **Card-id renumber.** The current `WIP-ALPHA-028-0.0.8` card moves to `DONE-028-0.0.8`. **Next available DONE-NNN id confirmed:** `DONE-028-0.0.8` is the next sequential id after the existing `DONE-027-0.0.8` (Filtering subsystem at line 1553 of `KANBAN.md`). No re-numbering of prior DONE cards is needed; the NNN sequence stays monotonic.
   - **Column move.** The card moves from the `## In progress` column (lines 130-246 of `KANBAN.md`) to the top of the `## Done` column (currently `DONE-027-0.0.8` Filtering subsystem at line 1553). The Done column lists most-recent-first, so `DONE-028-0.0.8` slots in BEFORE `DONE-027-0.0.8`.
   - **Card body — past-tense Done body, pinned verbatim.** The spec at line 1080 pins the past-tense body character-for-character. Worker 2 copies the spec line 1080 body into the `Card.body` (or the matching `CardItem` rows) verbatim. The spec body covers: shipped symbols, subpackage layout, five-layer architecture, `LazyRelatedClassMixin` sibling-import, Layer 6 deferral, six-member `Ordering` enum + NULLS positioning, list-shaped `orderBy:` tie-breaker mechanism, classmethod-pair resolver-facing API, active-input-only scope, `order_input_type(BranchOrder)` helper + orphan validation, `_helper_referenced_ordersets` clear via `registry.clear()`, finalizer phase 2.5 four subpasses, library fakeshop `orders.py` + `orders_genre.py`, 14 live HTTP tests' axes (scalar ASC / DESC_NULLS_LAST on `Book.subtitle` / forward-FK / reverse-FK denormalized-multiplicity / M2M absolute-import-path / flat-shorthand path / filter+order composition / optimizer cooperation / root `get_queryset` honoring / split-pair active-input-only / active-branch relation-level / multi-field priority / combined empty-list+null-direction no-op), and the no-version-bump note ("no version files change here unless the maintainer explicitly gives the version-bump command").
   - **Card Status / Priority / Severity / size labels.** Update `Status` field from `In progress` to `Shipped`. Leave `Priority: High`, `Parity: ⚛️ graphene-django (Required), 🍓 strawberry-graphql-django (Required)`, `Severity: Major`, `Relative size: L`, `Labels` unchanged (every prior DONE card preserves the same tag block; only the Status label flips).
   - **Glossary terms table.** The card's Glossary-terms table at lines 147-191 currently shows the five Ordering symbols with `planned for 0.0.8`. Update those five rows to `shipped (0.0.8)`. (Note: this table is also data-driven from the kanban-app database; the build script regenerates it from the live `docs/GLOSSARY.md` Index table when the markdown is rebuilt — so flipping the GLOSSARY index in step 1 is what propagates to KANBAN. Worker 2 should verify post-rebuild that the KANBAN glossary-terms table reflects `shipped (0.0.8)` for all five Ordering rows after running `scripts/build_kanban_md.py`.)
   - **Definition of done block (lines 204-212).** The current card body's `Definition of done` carries the spec's planning-time skeleton:
     ```
     - [ ] Add `docs/spec-orders.md`.
     - [ ] Add `django_strawberry_framework/orders/`.
     - [ ] Add mirrored `tests/orders/`.
     - [ ] Promote `Meta.orderset_class` only when ordering is applied end-to-end.
     - [ ] Support simple fields and relation paths.
     - [ ] Define interaction with filters and connection field.
     - [ ] Keep ordering declarations introspectable from the owning type/query surface.
     ```
     Per Spec line 1081: rewrite bullet 1 — `docs/spec-orders.md` becomes `docs/spec-028-orders-0_0_8.md`. Per the column-move convention: when a card moves to Done, the DoD bullets get checked off (`- [ ]` → `- [x]`) and the body text stays as historical record of what the card delivered. Slice 5's edits flip all seven DoD bullets to `- [x]` (each one is genuinely satisfied by the shipped slices 1-4) AND rewrite bullet 1's path.
   - **In-progress summary paragraph (line 132).** Per Spec line 1082: remove `WIP-ALPHA-028-0.0.8` from the active list. The paragraph becomes `"No active WIP cards"` (or whatever the kanban-app's default empty-in-progress string is) until the next 0.0.9 card moves in.
   - **Snapshot/In-progress paragraph at line 73-74.** The line currently says `0.0.8 is the active patch. WIP-ALPHA-028-0.0.8 (Ordering subsystem) is the only card currently in progress for this patch; the Filtering subsystem shipped as DONE-027-0.0.8.` Update to reflect both subsystems shipped: `0.0.8 ships the Filtering and Ordering subsystems. DONE-028-0.0.8 (Ordering subsystem) is the most recent ship; the Filtering subsystem shipped as DONE-027-0.0.8 earlier in the patch. No active WIP cards at present.`
   - **Spec map row (line 101).** The current line `| WIP-ALPHA-028-0.0.8 — Ordering subsystem | [spec-028-orders-0_0_8.md](docs/spec-028-orders-0_0_8.md) |` becomes `| DONE-028-0.0.8 — Ordering subsystem | [spec-028-orders-0_0_8.md](docs/spec-028-orders-0_0_8.md) |`. (The spec stays at its working location per AGENTS.md line 26 + Spec Revision 5; no archive move in this card.)
   - **Cross-reference card-body strings.** Search KANBAN.md for `WIP-ALPHA-028-0.0.8` (current case) — the string appears at lines 74, 101, 132, 135, 246/319/328/355/369/394/410/431 (cross-references from other planned cards naming the Ordering card as a dependency or related card). Every occurrence updates to `DONE-028-0.0.8` post-Slice-5. Worker 2 verifies the global rename via `grep -c "WIP-ALPHA-028-0\.0\.8" KANBAN.md` returning `0` after the SQLite update + rebuild.

7. **`CHANGELOG.md`** — append to existing `[Unreleased]` headings; no version-heading promotion.
   - Per Spec Decision 10 (line 790-808) + Revision 5 (line 60-65) + the build-plan's "No version bump in this card" build-wide flag (line 55): `[Unreleased]` heading at line 19 stays as `[Unreleased]`. No `## [0.0.8] - <date>` heading is created. The version-file edits (`pyproject.toml`, `django_strawberry_framework/__init__.py::__version__`, `tests/base/test_init.py::test_version`) are explicitly forbidden by Spec line 137.
   - **`### Added` bullet append.** The Spec at line 1087 pins the wording verbatim:
     > `Ordering subsystem — see the KANBAN past-tense body above for the canonical narrative (per M10 of `docs/feedback.md` rev1, the CHANGELOG and KANBAN past-tense bodies are not duplicated; this CHANGELOG bullet references the KANBAN body as the single source of truth). Headline: `OrderSet` / `RelatedOrder` / `Meta.orderset_class` shipped at `django_strawberry_framework/orders/` (five files); public `Ordering` enum (six members with NULLS positioning); cycle-safe five-layer lazy-resolution pipeline (Layer 6 + DISTINCT ON deferred to `0.0.9`); consumer-facing `order_input_type(OrderSet)` helper; finalizer phase-2.5 `_bind_ordersets()` mirroring the shipped filter side's discipline; 14 live HTTP tests in `examples/fakeshop/test_query/test_library_api.py`. Spec: `docs/spec-028-orders-0_0_8.md`.`
     Worker 2 inserts this as a new bullet under the existing `### Added` heading (immediately after the Filtering subsystem bullet at line 21). The reference style for cross-file links inside the bullet (e.g., `[KANBAN.md][kanban]`) follows AGENTS.md line 28's ref-style convention; Worker 2 may opt to keep the spec's pinned text verbatim with inline ref-style links matching the existing CHANGELOG link def block at lines 162-225 (the `[kanban]` ref-id is already defined at line 180; add the new ref-defs for `[glossary-orderset]` / `[glossary-relatedorder]` / `[glossary-metaorderset_class]` / `[glossary-ordering]` / `[glossary-order_input_type]` / `[glossary-configurationerror]` under the `<!-- docs/ -->` group at lines 182-195; add `[orders]` under the `<!-- django_strawberry_framework/ -->` group at lines 202-207; add `[card-ordering-subsystem]` under the `<!-- Root -->` group at lines 165-180 — alphabetical insertion within each group).
   - **`### Changed` bullet append.** The Spec at line 1090 pins the wording verbatim:
     > `Meta.orderset_class is no longer in `DEFERRED_META_KEYS`; declaring `Meta.orderset_class = MyOrder` now wires through to finalizer phase 2.5 and surfaces a working order input on the GraphQL type. Consumers who declared the key against `0.0.7` saw a `ConfigurationError`; against `0.0.8` it produces an order surface.`
     Worker 2 inserts this as a new bullet under the existing `### Changed` heading (immediately after the existing `_pascal_case` bullet at line 26). Same ref-style link rules apply.
   - **`### Notes` / `### Fixed` / `### Removed` — no edits.** Slice 5 only ships `### Added` + `### Changed` bullets; no `### Fixed` or `### Removed` work in this card.
   - **CHANGELOG-edit permission.** Per AGENTS.md line 21 + the build-plan's CHANGELOG-edit permission flag (line 56) + Spec DoD item 23 + Spec line 1092: Slice 5 carries the explicit permission for this card. No other slice in this build has touched CHANGELOG.md (verified — `git diff CHANGELOG.md` shows zero changes from the build cycle).
   - **Verbatim-copy contract** per BUILD.md "CHANGELOG sanity" review trigger: Worker 3's review diffs the new bullets against the spec-pinned text.

8. **`GOAL.md`** — verification only, no edit.
   - Per Spec line 1071 + DoD item 16 + the dispatch-prompt confirmation: `GOAL.md` already references `Meta.orderset_class = orders.GalaxyOrder` (line 108 / 130) and the per-app `orders.py` shape (line 21 in the file-layout block + lines 207-242 the rich `orders.py` consumer code block). No edit is needed beyond confirming the references resolve. Slice 5's GLOSSARY flip (step 1 above) makes `Meta.orderset_class` resolve as `shipped (0.0.8)`; `orders.py` resolves as the on-disk file under `examples/fakeshop/apps/library/`. Worker 2 confirms by running `grep -n "Meta.orderset_class\|orders\.py\|orders\.GalaxyOrder" GOAL.md` and verifying every hit is consistent with the post-Slice-5 reality.

**Post-edit verification grep sanity** (Worker 2 runs after every file edit; Worker 3 re-runs at review time):
- `grep -rE "TODO\(spec-028-orders-0_0_8 Slice 5\)" docs/ README.md TODAY.md KANBAN.md CHANGELOG.md` → empty (every Slice 5 TODO anchor removed).
- `grep -nE "planned for 0\.0\.8" docs/GLOSSARY.md` → no hits for the five Ordering entries (`OrderSet`, `RelatedOrder`, `Ordering`, `order_input_type`, `Meta.orderset_class`).
- `grep -c "WIP-ALPHA-028-0\.0\.8" KANBAN.md` → 0.
- `grep -c "DONE-028-0\.0\.8" KANBAN.md` → at least 1 (the Done card heading; plus the cross-references from the planned 0.0.9 cards if Worker 2 updated those too).
- `grep -nE "shipped \(0\.0\.8\)" docs/GLOSSARY.md | wc -l` → at least 9 (4 Filtering + 5 Ordering symbols in the Index table; bodies are separate).

### Test additions / updates

Slice 5 is docs-only — NO new tests under `tests/` and NO changes to existing tests. The test surface is:

- **`scripts/check_spec_glossary.py`** — per the spec's risk-and-open-questions at line 1107 + the build-plan's pre-flight check #6: the script consumes `docs/spec-028-orders-0_0_8-terms.csv` and verifies every named term has a resolvable anchor in `docs/GLOSSARY.md`. Slice 5's anchor work is body-text and Index-table flips only — the five Ordering anchors (`#orderset` / `#relatedorder` / `#ordering` / `#order_input_type` / `#metaorderset_class`) are stable across the body edits. Worker 3's review can run `uv run python scripts/check_spec_glossary.py --spec docs/spec-028-orders-0_0_8.md` to confirm the anchors still resolve; expected exit code 0.
- **`scripts/build_kanban_md.py` + `scripts/build_kanban_html.py`** — Worker 2 runs both after editing the kanban SQLite source. The generated `KANBAN.md` + `KANBAN.html` should reflect the column move + glossary-terms-table flip + DoD-bullets check-off + WIP-→-DONE id rename. No new tests pin these scripts' behavior; they're idempotent builders.
- **No new `tests/` files.** Slice 5 does not exercise package code. The 14 live HTTP tests already landed in Slice 4 and stay unchanged.

### Implementation discretion items

Slice 5 is heavily prescribed by the spec's Doc updates section (lines 1048-1092). The few items genuinely at Worker 2's discretion:

1. **Whether to add ref-style link defs for every new GLOSSARY anchor referenced from `docs/README.md` + `README.md` + `CHANGELOG.md`.** AGENTS.md line 28 mandates ref-style for cross-file links in standing docs; Worker 2 may inline-add the new `[glossary-orderset]` / `[glossary-relatedorder]` / `[glossary-ordering]` / `[glossary-order_input_type]` / `[glossary-metaorderset_class]` ref-defs at the bottom of each of the three files OR enumerate a smaller subset based on which anchors the new bullet bodies actually reference. The plan recommends the larger set for consistency with how DONE-027-0.0.8's Slice 5 added the filter ref-defs.
2. **Whether to remove the `tests/orders/` block from the target-shape `tests/` block in `docs/TREE.md` (lines 461-465).** Either keep-and-update or remove-as-graduated is a valid disposition. The plan recommends remove-as-graduated for symmetry with how `filters/` is handled, but either shape is correct as long as the on-disk reality is the canonical listing.
3. **Exact wording shape for the `TODAY.md` library current-state appendage.** The spec line 1075 names the symbols to mention but does not character-for-character pin the wording (unlike the KANBAN past-tense body or the CHANGELOG bullet). Worker 2 ships parallel narration matching the existing filter sentence's voice; the suggested shape above is a starting point.
4. **Whether to chase down the `Cross-reference card-body strings` of `WIP-ALPHA-028-0.0.8` across `KANBAN.md` (lines 246/319/328/355/369/394/410/431) into `DONE-028-0.0.8`.** Strict reading of Spec Slice 5 sub-bullets only names the column move + In-progress summary update; the cross-references in OTHER cards (like `TODO-ALPHA-030-0.0.9 DjangoConnectionField`'s dependency list at line 319) carry the old WIP-shape ID. The kanban app's rebuild script likely handles these automatically because every cross-reference is data-driven from the `Card.id` field — Worker 2 should verify post-rebuild that the cross-references propagate; if not, an additional SQLite update is needed. Worker 3's review confirms the global rename completed.
5. **Whether to ALSO update the `### In progress` summary paragraph at line 71-75 of KANBAN.md.** The spec at line 1082 mentions the `### In progress` summary; the current `KANBAN.md` carries that summary at lines 71-75 (the `### In progress` subheading under Snapshot) AND a second summary at line 132 (the `## In progress` column intro). Both should update. Plan above named both sites; Worker 2 confirms both are touched.
6. **`docs/TREE.md` post-edit verification for the `convert_relation` → `resolved_relation_annotation` baseline-dirty preserve.** The maintainer's working-tree change at lines 213 / 263 / 357 already swaps the per-file comment from `convert_relation` to `resolved_relation_annotation`. Worker 2's Slice 5 edit must NOT revert this; the `orders/` block insertion is at a different line range and does not conflict. Verify via `git diff docs/TREE.md` showing both the maintainer's three-line change AND Slice 5's new `orders/` block.

### Required helper runs

Skipped: Slice 5 is docs-only (.md edits + KANBAN/CHANGELOG); no `.py` files added or modified. Per BUILD.md "When to run the helper during build" — Worker 1 must run the helper when planning adds logic to any existing `.py` file ≥150 source lines OR any file under `types/`/`optimizer/`; Worker 3 must run when adding any new `.py` file or touching `optimizer/`/`types/` or adding ≥30 lines of new logic to any package file. Slice 5 triggers none of these.

### Spec slice checklist (verbatim)

The spec's nested sub-bullets for this slice from `## Slice checklist`, copied verbatim as `- [ ]` boxes. Worker 1 ticks each `- [x]` during final verification as the contract lands.

- [x] [`docs/GLOSSARY.md`][glossary]: flip [`OrderSet`][glossary-orderset], [`RelatedOrder`][glossary-relatedorder], [`Meta.orderset_class`][glossary-metaorderset_class], [`order_input_type`][glossary-order_input_type], and [`Ordering`][glossary-ordering] from `planned for 0.0.8` to `shipped (0.0.8)`. Update the order-symbol entry bodies, the [Index][glossary-index] table status column, and the Ordering category of the [Browse by category][glossary] block.
- [x] [`docs/README.md`][docs-readme]: add `OrderSet` / `RelatedOrder` / `order_input_type` / `Ordering` / `Meta.orderset_class` to the shipped-symbol bullet list under the `0.0.8` boundary. Per M3 of [`docs/feedback.md`][feedback] rev3, `Ordering` is part of the public subpackage surface per [Decision 2](#decision-2--subpackage-layout-and-public-export-surface) and must be listed consistently across every shipped-symbol sweep.
- [x] [`docs/TREE.md`][tree]: flip the `orders/` subpackage entry from `[alpha]` to on-disk (move from the "target package layout" section to the "current on-disk layout" section). List the five new files (`__init__.py`, `base.py`, `sets.py`, `factories.py`, `inputs.py`); list the mirrored `tests/orders/` tree.
- [x] [`README.md`][readme]: add `OrderSet` / `RelatedOrder` / `order_input_type` / `Ordering` to the shipped-symbol bullet list (under the `0.0.8` boundary, alongside the filter symbols promoted in `DONE-027-0.0.8`'s Slice 5). Per M3 of [`docs/feedback.md`][feedback] rev3, `Ordering` MUST be included.
- [x] [`GOAL.md`][goal]: the astronomy showcase already references `Meta.orderset_class` and the per-app `orders.py` shape — no edit needed there beyond confirming the references resolve.
- [x] [`TODAY.md`][today]: extend the "Shipped capabilities" enumeration with `OrderSet` / `Meta.orderset_class` / `RelatedOrder`; extend the fakeshop section to mention the new live `BranchOrder` / `BookOrder` / `LoanOrder` / `PatronOrder` declarations and the order live HTTP tests under [`examples/fakeshop/test_query/test_library_api.py`][fakeshop-test-library].
- [x] [`KANBAN.md`][kanban]: move `WIP-ALPHA-028-0.0.8` to the Done column with the next available `DONE-NNN-0.0.8` id. Past-tense Done body pinned in [Doc updates](#doc-updates). Rewrite the card body's `Definition of done` bullet 1 (`docs/spec-orders.md` → `docs/spec-028-orders-0_0_8.md`).
- [x] [`CHANGELOG.md`][changelog]: append `### Added` bullets to `[Unreleased]` for `OrderSet`, `RelatedOrder`, `Meta.orderset_class`, `Ordering` enum, `order_input_type`. Append a `### Changed` bullet noting that `Meta.orderset_class` is no longer in `DEFERRED_META_KEYS`. Do not promote `[Unreleased]` or create a release heading in this card unless the maintainer explicitly gives the version-bump command.
- [x] **No version-file edits in this card.** Leave `pyproject.toml`, [`django_strawberry_framework/__init__.py::__version__`][package-init], and [`tests/base/test_init.py::test_version`][test-base-init] unchanged unless the maintainer explicitly gives the version-bump command.

---

## Build report (Worker 2)

### Files touched

- `docs/GLOSSARY.md` — flipped five Index-table rows (`Meta.orderset_class`, `Ordering`, `OrderSet`, `order_input_type`, `RelatedOrder`) from `planned for 0.0.8` to `shipped (0.0.8)`; rewrote the five entry bodies per spec lines 1050-1056 verbatim contract; deleted the Slice 5 TODO comment block under `Meta.orderset_class`. Layered additively on top of the maintainer's prior `SyncMisuseError` entry (preserved). Browse-by-category Ordering line already enumerates the five symbols — verified anchors still resolve.
- `docs/README.md` — added an `ordering subsystem (new in 0.0.8) — …` bullet to the Shipped-today list alongside the filter sibling, mirroring its shape; removed `orders and permissions / cascade permissions` from the `Coming in 0.1.0` block and replaced with `permissions / cascade permissions`; deleted the Slice 5 TODO comment block; added `[glossary-orderset]` ref-def under `<!-- docs/ -->` (alphabetical).
- `docs/TREE.md` — inserted the on-disk `orders/` block between `filters/` and `utils/` in the current-on-disk-layout `django_strawberry_framework/` tree; updated the intro paragraph to enumerate `orders/` alongside the other on-disk subpackages; removed the `orders/` block from the target-package-layout block (the bracket-tagged `[alpha]` entry that carried the Slice 5 inputs.py TODO); inserted the `orders/` mirror block in the current-shape `tests/` tree with seven files; removed the now-graduated `tests/orders/` block from the target-shape tree per Worker 1's recommended disposition (#2 in implementation-discretion items). Layered additively on top of the maintainer's earlier `convert_relation` → `resolved_relation_annotation` per-file-comment fixes (preserved).
- `README.md` — rewrote the Status paragraph to name the ordering subsystem as the newest shipped surface (`OrderSet` / `RelatedOrder` / `Ordering` enum + members + NULLS positioning / `order_input_type` / `Meta.orderset_class`) alongside the filter symbols promoted in `DONE-027-0.0.8`'s Slice 5; deleted the Slice 5 TODO comment block.
- `TODAY.md` — extended the library current-state paragraph with the parallel ordering narration (`BranchOrder` / `ShelfOrder` / `BookOrder` / `LoanOrder` / `PatronOrder` at `apps/library/orders.py`; `GenreOrder` at `orders_genre.py`; 14 live HTTP order tests; filter+order composition on `allLibraryBooks`); removed `Meta.orderset_class` from the products unshipped-APIs list; demoted the "What the fakeshop example should wait for" `ordering, aggregates, and fieldsets` line to `aggregates and fieldsets`; deleted the Slice 5 TODO comment block. Layered additively on top of the working-tree as of Slice 5 build time.
- `CHANGELOG.md` — appended one `### Added` bullet under `[Unreleased]` (verbatim from spec line 1087) and one `### Changed` bullet (verbatim from spec line 1090). `[Unreleased]` heading preserved as-is (no promotion to `[0.0.8]`); no version-file edits. Added two new ref-defs in the bottom block — `[feedback]: docs/feedback.md` and `[glossary-metaorderset_class]: docs/GLOSSARY.md#metaorderset_class` — in alphabetical position under the `<!-- docs/ -->` group.
- `examples/fakeshop/db.sqlite3` — kanban app: flipped `Card("Ordering subsystem")` from `status=wip` / `planning_state=in_progress` / `planning_note="planned"` to `status=done` / `planning_state=shipped` / `planning_note="shipped"`; marked all seven Definition-of-done `CardItem` rows complete; rewrote DoD bullet 1 text from `"Add docs/spec-orders.md."` to `"Add docs/spec-028-orders-0_0_8.md."` per spec line 1081; deleted planning-time `scope` / `foundation_seam` / `verified_upstream` items; replaced the nine `other` items with one item carrying the spec line 1080 past-tense Done body verbatim; updated the `snapshot` `BoardDoc` body to declare both 0.0.8 subsystems shipped and no active WIP cards; replaced the `in-progress` column-doc body with `"No active WIP cards."`. glossary app: flipped `GlossaryTerm` `status_text` for the five Ordering symbols (`OrderSet` / `Ordering` / `order_input_type` / `RelatedOrder` / `Meta.orderset_class`) from `"planned for 0.0.8"` to `"shipped (0.0.8)"` AND `status` from `Planned` to `Shipped` so the kanban Done-card glossary-terms table renders correctly.
- `KANBAN.md` — regenerated via `uv run python scripts/build_kanban_md.py` after the SQLite update. The card now renders as `DONE-028-0.0.8` in the Done column (top of the column, before `DONE-027-0.0.8`); every cross-card reference of `WIP-ALPHA-028-0.0.8` across the planned 0.0.9-0.1.0 cards automatically updated to `DONE-028-0.0.8` via FK resolution; the Snapshot and `## In progress` column intro paragraphs reflect "no active WIP" state.
- `KANBAN.html` — regenerated via `uv run python scripts/build_kanban_html.py` for parity with `KANBAN.md`.

### Tests added or updated

None. Slice 5 is docs-only — no `.py` source or test changes.

### Validation run

- `uv run ruff format .` — pass (213 files left unchanged).
- `uv run ruff check --fix .` — pass (All checks passed!).
- `uv run python scripts/check_spec_glossary.py --spec docs/spec-028-orders-0_0_8.md` — exit 0 (`OK: 44 terms — all have glossary entries and at least one spec link.`).
- `grep -rnE "15.*live HTTP|two M7" docs/ README.md TODAY.md KANBAN.md CHANGELOG.md` excluding `docs/spec-028-orders-0_0_8.md` and `docs/builder/` — returns no findings. The canonical "14 live HTTP tests" count is consistent across every standing doc.
- `grep -rnE "TODO\(spec-028-orders-0_0_8 Slice 5\)" docs/ README.md TODAY.md KANBAN.md CHANGELOG.md` excluding `docs/builder/` and `docs/spec-028-orders-0_0_8.md` — only `docs/GLOSSARY2.md` (baseline-dirty out-of-scope, untracked) carries a leftover anchor. All in-scope standing-doc anchors deleted.
- `grep -c "WIP-ALPHA-028-0\.0\.8" KANBAN.md` — `0` (all references promoted to `DONE-028-0.0.8`).
- `git status --short` after both ruff invocations — modified files are all slice-intended (`CHANGELOG.md`, `KANBAN.md`, `KANBAN.html`, `README.md`, `TODAY.md`, `docs/GLOSSARY.md`, `docs/README.md`, `docs/TREE.md`, `examples/fakeshop/db.sqlite3`) plus the build artifact itself; no tool churn requiring revert. All other dirty files in the working tree are pre-existing baseline-dirty content (the long list in `docs/builder/build-028-orders-0_0_8.md` plus prior-slice artifacts and Worker 1's spec status-line edit) — left untouched per the additive-layering discipline.

### Implementation notes

- **Past-tense body delivery shape (KANBAN Done card).** Worker 1's plan said "Card body Definition of done bullets get checked off and the body text stays as historical record"; DONE-027-0.0.8's actual on-disk shape is just an `Other` section with two short summary bullets. I followed Worker 1's plan: kept the seven DoD bullets, checked all seven, rewrote bullet 1's path per spec line 1081; replaced the nine planning-time `other` items with one `other` item carrying the spec line 1080 past-tense body verbatim; deleted the `scope` / `foundation_seam` / `verified_upstream` planning items (they were `- [ ]`-shaped planning skeletons that have no surviving meaning on a Done card). Verbatim contract verified via `diff` against the spec — both sides are 4,585 characters character-for-character identical (excluding the spec's `    > "..."` blockquote wrapper).
- **`tests/orders/` disposition in the target-shape tree.** Worker 1's plan #2 gave Worker-2 discretion: keep-and-update or remove-as-graduated. Removed for symmetry with how `filters/` is handled — the on-disk `tests/orders/` block in the current-shape tree is the canonical listing; carrying a separate target-shape entry for a graduated subsystem would be redundant.
- **Ref-def scope in cross-file links (#1 of Worker 1's discretion items).** Added only the cross-file refs actually used in the new bullet bodies: `[glossary-orderset]` in `docs/README.md` (only ref used by the new ordering bullet); `[feedback]` and `[glossary-metaorderset_class]` in `CHANGELOG.md` (referenced by the new spec-verbatim Added/Changed bullets). The other potential refs (`[glossary-ordering]`, `[glossary-relatedorder]`, etc.) are not referenced by the new content, so no defs added — keeps the link-defs block lean.
- **Slice 5 cross-references in OTHER KANBAN cards (#4 of Worker 1's discretion items).** Verified by post-rebuild grep: every prior `WIP-ALPHA-028-0.0.8` cross-reference in dependent cards (`TODO-ALPHA-030-0.0.9 DjangoConnectionField`, the full-Relay-story card, the connection-aware-optimizer card, etc.) automatically rewrote to `DONE-028-0.0.8` via the kanban app's `BoardDocCardReference` / `CardReference` FK resolution at rebuild time. No additional SQLite update was needed.
- **GlossaryTerm DB updates AND direct GLOSSARY.md edits.** The kanban Glossary-terms table on the Done card reads from `apps.glossary.models.GlossaryTerm.status_text` (the glossary app's DB), not from `docs/GLOSSARY.md`. So Slice 5 needed two parallel edits: (a) update the five Ordering `GlossaryTerm` rows' `status` (FK to `GlossaryStatus.Shipped`) and `status_text` (`"shipped (0.0.8)"`) to fix the Done-card glossary-terms table; (b) edit `docs/GLOSSARY.md` Index table + bodies directly to flip the standing glossary surface. The two surfaces are not auto-synced — `build_glossary_md.py` exists but was not run (its output is in the baseline-dirty out-of-scope list as `docs/GLOSSARY2.md`).
- **CHANGELOG.md `### Added`/`### Changed` bullet placement.** Worker 1's plan suggested inserting "immediately after the Filtering subsystem bullet at line 21" / "immediately after the existing `_pascal_case` bullet at line 26". I followed that placement: Ordering Added bullet directly after the Filtering Added bullet under the existing `### Added` heading; Ordering Changed bullet directly after the existing `_pascal_case` Changed bullet under the existing `### Changed` heading. `[Unreleased]` heading preserved at line 19 — no promotion.

### Notes for Worker 3

- Use `diff` against the spec source for the three verbatim contracts:
  - KANBAN Done-card past-tense body (`Other` section, single bullet at `KANBAN.md` line 1514) against spec line 1080 (strip the spec's `    > "..."` blockquote wrapper). The two should be 4,585 characters identical.
  - CHANGELOG `### Added` bullet (CHANGELOG.md line 22) against spec line 1087.
  - CHANGELOG `### Changed` bullet against spec line 1090.
  - GLOSSARY entry bodies against spec lines 1050-1056 (these are paraphrased verbatim where the spec pins specific clauses; check the entry-body sentences against the spec's pinned-sentence shapes).
- The KANBAN rebuild is deterministic: re-running `uv run python scripts/build_kanban_md.py` against the current SQLite produces the same output. To verify, run the script and `git diff KANBAN.md` should show no changes.
- The kanban `Card.is_blocked` derivation runs against the current DB state; no card has `WIP-ALPHA-028` in `outgoing_references`-as-`blocked_by` because the Card itself flipped to `done`. Spot-check via the rebuilt KANBAN.md: no card carries a `blocked` badge that names the Ordering card.
- `GLOSSARY2.md` (untracked, baseline-dirty out-of-scope) still carries the old Slice 5 TODO anchor; this is the maintainer's working copy of an alternate glossary export and is not touched by Slice 5 per the baseline-dirty discipline.

### Notes for Worker 1 (spec reconciliation)

None. The spec's Doc updates section (lines 1048-1092) was prescriptive enough to land Slice 5 without spec gaps, conflicts, or unstated assumptions. The verbatim-copy contracts (KANBAN past-tense body, CHANGELOG Added, CHANGELOG Changed) all matched the spec text verbatim. The Worker 1 plan's caveat about preserving baseline-dirty maintainer edits in `GLOSSARY.md` / `TREE.md` / `TODAY.md` held — the maintainer's `SyncMisuseError` entry, `convert_relation` → `resolved_relation_annotation` fixes, and any other in-progress edits stayed untouched and the Slice 5 edits layered on top cleanly.

---

## Review (Worker 3)

### High:

None.

### Medium:

#### Out-of-scope edit to a shipped `[0.0.7]` Added bullet

`CHANGELOG.md` line 35 (the `safe_wrap_connection_method` bullet under the frozen `## [0.0.7] - 2026-05-27` heading) was rewritten from `[`django_strawberry_framework.test`][test-init]` to `[`django_strawberry_framework.testing`][test-init]`, AND the matching `[test-init]` ref-def at line 207/211 flipped from `django_strawberry_framework/test/__init__.py` to `django_strawberry_framework/testing/__init__.py`. Worker 2's build report at line 195 only lists the two `[Unreleased]` bullets and "two new ref-defs in the bottom block — `[feedback]: docs/feedback.md` and `[glossary-metaorderset_class]: docs/GLOSSARY.md#metaorderset_class`" — the `[test-init]` path swap is not surfaced. The spec contract authorizes Slice 5 to **append** to `[Unreleased]` only ([Decision 10](#decision-10--version-bumps-are-maintainer-commanded) + spec lines 1084-1092 + AGENTS.md "Do not update CHANGELOG.md unless explicitly instructed"). A retroactive edit to a frozen released-version Added bullet is outside that grant, even when the underlying source-tree path is genuinely the live one (the directory is `testing/` on disk; the `test/__init__.py` ref-def was already a dead link at HEAD baseline).

```CHANGELOG.md (around line 35 and line 211)
- Public export from [`django_strawberry_framework.testing`][test-init].
- [test-init]: django_strawberry_framework/testing/__init__.py
```

Why it matters: shipped-version bullets are change history, not living documentation. Quietly correcting a link in a frozen entry breaks the "what was in 0.0.7" record (the source path stated against the 0.0.7 release was `test/__init__.py`, even if it would later be renamed). It also normalizes silent retroactive edits to released entries.

Recommended resolution paths (Worker 1 owns the call):

1. **Revert the two `test` → `testing` edits in the `[0.0.7]` block** (the Ordering Added + Changed bullets under `[Unreleased]` are unaffected). Re-record the broken link as a `BACKLOG.md` cleanup item.
2. **Keep the edits and record them under `### Notes for Worker 1 (spec reconciliation)` as a deliberate cleanup**, with a one-line rationale that the `test/` path never existed in any released package and the ref-def at HEAD was already dead — i.e., the edit is a typo fix rather than a behavior re-claim.

`Escalated: Worker 1 picks 1 or 2 at final verification.`

### Low:

#### Stale `docs/spec-orders.md` reference in a card-reference narrative

`KANBAN.md` line 1519 (`#### Card references` block under `DONE-028-0.0.8`) still says `New `orders/` subpackage + `docs/spec-orders.md` + mirrored tests.` The Definition-of-done bullet 1 was correctly rewritten to `docs/spec-028-orders-0_0_8.md` at line 1504 per spec line 1081. The Card-references entry is a separate narrative breadcrumb that the kanban-app rebuild carried forward from the planning-time row, not a DoD bullet. Spec line 1081 names only the DoD bullet 1, so this is not a contract violation — but the path is now stale (the canonical spec is `docs/spec-028-orders-0_0_8.md` post-Decision-1). Recommend the maintainer update the underlying kanban-app `Card-references` row in a follow-up to drop `docs/spec-orders.md` (or rewrite to `docs/spec-028-orders-0_0_8.md` if the reference is intentional).

### DRY findings

None. The plan's M10-rule discipline (CHANGELOG bullet references the KANBAN past-tense body as the single source of truth; the spec-pinned headline-only bullet shipped verbatim) held. The five GLOSSARY entry bodies pick up only the clauses the spec pins at lines 1050-1056 without re-narrating the KANBAN past-tense body. No content duplication across the touched files.

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` is empty. Top-level `__all__` unchanged. Slice 5 is docs-only — no `.py` edits — so the public surface is unaffected by construction. DoD item 25 (top-level `__all__` is NOT widened) is satisfied.

### CHANGELOG sanity

Slice 5 IS this surface. Findings of the verbatim-diff and end-to-end read:

- **Version line.** `[Unreleased]` heading at line 19 is preserved as `[Unreleased]`. No `## [0.0.8] - <date>` heading was added. `pyproject.toml` `version = "0.0.8"` and `django_strawberry_framework/__init__.py::__version__ = "0.0.8"` are both unchanged in this diff (`git diff -- pyproject.toml django_strawberry_framework/__init__.py tests/base/test_init.py` is empty). Confirms Decision 10 / Revision 5 contract — no version bump in this card.
- **Authorized headings.** `### Added` (line 20) and `### Changed` (line 24) are the two headings used. The spec at lines 1084-1090 authorizes exactly these two. No `### Fixed` / `### Removed` were touched.
- **Verbatim-diff: `### Added` Ordering bullet.** `CHANGELOG.md` line 22 vs spec line 1087 — **character-for-character match** (verified via `diff /tmp/spec_changelog_added.txt /tmp/changelog_added_normalized.txt` returning empty after normalizing the spec's `    > "..."` blockquote wrapper).
- **Verbatim-diff: `### Changed` Ordering bullet.** `CHANGELOG.md` line 26 vs spec line 1090 — **character-for-character match** (verified via `diff` against the spec source).
- **Wording check.** Neither bullet overstates or understates the change. The Added bullet defers the long-form narrative to the KANBAN body (per M10 of feedback.md rev1), as the spec dictates. The Changed bullet names the `0.0.7 → 0.0.8` behavior delta accurately (declaring `Meta.orderset_class` against `0.0.7` raised `ConfigurationError`; against `0.0.8` produces a working order surface).
- **Ref-def additions.** `[feedback]: docs/feedback.md` (under `<!-- docs/ -->`) and `[glossary-metaorderset_class]: docs/GLOSSARY.md#metaorderset_class` (under `<!-- docs/ -->`) were added; both are referenced by the new bullets. Alphabetical placement within the group.
- **Out-of-scope edit (Medium finding above).** The `test/__init__.py` → `testing/__init__.py` retroactive edit to the frozen `[0.0.7]` Added bullet is the only finding here.

### Documentation / release sanity

Slice 5 IS this surface. Findings of the verbatim-diff and end-to-end read:

- **Version strings / shipped statuses / card IDs match the active spec and the post-slice package version.** Package version stays at `0.0.8` at both `pyproject.toml` and `__init__.py`. The Ordering card flips from `WIP-ALPHA-028-0.0.8` to `DONE-028-0.0.8`; `grep -c "WIP-ALPHA-028-0\.0\.8" KANBAN.md` returns `0`; the Done card heading at line 1440 reads `### [DONE-028-0.0.8 — Ordering subsystem]`. The Snapshot paragraph at line 74 names both subsystems shipped and "No active WIP cards". The Spec-map row at line 101 says `DONE-028-0.0.8 — Ordering subsystem`. All consistent with Slice 5's plan.
- **Verbatim-diff: KANBAN past-tense Done-card body.** `KANBAN.md` line 1514 vs spec line 1080 — **character-for-character match** (verified via `diff /tmp/spec_kanban_body.txt /tmp/kanban_body_normalized.txt` returning empty after normalizing the spec's `    > "..."` blockquote wrapper). The full ~4,594-character past-tense narrative shipped exactly as the spec pins it.
- **Verbatim-diff: five GLOSSARY entry bodies vs spec lines 1050-1056.** The spec's Doc-updates section at lines 1050-1056 uses the verb "Update body to describe X / Y / Z" rather than a strict character-for-character contract for the entry bodies (unlike spec line 1080 / 1087 / 1090 which use `> "..."` blockquote pins). Each entry was checked end-to-end against the spec's clause list:
  - **`OrderSet`** (GLOSSARY.md lines 793-801 vs spec line 1051): all clauses present — declarative Meta.model/Meta.fields with "__all__" shorthand covering column-backed fields incl. forward FK/OneToOne and excl. reverse/M2M; RelatedOrder for cross-relation traversal; check_*_permission denial gates with active-input-only scope plus active-branch double-dispatch dedup per `(OrderSet class, method name)`; list-shaped orderBy tie-breaker; six-member Ordering enum with NULLS; five-layer port + Layer 6 deferred to 0.0.9. The "per B4 of feedback.md rev3 / per H3 of rev3" inline citations are dropped (appropriate for a glossary surface). **Match the contract.**
  - **`RelatedOrder`** (GLOSSARY.md lines 879-887 vs spec line 1052): all clauses present — class / absolute path / unqualified name target acceptance; sibling import from `sets_mixins.LazyRelatedClassMixin`; the "not `filters.base` as stated in earlier revisions" reconciliation breadcrumb is preserved. Plus the position-side-channel note + parent-side `check_<branch>_permission` gate description from Spec Rev4 H4. **Match the contract.**
  - **`Meta.orderset_class`** (GLOSSARY.md lines 697-707 vs spec line 1053): consumer-facing wiring + promotion-from-DEFERRED_META_KEYS gate both described, with the finalizer phase-2.5 binding + `_owner_definition` wiring + module-global materialization spelled out. **Match the contract.**
  - **`order_input_type`** (GLOSSARY.md lines 803-811 vs spec line 1054): element-type `Annotated["<Name>OrderInputType", strawberry.lazy("django_strawberry_framework.orders.inputs")]`; eager validation; `list[order_input_type(BranchOrder)] | None = None` consumer usage; orphan validation at finalize via `_helper_referenced_ordersets` ledger that `registry.clear()` co-clears. **Match the contract.**
  - **`Ordering`** (GLOSSARY.md lines 785-791 vs spec line 1055): six members enumerated; `resolve(field_path)` returns Django `OrderBy` expressions; the `F(field_path).asc(nulls_first=...)` / `desc(nulls_last=...)` shape spelled out. **Match the contract.**
  - The Index table status column at lines 91/97/98/99/105 reads `shipped (0.0.8)` for all five Ordering entries; the Browse-by-category Ordering line at line 130 enumerates the five symbols with stable anchors.
- **KANBAN card move discipline.** The Ordering card no longer appears under any column except Done. The `### In progress` summary paragraph at line 74 / 132 reflects "No active WIP cards". The Done card heading at line 1440 appears exactly once. Cross-references in dependent cards (0.0.9 cohort) propagated to `DONE-028-0.0.8` via the kanban-app rebuild's FK resolution.
- **DoD bullet rewrite.** KANBAN.md line 1504 reads `- [x] Add docs/spec-028-orders-0_0_8.md.` per spec line 1081. All seven DoD bullets are checked off; bullet 1's path is the structured filename.
- **`docs/TREE.md` flip.** The `orders/` block was removed from the target-package-layout section (no `[alpha]` tag remains) and added under the current-on-disk-layout block at line 234 with all five files (`__init__.py`, `base.py`, `sets.py`, `factories.py`, `inputs.py`). The `tests/orders/` mirror tree appears in the current-shape `tests/` block at line 379 with all seven files (`__init__.py`, `test_base.py`, `test_sets.py`, `test_factories.py`, `test_inputs.py`, `test_finalizer.py`, `test_composition.py`). Per Worker 2's chosen disposition (recorded in `### Implementation notes`), the now-graduated `tests/orders/` target-shape block was removed — symmetric with how `filters/` is handled.
- **Markdown link convention.** Worker 2 added five new ref-def entries (one in `docs/README.md`, two in `CHANGELOG.md`, plus the verified-present ref-defs in `KANBAN.md` regenerated from the SQLite source). No new inline `](path/file.md)` cross-file link appears in the Slice-5 diff (`git diff -- docs/README.md README.md TODAY.md docs/GLOSSARY.md docs/TREE.md CHANGELOG.md | grep '^+' | grep -E '\]\([a-zA-Z][^)]*\.md'` returns empty).
- **Status-flip / straggler greps.** `grep -nE "planned for 0\.0\.8" docs/GLOSSARY.md docs/README.md README.md TODAY.md docs/TREE.md` returns empty (no stragglers in the standing docs). `grep -rnE "15.*live HTTP|two M7" docs/ README.md TODAY.md KANBAN.md CHANGELOG.md` (excluding `docs/builder/` and `docs/spec-028-orders-0_0_8.md`) returns empty. `grep -rnE "OrderSet.*planned|RelatedOrder.*planned|Ordering.*planned|order_input_type.*planned|Meta\.orderset_class.*planned"` returns only the spec's own historical-context references (lines 71/74/75/129/159/1051-1056/1107/1146 — all in `docs/spec-028-orders-0_0_8.md`), `docs/GLOSSARY2.md` (untracked, baseline-dirty), and `docs/SPECS/NEXT.md`'s generic `planned for 0.0.9` example in a CSV-format explanation. No standing-doc straggler.
- **No version-file edits.** `git diff -- pyproject.toml django_strawberry_framework/__init__.py tests/base/test_init.py` is empty.
- **`[Unreleased]` not promoted.** No `## [0.0.8] - <date>` heading was added. `### Added` and `### Changed` bullets are under `[Unreleased]`.
- **Spec link / glossary anchor sanity.** `uv run python scripts/check_spec_glossary.py --spec docs/spec-028-orders-0_0_8.md` exits 0 with `OK: 44 terms — all have glossary entries and at least one spec link.`.
- **TODO-anchor leakage.** `grep -rnE "TODO\(spec-028-orders-0_0_8 Slice 5\)" docs/ README.md TODAY.md KANBAN.md CHANGELOG.md` (excluding `docs/builder/` and `docs/spec-028-orders-0_0_8.md`) returns only `docs/GLOSSARY2.md` (baseline-dirty out-of-scope, untracked — correctly preserved per the additive-layering discipline). All five in-scope TODO anchors removed (`docs/GLOSSARY.md`, `docs/README.md`, `README.md`, `docs/TREE.md`, `TODAY.md`).
- **Active-spec archival.** Not applicable here — Decision 10 / Revision 5 explicitly says no archival. The spec stays at its working location at `docs/spec-028-orders-0_0_8.md`.
- **Baseline-dirty discipline.** Worker 2 did not touch baseline-dirty paths outside the three Slice 5 carve-outs (`docs/GLOSSARY.md`, `docs/TREE.md`, `TODAY.md`). The maintainer's prior `SyncMisuseError` entry in `docs/GLOSSARY.md` and the `convert_relation` → `resolved_relation_annotation` per-file-comment fix in `docs/TREE.md` are both preserved; Slice 5 layered additively on top. Other baseline-dirty paths (`docs/review/`, optimizer files, `examples/fakeshop/apps/kanban/*`, etc.) are unchanged by Slice 5 (the working-tree diff for those files is from prior slice work or maintainer concurrent edits, not from Worker 2's Slice-5 pass).

### What looks solid

- The KANBAN past-tense body landed character-for-character against the spec's 4,594-character pinned text — the longest verbatim contract in the slice cleared cleanly.
- The Add/Change bullet additions on `[Unreleased]` placed correctly (Ordering Added immediately after the Filtering Added bullet under the existing `### Added`; Ordering Changed immediately after the Filtering `Meta.filterset_class` Changed bullet under the existing `### Changed`). `### Added`/`### Changed` headings already existed from DONE-027-0.0.8 and were not duplicated.
- The GLOSSARY Index table flipped all five Ordering rows (`Meta.orderset_class`, `Ordering`, `OrderSet`, `order_input_type`, `RelatedOrder`) to `shipped (0.0.8)` consistently. Browse-by-category Ordering line at GLOSSARY.md line 130 enumerates the five symbols cleanly.
- The shipped-symbol lists in `docs/README.md` (ordering subsystem bullet alongside the filter sibling) and `README.md` (Status paragraph naming `OrderSet` / `RelatedOrder` / `Ordering` / `order_input_type` / `Meta.orderset_class`) BOTH include `Ordering` per M3 of feedback.md rev3.
- The kanban-app database update + `scripts/build_kanban_md.py` rebuild propagated the column move + glossary-terms-table flip + DoD-bullet check-off + WIP→DONE id rename uniformly across `KANBAN.md`, including FK-resolved cross-references in dependent 0.0.9 cards (no manual sweep needed).
- The additive-layering discipline on the three carve-out baseline-dirty files (`docs/GLOSSARY.md`, `docs/TREE.md`, `TODAY.md`) held: maintainer's in-progress edits stayed untouched, Slice 5's edits layered on top.
- No inline cross-file Markdown links introduced (all new ref-defs landed in the bottom blocks under the correct path-based groups, alphabetical within group).

### Temp test verification

No temp tests created. Slice 5 is docs-only — the review proved verbatim contracts via `diff` against extracted spec lines (`/tmp/spec_kanban_body.txt`, `/tmp/spec_changelog_added.txt`, `/tmp/spec_changelog_changed.txt`) and ran the standing `scripts/check_spec_glossary.py` smoke. No behavior to pin via a temp pytest run.

### Notes for Worker 1 (spec reconciliation)

`Escalated:` the Medium finding above ("Out-of-scope edit to a shipped `[0.0.7]` Added bullet") needs Worker 1's call at final verification. Two resolution paths:

1. **Revert path.** Restore CHANGELOG.md line 35 and line 211 to the HEAD baseline (`django_strawberry_framework.test` / `test/__init__.py`). The broken link in the frozen `0.0.7` entry becomes a separate cleanup item (file a `BACKLOG.md` entry or fold it into a maintainer-led link-sweep). The slice's authorized scope (append to `[Unreleased]`) lands clean.
2. **Keep-and-record path.** Leave the corrected paths in place and add a one-line entry under the artifact's `### Spec changes made (Worker 1 only)` section (Worker 1 owns that section) recording that the `test/` ref-def was already a dead link at HEAD baseline and the correction is a typo fix rather than a behavior re-claim. The frozen-release-entry editing norm gets an explicit dispensation for this specific case.

The choice depends on how strictly the maintainer wants frozen-release entries treated. Worker 3 has no preference; both are defensible. Worker 1 should pick at final verification.

No spec edit is needed to the Doc updates section — the Slice-5 contract at spec lines 1050-1092 was prescriptive enough and Worker 2 followed it faithfully for every authorized edit.

### Review outcome

`review-accepted` — every spec-contracted edit landed, all three verbatim contracts (KANBAN body + CHANGELOG Added + CHANGELOG Changed) cleared character-for-character, the five GLOSSARY entry bodies satisfy the spec's clause-list contracts at lines 1050-1056, the public surface is unchanged, no version-file edits, `[Unreleased]` not promoted, no in-scope TODO-anchor leakage, baseline-dirty discipline held. One Medium finding is transparently escalated to Worker 1 for resolution at final verification (the resolution does not change Slice 5's deliverable, only how the auxiliary `test/` → `testing/` link-rot fix is recorded).

---

## Final verification (Worker 1)

- Spec slice checklist: all 9 verbatim `- [ ]` boxes in `### Spec slice checklist (verbatim)` ticked `- [x]`. Every contract landed against the live working tree (`docs/GLOSSARY.md` Index + entry-body flips; `docs/README.md` shipped-list bullet + ref-def; `docs/TREE.md` orders/ + tests/orders/ on-disk promotion; `README.md` Status-paragraph rewrite; `GOAL.md` references verified resolvable; `TODAY.md` library current-state appendage + unshipped-APIs demotion; `KANBAN.md` rebuilt from SQLite update — column move + DoD-bullet flip + DoD-bullet-1 path rewrite; `CHANGELOG.md` `### Added` + `### Changed` bullets under preserved `[Unreleased]`; no version-file edits). No silently un-ticked boxes.
- DRY check across this slice and prior accepted slices: no new duplication. The M10-of-rev1 discipline (CHANGELOG bullet references KANBAN past-tense body as single source of truth) held — the long-form narrative lives only in `KANBAN.md` Done card body (verbatim from spec line 1080), the CHANGELOG bullet ships the spec-pinned one-line headline. GLOSSARY entry bodies pick up only the clauses the spec pins at lines 1050-1056 without re-narrating the KANBAN body. Five new ref-defs (1 in `docs/README.md`, 2 in `CHANGELOG.md`, 2 propagated via the kanban-app rebuild) land in the canonical bottom-block reference-style convention, alphabetical within group. `scripts/check_spec_glossary.py --spec docs/spec-028-orders-0_0_8.md` exits 0 (`OK: 44 terms — all have glossary entries and at least one spec link.`).
- Existing tests still pass: `uv run pytest tests/orders/ tests/types/ examples/fakeshop/test_query/test_library_api.py --no-cov` — 410 passed, 2 skipped, in 9.67s. No regressions surfaced by Slice 5's docs-only edits (expected, since the slice touches no `.py` files).
- Spec reconciliation: two reconciliation actions applied at final verification (recorded under `### Spec changes made (Worker 1 only)` below). (1) Worker 3's escalated Medium ("Out-of-scope edit to a shipped `[0.0.7]` Added bullet") resolved as **Option A — keep the `test` → `testing` correction**, recorded as deliberate link-rot cleanup. (2) Worker 3's Low ("Stale `docs/spec-orders.md` reference in a card-reference narrative") resolved as **Option A — fix now via the kanban-app SQLite source-of-truth + rebuild**, recorded as in-scope KANBAN cross-reference hygiene. Plus the standing spec-status-line re-verification edit (line 4 status header rolled forward to reflect Slice 5 shipping).
- Final status: `final-accepted`.

### Summary

Slice 5 ships the Ordering subsystem's docs / KANBAN / CHANGELOG status surface: five GLOSSARY entries flipped to `shipped (0.0.8)` with bodies updated per spec lines 1050-1056; shipped-bullet lists in `docs/README.md` and `README.md` extended with `Ordering`; `docs/TREE.md` `orders/` block promoted from target-package-layout to current-on-disk-layout with all five files listed, plus the mirror `tests/orders/` block; `TODAY.md` library current-state paragraph extended with parallel order narration; KANBAN card promoted from `WIP-ALPHA-028-0.0.8` to `DONE-028-0.0.8` via SQLite update + `build_kanban_md.py` / `build_kanban_html.py` rebuild, with the spec-pinned past-tense Done body (line 1080) shipped character-for-character; CHANGELOG `### Added` + `### Changed` bullets appended under preserved `[Unreleased]` per Decision 10 (no version-heading promotion, no version-file edits). 410 focused tests pass; spec-glossary check exits clean; KANBAN rebuild is deterministic. The build is now staged to spawn Slice 6 (filter+order composition smoke).

### Spec changes made (Worker 1 only)

- `docs/spec-028-orders-0_0_8.md` line 4 status header rolled forward to `Slices 1-5 shipped (2026-06-02); Slice 6 still pending`, and the post-Slice-5 reality surface (TREE.md flip, GLOSSARY Index flip, KANBAN Done-card move, CHANGELOG bullets) named explicitly. Triggered by Slice 5 shipping; per the Worker 1 spec status-line re-verification rule (`docs/builder/worker-1.md` "Spec status-line re-verification" section), stale status lines compound across slices so the per-spawn refresh catches them at the cheapest point.
- `CHANGELOG.md` line 35 + line 211 `test` → `testing` link-rot cleanup to the frozen `[0.0.7]` `safe_wrap_connection_method` Added bullet is **accepted** under Worker-3 escalation Option A. The `test/__init__.py` ref-def at HEAD baseline was already a dead link (the maintainer renamed `test/` → `testing/` in commit `4f2b4c6` on 2026-06-01, after the `[0.0.7]` release tag on 2026-05-27 but before this build). The frozen `[0.0.7]` bullet's STATEMENT of what shipped in 0.0.7 stays unchanged (the consumer-facing public-export symbol is what it always was — only the `path on disk` citation moved). Per AGENTS.md "always recommend the root-cause fix over the surface patch" — fixing the broken citation IS the root-cause fix; reverting re-introduces the broken state and defers the fix to a maintainer follow-up that would land the exact same two lines. The Slice 3 Escalation 2 revert precedent does NOT apply here because that case concerned a wording change to live validation code that risked propagating to consumer error messages; this case is a stale-path citation fix in a frozen historical entry with zero behavior risk. Not a behavioral change.
- `examples/fakeshop/db.sqlite3` kanban-app `CardReference.id=51` `raw_text` updated from `docs/spec-orders.md` → `docs/spec-028-orders-0_0_8.md` (the predecessor spec filename predating the `spec-NNN-topic-X_X_X.md` convention pinned in Decision 1). `KANBAN.md` + `KANBAN.html` regenerated via `uv run python scripts/build_kanban_md.py` + `uv run python scripts/build_kanban_html.py`. Resolves Worker 3's Low finding ("Stale `docs/spec-orders.md` reference in a card-reference narrative"). Decision Option A — fix now via Slice 5's existing kanban-rebuild infrastructure. Rationale: although the spec's Doc updates section names DoD-bullet-1 only for the spec-filename rewrite, the broader principle from Slice 4's final-verification ("if a stale reference lives in another file, Slice 5 must catch it") makes Slice 5 the right slice for KANBAN cross-reference hygiene. The card-references block is a separate kanban-app field from the DoD bullet, but it carries the same predecessor-filename stragglers that the DoD bullet rewrite was contracted to clear. Fix lands as part of Slice 5's KANBAN-rebuild scope. Not a behavioral change.



