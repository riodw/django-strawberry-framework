# Build: Slice 5 — doc updates + card-completion wrap

Spec reference: `docs/spec-036-mutations-0_0_11.md` (Slice checklist lines 65-66; Doc updates lines 527-544; Definition of done items 7 + 8 lines 596-599; Decisions 5/6/7/9/13/15 for the final public names)
Status: final-accepted

## Plan (Worker 1)

This is a **doc-only + DB-backed card-wrap** slice. It ships **zero package source / test changes**. It touches three classes of artifact:

1. **DB-backed generated docs** — `docs/GLOSSARY.md`, `KANBAN.md`, `KANBAN.html` are rendered from the kanban/glossary tables in `examples/fakeshop/db.sqlite3`. They are edited via the **Django ORM, then regenerated** (`scripts/build_glossary_md.py`, `scripts/build_kanban_md.py`, `scripts/build_kanban_html.py`) — NEVER hand-edited (a hand-edit is silently reverted by the next regenerate; a raw SQL insert skips the `post_save` UUID side-row the GraphQL render reads). See BUILD.md "Generated docs are DB-backed" + worker-0.md "Closing out a kanban card".
2. **Hand-authored standing docs** — `README.md`, `docs/README.md`, `GOAL.md`, `TODAY.md` are normal markdown, edited directly. Each has a `TODO(spec-036 Slice 5)` HTML-comment marker that Slice 5 resolves and removes.
3. **CHANGELOG.md** — edited directly, **AUTHORIZED** by the maintainer for this slice (see below). `### Added` bullets under `[Unreleased]`; **no version heading promotion** (Decision 13).

**Maintainer decisions baked into this plan (already obtained by Worker 0):**

- **CHANGELOG.md edit is AUTHORIZED.** AGENTS.md #"Do not update CHANGELOG.md unless explicitly instructed" reserves CHANGELOG edits for an explicit instruction; the maintainer has given it for Slice 5. The edit adds `[Unreleased] ### Added` bullets only. **No `## [0.0.11]` version heading** — the `0.0.11` bump stays with the joint cut (Decision 13).
- **Proceed on the current (dirty) `examples/fakeshop/db.sqlite3`.** The maintainer confirmed the dirty DB carries their just-committed kanban-rendering refactor's DB side and is intended/safe. Because the DB baseline is **NOT HEAD-clean**, the byte-clean-regenerate verification (worker-0.md step 8) is run as **(a)** a baseline regenerate-to-temp **before any DB edit** (separates pre-existing render drift from this card's own changes) + **(b)** regenerate STABILITY (two consecutive regenerates produce byte-identical output), NOT a naive `git diff` clean-vs-HEAD.

**Spec status-line re-verification (this spawn):** spec line 5 header read "Slices 1-3 shipped, Slice 4 next" at spawn; Slice 4 is `final-accepted` (build-plan checklist `[x]`). Edited to "Slices 1-4 shipped, Slice 5 next" — recorded under `### Spec changes made (Worker 1 only)`.

### DB facts verified against `examples/fakeshop/db.sqlite3` (per worker-0.md "verify references against the DB before editing")

- **Card 36** exists, `status.key == "wip"`, target_version `0.0.11 (alpha)`, milestone `alpha`. DONE cards are numbered **1-35 contiguously**, so card 36 is next. The kanban renderer (`scripts/build_kanban_md.py` lines 110-111) **drops the milestone prefix for `done` cards**, so the rendered id will be **`DONE-036-0.0.11`** (NOT `DONE-ALPHA-036`). **`NNN = 036`.**
- **`SpecDoc` for card 36 ALREADY EXISTS** with `name="spec-036-mutations-0_0_11"` and `url=".../blob/main/docs/spec-036-mutations-0_0_11.md"` (the live `docs/` working path the spec requires — NOT `docs/SPECS/`). So worker-0.md step 2 is a **no-op verify / update-if-drifted**, NOT a `.create()` (a `.create()` would collide on the unique `name`). Confirm the url still points at the live `docs/spec-036-mutations-0_0_11.md` path and leave it.
- **`card.glossary_links.count() == 0`** today → the DONE-card invariant (≥1 `CardGlossaryTerm`) is NOT yet satisfied; the bootstrap link (step 3) + `import_spec_terms` (step 5) populate it.
- **Three terms to promote exist and are `planned`:** `djangomutation` (`planned for \`0.0.11\``), `input-type-generation` (`planned for \`0.0.11\``), `fielderror-envelope` (`planned for \`0.0.11\``). `GlossaryStatus` keys are exactly `{shipped, planned}`.
- **Two net-new headings are ABSENT (expected):** `djangomutationfield`, `djangomodelpermission`. Both are seeded as net-new `GlossaryTerm` rows this slice. They are intentionally NOT in `spec-036-mutations-0_0_11-terms.csv` (spec DoD item 1 / Risks line 554) — they have no heading until this slice, so listing them would fail `check_spec_glossary.py`. **Do NOT add them to the CSV.**
- **`only-projection` term exists** (target of the G2-handoff one-line note).
- **Alphabetical-neighbor ordering** (renderer sorts entries by `(entry_order, title_sort)`, Index by `(index_order, title_sort)`; a tie + larger `title_sort` slots right after the neighbor with no renumbering):
  - `DjangoMutationField` (title_sort `djangomutationfield`) → neighbor `DjangoMutation` (`entry_order=23, index_order=19`); `djangomutationfield > djangomutation` and `< djangonodefield`(24) → set **entry_order=23, index_order=19**.
  - `DjangoModelPermission` (title_sort `djangomodelpermission`) → alphabetically between `DjangoModelFormMutation` (`entry_order=22, index_order=18`, title_sort `djangomodelformmutation`) and `DjangoMutation`(23) — `f<p` so `djangomodelformmutation < djangomodelpermission < djangomutation` → neighbor `DjangoModelFormMutation`; set **entry_order=22, index_order=18**.
- **Browse-by-category memberships** (model fields are `key`/`label`, membership sort is `order` alone):
  - `mutations` (cat id 102): order 0..6 = DjangoMutation, DjangoFormMutation, DjangoModelFormMutation, SerializerMutation, Input type generation, FieldError envelope, Auth mutations. `DjangoMutationField` is inserted (per the rendered row's natural reading, right after `DjangoMutation`). Because `(category, order)` is unique, bump the existing members into a temp band (`order += 1000`) then reassign the full desired order `0..N`.
  - `permissions` (cat id 99): order 0..2 = get_queryset visibility hook, apply_cascade_permissions, Per-field permission hooks. `DjangoModelPermission` (the write-auth entry) is added here; same temp-band reassign to avoid the unique collision.

### DRY analysis

- **Existing patterns reused.**
  - The **entire DB-backed card-close procedure** is worker-0.md "Closing out a kanban card" (lines 159-189): DONE-card invariants, `SpecDoc` create/update, `import_spec_terms`, the terms-CSV anchor rule, the seed-net-new-term steps (body = text between `**Status:** …` and the next `## `; `title_sort`, `entry_order`/`index_order` = preceding neighbor; category temp-band reassign), regenerate all three docs, the verification. The plan encodes that procedure verbatim as steps below so Worker 2 follows it without re-reading.
  - The **GLOSSARY entry bodies** are sourced from the already-committed `docs/GLOSSARY.md` (the existing planned entries' prose is updated in-DB; the net-new entries' bodies are authored fresh from the spec contract). No new prose pattern is invented; the wording mirrors the existing `## DjangoConnectionField` / `## apply_cascade_permissions` entry shape (Status line → contract body → See-also).
  - The **README/docs-README/TODAY/GOAL** edits are surgical in-place swaps of the existing roadmap prose + removal of the four TODO-comment anchors. No new doc structure.
  - The **CHANGELOG `### Added` bullets** follow the existing `[Unreleased]` entry shape (bold lead-in, `[ref-id]` reference-style links to GLOSSARY anchors, the same density as the `apply_cascade_permissions` bullet already there).
- **New helpers justified.** None — this is doc/DB work, no code.
- **Duplication risk avoided.** Two specific traps:
  1. **Hand-editing the rendered generated docs.** The naive Slice-5 implementation hand-edits `docs/GLOSSARY.md` / `KANBAN.md` directly — which the next regenerate silently reverts and which double-maintains the same fact in DB + markdown. The plan forces the **DB-edit-then-regenerate** path and forbids touching the rendered files except via the generator.
  2. **Re-stating the wire contract in prose that drifts from GLOSSARY.** The final public names (inputs / payload slot / FieldError / `<field>_id` / `"__all__"`) are pinned **once** in GLOSSARY (the single source of truth per spec line 535); README/TODAY/GOAL reference the shape at a summary level and link to GLOSSARY rather than re-pinning every name.

### Implementation steps

Line numbers are pin-at-write-time navigational hints — verify against current source before editing.

#### Part A — hand-authored standing docs (direct markdown edits)

1. **`README.md` — resolve the `TODO(spec-036 Slice 5)` marker (lines 57-62) and flip the DRF pitch (line 64).**
   - Delete the `<!-- TODO(spec-036 Slice 5): … -->` comment block (lines 57-62).
   - Rewrite the "Coming from DRF + django-filter?" paragraph (line 64): change "Mutations are on the roadmap: the `DjangoMutation` foundation lands in `0.0.11` …" → **shipped** wording: the model-driven `DjangoMutation` create/update/delete foundation (auto-generated `Input` / `PartialInput`, the shared `FieldError` envelope, `DjangoMutationField`) **ships today in `0.0.11`**; keep the DRF-serializer flavor (`Meta.serializer_class`) explicitly targeted at `0.0.13`. Do NOT imply the form (`0.0.12`) / serializer / auth (`0.0.13`) flavors are part of the `0.0.11` foundation (spec Doc-updates watch-out, line 539).
   - **Status line (line 66):** the README "Status" section currently reads "**`0.0.10`, single-maintainer, alpha-quality.**" and "Newest shipped surface: the cascade-permissions subsystem (`0.0.10`)". **Discretion item (see below)** — the literal package version string stays `0.0.10` (Decision 13: no version bump in this card; the `0.0.11` cut is joint). Whether to refresh the "Newest shipped surface" prose to lead with mutations is at Worker 2's discretion but should NOT assert a `0.0.11` package version. Safe default: leave the `0.0.10` version string; optionally add a sentence that the `0.0.11` mutation foundation is now built/shipped on the write side. If Worker 2 is unsure, leave the Status prose as-is except the DRF pitch above — the DRF pitch flip is the only REQUIRED README change the spec names.

2. **`docs/README.md` — resolve the `TODO(spec-036 Slice 5)` marker (lines 93-98) and move mutations to "Shipped today".**
   - Delete the `<!-- TODO(spec-036 Slice 5): … -->` comment (lines 93-98).
   - In the "Shipped today (`0.0.10`)" bullet list (lines 104-129), add a mutations bullet describing the `class Meta` create/update/delete shape: `DjangoMutation` base + `Meta.operation`, auto-generated `<Model>Input` / `<Model>PartialInput`, the shared `FieldError` envelope, `DjangoMutationField`, `DjangoModelPermission` write-auth, optimizer-composed post-write re-fetch; link `[GLOSSARY.md#djangomutation]`.
   - In the "Coming next — remaining alpha" block (lines 131-135): line 132 currently lumps "`0.0.11` — mutations + auto-generated `Input` types, and the `Upload` scalar + file/image field mapping". Since the mutation foundation now ships, **either** move the mutation half up to Shipped and leave `0.0.11 — the Upload scalar + file/image field mapping` in Coming-next (the `Upload` sibling card 037 is still unshipped), **or** keep the `0.0.11` line noting the `Upload` sibling remains. Discretion item — keep `Upload`/file-image as the still-coming `0.0.11` sibling; do not claim 037 shipped.
   - **The "Shipped today (`0.0.10`)" heading version string stays `0.0.10`** (Decision 13). The mutation bullet can note "(new in `0.0.11`)" inline the way other bullets note their ship version, without changing the section's `0.0.10` heading.

3. **`GOAL.md` — the DRF-migration mutation diff now references a shipped base (lines 458-465).**
   - Delete the `<!-- TODO(spec-036 Slice 5): … -->` comment block (lines 458-463).
   - Rewrite the prose under "### Coming from DRF + `django-filter`" (lines 464-465 + the surrounding paragraph): mark the `DjangoMutation` base itself as **shipped for model-driven operations** (`0.0.11`), keep the `SerializerMutation` / `Meta.serializer_class` flavor targeted at `0.0.13`, and keep the single shared `FieldError` envelope wording aligned with `docs/GLOSSARY.md`. The `class CreateCategory(DjangoMutation): class Meta: serializer_class = CategorySerializer` code block (lines 490-493) illustrates the **serializer flavor (`0.0.13`)** — keep it as the `0.0.13` shape, but its base class is now shipped. Add a note that a model-driven `class Meta: model = …; operation = "create"` mutation ships today (the foundation), the serializer flavor lands `0.0.13`.

4. **`TODAY.md` — resolve the `TODO(spec-036 Slice 5)` marker (lines 11-16) and add a "Mutations on products today" section.**
   - Delete the `<!-- TODO(spec-036 Slice 5): … -->` comment (lines 11-16).
   - Add a new section (placement: after "Filtering and ordering on products today" / near "Visibility filtering via `get_queryset`", products-centric) titled e.g. `## Mutations on products today`. Content (keep it products-centric per the file's scope guard, lines 5-7): products now exposes a `Mutation` with `createItem` / `updateItem` / `deleteItem` (+ at least one `Category` write) as `DjangoMutationField`s; the shared `errors: list[FieldError]` envelope (field-keyed validation messages, the `"__all__"` sentinel for `unique_item_per_category`); permission-scoped `update` / `delete` lookups (a row the caller cannot *see* via `get_queryset` is not-found, no existence leak) layered with the separate `DjangoModelPermission` write-auth (anonymous / missing model-perm denied with a top-level `GraphQLError`); the optimizer-backed post-write response re-fetch (`select_related` / `prefetch_related`, no `.only()` under the mutation operation — the G2 path). Reference `[GLOSSARY.md#djangomutation]` and keep wire examples products-shaped (Item/Category). Do NOT broaden TODAY.md to other apps.
   - **Do NOT change the file's "(`0.0.10`)" capability-version markers to `0.0.11`** for the package as a whole — the mutation section can note "(new in `0.0.11`)" inline.

#### Part B — CHANGELOG.md (AUTHORIZED direct edit, no version heading)

5. **`CHANGELOG.md` — add `### Added` bullets under the existing `[Unreleased]` heading (after line 24, within the existing `### Added` block that already holds the cascade bullets — do NOT create a new `## [0.0.11]` heading).**
   Planned `### Added` wording (one bullet per shipped contract; reference-style `[ref-id]` links to GLOSSARY anchors, matching the existing `[Unreleased]` density):

   - **`DjangoMutation` (the write-side foundation).** The package's first write surface — a [`DjangoMutation`][glossary-djangomutation] base class configured through a nested `class Meta` (`model` + `operation` ∈ `{"create", "update", "delete"}`, the DRF shape, not Strawberry decorators), exposed on the schema's `Mutation` type through the [`DjangoMutationField`][glossary-djangomutationfield] factory (the write-side sibling of [`DjangoConnectionField`][glossary-djangoconnectionfield]; assigned with no class-attribute annotation, typed from the generated payload via a `strawberry.lazy` forward-ref). Sync and async resolver paths; the write runs inside one `transaction.atomic()` (async via a single `sync_to_async(thread_sensitive=True)`). Ships under `django_strawberry_framework/mutations/` and is exported from the package root.
   - **Auto-generated `Input` / `PartialInput` types.** [Input type generation][glossary-input-type-generation] derives `<Model>Input` (each editable field required only when it has no usable Django `default` / `blank` / `null`, else optional) and `<Model>PartialInput` (every field optional) from the mutation's `Meta.model` editable fields narrowed by its own `Meta.fields` / `Meta.exclude` — reusing the read-side scalar / choice-enum / specialized-scalar converters so read and write share one wire contract. Forward FK / OneToOne map to a single `<field>_id` (a `GlobalID` for a Relay-Node target, the raw pk scalar otherwise, type-checked against the relation target at decode); M2M maps to `list[<id>]` (replace-on-provide / clear-on-empty / unchanged-on-omit). The canonical full editable shape takes the stable `<Model>Input` name; narrowed shapes take a deterministic shape-derived name, and two distinct shapes colliding on one generated name raise [`ConfigurationError`][glossary-configurationerror] at finalization. The relation-override contract from `spec-010` holds on the input side.
   - **Shared `FieldError` envelope + per-mutation payload.** A public [`FieldError`][glossary-fielderror-envelope] `@strawberry.type` (`field: str`, `messages: list[str]` — graphene-django's `ErrorType` shape) surfaced through a generated `<Name>Payload` carrying the mutated object in a uniform `node` slot (Relay-Node target) / `result` slot (non-Node target) — never model-derived — plus `errors: list[FieldError]!`. `full_clean()` validation (incl. `validate_constraints()` `UniqueConstraint` checks caught before `save()`, multi-field constraints keyed to Django's `"__all__"` sentinel) populates the envelope and returns a null object rather than raising at the GraphQL boundary. The envelope is **frozen here** for reuse by the `0.0.12` form and `0.0.13` serializer / auth flavor cards.
   - **Write authorization — `DjangoModelPermission` / `Meta.permission_classes`.** A first-class, DRF-shaped write-auth contract: [`Meta.permission_classes`][glossary-djangomodelpermission] (default `[DjangoModelPermission]`, the Django `add` / `change` / `delete` model perms) plus an overridable `check_permission(info, operation, data, instance=None)`, run by all three operations and kept **separate** from `get_queryset` visibility (can-view ≠ can-write). An unauthorized / anonymous caller is denied with a top-level `GraphQLError` before any write. `DjangoModelPermission` is exported from the package root.
   - **Permission- and optimizer-composition.** `update` / `delete` lookups run through the target type's [`get_queryset`][glossary-get_queryset-visibility-hook] (+ any [`apply_cascade_permissions`][glossary-apply_cascade_permissions]) for visibility — a row the caller cannot see is not-found, no existence leak. The post-write row is re-fetched **by pk** and routed through [`DjangoOptimizerExtension`][glossary-djangooptimizerextension] for the response selection; under the mutation operation the `spec-035` **G2** gate keeps `select_related` / `prefetch_related` but suppresses `.only(...)` column deferral, so the re-fetched instance carries no deferred-field set ([`only()` projection][glossary-only-projection] / [FK-id elision][glossary-fk-id-elision]). The products example gains a live `Mutation` write surface that discharges the `spec-035` G2 live-test handoff.

   **Reference-style link defs:** add any new `[ref-id]` defs the bullets introduce (e.g. `[glossary-djangomutationfield]`, `[glossary-djangomodelpermission]`) to CHANGELOG.md's bottom `<!-- LINK DEFINITIONS -->` block under the `<!-- docs/ -->` group, alphabetical within the group, matching the convention (verify whether CHANGELOG already defines `[glossary-djangomutation]` / `[glossary-input-type-generation]` / `[glossary-fielderror-envelope]` etc.; reuse existing defs, add only missing ones). **No `## [0.0.11]` heading; no `### Changed` / `### Fixed` for this card.**

#### Part C — DB-backed glossary + KANBAN (Django ORM edits, then regenerate) — the worker-0.md "Closing out a kanban card" procedure

Run DB edits via `uv run python examples/fakeshop/manage.py shell`; regenerate from repo root. **Use the ORM only, never raw SQL** (the `post_save` UUID side-row is load-bearing for the GraphQL render).

6. **Baseline regenerate-to-temp BEFORE any DB edit (maintainer-decision-2 verification setup).** Before mutating the DB, run the three generators writing to temp copies (or capture the current rendered output hashes) so a pre-existing render drift from the maintainer's dirty DB is separable from this card's own changes. Record the baseline. (This is the "separate pre-existing render drift from the card-move's own changes" half of the stability verification.)

7. **Seed the two net-new `GlossaryTerm` rows** (worker-0.md step 1):
   - **`DjangoMutationField`**: `anchor="djangomutationfield"`, `title="\`DjangoMutationField\`"`, `status=GlossaryStatus key "shipped"`, `status_text="shipped (\`0.0.11\`)"`, `title_sort="djangomutationfield"` (`title.replace("\`","").lower()`), `entry_order=23`, `index_order=19`. Body authored from the spec: the field factory exposing a `DjangoMutation` on the schema's `Mutation` type (write-side sibling of `DjangoConnectionField`); assigned with **no** class-attribute annotation and typed from the generated `<Name>Payload` via a `strawberry.lazy` forward-ref (the payload is generated at finalization and has no importable name at import); synthesizes the per-operation argument signature (`data: <Model>Input!` / `<Model>PartialInput!` + `id:` for update/delete); sync/async dispatch. See-also: `DjangoMutation`, `DjangoConnectionField`, `FieldError` envelope.
   - **`DjangoModelPermission`** (the write-authorization entry): `anchor="djangomodelpermission"`, `title="\`DjangoModelPermission\`"`, `status` key `shipped`, `status_text="shipped (\`0.0.11\`)"`, `title_sort="djangomodelpermission"`, `entry_order=22`, `index_order=18`. Body authored from Decision 15: the default write-auth permission class consumers pass to `Meta.permission_classes` (default `[DjangoModelPermission]`, the Django `add`/`change`/`delete` model perms); the overridable `check_permission(info, operation, data, instance=None)` seam; run by all three operations; kept **separate** from `get_queryset` visibility (can-view ≠ can-write); a denial raises a top-level `GraphQLError`. See-also: `DjangoMutation`, `get_queryset` visibility hook, Per-field permission hooks.
   - **Category memberships:** add a `GlossaryCategoryMembership` for `DjangoMutationField` in the `mutations` category (cat id 102) and for `DjangoModelPermission` in the `permissions` category (cat id 99). For each category, bump existing members into a temp band (`order += 1000`) then reassign the full desired `order` `0..N-1` to avoid the `(category, order)` unique collision. Desired `mutations` order: insert `DjangoMutationField` right after `DjangoMutation` (→ DjangoMutation, **DjangoMutationField**, DjangoFormMutation, DjangoModelFormMutation, SerializerMutation, Input type generation, FieldError envelope, Auth mutations). Desired `permissions` order: append `DjangoModelPermission` (→ get_queryset visibility hook, apply_cascade_permissions, **DjangoModelPermission**, Per-field permission hooks — or place after apply_cascade_permissions; final placement is a discretion item, keep it adjacent to the write/visibility entries).

8. **Promote the three existing planned terms** (`djangomutation`, `input-type-generation`, `fielderror-envelope`): set each `GlossaryTerm.status = GlossaryStatus.objects.get(key="shipped")`, `status_text = "shipped (\`0.0.11\`)"`, and update each `body` to the **shipped contract** (the `Meta.operation` selector, the `Input` / `PartialInput` shape with the per-field-required/optional + M2M-always-optional rule, the `<Name>Payload` wrapper with the uniform `node`/`result` slot, the `<field>_id` relation naming, the `"__all__"` sentinel, the frozen-for-flavors note for `fielderror-envelope`). Source the new body prose from the spec Decisions 6/7 and the committed GLOSSARY entry shape. **Pin the final public names** in these bodies (spec line 535) so GLOSSARY is the single source of truth: `<Model>Input` / `<Model>PartialInput` (+ shape-derived names for narrowed shapes), `<Name>Payload` + `node`/`result` slot, `FieldError`, `<field>_id`, `"__all__"`.

9. **Append the G2-handoff note to the `only-projection` term body** (worker-0.md "reconcile existing term whose body the build hand-edited"): add a one-line note that the G2 mutation gate (keep `select_related`/`prefetch_related`, suppress `.only()` on a mutation re-fetch) is now exercised **live** by the products write surface — the `spec-035` G2 live-test handoff discharged. Edit `GlossaryTerm.body` for anchor `only-projection` in the DB (NOT the rendered file).

10. **Public exports + Index (GLOSSARY).** The GLOSSARY "## Public exports" list (rendered lines 22-55, currently carries a `TODO(spec-036 Slice 5)` comment block at lines 24-29) and the "## Index" table are **also DB-rendered or partly static** — DETERMINE which during the build pass: if the Public-exports list and Index are rendered from the glossary DB (terms + a status/export flag), the new terms' `shipped` status + their presence as `GlossaryTerm` rows drives the Index status column automatically, and the Public-exports membership may need a DB flag set; if any part is a static header in a DB-stored document body, edit that body in the DB. **The `TODO(spec-036 Slice 5)` comment at GLOSSARY lines 24-29 must be removed** as part of this (it lives in whatever source the Public-exports block renders from — DB document body or the generator's static preamble; Worker 2 traces it via `scripts/build_glossary_md.py`). Net effect required: `DjangoMutationField` and `DjangoModelPermission` appear in **Public exports** and the **Index** (status `shipped (\`0.0.11\`)`); the three promoted terms show `shipped (\`0.0.11\`)` in the Index status column; the **Mutations** browse-by-category row gains `DjangoMutationField` and the **Permissions** row gains `DjangoModelPermission`. **If the Public-exports list / Index / Browse-by-category rows turn out to be a hand-maintained static block inside the generator script `scripts/build_glossary_md.py` — STOP: that script is the maintainer's owned/committed file and Slice 5 RUNS it, never edits it (per the isolation rule). Surface this to Worker 1 in `### Notes for Worker 1 (spec reconciliation)` rather than editing the script.**

11. **`SpecDoc` (worker-0.md step 2):** the row already exists for card 36 with the correct live-`docs/` url. **Verify** `SpecDoc.objects.get(card=card_36).url` contains `docs/spec-036-mutations-0_0_11.md` (NOT `docs/SPECS/`); if drifted, **update** (do NOT `.create()` — `name` is unique). Expected: no change needed.

12. **Bootstrap ≥1 glossary link (worker-0.md step 3):** create one `CardGlossaryTerm` linking card 36 to a term in the spec CSV (e.g. `djangomutation`) so the done-save invariant passes; `import_spec_terms` reconciles the full set in step 14.

13. **Flip card 36 → done (worker-0.md step 4):** `card.status = Status.objects.get(key="done"); card.save()` (ORM `.save()` fires pre_save validation + sets `milestone_id`; the rendered id auto-becomes `DONE-036-0.0.11`). The DONE-card invariant (linked `SpecDoc` ✓ from step 11 + ≥1 `CardGlossaryTerm` ✓ from step 12) must hold before this save or it raises.

14. **Sync full glossary-link set (worker-0.md step 5):** `uv run python examples/fakeshop/manage.py import_spec_terms` — creates `CardGlossaryTerm` + `GlossarySpecMention` rows from `spec-036-mutations-0_0_11-terms.csv` for every done card. **Precondition:** every anchor in that CSV must already exist as a `GlossaryTerm` row — verified above (the 39 CSV anchors are all existing terms; the two net-new public symbols are deliberately NOT in the CSV, so this precondition holds). If `import_spec_terms` reports a missing anchor, STOP and surface it.

15. **Fix card-body content the spec wrap names + tick shipped DoD CardItems (worker-0.md step 6):** mark every **shipped** `definition_of_done` `CardItem.is_complete = True` for card 36 (the done-card convention). Spec DoD items 1-7 are all shipped by this card; DoD item 8 (no version bump) is a constraint satisfied by *not* editing version files — mark it complete too if it is a tracked CardItem. **No DoD item is deferred** in this card, so no `is_complete=False` follow-up markers. Fix any stale `docs/spec-0NN-…` filename refs or `## [0.0.X]` → `[Unreleased]` text in card-body `CardItem.text` only if the spec wrap names them; otherwise leave unrelated card-body prose alone. Keep edits to what the spec authorizes.

16. **Regenerate all three docs (worker-0.md step 7), from repo root:** `uv run python scripts/build_kanban_md.py`, `uv run python scripts/build_kanban_html.py`, `uv run python scripts/build_glossary_md.py`. These OVERWRITE the rendered `KANBAN.md`, `KANBAN.html`, `docs/GLOSSARY.md`. **Never hand-edit those three rendered files; never edit the three generator scripts.**

#### Verification (worker-0.md step 8, adapted for the dirty-DB baseline per maintainer decision 2)

17. **`uv run python examples/fakeshop/manage.py import_spec_terms --check`** → reports OK for all done cards (card 36 included).
18. **Regenerate STABILITY:** run the three generators **twice consecutively** and hash the outputs — the second regenerate must produce **byte-identical** `docs/GLOSSARY.md` / `KANBAN.md` / `KANBAN.html` to the first. (A naive `git diff` clean-vs-HEAD is NOT the gate here because the DB baseline is maintainer-dirty.) Compare the post-edit regenerate against the **step-6 baseline-to-temp** capture to confirm the only deltas are this card's intended changes (the three promotions, two net-new entries, the `only-projection` note, the card→DONE move, the DoD ticks) plus any pre-existing render drift the baseline already showed — NOT new unexplained drift.
19. **`KANBAN.md` shows `DONE-036-0.0.11`** in the Done section with its DoD ticked and the spec map pointing at the live `docs/spec-036-mutations-0_0_11.md`.
20. **`docs/GLOSSARY.md`** shows the three promoted entries as `shipped (\`0.0.11\`)`, the two net-new entries present (in the right alphabetical slots, in Public exports + Index + the Mutations/Permissions browse rows), the `only-projection` G2 note, and **no remaining `TODO(spec-036 Slice 5)` comment** (the GLOSSARY Public-exports TODO block removed).
21. **No remaining `TODO(spec-036 Slice 5)` markers** anywhere: `grep -rn "TODO(spec-036 Slice 5)"` across `README.md`, `docs/README.md`, `GOAL.md`, `TODAY.md`, `docs/GLOSSARY.md` returns nothing.
22. **`uv run python examples/fakeshop/manage.py check`** passes.
23. **No version-file edits (Decision 13 / DoD item 8):** `git diff` shows `pyproject.toml`, `django_strawberry_framework/__init__.py` (`__version__`), `tests/base/test_init.py`, `uv.lock` **byte-unchanged**. (The four public symbols' `__all__` + `tests/base/test_init.py` exports-pin were already landed in Slices 1-3; Slice 5 adds NO new symbol and must not touch them.)
24. **CSV unchanged:** `spec-036-mutations-0_0_11-terms.csv` is NOT edited to add `DjangoMutationField` / `DjangoModelPermission` (they have headings now but stay out of the CSV per spec DoD item 1 / Risks line 554).

Hand the regenerated `KANBAN.md` / `KANBAN.html` / `docs/GLOSSARY.md` + `examples/fakeshop/db.sqlite3` (and the hand-edited `README.md` / `docs/README.md` / `GOAL.md` / `TODAY.md` / `CHANGELOG.md`) to the maintainer for review and commit. **Workers never commit.**

### Test additions / updates

**None — doc-only + DB-data slice.** No package source or test changes. The CHANGELOG / KANBAN / GLOSSARY surfaces are validated by Worker 3's "Documentation / release sanity" + "CHANGELOG sanity" review checks and by the worker-0.md regenerate-stability verification above, not by `pytest`. The package suite, Django `check`, and `makemigrations --check` are exercised at the final test-run gate (`bld-final.md`), not in this slice.

### Implementation discretion items

These are assessed-and-decided to be Worker 2's choice (style / placement / equivalent shape); none is an architectural escape hatch:

1. **README.md "Status" prose refresh beyond the DRF pitch.** The DRF-pitch flip (step 1) is REQUIRED. Whether to also refresh the "Newest shipped surface" sentence to lead with the mutation foundation is Worker 2's call — provided it does **not** assert a `0.0.11` package version (the literal `0.0.10` version string stays, Decision 13). Safe default: leave Status prose, flip only the DRF pitch.
2. **Where `0.0.11` mutations land in `docs/README.md`'s "Coming next" list.** Move the mutation half to Shipped and keep the `Upload` sibling in `0.0.11`-coming-next, vs. keep a `0.0.11` line noting `Upload` still pending — either reads correctly as long as 037 (`Upload`) is NOT claimed shipped.
3. **Exact placement of the new `TODAY.md` mutations section** (after filtering/ordering vs. near the `get_queryset` visibility section) — any products-centric placement that keeps the file's scope guard intact.
4. **`DjangoModelPermission` membership order within the `permissions` browse category** (after `apply_cascade_permissions` vs. after `Per-field permission hooks`) — keep it adjacent to the write/visibility entries.
5. **GLOSSARY net-new entry body wording** — author from the spec contract; mirror the existing entry shape (`**Status:**` → contract body → **See also:**). The load-bearing requirement is that the final public names are pinned; the prose around them is discretion.
6. **CHANGELOG bullet granularity** — five bullets as drafted vs. a coherent merge, as long as every shipped contract (DjangoMutation base + Meta.operation; auto-gen Input/PartialInput; FieldError envelope + node/result payload; DjangoMutationField; DjangoModelPermission/check_permission write-auth; the G2 mutation re-fetch) is named and no version heading is added.

### Spec slice checklist (verbatim)

The spec's Slice 5 nested sub-bullets from `## Slice checklist` (lines 65-66), copied verbatim. Worker 2 ticks each `- [x]` as it lands the sub-check; Worker 1 audits at final verification.

- [x] Slice 5: doc updates + card-completion wrap (per [Doc updates](#doc-updates))
  - [x] [`docs/GLOSSARY.md`][glossary] (promote [`DjangoMutation`][glossary-djangomutation] / [Input type generation][glossary-input-type-generation] / [`FieldError` envelope][glossary-fielderror-envelope] to `shipped (0.0.11)`, add the net-new `DjangoMutationField` **and `DjangoModelPermission` / write-auth** entries, pin the final public names — inputs / payload `node`·`result` slot / `FieldError` / `<field>_id` / `"__all__"` sentinel (AR test/doc correction), and append the G2-handoff-discharged note to the [`only()` projection][glossary-only-projection] entry), [`docs/README.md`][docs-readme] / [`README.md`][readme] (move mutations from "Coming next" to "Shipped today"), [`GOAL.md`][goal] (the DRF-migration mutation diff now references a shipped base), [`TODAY.md`][today] (products now demonstrates a write surface), [`CHANGELOG.md`][changelog] (only if the Slice 5 maintainer prompt explicitly requests it), [`KANBAN.md`][kanban] (card → Done via the kanban DB + re-render). **No version-file edits** ([Decision 13](#decision-13--version-bumps-are-owned-by-the-joint-0011-cut)).

---

## Build report (Worker 2)

Doc-only + DB-backed card-wrap slice. Zero package source / test changes. Generator scripts NOT edited (run-only). No version-file edits. The two net-new public symbols stayed OUT of the terms CSV.

### Files touched

**Prose docs (direct markdown edits):**
- `README.md` — removed the `TODO(spec-036 Slice 5)` comment block; flipped the "Coming from DRF + django-filter?" pitch from roadmap to *shipped* (model-driven `DjangoMutation` create/update/delete ships in `0.0.11` with auto-gen `<Model>Input`/`<Model>PartialInput`, the `FieldError` envelope, `DjangoMutationField`; DRF-serializer flavor stays explicitly `0.0.13`). Discretion item 1: left the `0.0.10` Status version string and Status prose unchanged (Decision 13, safe default).
- `docs/README.md` — removed the `TODO(spec-036 Slice 5)` comment; added a "mutations + auto-generated `Input` types (new in `0.0.11`)" bullet to **Shipped today**; changed the "Coming next" `0.0.11` line to name only the `Upload` sibling (037 not claimed shipped — discretion item 2). Added `[glossary-djangomutation]` link def.
- `GOAL.md` — removed the `TODO(spec-036 Slice 5)` comment block; rewrote the "Coming from DRF + `django-filter`" prose so the `DjangoMutation` base is shipped (`0.0.11`) for model-driven ops while the `Meta.serializer_class` flavor stays `0.0.13`; split the code block into a shipped model-driven `CreateCategory` (`Meta: model + operation="create"`) and a `0.0.13` `CreateCategoryFromSerializer` (`Meta.serializer_class`).
- `TODAY.md` — removed the `TODO(spec-036 Slice 5)` comment; added a products-centric "## Mutations on products today" section after the `get_queryset` visibility section (discretion item 3) covering create/update/delete over `Item`/`Category`, the `errors: list[FieldError]` envelope + `"__all__"` sentinel, permission-scoped lookups, the separate `DjangoModelPermission` write-auth, and the optimizer-backed post-write re-fetch under the G2 gate; kept it products-shaped. Added `[glossary-djangomutation]` link def.
- `CHANGELOG.md` — **AUTHORIZED** edit: added five `### Added` bullets under the existing `[Unreleased]` heading (DjangoMutation base+field factory; auto-gen Input/PartialInput; FieldError envelope+payload; DjangoModelPermission write-auth; permission+optimizer composition / G2 live handoff). **No `## [0.0.11]` version heading.** Added six missing `[ref-id]` defs to the `<!-- docs/ -->` group (apply_cascade_permissions, djangomutation, djangomutationfield, djangomodelpermission, fielderror-envelope, input-type-generation); reused existing defs otherwise. All 12 ref-ids used in the new bullets verified to resolve.

**DB-backed generated docs (DB edited via Django ORM, then regenerated — NEVER hand-edited):**
- `docs/GLOSSARY.md` (regenerated) — promotions, two net-new entries, public-exports + Index + browse rows, G2 note, reconciled drift.
- `KANBAN.md` / `KANBAN.html` (regenerated) — card 36 → `DONE-036-0.0.11`.
- `examples/fakeshop/db.sqlite3` — the ORM edits below. (DB was already dirty at slice start — maintainer's, approved; my edits add to it, the pre-existing dirtiness is NOT reverted.)

**DB edits (Django ORM `manage.py shell`, never raw SQL):**
- Promoted `GlossaryTerm` `djangomutation` / `input-type-generation` / `fielderror-envelope` → `status=shipped`, `status_text="shipped (\`0.0.11\`)"`, bodies rewritten to the shipped contract (pinning the final public names: `<Model>Input` / `<Model>PartialInput` + shape-derived names, `<Name>Payload` + uniform `node`/`result` slot, `FieldError`, `<field>_id`, `"__all__"` sentinel; frozen-for-flavors note on FieldError).
- Seeded two net-new `GlossaryTerm` rows: `DjangoMutationField` (`entry_order=23, index_order=19`, after `DjangoMutation`) and `DjangoModelPermission` (`entry_order=22, index_order=18`, after `DjangoModelFormMutation`) — both `shipped (\`0.0.11\`)`.
- Added `GlossaryCategoryMembership` rows: `DjangoMutationField` into `mutations` (after `DjangoMutation`), `DjangoModelPermission` into `permissions` (after `apply_cascade_permissions`, discretion item 4) — temp-band reassign to dodge the `(category, order)` unique constraint.
- Appended the G2-handoff-discharged note to the `only-projection` term body.
- Edited the `public-exports` `BoardDoc` body (namespace `glossary`, key `public-exports` — the DB document body the generator renders, NOT a generator-script static block, so the plan's STOP caveat did not trigger): added `DjangoModelPermission` / `DjangoMutation` / `DjangoMutationField` / `FieldError` export lines in alphabetical position. The `TODO(spec-036 Slice 5)` at rendered GLOSSARY line 24 was render-drift (never in the DB body) and disappeared on regenerate.
- `SpecDoc` for card 36 verified (already exists, live `docs/spec-036-mutations-0_0_11.md` url) — no-op confirm, no `.create()`.
- Bootstrapped one `CardGlossaryTerm` (card 36 → `djangomutation`), flipped card 36 `status=done` (rendered id auto-became `DONE-036-0.0.11`), ran `import_spec_terms` (synced the full link set), marked all 6 `definition_of_done` `CardItem.is_complete=True`.
- **Reconciled pre-existing committed-ahead-of-DB GLOSSARY drift** (worker-0.md step 1 note): synced the committed `DjangoListField` (`is_async_callable`) and `GraphQLTestCase` (`graphene-django utils/testing`) bodies into the DB so the regenerate did not revert that shipped doc content. This is maintainer drift unrelated to mutations, surfaced under Notes for Worker 1.

### Tests added or updated

**None — doc-only + DB-data slice.** No package source or test changes.

### Validation run

- `uv run ruff format .` — pass (282 files left unchanged; no Python touched by this slice).
- `uv run ruff check --fix .` — pass ("All checks passed!").
- **Baseline regenerate-to-temp BEFORE any DB edit** (maintainer decision 2): captured the pure-DB render of all three docs vs the committed files. KANBAN.md / KANBAN.html showed no baseline drift; `docs/GLOSSARY.md` showed pre-existing drift in 4 areas (the TODO comment [removed on regenerate], `DjangoListField`, `GraphQLTestCase`, `Input type generation` bodies — committed ahead of DB).
- **Regenerate STABILITY** (two consecutive regenerates): `docs/GLOSSARY.md` / `KANBAN.md` / `KANBAN.html` all byte-identical across runs (shasum-verified YES/YES/YES).
- **Post-edit regenerate vs step-6 baseline diff:** the only deltas are this card's intended changes (3 promotions, 2 net-new entries + memberships, public-exports + Index + browse rows, the G2 note, the card→DONE move + DoD ticks, the two reconciled drift bodies). No new unexplained drift.
- `uv run python examples/fakeshop/manage.py import_spec_terms --check` — `OK: 36 done cards have glossary links.`
- `uv run python examples/fakeshop/manage.py check` — `System check identified no issues (0 silenced).`
- `uv run python scripts/check_spec_glossary.py --spec docs/spec-036-mutations-0_0_11.md` — `OK: 38 terms`.
- `KANBAN.md` shows `DONE-036-0.0.11` in the Done section, all 6 DoD `- [x]`, spec map → `docs/spec-036-mutations-0_0_11.md` (live working path, NOT `docs/SPECS/`).
- `grep -rn "TODO(spec-036 Slice 5)"` across README/docs-README/GOAL/TODAY/GLOSSARY → none.
- **Version files byte-unchanged** (Decision 13 / DoD item 8): `git diff` shows no change to `pyproject.toml`, `uv.lock`; `django_strawberry_framework/__init__.py` and `tests/base/test_init.py` carry only the prior-slice `__all__` export additions (the four public symbols + a comment), `__version__` stays `"0.0.10"` — no edit by this slice.
- **Terms CSV unchanged:** `docs/spec-036-mutations-0_0_11-terms.csv` not edited; `DjangoMutationField` / `DjangoModelPermission` are NOT anchors in it (the only string match is inside the `DjangoConnectionField` row's description).
- **Generator scripts NOT edited:** `git diff` of `scripts/build_kanban_md.py` / `build_kanban_html.py` / `build_glossary_md.py` is empty.
- `git status --short`: every modified file classified — Slice-5-intended (the 5 prose docs, the 3 regenerated DB docs, `db.sqlite3`) stay; all other modified files are prior-slice (1-4) work uncommitted because the build runs end-to-end (no maintainer pause between slices) and are out of this slice's scope. No unrelated tool churn introduced by this slice.

### Implementation notes

- **`public-exports` is a DB `BoardDoc` body, not a generator-script static block.** `scripts/build_glossary_md.py` renders the Public-exports section from a `BoardDoc` (namespace `glossary`, key `public-exports`) and the Index / Browse-by-category from `allGlossaryTerms` / `allGlossaryCategoryMemberships`. All three are DB-driven, so the plan's STOP caveat (hand-maintained static block inside the generator) did NOT trigger — I edited the DB body, never the script.
- **Category temp-band reassign needs a band above the new member's seed order.** First attempt seeded the new membership at `order=1000` then bumped existing members `+1000` — an existing `order=0` → `1000` collided with the new member on the `(category, order)` unique constraint. Fixed by an idempotent two-stage reassign: stage 1 sets every membership to a unique high temp order (`5000 + index`), stage 2 assigns the final `0..N-1`. SQLite autocommits each `.save()`, so the first run's promotions/seeds persisted; the fixup script is fully idempotent against that partial state.
- **`only-projection` G2 note inserted into the existing 0.0.10 G2 paragraph** (appended after `(see [\`DjangoOptimizerExtension\`]...)`), not as a new trailing paragraph, so the 0.0.10 and 0.0.11 G2 prose read as one thread.
- **GLOSSARY entry bodies authored from the spec Decisions 6/7/15**, mirroring the existing `**Status:** → body → **See also:**` shape (discretion item 5). The final public names are pinned in the bodies (the load-bearing requirement); surrounding prose is the discretion.
- **CHANGELOG bullets kept as five** (discretion item 6) so each shipped contract is individually named; no version heading.

### Notes for Worker 3

- Generated docs are DB-backed: review `docs/GLOSSARY.md` / `KANBAN.md` / `KANBAN.html` as render output, not hand-edits. The authoritative source of the GLOSSARY/KANBAN changes is the DB (`examples/fakeshop/db.sqlite3`); a `git diff` of the rendered files is the visible artifact but the DB edits drive it. Regenerate-stability + the baseline-diff (above) are the correctness gate, not a clean `git diff` vs HEAD (the DB baseline was maintainer-dirty).
- The `docs/GLOSSARY.md` git-diff vs HEAD will include the reconciled `DjangoListField` / `GraphQLTestCase` bodies. Those are NOT new content — they restore the committed file's existing text into the DB so the regenerate preserves it (pre-existing maintainer drift). Compare against `/tmp/slice5_baseline/GLOSSARY.regen.md` (the pure-DB pre-edit render) to see they net to zero change vs the committed file for those two entries.
- The remaining `TODO-ALPHA-036` mentions in `KANBAN.md` (4, unchanged from baseline) are raw card-body prose in OTHER cards (037/038/040) naming card 036 by its old id — pre-existing, not this slice's surface.
- Markdown link sanity verified: all reference-style ref-ids in the 5 prose docs resolve; all in-page `(#anchor)` links in the new GLOSSARY entries resolve.

### Notes for Worker 1 (spec reconciliation)

- **Stale card-DoD filename ref left as-is (recorded, not reconciled).** The card 36 `definition_of_done` CardItem 0 reads "Add `docs/spec-mutations.md`" — the card's own DoD names `docs/spec-mutations.md`, but the spec lives at `docs/spec-036-mutations-0_0_11.md` (spec Decision 1; the conflict is explicitly *recorded, not silently reconciled* per spec Risks line 555 and the NEXT.md boundary rule, and worker-0.md warns against partial-fixing a reference that diverges across multiple un-editable surfaces). I marked the item complete (it IS satisfied by the structured spec) but did NOT rewrite the CardItem text. Flagging for the deferred-work catalog.
- **Pre-existing committed-ahead-of-DB GLOSSARY drift reconciled into the DB** (`DjangoListField` `is_async_callable` body; `GraphQLTestCase` `graphene-django utils/testing` body). This is maintainer drift unrelated to mutations; worker-0.md step 1 explicitly instructs syncing such committed bodies into the DB so the regenerate does not revert them. No content change vs the committed file — only the DB now matches. If Worker 1 prefers this stay out of the Slice-5 DB diff, the alternative (let the regenerate revert committed shipped content) is a regression, so I synced them; surface for awareness.
- **`DjangoModelPermission` glossary entry title/anchor.** The spec (Doc-updates line 534, DoD item 7) names a "net-new write-authorization entry naming the `check_permission` / `Meta.permission_classes` seam and the `DjangoModelPermission` default" and adds `DjangoModelPermission` to Public exports + Index. I titled the entry `` `DjangoModelPermission` `` (anchor `djangomodelpermission`) per the build plan steps 7/10, since it is the named public symbol. The entry body covers the full `check_permission` / `Meta.permission_classes` seam.

---

## Review (Worker 3)

Doc-only + DB-backed-generated-doc slice. The load-bearing checks are `### Documentation / release sanity` and `### CHANGELOG sanity`. Reviewed the working-tree diffs for the five prose docs (`README.md`, `docs/README.md`, `GOAL.md`, `TODAY.md`, `CHANGELOG.md`), the three DB-backed rendered docs (`docs/GLOSSARY.md`, `KANBAN.md`, `KANBAN.html`), and re-ran the read-only verifications (regenerate stability, `import_spec_terms --check`, `check_spec_glossary.py`) without leaving the tree or DB in a different state than I found it.

### High:

None.

### Medium:

None.

### Low:

#### L5-1 — Moved DONE card body carries a stale free-text `- Status: In progress` line

The rendered `DONE-036-0.0.11` card body shows `- Status: In progress` (KANBAN.md, in the Done section). This is the per-card free-text `status_text` DB field, **distinct** from the kanban `status` key that drives section placement and the `DONE-` id prefix. It is cosmetic/historical metadata, not an authoritative doneness signal — verified that several genuinely-shipped DONE cards in the *committed* file carry the same kind of stale body text (`DONE-033`/`032`/`030` render `- Status: Planned`; `DONE-031` renders `- Status: Needs spec`), so this is established renderer/data behavior, not a Slice-5 regression. The load-bearing doneness signals are all correct: the card is under `## Done`, the rendered id is `DONE-036-0.0.11`, all six DoD checkboxes are `[x]`, and the card's Glossary-terms table + spec map show `shipped (0.0.11)` at the live spec path. Recommended change: none required for this slice (consistent with the existing board); a maintainer follow-up could normalize body `status_text` on the card-close ORM move so DONE cards read `Shipped`, but that is a generator/data convention question spanning all historical cards, out of this slice's authorized scope.

### DRY findings

Minimal, as expected for a doc/DB slice. The final public names (inputs / payload `node`·`result` slot / `FieldError` / `<field>_id` / `"__all__"` sentinel) are pinned **once** in `docs/GLOSSARY.md` (the single source of truth per spec line 535); `README.md` / `docs/README.md` / `GOAL.md` / `TODAY.md` reference the shape at a summary level and link to the GLOSSARY anchor rather than re-pinning every name — no prose-vs-glossary drift introduced. The CHANGELOG `### Added` bullets reuse the existing reference-style `[ref-id]` link convention and the `[Unreleased]` bullet density. No duplicated literals/keys/structures.

### Public-surface check

Confirmed **Slice 5 made NO `django_strawberry_framework/__init__.py` change**. The `M` on that file is the cumulative Slices 1-3 public-export block (per build-plan context flag line 20): it adds the four net-new symbols `DjangoModelPermission` / `DjangoMutation` / `DjangoMutationField` / `FieldError` to the import + `__all__` and removes the resolved `TODO(spec-036 Slice 1-3)` comment. `__version__` stays `"0.0.10"`; `pyproject.toml` / `uv.lock` byte-unchanged; `tests/base/test_init.py::test_version` untouched (its diff is only the `__all__` exports-pin + a comment refresh, both cumulative). The four symbols are spec-authorized (Decision 5 / Decision 15). No Slice-5 public-surface change.

### CHANGELOG sanity

The slice touches `CHANGELOG.md` — full check performed:

- **Version line / heading.** Five `### Added` bullets land under the existing `## [Unreleased]` heading. **No new `## [0.0.11]` version heading was promoted** (the most-recent dated heading is `## [0.0.9] - 2026-06-13`), so Decision 13 / DoD item 8 are satisfied. `### Added` is the spec-authorized heading; no spurious `### Changed` / `### Fixed` / `### Removed` for this card.
- **Heading authorized.** Spec Doc-updates line 542 authorizes `### Added` bullets under `[Unreleased]`; the maintainer prompt authorized the `CHANGELOG.md` edit (build-plan context flag line 18). Confirmed.
- **Wording matches shipped behavior, overstates/understates nothing.** The five bullets name exactly the shipped contracts: (1) `DjangoMutation` base + `Meta`/`operation` selector + `DjangoMutationField` factory + the `transaction.atomic()` / single-`sync_to_async(thread_sensitive=True)` boundary; (2) auto-gen `<Model>Input`/`<Model>PartialInput` with the per-field-required-by-default rule, `<field>_id` / `list[<id>]` relation mapping, shape-derived names + `ConfigurationError` collision, `spec-010` override; (3) the `FieldError` envelope (`field: str`, `messages: list[str]`, `ErrorType` shape) + `<Name>Payload` uniform `node`/`result` slot + `validate_constraints()`/`"__all__"` + frozen-here; (4) `DjangoModelPermission` / `Meta.permission_classes` write-auth + `check_permission` + top-level `GraphQLError`; (5) the permission+optimizer composition / G2 live handoff. All four public symbols, the envelope, and the G2 composition are present and accurate.
- **Verbatim spot-check.** `diff`'d the shipped bullets against the plan's pinned canonical wording (artifact step 5). Two trivial deltas, both Worker-2 discretion (plan discretion item 6) and both improvements: bullet 1 "with no class-attribute annotation**,** typed" → "**and** typed" (grammar); bullet 5 heading "Permission- and optimizer-composition." → "...**for mutations**." (clarity). NOT verbatim-copy violations — the plan bullets are Worker 1's draft, not spec-pinned canonical text; the spec pins no verbatim CHANGELOG block. Both read coherently against shipped behavior.
- **Link defs resolve.** All 12 reference-style `[ref-id]`s used in the new bullets have link definitions in the `<!-- docs/ -->` group, and all resolve to real `##` headings in `docs/GLOSSARY.md`. Six net-new defs were added (apply_cascade_permissions, djangomutation, djangomutationfield, djangomodelpermission, fielderror-envelope, input-type-generation), alphabetical within the group.

### Documentation / release sanity

The slice touches docs + KANBAN + GLOSSARY — full check performed:

- **Version strings / shipped-planned statuses / card IDs.** Package version stays `0.0.10` (no bump landed, Decision 13) — verified `pyproject.toml`/`uv.lock` byte-unchanged and `__version__ = "0.0.10"`. `docs/GLOSSARY.md` "Current package version: `0.0.10`" line is unchanged. The README/docs-README "Shipped today (`0.0.10`)" section headings stay `0.0.10`; the new mutation bullets correctly note "(new in `0.0.11`)" inline without asserting a `0.0.11` package version. README Status prose left at `0.0.10` (discretion item 1, safe default).
- **DONE-036 card move.** `TODO-ALPHA-036-0.0.11` (rendered `WIP-ALPHA-036-0.0.11`) was removed from the WIP section and appears in the Done section **exactly once** as `DONE-036-0.0.11`. Its `SpecDoc` reference points at the **live** `docs/spec-036-mutations-0_0_11.md` path (NOT `docs/SPECS/`), in both the WIP/DONE spec-map table and the card body. All six `definition_of_done` checkboxes are ticked `- [x]`. The progress rollup updated coherently (35→36 done, Alpha 79.5%→81.8%). Cross-references to `036` in sibling cards (035 / 037 / 038 / 039 / 040 / beta cards) flipped `WIP-ALPHA-036` → `DONE-036`, and the `DjangoMutation` / `Input type generation` glossary-term-status cells inside other cards' tables (035 and the index regions) flipped `planned for 0.0.11` → `shipped (0.0.11)`.
- **GLOSSARY promotions + net-new entries.** The three entries (`DjangoMutation` / `Input type generation` / `FieldError` envelope) are promoted to `shipped (0.0.11)` (status line + Index status column + the other cards' term tables). The two net-new entries exist: `## \`DjangoMutationField\`` (alphabetically after `DjangoMutation`) and `## \`DjangoModelPermission\`` (after `DjangoModelFormMutation`, before `DjangoMutation`) — both `shipped (0.0.11)`. Both appear in **Public exports**, in the **Index** with status `shipped (\`0.0.11\`)`, the **Mutations** browse-by-category row gained `DjangoMutationField`, and the **Permissions** browse row gained `DjangoModelPermission`. The final public names are pinned in the entry bodies. The `only()` projection entry carries the appended G2-handoff-discharged note. The GLOSSARY Public-exports `TODO(spec-036 Slice 5)` block is gone.
- **Markdown links.** All new in-doc anchors resolve: the six GLOSSARY anchors used by the CHANGELOG (`#djangomodelpermission` / `#djangomutation` / `#djangomutationfield` / `#fielderror-envelope` / `#input-type-generation` / `#only-projection`) map to real `##` headings; the new `[glossary-djangomutation]` ref-ids added to `docs/README.md` (line 276) and `TODAY.md` (line 335) have link defs.
- **Verbatim spot-check (GLOSSARY bodies + KANBAN card body).** The GLOSSARY entry bodies are authored-from-spec (Decisions 5/6/7/15), not a verbatim spec-block copy — the spec provides no fenced canonical GLOSSARY body to `diff` against (unlike a code drop-in). Cross-read each body against its Decision: every final public name the spec mandates is pinned character-correct (`<Model>Input`/`<Model>PartialInput` + shape-derived names; `<Name>Payload`; uniform `node`/`result` slot never model-derived; `FieldError` `field: str`/`messages: list[str]`; `<field>_id` GlobalID-vs-rawpk type-checked-at-decode; `list[<id>]` replace/clear/omit; `"__all__"` sentinel; `Meta.permission_classes` default `[DjangoModelPermission]` + `check_permission(info, operation, data, instance=None)`). This is the plan's discretion item 5 (author from spec, pin the names). The KANBAN card body is rendered from the DB and carries the card's own DoD text verbatim (including the `docs/spec-mutations.md` filename — see Notes for Worker 1).
- **No obsolete wording.** All four `TODO(spec-036 Slice 5)` markers in `README.md` / `docs/README.md` / `GOAL.md` / `TODAY.md` and the GLOSSARY Public-exports `TODO` block are removed (`grep` returns none). No "on the roadmap" / "coming soon" / "planned" mutation wording remains: README's DRF pitch flipped to "Mutations ship today too"; docs-README moved the mutation half to Shipped and left only the `Upload` sibling in the `0.0.11` Coming-next line (037 not claimed shipped, discretion item 2); GOAL split the code block into a shipped `0.0.11` model-driven `CreateCategory` and a `0.0.13` `CreateCategoryFromSerializer`, both on the shipped base; TODAY added the products-centric "Mutations on products today" section, kept products-scoped.
- **Terms-CSV exclusion.** `docs/spec-036-mutations-0_0_11-terms.csv` is byte-unchanged; `DjangoMutationField` / `DjangoModelPermission` are NOT anchors in it (spec DoD item 1 / Risks line 554). `check_spec_glossary.py` → `OK: 38 terms`.

**Regenerate stability + generator-script integrity + import_spec_terms.** Re-ran all three generators twice consecutively — `docs/GLOSSARY.md` / `KANBAN.md` / `KANBAN.html` are byte-identical across both runs (shasum-stable) AND byte-identical to the committed working-tree render (the rendered files already match the DB; the regenerate changed nothing and left the tree's `M` set unchanged). `import_spec_terms --check` → `OK: 36 done cards have glossary links.` (card 36 included). The three generator scripts (`build_kanban_md.py` / `build_kanban_html.py` / `build_glossary_md.py`) have an empty `git diff` — confirmed UNCHANGED (run-not-edit honored). The spec body (`docs/spec-036-mutations-0_0_11.md`) `M` is Worker 1's status-header roll + Slice-1 Decision-6 edit — out of Slice-5 review scope, confirmed not a Slice-5 deliverable.

**Worker 2's two flags.**

1. **Card-DoD `docs/spec-mutations.md` filename conflict** — verified consistent, not a partial-fix. The card body still reads `- [x] Add docs/spec-mutations.md.` (recorded, not silently reconciled), which matches spec Risks line 555 and DoD item 1 (the spec lives at `docs/spec-036-mutations-0_0_11.md`). Worker 2 correctly ticked the item complete (the structured spec satisfies the intent) without rewriting the stale card-body string. The authoritative spec references — the KANBAN WIP/DONE spec map and the `SpecDoc` url — both point at the live `docs/spec-036-mutations-0_0_11.md`, so no surface diverges. Carried to the deferred-work catalog (Notes for Worker 1). Not a finding.

2. **Pre-existing `DjangoListField` / `GraphQLTestCase` drift-body reconciliations** — verified net-zero vs the committed `docs/GLOSSARY.md`. Neither entry's own body shows any change in the working-tree GLOSSARY diff (`GraphQLTestCase` has zero diff lines; the only `DjangoListField` match is a cross-reference link *inside the new `DjangoMutationField` entry body*, not an edit to the `DjangoListField` entry). Worker 2 synced the DB to already-committed doc content so the regenerate preserves it — exactly worker-0.md step 1's instruction (otherwise the regenerate would revert committed shipped content, a regression). Not a finding.

**Verbatim sub-check boxes Worker 2 ticked.** The Plan's `### Spec slice checklist (verbatim)` has the Slice-5 top box and its single nested sub-bullet both ticked `- [x]`. Confirmed both landed: the GLOSSARY promotions + the two net-new entries + the pinned public names + the `only()` G2 note (the nested sub-bullet's contract) are all present in the regenerated `docs/GLOSSARY.md`, and the README/docs-README "Shipped today" move, the GOAL shipped-base diff, the TODAY products write surface, the CHANGELOG `[Unreleased]` bullets, and the KANBAN card→Done move are all present. No over-tick.

### What looks solid

- The DB-edit-then-regenerate discipline was followed correctly: the rendered docs are at a stable fixed point (two consecutive regenerates byte-identical, and identical to the committed tree), generator scripts untouched, and the DONE-card invariants (`SpecDoc` + ≥1 `CardGlossaryTerm` + `import_spec_terms`) all hold.
- The single-source-of-truth split is clean — final public names pinned once in GLOSSARY, summarized-and-linked everywhere else; no prose-vs-glossary drift.
- The CHANGELOG correctly adds `[Unreleased] ### Added` bullets with no version-heading promotion, accurately naming all four public symbols + the envelope + the G2 composition, with every ref-id resolving.
- No version-file edit anywhere (Decision 13 / DoD item 8) and no Slice-5 public-surface change; the terms CSV correctly excludes the two net-new symbols.
- All four `TODO(spec-036 Slice 5)` markers + the GLOSSARY Public-exports TODO block resolved and removed.

### Temp test verification

No temp tests created. This is a doc-only + DB-backed-generated-doc slice with zero package source / test changes; the correctness gate is the documentation/release sanity + CHANGELOG sanity reviews plus the regenerate-stability / `import_spec_terms --check` / `check_spec_glossary.py` verifications (all run read-only above), not `pytest`. Nothing to promote.

### Notes for Worker 1 (spec reconciliation)

- **Deferred-work catalog item — card-DoD filename conflict.** Carry forward: card 36's `definition_of_done` CardItem 0 reads "Add `docs/spec-mutations.md`" while the structured spec lives at `docs/spec-036-mutations-0_0_11.md` (spec Decision 1; conflict *recorded-not-reconciled* per spec Risks line 555). Worker 2 marked the item complete and left the card-body text intact (correct, per the NEXT.md "prefer the structured spec, surface the conflict" boundary rule). Surface this in `bld-final.md`'s `### Deferred work catalog`; no per-card rewrite is owed within this build.
- **Low L5-1 (optional maintainer follow-up):** the moved DONE card body's free-text `- Status: In progress` is consistent with other historical DONE cards (several render `Planned`/`Needs spec`). Normalizing the `status_text` DB field on the card-close ORM move is a generator/data convention question spanning all historical cards — not this slice's scope, and not blocking.

### Review outcome

`review-accepted`. Zero High, zero Medium, one Low (cosmetic, consistent with the existing board, recorded with a rejection-of-blocking-status reason). All load-bearing checks pass: version strings stay `0.0.10`, the DONE-036 card moved exactly once with the live spec path and ticked DoD, the three GLOSSARY promotions + two net-new entries + Public-exports/Index/browse rows + the G2 note all landed, the verbatim/authored-from-spec text pins the canonical public names, no obsolete wording remains, the terms CSV correctly excludes the two net-new symbols, the CHANGELOG adds `[Unreleased] ### Added` bullets with no version-heading promotion, the regenerate is stable + byte-clean, `import_spec_terms --check` reports OK, the generator scripts are unchanged, and Slice 5 made no `__init__.py` change. Worker 2's two flags are both verified consistent (not findings); the filename conflict is carried for the deferred-work catalog.

---

## Final verification (Worker 1)

**Status: `final-accepted`.** All Slice-5 contracts landed; the one focused-test failure is a pre-existing HEAD inconsistency outside this slice's (and this build's) scope, surfaced to Worker 0 / the maintainer and the deferred-work catalog. Details below.

### Spec slice checklist audit

Both verbatim boxes (the Slice-5 top box + its single nested sub-bullet) are `- [x]` and both genuinely landed in the working tree — no over-tick, no silently un-ticked box:

- **GLOSSARY promotions** — `DjangoMutation` (entry line 381) / `Input type generation` (630) / `FieldError` envelope (492) all show `**Status:** shipped (\`0.0.11\`)` and `shipped (\`0.0.11\`)` in the Index (lines 79 / 94 / 85). Verified.
- **Two net-new entries** — `## \`DjangoModelPermission\`` (line 373) and `## \`DjangoMutationField\`` (389), both `shipped (\`0.0.11\`)`, in the right alphabetical slots (`djangomodelpermission` < `djangomutation` < `djangomutationfield`). Both in **Public exports** (lines 30 / 32) + **Index** (78 / 80); Mutations browse row gained `DjangoMutationField` (line 162), Permissions browse row gained `DjangoModelPermission` (line 159). Verified.
- **Final public names pinned** in the entry bodies (single source of truth, spec line 535): `<Model>Input`/`<Model>PartialInput` + shape-derived names, `<Name>Payload` + uniform `node`/`result` slot (never model-derived), `FieldError` (`field: str`/`messages: list[str]`), `<field>_id` (GlobalID-vs-raw-pk, type-checked at decode), `list[<id>]`, `"__all__"` sentinel, `Meta.permission_classes` default `[DjangoModelPermission]`, `check_permission(info, operation, data, instance=None)`. Verified by reading the `DjangoMutation` / `Input type generation` / `FieldError envelope` / `DjangoModelPermission` / `DjangoMutationField` bodies.
- **G2-handoff note on `only()` projection** — line 924: "As of `0.0.11` the G2 mutation gate is exercised **live** by the products write surface — the `spec-035` G2 live-test handoff is discharged." Inserted into the existing 0.0.10 G2 paragraph. Verified.
- **README / docs/README "Shipped today"** — README DRF pitch flipped roadmap→shipped (model-driven `DjangoMutation` `0.0.11`, serializer flavor kept `0.0.13`); docs/README moved the mutation half to Shipped-today (inline "new in `0.0.11`") and left only the `Upload` sibling in the `0.0.11` Coming-next line. Verified via `git diff`.
- **GOAL shipped-base diff** — DRF-migration prose marks the `DjangoMutation` base shipped (`0.0.11`) for model-driven ops, serializer flavor `0.0.13`; code block split into shipped `CreateCategory` (model+operation) and `0.0.13` `CreateCategoryFromSerializer`. Verified.
- **TODAY products write section** — products-centric "## Mutations on products today" section with Item/Category wire examples, the `errors: list[FieldError]` envelope + `"__all__"`, permission-scoped lookups, separate `DjangoModelPermission` write-auth, optimizer-backed re-fetch under G2. Kept products-scoped. Verified.
- **CHANGELOG `[Unreleased] ### Added`** — five `### Added` bullets under the existing `## [Unreleased]` heading, **no `## [0.0.11]` version heading** (`grep "## \[0.0.11\]"` → none). Bullets match the spec body's [Unreleased] section. Six new `[ref-id]` defs added to the `<!-- docs/ -->` group; all resolve. Verified.
- **DONE-036 card move** — `KANBAN.md` line 1329 shows `### [DONE-036-0.0.11 - Mutations + auto-generated Input types]` under `## Done` (1324), **exactly once** in the Done section. Spec map (line 100 + card body line 1337) points at the **live** `docs/spec-036-mutations-0_0_11.md` path (NOT `docs/SPECS/`). All six `definition_of_done` checkboxes `- [x]` (lines 1399-1404). The card's Glossary-terms table shows the three promotions as `shipped (\`0.0.11\`)`. Verified.

No remaining `- [ ]` Slice-5 boxes; nothing deferred at the checklist level.

### Doc-updates + DoD items 7/8 confirmation

- **DoD item 7** (GLOSSARY promotions + net-new entries + docs moves + KANBAN move) — all landed (see audit above). Confirmed.
- **DoD item 8** (no version bump) — `pyproject.toml` and `uv.lock` byte-unchanged (absent from `git diff --stat`); `django_strawberry_framework/__init__.py` `__version__ = "0.0.10"` unchanged (the file's diff is the cumulative Slices-1-3 `__all__` four-symbol export block + the resolved TODO comment — NOT a version edit, NOT a Slice-5 edit); `tests/base/test_init.py` carries only the cumulative `__all__` exports-pin + comment refresh, `test_version` untouched. No `## [0.0.11]` CHANGELOG heading promoted. Confirmed.
- **Terms-CSV exclusion (DoD item 1)** — `docs/spec-036-mutations-0_0_11-terms.csv` byte-unchanged (empty `git diff --stat`); `DjangoMutationField` / `DjangoModelPermission` are NOT anchors in it (the only `DjangoMutationField` string match is inside the `DjangoConnectionField` row's description; `DjangoModelPermission` is absent entirely). `check_spec_glossary.py` → `OK: 38 terms`. Confirmed.
- **Generator scripts untouched** — `build_kanban_md.py` / `build_kanban_html.py` / `build_glossary_md.py` have empty `git diff` (run-not-edit honored). Confirmed.

### Existing tests + regenerate stability + import_spec_terms

- **`import_spec_terms --check`** → `OK: 36 done cards have glossary links.` (card 36 included). PASS.
- **Regenerate stability** — captured the committed rendered hashes, ran all three generators **twice consecutively**; `docs/GLOSSARY.md` / `KANBAN.md` / `KANBAN.html` are byte-identical across both runs AND byte-identical to the committed working-tree render (the rendered files are already at the DB fixed point; the regenerate changed nothing). `git diff --stat` after two regenerates shows only the Slice-5 intended deltas — no new churn. PASS.
- **`manage.py check`** → "System check identified no issues (0 silenced)." PASS.
- **Focused DB-backed scope** (`examples/fakeshop/apps/kanban/tests/ apps/glossary/tests/ test_query/test_kanban_api.py test_query/test_glossary_api.py --no-cov`) → **78 passed, 2 failed**. The 2 failures are NOT a Slice-5 regression — see below. The render-driven surface Slice 5 actually edits is fully green:
  - **Live render-driven API tests** (`test_kanban_api.py` + `test_glossary_api.py`) → **36 passed** (the GraphQL render path the DB edits drive).
  - **Glossary app tests** (incl. `test_import_spec_terms`) → **10 passed**.

#### The two focused-test failures are pre-existing HEAD inconsistency, out of Slice-5 (and out of this build's) scope

- `examples/fakeshop/apps/kanban/tests/test_commands.py::test_import_card_predicted_files_command_marks_directories`
- `examples/fakeshop/apps/kanban/tests/test_services.py::test_create_card_from_spec_creates_planned_rows_for_future_paths`

Both fail with `assert True is False` on `<TrackedPath: django_strawberry_framework/mutations/>.is_current`. Root cause: both tests hardcode `PLANNED_PACKAGE_DIR = "django_strawberry_framework/mutations/"` and `PLANNED_TEST_FILE = "tests/mutations/test_inputs.py"` and assert they resolve to `is_current=False` (predicted/planned-path rows). But `examples/fakeshop/apps/kanban/constants.py` — the generated tracked-path allowlist — now lists `django_strawberry_framework/mutations/` (line ~175), `tests/mutations/` (~193), and `tests/mutations/test_inputs.py` (~114). `services.py::sync_tracked_paths_from_constants` flips any path in those constants to `is_current=True`, so the `is_current is False` assertions now fail.

Why this is not a Slice-5 / build finding:
- `constants.py`, `services.py`, and **both failing test files are byte-identical to HEAD** (empty `git diff --stat` vs HEAD); they are not in this build's authored diff at all. The mutations entries were added to `constants.py` by the maintainer's own most-recent commit (`a1713981` "036 - Add TODO comments"), which did not update the two tests that hardcode those paths as planned. `django_strawberry_framework/mutations/` and `tests/mutations/` are already **tracked at HEAD** (`git ls-tree HEAD`).
- These tests run against a fresh migration-built test DB, not the dirty `db.sqlite3` Slice 5 edited; they fail identically at the committed HEAD, independent of every uncommitted change in this build.
- Slice 5 touched neither `constants.py` nor these tests; the failures are driven by the generated allowlist vs. hardcoded-test-constant drift, NOT by any kanban/glossary DB edit or regenerate this slice made (the surfaces Slice 5 does drive — `test_kanban_api.py` / `test_glossary_api.py` / glossary app tests — are all green).

Per `AGENTS.md` rule 33 (pre-existing breakage from another's committed work is not auto-owned/reverted by the current task), this does not block Slice-5 `final-accepted`. It IS a genuine repo-level breakage that will trip the `bld-final.md` full `pytest` sweep, so it is surfaced to Worker 0 (for the final gate) and the maintainer, and filed as a background task + deferred-work-catalog item. The correct fix is to repoint the two tests' planned-path constants at a path that is genuinely still absent from `constants.py`.

### DRY check

Minimal, as expected for a doc/DB slice. The final public names are pinned **once** in `docs/GLOSSARY.md` (single source of truth, spec line 535); README / docs/README / GOAL / TODAY reference the shape at a summary level and link to the GLOSSARY anchor rather than re-pinning every name — no prose-vs-glossary drift. The CHANGELOG `### Added` bullets reuse the existing reference-style `[ref-id]` convention and `[Unreleased]` density. No duplicated literals/keys/structures introduced. Clean.

### Spec reconciliation — Worker 2's two flags

1. **Card-DoD `docs/spec-mutations.md` filename conflict** — decision: **leave consistent-and-recorded; carry to the final-gate deferred-work catalog.** The card-36 DoD CardItem 0 reads "Add `docs/spec-mutations.md`" while the spec lives at `docs/spec-036-mutations-0_0_11.md` (Decision 1). The spec already *records-not-reconciles* this exact filename conflict at Risks line 555, per the `docs/SPECS/NEXT.md` "prefer the structured spec, surface the conflict" boundary rule. Worker 2 correctly ticked the DoD item complete (the structured spec satisfies the intent) without rewriting the stale card-body string, and the authoritative references — the KANBAN WIP/DONE spec map (line 100) and the `SpecDoc` url — both point at the live `docs/spec-036-mutations-0_0_11.md`, so no surface diverges. Leaving it consistent-and-recorded is correct (a per-card rewrite of a string that diverges across multiple un-editable surfaces is exactly what worker-0.md warns against). **No spec edit needed** — Risks line 555 already documents it. It belongs in `bld-final.md`'s deferred-work catalog.

2. **`DjangoListField` / `GraphQLTestCase` GLOSSARY drift-body reconciliations** — decision: **net-zero, correct, not a finding, no spec edit.** Worker 2 synced the DB bodies for these two pre-existing committed-ahead-of-DB entries so the regenerate preserves the already-committed shipped doc content (worker-0.md step 1's explicit instruction; the alternative — letting the regenerate revert committed shipped content — is a regression). Independently verified net-zero vs the committed `docs/GLOSSARY.md`: `GraphQLTestCase` has zero diff lines; the only `DjangoListField` match in the GLOSSARY diff is a cross-reference link *inside the new `DjangoMutationField` entry body*, not an edit to the `DjangoListField` entry. The regenerate-stability proof (two byte-identical regenerates) confirms the DB and the rendered file are at one fixed point. Maintainer drift unrelated to mutations, correctly reconciled.

### Spec status-line re-verification (this spawn)

Spec line 5 read "**IN PROGRESS** (card `TODO-ALPHA-036-0.0.11`; Slices 1-4 shipped, Slice 5 next)" at spawn. With Slice 5 now `final-accepted`, all five slices have shipped and the build is complete. As spec custodian (the prompt explicitly defers this call to me), I rolled the status clause to "**COMPLETE** (card `DONE-036-0.0.11`; all five slices shipped — build complete)" — also flipping the card id `TODO-ALPHA-036-0.0.11` → `DONE-036-0.0.11` to match the now-shipped state. Mechanical accuracy fix; no contract change. Recorded below.

### Summary

Slice 5 ships the doc + card-completion wrap for the `0.0.11` mutation foundation, end-to-end and DB-correct: three GLOSSARY entries promoted to `shipped (0.0.11)` (`DjangoMutation` / `Input type generation` / `FieldError` envelope), two net-new entries seeded (`DjangoMutationField` + `DjangoModelPermission` write-auth) in Public exports + Index + the Mutations/Permissions browse rows with the final public names pinned, the G2-handoff-discharged note appended to the `only()` projection entry, README/docs-README mutations moved to "Shipped today", the GOAL DRF-migration diff referencing the shipped base, a products-centric TODAY write-surface section, the AUTHORIZED `[Unreleased] ### Added` CHANGELOG bullets with no version-heading promotion, and card 36 moved to `DONE-036-0.0.11` (SpecDoc at the live `docs/` path, all six DoD ticked, moved exactly once). No version-file edits (Decision 13 / DoD 8) and the two net-new symbols stayed out of the terms CSV (DoD 1). Regenerate is byte-stable, `import_spec_terms --check` and `manage.py check` pass, and the render-driven test surface Slice 5 drives is green. The only two focused-test failures are a pre-existing HEAD inconsistency (kanban example `constants.py` allowlist vs. two tests hardcoding the mutations paths as planned), byte-identical-to-HEAD and outside this slice's and this build's authored scope — surfaced to Worker 0 / the maintainer and the deferred-work catalog, not a Slice-5 regression.

### Spec changes made (Worker 1 only)

- **`docs/spec-036-mutations-0_0_11.md` line 5 (status header)** — Slice-5 planning pass: rolled "Slices 1-3 shipped, Slice 4 next" → "Slices 1-4 shipped, Slice 5 next". Reason: per-spawn spec status-line re-verification (worker-1.md) — Slice 4 was `final-accepted`, so the prior header understated build progress. Mechanical accuracy fix; no contract change.
- **`docs/spec-036-mutations-0_0_11.md` line 5 (status header)** — Slice-5 final-verification pass: rolled "**IN PROGRESS** (card `TODO-ALPHA-036-0.0.11`; Slices 1-4 shipped, Slice 5 next)" → "**COMPLETE** (card `DONE-036-0.0.11`; all five slices shipped — build complete)". Reason: per-spawn spec status-line re-verification (worker-1.md) — Slice 5 is now `final-accepted`, every slice has shipped, and the card has moved to Done; the prior IN PROGRESS / TODO-ALPHA header understated the completed state. Spec-custody call explicitly deferred to Worker 1 by the final-verification task. Mechanical accuracy fix; no contract change.
