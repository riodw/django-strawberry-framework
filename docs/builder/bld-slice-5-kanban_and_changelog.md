# Build: Slice 5 — KANBAN + CHANGELOG

Spec reference: `docs/spec-020-scalar_map_helper-0_0_7.md` (lines 57-60 for the Slice 5 sub-checks; lines 574-601 for the `## Doc updates` KANBAN + CHANGELOG canonical wording; Decision 8 at lines 607+ for version posture; DoD items 15, 16, 17 at spec lines 653-655)
Status: final-accepted

## Plan (Worker 1)

### DRY analysis

- **Existing patterns reused.** The canonical text drop-ins for this slice are pre-pinned verbatim in the spec's `## Doc updates` section: the past-tense Done body at `docs/spec-020-scalar_map_helper-0_0_7.md` line 577 (the long paragraph beginning "Pinned the package-defined scalar registration path…"), the `### Added` paragraph at spec line 584, the `### Changed` Breaking-change paragraph with before/after diff at spec lines 587-595, and the `### Removed` paragraph at spec line 598. Worker 2 copies these verbatim — no paraphrasing. The KANBAN past-tense Done-body wording also already exists as a precedent in the `DONE-016-0.0.7` / `DONE-017-0.0.7` / `DONE-019-0.0.7` cards' Done bodies (`KANBAN.md::DONE-016-0.0.7`, `KANBAN.md::DONE-017-0.0.7`, `KANBAN.md::DONE-019-0.0.7`) — the past-tense shape, the per-slice file list, the spec citation, and the version-bump-deferred sentence are the same pattern Worker 2 mirrors.
- **New helpers justified.** None. Slice 5 is Markdown-only.
- **Duplication risk avoided.** The past-tense Done body (in `KANBAN.md`) and the `[Unreleased] ### Changed` Breaking-change paragraph (in `CHANGELOG.md`) both describe the same migration. The repetition is intentional — `KANBAN.md` tells the historical card story (what shipped, which files, which spec); `CHANGELOG.md` tells the consumer-facing release story (what breaks for consumers, exact migration diff). Not a DRY violation per BUILD.md severity definitions; both surfaces are required by spec DoD items 15 and 16.

**Static inspection helper:** SKIPPED — Slice 5 touches only Markdown (`KANBAN.md`, `CHANGELOG.md`). `scripts/review_inspect.py` operates on Python source; not applicable here. Recorded per BUILD.md "Worker 1 and Worker 3 may skip the helper for files where the artifact will be a 'no review-worthy logic' disposition … The skip must be recorded explicitly with a short reason."

### Implementation steps

Line numbers are pin-at-write-time navigational hints. Verify against the current source before editing — Worker 2 should re-grep for the anchor strings if the file shifts.

1. **`KANBAN.md`** — five edits in this file.

   1a. Locate the `WIP-ALPHA-020-0.0.7` card. It is at `KANBAN.md` line 76 (`### WIP-ALPHA-020-0.0.7 — Warning-free scalar registration via \`StrawberryConfig.scalar_map\``) and runs through approximately line 168 (the closing "Open design questions for the spec" block ends before the `## To Do - Alpha (0.1.0)` section divider at line 169). Confirm the card's end-boundary by re-reading the file before cutting.

   1b. Determine the next available `DONE-NNN-0.0.7` id. The current Done column has `DONE-016-0.0.7`, `DONE-017-0.0.7`, `DONE-018-0.0.7`, `DONE-019-0.0.7`, and `DONE-046-0.0.7` (per `KANBAN.md` line 1764's NNN note: `046` was intentionally assigned ahead of the prior max `TODO-STABLE-045-1.0.0` to avoid a multi-file rename; per the KANBAN card-ID convention, NNN is not stable and grouping is by version, not NNN). The **next available** id is therefore `DONE-047-0.0.7` (one above the current max of `046`). Worker 2 should re-verify by `grep -nE "^### DONE-[0-9]+-0\.0\.7" KANBAN.md` at edit time in case another card shifted the column in the interim.

   1c. Move the card to the Done section. Locate the existing Done-block insertion point in `KANBAN.md` — after `DONE-046-0.0.7` (ends near line 1764 with the `NNN note:` line) and before the `## Release readiness checklist` heading at line 1766. Insert the new `### DONE-047-0.0.7 — Warning-free scalar registration via \`StrawberryConfig.scalar_map\`` card immediately before `## Release readiness checklist` (continuing the Done column's NNN-ascending chronological order within the `0.0.7` cohort). Delete the entire `### WIP-ALPHA-020-0.0.7 — …` card block at the original position (lines ~76-168) in the same edit.

   1d. The body of the new `### DONE-047-0.0.7 — …` card is the verbatim past-tense paragraph from spec line 577 (the paragraph beginning "Pinned the package-defined scalar registration path:" and ending with "The version bump from `0.0.7 → 0.0.8` is NOT in this card per Decision 8."). Drop in as-is; preserve every Markdown link, code span, and reference. Match the surrounding Done-card visual shape used by `DONE-019-0.0.7` (a "Parity:" line above the body, a "Spec:" / "Build plan:" line at the bottom) — the spec line 577 paragraph already includes the "Spec: `docs/spec-020-scalar_map_helper-0_0_7.md`" citation; add a `Build plan: \`docs/builder/build-020-scalar_map_helper-0_0_7.md\`.` sentence after it to match the `DONE-019-0.0.7` shape (`KANBAN.md` line 1750). Parity line wording is Worker 2 discretion — the precedent from `DONE-019-0.0.7` line 1746 (`Parity: ⚛️&🍓 parity-adjacent …`) is the closest match for this card's posture (no upstream `StrawberryConfig`-bundling helper precedent exists per spec line 108-117 "Borrowing posture"); Worker 2 may copy that sentence or omit the Parity line entirely (the spec's verbatim paragraph stands on its own). Either shape is acceptable per the "match the surrounding Done-card visual shape" instruction.

   1e. Rewrite the card body's `Definition of done` bullet 1. In the original `WIP-ALPHA-020-0.0.7` card body at `KANBAN.md` line 125, the bullet reads "New spec: `docs/spec-scalar_map_helper.md` settling the helper API shape, …". The Slice 5 sub-bullet at spec line 578 directs Worker 2 to rewrite this to point at the structured spec filename per Decision 1. The replacement target path is **`docs/spec-020-scalar_map_helper-0_0_7.md`** (the current working location), NOT `docs/SPECS/spec-020-scalar_map_helper-0_0_7.md`. Rationale: spec Decision 1's lifecycle clause (spec line 220) says "references point at whichever path the file actually has at the time the reference is written. While this spec is at `docs/spec-020-scalar_map_helper-0_0_7.md`, every reference uses that path." There is no spec-archival slice in this card (per BUILD.md "Spec stays at its working location" — closing a build does NOT imply moving the spec). The spec line 578 sub-bullet's "after the Step-8 archive pass" phrasing is a forward-looking pointer to a hypothetical future archival pass; the Slice 5 edit happens BEFORE any such pass, so the live working path is the correct target. Worker 2 rewrites the bullet 1 path-segment from `docs/spec-scalar_map_helper.md` to `docs/spec-020-scalar_map_helper-0_0_7.md` and preserves the rest of the bullet wording in place. (Note: the WIP card body is being deleted in step 1c above as part of the Done move; the `Definition of done` bullet 1 rewrite is captured INSIDE that new Done-body construction. Per the spec's verbatim past-tense Done body at spec line 577, the Done body does NOT include a `Definition of done` section — instead it summarizes the shipped scope in prose. Therefore: the original `Definition of done` block in the WIP card body is **dropped wholesale** when the card moves to Done. The "rewrite bullet 1" sub-check is satisfied by deletion-as-move: the bullet ceases to exist in its stale form because the entire pre-Done card body is replaced by the verbatim Done-body paragraph. Worker 2 confirms in the build report that the post-edit `KANBAN.md` contains zero occurrences of the literal `docs/spec-scalar_map_helper.md` string.)

   1f. Update the `### In progress` summary paragraph at `KANBAN.md` line 50 (the paragraph beginning "`0.0.7` is the active patch."). The current text names `WIP-ALPHA-020-0.0.7` as "still queued" and lists `DONE-016`/`DONE-017`/`DONE-018`/`DONE-019`/`DONE-046` as shipped. Rewrite to reflect the post-Slice-5 state: remove `WIP-ALPHA-020-0.0.7` from the remaining-cards list; ADD the new `DONE-047-0.0.7` to the shipped-cards list. The version-bump sentence at the paragraph's end ("The last `0.0.7` card to ship owns the version bump from `0.0.6` per Decision 10 of `docs/SPECS/spec-016-list_field-0_0_7.md`") is preserved as-is — but Worker 2 should re-read the resulting paragraph to confirm the sentence still reads cleanly given that this card explicitly does NOT bump the version per spec Decision 8 / DoD item 17 (the sentence remains forward-looking against a hypothetical future card; no edit needed, just a sanity check). Worker 2 has minor wording discretion to keep the paragraph grammatical (e.g., "Five WIP cards were opened together" was true historically and stays; the count of shipped cards updates).

2. **`CHANGELOG.md`** — four edits in this file. Per the CHANGELOG-edit-permission rule at `AGENTS.md` line 21 ("Do not update `CHANGELOG.md` unless explicitly instructed"), the spec's Slice 5 sub-bullet at spec line 601 grants the explicit instruction: "this Slice 5 bullet is the explicit instruction" (cited verbatim from spec line 601). Worker 2 has authorization to edit `CHANGELOG.md` in this slice ONLY for the four edits enumerated below.

   2a. Add `### Added` subsection under `[Unreleased]`. Inspect: `CHANGELOG.md` line 19 (`## [Unreleased]`) is followed at line 20 by `### Changed` and at line 25 by `### Fixed` — `### Added` is currently absent under `[Unreleased]`. Per "Keep a Changelog" convention (alphabetical: `Added → Changed → Deprecated → Removed → Fixed → Security`), place the new `### Added` immediately after `## [Unreleased]` on line 19 and BEFORE `### Changed` on line 20. Body: verbatim drop-in of the spec line 584 paragraph (beginning "`strawberry_config` — factory function (`django_strawberry_framework/scalars.py`) returning a `strawberry.schema.config.StrawberryConfig` pre-populated …"). Render as a single bullet under the new `### Added` heading (match the shape used by `[0.0.7] ### Added` at `CHANGELOG.md` lines 29-35: each Added entry is one `-`-prefixed bullet).

   2b. Append a new bullet to the existing `[Unreleased] ### Changed` subsection (currently at `CHANGELOG.md` lines 20-23, three bullets describing `manage.py export_schema` changes). The new bullet is the verbatim drop-in of spec lines 587-595 (the "**Breaking change**: `BigInt` registration moved …" paragraph followed by a triple-backtick `diff` fenced block followed by the "The wire format, parser, serializer, and direct-annotation usage of `BigInt` are unchanged." sentence). **Fence-rendering note:** the spec wraps this paragraph and its inner ```` ```diff ```` block inside a top-level Markdown block-quote (the spec lines are prefixed with `> ` in the spec source); when copying into `CHANGELOG.md`, Worker 2 **drops the leading `> ` block-quote prefixes** so the paragraph reads as a normal bullet body and the inner diff fence renders correctly as a regular Markdown fenced code block. The inner fence is triple-backticks (`” ``` ”`); the surrounding `CHANGELOG.md` content uses no outer fences, so no four-backtick escalation is needed. Append after the existing third bullet at line 23; before the `### Fixed` heading at line 25.

   2c. Add `### Removed` subsection under `[Unreleased]`. Currently absent (line 25 jumps from `### Fixed` directly to `## [0.0.7]` on line 28). Per "Keep a Changelog" convention, `### Removed` goes AFTER `### Fixed`. Insert the new `### Removed` heading after the existing `[Unreleased] ### Fixed` bullet (at `CHANGELOG.md` line 26's bullet ends; the next blank line precedes `## [0.0.7]` on line 28). Body: verbatim drop-in of the spec line 598 paragraph ("Internal `warnings.catch_warnings()` suppression block in `django_strawberry_framework/scalars.py` that silenced Strawberry's `Passing a class to strawberry.scalar() is deprecated` `DeprecationWarning`. No longer needed — the migrated registration path uses Strawberry's no-warning `strawberry.scalar(name=..., serialize=..., parse_value=...)` overload."). Render as a single bullet under the new `### Removed` heading.

   2d. Remove the `[0.0.6] ### Notes` line at the "Migration to a `scalar_map`-based design" anchor. Currently `CHANGELOG.md` line 77 is the `### Notes` heading and line 78 is the SINGLE bullet beginning "The internal `BigInt` scalar definition uses `strawberry.scalar(NewType, ...)`, which Strawberry deprecates …" (verified by reading the file). The bullet contains the literal sentence "Migration to a `scalar_map`-based design is tracked as a follow-up and will be a real public-API change for consumers using `BigInt` directly." which spec line 599 directs Worker 2 to remove. Because the `[0.0.6] ### Notes` subsection contains ONLY this single bullet (verified — line 79 is blank and line 80 is the next section's `## [0.0.5]` heading), removing the bullet leaves the `### Notes` heading orphaned with no content. **Worker 2 discretion:** remove the entire `### Notes` subsection (both the line-77 heading AND the line-78 bullet) rather than leaving an empty `### Notes` heading. Rationale per the task brief: "If the `### Notes` subsection becomes empty after removing the line, the WHOLE subsection should be removed (Worker 2 discretion / common sense — empty subsections clutter the changelog)." This is the right call for the `[0.0.6]` snapshot because the `Notes` line was a forward-looking pointer to this very card (spec line 599 frames it as "the placeholder note has served its purpose"); removing both the heading and the bullet keeps `[0.0.6]` as a clean shipped-snapshot.

3. **Validation.** Run `uv run ruff format .` and then `uv run ruff check --fix .`. Both should be no-ops for this slice (ruff does not touch Markdown files), but the run is part of the validation contract per `AGENTS.md` line 15 ("Run `uv run ruff format .` and `uv run ruff check --fix .` after every edit") and per BUILD.md's per-slice validation block. Confirm zero working-tree drift in non-`KANBAN.md` / non-`CHANGELOG.md` files post-ruff via `git status --short`.

### Test additions / updates

- None. Slice 5 ships zero new tests and modifies zero existing tests. Markdown-only edit; nothing in the test surface is reachable from `KANBAN.md` or `CHANGELOG.md` content. The Slice 1/2/3/4 test surface remains the canonical pinning for the `strawberry_config` factory and the `BigInt` round trip.

### Implementation discretion items

- **The next available `DONE-NNN-0.0.7` id is `DONE-047-0.0.7`** at the time of this plan. Worker 2 re-verifies via `grep -nE "^### DONE-[0-9]+-0\.0\.7" KANBAN.md` immediately before the edit; if another card shifted the column in the interim, Worker 2 uses the (new max) + 1.
- **Whether to keep the `[0.0.6] ### Notes` heading after stripping the migration sentence.** Worker 2 should remove the entire `### Notes` subsection (heading + bullet) because the subsection becomes empty post-removal and an empty heading is visual clutter. (Captured as discretion per the task brief; pre-decided here as "remove the whole subsection" so Worker 2 doesn't have to re-derive the call.)
- **Exact placement of `### Added` and `### Removed` within `[Unreleased]`.** Per "Keep a Changelog" alphabetical convention: `### Added` goes BEFORE `### Changed`; `### Removed` goes AFTER `### Fixed`. Pinned in steps 2a and 2c above; no further discretion needed.
- **`DONE-047-0.0.7` card's Parity line.** Worker 2 may include a "Parity: …" line above the verbatim past-tense body (matching the `DONE-016-0.0.7` / `DONE-017-0.0.7` / `DONE-019-0.0.7` shape) OR omit it (spec line 577's paragraph is self-contained). Recommended: omit, since the spec's verbatim wording does not provide parity framing and the spec's "Borrowing posture" section (lines 108-117) explicitly notes there is no upstream precedent for this helper.
- **`DONE-047-0.0.7` card's `Build plan:` citation line.** Worker 2 appends a `Build plan: \`docs/builder/build-020-scalar_map_helper-0_0_7.md\`.` sentence after the verbatim past-tense paragraph, matching `DONE-019-0.0.7`'s shape at `KANBAN.md` line 1750. (Captured as discretion because the spec's verbatim paragraph at line 577 already includes a `Spec:` citation but not a `Build plan:` citation; adding the build-plan citation is the consistent shape with sibling Done cards. Worker 2 may also omit if they prefer to stay strictly verbatim — either shape passes Worker 3 review.)
- **Out of scope per spec Decision 8 / DoD item 17 — version-bump exclusions.** Slice 5 does NOT touch:
  - `pyproject.toml` `[project].version`
  - `django_strawberry_framework/__init__.py::__version__`
  - `tests/base/test_init.py`'s pinned version assertion (the `__version__ == "0.0.7"` check)

  A future maintainer-led cut promotes `[Unreleased]` to `[0.0.8]` and bumps these three files atomically in one separate commit. Slice 5 leaves `__version__ = "0.0.7"` in place and accumulates the `[Unreleased]` bullets against the unbumped version. This exclusion is explicitly recorded here so Worker 2 cannot accidentally promote `[Unreleased]` to `[0.0.8]` or edit `__version__` while in `CHANGELOG.md`.

### Spec slice checklist (verbatim)

The four sub-bullets below are copied verbatim from `docs/spec-020-scalar_map_helper-0_0_7.md` lines 57-60 (the Slice 5 block under `## Slice checklist`). Preserve exact text, nested punctuation, and inline citations.

- [x] Slice 5: KANBAN + CHANGELOG
  - [x] [`KANBAN.md`](../KANBAN.md): move `WIP-ALPHA-020-0.0.7` to the Done column with the next available `DONE-NNN-0.0.7` id (the column-move pass renumbers as usual; the next available id is determined at merge time, not pinned in this spec). The past-tense Done body summarizes the shipped scope; full wording pinned in [Doc updates](#doc-updates). Update the card body's `Definition of done` bullet 1 (`docs/spec-scalar_map_helper.md` → `docs/SPECS/spec-020-scalar_map_helper-0_0_7.md` after the Step-8 archive pass per [Decision 1](#decision-1--spec-filename-and-canonical-naming)). Update the `### In progress` summary paragraph (anchored at [`KANBAN.md #"### In progress"`](../KANBAN.md)) to remove `WIP-ALPHA-020-0.0.7` from the remaining-cards list once this card moves to Done.
  - [x] [`CHANGELOG.md`](../CHANGELOG.md): **append** to the existing `[Unreleased]` `### Changed` subsection (existing changes there are non-breaking; this card's breaking-but-alpha-OK migration adds the fourth bullet); AND `### Added` (for the `strawberry_config` factory — the `[Unreleased]` block currently has `### Changed` and `### Fixed` only; add `### Added` if absent, placing it alphabetically before `### Changed` per "Keep a Changelog" convention); AND `### Removed` (for the suppression block removal — also add the subsection if absent, placing it after `### Fixed`). Per the CHANGELOG-edit-permission rule at [`AGENTS.md #"Do not update CHANGELOG.md unless explicitly instructed"`](../AGENTS.md), this Slice 5 bullet is the explicit `CHANGELOG.md` edit permission. Also remove the literal `[0.0.6]` `### Notes` line "The internal `BigInt` scalar definition uses `strawberry.scalar(NewType, ...)`, which Strawberry deprecates in favor of `StrawberryConfig.scalar_map`. The deprecation warning is suppressed at the definition site so the package import remains clean. Migration to a `scalar_map`-based design is tracked as a follow-up and will be a real public-API change for consumers using `BigInt` directly." at [`CHANGELOG.md #"Migration to a"`](../CHANGELOG.md) — that `Notes` entry advertised the architectural debt this card pays down; with the migration shipped, the placeholder note has served its purpose and removing it keeps the `[0.0.6]` section accurate as a snapshot of what shipped (the `Notes` line is a forward-looking pointer, not a historical fact).
  - [x] Version bump: NOT in this card per [Decision 8](#decision-8--version-posture-cut-already-shipped-this-card-lands-under-unreleased). The `[Unreleased]` `### Added` / `### Changed` / `### Removed` bullets accumulate against an unbumped `__version__ = "0.0.7"` until a future maintainer-led cut promotes them to `[0.0.8]` and bumps `pyproject.toml`, `django_strawberry_framework/__init__.py`'s `__version__`, and `tests/base/test_init.py`'s pinned version assertion in one atomic commit. This card does NOT do the bump.

---

## Build report (Worker 2)

### Files touched

- `KANBAN.md` — three edits: (a) rewrote the `### In progress` summary paragraph (line ~50) to remove `WIP-ALPHA-020-0.0.7` from the remaining-cards list and add `DONE-047-0.0.7` to the shipped-cards list (shipped-count narrative flipped from "four have shipped" to "all five have shipped"); (b) deleted the entire `### WIP-ALPHA-020-0.0.7 …` card block (previously lines 76-167) from `## In progress`, leaving a single-italics placeholder under the `## In progress` heading noting the `0.0.7` queue is empty; (c) inserted the new `### DONE-047-0.0.7 — Warning-free scalar registration via \`StrawberryConfig.scalar_map\`` card immediately before `## Release readiness checklist` (after `DONE-046-0.0.7`). The card body is the verbatim past-tense paragraph from spec line 577 followed by a `Build plan:` citation line (matching `DONE-019-0.0.7`'s shape per plan discretion item 5). No `Parity:` line — omitted per plan discretion (spec line 577 paragraph is self-contained and there is no upstream `StrawberryConfig`-bundling helper precedent). The pre-Done `Definition of done` block was dropped wholesale as the plan called for (Done body is a prose summary, no `Definition of done` section).
- `CHANGELOG.md` — four edits all in the `[Unreleased]` and `[0.0.6]` sections: (a) inserted `### Added` subsection before `### Changed` under `[Unreleased]`, body is the verbatim spec-line-586 paragraph (blockquote `> ` markers stripped); (b) appended a new `**Breaking change**: BigInt registration moved …` bullet to the existing `[Unreleased] ### Changed` subsection, with the inner triple-backtick `diff` fenced block, body is the verbatim spec-lines-589-597 content (blockquote `> ` markers stripped, two-space indent preserved on the fenced block to keep it inside the bullet); (c) inserted `### Removed` subsection AFTER `### Fixed` under `[Unreleased]`, body is the verbatim spec-line-600 paragraph (blockquote `> ` markers stripped); (d) removed the entire `[0.0.6] ### Notes` subsection (heading + the single bullet about the suppressed deprecation) per plan discretion item 2 ("remove the whole subsection" because the only bullet was being stripped).

### Tests added or updated

None. Slice 5 is Markdown-only per the plan's "Test additions / updates" section.

### Validation run

- `uv run ruff format .` — pass (no-op: `118 files left unchanged`; ruff does not touch Markdown).
- `uv run ruff check --fix .` — pass (`All checks passed!`).
- `git status --short` after both ruff invocations — slice-intended modifications: `KANBAN.md`, `CHANGELOG.md`. All other modified files (`GOAL.md`, `TODAY.md`, `django_strawberry_framework/__init__.py`, `django_strawberry_framework/scalars.py`, `docs/GLOSSARY.md`, `docs/README.md`, `docs/spec-020-scalar_map_helper-0_0_7-terms.csv`, `docs/spec-020-scalar_map_helper-0_0_7.md`, `examples/fakeshop/config/schema.py`, `tests/base/test_init.py`, `tests/test_scalars.py`, `tests/types/test_converters.py`) are Slice-1-through-Slice-4 carryover from earlier passes in the same build cycle (NOT Slice-5 churn) and are out-of-scope to revert per `AGENTS.md` line 32. Untracked `docs/builder/bld-slice-*.md` and `docs/builder/build-020-scalar_map_helper-0_0_7.md` files are this cycle's build artifacts.
- Sanity greps:
  - `grep -c "docs/spec-scalar_map_helper.md" KANBAN.md` → `0` (stale spec path eliminated; original ref was inside the deleted WIP card body).
  - `grep -nE "^### (WIP-ALPHA-020|DONE-047)-0\.0\.7" KANBAN.md` → exactly one match: the new `DONE-047-0.0.7` heading at line 1675; zero `WIP-ALPHA-020-0.0.7` headings.
  - `grep -nE "Migration to a" CHANGELOG.md` → zero matches (the `[0.0.6]` `Notes` sentence is gone).
- Version-bump exclusion verified: `pyproject.toml [project].version`, `django_strawberry_framework/__init__.py::__version__`, and `tests/base/test_init.py`'s version assertion all still read `0.0.7` — Slice 5 did not touch them (the `__init__.py` and `test_init.py` diffs from earlier slices add `strawberry_config` to the public surface, not the version string).

No focused tests run for Slice 5 — Markdown-only edit; no test surface reachable from `KANBAN.md` / `CHANGELOG.md` content. The Slice 2 test surface remains the canonical pinning for the `strawberry_config` factory and the `BigInt` round trip.

### Implementation notes

- **`### In progress` summary paragraph wording.** The plan called for "remove `WIP-ALPHA-020-0.0.7` from the remaining-cards list" and add `DONE-047-0.0.7` to the shipped-cards list. Reshaped the narrative from "four have shipped" / "The remaining card is still queued" to "all five have shipped" / "Full card detail lives under the `## Done` board column below". The version-bump sentence at the paragraph's end is preserved verbatim per the plan's sanity-check guidance — it remains forward-looking against a hypothetical future card and reads cleanly given that this card explicitly defers the bump (per Decision 8). Shape decision: kept the single-paragraph summary form rather than re-flowing to multiple paragraphs, matching the prior summary's voice.
- **`## In progress` board column when empty.** The plan deleted the WIP card block but didn't pin replacement text for the now-empty `## In progress` heading. Chose to add a single-line italicized placeholder (`_No cards in progress. The `0.0.7` queue is empty; the `0.1.0` cohort begins with `TODO-ALPHA-021-0.0.8`._`) rather than leaving the heading bare. Rationale: an empty board column section reads as suspicious (missing-content vibe); a one-line stub disambiguates "queue genuinely empty" from "content lost in a bad edit". Falls within Worker-2 discretion since the plan says "Worker 2 has minor wording discretion to keep the paragraph grammatical" and the same posture extends to the structural void left when the only WIP card moves to Done.
- **`DONE-047-0.0.7` Build-plan citation form.** Per plan discretion item 5, added `Build plan: [\`docs/builder/build-020-scalar_map_helper-0_0_7.md\`](docs/builder/build-020-scalar_map_helper-0_0_7.md).` as a separate paragraph after the verbatim past-tense body. Matched `DONE-019-0.0.7`'s shape (which uses a separate trailing paragraph with the markdown-linked path). Did NOT add a `Parity:` line per plan discretion item 4 — the verbatim paragraph is self-contained.
- **Verbatim drop-in fidelity.** All four CHANGELOG.md verbatim drop-ins (spec lines 586, 589-597, 600) match the spec character-for-character apart from stripping the spec's blockquote `> ` markers (a pre-pinned plan instruction). The inner triple-backtick `diff` fence inside the Breaking-change bullet survived intact under a two-space indent (the bullet's continuation rule) without needing four-backtick escalation, since `CHANGELOG.md` has no outer fences.
- **Empty `### Notes` subsection removal.** Per plan discretion item 2, removed both the `### Notes` heading and the single bullet beneath it at `[0.0.6]` — leaving an empty `### Notes` heading would be visual clutter.

### Notes for Worker 3

- Slice 5 is Markdown-only; `scripts/review_inspect.py` is not applicable (it operates on Python source). The Plan recorded this skip explicitly under "Static inspection helper: SKIPPED".
- Worker 3's `CHANGELOG sanity` section in the BUILD.md review template should check: (a) the new `[Unreleased]` `### Added` / `### Changed` (appended) / `### Removed` headings match what the spec authorized (yes — spec lines 581-598 and DoD item 16); (b) wording matches the canonical drop-ins from spec lines 586, 589-597, 600 (verbatim apart from `> ` strip); (c) version-bump exclusion holds (yes — `__version__` still `0.0.7`).
- Worker 3's `Documentation / release sanity` section should check: (a) the new `### DONE-047-0.0.7 — …` card in `KANBAN.md` Done column with the verbatim spec-line-577 body; (b) the `### In progress` summary paragraph removed `WIP-ALPHA-020-0.0.7` and added `DONE-047-0.0.7`; (c) zero remaining occurrences of `docs/spec-scalar_map_helper.md` in `KANBAN.md`; (d) the `[0.0.6] ### Notes` removal preserved the rest of `[0.0.6]` intact (the `### Added` / `### Changed` subsections above are untouched).
- The fenced `diff` block inside the `[Unreleased] ### Changed` Breaking-change bullet uses two-space indent (bullet continuation). Reads correctly under standard CommonMark / Keep-a-Changelog rendering; if Worker 3 wants to double-check, the GitHub web preview is the canonical render.

### Notes for Worker 1 (spec reconciliation)

No spec gaps surfaced. Slice 5 implemented exactly the four sub-checks from spec lines 57-60 using the verbatim Doc-updates drop-ins at spec lines 577, 586, 589-597, and 600. Version-bump exclusion held per Decision 8 / DoD item 17 — `pyproject.toml`, `__version__`, and `tests/base/test_init.py`'s version assertion all still read `0.0.7`. The plan's pre-resolved decisions (next-available id `DONE-047-0.0.7`; `[0.0.6] ### Notes` subsection removed entirely; `### Added` before `### Changed`; `### Removed` after `### Fixed`; spec path stays at the working `docs/spec-020-scalar_map_helper-0_0_7.md`) all landed as written.

---

## Review (Worker 3)

### High:

None.

### Medium:

None.

### Low:

None.

### DRY findings

- None. The deliberate duplication between the `KANBAN.md` Done body (historical card story) and the `CHANGELOG.md` `### Changed` Breaking-change bullet (consumer-facing release story) is justified by the plan's DRY analysis and is mandated by DoD items 15 and 16. Both surfaces carry the verbatim spec drop-ins (spec lines 577 vs 587-595 respectively); they describe the same migration from complementary angles, not redundantly.

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` shows the Slice 1 carry-forward (`from .scalars import BigInt, strawberry_config` import widening plus `"strawberry_config"` appended to `__all__`). This diff is NOT part of Slice 5 — it belongs to Slice 1 and was already reviewed under `bld-slice-1-helper_module_and_bigint_redefinition.md`. Slice 5 itself touched only `KANBAN.md` and `CHANGELOG.md`; the public-surface contribution from Slice 5 alone is zero. Per BUILD.md's cumulative-diff-trap rule, this re-export change is out-of-scope here.

### CHANGELOG sanity

Heavy-lift check per BUILD.md "CHANGELOG sanity":

- **Version line.** `pyproject.toml [project].version = "0.0.7"` (line 4) and `django_strawberry_framework/__init__.py::__version__ = "0.0.7"` (line 26) still match; the `[Unreleased]` heading is untouched and there is no `[0.0.8]` heading. Slice 5 correctly did not bump the version per Decision 8 / DoD item 17. CONFIRMED.
- **Headings under `[Unreleased]`.** Order in the diff (`CHANGELOG.md` lines 20, 23, 37, 40):
  1. `### Added` (new — spec lines 581-584 authorize)
  2. `### Changed` (already existed; bullet appended — spec lines 585-595 authorize)
  3. `### Fixed` (already existed; unchanged)
  4. `### Removed` (new — spec lines 596-598 authorize)
  This matches the spec's explicit placement rule (`Added` BEFORE `Changed`; `Removed` AFTER `Fixed`). CONFIRMED.
- **Verbatim drop-ins (character-identical apart from `> ` blockquote stripping).** Verified programmatically:
  - Spec line 584 → `CHANGELOG.md` line 21 (the `### Added` bullet): byte-identical after stripping `    > "` / `"`.
  - Spec lines 587-595 → `CHANGELOG.md` lines 27-35 (the `### Changed` Breaking-change bullet with inner fenced `diff` block): byte-identical after stripping `    > ` / `    >` / surrounding `"` and applying the two-space continuation indent to keep the fenced block inside the list bullet. The triple-backtick `diff` fence does not collide with any outer fence (`CHANGELOG.md` carries no outer fences) so no four-backtick escalation is needed; the GitHub-rendered preview renders the diff block correctly under the bullet.
  - Spec line 598 → `CHANGELOG.md` line 41 (the `### Removed` bullet): byte-identical after stripping `    > "` / `"`.
- **Wording proportionality.** The Breaking-change paragraph accurately describes Slice 1 (the `strawberry.scalar(name=..., serialize=..., parse_value=...)` overload + bare `NewType` redefinition + `_PACKAGE_SCALAR_MAP`), Slice 3 (the example schema `config=strawberry_config()` migration), and Slice 4 (docs migrations). It does not overstate or understate the migration scope.
- **`[0.0.6] ### Notes` removal.** `grep -n "Migration to a" CHANGELOG.md` returns zero matches; the `### Notes` heading at the old line 67 of the prior `[0.0.6]` block is removed entirely (heading + bullet) per the plan's discretion item 2. The `[0.0.6]` block at the current `CHANGELOG.md` line 70 now ends at the last `### Removed` bullet (line 90) and is followed cleanly by `## [0.0.5]` at line 92; the surrounding `[0.0.7]`-section `### Notes` at line 67 belongs to `[0.0.7]` and was untouched. CONFIRMED.

### Documentation / release sanity

Heavy-lift check per BUILD.md "Documentation / release sanity":

- **`WIP-ALPHA-020-0.0.7` removed; `DONE-047-0.0.7` exists exactly once.** `grep -c "^### WIP-ALPHA-020-0.0.7" KANBAN.md` returns 0; `grep -c "^### DONE-047-0.0.7" KANBAN.md` returns 1 (heading at line 1675). The plan's chosen id matches Worker 2's id. CONFIRMED.
- **`### In progress` summary paragraph rewritten.** At `KANBAN.md` line 50: `WIP-ALPHA-020-0.0.7` no longer appears in the remaining-cards list; the narrative flipped from "four have shipped" / "The remaining card ... is still queued" to "all five have shipped" with `DONE-047-0.0.7` named in both the shipped-list sentence and the "in the `## Done` column" enumeration. The version-bump sentence at the paragraph's end remains forward-looking and reads cleanly under the spec's Decision 8 / DoD item 17 deferral posture. CONFIRMED.
- **`DONE-047-0.0.7` Done body matches spec line 577 verbatim.** Verified programmatically: `KANBAN.md` line 1677 is byte-identical to spec line 577 after stripping the spec's `    > "` / `"` wrappers. Spec lines 577-579 do NOT contain a `Parity:` line, and Worker 2 correctly omitted one per plan discretion item 4 (the spec's verbatim paragraph stands on its own; the spec's "Borrowing posture" section at spec lines 108-122 records no upstream `StrawberryConfig`-bundling precedent to claim parity with). The `Build plan:` citation line at `KANBAN.md` line 1679 is the discretion-item-5 shape — acceptable per the plan.
- **`Definition of done` bullet 1 path rewrite.** `grep -c "docs/spec-scalar_map_helper.md" KANBAN.md` returns 0. The pre-Done WIP card's `Definition of done` block (which contained the stale `docs/spec-scalar_map_helper.md` reference) was dropped wholesale when the card moved to Done — Worker 2 correctly handled this via deletion-as-move, leaving the verbatim past-tense Done body in its place. The Done body's `Spec:` citation already uses the canonical `docs/spec-020-scalar_map_helper-0_0_7.md` path. CONFIRMED.
- **`## In progress` board column placeholder.** The plan deleted the WIP card body and Worker 2 added a single-line italicized stub (`_No cards in progress. The \`0.0.7\` queue is empty; the \`0.1.0\` cohort begins with \`TODO-ALPHA-021-0.0.8\`._`). This is Worker 2 discretion and stays within the spirit of the plan; `grep -nE "^### TODO-ALPHA-021" KANBAN.md` confirms `TODO-ALPHA-021-0.0.8 — Filtering subsystem` at line 82 is the real next card, so the placeholder text is accurate and not stale.
- **No "coming soon" / "planned" / old-version wording remains.** The `[0.0.6] ### Notes` line about "tracked as a follow-up" is gone (`grep -n "Migration to a" CHANGELOG.md` returns no matches). The new `DONE-047-0.0.7` Done body is past-tense throughout ("Pinned the package-defined scalar registration path…", "is redefined…", "is removed because…", "migrates to the new pattern", "migrate too"). CONFIRMED.

### What looks solid

- Verbatim spec drop-ins land byte-identical at all three CHANGELOG sites (Added, Changed bullet, Removed) and the KANBAN Done body site, with the spec's `> ` blockquote markers stripped consistently.
- The fenced `diff` block inside the `### Changed` Breaking-change bullet is correctly indented two spaces to remain inside the Markdown list item; under standard CommonMark rendering this preserves the bullet structure while keeping the diff fence valid.
- The `### Added` / `### Removed` heading placements follow the spec's explicit placement rules (`Added` BEFORE `Changed`; `Removed` AFTER `Fixed`), not the canonical Keep a Changelog ordering — which is correct because the spec is the local authority and explicitly chose this placement.
- The empty `## In progress` board column gracefully degrades to a one-line italicized stub rather than leaving the heading bare; the stub references a real next card (`TODO-ALPHA-021-0.0.8`), so it is forward-accurate.
- Version-bump exclusion held cleanly — `pyproject.toml`, `__version__`, and `tests/base/test_init.py` all still read `0.0.7`, matching Decision 8 / DoD item 17.

### Temp test verification

- None. Slice 5 is Markdown-only — there is no Python surface to probe with temp tests. No temp test files were created under `docs/builder/temp-tests/slice-5/`.

### Static inspection helper

SKIPPED — Slice 5 touches only Markdown (`KANBAN.md`, `CHANGELOG.md`); `scripts/review_inspect.py` operates on Python source. Skip recorded per BUILD.md "Worker 1 and Worker 3 may skip the helper for files where the artifact will be a 'no review-worthy logic' disposition … The skip must be recorded explicitly with a short reason."

### Notes for Worker 1 (spec reconciliation)

- No spec edits required. Slice 5 implemented the spec's Slice 5 sub-checks (lines 57-60) using the canonical verbatim drop-ins at spec lines 577, 586, 589-597, and 600. The Decision 8 / DoD item 17 version-bump deferral held.
- Sanity note for the integration pass: the `### In progress` summary paragraph at `KANBAN.md` line 50 still carries the version-bump-deferred sentence ("The last `0.0.7` card to ship owns the version bump…"). Given that `DONE-047-0.0.7` is the last `0.0.7` card and explicitly does NOT bump the version per Decision 8, this sentence now reads as forward-looking against a hypothetical future card rather than the now-shipped one. It still parses correctly but is mildly stale; whether to leave it as a maintainer-cut-time signal or rewrite it is the maintainer's call at the next `0.0.8` cut, not a Slice 5 finding.

### Review outcome

`review-accepted` — every CHANGELOG and KANBAN edit matches the spec's verbatim drop-ins byte-for-byte, the heading-placement rules are honored, the `[0.0.6] ### Notes` removal is complete, the version-bump exclusion held, and no High/Medium/Low findings surfaced. Status line updated to `review-accepted` at the top of this artifact.

---

## Final verification (Worker 1)

- **Spec slice checklist.** Every `- [ ]` in the Plan's `### Spec slice checklist (verbatim)` is now `- [x]`:
  - `KANBAN.md` move WIP→Done: ticked. `grep -c "^### WIP-ALPHA-020-0.0.7" KANBAN.md` = 0; `grep -c "^### DONE-047-0.0.7" KANBAN.md` = 1 at line 1675. `Definition of done` bullet-1 stale path eliminated (`grep -c "docs/spec-scalar_map_helper.md" KANBAN.md` = 0). `### In progress` summary paragraph at `KANBAN.md` line 50 lists `DONE-047-0.0.7` among the six shipped `0.0.7` cards; `WIP-ALPHA-020-0.0.7` removed from the remaining-cards list (narrative flipped to "all five have shipped"). The `## In progress` board column carries a single-line italicized stub referencing the next real card `TODO-ALPHA-021-0.0.8`.
  - `CHANGELOG.md` four-edit set: ticked. `[Unreleased]` now carries `### Added` (line 20) → `### Changed` (line 23, with the new Breaking-change bullet at line 27 plus the inner triple-backtick `diff` fenced block at lines 29-33) → `### Fixed` (line 37) → `### Removed` (line 40). `[0.0.6] ### Notes` section removed entirely (`grep -c "Migration to a" CHANGELOG.md` = 0). All three new `[Unreleased]` bullets are byte-identical to the spec's verbatim drop-ins at spec lines 584, 587-595, and 598 apart from the documented `> ` blockquote-marker strip.
  - Version bump exclusion: ticked. `pyproject.toml [project].version = "0.0.7"`, `django_strawberry_framework/__init__.py::__version__ = "0.0.7"`, and `tests/base/test_init.py`'s pinned-version assertion all unchanged. Spec Decision 8 / DoD item 17 held.
- **DRY check across slices.** The repeated `config=strawberry_config()` pattern across Slice 2 (10 converter-table sites + 2 integration tests), Slice 3 (the fakeshop project schema), Slice 4 (six doc-side sites), and now Slice 5's CHANGELOG `### Changed` before/after diff block is the canonical consumer-migration story per Decision 2 (which explicitly rejected a `dst.Schema(...)` wrapper). Confirmed not a DRY violation. The deliberate `KANBAN.md` Done-body vs `CHANGELOG.md` `### Changed` bullet repetition is justified by their complementary audiences (historical card story vs consumer release story) and mandated by DoD items 15 and 16. No new cross-slice duplication surfaced.
- **Existing tests still pass.** Slice 5 ships zero code/test changes. Per spec authorization and Worker 0's dispatch guidance, the full pytest sweep is deferred to the `bld-final.md` final test-run gate. No focused tests run here. Slice 2's `final-accepted` pass already validated the test surface (`uv run pytest --no-cov tests/test_scalars.py tests/base/test_init.py tests/types/test_converters.py` → 103 passed, 2 skipped, 0 failed); Slice 3's pass validated the fakeshop trees (75 passed, 1 skipped, 0 failed); no Slice 5 edit could regress those because Slice 5 touched only `KANBAN.md` and `CHANGELOG.md`.
- **Spec reconciliation.** Worker 3's `### Notes for Worker 1 (spec reconciliation)` block flagged the `### In progress` summary paragraph's "The last `0.0.7` card to ship owns the version bump from `0.0.6` per Decision 10 of `docs/SPECS/spec-016-list_field-0_0_7.md`" sentence as mildly stale because `DONE-047-0.0.7` is the last `0.0.7` card and explicitly defers the bump per Decision 8. Disposition: NO SPEC EDIT NEEDED. The sentence is forward-looking against a hypothetical future card (the next card to ship under any cut owns its bump); it still parses correctly as informational maintainer-cut-time content rather than a Slice 5 contract violation. Plan pre-decided to leave it; confirmed.
- **Status-line refresh (spec line 4).** Updated from `"in flight — Slices 1–4 shipped (helper module + \`BigInt\` redefinition; tests; example-app migration; docs); Slice 5 remains."` to `"shipped — Slices 1–5 all landed (helper module + \`BigInt\` redefinition; tests; example-app migration; docs; KANBAN + CHANGELOG). Only the cross-slice integration pass and the final test-run gate remain (workflow gates, not slices)."` per the per-spawn re-verification rule in `worker-1.md`. Recorded under `### Spec changes made (Worker 1 only)` below.
- **Final status.** `final-accepted`. Status line at the top of this artifact updated.

### Summary

Slice 5 closed the build's mutation surface with Markdown-only edits across `KANBAN.md` and `CHANGELOG.md`: moved `WIP-ALPHA-020-0.0.7` to `DONE-047-0.0.7` in the Done column with the verbatim past-tense body from spec line 577 plus a `Build plan:` citation; rewrote the `### In progress` summary paragraph to drop `WIP-ALPHA-020-0.0.7` and add `DONE-047-0.0.7`; left a single-line italicized stub under the now-empty `## In progress` board column referencing the next real card `TODO-ALPHA-021-0.0.8`; added `### Added`, appended a Breaking-change bullet to `### Changed`, and added `### Removed` to the `[Unreleased]` section of `CHANGELOG.md` using the spec's verbatim drop-ins at spec lines 584, 587-595, and 598; removed the `[0.0.6] ### Notes` subsection entirely (heading + sole bullet) since the migration-tracker sentence the spec was paying down was that subsection's only content. Version-bump exclusion held per Decision 8 / DoD item 17 — `pyproject.toml`, `__version__`, and the pinned-version test all stay at `0.0.7`; a future maintainer-led cut promotes `[Unreleased]` to `[0.0.8]` atomically.

### Spec changes made (Worker 1 only)

- `docs/spec-020-scalar_map_helper-0_0_7.md` line 4 (Status header): refreshed from "in flight — Slices 1–4 shipped … Slice 5 remains." to "shipped — Slices 1–5 all landed … Only the cross-slice integration pass and the final test-run gate remain (workflow gates, not slices)." Per-spawn status-line re-verification rule in `worker-1.md`; the prior status was stale post-Slice-5 landing.
- `docs/spec-020-scalar_map_helper-0_0_7.md` `## Slice checklist` (Slice 5 block, spec lines 57-60): three sub-bullets ticked `- [x]` (parent Slice 5 + KANBAN sub-check + CHANGELOG sub-check + version-bump-exclusion sub-check) as the implementations landed verbatim in `KANBAN.md` and `CHANGELOG.md`. Standard final-verification ticking, not a content edit.
