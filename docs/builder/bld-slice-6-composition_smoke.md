# Build: Slice 6 — Cross-card composition smoke test with shipped Filtering subsystem

Spec reference: `docs/spec-028-orders-0_0_8.md` (Slice 6 sub-bullet at lines 138-139; Decision 8 — Cooperation with filtering / `get_queryset` / the optimizer at lines 644-695; Decision 9 — Input-class namespace lifecycle at lines 697-789; Test plan `tests/orders/test_composition.py` (Slice 6) section at lines 1014-1022; Implementation-plan Slice-6 row at line 968)
Status: final-accepted

## Plan (Worker 1)

### DRY analysis

Slice 6 ships ONE new package-internal test file under `tests/orders/test_composition.py`. The file already exists as a 10-line stub TODO-pseudocode placeholder (`tests/orders/test_composition.py:1-10`) per the pre-Slice-1 scaffolding seeded by Slice 1; Slice 6 REPLACES the stub with the real test body. NOTHING under `django_strawberry_framework/` is touched (the order + filter subsystems are already shipped through Slices 1-5 of this card and `DONE-027-0.0.8`); NOTHING under `examples/fakeshop/` is touched (the cross-card composition is already exercised live by Slice 4's `test_library_books_filter_and_order_compose` at `examples/fakeshop/test_query/test_library_api.py:1500-1543`). Slice 6 is a pure package-internal test slice that pins the composition contract from inside the package's test boundary.

The Slice 4 live HTTP test `test_library_books_filter_and_order_compose` (test_library_api.py:1500-1543) already pins the **live consumer-visible behavior** of filter+order composition end-to-end (GraphQL surface accepts both arguments; the response is filtered AND ordered). Slice 6 MUST NOT duplicate that assertion altitude; per spec line 138-139 and AGENTS.md "Test through real usage and prefer the example project: any coverage line achievable via a real GraphQL query against fakeshop in examples/fakeshop/test_query/ MUST be earned that way", the live-HTTP path already owns the consumer-visible composition coverage. Slice 6 therefore pins a **complementary** package-internal angle that the live HTTP path does NOT (and arguably cannot) cleanly assert: the FINALIZER/REGISTRY composition contract that both factories' input types are materialized into their respective per-module namespaces in one `finalize_django_types()` pass, that both `_helper_referenced_*` ledgers register cleanly when both helpers are called for the same `DjangoType`, that the apply pipelines produce a queryset whose **SQL** carries both `WHERE` and `ORDER BY` clauses in the canonical chain order, and that the shared `sets_mixins.LazyRelatedClassMixin` resolves correctly when called from both subsystems on the same finalize.

- **Existing patterns reused.**
  - `tests/orders/test_finalizer.py::_isolate_registry` autouse fixture (tests/orders/test_finalizer.py:51-65) — the canonical reset shape clearing `registry`, `_field_specs`, `_helper_referenced_ordersets`, `_materialized_names`, `OrderArgumentsFactory.input_object_types`, and `OrderArgumentsFactory._type_orderset_registry` around each test. Slice 6 reuses this shape **extended** to also reset the filter-side equivalents (`_helper_referenced_filtersets`, `_materialized_names` in `filters.inputs`, `FilterArgumentsFactory.input_object_types`, `FilterArgumentsFactory._type_filterset_registry`) so the composition test starts from a clean registry-state on BOTH subsystems. The filter-side reset pattern lives at `tests/filters/test_factories.py:42-48`.
  - `tests/orders/test_finalizer.py::test_phase_2_5_materializes_input_classes_into_module_globals` shape (tests/orders/test_finalizer.py:380-388) — the "assert `hasattr(inputs_module, '<X>OrderInputType')` AND `'<X>OrderInputType' in _materialized_names`" assertion pair Slice 6 mirrors for both `<X>OrderInputType` AND `<X>FilterInputType` against their respective inputs modules and ledgers (`django_strawberry_framework.orders.inputs` + `_materialized_names` on the order side; `django_strawberry_framework.filters.inputs` + the filter side's `_materialized_names` ledger).
  - `tests/orders/test_sets.py::_make_info` (tests/orders/test_sets.py:318-322) — the minimal `info`-shaped stub `SimpleNamespace(context=SimpleNamespace(request=HttpRequest()))` Slice 6 reuses verbatim for the apply-pipeline-into-queryset step. (Worker 2 may also factor the shared `_make_info` into a small helper local to the test file rather than re-import, at discretion — see `### Implementation discretion items`.)
  - `tests/orders/test_sets.py::test_orderset_apply_sync_applies_order_by_to_queryset` shape (tests/orders/test_sets.py:365-378) — the `list(result.query.order_by)` introspection pattern Slice 6 mirrors for the ORDER BY half of the composition assertion. The filter side's analogous pattern is `result.query.where` at `tests/filters/test_sets.py::test_apply_sync_filters_against_simple_scalar_input` (test_sets.py:845-855) — Slice 6 mirrors that pattern for the WHERE half.
  - `examples/fakeshop/apps/library/models` — Slice 6 reuses the fakeshop library models (`Book`, `Shelf`, `Branch`, `Genre`, etc.) as fixture models, NOT a test-only `apps.fake` model. The library models are already imported by every shipped `tests/orders/test_*.py` file (the `from apps.library import models as library_models` shape at e.g. tests/orders/test_factories.py:14 + tests/orders/test_finalizer.py:30) and by the filter-side `tests/filters/` tree. Reusing them keeps Slice 6 in the same test-boundary shape as every other `tests/orders/` test.
  - The shipped fakeshop library schema (`examples/fakeshop/apps/library/schema.py:55-71` for `BookType` carrying BOTH `filterset_class = filters.BookFilter` AND `orderset_class = orders.BookOrder`) is the canonical real-world example of the composition contract Slice 6 pins. Five library DjangoTypes (`LoanType`/`BookType`/`ShelfType`/`GenreType`/`BranchType`) currently carry both `filterset_class` and `orderset_class`; Slice 6's test does NOT need to instantiate any of them — it constructs a minimal local `DjangoType` from a library model so the registry-isolated fixture state is clean (the `_reload_project_schema_for_acceptance_tests` autouse path from `examples/fakeshop/test_query/test_library_api.py:47-50` is heavyweight and lives at the live-HTTP boundary, not the package-internal one).

- **New helpers justified — none.** Slice 6 ships ONE test file under `tests/orders/` containing ~2 test functions and an autouse `_isolate_registry` fixture (extended from the order-side shape to also reset the filter-side state). No new module-level helpers; no fixture promoted to a shared location. The test file's body is self-contained per `AGENTS.md` "Test through real usage" — the apply pipelines run against a real queryset from a real fakeshop library model.

- **Duplication risk avoided (four risks named explicitly).**
  - **Risk: Slice 6 duplicating Slice 4's live HTTP `test_library_books_filter_and_order_compose`.** Naive Slice 6 would assert "both arguments are accepted on the GraphQL surface AND the response is filtered + ordered" — exactly the assertion Slice 4 already owns at the live HTTP boundary (`test_library_api.py:1500-1543`). **Avoidance:** Slice 6 phrases its assertion at the **package-internal** altitude — input classes materialized into their respective per-module namespaces; `_helper_referenced_*` ledgers register cleanly; the apply pipelines produce a queryset whose `query.where` AND `query.order_by` carry the expected clauses; the shared `sets_mixins.LazyRelatedClassMixin` does NOT leak resolution state between subsystems. The live HTTP test asserts consumer-visible behavior; Slice 6 asserts implementation-contract behavior. Worker 3's review confirms the two tests do NOT overlap by reading both bodies side-by-side.
  - **Risk: a test-only `apps.fake` model + DjangoType pair that diverges from every other `tests/orders/` test.** Naive Slice 6 would declare a synthetic `class FakeModel(models.Model)` inside the test file (with `Meta.app_label = "library"` hacks) instead of reusing the shipped fakeshop library models. **Avoidance:** Slice 6 imports `from apps.library.models import Book, Shelf` (the same shape every `tests/orders/test_*.py` file uses) and constructs a small local `FilterSet` + `OrderSet` + `DjangoType` against the `Book` model. The local declarations live INSIDE the test function bodies (the `tests/orders/test_finalizer.py` convention — every test declares its own classes inside the function body, not at module scope) so the registry isolation fixture can reset cleanly between tests.
  - **Risk: re-deriving the registry-clear fixture shape from scratch rather than mirroring `tests/orders/test_finalizer.py`.** The `tests/orders/test_finalizer.py::_isolate_registry` fixture (test_finalizer.py:51-65) already pins the canonical 6-line clear sequence: `registry.clear()` + `_field_specs.clear()` + `_helper_referenced_ordersets.clear()` + `_materialized_names.clear()` + `OrderArgumentsFactory.input_object_types.clear()` + `OrderArgumentsFactory._type_orderset_registry.clear()`. **Avoidance:** Slice 6's `_isolate_registry` fixture mirrors this shape extended with the filter-side equivalents (sibling-imported from `django_strawberry_framework.filters` per the filter `__init__.py`'s `_helper_referenced_filtersets` + `django_strawberry_framework.filters.inputs._materialized_names` + `django_strawberry_framework.filters.factories.FilterArgumentsFactory.input_object_types` + `FilterArgumentsFactory._type_filterset_registry`). The extended shape is what makes Slice 6's test the composition pin: both subsystems start clean and both subsystems get exercised against the same `DjangoType`.
  - **Risk: asserting against raw SQL strings via `str(queryset.query)` (fragile, backend-dependent quoting).** Naive Slice 6 would write `assert "WHERE" in str(queryset.query)` and `assert "ORDER BY" in str(queryset.query)` — SQL-substring matching is fragile (SQLite uses double-quoted identifiers; PostgreSQL uses double-quoted identifiers; MySQL uses backtick-quoted identifiers; the WHERE clause's exact form depends on the lookup transform pipeline). Per spec test plan line 1019 ("the SQL string check is fragile but the assertion is robust because the queryset's `.where` and `.order_by` attributes are stable Django internals"), **Slice 6 asserts against `queryset.query.where` (a `WhereNode` instance — non-empty when the filter pipeline ran) AND `list(queryset.query.order_by)` (a list of `OrderBy` expressions OR strings — non-empty AND containing the expected field) rather than substring-matching the SQL string.** The `query.where` check uses `len(list(queryset.query.where.children))` > 0 OR `bool(queryset.query.where)` (the `WhereNode.__bool__` is True only when children are present). The `query.order_by` check is `list(queryset.query.order_by)` (the same pattern Slice 2's `test_orderset_apply_sync_applies_order_by_to_queryset` uses at tests/orders/test_sets.py:371-376). This is the same idiom every other `tests/orders/` and `tests/filters/` test uses to inspect queryset internals.

- **`scripts/review_inspect.py` planning-time disposition.** Slice 6 creates ONE new non-package test file under `tests/orders/test_composition.py`. The current stub is 10 lines; the post-Slice-6 file is estimated at ~80-120 source lines (one autouse fixture + 1-2 test bodies + minimal imports). Per BUILD.md "Worker 3 must run the helper during review when ... The slice adds 50 or more lines of new logic to any file outside `django_strawberry_framework/`", the Worker-3 helper trigger fires if the post-Slice-6 file is ≥50 lines (it will be). Worker 1 records this trigger so Worker 3 knows the helper run is required at review. **At planning time the helper does NOT fire** — Worker 1's planning helper trigger is "The plan adds logic to any existing `.py` file with at least 150 source lines" OR "any file under `django_strawberry_framework/optimizer/` or `django_strawberry_framework/types/`" (BUILD.md "When to run the helper during build"). Slice 6 touches neither; the current stub is 10 lines (under 150) and `tests/orders/test_composition.py` is not under `optimizer/` or `types/`. **Worker 1 planning helper disposition: skipped (file is below the 150-line trigger AND outside optimizer/types).** Worker 3 review helper disposition: required (post-edit file will be ≥50 lines outside `django_strawberry_framework/`).

### Implementation steps

Slice 6 lands ONE file edit: replace the 10-line TODO-pseudocode stub at `tests/orders/test_composition.py` with the actual test body. Line anchors below are pin-at-write-time hints per `BUILD.md` Implementation steps note; Worker 2 verifies against the live source before editing.

**Design decision pinned at planning: Option A (re-use shipped fakeshop library models).** Per the AGENTS.md rule "Test through real usage and prefer the example project: any coverage line achievable via a real GraphQL query against fakeshop in examples/fakeshop/test_query/ MUST be earned that way" — and given Slice 4's `test_library_books_filter_and_order_compose` already pins the live composition behavior — Slice 6 reuses the shipped fakeshop library `Book` model as the fixture model but constructs LOCAL `FilterSet` + `OrderSet` + `DjangoType` declarations inside each test function so the registry isolation works cleanly. Worker 2 does NOT re-instantiate `apps.library.filters.BookFilter` or `apps.library.orders.BookOrder` (those classes are already bound to `apps.library.schema.BookType` at the schema-module-import time and would collide with the local test `DjangoType` if reused).

Option B (define a fully synthetic test-only model) was considered and rejected — it would require a `Meta.app_label = "library"` hack OR a fresh test app + migration, neither of which fits the "test through real usage" rule. Reusing the library `Book` model with local `FilterSet`/`OrderSet`/`DjangoType` declarations is the smallest fixture surface that exercises the real package contract.

1. **Replace the TODO stub at `tests/orders/test_composition.py`** with a docstring describing the package-internal composition contract being pinned, the imports, the extended `_isolate_registry` fixture, the `_make_info` helper, and one combined test (or a pair of tightly-scoped tests — see Worker 2 discretion below). Pin-at-write-time anchors:
   - Replace the current stub at `tests/orders/test_composition.py:1-10`.
   - Module docstring at top describes the four assertions per spec test plan lines 1014-1022: (a) both factories' input types materialized into their respective per-module namespaces; (b) both `_helper_referenced_*` ledgers register cleanly; (c) a resolver consuming both arguments produces a queryset whose SQL carries `WHERE <filter>` AND `ORDER BY <order>` clauses; (d) the shared `sets_mixins.LazyRelatedClassMixin` does not leak resolution state between subsystems.
   - Imports (model + framework): `from __future__ import annotations` + (blank line) + `import pytest` + (blank line) + `import strawberry` + `from apps.library.models import Book` + (blank line) + `from django_strawberry_framework import DjangoType, finalize_django_types` + `from django_strawberry_framework.filters import FilterSet` + `from django_strawberry_framework.filters.factories import FilterArgumentsFactory` + `from django_strawberry_framework.filters.inputs import INPUTS_MODULE_PATH as FILTER_INPUTS_MODULE_PATH` + `from django_strawberry_framework.filters import _helper_referenced_filtersets` + `from django_strawberry_framework.orders import OrderSet` + `from django_strawberry_framework.orders import _helper_referenced_ordersets` + `from django_strawberry_framework.orders.factories import OrderArgumentsFactory` + `from django_strawberry_framework.orders.inputs import Ordering, INPUTS_MODULE_PATH as ORDER_INPUTS_MODULE_PATH` + `from django_strawberry_framework.registry import registry`. The two `INPUTS_MODULE_PATH` re-exports are aliased to `FILTER_INPUTS_MODULE_PATH` / `ORDER_INPUTS_MODULE_PATH` so the test body reads cleanly. Worker 2 may also alias `_materialized_names` from both subsystems but at planning Worker 1 prefers using `getattr(sys.modules[X], "...InputType")` for the reachability assertion (simpler, more honest — that IS what `LazyType.resolve_type` does at schema-build time).
   - Import the filter-side `_materialized_names` ledger as `from django_strawberry_framework.filters.inputs import _materialized_names as filter_materialized_names` and the order-side as `from django_strawberry_framework.orders.inputs import _materialized_names as order_materialized_names` so the two ledgers can be cleared independently in the fixture and probed independently in the assertions.
   - The autouse `_isolate_registry` fixture clears 12 caches around each test (mirrors `tests/orders/test_finalizer.py::_isolate_registry` extended with the filter-side equivalents). The clear-tier ordering inside the fixture body is: (1) `registry.clear()` — this single call already triggers both `clear_order_input_namespace()` and `clear_filter_input_namespace()` plus both `_helper_referenced_*.clear()` via the local-import dance per Decision 9 line 775 + the shipped filter-side equivalent; (2) explicit defensive clears on the two `*_materialized_names` ledgers, both `*ArgumentsFactory.input_object_types`, and both `*ArgumentsFactory._type_*_registry` caches (defensive because the `registry.clear()` path SHOULD already cover them via the helper functions, but pinning them explicitly here makes a regression in the lifecycle contract surface as a test failure here too).
   - The `_make_info` helper inside the test file: `def _make_info() -> object: ...` returning a `SimpleNamespace(context=SimpleNamespace(request=HttpRequest()))` per the `tests/orders/test_sets.py:318-322` shape. Per AGENTS.md "First line of every catalog/auth test: seed_data(N) or create_users(N) from apps.products.services" — **this is NOT a catalog test (the library app has no `services.py` and the AGENTS.md rule explicitly carves out the library app: "Library acceptance tests use inline Model.objects.create; the library app has no services.py" at AGENTS.md line 8). The first-line `seed_data(N)` rule does NOT apply.** The test fixture model is `apps.library.models.Book`, not `apps.products.models.Item`/`Entry`/`Property`. Worker 2 does NOT prepend `seed_data(N)`. Per the library test convention, the test body uses inline `Book.objects.create(...)` if a real row is needed for the queryset to compile, OR (preferred per the spec's "SQL string check is fragile but the .where / .order_by attributes are stable" guidance) the test inspects `queryset.query` attributes WITHOUT a `.django_db` fixture mark — the `WhereNode` and `OrderBy` lists are populated at `.filter(...)` / `.order_by(...)` time on an UNEXECUTED queryset, so the assertion does not require database hits.

2. **Test body shape (one combined function — recommended) OR two tightly-scoped functions (Worker 2 discretion).** Per the spec's Slice 6 sub-bullet ("**One** package-internal test under `tests/orders/test_composition.py`"), Worker 1's preferred shape is ONE test function `test_filter_and_order_compose_through_finalizer_and_apply_pipelines` that exercises every assertion in sequence so the contract is pinned as one atomic story. Worker 2 may split into two tightly-scoped tests at discretion if the combined body grows past ~60 lines and individual failures would benefit from named test failures rather than a single multi-line assertion. The discretion item below records the two acceptable shapes. **For the planning pass, Worker 1 pins the ONE-test shape as the canonical shape; Worker 2 may split if implementation reveals readability benefits.**

   The test body in the ONE-test shape:
   - Declare a local `BookFilter(FilterSet)` with `class Meta: model = Book; fields = {"title": ["exact", "icontains"]}` (dict-of-lookups shape — filter-side syntax per `tests/filters/test_sets.py` convention).
   - Declare a local `BookOrder(OrderSet)` with `class Meta: model = Book; fields = ["title"]` (list shape — order-side syntax per `tests/orders/test_sets.py` convention).
   - Declare a local `BookType(DjangoType)` with `class Meta: model = Book; fields = ("id", "title"); filterset_class = BookFilter; orderset_class = BookOrder`.
   - Call `finalize_django_types()`.
   - **Assertion 1 (input types reachable from both subsystems' namespaces).** `import sys` + `filter_inputs_mod = sys.modules[FILTER_INPUTS_MODULE_PATH]` + `order_inputs_mod = sys.modules[ORDER_INPUTS_MODULE_PATH]` + `assert hasattr(filter_inputs_mod, "BookFilterInputType")` + `assert hasattr(order_inputs_mod, "BookOrderInputType")` + `assert "BookFilterInputType" in filter_materialized_names` + `assert "BookOrderInputType" in order_materialized_names` + `assert "BookFilterInputType" in FilterArgumentsFactory.input_object_types` + `assert "BookOrderInputType" in OrderArgumentsFactory.input_object_types`. This is the "both factories' input types are reachable from the schema" half of spec line 139 phrased at the materialization-ledger altitude.
   - **Assertion 2 (resolver consuming both arguments produces a queryset whose SQL carries WHERE AND ORDER BY in expected order).** Build the input objects: `BookFilterInputType = FilterArgumentsFactory.input_object_types["BookFilterInputType"]` + `BookOrderInputType = OrderArgumentsFactory.input_object_types["BookOrderInputType"]` + `filter_input = BookFilterInputType(title=BookFilterInputType.lookup_args_type(exact="Foundation"))` (filter-side input shape per Slice 4 live-HTTP test conventions) + `order_input = [BookOrderInputType(title=Ordering.ASC)]`. Chain the apply pipelines in canonical resolver order: `info = _make_info()` + `queryset = Book.objects.all()` + `queryset = BookFilter.apply_sync(filter_input, queryset, info)` + `queryset = BookOrder.apply_sync(order_input, queryset, info)`. Assertions: `assert bool(queryset.query.where)` (the `WhereNode` is non-empty — `apply_sync(filter, ...)` populated it) + `order_by = list(queryset.query.order_by)` + `assert order_by` (non-empty — `apply_sync(order, ...)` populated it). Optional: `assert "title" in str(order_by)` for a tighter pin on which column the `ORDER BY` clause references (Django's `OrderBy` repr carries the column name). The "in the expected order" half of spec line 139 is the canonical resolver chain: visibility → filter → order, so the WHERE clause's `WhereNode` is established BEFORE the `ORDER BY` clauses chain to the same queryset's `.query.order_by`. The test does NOT need to inspect raw SQL substring order (the resolver order is the contract, not the textual SQL order).
   - **Assertion 3 (shared `sets_mixins.LazyRelatedClassMixin` does not leak resolution state between subsystems).** The spec test plan line 1020 names this assertion: "The shared `LazyRelatedClassMixin` resolution works correctly when called from both subsystems in the same finalize pass (verifies the sibling-import sharing doesn't cause a resolution-state leak)." The minimal pin: re-declare a sibling pair of `RelatedFilter` AND `RelatedOrder` against the SAME `Book.shelf` relation (e.g., `BookFilter.shelf = RelatedFilter(ShelfFilter, field_name="shelf")` AND `BookOrder.shelf = RelatedOrder(ShelfOrder, field_name="shelf")`), call `finalize_django_types()`, and assert BOTH `BookFilter.related_filters["shelf"].bound_filterset is BookFilter` AND `BookOrder.related_orders["shelf"].bound_orderset is BookOrder` (the per-side bound-class attributes are set by their respective metaclasses through the shared `LazyRelatedClassMixin` resolution path; if the mixin leaked state, one side would see the other's `bound_*` class). **Worker 2 discretion:** if the local `ShelfFilter` + `ShelfOrder` + `ShelfType` declarations bloat the test body past ~80 lines, Worker 2 may simplify this assertion to a class-attribute identity check on the mixin itself: `from django_strawberry_framework.sets_mixins import LazyRelatedClassMixin` + `assert LazyRelatedClassMixin in BookFilter.__mro__` + `assert LazyRelatedClassMixin in BookOrder.__mro__` (both classes inherit the same mixin via different paths — the filter side inherits indirectly through its own base; the order side inherits indirectly through its own base — so the MRO-membership pin is a smaller surface). At planning Worker 1 prefers the `RelatedFilter`/`RelatedOrder` shape (it actually exercises the resolution path) but accepts either shape per discretion.

3. **No CHANGELOG edit, no docs edit, no spec edit.** Slice 6's spec contract (line 138-139) covers only the test file. Per the build plan context flag (build-028-orders-0_0_8.md line 56: "CHANGELOG-edit permission: Slice 5 carries the spec's explicit-instruction permission to edit `CHANGELOG.md`. No other slice may touch the changelog."), Slice 6 ships zero CHANGELOG edits. Slice 6 also ships zero standing-doc edits — Slice 5 already moved all the Ordering symbols' status flags to `shipped (0.0.8)` and renumbered the live-HTTP test count; the post-Slice-5 doc state is already accurate for the post-Slice-6 reality (the composition test is package-internal coverage, not a new symbol or capability). Worker 2 does NOT touch `docs/` or `KANBAN.md` or `CHANGELOG.md`.

4. **`uv run ruff format .` + `uv run ruff check --fix .`.** Per AGENTS.md the post-edit ruff cycle runs after every edit; Worker 2 runs both. Per the AGENTS.md trailing-comma rule, any list/dict/tuple/def-signature ≥ 4 items must carry a trailing comma; the test file's imports list and the `Meta.fields = (...)` tuples must follow this rule.

### Test additions / updates

- **`tests/orders/test_composition.py`** — replace the 10-line TODO stub with the real test body. Pin one test function (or a tightly-scoped pair at Worker 2 discretion):

  - `test_filter_and_order_compose_through_finalizer_and_apply_pipelines` — the canonical Slice 6 test per spec line 138-139. Asserts (a) both `<X>FilterInputType` and `<X>OrderInputType` are materialized as module globals of their respective `inputs.py` modules; (b) both `_materialized_names` ledgers carry the names; (c) both factory class-level `input_object_types` carry the names; (d) the filter+order apply chain produces a queryset whose `query.where` is non-empty AND `query.order_by` is non-empty AND the OrderBy expression references the expected column; (e) the shared `LazyRelatedClassMixin` does not leak state between the two subsystems' `related_*` resolution.

  **Alternative pair shape (Worker 2 discretion):**
  - `test_compose_filter_and_order_factories_both_materialize` — covers assertions (a)-(c) of the combined shape above (the materialization half).
  - `test_compose_filter_and_order_produces_where_and_order_by_clauses` — covers assertions (d)-(e) of the combined shape above (the apply-pipeline + shared-mixin half).

  Either shape is acceptable; Worker 1's preference is the ONE-test shape per spec line 138-139's literal "**One** package-internal test" phrasing. Worker 2 chooses based on implementation readability.

- **Temp test opportunities for Worker 3.** None expected. The test body is short enough (~60-100 lines) that Worker 3 can read it end-to-end without a scratch reproduction. If Worker 3 wants to confirm the `query.where` introspection is stable across Django versions, a temp test under `docs/builder/temp-tests/slice-6/test_query_where_introspection.py` may be created and disposed per BUILD.md "Temp test verification".

### Implementation discretion items

Worker 1 has assessed the design and pinned the load-bearing decisions in `### Implementation steps`. The items below are **stylistic equivalents** at Worker 2's discretion:

- **ONE combined test function vs TWO tightly-scoped tests.** Per spec line 138-139 the canonical shape is ONE test. Worker 2 may split into two tests `test_compose_filter_and_order_factories_both_materialize` + `test_compose_filter_and_order_produces_where_and_order_by_clauses` if the combined body grows past ~60 source lines and named failures would aid debugging. Worker 1 prefers the ONE-test shape but accepts either.
- **`_make_info()` local helper vs sibling-import from `tests/orders/test_sets.py`.** The helper is 5 lines of inline `SimpleNamespace` construction; Worker 2 may either declare it inline at module scope (preferred — matches the test_sets.py / test_finalizer.py convention) or sibling-import (lighter file but cross-test-module dependency). Worker 1 prefers the inline declaration.
- **Module-global `import sys` at the top vs inline `import sys` inside the test body.** Both are acceptable; the test file imports many module-level symbols anyway so a top-of-file `import sys` is consistent.
- **Assertion 3 shape (RelatedFilter+RelatedOrder declarations vs MRO-membership pin).** Both shapes are acceptable; Worker 1's preferred shape is the `RelatedFilter`/`RelatedOrder` declaration (actually exercises the resolution path) but the MRO pin is a valid simpler form. Worker 2 chooses based on the body-size budget — if the local `ShelfFilter`+`ShelfOrder`+`ShelfType` declarations push the test body past ~100 lines, switch to the MRO pin.
- **Filter input value construction shape — `BookFilterInputType(title=BookFilterInputType.lookup_args_type(exact="Foundation"))` vs the SimpleNamespace-shaped raw dict the filter-side `apply_sync` also accepts.** The shipped filter apply pipeline accepts both shapes (the Strawberry input dataclass shape AND a dict-shape); Worker 2 chooses based on which produces the cleanest local-test body. Worker 1's preference is the dataclass shape because it mirrors the production resolver-site call.
- **Inline `Book.objects.create(...)` row seeding vs unexecuted-queryset introspection.** Worker 1 prefers the unexecuted-queryset path (`Book.objects.all()` + `.filter(...)` + `.order_by(...)` against `queryset.query.where` / `.query.order_by` without ever hitting the database) because it dodges the `pytest.mark.django_db` decorator and runs faster. If Worker 2 finds the unexecuted-queryset path produces an empty `WhereNode` for any reason (some filter pipelines short-circuit on empty querysets), Worker 2 may fall back to a `.django_db` decorator + a single seeded row. Either shape is acceptable.

### Spec slice checklist (verbatim)

The spec's nested sub-bullets for Slice 6 from `## Slice checklist` at spec lines 138-139, copied verbatim:

- [x] Slice 6: Cross-card composition smoke test with the shipped Filtering subsystem
  - [x] One package-internal test under [`tests/orders/test_composition.py`][test-orders-composition] (new) that constructs a `DjangoType` with BOTH `Meta.filterset_class` AND `Meta.orderset_class` set, calls `finalize_django_types()`, and asserts both factories' input types are reachable from the schema AND a resolver that consumes both arguments produces a queryset whose SQL carries `WHERE <filter>` AND `ORDER BY <order>` clauses in the expected order. The Filtering subsystem already shipped, so the test does NOT need to wait for a sibling card; it lands in this card's PR as Slice 6 directly. (The Slice-6 conditional clause from the Filtering spec's [Slice checklist](#slice-checklist) — "carried by sibling" — is satisfied here by this card carrying the composition test instead.)

---

## Build report (Worker 2)

### Files touched

- `tests/orders/test_composition.py` — replaced the 10-line TODO-pseudocode stub with the Slice 6 composition test body. One autouse `_isolate_registry` fixture clearing twelve caches (both subsystems' `_field_specs`, `_materialized_names`, `_helper_referenced_*`, `*ArgumentsFactory.input_object_types`, and `*ArgumentsFactory._type_*_registry`), one local `_make_info` helper, and two test functions: `test_filter_and_order_compose_through_finalizer_and_apply_pipelines` (the canonical Slice-6 contract test per spec line 138-139) plus `test_filter_and_order_share_lazy_related_class_mixin_via_neutral_module` (the shared-mixin-identity pin per Spec Revision 4 H1 / Plan Assertion 3).

### Tests added or updated

- `tests/orders/test_composition.py::test_filter_and_order_compose_through_finalizer_and_apply_pipelines` — pins (a) both per-module inputs modules carry the materialized classes (`hasattr(sys.modules[FILTER_INPUTS_MODULE_PATH], "BookFilterInputType")` + `hasattr(sys.modules[ORDER_INPUTS_MODULE_PATH], "BookOrderInputType")`), (b) both `_materialized_names` ledgers register the names, (c) both factories' `input_object_types` carry the names, (d) the apply chain `BookFilter.apply_sync(filter_input, qs, info)` → `BookOrder.apply_sync(order_input, qs, info)` produces `bool(queryset.query.where) is True` AND `list(queryset.query.order_by)` non-empty referencing the `title` column, AND (e) the captured SQL string (under `CaptureQueriesContext`) carries `WHERE` AND `ORDER BY` clauses with `WHERE` preceding `ORDER BY` in the canonical resolver-chain order.
- `tests/orders/test_composition.py::test_filter_and_order_share_lazy_related_class_mixin_via_neutral_module` — pins `LazyRelatedClassMixin in RelatedFilter.__mro__` AND `LazyRelatedClassMixin in RelatedOrder.__mro__` AND `filter_mixin is order_mixin is LazyRelatedClassMixin` (same class object across both subsystems' `Related*` primitives, not a sibling-duplicate copy). A future refactor that re-declared a sibling copy under either subsystem would fail this identity pin loudly.

### Validation run

- `uv run ruff format .` — pass (1 file reformatted on first run -- `tests/orders/test_composition.py` itself; 213 files left unchanged on the post-edit re-run; the COM812-vs-formatter warning printed by ruff is pre-existing repo config, not Slice-6-introduced).
- `uv run ruff check --fix .` — pass (All checks passed!).
- `git status --short` after both ruff invocations — only one slice-intended change under `tests/` (`tests/orders/test_composition.py`). All other modified files (`KANBAN.html`/`KANBAN.md`/`CHANGELOG.md`/`README.md`/`TODAY.md`/`docs/...`/`django_strawberry_framework/...`/`examples/...`/`scripts/...`/`tests/types/test_base.py`/`tests/types/test_relay_interfaces.py`/etc.) are baseline-dirty paths from prior slices in this build (1-5) or from the maintainer's concurrent work surfaced in the build plan preamble; none reverted, none edited by Slice 6. No tool churn introduced.
- Focused test run: `uv run pytest --no-cov tests/orders/test_composition.py -v` — 2 passed in 0.63s. Broader sanity sweep: `uv run pytest --no-cov tests/orders/` — 117 passed in 0.94s. No `--cov*` flags used.

### Implementation notes

- **Plan-vs-implementation drift on the filter-input shape.** Plan Step 2 pinned `fields = {"title": ["exact", "icontains"]}` plus the per-field operator-bag input shape `BookFilterInputType(title=BookFilterInputType.lookup_args_type(exact="Foundation"))`. At build time the dict-shape input `{"title": {"icontains": "Foundation"}}` produced an empty `WhereNode` because `FilterSet._normalize_input`'s `_operator_bag_items(raw_value)` sniff requires `__dataclass_fields__` and a plain `dict` value falls through to the scalar branch (the dict gets stuffed verbatim into `data["title"]` which django-filter's form rejects silently into a no-op clause). Mechanically-obvious small drift per `worker-2.md` "Plan-vs-implementation drift" guidance: swapped to `fields = {"title": ["exact"]}` + scalar `filter_input = {"title": "Foundation"}` (the single-lookup form makes django-filter's `title` `CharFilter` accept a top-level scalar and emit `WHERE title = 'Foundation'`). Behaviour pinned at the same altitude — filter pipeline populates `WhereNode`. Flagged under `### Notes for Worker 1`.
- **Plan-vs-implementation drift on the LazyRelatedClassMixin assertion.** Plan Step 2 + Assertion 3 sketched both an MRO check on `BookFilter` / `BookOrder` AND a `BookFilter.related_filters["shelf"].bound_filterset` / `BookOrder.related_orders["shelf"].bound_orderset` shape. At build time the MRO check on `BookFilter.__mro__` failed because `FilterSet` (declared at `django_strawberry_framework/filters/sets.py::FilterSet`) inherits only `ClassBasedTypeNameMixin` — `LazyRelatedClassMixin` is mixed into the `Related*` primitives (`RelatedFilter`/`RelatedOrder`) NOT into the `*Set` parents themselves. Repointed the assertion at `RelatedFilter.__mro__` + `RelatedOrder.__mro__` + the class-identity check `filter_mixin is order_mixin is LazyRelatedClassMixin`. This is the same contract Worker 1's plan was reaching for (mixin shared, not duplicated) at the correct MRO level. Flagged under `### Notes for Worker 1`.
- **SQL-string introspection via `CaptureQueriesContext`.** Plan Step 2 + Risk-avoidance #4 explicitly steered AWAY from raw `str(queryset.query)` substring matching as fragile. Build kept that guidance for the *primary* introspection (`bool(queryset.query.where)` + `list(queryset.query.order_by)` — stable Django internals) AND added a complementary `CaptureQueriesContext` block that executes the queryset and asserts the captured SQL string carries both `WHERE` AND `ORDER BY` clauses in canonical order (WHERE before ORDER BY). The .upper() before `find(...)` dodges identifier-quoting case differences across backends; the ordering check is a substring-position comparison (not a regex), which is stable. The combination pins the contract at two altitudes — the Django-internal attribute shape AND the rendered SQL output — without making the assertion fragile to backend quoting.
- **`@pytest.mark.django_db` only on the test that executes the queryset.** The composition test executes the queryset inside `CaptureQueriesContext` so the SQL-string assertion fires; that requires `django_db`. The shared-mixin-identity test does no ORM work and runs without `django_db` (faster). Two tests, two markers, minimal fixture surface per test.
- **Sibling-imports for both `INPUTS_MODULE_PATH` constants** aliased as `FILTER_INPUTS_MODULE_PATH` / `ORDER_INPUTS_MODULE_PATH` so the test body reads cleanly. Both ledgers (`_filter_materialized_names` / `_order_materialized_names`) also aliased so the autouse fixture can clear them independently from one site each. The `_field_specs` ledger on both sides is also imported (aliased) and cleared defensively, even though `registry.clear()` cascades to them via the local-import dance — pinning the explicit clear means a regression in the lifecycle contract surfaces as test bleed across runs rather than silently.
- **`_isolate_registry` clears 12 caches in both directions (setup + teardown).** Mirrors the `tests/orders/test_finalizer.py::_isolate_registry` shape extended with the 6 filter-side equivalents. Both subsystems start clean, both subsystems end clean — Slice 6 is the only test file in `tests/orders/` that clears both subsystems' caches, which is the load-bearing thing that makes the composition test the right altitude.

### Notes for Worker 3

- The test file went from 10 lines (TODO-pseudocode stub) to ~250 lines, crossing the BUILD.md "Worker 3 must run the helper during review when ... the slice adds 50 or more lines of new logic to any file outside `django_strawberry_framework/`" trigger. Worker 1's Plan disposition was "Worker 3 review helper disposition: required (post-edit file will be ≥50 lines outside `django_strawberry_framework/`)" -- the helper run is required at review. No shadow-file output was used at build time (the file is a pure test fixture; no review-worthy logic was sketched in shadow output during build).
- No `docs/builder/temp-tests/slice-6/` scratch tests created at build time. Worker 3 may want to confirm cross-Django-version stability of the `query.where` / `query.order_by` introspection idioms if comfort with the Django-internal contract is low; Worker 1's Plan flagged this as an opportunity. Build pass observed `WhereNode.__bool__` is True iff `WhereNode.children` is non-empty (the standard Django ORM contract since `django.db.models.sql.where`); `list(queryset.query.order_by)` is stable across at least Django 4.x → 6.x (the active environment runs Django 6.0.5).
- The autouse fixture clears the order side's `_field_specs` ledger by import alias (`_order_field_specs`) — the existing `tests/orders/test_finalizer.py::_isolate_registry` does the same via `_field_specs` direct import; either name is fine, the test file picked aliases to mirror the filter-side aliasing for symmetry. The filter-side `_filter_field_specs` clear is the load-bearing addition vs. Worker 1's Plan list (Plan named `_materialized_names` + `_helper_referenced_filtersets` + `input_object_types` + `_type_filterset_registry`; build added `_field_specs` for both subsystems for symmetric coverage — neither subsystem documents `_field_specs` as auto-cleared by `registry.clear()`, so the explicit clear is the conservative shape).

### Notes for Worker 1 (spec reconciliation)

Two implementation deviations from the Plan, both small and mechanically obvious per `worker-2.md` "Plan-vs-implementation drift":

1. **Filter input dict shape (`fields = {"title": ["exact"]}` + scalar value vs. `fields = {"title": ["exact", "icontains"]}` + bag-dataclass instance).** Plan Step 2 pinned the bag-dataclass shape; build observed that the dict-input path's `_operator_bag_items(raw_value)` requires `__dataclass_fields__` and a plain nested dict falls through to the scalar branch silently. Swapped to single-lookup `["exact"]` + flat scalar so the dict-input path emits `data["title"] = "Foundation"` directly. Same contract (filter pipeline populates `WhereNode` non-trivially with a `WHERE title = 'Foundation'` clause), smaller surface, no spec change needed. Spec line 138-139's contract reads "asserts ... a resolver that consumes both arguments produces a queryset whose SQL carries `WHERE <filter>` AND `ORDER BY <order>` clauses" — the simplified filter shape honors this verbatim. No spec edit recommended.
2. **LazyRelatedClassMixin MRO check on `RelatedFilter`/`RelatedOrder` vs. on `BookFilter`/`BookOrder`.** Plan Step 2 Assertion 3 sketched the `BookFilter.__mro__` / `BookOrder.__mro__` check; build observed that `LazyRelatedClassMixin` is mixed into the `Related*` primitives (per `django_strawberry_framework/filters/base.py::RelatedFilter` and `django_strawberry_framework/orders/base.py::RelatedOrder`), NOT into the `*Set` parents. The shared-mixin contract is honored at the `Related*` level — that's where Spec Revision 4 H1's "neutral shared port" sibling-imports actually land. Repointed the assertion accordingly. No spec edit recommended; the Plan's stated contract ("shared `LazyRelatedClassMixin` does not leak resolution state between subsystems") is honored at the correct MRO level.

Neither deviation changes the four-part composition contract (materialization + factory-cache + module-globals reachability + apply-pipeline SQL chain). Both are stylistic / mechanical corrections of how the assertion is *expressed*, not what is *asserted*. Worker 1's final verification may either accept the implementation as-is or edit the spec/plan to record the corrected shapes for future readers; either path closes the slice cleanly.

---

## Review (Worker 3)

### High:

None.

### Medium:

None.

### Low:

None.

### DRY findings

- The autouse `_isolate_registry` fixture body literally duplicates its 12-line clear sequence in setup and teardown (lines 102-112 vs 114-124). The shipped `tests/orders/test_finalizer.py::_isolate_registry` shape does the same (clears on both sides of the `yield`), so the duplication is consistent with the carry-over pattern Slice 6's plan explicitly mirrors; the DRY-tightening would be factoring out a local `def _clear_both_subsystems(): ...` and calling it twice. Worth recording at integration-pass altitude (the same pattern now lives in two files, with `tests/filters/` having its own per-test clears that would coalesce cleanly) but not a Slice-6 finding because the duplicated literal IS the shape the Plan endorsed.
- The repeated string literals `BookFilterInputType` (3x) and `BookOrderInputType` (4x) surfaced by `scripts/review_inspect.py` are the materialization-ledger keys being asserted at four altitudes (per-module `hasattr`, `_materialized_names` ledger, `*ArgumentsFactory.input_object_types`, factory-cache lookup). Each repetition pins a distinct assertion; consolidating to a constant would hide which altitude each assertion targets. Not a DRY finding; documented for the integration pass.

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` is empty. `__all__` unchanged. No new public exports. Authorized: Slice 6's spec contract (lines 138-139) is a pure test addition; the Plan's Files-touched list names only `tests/orders/test_composition.py`.

### CHANGELOG sanity

Not applicable; slice did not modify CHANGELOG.md.

### Documentation / release sanity

Not applicable; slice did not modify docs/release/KANBAN/archive surfaces.

### What looks solid

- Spec contract per lines 138-139 is honored end-to-end. The Plan's four assertion targets (a) `<X>FilterInputType` AND `<X>OrderInputType` materialized as module globals (Assertion 1 at lines 182-185), (b) both factories' `input_object_types` carry the classes (Assertion 3 at lines 196-197), (c) the `LazyType.resolve_type` lookup over both inputs modules (modeled by the `sys.modules[INPUTS_MODULE_PATH]` + `hasattr` pair at lines 182-185), and (d) `CaptureQueriesContext` confirming `WHERE` precedes `ORDER BY` in the captured SQL (lines 228-236) all land cleanly.
- Spec-test-count discretion is plan-licensed. Spec line 138-139 says "One package-internal test" but Plan's `### Implementation discretion items` explicitly grants Worker 2 the option to split into two tightly-scoped tests at discretion; Worker 2 split into `test_filter_and_order_compose_through_finalizer_and_apply_pipelines` (the four-altitude composition pin) plus `test_filter_and_order_share_lazy_related_class_mixin_via_neutral_module` (the Plan's Assertion 5 / spec test-plan line 1020 shared-mixin pin). The split sharpens individual failure attribution without changing the contract; accepted.
- Two plan-vs-implementation drifts surfaced in `### Notes for Worker 1 (spec reconciliation)` are both grounded in the shipped source. Verified by reading `django_strawberry_framework/filters/sets.py::FilterSet._normalize_input` (lines 591-706) — the dict-input path with a nested-dict value falls through `_operator_bag_items` (which only accepts `__dataclass_fields__`-bearing objects per its body at lines 727-751) into the bottom scalar branch where a plain nested dict would be silently rejected by `django-filter`'s `CharFilter` form. The single-lookup form + flat scalar that Worker 2 shipped emits `data["title"] = "Foundation"` directly and produces the intended `WHERE` clause. Same verification holds for the `LazyRelatedClassMixin` MRO repoint: confirmed `LazyRelatedClassMixin` is mixed into `RelatedFilter` (filters/base.py::RelatedFilter line 303) and `RelatedOrder` (orders/base.py::RelatedOrder line 30) directly via sibling-imports from `..sets_mixins`; the `*Set` parents do not inherit it. Both drifts are mechanically correct; spec edit not required.
- Local LOCAL declarations of `BookFilter` / `BookOrder` / `BookType` INSIDE the test function body (lines 154-175) avoid colliding with the already-schema-bound `apps.library.{filters,orders,schema}.*` classes; this is the only viable shape given the live fakeshop schema already binds `BookFilter` / `BookOrder` to `BookType`. The autouse fixture's 12-cache clear (registry + 5 filter-side + 5 order-side + both `_field_specs`) keeps both subsystems' state clean across tests.
- `finalize_django_types()` is called at line 177 per Spec line 138's explicit contract; the call is gated by `@pytest.mark.django_db` only on the test that executes the queryset under `CaptureQueriesContext`, so the second test (the mixin-identity pin) runs without DB cost.
- The AGENTS.md library-app carve-out at line 8 ("Library acceptance tests use inline Model.objects.create; the library app has no services.py") is correctly applied — no `seed_data(N)` first-line, no DB row creation needed (the SQL inspection works on an unevaluated queryset and an empty result set), the `pytest.mark.django_db` decorator gives just enough DB hookup for `list(queryset)` to execute.
- The SQL inspection avoids the fragile-substring trap that the Plan explicitly warned against: primary contract is Django-internal attribute introspection (`bool(queryset.query.where)` + `list(queryset.query.order_by)`), and the `CaptureQueriesContext` substring check normalizes via `.upper()` before `.find(...)` so backend identifier-quoting differences (SQLite vs PostgreSQL vs MySQL) do not break it. The clause-ordering pin (`where_idx < order_idx`) is the contract Spec line 139 names: "in the expected order."

### Temp test verification

- No temp tests created. The test body is short enough to read end-to-end; the cross-Django-version stability of `query.where` / `query.order_by` introspection was the only opportunity Worker 2 flagged for a scratch reproduction, and the Build report records the Django 6.0.5 attribute shape directly. No follow-up.

### Notes for Worker 1 (spec reconciliation)

- Two plan-vs-implementation drifts already recorded in Worker 2's `### Notes for Worker 1` are accepted as mechanically grounded in shipped source code:
  1. Filter-input dict shape: `fields = {"title": ["exact"]}` + scalar value is the single supported path for the dict-shaped `apply_sync` input; the originally-planned `{"title": ["exact", "icontains"]}` + bag-dataclass shape would have required constructing a Strawberry dataclass instance via `FilterArgumentsFactory.input_object_types["BookFilterTitleFilterInputType"](exact="Foundation")` which adds fixture surface without changing the assertion altitude. No spec edit required.
  2. `LazyRelatedClassMixin` MRO check: pinned at `RelatedFilter.__mro__` / `RelatedOrder.__mro__` is the only mechanically correct level — the `*Set` parents inherit only `ClassBasedTypeNameMixin`. Worker 2's class-identity pin (`filter_mixin is order_mixin is LazyRelatedClassMixin`) is STRONGER than the Plan's bare MRO membership check (catches a sibling-copy refactor that would still satisfy MRO membership). No spec edit required.
- Optional Plan/Spec-text update suggestion (not blocking): Worker 1 may want to retroactively pin the corrected assertion shapes in the Plan's Step 2 description so future archaeology against the Plan matches what shipped. Per `BUILD.md`'s spec-reconciliation pattern, this is a Worker 1 final-verification call.

### Review outcome

`review-accepted`. The Plan's four assertion targets (materialization, factory-cache, `LazyType.resolve_type` reachability, `CaptureQueriesContext` SQL ordering) all land cleanly; the two plan-vs-implementation drifts are mechanically grounded and recorded; no source code was touched; the public surface is unchanged; the broader `tests/orders/` + `tests/filters/` sweep passes 345 tests. The artifact's top-level `Status:` line is updated to `review-accepted`.

---

## Final verification (Worker 1)

- Spec slice checklist: one verbatim sub-bullet (`One package-internal test under tests/orders/test_composition.py ...`); ticked. The spec body says "**One** package-internal test" but Worker 2 shipped two tightly-scoped tests (`test_filter_and_order_compose_through_finalizer_and_apply_pipelines` + `test_filter_and_order_share_lazy_related_class_mixin_via_neutral_module`); the split is plan-licensed under `### Implementation discretion items` (line 81: "ONE combined test function vs TWO tightly-scoped tests"), Worker 3 explicitly accepted the split (`### What looks solid` bullet 2: "Spec-test-count discretion is plan-licensed"), and the two tests partition the composition contract cleanly (the first pins the four-altitude materialization + apply-pipeline contract under `@pytest.mark.django_db`, the second pins the neutral-mixin sharing contract without DB cost). Single sub-bullet is ticked.
- DRY check across this slice and prior accepted slices: no new cross-slice duplication. Slice 6 ships ONE test file (`tests/orders/test_composition.py`); zero source code touched; zero new helpers added under `django_strawberry_framework/`. The autouse `_isolate_registry` fixture in `tests/orders/test_composition.py` is a deliberate extension of `tests/orders/test_finalizer.py::_isolate_registry` with the filter-side equivalents added — the two clear-sequences are not literal duplicates because the composition test must reset BOTH subsystems (Slice 6's load-bearing contract per spec line 1020), while `test_finalizer.py` only resets the order side. Worker 3's `### DRY findings` already recorded the consolidation candidate ("factoring out a local `def _clear_both_subsystems()`") as integration-pass altitude work, NOT a Slice-6 finding because the duplicated literal IS the shape Slice 6's Plan endorsed. The two repeated string literals `BookFilterInputType` (3x) and `BookOrderInputType` (4x) surfaced by `scripts/review_inspect.py` are deliberately per-altitude assertion keys (per-module `hasattr`, `_materialized_names` ledger, factory `input_object_types`); consolidating to a named constant would hide the assertion altitude. No Slice-6-blocking duplication.
- Existing tests still pass: `uv run pytest tests/orders/ tests/filters/ tests/types/ tests/base/ --no-cov` — **615 passed, 2 skipped, 1 warning in 4.55s**. The warning is a pre-existing strawberry extension-passing DeprecationWarning at `tests/types/test_generic_foreign_key.py:100`, unrelated to Slice 6. No `--cov*` flags used.
- Spec reconciliation: **Option A — accept both drifts, no spec body edit.** Worker 2's two plan-vs-implementation drifts (filter-input dict shape `{"title": ["exact"]}` + scalar value; `LazyRelatedClassMixin` MRO check repointed from `BookFilter`/`BookOrder` to `RelatedFilter`/`RelatedOrder`) are both mechanically grounded in shipped source code. Worker 3's review explicitly verified both grounded shapes against `django_strawberry_framework/filters/sets.py::FilterSet._normalize_input` (the dict-with-nested-dict-value branch routes through `_operator_bag_items` which only accepts `__dataclass_fields__`-bearing objects) and `django_strawberry_framework/filters/base.py::RelatedFilter` + `django_strawberry_framework/orders/base.py::RelatedOrder` (both inherit `LazyRelatedClassMixin` directly via sibling-import from `..sets_mixins`; the `*Set` parents inherit only `ClassBasedTypeNameMixin`). Per the "Do NOT amend prior artifact sections" rule (BUILD.md "Re-pass sections" + this prompt's framing), Worker 1 does NOT edit the Plan section retroactively; the drift acceptance is recorded under `### Spec changes made (Worker 1 only)` below. The spec body's Slice 6 sub-bullet (lines 138-139) and the test-plan section (lines 1014-1022) are content-correct against what shipped (both describe a `DjangoType` with both Meta keys; both name the WHERE/ORDER BY composition assertion; both name the shared `LazyRelatedClassMixin` no-leak assertion); no spec body edit required.
- Final status: `final-accepted`.

### Summary

Slice 6 ships exactly one new test file at `tests/orders/test_composition.py` (replaces the 10-line TODO pseudocode stub seeded at Slice 1). The file carries two test functions plus one autouse `_isolate_registry` fixture: `test_filter_and_order_compose_through_finalizer_and_apply_pipelines` pins the four-altitude package-internal composition contract (both per-module input-class namespaces materialize the `<X>FilterInputType` + `<X>OrderInputType` classes, both `_materialized_names` ledgers + both `*ArgumentsFactory.input_object_types` carry the names, both inputs modules' `module.__dict__` lookup paths find the classes, and the `BookFilter.apply_sync(...) → BookOrder.apply_sync(...)` chain produces a queryset whose `query.where` is non-empty + `query.order_by` references the `title` column + the `CaptureQueriesContext`-captured SQL carries `WHERE` strictly before `ORDER BY`); `test_filter_and_order_share_lazy_related_class_mixin_via_neutral_module` pins the Spec-Revision-4-H1 neutral-mixin sharing contract (`LazyRelatedClassMixin` is the same class object in both `RelatedFilter.__mro__` and `RelatedOrder.__mro__`, not a sibling-copy duplicate). The Slice-6 conditional clause from the Filtering spec's Slice checklist ("carried by sibling") is satisfied by this card carrying the composition test directly; no other source / docs / KANBAN / CHANGELOG / spec-body changes shipped in Slice 6.

### Spec changes made (Worker 1 only)

- **`docs/spec-028-orders-0_0_8.md` line 4 (status header) — rolled forward post-Slice-6.** Was: "Slices 1 (Foundation) and 2 (Factories) shipped (2026-06-01); Slices 3 (Wiring), 4 (Live HTTP coverage), and 5 (Docs + KANBAN + CHANGELOG) shipped (2026-06-02); Slice 6 still pending. ..." Now: "Slices 1 (Foundation) and 2 (Factories) shipped (2026-06-01); Slices 3 (Wiring), 4 (Live HTTP coverage), 5 (Docs + KANBAN + CHANGELOG), and 6 (Cross-card composition smoke) shipped (2026-06-02); cross-slice integration pass + final test-run gate pending. ... ; [`tests/orders/test_composition.py`][test-orders-composition] carries the package-internal cross-card filter+order composition smoke test (two test functions per Slice 6 plan-licensed split). ..." Per the spec status-line re-verification rule (worker-1.md "Spec status-line re-verification (every Worker 1 spawn)"): the status header described Slice 6 as "still pending" before this pass; post-Slice-6-final-acceptance reality is Slices 1-6 shipped with the integration + final gates pending. Single status-line edit; no other spec body changes.
- **Worker 2's two plan-vs-implementation drifts accepted (no spec body edit).** (1) Filter-input dict shape: Plan Step 2 pinned `fields = {"title": ["exact", "icontains"]}` + bag-dataclass input shape; build observed that the dict-shape input with a nested-dict value falls through `FilterSet._normalize_input::_operator_bag_items` (which only accepts `__dataclass_fields__`-bearing objects) into the silent-no-op scalar branch. Worker 2 swapped to `fields = {"title": ["exact"]}` + flat scalar value `{"title": "Foundation"}` so django-filter's `CharFilter` form emits the intended `WHERE title = 'Foundation'` clause. Same composition contract honored at the same altitude; spec line 138-139's "queryset whose SQL carries `WHERE <filter>` AND `ORDER BY <order>` clauses in the expected order" wording is honored verbatim. (2) `LazyRelatedClassMixin` MRO assertion: Plan Step 2 Assertion 3 sketched the check on `BookFilter.__mro__` / `BookOrder.__mro__`; build observed that `LazyRelatedClassMixin` is mixed into the `Related*` primitives (per `django_strawberry_framework/filters/base.py::RelatedFilter` and `django_strawberry_framework/orders/base.py::RelatedOrder` direct sibling-imports from `..sets_mixins`), NOT into the `*Set` parents themselves. Worker 2 repointed to `RelatedFilter.__mro__` / `RelatedOrder.__mro__` + a class-identity check `filter_mixin is order_mixin is LazyRelatedClassMixin` that is STRONGER than the Plan's bare MRO-membership check (catches a future sibling-copy refactor that would still satisfy MRO membership). Spec test-plan line 1020's "shared `LazyRelatedClassMixin` resolution works correctly when called from both subsystems in the same finalize pass" wording is honored at the correct MRO level. Both drifts are mechanically-corrected expressions of the same Plan-stated contract; per the "Do NOT amend prior artifact sections" append-only rule, the drift acceptance is recorded here rather than via a retroactive Plan-section edit. No spec body edit required.

<!-- LINK DEFINITIONS -->

<!-- Root -->

<!-- docs/ -->

<!-- docs/SPECS/ -->

<!-- docs/builder/ -->

<!-- django_strawberry_framework/ -->

<!-- tests/ -->
[test-orders-composition]: ../../tests/orders/test_composition.py

<!-- examples/ -->

<!-- scripts/ -->

<!-- .venv/ -->

<!-- External -->
