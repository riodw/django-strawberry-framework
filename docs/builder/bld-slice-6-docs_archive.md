# Build: Slice 6 — Docs, KANBAN, CHANGELOG, archive

Spec reference: `docs/SPECS/spec-deferred_scalars.md` (archived path after Worker 1 final-verification `git mv`; pre-archive line numbers — verbatim DONE-013 body at lines 207-228; verbatim TODO-045 body at lines 232-322; archival pointer at line 330; CHANGELOG `Notes` text at lines 327-328. After Worker 1's strip, the archived file no longer carries the inline TODO-045 body — those line numbers describe the spec state at the time of Slices 1-6 planning; for live follow-up state see `KANBAN.md`'s `TODO-ALPHA-045-0.0.7` card.)
Status: final-accepted

## Plan (Worker 1)

### DRY analysis

This slice is prose + KANBAN + CHANGELOG + spec-archive lifecycle work. No `.py` source changes. The DRY questions are:

- **Existing patterns reused.** The spec is the single source of truth for every verbatim drop-in. Three load-bearing verbatim blocks already live in the spec:
  - The `BigInt` scalar entry text — `docs/spec-deferred_scalars.md:200`.
  - The DONE-013-0.0.6 KANBAN body — `docs/spec-deferred_scalars.md:207-228` (fenced ```` ```markdown ```` block).
  - The TODO-ALPHA-045-0.0.7 KANBAN body — `docs/spec-deferred_scalars.md:231-322` (fenced ```` ````markdown ```` four-backtick outer fence so the inner ```` ```python ```` Python snippet renders correctly per revision-10 H1 fix).
  Worker 2 copies these blocks character-for-character; the spec's prose is the reference, not Worker 2's recall. The `CHANGELOG.md` `Notes` text (spec lines 327-328) is also a verbatim drop-in.
- **New helpers justified.** None. No code logic. No new modules. No new helpers. No new repeated literal that warrants extraction (every site is a one-off prose edit). The KANBAN `TODO-ALPHA-045-0.0.7` card body uses a unique four-backtick outer fence — the only nested-fence site in the file — so it does not interact with anything else in `KANBAN.md`.
- **Duplication risk avoided.** The principal duplication risk is divergence between the inline `TODO-ALPHA-045-0.0.7` body in the *archived* spec and the live body in `KANBAN.md` once the follow-up card evolves. The spec's revision-9 M2 fix already mitigates this by mandating that Slice 6 strip the inline body from the spec and replace it with a one-line `See KANBAN.md` pointer before archiving (spec line 330). The plan honors that mitigation as a Worker 1 final-verification step (see Implementation steps below — Worker 2 does **not** touch the spec). The DONE-013-0.0.6 inline body stays as a historical record per spec line 332.

Cross-slice DRY watchpoints inherited from the prior consolidated memory entry (Slices 1-4 build): six candidates flagged for the integration pass. None of them are mutated by Slice 6 (Slice 6 touches zero `.py` source under `django_strawberry_framework/`), so the integration-pass scan stays evaluable after Slice 6 lands.

### Implementation steps

Worker 2 implements all seven non-archive edits below. Worker 2 does **not** touch the active spec (`docs/spec-deferred_scalars.md`) — neither the inline-body strip nor the file move. Per `docs/builder/BUILD.md` "Spec lifecycle and archive work": "Worker 2 does not move or edit the active spec." The inline-body strip is content-level but still an active-spec edit, so it belongs to Worker 1 final verification together with the lifecycle move. See `### Implementation discretion items` for the explicit Worker 1 / Worker 2 split.

Line numbers below are pin-at-write-time navigational hints; verify against the current source before editing.

1. **`README.md` — version line bump (`README.md:45`).** Change `**\`0.0.5\`, single-maintainer, alpha-quality.**` → `**\`0.0.6\`, single-maintainer, alpha-quality.**`. Pin: the surrounding sentence (`Fine for internal tools and prototypes; not production. The public names are stable; correctness and edge-case behavior are still hardening.`) stays unchanged.

2. **`docs/README.md` — three edits.**
   - **Edit 2a (`docs/README.md:89`):** `**Shipped today** (\`0.0.5\`):` → `**Shipped today** (\`0.0.6\`):`. (The `0.0.5` shipping note moves to `0.0.6` — same line, same shape.)
   - **Edit 2b (`docs/README.md:101+110`):** Remove the `- specialized scalar conversions (\`BigIntegerField\`, \`JSONField\`, \`ArrayField\`, \`HStoreField\`)` bullet from the "Coming in `0.1.0`" callout (currently line 110). The spec instruction (line 196) writes "out of the 'Coming in `0.1.0`' callouts at `docs/README.md:101` and `docs/README.md:112`" — those two anchor numbers point to the heading callouts that bracket the bullet (line 101 opens "Coming in `0.1.0`"; line 112 opens "Coming in `1.0.0`"). The bullet itself is between them at line 110.
   - **Edit 2c (`docs/README.md:90-99` shipped-today block):** Add a new bullet to the shipped-today block reflecting the now-shipped specialized scalar conversions. Worker 2 discretion: place it after the existing scalar-conversion bullet (line 91) to read coherently as a follow-up to the base scalar-conversion line; copy phrasing from the spec's [Specialized scalar conversions](FEATURES.md#specialized-scalar-conversions) shipped form — suggested wording: `- specialized scalar conversions (\`BigIntegerField\` / \`PositiveBigIntegerField\` → \`BigInt\`, \`JSONField\` → \`JSON\`, PostgreSQL \`ArrayField\` → \`list[T]\`, PostgreSQL \`HStoreField\` → \`JSON\`)`. The exact phrasing is not pinned by the spec for this site; coherence with the docs/FEATURES.md shipped entry is the bar.

3. **`docs/TREE.md` — two edits.**
   - **Edit 3a (`docs/TREE.md:203` area — current package layout):** Add a `│   ├── scalars.py             # BigInt scalar (NewType-based; suppressed Strawberry deprecation)` line near `converters.py` in the **current** package layout (the first code-fenced tree near line 194-222). Worker 2 discretion: the exact one-line description text is at Worker 2's discretion (must mention `BigInt` + the suppressed-deprecation reality); placement within the `django_strawberry_framework/` tree is at the top level (alongside `__init__.py`, `conf.py`, `exceptions.py`, `registry.py`) — *not* inside `types/`. `scalars.py` is a top-level module under `django_strawberry_framework/`, per Slice 1's `Notes for Worker 1`.
   - **Edit 3b (`docs/TREE.md:245` area — target package layout):** Add the same `scalars.py` line near the target-layout tree's top-level area (the code-fenced tree starting near line 230). Same placement rule: top level, not under `types/`.

4. **`docs/FEATURES.md` — five edits.**
   - **Edit 4a (Specialized scalar conversions section, currently at `docs/FEATURES.md:901-912`):** Change `**Status:** planned for \`0.0.6\`.` → `**Status:** shipped (\`0.0.6\`).`. **Replace the existing line at `docs/FEATURES.md:910`** (currently reads `- PostgreSQL \`HStoreField\` → \`dict[str, str | None]\` (soft-registered, only when \`django.contrib.postgres\` is installed)`) **with** `- PostgreSQL \`HStoreField\` → \`strawberry.scalars.JSON\` (soft-registered, only when \`django.contrib.postgres.fields\` imports successfully)`. **Also update the `PositiveBigIntegerField` bullet to read `→ BigInt`** — locate the existing line at `docs/FEATURES.md:907` (currently reads `- \`BigIntegerField\` → JSON-safe [\`BigInt\`](#bigint-scalar) scalar (string-serialized at the wire to survive JavaScript's 53-bit integer limit)`) and add a sibling bullet (or modify the existing one if Worker 2 reads it as covering both) to also surface `PositiveBigIntegerField → BigInt`. Worker 2 discretion: either a new bullet (`- \`PositiveBigIntegerField\` → JSON-safe [\`BigInt\`](#bigint-scalar) scalar`) or extending the existing `BigIntegerField` bullet to name both Django source fields explicitly. The shipped `PositiveBigIntegerField` switch is a breaking change per Slice 1 + CHANGELOG; the FEATURES entry should make both source fields discoverable.
   - **Edit 4b (`BigInt` scalar section, currently at `docs/FEATURES.md:161-167`):** Change `**Status:** planned for \`0.0.6\`.` → `**Status:** shipped (\`0.0.6\`).`. **Replace the entry body** (currently the one paragraph at line 165) **with the verbatim spec text from spec line 200**: `JSON-safe scalar typically used to map Django's 64-bit integer fields \`BigIntegerField\` and \`PositiveBigIntegerField\` (not \`BigAutoField\`). Technically arbitrary-precision: serialized via Python \`str(int_value)\`, which handles any \`int\`. Wire format is a decimal string to survive GraphQL's signed 32-bit \`Int\` boundary (executing a query returning an \`int\`-annotated value past \`2**31 - 1\` raises a \`GraphQLError\` with message containing \`Int cannot represent non 32-bit signed integer value\`). Strict parser accepts Python \`int\` (excluding \`bool\`) and strings matching \`^(0|-?[1-9][0-9]*)$\` — plain ASCII decimal, optional leading minus for non-zero, no leading zeroes (except \`"0"\` itself), no underscores, no plus sign, no Unicode digits. Strict serializer rejects \`bool\`, \`float\`, \`str\`, \`Decimal\`, and any non-\`int\` type with \`TypeError\`. Part of [Specialized scalar conversions](#specialized-scalar-conversions).` Keep the existing `**See also:**` footer.
   - **Edit 4c (Scalar field conversion section, currently at `docs/FEATURES.md:843-867`):** Add the new field-type bullets to the bulleted "Shipped scalar support:" list at `docs/FEATURES.md:847-859`. Worker 2 discretion on exact wording and placement; the list already has bullets for the prior scalars and the new bullets must name `BigIntegerField → BigInt`, `JSONField → JSON`, `ArrayField → list[T]`, `HStoreField → JSON`, and clarify the `PositiveBigIntegerField → BigInt` change (line 850 currently lists `PositiveIntegerField` under the int bucket but does **not** name `PositiveBigIntegerField`; Slice 1 + CHANGELOG mark the switch as breaking). The "Deferred mappings" line at `docs/FEATURES.md:865` must be **removed entirely** (the four fields it names are no longer deferred). Worker 2 discretion: also tighten the "Subclass MRO walk" paragraph if needed to stay consistent with the new bullets, but no rewording is mandated.
   - **Edit 4d (Index section at `docs/FEATURES.md:33-105`):** Update two status badges:
     - Line 42: `| [\`BigInt\` scalar](#bigint-scalar) | planned for \`0.0.6\` |` → `| [\`BigInt\` scalar](#bigint-scalar) | shipped (\`0.0.6\`) |`.
     - Line 102: `| [Specialized scalar conversions](#specialized-scalar-conversions) | planned for \`0.0.6\` |` → `| [Specialized scalar conversions](#specialized-scalar-conversions) | shipped (\`0.0.6\`) |`.
   - **Edit 4e (Public exports section at `docs/FEATURES.md:22-31`):** Add a `- [\`BigInt\`](#bigint-scalar) — JSON-safe scalar for 64-bit integer fields.` bullet to the bulleted "Symbols re-exported from `django_strawberry_framework`:" list. Worker 2 discretion: place after `auto` (line 30) or alphabetically — `BigInt` slots near the top alphabetically. Also add a one-line **note** under the bulleted list (a `_Note:_ ...` line, not a new bullet) reflecting the spec's pin (line 203): `The import path is now clean — no Strawberry deprecation warning escapes (the deprecation is suppressed at the definition site in \`scalars.py\`).`

5. **`TODAY.md` — expand "What fakeshop model fields work today" with the four new scalars.** The "Scalar conversions:" bulleted list at `TODAY.md:147-159` lists the current Django field → Python type mappings. Add new bullets covering: `BigIntegerField` / `PositiveBigIntegerField` → `BigInt`, `JSONField` → `strawberry.scalars.JSON`, PostgreSQL `ArrayField` → `list[T]` (recursive through `field.base_field`), PostgreSQL `HStoreField` → `strawberry.scalars.JSON` (soft-registered, only when `django.contrib.postgres.fields` imports successfully). Worker 2 discretion: exact placement within the list and word-for-word phrasing; coherence with the FEATURES.md shipped list (Edit 4c) is the bar.

6. **`KANBAN.md` — two card-level edits.**
   - **Edit 6a (move `TODO-ALPHA-013-0.0.6` → `DONE-013-0.0.6`):** Cut the `### TODO-ALPHA-013-0.0.6 — Deferred scalar conversions` card body at `KANBAN.md:87-118` (everything from the heading through the `Files likely touched:` block ending before the next `### TODO-ALPHA-014-0.0.6` card at line 120). Paste a **new** DONE-013-0.0.6 card at the end of the `## Done` section (after the current last DONE card `DONE-012-0.0.6` ending at `KANBAN.md:1597`) using the **verbatim spec body at spec lines 208-227**, repeated here in canonical form:

     ```markdown
     ### DONE-013-0.0.6 — Deferred scalar conversions

     Slice-by-slice scope (per `docs/SPECS/spec-deferred_scalars.md`):

     - Public `BigInt` scalar (`django_strawberry_framework/scalars.py`, `NewType`-based) with the Strawberry class-direct-to-`scalar()` `DeprecationWarning` suppressed at the definition site so consumers see no warning at import time.
     - Strict `BigInt` parser via regex `^(0|-?[1-9][0-9]*)$` — rejects `bool`, `float`, empty / whitespace-padded strings, non-decimal strings, underscores, plus signs, leading zeroes, `-0`, and Unicode digits.
     - Strict `BigInt` serializer — rejects `bool`, `float`, `str`, `Decimal`, and any non-`int` type with `TypeError`.
     - `BigIntegerField → BigInt` and `PositiveBigIntegerField → BigInt` in `SCALAR_MAP`. `BigAutoField` preserved as `int` (no current-day override recourse; wait for TODO-ALPHA-015).
     - `JSONField → strawberry.scalars.JSON` in `SCALAR_MAP`.
     - `ArrayField` and `HStoreField` mapped via sentinel-guarded branches in `convert_scalar`. `HStoreField` not added to `SCALAR_MAP`.
     - `ArrayField` rejects nested arrays and outer `choices` with `ConfigurationError`.
     - `SCALAR_MAP`'s declared value type widened from `dict[type[models.Field], type]` to `dict[type[models.Field], Any]`.
     - `BigInt` added to `django_strawberry_framework.__all__`; `tests/base/test_init.py`'s pinned `__all__` and `__version__` assertions updated.
     - Atomic version-bump quintet: `pyproject.toml`, `__init__.py`, `tests/base/test_init.py`, `docs/FEATURES.md` package-version line, `uv.lock`.
     - 100% coverage via `tests/test_scalars.py` (new flat file) and `tests/types/test_converters.py` (extended). Includes a `test_package_import_does_not_emit_strawberry_deprecation_warning` guard so future regressions to the suppression are explicit.
     - Docs: `docs/FEATURES.md`, `docs/README.md`, `README.md`, `docs/TREE.md`, `TODAY.md`, `CHANGELOG.md`.

     Design notes carried into `0.0.6`:

     - The internal Strawberry deprecation about passing a class (or `NewType`) to `strawberry.scalar(...)` is suppressed at the definition site (tight `warnings.catch_warnings()` filter). The package import surface is therefore clean. Migration to a `StrawberryConfig.scalar_map`-based design is roadmapped as `TODO-ALPHA-045-0.0.7` — that path is a real public-API change (consumers using `BigInt` directly will merge a package-provided `StrawberryConfig` into their `strawberry.Schema(...)`), not an internal-only refactor.
     ```

     Note the spec body references the archived path `docs/SPECS/spec-deferred_scalars.md` — this is the path the spec will live at after Worker 1's final-verification archival step (step 9 below), so the DONE card body is forward-correct.

   - **Edit 6b (add `TODO-ALPHA-045-0.0.7`):** Insert a **new** `### TODO-ALPHA-045-0.0.7 — Warning-free scalar registration via \`StrawberryConfig.scalar_map\`` card in the `## To Do - Alpha (0.1.0)` column, **after** `TODO-ALPHA-019-0.0.7 — Multi-database cooperation contract` (currently ending at `KANBAN.md:303`) and **before** the next card `TODO-ALPHA-020-0.0.8 — Filtering subsystem` (currently at `KANBAN.md:305`). The card body is the verbatim spec block at spec lines 232-321 (the fenced ````markdown`...```` four-backtick outer fence wraps the body; Worker 2 copies the *body inside* the four-backtick fence — the four-backtick fence itself is the spec's wrapper and does **not** survive into the live `KANBAN.md`, which uses regular `### ` headings and ` ```python ` snippets directly).

     **Critical:** Worker 2 must verify after pasting that the inner ` ```python ` code block (currently at spec lines 252-261) renders as a regular Python fenced code block in `KANBAN.md` — i.e., it sits inside the card body as a normal `python`-tagged fence, not nested inside another fence. The four-backtick outer fence exists only in the spec to let the inner triple-backtick render *within the spec's verbatim block*; in the live `KANBAN.md` it's just a card body containing one ordinary fenced Python snippet. If Worker 2 pastes the literal four-backtick wrapper into `KANBAN.md`, the card will render incorrectly. **Worker 2 should strip the outermost four-backtick fence and the trailing `````` closer** before pasting; everything else (heading, prose, `python`-fenced snippet, bullets, open design questions) is verbatim.

     **NNN selection rationale:** spec line 229 mandates "append at NNN 045". Current max NNN in `KANBAN.md` is `TODO-STABLE-044-1.0.0` (verified at `KANBAN.md:1025`). The next available is **045**. Insertion position: the card is for `0.0.7`, so it belongs in the 0.0.7 cluster (currently `TODO-ALPHA-016-0.0.7` through `TODO-ALPHA-019-0.0.7` at `KANBAN.md:206-303`) — appended at the end of that cluster, immediately after `TODO-ALPHA-019-0.0.7` and before the first `0.0.8` card. Do NOT renumber any existing card per spec line 229 and rev-9 M1.

7. **`CHANGELOG.md` — `[Unreleased]` section edits.** Permission granted by spec lines 324-325 ("permission granted by this spec, overriding [`AGENTS.md`](../AGENTS.md)'s default prohibition"). The current `[Unreleased]` block at `CHANGELOG.md:19-21` has one `### Changed` entry (DONE-012). Slice 6 adds three sub-sections under the same `[Unreleased]` heading. Worker 2 must not promote `[Unreleased]` to `[0.0.6]` — that's a release-time maintainer action gated on PyPI publish per spec [Definition of done](#definition-of-done). The shape:
   - **`### Added` (new sub-section):**
     - `BigInt` public scalar export (JSON-safe; string-serialized; strict parser and strict serializer). See [`BigInt` scalar](docs/FEATURES.md#bigint-scalar). Tracked as `DONE-013-0.0.6` in [`KANBAN.md`](KANBAN.md).
     - `JSONField → strawberry.scalars.JSON` mapping in `SCALAR_MAP`.
     - `HStoreField → strawberry.scalars.JSON` mapping via sentinel-guarded branch in `convert_scalar` (soft-registered, only when `django.contrib.postgres.fields` imports successfully).
     - `ArrayField` recursion through `field.base_field` via sentinel-guarded branch in `convert_scalar`. Nested `ArrayField` and outer `choices` on `ArrayField` / `HStoreField` rejected with `ConfigurationError`.
   - **`### Changed` (extend the existing sub-section at `CHANGELOG.md:20`):** Add a new bullet to the existing `### Changed` block:
     - `PositiveBigIntegerField` mapping switched from `int` to [`BigInt`](docs/FEATURES.md#bigint-scalar). Breaking wire-format change: `PositiveBigIntegerField` values are now serialized as decimal strings on the wire (not JSON integers) to survive GraphQL's signed 32-bit `Int` boundary. Consumers using the existing 32-bit `int` shape must update wire-format expectations.
   - **`### Notes` (new sub-section, verbatim from spec lines 327-328):**
     - `The internal `BigInt` scalar definition uses `strawberry.scalar(NewType, ...)`, which Strawberry deprecates in favor of `StrawberryConfig.scalar_map`. The deprecation warning is suppressed at the definition site so the package import remains clean. Migration to a `scalar_map`-based design is tracked as a follow-up and will be a real public-API change for consumers using `BigInt` directly.`

   Worker 2 discretion: the exact wording of the `### Added` bullets (and the `### Changed` bullet) is at Worker 2's discretion provided the four shipped behaviors are listed and the breaking `PositiveBigIntegerField` switch is named as breaking. The `### Notes` bullet is **verbatim** from spec lines 327-328 — no paraphrasing.

8. **(Worker 1 final verification, not Worker 2.)** Strip the inline `TODO-ALPHA-045-0.0.7` card body from `docs/spec-deferred_scalars.md`'s Slice 6 fenced markdown block (the four-backtick fence at spec lines 231-322) AND replace it with the one-line pointer text from spec line 330: `> "See \`KANBAN.md\` for the current \`TODO-ALPHA-045-0.0.7\` card body — the verbatim drop-in below was used for the initial card creation in this commit and may be out of date once the follow-up spec evolves the card."`. The inline `DONE-013-0.0.6` body at spec lines 207-228 stays (historical record per spec line 332).

9. **(Worker 1 final verification, not Worker 2.)** Archive the active spec by **moving** the file `docs/spec-deferred_scalars.md` → `docs/SPECS/spec-deferred_scalars.md` (using `git mv` so the rename is preserved for the maintainer's commit). `docs/SPECS/` exists already (verified — currently contains `spec-relay_interfaces.md`). Record both steps 8 and 9 under `### Spec changes made (Worker 1 only)` in the artifact's final-verification section, with old / new paths.

After steps 1-7 ship and Worker 3 accepts, Worker 1 runs the mechanical strip-then-move (steps 8 + 9) during final verification, updates the artifact's `Spec reference:` line if needed (the line currently cites `docs/spec-deferred_scalars.md` line numbers — those line numbers stay valid against the *archived* path after the move; the spec reference should also be updated to cite `docs/SPECS/spec-deferred_scalars.md` to point at the archived path), and sets `Status: final-accepted`.

### Test additions / updates

**None.** Slice 6 ships zero new test code. The shipped scalar behavior is already pinned by tests added in Slices 1-4 (`tests/test_scalars.py`, `tests/types/test_converters.py`) — those tests still pass under Slice 5's version bump (verified at Slice 5 final-verification: 79 passed + 2 expected skips). Slice 6 changes prose, KANBAN, CHANGELOG, and (post-archival) the spec path; none of those have a Python test surface.

The version-pinning tests in `tests/base/test_init.py` (`test_version`, `test_public_api_surface_is_pinned`) still apply and stay green — Slice 5 updated them to `0.0.6` and pinned `BigInt` in `__all__`; Slice 6 does not change either.

Temp-test opportunities for Worker 3: **none expected**. The CHANGELOG sanity sub-checklist (per BUILD.md "CHANGELOG sanity") and the documentation/release sanity sub-checklist (per BUILD.md "Documentation / release sanity") both run as reading exercises against the diff — no temp-test harness needed.

### Implementation discretion items

The following items are at Worker 2's discretion (planner has assessed and decided the choice is equivalent between two-or-more valid shapes); structural decisions are NOT delegated here:

- **Exact wording of new `docs/README.md` and `TODAY.md` shipped-scalar bullets** (steps 2c and 5). The spec does not pin wording for these sites; coherence with the FEATURES.md shipped entry (step 4a/4c) is the bar. Pick phrasing that reads naturally in each file's surrounding bullet style.
- **Exact wording of the `docs/TREE.md` `scalars.py` one-line description** (step 3a/3b). Must name `BigInt` and mention the suppressed Strawberry deprecation; placement is top-level under `django_strawberry_framework/`.
- **Exact placement of the new `BigInt` bullet in `docs/FEATURES.md` Public exports list** (step 4e). Alphabetical (slots near top) or appended after `auto` — both read fine. The note line after the bulleted list is non-negotiable.
- **Exact wording of the `CHANGELOG.md [Unreleased] ### Added` bullets and the breaking-change `### Changed` bullet** (step 7). The shipped behaviors and the breaking-change framing are pinned; the prose is at Worker 2's discretion. The `### Notes` block is verbatim — no paraphrasing.
- **`docs/FEATURES.md` Scalar field conversion edit shape** (step 4c). Worker 2 chooses whether to extend existing bullets or add new ones, and whether to tighten the surrounding "Subclass MRO walk" paragraph. The required end state: four new scalar mappings discoverable from the list, the deferred-mappings line at the section's bottom removed, and the `PositiveBigIntegerField` switch named.

**Worker 1 / Worker 2 split for the active spec (non-negotiable, NOT discretion):**

- **Worker 2 does NOT touch `docs/spec-deferred_scalars.md`** — neither the inline-card-body strip (spec line 328 / spec content edit) nor the file-move archival (lifecycle). Both belong to Worker 1 final verification per BUILD.md "Spec lifecycle and archive work": "Worker 2 implements the durable docs, KANBAN, changelog, and release-file edits named by the plan; Worker 2 does not move or edit the active spec." The inline-strip is content-level but still an active-spec edit, so it stays with Worker 1 along with the lifecycle move (and lets the move-and-strip ship atomically).
- **Worker 1 (final verification) performs both the strip (step 8) and the move (step 9)** in one pass. The order is strip-then-move: edit the live `docs/spec-deferred_scalars.md` to replace the inline TODO-045 body with the one-line pointer, then `git mv` the file to `docs/SPECS/spec-deferred_scalars.md`. Record both under `### Spec changes made (Worker 1 only)`.
- **Worker 3 review** does not reject Slice 6 for the missing strip-and-move; per BUILD.md, "Worker 3 still reviews the durable docs and verifies the plan assigned the archive step." Worker 3 should confirm the plan names step 8 and step 9 as Worker 1-owned (this section does).

**Slice 6a/6b split (fallback, not default):** spec line 194 offers a fallback split if reviewer PR feedback flags commit size: 6a (shipped-state propagation: DONE-013 body + `docs/FEATURES.md` + `docs/README.md` + root `README.md` + `docs/TREE.md` + `TODAY.md` + `CHANGELOG.md` + spec archive) and 6b (forward-look: TODO-045 card creation). Plan around a single commit by default. If Worker 3 review surfaces "this commit is too large for sensible review," Worker 1 may invoke the split as a spec edit under final-verification's `Spec changes made (Worker 1 only)` section and ask Worker 0 to regenerate the build plan's checklist with `Slice 6a` and `Slice 6b` rows in declared order.

**Static inspection helper:** not required for Slice 6 — no `.py` file changes. Skip with reason.

---

## Build report (Worker 2)

### Files touched

- `README.md` — line 45 version bump `**\`0.0.5\`, single-maintainer, alpha-quality.**` → `**\`0.0.6\`, single-maintainer, alpha-quality.**`. Surrounding sentence preserved.
- `docs/README.md` — three edits per the plan: (a) `**Shipped today** (\`0.0.5\`):` → `**Shipped today** (\`0.0.6\`):`; (b) new shipped-today bullet added immediately after the base scalar-conversion line listing the four new specialized scalar conversions (`BigIntegerField` / `PositiveBigIntegerField` → `BigInt`, `JSONField` → `JSON`, PostgreSQL `ArrayField` → `list[T]`, PostgreSQL `HStoreField` → `JSON`); (c) removed the `specialized scalar conversions (...)` bullet from the "Coming in `0.1.0`" callout.
- `docs/TREE.md` — two edits: added `scalars.py` line at the top level under `django_strawberry_framework/` in both the current package layout (after `registry.py`) and the target package layout (after `registry.py`). Also updated the current-layout `__init__.py` comment to include `BigInt` in the listed re-exports (the comment otherwise drifts against the public surface bumped in Slice 1).
- `docs/FEATURES.md` — five edits: (4a) `Specialized scalar conversions` status flipped to `shipped (\`0.0.6\`)`, HStore bullet rewritten to `strawberry.scalars.JSON (soft-registered, only when django.contrib.postgres.fields imports successfully)`, new sibling bullet added for `PositiveBigIntegerField → BigInt`; (4b) `BigInt` scalar status flipped to `shipped (\`0.0.6\`)` and entry body replaced with the verbatim spec text from spec line 200; (4c) `Scalar field conversion` shipped-list extended with `BigIntegerField` / `PositiveBigIntegerField` → `BigInt`, `JSONField` → `JSON`, PostgreSQL `ArrayField`, PostgreSQL `HStoreField` bullets, plus the deferred-mappings line removed; (4d) Index status badges flipped for `BigInt` scalar and Specialized scalar conversions; (4e) `BigInt` added to Public exports with the clean-import-path note as a `_Note:_ ...` line under the bulleted list.
- `TODAY.md` — Scalar-conversions block expanded with four new bullets (`BigIntegerField`/`PositiveBigIntegerField` → `BigInt` named as a breaking switch, `JSONField` → `strawberry.scalars.JSON`, PostgreSQL `ArrayField` → `list[T]` with the soft-registered note, PostgreSQL `HStoreField` → `strawberry.scalars.JSON` with the soft-registered note).
- `KANBAN.md` — two card-level edits: (6a) the `TODO-ALPHA-013-0.0.6` card body removed from the `To Do - Alpha (0.1.0)` column; a new `DONE-013-0.0.6` card appended to the `Done` section after `DONE-012-0.0.6` and immediately before `## Release readiness checklist`, using the verbatim spec body at spec lines 208-227; (6b) a new `TODO-ALPHA-045-0.0.7 — Warning-free scalar registration via \`StrawberryConfig.scalar_map\`` card inserted in the `To Do - Alpha (0.1.0)` column after `TODO-ALPHA-019-0.0.7` (and its `Out of scope:` block) and before `TODO-ALPHA-020-0.0.8`. Body verbatim from spec lines 232-321 with the spec's four-space indentation (inside the four-backtick fence wrapper) stripped and the outer four-backtick fence dropped — so the inner ` ```python ` block survives as a regular fenced code block in the live KANBAN card.
- `CHANGELOG.md` — `[Unreleased]` section now carries a new `### Added` sub-section (four bullets covering `BigInt`, `JSONField`, `HStoreField`, `ArrayField`), the existing `### Changed` sub-section extended with the `PositiveBigIntegerField` breaking-switch bullet, and a new `### Notes` sub-section carrying the verbatim spec text from spec lines 327-328.

### Tests added or updated

None — Slice 6 is documentation/release work; the existing test suite continues to apply. The version-pinning tests in `tests/base/test_init.py` (`test_version`, `test_public_api_surface_is_pinned`) were updated to `0.0.6` and pinned `BigInt` in `__all__` during Slice 5; Slice 6 does not modify them.

### Validation run

- `uv run ruff format .` — pass (99 files left unchanged; the standard COM812-vs-formatter warning prints but is the pre-existing project posture, not Slice-6-introduced).
- `uv run ruff check --fix .` — pass (`All checks passed!`).
- No focused `pytest` run; the artifact did not require one (Slice 6 ships zero new Python code and the plan's `Test additions / updates` block records `None`).

### Implementation notes

- **`docs/README.md` shipped-today bullet placement (step 2c).** Placed immediately after the existing base scalar-conversion bullet so the new specialized-scalars line reads as a follow-up to the same topic. Wording chosen to mirror the `docs/FEATURES.md` Specialized scalar conversions shipped form (`BigIntegerField` / `PositiveBigIntegerField` → `BigInt`, `JSONField` → `JSON`, PostgreSQL `ArrayField` → `list[T]`, PostgreSQL `HStoreField` → `JSON`) — coherence with the FEATURES.md shipped entry is the bar per the plan's discretion item.
- **`docs/TREE.md` `scalars.py` description text (step 3a/3b).** Chose `BigInt public scalar (NewType-based; Strawberry deprecation suppressed at definition site)` — names `BigInt` and mentions the suppressed deprecation per the plan's discretion item. The same wording lands in both the current and target layouts for symmetry.
- **`docs/TREE.md` current-layout `__init__.py` re-export comment.** The existing comment listed `DjangoType, DjangoOptimizerExtension, OptimizerHint, finalize_django_types, auto`; updated to insert `BigInt` between `OptimizerHint` and `finalize_django_types` so the comment matches the public surface bumped in Slice 1. The target-layout `__init__.py` line carries the shorter `public-API re-exports` comment, which does not enumerate symbols, so it needs no update. This edit is implied by Slice 1's public-API bump but the plan only names `scalars.py`; recording the additional touch here so Worker 3 can verify it.
- **`docs/FEATURES.md` Specialized-scalar-conversions `PositiveBigIntegerField` bullet shape (Edit 4a).** Added it as a separate sibling bullet rather than extending the `BigIntegerField` bullet — the section already lists each Django field type on its own line and the parallel reads cleaner. The breaking-change framing lives in the CHANGELOG and the Scalar field conversion section's bullet (Edit 4c); duplicating it on every site would clutter the prose.
- **`docs/FEATURES.md` Scalar field conversion bullets (Edit 4c).** Added separate bullets for `BigIntegerField` / `PositiveBigIntegerField` (named as a breaking switch on the same line), `JSONField`, PostgreSQL `ArrayField`, and PostgreSQL `HStoreField`. The `ArrayField` and `HStoreField` bullets call out the soft-registration condition (only when `django.contrib.postgres.fields` imports successfully) and the `ConfigurationError` rejections (nested `ArrayField`, outer `choices`) so the section is self-contained without forcing readers into Specialized scalar conversions. Removed the `Deferred mappings — ...` line at the section's bottom per the plan; the four fields it named are no longer deferred.
- **`docs/FEATURES.md` Public exports placement (Edit 4e).** Placed the new `BigInt` bullet at the top of the bulleted list (alphabetical: `B` comes before `D`, the next existing entry). The clean-import-path note lands as a single-paragraph `_Note:_` after the bulleted list — not inside the list — so it reads as a footnote-style annotation rather than an additional symbol entry.
- **`KANBAN.md` TODO-045 nested-fence handling.** The spec wraps the TODO-045 card body in a four-backtick fence (` ````markdown ` ... ` ```` `) so its inner ` ```python ` block survives in the spec. When pasting into the live `KANBAN.md`, only the four-backtick wrapper and the spec's 4-space block-indent get stripped; the inner ` ```python ` fence and its closing ` ``` ` are preserved verbatim. The card now renders as a regular `### `-headed card with one ordinary fenced Python snippet inside. Verified visually that the heading, prose, code block, bullets, and open design questions all sit at column 0 — no residual indentation, no nested fences.
- **`KANBAN.md` DONE-013 placement.** Appended after `DONE-012-0.0.6` and immediately before the `## Release readiness checklist` heading. The card body is verbatim from spec lines 208-227 (DONE heading + bullets + Design-notes paragraph). The body references the archived path `docs/SPECS/spec-deferred_scalars.md` — that path is forward-correct: Worker 1's final-verification move (step 9 in the plan) lands the spec at that path, so the link resolves after the move.
- **`CHANGELOG.md` `### Added` and `### Changed` bullet wording.** The four behaviors required by the plan (`BigInt` export, JSON mapping, HStore mapping, `ArrayField` recursion) are each named explicitly with their soft-registration / sentinel-guard / rejection semantics; the breaking-change framing on the `PositiveBigIntegerField` bullet names it as a wire-format change consumers must absorb. The `### Notes` block is verbatim from spec lines 327-328 with no paraphrasing.
- **Ordering of sub-sections inside `[Unreleased]`.** Chose `### Added` → `### Changed` → `### Notes` (Keep-a-Changelog ordering with `Notes` as a custom trailer per spec). The existing `### Changed` block was kept as the lead bullet under the new `### Changed` heading and the new `PositiveBigIntegerField` bullet appended after it — preserving DONE-012 attribution and reading chronologically.

### Notes for Worker 3

- **No `.py` source changes in this slice.** The slice is prose + KANBAN + CHANGELOG only; the helper-skip recorded in the plan applies (no `scripts/review_inspect.py` invocation).
- **`docs/TREE.md` extra touch.** I made one edit beyond the plan's strict text — added `BigInt` to the existing `__init__.py` re-exports comment in the current package layout (`docs/TREE.md` near line 195). The plan only named the `scalars.py` line addition; the `__init__.py` comment update is implied by Slice 1's public-API bump (Slice 5 / Slice 1 added `BigInt` to `__all__`). Recorded in `### Implementation notes` so the diff reviewer can verify. Reject this as out-of-scope if you read it as such; I judged it small enough to land inline and named it in the build report.
- **`KANBAN.md` insertion-point check.** TODO-ALPHA-045-0.0.7 is inserted between TODO-ALPHA-019-0.0.7's `Out of scope:` block and the start of TODO-ALPHA-020-0.0.8 — i.e., at the end of the 0.0.7 cluster, before the first 0.0.8 card. NNN sequence is 019 → 045 → 020 within that segment, which is non-contiguous but correct per spec line 229 ("Append at NNN 045 ... KANBAN groups by version, not by NNN").
- **CHANGELOG `### Notes` text.** Verbatim copy of spec lines 327-328. The single-quote characters and backtick-fenced inline code spans match the spec character-for-character. Cross-checked by inspection; no paraphrasing.
- **DONE-013 body.** Verbatim from spec lines 208-227. The four-space block-indent inside the spec's outer fence was stripped (so headings and bullets sit at column 0 in `KANBAN.md`). The body's reference to `docs/SPECS/spec-deferred_scalars.md` is forward-correct — Worker 1 will land the move during final verification.

### Notes for Worker 1 (spec reconciliation)

- **Spec was NOT edited by Worker 2.** Per the plan's `Implementation discretion items` (Worker 1 / Worker 2 split) and per `docs/builder/BUILD.md` "Spec lifecycle and archive work", the inline-card-body strip (plan step 8) and the `git mv` to `docs/SPECS/spec-deferred_scalars.md` (plan step 9) both belong to Worker 1's final-verification pass. The artifact's `Spec reference:` line and any other build-cycle citations of the live spec path should be updated to `docs/SPECS/spec-deferred_scalars.md` after the move; the spec's line numbers stay valid against the archived path.
- **No structural drift.** Every verbatim spec drop-in landed as-written (DONE-013 body, TODO-045 body, BigInt entry text, CHANGELOG Notes). No spec-text anchor needed adjustment; the line numbers cited in the plan all matched the current file state.
- **Small drift — minor extra edit.** Beyond the plan's seven steps, I updated the `docs/TREE.md` current-layout `__init__.py` comment to list `BigInt` alongside the other re-exports. The plan's step 3 named only the `scalars.py` line; the comment update is implied by Slice 1's public-API bump. If you read this as out-of-scope, the rollback is one-line. Recorded under `### Implementation notes` and `### Notes for Worker 3` so the diff reviewer can decide.

---

## Review (Worker 3)

### High:

None.

### Medium:

None.

### Low:

None.

### DRY findings

- No new code logic was introduced by Slice 6; the slice ships prose + KANBAN + CHANGELOG only. The principal cross-doc DRY consideration — the spec-pinned verbatim drop-ins (DONE-013 body, TODO-045 body, BigInt entry text, CHANGELOG `Notes`) — was honored character-for-character (verified by `diff <(sed -n '208,227p' spec | sed 's/^    //') <(sed -n '1657,1676p' KANBAN.md)` → zero diff for DONE-013; `diff <(sed -n '232,321p' spec | sed 's/^    //') <(sed -n '272,361p' KANBAN.md)` → zero diff for TODO-045). The spec remains the single source of truth and no parallel near-copy was introduced.
- The DONE-013 + TODO-045 cards both reference `docs/SPECS/spec-deferred_scalars.md` — that's intentionally forward-correct (resolves after Worker 1's archival `git mv`), not a DRY duplication. Worker 1's strip-step at final verification keeps the spec's inline TODO-045 body from drifting against the live KANBAN card body.
- The carry-forward integration-pass DRY watchpoints from Slices 1-4 (sentinel/helper pair, `convert_scalar` parallel branches, `_make_one_field_schema` extraction candidate, `app_label` literal count, `ConfigurationError` message-template) are not mutated by Slice 6 — Slice 6 touches zero `.py` source under `django_strawberry_framework/`, so the integration-pass scan stays evaluable post-Slice-6.

### Public-surface check

Slice 6 did NOT change `django_strawberry_framework/__init__.py`. Confirmed: `git diff -- django_strawberry_framework/__init__.py` shows only the cumulative-diff entries from Slices 1 (adds `from .scalars import BigInt` + `"BigInt"` in `__all__`) and Slice 5 (bumps `__version__` from `0.0.5` to `0.0.6`). No Slice-6 contribution to the public-API module. The plan's `### Files touched` block does not name `__init__.py` and the Slice 6 implementation notes do not edit `__all__` or the re-export list. Authorization for the cumulative-diff entries was Slice 1's planning pass (spec line 203 authorizes `BigInt` as a new public export) and Slice 5's atomic version-bump quintet.

### CHANGELOG sanity

Slice 6 touches `CHANGELOG.md`. Verified end-to-end:

- **Version line match.** `pyproject.toml:4` and `django_strawberry_framework/__init__.py:25` both read `version = "0.0.6"` / `__version__ = "0.0.6"`. The new entries land under `## [Unreleased]` (not `## [0.0.6]`), which is correct per the plan note that `[Unreleased]` is only promoted to `[0.0.6]` at PyPI publish (Definition of done; a maintainer-owned step).
- **Authorized sub-headings.** Spec lines 324-327 authorize `Added`, `Changed`, `Notes`. The diff at `CHANGELOG.md:20`, `:26`, `:30` shows exactly these three sub-headings. No unauthorized `Fixed` / `Removed` / `Deprecated` / `Security` headings were added.
- **Wording.** `### Notes` at line 31 is verbatim from spec lines 327-328 (character-for-character match including the `StrawberryConfig.scalar_map` reference and the `scalar_map`-based-design follow-up phrasing). `### Added` bullets (lines 21-24) cover all four shipped behaviors the spec enumerates: `BigInt` export, `JSONField → JSON`, `HStoreField → JSON` (with the soft-registration note named explicitly), and `ArrayField` recursion (with the `ConfigurationError` rejection for nested + outer-`choices` named explicitly). `### Changed` bullet at line 28 correctly flags `PositiveBigIntegerField` as a **breaking wire-format change** (matches spec line 326's pinned framing); the bullet explicitly names the breakage shape (decimal strings on the wire, not JSON integers) so consumers cannot misread it as internal-only.
- **No over- or under-statement.** The `### Added` `BigInt` bullet does not falsely imply the scalar-map migration is in this release; it correctly references the FEATURES.md anchor for behavior detail and KANBAN.md for the DONE card. The `### Changed` bullet correctly names the wire-format breakage without overclaiming (e.g., does not claim signature changes to `convert_scalar` or `SCALAR_MAP`'s public exposure). The `### Notes` text correctly characterizes the suppression as a workaround tracked as a follow-up, matching the TODO-ALPHA-045-0.0.7 card framing.

### Documentation / release sanity

Slice 6 touches docs/release/KANBAN/archive surfaces. Verified end-to-end:

- **Version strings + statuses match.** `README.md:45` is `0.0.6`, `docs/README.md:89` is `0.0.6` (shipped today), `docs/FEATURES.md:20` is `0.0.6` (carried over from Slice 5; not regressed by Slice 6). `docs/FEATURES.md:166` is `**Status:** shipped (\`0.0.6\`)` for BigInt scalar, `docs/FEATURES.md:908` is `**Status:** shipped (\`0.0.6\`)` for Specialized scalar conversions. Index badges at line 45 (`BigInt scalar | shipped (\`0.0.6\`)`) and line 105 (`Specialized scalar conversions | shipped (\`0.0.6\`)`) match.
- **KANBAN card-NNN ordering.** `TODO-ALPHA-013-0.0.6` body removed from `## To Do - Alpha (0.1.0)` (no orphan reference except line 879's pre-existing Definition-of-done bullet, which reads "Every other Alpha card (`TODO-ALPHA-013-0.0.6` through ...) is in `DONE`" — that line correctly describes the post-move state and was not added or edited by Slice 6). `DONE-013-0.0.6` body appears at KANBAN.md:1657-1676 in the `Done` section, immediately after `DONE-012-0.0.6` and before `## Release readiness checklist`. New card `TODO-ALPHA-045-0.0.7` appears at KANBAN.md:272 (between `TODO-ALPHA-019-0.0.7` ending at line 270 and `TODO-ALPHA-020-0.0.8` starting at line 363), at NNN 045 as the spec rev-9 M1 mandates — not renumbered to fit the 0.0.7 cluster contiguously, which would have been the explicitly-killed renumber-cascading-rename option.
- **TODO-045 four-backtick fence handling.** Verified `grep "^\`\`\`\`" KANBAN.md` returns zero hits — the spec's outer four-backtick wrapper was correctly stripped during paste. The inner ` ```python ` fence at KANBAN.md:292-301 survives as a regular fenced code block; the heading + prose + Python snippet + bullets + open design questions all sit at column 0 (no residual 4-space indent from the spec's outer fence). Markdown will render the card correctly with one Python code block.
- **Verbatim drop-in adherence.** Three character-for-character comparisons performed (post-indent-strip): (a) `docs/FEATURES.md:168` BigInt entry text against spec line 200 → match; (b) KANBAN.md:1657-1676 DONE-013 body against spec lines 208-227 (4-space indent stripped) → `diff` returns no output → exact match; (c) KANBAN.md:272-361 TODO-045 body against spec lines 232-321 (4-space indent stripped) → `diff` returns no output → exact match. Zero deviation, zero paraphrasing.
- **`docs/FEATURES.md:910` (now :916) HStore line replacement.** Spec line 199 pins the before / after wording. Old line read `PostgreSQL HStoreField → dict[str, str | None] (soft-registered, only when django.contrib.postgres is installed)`; new line at `docs/FEATURES.md:916` reads `PostgreSQL HStoreField → strawberry.scalars.JSON (soft-registered, only when django.contrib.postgres.fields imports successfully)` — exact match against the spec's pinned replacement. The `PositiveBigIntegerField → BigInt` sibling bullet was added at line 913 per the spec instruction.
- **Markdown link resolution.** Links introduced or moved by the slice: CHANGELOG.md `[\`BigInt\` scalar](docs/FEATURES.md#bigint-scalar)` and `[\`KANBAN.md\`](KANBAN.md)` → both files exist and the anchor `#bigint-scalar` is the actual H2 heading at `docs/FEATURES.md:164`. DONE-013 + TODO-045 references to `docs/SPECS/spec-deferred_scalars.md` are forward-looking — they will resolve after Worker 1's final-verification `git mv`. **Flag as expected**, not a finding.
- **Active-spec archival NOT yet done by Worker 2.** Confirmed `docs/spec-deferred_scalars.md` still exists (verified via `ls -la`) and `docs/SPECS/spec-deferred_scalars.md` does NOT yet exist (verified via `ls docs/SPECS/` → only `spec-relay_interfaces.md`). This matches the plan's explicit Worker 1 / Worker 2 split: Worker 2 does not move or edit the active spec; Worker 1 owns the strip + `git mv` at final verification. Worker 2 correctly did not touch the spec.
- **No obsolete "coming soon" / "planned" / old-version wording in Slice-6-updated files.** Walked the four pre-slice obsolete sites the plan named: (a) `docs/README.md:89` reads `Shipped today (\`0.0.6\`)` — no `0.0.5`; (b) `docs/README.md:101+112` (the surrounding "Coming in 0.1.0" callouts) no longer contain the `specialized scalar conversions (BigIntegerField, JSONField, ArrayField, HStoreField)` bullet — correctly relocated to shipped-today at line 92; (c) `docs/FEATURES.md` BigInt scalar entry no longer reads "planned for 0.0.6" — flipped to shipped; (d) `docs/FEATURES.md:872` (formerly the `Deferred mappings — BigIntegerField, JSONField, ...` line) has been deleted — verified the line no longer appears in the file.

### What looks solid

- **Verbatim drop-in discipline.** The three load-bearing spec verbatim blocks (BigInt entry text at FEATURES.md, DONE-013 body in KANBAN.md, TODO-045 body in KANBAN.md) landed with zero deviation. Worker 2 also handled the spec's only nested-fence trap correctly (four-backtick outer fence stripped, inner ` ```python ` block preserved) — a rendering bug here would have been a High finding; the actual handling is clean.
- **CHANGELOG sub-section ordering and authorization.** Authorized headings only (`Added`, `Changed`, `Notes` — Keep-a-Changelog ordering with `Notes` as a custom trailer per spec). The `### Changed` block correctly extends the existing DONE-012 bullet rather than replacing it, preserving the chronological reading. The breaking-change framing on `PositiveBigIntegerField` is explicit and unambiguous (decimal-string wire format, not JSON integers).
- **Cross-doc coherence.** `docs/FEATURES.md` Public exports + Index + BigInt entry + Specialized scalar conversions + Scalar field conversion all read as a single coherent shipped-state. `TODAY.md` Scalar-conversions block lists the four new mappings with the same soft-registration framing as `docs/FEATURES.md`. `docs/README.md` shipped-today + `README.md:45` version line agree.
- **`docs/TREE.md` extra edit.** Worker 2's small extra edit (adding `BigInt` to the current-layout `__init__.py` re-exports comment) is correct and self-contained — the comment otherwise would have drifted against the public surface bumped in Slices 1 + 5. Worker 2 surfaced it in build report `### Notes for Worker 3` so the reviewer can evaluate it. I accept the edit as a coherence fix rather than scope creep; the alternative (leaving the comment stale) would be worse for future readers using TREE.md as a navigation aid.
- **Forward-looking link discipline.** The DONE-013 + TODO-045 references to `docs/SPECS/spec-deferred_scalars.md` are deliberate forward-looking links that resolve after Worker 1's archival move. The plan's Worker 1 / Worker 2 split is documented and Worker 2 honored it (no spec edits by Worker 2). This is the cleanest possible handoff to Worker 1's final verification.

### Temp test verification

Not applicable; slice did not modify Python source surface.

### Notes for Worker 1 (spec reconciliation)

- **Two remaining lifecycle edits assigned to Worker 1 by the plan.** During final verification, Worker 1 performs (a) the inline-strip of the TODO-ALPHA-045-0.0.7 verbatim body from `docs/spec-deferred_scalars.md`'s Slice 6 fenced block (spec lines 231-322) and its replacement with the one-line `See KANBAN.md for the current TODO-ALPHA-045-0.0.7 card body — the verbatim drop-in below was used for the initial card creation in this commit and may be out of date once the follow-up spec evolves the card.` pointer (per spec line 330); and (b) the `git mv docs/spec-deferred_scalars.md docs/SPECS/spec-deferred_scalars.md` archival. Both must be recorded under `### Spec changes made (Worker 1 only)` with old / new paths. The artifact's `Spec reference:` line should also be updated to cite the archived path after the move (current cite `docs/spec-deferred_scalars.md (lines 194-333; ...)` becomes `docs/SPECS/spec-deferred_scalars.md (lines 194-333; ...)`).
- **Per the plan's Worker 1 / Worker 2 split section**, Worker 3 explicitly does not reject Slice 6 for the missing strip-and-move. Confirmed the plan assigns both steps to Worker 1 (`Implementation discretion items` "Worker 1 / Worker 2 split for the active spec (non-negotiable, NOT discretion)" + plan steps 8-9). Per BUILD.md "Spec lifecycle and archive work": Worker 3 reviews the durable docs and verifies the plan assigned the archive step; both verifications pass.
- **No structural drift surfaced.** The slice is a faithful execution of the plan with one named small-drift edit (the `docs/TREE.md` `__init__.py` comment update, accepted in review above). No spec ambiguity, no Decision-level reconciliation needed beyond the Worker-1-owned lifecycle work already named in the plan.
- **Cumulative-diff hygiene for Worker 1's pre-commit re-check.** Per Slice 5 memory carry-forward: at final verification, walk the seven Slice-5-pinned `0.0.6` literals to confirm none were accidentally reverted during Slice 6's wider markdown rewrites (especially `docs/FEATURES.md:20` — `Current package version`). Verified at review time and the line stays at `0.0.6`; logging here so the maintainer's final scan has a deterministic checklist to run.

### Review outcome

`review-accepted` — every spec-required behavior in Slice 6 reflected in the diff; no High / Medium / Low findings surfaced; verbatim drop-ins match the spec character-for-character; the public-surface check, CHANGELOG sanity check, and documentation/release sanity check all pass; Worker-1-owned lifecycle work (inline strip + archival `git mv`) is correctly deferred to final verification per the plan's explicit Worker 1 / Worker 2 split.

---

## Final verification (Worker 1)

### DRY check across this slice and Slices 1-5

Slice 6 is documentation, KANBAN, CHANGELOG, and spec-archive lifecycle work. Zero `.py` source changes. The DRY framing for this slice is single-source-of-truth, not helper reuse:

- **Spec as single source of truth for verbatim drop-ins.** Three load-bearing prose blocks are copied character-for-character from the spec into durable docs / KANBAN / CHANGELOG: the BigInt entry text (spec line 200 → `docs/FEATURES.md` BigInt entry), the DONE-013-0.0.6 body (spec lines 207-228 → `KANBAN.md` Done section), and the CHANGELOG `Notes` text (spec lines 327-328 → `CHANGELOG.md` `[Unreleased] ### Notes`). Worker 3's review section logged `diff` results confirming zero deviation for the DONE-013 + TODO-045 verbatim copies; I walked the BigInt entry text and CHANGELOG `Notes` block at final verification and confirmed both are character-for-character. This is the DRY decision — spec stays authoritative; durable doc carries the one canonical copy.
- **TODO-045 verbatim body, spec → KANBAN, single hop.** The spec previously carried the verbatim TODO-045 body twice (once at the original lines 232-321 as the source-of-truth drop-in, and implicitly again in `KANBAN.md` after paste). The Worker 1 strip below replaces the spec's inline copy with a `See KANBAN.md` pointer — KANBAN.md becomes the single live copy, and the archived spec's revision-9 M2 mitigation lands as designed. No duplication remains after the strip.
- **Cross-slice DRY (Slices 1-5).** Slice 6 touches zero `.py` source under `django_strawberry_framework/` and zero test code; the six cross-slice DRY watchpoints inherited from Slices 1-4 (consolidated in `docs/builder/worker-memory/worker-1.md` lines 7-14: `_resolve_array_field` / `_resolve_hstore_field` pair, `_ARRAY_FIELD_CLS` / `_HSTORE_FIELD_CLS` sentinels, parallel `convert_scalar` sentinel branches, outer-`choices` rejection templates, `_make_one_field_schema` test-helper extraction candidate, `app_label` literal density) are not mutated by Slice 6. The integration pass spawn evaluates them against the full post-Slice-6 build with full parameter-surface visibility. No new duplication introduced.
- **No new prose-level duplication observed.** The `docs/FEATURES.md` Public-exports `BigInt` bullet and Index `BigInt` row are deliberate parallel entries (the Index is a TOC for the entry sections), not duplication. Same for the CHANGELOG `### Added` + KANBAN DONE-013 listing of the four shipped behaviors — Keep-a-Changelog and KANBAN serve different audiences; each carries its own canonical phrasing.

### Existing tests still pass

Focused sweep: `uv run pytest tests/test_scalars.py tests/types/test_converters.py tests/base/test_init.py -x --no-cov` → **79 passed, 2 skipped, 2 warnings in 0.25s**. Same count as Slice 5 final-verification (which is correct — Slice 6 ships zero Python code). The two skips remain the postgres-contrib-gated `test_real_array_field_compatible_with_strawberry` and `test_real_hstore_field_compatible_with_strawberry`; the two warnings remain the pre-existing `test_choice_enums._owner` re-registration `RuntimeWarning`s flagged in the Slice 1-4 consolidated memory entry. No regressions.

Per `docs/builder/BUILD.md` "Coverage is the maintainer's gate, not a worker's tool", the `--no-cov` flag here disables the project-wide auto-applied `--cov` flag in `pytest.ini addopts` (Worker 1 does not introduce coverage inspection; the auto-applied flag is project-wide config, structurally identical to CI). Coverage gating belongs to CI's `fail_under = 100` and the maintainer; existing-tests-pass gate is satisfied.

### Spec reconciliation + active-spec lifecycle work (Worker 1 only)

Performed both Worker-1-owned final-verification steps in single strip-then-move pass:

1. **Strip.** Replaced the four-backtick fenced block at the original spec lines 231-322 (the verbatim `TODO-ALPHA-045-0.0.7` card body wrapped in ` ````markdown ` outer fence with inner ` ```python ` code block) with the one-line pointer text from the original spec line 330: `> "See \`KANBAN.md\` for the current \`TODO-ALPHA-045-0.0.7\` card body — the verbatim drop-in below was used for the initial card creation in this commit and may be out of date once the follow-up spec evolves the card."` (preserved as a blockquote at the same 4-space indentation depth as the surrounding checklist sub-content). The inline `DONE-013-0.0.6` body at the original spec lines 207-228 stays — historical record per the original spec line 332. The follow-up archival-step instruction at the original spec lines 328-332 also stays in the archived file as a record of what Worker 1 did during this final-verification pass; the strip and move are both already complete, so the instruction reads retrospectively but is not load-bearing for live state.
2. **Move.** `git mv docs/spec-deferred_scalars.md docs/SPECS/spec-deferred_scalars.md` — directory `docs/SPECS/` already existed (contained `spec-relay_interfaces.md` from the prior `move old spec;` commit at `a7c8f8f`). `git status --short` now shows `RM docs/spec-deferred_scalars.md -> docs/SPECS/spec-deferred_scalars.md` — the rename and the strip are staged together as a single change for the maintainer's commit. Old path no longer resolves; new path resolves.

### Cross-cutting verification

- **Forward-looking links to the archived path resolve.** `KANBAN.md` DONE-013-0.0.6 body at line ~1657 references `docs/SPECS/spec-deferred_scalars.md` (per Worker 3's review) — verified the file exists at the new path; link resolves. The Slice-6 plan's Worker-2-written prose (the `docs/FEATURES.md`, `docs/README.md`, `TODAY.md`, `CHANGELOG.md`, `docs/TREE.md`, `README.md` edits) does not anchor at `docs/SPECS/spec-deferred_scalars.md` directly — those files cite shipped behavior via FEATURES.md anchors and KANBAN.md card IDs, not via the spec path. No broken forward links.
- **Build plan + bld-slice-* artifacts intentionally still cite the original `docs/spec-deferred_scalars.md` path.** Per the task brief: those references describe historical state at the time of writing and should NOT be retroactively updated. Verified via spot-check: `docs/builder/build-deferred_scalars-0_0_6.md` and `docs/builder/bld-slice-1-bigint_scalar.md` through `bld-slice-5-version_bump.md` all cite `docs/spec-deferred_scalars.md` — that is correct; the spec line numbers cited in those artifacts are pinned at the pre-strip, pre-move spec state. The maintainer's commit history (`d05fb15 Finalize docs/spec-deferred_scalars.md`) preserves the pre-strip content if anyone needs to dereference a historical artifact line cite. The current artifact's `Spec reference:` line was updated to the archived path because this artifact is the one whose Worker 1 final-verification pass performed the move; the artifact and the move are atomic.
- **Slice-6 plan steps 1-7 (Worker 2's seven Worker-2-owned edit groups) all landed.** Worker 3's review section walked all seven groups end-to-end with `diff`-verified verbatim adherence on the three load-bearing spec drop-ins. I re-read the Build report (Worker 2) and Review (Worker 3) sections against the current diff at final verification; both reports remain accurate and no Slice-6 prose has drifted since the review pass.
- **Cumulative-diff hygiene checkpoint (per Worker 3's Notes for Worker 1).** Spot-checked the seven Slice-5-pinned `0.0.6` literals: `pyproject.toml:4` is `0.0.6`; `django_strawberry_framework/__init__.py:25` is `__version__ = "0.0.6"`; `tests/base/test_init.py` `test_version` pins `"0.0.6"`; `docs/FEATURES.md:20` reads `Current package version: \`0.0.6\``; `uv.lock` package line is `0.0.6`; `README.md:45` is `0.0.6` (Slice 6 edit); `docs/README.md:89` is `0.0.6` (Slice 6 edit). None reverted during Slice 6's markdown sweep.
- **Spec status-line re-verification (per Worker 1 BUILD.md spawn-time rule).** The archived spec's status line at `docs/SPECS/spec-deferred_scalars.md:4` reads `Status: draft (revision 10, post-feedback2 re-review).`. This is now the archive's permanent status snapshot — the maintainer may choose to flip it to `archived` at commit time, but that is a maintainer-discretion edit, not a Worker-1-required reconciliation. The spec's lifecycle has closed (build delivered); the file is now historical, and the spec's slice checklist with unchecked boxes accurately reflects the pre-build state. Leaving the status line as `draft (revision 10, post-feedback2 re-review)` preserves the historical record of the spec's final pre-build state; updating it would erase that. Defer to maintainer.

### Final status

`final-accepted` — Slice 6 ships clean: zero `.py` source changes; seven Worker-2-owned doc / KANBAN / CHANGELOG edit groups verified against the spec; two Worker-1-owned active-spec lifecycle steps (inline TODO-045 strip + `git mv` archival) completed in this pass; 79 focused tests pass + 2 expected skips with zero regressions; no new cross-slice DRY duplication introduced; forward-looking links to the archived spec path resolve; build-plan and Slices-1-5 artifacts intentionally retain pre-archive spec citations as historical references.

### Summary

Slice 6 closes the deferred-scalars build cycle by propagating the shipped-state into every durable doc surface (root `README.md`, `docs/README.md`, `docs/TREE.md` for both current and target package layouts, `docs/FEATURES.md` for five entries spanning Index + Public exports + BigInt scalar + Specialized scalar conversions + Scalar field conversion, `TODAY.md`), moving the `TODO-ALPHA-013-0.0.6` card to a `DONE-013-0.0.6` body in `KANBAN.md`'s Done section, adding the follow-up `TODO-ALPHA-045-0.0.7 — Warning-free scalar registration via StrawberryConfig.scalar_map` card to the 0.0.7 cluster, posting the `[Unreleased] ### Added` / `### Changed` / `### Notes` entries to `CHANGELOG.md`, and archiving the active spec to `docs/SPECS/spec-deferred_scalars.md` with the inline TODO-045 verbatim body stripped per the revision-9 M2 anti-drift mitigation. Verbatim spec drop-ins (BigInt entry text, DONE-013 body, TODO-045 body, CHANGELOG `Notes`) landed character-for-character. Build cycle is now ready for the cross-slice integration pass.

### Spec changes made (Worker 1 only)

Two edits to the active spec, both during this final-verification pass, both authorized by the Slice-6 plan's `Implementation discretion items` Worker 1 / Worker 2 split and by `docs/builder/BUILD.md` "Spec lifecycle and archive work":

1. **`docs/spec-deferred_scalars.md` lines 231-322 (original) → replaced with one-line blockquote pointer.** Stripped the four-backtick fenced `TODO-ALPHA-045-0.0.7` verbatim body (the ` ````markdown ` block containing the inner ` ```python ` code snippet) and replaced it with the verbatim one-line pointer text from the original spec line 330: `> "See \`KANBAN.md\` for the current \`TODO-ALPHA-045-0.0.7\` card body — the verbatim drop-in below was used for the initial card creation in this commit and may be out of date once the follow-up spec evolves the card."`. Reason: prevent drift between the archived spec's inline TODO-045 body and the evolving live `KANBAN.md` card body, per spec revision-9 M2. Triggered by: Slice 6.
2. **`docs/spec-deferred_scalars.md` → `docs/SPECS/spec-deferred_scalars.md` (`git mv`).** Archived the active spec to the durable `docs/SPECS/` directory following BUILD.md "Spec lifecycle and archive work". Old path no longer resolves; new path resolves; rename is staged for the maintainer's commit. Reason: spec is fully shipped — durable docs (`docs/FEATURES.md`, `KANBAN.md`, `CHANGELOG.md`, `docs/TREE.md`, `TODAY.md`, both READMEs) are now the live source of truth for shipped behavior; KANBAN's `TODO-ALPHA-045-0.0.7` card is the live source of truth for the follow-up. Triggered by: Slice 6.

The `DONE-013-0.0.6` inline body at the original spec lines 207-228 was intentionally preserved (per spec line 332's instruction): it is a historical record of what shipped, not a live evolving card, so the drift risk that motivates the TODO-045 strip does not apply.

---

## Post-build maintainer revert (2026-05-17)

After build closeout, the maintainer updated the standing workflow (`docs/builder/BUILD.md` "Spec stays at its working location", replacing the prior "Spec lifecycle and archive work" section) to make spec archival opt-in only when a spec explicitly declares it AND the maintainer chooses to honor that declaration — the standing workflow now has no archival default. The maintainer then reverted Slice 6's spec-archival step for this build: `git mv docs/SPECS/spec-deferred_scalars.md docs/spec-deferred_scalars.md`. The inline TODO-045 strip from `### Spec changes made (Worker 1 only)` item 1 is **preserved** (that change deduplicates the spec against the live KANBAN card and remains useful regardless of archival posture); item 2's archival move is **reverted**.

Live-doc cleanup also applied as part of the revert: `KANBAN.md`'s DONE-013-0.0.6 and TODO-ALPHA-045-0.0.7 card bodies originally cited the spec at `docs/SPECS/spec-deferred_scalars.md` as a forward-looking link; both citations were updated to `docs/spec-deferred_scalars.md` (the spec's current and now-permanent location). The spec's own internal references to its erstwhile-archived path (Slice 6 checklist line "Archive this spec...", Implementation-plan Slice 6 narrative, Definition-of-done "Spec archived to ..." line) are obsolete-but-historical and were not edited — they describe what Slice 6 originally intended to do.

This artifact's `### Spec changes made (Worker 1 only)` section above and the Slice 6 plan's archive step are retained as the historical record of what the build performed before the maintainer's revert. The build plan's `Slice 6` checkbox stays `- [x]` — Slice 6 closed as planned; the post-build revert is a separate maintainer action, not a re-opening of the slice.
