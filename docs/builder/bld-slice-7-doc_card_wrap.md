# Build: Slice 7 — doc updates + card-completion wrap

Spec reference: `docs/spec-032-full_relay-0_0_9.md` (lines 111-120 slice checklist; lines 608-622 Doc updates; line 628 Risks first item — the two net-new glossary entries + terms-CSV rule; lines 683-686 DoD items 10-12; Decision 13 lines 488-494)
Status: final-accepted

## Plan (Worker 1)

**Spec status-line re-verification (per-spawn):** spec line 5 reads "Slices 1–6 implemented (final-accepted in the build cycle, uncommitted; …); Slice 7 not started." Verified accurate at planning time — no header edit needed this spawn. Worker 1 updates it at final verification when Slice 7 lands.

**Slice character.** This is an all-docs wrap slice. No package source or test logic changes. The working tree already carries Slices 1–6 (uncommitted, final-accepted) — treat those diffs as this slice's baseline; do not revert or re-touch them. The slice carries the **explicit per-card CHANGELOG-edit grant** (spec line 118 / Doc updates intro line 610): Worker 2 IS authorized to edit `CHANGELOG.md` exactly as specified in step 6 below — this sentence is the explicit naming the spec requires.

**CRITICAL — generated docs are DB-backed.** `KANBAN.md`, `KANBAN.html`, and `docs/GLOSSARY.md` are rendered from `examples/fakeshop/db.sqlite3` by `scripts/build_kanban_md.py` / `build_kanban_html.py` / `build_glossary_md.py`. Every GLOSSARY/KANBAN change in this slice is a Django-ORM edit followed by regeneration (BUILD.md "Generated docs are DB-backed"; worker-0.md "Closing out a kanban card"). Hand-editing the rendered markdown is forbidden — the regenerate silently reverts it. Always ORM (`manage.py shell`), never raw SQL (the `post_save` `UUIDModel` side-row would be skipped and the GraphQL render breaks). The DB is git-tracked; `git checkout -- examples/fakeshop/db.sqlite3` reverts a bad edit.

**Verified DB state (read-only ORM inspection, 2026-06-11):**

- Card `number=32` is `status.key="wip"`, title "Full Relay story (Node + Connection + Root + validation)". Its `SpecDoc` **already exists**: `name="spec-032-full_relay-0_0_9"`, `url="https://github.com/riodw/django-strawberry-framework/blob/main/docs/spec-032-full_relay-0_0_9.md"` — step 2 of the worker-0 procedure is **confirm-only, do not create**.
- `card.glossary_links.count() == 0` — the done-save signal requires ≥1 `CardGlossaryTerm` BEFORE the status flip (bootstrap in step 12).
- Card 32's 68 `CardItem`s sit in sections `other` (66) and `verified_upstream` (2) — unlike DONE cards 29/30/31, which carry a populated, fully-ticked `definition_of_done` section. Git history (`git show 27ed0b30:KANBAN.md`, card 032's original `#### Definition of done` block) identifies the card's DoD as exactly the 11 items now at `other` orders **40–50** ("New spec: `docs/spec-relay_connection.md` covering all eight goals…" through "100% coverage across the new code paths…"). `CardItem` has `UniqueConstraint(card, section, order)`; the `definition_of_done` Section row exists and has zero items for this card, so re-sectioning to orders 0–10 collides with nothing.
- Stale spec-filename texts to fix (worker-0 step 6's named fix class): `other` order 40 and `other` order 59 both say `docs/spec-relay_connection.md`.
- Glossary: `GlossaryTerm(anchor="djangonodefield")` exists — `status=planned`, `status_text="planned for \`0.0.9\`"`, `entry_order=24`, `index_order=20`, preceded by `djangomutation` (23/19) and followed by `djangooptimizerextension` (25/21). `djangonodesfield` and `metarelation_shapes` do **not** exist. Statuses: `shipped` / `planned`. The renderer sorts entries by `(entry_order, title_sort)` and the Index by `(index_order, title_sort)`; the "See also:" line is part of `GlossaryTerm.body`.
- Alphabetical-neighbor placement values (worker-0 step 1 tie-trick):
  - `DjangoNodesField` → preceding neighbor `DjangoNodeField`: set `entry_order=24`, `index_order=20`, `title_sort="djangonodesfield"` (ties with the neighbor; larger title_sort slots it immediately after).
  - `Meta.relation_shapes` → preceding neighbor `Meta.primary` (entry 49 / index 45; note `Meta.required_overrides` already shares 49/45 via the same trick and `"meta.relation_shapes" < "meta.required_overrides"` lexically): set `entry_order=49`, `index_order=45`, `title_sort="meta.relation_shapes"`.
- Category memberships (sorted by `order` alone; `(category, order)` unique → use the temp band):
  - `relay` today (0–7): Relay Node integration, RELAY_GLOBALID_STRATEGY, `DjangoNodeField`, `DjangoConnectionField`, `DjangoConnection`, `Meta.connection`, Connection-aware optimizer planning, `SyncMisuseError`.
  - `type-generation` today (0–14): `DjangoType`, `Meta.model`, `Meta.fields`, `Meta.exclude`, `Meta.name`, `Meta.description`, `Meta.primary`, `Meta.interfaces`, `Meta.connection`, `Meta.globalid_strategy`, `Meta.nullable_overrides`, `Meta.required_overrides`, Definition-order independence, `finalize_django_types`, `ConfigurationError`.
- The staged `TODO(spec-032 … Slice 7)` comments inside `docs/GLOSSARY.md:357` and `KANBAN.md:152` are **hand-placed in the rendered files only** (verified: NOT in any DB body) — the regenerate removes them automatically; no DB cleanup needed for those two.
- Terms CSV: `docs/spec-032-full_relay-0_0_9-terms.csv` (38 rows + header) carries `DjangoNodeField` but **not** `DjangoNodesField` / `Meta.relation_shapes` — per the spec's Risks line 628 and DoD item 1, the convention APPLIES: once Slice 7 creates the glossary entries, both terms "belong in the companion terms CSV". `scripts/check_spec_glossary.py` enforces two checks per CSV row: the GLOSSARY heading exists AND the spec links the anchor (`[term][glossary-<anchor>]` ref-style). The second check needs a **spec edit** (Worker 1-owned — see the Worker 1 final-verification steps).

**Staged Slice-7 TODO anchors (this slice removes its own; grep-verified 2026-06-11):** `TODAY.md:210`, `README.md:51`, `CHANGELOG.md:22`, `GOAL.md:165`, `docs/README.md:118`, `docs/GLOSSARY.md:357` (rendered-file only), `docs/TREE.md:193`, `KANBAN.md:152` (rendered-file only). `docs/feedback.md:3` mentions the anchors descriptively (reusable review scratch surface) — leave untouched. Shadow/bld-artifact mentions are scratch — leave untouched.

**review_inspect.py: skipped — recorded reason:** all-docs slice; no `.py` file gains logic (the only Python-adjacent change is `examples/fakeshop/db.sqlite3` row data via the ORM shell). Per BUILD.md the helper is mandatory only when a plan adds logic to a ≥150-line `.py` file or `optimizer/`/`types/` — neither applies. If implementation unexpectedly requires a `.py` edit, stop and re-plan (it would mean this slice's scope was misread).

### DRY analysis

- **Existing patterns reused.**
  - The DB-backed close-out procedure is `docs/builder/worker-0.md` "Closing out a kanban card" steps 1–8 verbatim — this plan instantiates it with verified values rather than inventing a parallel procedure.
  - Glossary entry shape (Status line + body + `**See also:**` tail), Index-row format (`| [\`X\`](#x) | shipped (\`0.0.9\`) |`), and Browse-by-category line format are copied from the shipped `DjangoConnectionField` / `Meta.globalid_strategy` entries (docs/GLOSSARY.md:284-293, 685-707) — the renderer (`scripts/build_glossary_md.py::render_term/render_index/render_browse`) generates Index and Browse lines from row data, so only the **body** text is authored.
  - The neighbor-tie ordering trick reuses the exact precedent already in the data: `Meta.required_overrides` sits at `entry_order=49,index_order=45` tied with `Meta.primary` — `Meta.relation_shapes` joins the same tie band.
  - CHANGELOG bullets follow the `[Unreleased] ### Added` house style of the `DONE-030`/`DONE-031` bullets (bold lead-in, ref-style glossary links, defs in the bottom block).
- **Canonical phrasings pinned (the verbatim sources the docs must echo — no fresh paraphrase per surface):**
  - Root-fields contract: spec Goal 1 (line 148) + DoD item 3 (line 662) — "null for hidden and missing rows with no existence leak", "`GraphQLError` … `extensions={"code": "GLOBALID_INVALID"}`", "per-type-batched, order-preserving", "`id: strawberry.ID` (raw string … `node(id: ID!)`)".
  - Relation upgrade: spec Goal 2 (line 149) + Decision 6/7 — "`<field>Connection` sibling by default (`"both"`)", "`"connection"` suppresses the list field; `"list"` suppresses the connection", "Relay-Node-shaped declaring type **and** target".
  - testing.relay: Decision 10 / DoD item 8 — "strategy-aware `global_id_for(type_cls, id)`", "the public re-export `decode_global_id(gid)`", the secondary-emitter asymmetry sentence.
  - Interface-only-schema reachability note: spec Edge cases line 526 / Risks line 635 (`strawberry.Schema(types=[...])` recourse) — already mirrored in `django_strawberry_framework/relay.py:33`; the glossary body reuses that sentence shape.
  - Latent-break refresh: ONE canonical replacement sentence (given in step 5) used by BOTH `TODAY.md` and the `CHANGELOG.md` `### Changed` amendment, so the two copies of the stale "nothing decodes a GlobalID until root `node(id:)` ships" claim stay in lockstep.
- **New helpers justified:** none. No code; the ORM edits are a one-off `manage.py shell` script (heredoc), not a reusable module.
- **Duplication risk avoided:** the naive implementation would author the new GLOSSARY Index rows / Browse lines by hand in the rendered file (forbidden — and double-maintained); and would paraphrase the Relay contract independently in five doc surfaces. Prevented by (a) DB-only authorship + regeneration, (b) the pinned canonical phrasings above, (c) docs/README bullets explicitly cross-linking the glossary anchors instead of restating the full contract.

### Implementation steps

Line numbers are pin-at-write-time navigational hints — verify against the current file before editing.

**Part A — plain-markdown doc edits (Worker 2). Each step removes its own staged TODO block in the same edit.**

1. **`GOAL.md` (~line 150, 165-176):** flip the two showcase fields exactly as the staged block prescribes — `galaxy: GalaxyNode = DjangoNodeField(GalaxyNode)` → `galaxy: GalaxyNode | None = DjangoNodeField(GalaxyNode)` and `celestial_body: CelestialBodyNode = DjangoNodeField(CelestialBodyNode)` → `celestial_body: CelestialBodyNode | None = DjangoNodeField(CelestialBodyNode)`. Remove the TODO comment block (lines ~164-176). No other GOAL.md claims change.
2. **`README.md` (~lines 49-60):** in the Status paragraph, the newest-shipped-surface sentence currently opens "Newest shipped surface: the model-anchored Relay `GlobalID` default (`0.0.9`) — …". Re-lead with the full Relay story as the newest shipped surface: root `node(id:)` / `nodes(ids:)` via `DjangoNodeField` / `DjangoNodesField` (bare interface + typed forms, server-side decode through the strategy system, null-for-invisible / `GLOBALID_INVALID`-for-malformed), the relation-as-Connection upgrade with `Meta.relation_shapes` (`"list"` / `"connection"` / `"both"`), and the `testing.relay` helpers (`global_id_for` / `decode_global_id`) — landing "alongside" the existing GlobalID-strategy and `DjangoConnectionField` sentences, which are demoted into the alongside-chain rather than deleted. Keep the opening "**`0.0.8`, single-maintainer, alpha-quality.**" untouched (Decision 13 — no version-number change). Remove the TODO block.
3. **`docs/README.md` (~lines 113-130):** add three shipped-surface bullets at the end of the "Shipped today" list, matching sibling style ("(new in `0.0.9`)" parenthetical + closing `See [\`GLOSSARY.md#…\`][ref]` link):
   - `DjangoNodeField` / `DjangoNodesField` (new in `0.0.9`) — root Relay refetch fields, bare interface (`node: relay.Node | None = DjangoNodeField()`, `nodes: list[relay.Node | None] = DjangoNodesField()`) and typed (`genre: GenreType | None = DjangoNodeField(GenreType)`) forms; `id: ID!` raw-string arguments decoded server-side via the strategy-aware dispatch; dispatch to the type's `resolve_node` / `resolve_nodes` honoring `get_queryset`; `null` for hidden/missing/uncoercible-pk ids (no existence leak), `GraphQLError` with `extensions={"code": "GLOBALID_INVALID"}` for malformed ids; `nodes` is per-type-batched, order-preserving, with `null` holes and duplicate-id support. See `GLOSSARY.md#djangonodefield`.
   - relation-as-Connection upgrade + `Meta.relation_shapes` (new in `0.0.9`) — every Relay-Node-shaped type's many-side relations whose target is also Relay-Node-shaped gain a `<field>Connection` sibling by default (`"both"`); `Meta.relation_shapes = {"<field>": "list" | "connection" | "both"}` narrows per relation; synthesized at finalization Phase 2.5 reusing the shipped connection machinery (per-target connection classes, sidecar `filter:` / `orderBy:` arguments, target-driven `totalCount`). See `GLOSSARY.md#metarelation_shapes`.
   - `testing.relay` helpers (new in `0.0.9`) — `django_strawberry_framework.testing.relay.global_id_for(type_cls, id)` (the strategy-aware encoded `GlobalID` a finalized Relay-Node-shaped type emits) and `decode_global_id(gid)` (public re-export of the decode dispatch). See `GLOSSARY.md#djangonodefield` cross-refs (the helpers have no own entry; the spec does not create one).
   Add the matching ref defs (`glossary-djangonodefield`-style ids resolving to `GLOSSARY.md#…`) alphabetically under the file's `<!-- docs/ -->` link-def group. Then update the "Coming next" `0.0.9` line: the in-progress remainder shrinks to connection-aware optimizer planning (033) — e.g. "`0.0.9` *(in progress)* — connection-aware optimizer planning and a `DjangoType` consumer-DX cleanup pass (the full Relay story — root node/nodes fields, relation-as-Connection + `Meta.relation_shapes`, `testing.relay` — and `DjangoConnectionField` shipped above)". Remove the TODO block.
4. **`docs/TREE.md` (~lines 192-203 TODO block; insertion points below):**
   - Current-layout package tree: add `├── relay.py              # DjangoNodeField / DjangoNodesField root refetch factories (server-side GlobalID decode dispatch; shipped in 0.0.9)` directly after the `connection.py` line (~line 218); add `│   └── relay.py             # global_id_for / decode_global_id public test helpers (0.0.9)` under `testing/` (~line 227, after `_wrap.py`, adjusting the box-drawing `├──`/`└──` characters).
   - Current-layout tests tree (~lines 357-412): add `├── test_relay_node_field.py # DjangoNodeField / DjangoNodesField root-field surface (card-named two-file split, spec-032 Decision 11)` and `├── test_relay_connection.py # relation-as-Connection upgrade + cursor-conformance surface` beside `test_connection.py` (~line 364), and `│   └── test_relay.py        # ← testing/relay.py global_id_for / decode_global_id` under `tests/testing/` (~line 368).
   - Target trees (planner-authorized coherence extension — the spec names "current-layout trees", but the target layout claims to superset the current one and already lists `connection.py` / `test_connection.py`; leaving the shipped modules out would make the file tell two stories): add `relay.py` to the target package tree after `connection.py` (~line 283), `relay.py` under the target `testing/` (~line 333), and the three test files to the target tests tree (~lines 457-502). Recorded for Worker 1 spec reconciliation.
   - Remove the TODO block (~lines 192-204).
5. **`TODAY.md` (~lines 209-217):** refresh ONLY the breaking-change note's latent-break sentence. Replace "In `0.0.9` the break is **latent** — nothing decodes a `GlobalID` until root `node(id:)` ships (`WIP-ALPHA-032-0.0.9`), so a consumer who upgrades without acting sees nothing wrong until then, at which point every old client-cached type-anchored ID is undecodable." with the canonical replacement: "In `0.0.9` the break lands **live** alongside its consumer — root `node(id:)` / `nodes(ids:)` (`DONE-032-0.0.9`) decode every emitted ID, so every old client-cached type-anchored ID is undecodable under the `model` default the moment the upgrade deploys." Keep the rest of the note (the three-step migration sequence and the products-centric framing) byte-identical; no products-surface claims change (activation stays `TODO-BETA-051-0.1.5`). Remove the TODO block.
6. **`CHANGELOG.md` (THE EXPLICIT PER-CARD GRANT — `[Unreleased]` only, NO version-heading promotion per Decision 13):**
   - Under `### Added`, prepend three bullets (canonical texts; house style with ref-style links):
     1. **`DjangoNodeField` / `DjangoNodesField` (root Relay refetch fields).** New top-level `django_strawberry_framework/relay.py` ships the Relay-spec root fields in bare interface form (`node: relay.Node | None = DjangoNodeField()`, `nodes: list[relay.Node | None] = DjangoNodesField()`) and typed form (`genre: GenreType | None = DjangoNodeField(GenreType)`, `DjangoNodesField(GenreType)`); both exported from the package root. Arguments are declared `id: strawberry.ID` / `ids: [ID!]!` (raw strings) and decoded **server-side** through the strategy-aware `decode_global_id` dispatch, so the model-label `0.0.9` default payload resolves and the client's claim of an id's type is never trusted. Resolution dispatches to the type's `resolve_node` / `resolve_nodes` defaults (honoring the [`get_queryset` visibility hook][glossary-get_queryset-visibility-hook]): hidden, missing, and uncoercible-pk ids return `null` with no existence leak; malformed ids raise `GraphQLError` with `extensions={"code": "GLOBALID_INVALID"}` (a malformed or wrong-type id anywhere in a typed/batch field fails the whole field); `nodes(ids:)` is per-type-batched, order-preserving, with positional `null` holes and duplicate-id support. A declared node field on a registry with no Relay-Node-shaped types raises [`ConfigurationError`][glossary-configurationerror] at finalization.
     2. **Relation-as-Connection upgrade + `Meta.relation_shapes`.** Every Relay-Node-shaped [`DjangoType`][glossary-djangotype]'s selected many-side relations (reverse FK, forward / reverse M2M) whose target type is also Relay-Node-shaped now synthesize a `<field>Connection` sibling at finalization Phase 2.5, reusing the shipped [`DjangoConnectionField`][glossary-djangoconnectionfield] machinery — per-target connection classes, sidecar-derived `filter:` / `orderBy:` arguments, and target-driven `totalCount` via [`Meta.connection`][glossary-metaconnection]. The net-new `Meta.relation_shapes` key (`dict[str, str]`, values `"list"` / `"connection"` / `"both"`; `"both"` is the implicit default) narrows the shape per relation: `"connection"` suppresses the list field, `"list"` suppresses the connection. Validated at type creation (unknown keys/values/shapes, non-many-side or consumer-authored targets, and non-Relay declaring types raise [`ConfigurationError`][glossary-configurationerror]); an explicit shape naming a non-Node-target relation raises at finalization; non-Node targets degrade silently under the implicit default.
     3. **`django_strawberry_framework.testing.relay` (public test helpers).** `global_id_for(type_cls, id)` returns the strategy-aware encoded `GlobalID` string a finalized Relay-Node-shaped type emits for a pk (raises [`ConfigurationError`][glossary-configurationerror] for `callable`/custom strategies, unfinalized, or non-Node inputs); `decode_global_id(gid)` is the public re-export of the internal dispatch returning `(target_type, node_id)`. The pair is deliberately asymmetric for a secondary model-label emitter (decode routes to the model's **primary** type); round-trip identity holds for lone/primary model-label types and `type`-strategy payloads. Import from the module path — the helpers are intentionally NOT re-exported from `django_strawberry_framework.testing` (the export gate stays intact).
   - Amend the existing `### Changed` first bullet's stale sentence "The break is **latent** in `0.0.9` — nothing decodes a `GlobalID` until root `node(id:)` ships (`WIP-ALPHA-032-0.0.9`), so an unprepared consumer sees nothing wrong until then, at which point every old client-cached type-anchored ID is undecodable." to mirror step 5's canonical replacement: "The break lands **live** in `0.0.9` alongside its consumer — root `node(id:)` / `nodes(ids:)` (`DONE-032-0.0.9`) decode every emitted ID, so every old client-cached type-anchored ID is undecodable under the `model` default the moment the upgrade deploys." (Planner-authorized one-sentence amendment beyond the literal `### Added` grant — same stale claim the spec orders fixed in TODAY.md; recorded for Worker 1 spec reconciliation, which will extend the spec's grant wording at final verification.)
   - Add any missing ref defs used by the new bullets (`glossary-get_queryset-visibility-hook`, `glossary-metaconnection`, etc. — check which already exist) alphabetically in the bottom block's `<!-- docs/ -->` group.
   - Remove the TODO block (~lines 21-36). Do NOT touch `## [0.0.8]` or promote any heading.
7. **`docs/spec-032-full_relay-0_0_9-terms.csv`:** add two rows (the convention applies once the glossary entries exist — spec Risks line 628 / DoD item 1):
   - after the `DjangoNodeField` row (line 4): `DjangoNodesField,djangonodesfield,Net-new batch refetch sibling this card ships; glossary entry created by Slice 7 of the build`
   - after the `Meta.connection` row (line 10): `Meta.relation_shapes,metarelation_shapes,Net-new relation-shape narrowing key this card ships; glossary entry created by Slice 7 of the build`
   Note: `scripts/check_spec_glossary.py` will report the two new terms as spec-unlinked until Worker 1's final-verification spec edit (below) adds the `[…][glossary-…]` links — expected-red between build and final verification; Worker 3 should not treat the interim failure as a build defect.

**Part B — DB-backed edits (Worker 2; `uv run python examples/fakeshop/manage.py shell` from repo root, one scripted pass; ORM only). Order matters: terms → bodies → categories → card-item fixes → glossary-link bootstrap → status flip → import → regenerate → verify.**

8. **Flip + rewrite `GlossaryTerm(anchor="djangonodefield")`:** `status = GlossaryStatus.objects.get(key="shipped")`; `status_text = "shipped (\`0.0.9\`)"`; replace `body` with the rewrite covering both forms (canonical body — echoes spec Decisions 4/5 and Edge cases line 526):

   > Root single-node refetch field, shipped in two forms: the Relay-spec bare interface form `node: relay.Node | None = DjangoNodeField()` (the literal `node(id: ID!): Node` field) and the typed form `genre: GenreType | None = DjangoNodeField(GenreType)` — both nullable-by-contract (the resolver is dispatched `required=False` unconditionally; the optional annotation is the supported spelling). The synthesized resolver declares `id: strawberry.ID` (a raw string, deliberately not `relay.GlobalID`, so malformed ids reach the package) and decodes **server-side** through the strategy-aware decode dispatch — model-label, type-name, and transitional payloads all resolve, and the client's claim of which type an id belongs to is never trusted. Resolution dispatches to the target type's `resolve_node` default, honoring [`get_queryset`](#get_queryset-visibility-hook): hidden, missing, and uncoercible-pk ids return `null` (the hidden and missing paths share one queryset code path — no existence leak); a malformed id raises `GraphQLError` with `extensions={"code": "GLOBALID_INVALID"}`; the typed form rejects an id that decodes to a different type with a `GraphQLError` naming the expected and received types. A declared node field on a registry with no Relay-Node-shaped types raises [`ConfigurationError`](#configurationerror) at finalization. A schema whose *only* root field is the interface-typed `node` must pass its concrete types via `strawberry.Schema(types=[...])` or expose them through other fields (engine behavior, documented in the field docstring).
   >
   > **See also:** [`DjangoNodesField`](#djangonodesfield) · [`DjangoConnectionField`](#djangoconnectionfield) · [Relay Node integration](#relay-node-integration) · [`Meta.globalid_strategy`](#metaglobalid_strategy).

9. **Create `GlossaryTerm` `DjangoNodesField`:** `title="\`DjangoNodesField\`"`, `title_sort="djangonodesfield"`, `anchor="djangonodesfield"`, `status=shipped`, `status_text="shipped (\`0.0.9\`)"`, `entry_order=24`, `index_order=20`. Body (canonical — echoes spec Decision 4/5 and DoD item 3):

   > Root batch refetch field — the Relay-spec `nodes(ids: [ID!]!): [Node]!` sibling of [`DjangoNodeField`](#djangonodefield), in the same two forms: bare (`nodes: list[relay.Node | None] = DjangoNodesField()`) and typed (`DjangoNodesField(GenreType)`, which additionally runs the per-id declared-target check). Ids are decoded server-side and resolved **per-type batched** (`resolve_nodes` once per distinct type, honoring [`get_queryset`](#get_queryset-visibility-hook)) with input order preserved and duplicate ids supported. Well-formed-but-invisible/missing/uncoercible-pk ids become positional `null` holes; a malformed id — or a wrong-type id in the typed form — anywhere in the batch fails the **whole field** (`GraphQLError`, `GLOBALID_INVALID` for malformed; the `[Node]!` non-null nulls the enclosing `data`). The batch is deliberately uncapped in `0.0.9` (parity with both upstreams; request-size limiting belongs to the consumer's transport layer).
   >
   > **See also:** [`DjangoNodeField`](#djangonodefield) · [Relay Node integration](#relay-node-integration) · [`get_queryset` visibility hook](#get_queryset-visibility-hook).

10. **Create `GlossaryTerm` `Meta.relation_shapes`:** `title="\`Meta.relation_shapes\`"`, `title_sort="meta.relation_shapes"`, `anchor="metarelation_shapes"`, `status=shipped`, `status_text="shipped (\`0.0.9\`)"`, `entry_order=49`, `index_order=45`. Body (canonical — echoes spec Goal 2, Decisions 6/7):

    > Per-relation narrowing key for the relation-as-Connection upgrade. On a Relay-Node-shaped [`DjangoType`](#djangotype), every selected many-side relation (reverse FK, forward / reverse M2M) whose target type is also Relay-Node-shaped synthesizes a `<field>Connection` sibling at finalization Phase 2.5 by default, reusing the shipped [`DjangoConnectionField`](#djangoconnectionfield) machinery — per-target connection classes, sidecar-derived `filter:` / `orderBy:` arguments, and the target type's [`Meta.connection`](#metaconnection) `totalCount` opt-in. `Meta.relation_shapes` is a `dict[str, str]` with values `"list"` / `"connection"` / `"both"` (`"both"` is the implicit default): `"connection"` suppresses the `list[T]` field, `"list"` suppresses the connection. Validated at type creation — unknown keys / values / shapes, a key naming a non-relation / non-many-side / unselected field, a key naming a consumer-authored relation (the override owns the shape), and declaring the key on a non-Relay-Node type all raise [`ConfigurationError`](#configurationerror); an explicit shape naming a relation whose target is not Relay-Node-shaped raises at finalization, while non-Node targets degrade silently (stay list-only) under the implicit default.
    >
    > **See also:** [`DjangoConnectionField`](#djangoconnectionfield) · [`Meta.connection`](#metaconnection) · [Relay Node integration](#relay-node-integration) · [`DjangoType`](#djangotype).

11. **Extend the two cross-referenced bodies (DB `GlossaryTerm.body` edits):**
    - `relay-node-integration`: append to the `0.0.9` paragraph (before the **See also** line) one sentence: "With `DONE-032-0.0.9` the root refetch surface is shipped: [`DjangoNodeField`](#djangonodefield) / [`DjangoNodesField`](#djangonodesfield) decode every emitted payload server-side and dispatch to these `resolve_node` / `resolve_nodes` defaults, and many-side relations between Relay-Node-shaped types synthesize connection siblings per [`Meta.relation_shapes`](#metarelation_shapes)." Extend its **See also** tail with `[\`DjangoNodesField\`](#djangonodesfield)` (after the existing `DjangoNodeField` link).
    - `djangoconnectionfield`: append a relation-as-Connection cross-reference sentence: "As of `0.0.9`, relations between Relay-Node-shaped types synthesize `<field>Connection` siblings through this same machinery — see [`Meta.relation_shapes`](#metarelation_shapes)." Extend its **See also** tail with `[\`Meta.relation_shapes\`](#metarelation_shapes)`. **Also correct the stale strictness sentence** "A [Strictness mode](#strictness-mode) `\"raise\"` run surfaces an unplanned nested-connection access as an N+1 (no silent cap)." → "A [Strictness mode](#strictness-mode) `\"raise\"` run does **not** flag an unplanned nested-connection access — `_check_n1` fires only from the generated list-relation resolvers; strictness-mode interaction with connections lands with [Connection-aware optimizer planning](#connection-aware-optimizer-planning) (`WIP-ALPHA-033-0.0.9`)." (The shipped sentence states behavior nothing implements — spec Revision 6 P2 / Key-glossary "Strictness mode" bullet are the canonical source; recorded for Worker 1 spec reconciliation.)
    - Mirror the new **See also** links as `GlossaryTermLink` rows (kind `see-also`, appended `order` values, `raw_label` matching the rendered label) for all entries touched/created in steps 8–11, matching the existing convention (every shipped term's See-also line has mirror rows).
12. **Category memberships (`GlossaryCategoryMembership`; temp-band renumber per worker-0 step 1 — `order += 1000` on existing members first, then assign 0..N-1):**
    - `relay` final order (10): Relay Node integration, RELAY_GLOBALID_STRATEGY, `DjangoNodeField`, **`DjangoNodesField`**, `DjangoConnectionField`, `DjangoConnection`, `Meta.connection`, **`Meta.relation_shapes`**, Connection-aware optimizer planning, `SyncMisuseError`.
    - `type-generation` final order (16): existing 0–8 unchanged (`DjangoType` … `Meta.connection`), **`Meta.relation_shapes`** ninth (after `Meta.connection`), then `Meta.globalid_strategy`, `Meta.nullable_overrides`, `Meta.required_overrides`, Definition-order independence, `finalize_django_types`, `ConfigurationError`.
13. **Public-exports glossary doc (`BoardDoc` `namespace="glossary"`, `key="public-exports"`):** add to the "Symbols re-exported from `django_strawberry_framework`" list (alphabetical with siblings): `DjangoConnection`, `DjangoConnectionField`, `DjangoNodeField`, `DjangoNodesField` — the on-disk `__all__` already exports all four; the first two are pre-existing `DONE-030` drift this slice repairs because it deliberately regenerates the file (recorded for Worker 1 spec reconciliation). Do NOT add `global_id_for` / `decode_global_id` to the testing-subpackage list — they live at `testing.relay` module path and are deliberately not re-exported (Decision 10 export gate).
14. **CardItem fixes (card 32):**
    - Text fixes (worker-0 step 6's named class): `other` order 40 → "New spec: `docs/spec-032-full_relay-0_0_9.md` covering all eight goals above with worked examples and decision rationale."; `other` order 59 → "`docs/spec-032-full_relay-0_0_9.md` (new)".
    - DoD completion (done-card convention): move the 11 items at `other` orders 40–50 to `section=Section(key="definition_of_done")` with `order=0..10` (input order preserved) and `is_complete=True` (matching DONE-029/030/031, whose DoD sections render ticked `- [x]`; card 32's DoD landed mis-sectioned under `other` at import time — verified against the card's original `#### Definition of done` block in git history). The `(card, section, order)` unique constraint is satisfied (target section empty for this card).
15. **Bootstrap one `CardGlossaryTerm`** for card 32 (term `relay-node-integration` — the CSV's first row) so the done-save signal passes; `import_spec_terms` reconciles the full set in step 17.
16. **Status flip:** confirm the existing `SpecDoc` (verified above — do not create a duplicate; `name` is unique), then `card.status = Status.objects.get(key="done"); card.save()` (ORM save fires the pre_save validation and milestone sync). The rendered id auto-becomes `DONE-032-0.0.9` (done cards drop the milestone prefix) — this IS "the next DONE-NNN-0.0.9 id" since NNN is the stable card number.
17. **Sync the full glossary-link set:** `uv run python examples/fakeshop/manage.py import_spec_terms` (repo root; requires step 7's CSV rows and steps 9–10's term rows to already exist — every CSV anchor must resolve to a `GlossaryTerm`).
18. **Regenerate all three docs (repo root):** `uv run python scripts/build_kanban_md.py`, `uv run python scripts/build_kanban_html.py`, `uv run python scripts/build_glossary_md.py`. The regeneration itself removes the two staged TODO anchors that live only in the rendered `docs/GLOSSARY.md` / `KANBAN.md`.
19. **Byte-clean + invariant verification:**
    - Re-run all three build scripts a second time; `git status --short` must show no further change after the second run (proves the DB ⇄ rendered files are in lockstep; the first run's diff is this slice's intended change, reviewed in step 20).
    - `uv run python examples/fakeshop/manage.py import_spec_terms --check` → OK for all done cards.
    - `uv run python examples/fakeshop/manage.py check` → passes.
    - `KANBAN.md` shows `DONE-032-0.0.9` in the Done column exactly once (removed from WIP), with the `#### Definition of done` section rendering 11 ticked `- [x]` items and the Spec line pointing at `docs/spec-032-full_relay-0_0_9.md`.
    - `git diff docs/GLOSSARY.md` contains ONLY: the `DjangoNodeField` Index-row + entry flip/rewrite, the two new entries with Index rows, the two Browse-by-category line changes, the two cross-referenced body extensions (+ the strictness correction), the Public-exports additions, and the staged TODO removal.
    - `grep -rn "TODO(spec-032" --include="*.md" --include="*.py"` over tracked files → only `docs/feedback.md` (descriptive scratch) and `docs/builder/bld-*` artifacts remain; all eight Slice-7 anchors gone; no anchors of other slices existed (verified pre-plan).
20. **Standard close:** `uv run ruff format .` and `uv run ruff check --fix .` (expected no-ops — no `.py` edits); `git status --short` classification in the build report — expected modified set: `GOAL.md`, `README.md`, `TODAY.md`, `CHANGELOG.md`, `docs/README.md`, `docs/TREE.md`, `docs/GLOSSARY.md`, `KANBAN.md`, `KANBAN.html`, `docs/spec-032-full_relay-0_0_9-terms.csv`, `examples/fakeshop/db.sqlite3` (plus the pre-existing Slice 1–6 baseline). **NO version-file edits**: `pyproject.toml`, `django_strawberry_framework/__init__.py` `__version__`, `tests/base/test_init.py::test_version`, `uv.lock` stay at `0.0.8` (Decision 13). Workers never commit — the regenerated docs + `db.sqlite3` are handed to the maintainer.

**Part C — Worker 1-owned steps (final verification; NOT Worker 2's):**

21. Spec edit (spec custody): add link defs `[glossary-djangonodesfield]: GLOSSARY.md#djangonodesfield` and `[glossary-metarelation_shapes]: GLOSSARY.md#metarelation_shapes` to the spec's `<!-- docs/ -->` group; retarget the spec body's `[\`Meta.relation_shapes\`][glossary-djangotype]` uses to `[glossary-metarelation_shapes]` and add at least one `[\`DjangoNodesField\`][glossary-djangonodesfield]` body use; update spec line 5 status ("Slices 1–7 implemented") and the line-9 predecessor sentence ("it does **not** yet carry `DjangoNodesField` or `Meta.relation_shapes` entries" — now stale); re-run `uv run python scripts/check_spec_glossary.py --spec docs/spec-032-full_relay-0_0_9.md` expecting `OK: 40 terms`. Record the CHANGELOG `### Changed`-amendment grant extension, the Public-exports addition, the strictness-sentence correction, the target-tree additions, and the DoD re-sectioning rationale under `### Spec changes made (Worker 1 only)`.
22. Per BUILD.md "Spec stays at its working location": **no spec archival in this card** — verified: the Slice 7 checklist (spec lines 111-120) names no archival/relocation step, so the opt-in lifecycle clause is not triggered. The spec remains at `docs/spec-032-full_relay-0_0_9.md`; the docs/SPECS/NEXT.md Step 8 batched archive pass belongs to the NEXT spec's author.

### Test additions / updates

- **None.** No test files are added or modified by this slice — explicitly: the slice is docs + DB wrap only, and the spec's Slice 7 checklist names no test work (all test contracts shipped with Slices 1–6).
- Verification is command-based (steps 17–19): `import_spec_terms --check`, `manage.py check`, the double-regenerate byte-clean check, and the TODO-anchor grep. Worker 1's final verification additionally runs a focused `uv run pytest --no-cov tests/base/test_init.py` to pin that no version/export drift crept in (the `test_version` 0.0.8 pin and the `__all__` pin must still pass untouched).
- No temp/scratch tests are appropriate; Worker 3's review is a read-through of the doc diffs against the canonical phrasings plus re-running the step-19 verification commands.

### Implementation discretion items

Assessed and decided as Worker 2's choice (style-level only):

- Sentence joinery and ordering inside the rewritten `README.md` status paragraph and the `docs/README.md` "Coming next" line — the pinned content and canonical phrasings are fixed; how the alongside-chain clauses are stitched is discretionary.
- Whether the ORM work runs as one `manage.py shell -c` heredoc or several sequential invocations (idempotence guard recommended: check-before-create on the two new terms so a re-run after a partial failure does not duplicate).
- The `notes` column wording of the two new CSV rows (anchor + term columns are pinned; notes are free-form per the file's style).
- `docs/TREE.md` comment-column wording for the five added tree lines (content pinned above; spacing/abbreviation to match column alignment is discretionary), and whether to refresh the current-layout `__init__.py` comment's parenthetical export list (stale — it predates the connection/relay exports; refreshing it to match the real `__all__` is authorized but optional).
- Exact `order` values for the appended `GlossaryTermLink` rows (must follow the existing per-term sequence; renumbering existing rows is not).

### Spec slice checklist (verbatim)

- [x] [`docs/GLOSSARY.md`][glossary]: flip [`DjangoNodeField`][glossary-djangonodefield] to `shipped (0.0.9)` and rewrite its body for both forms; add net-new `## DjangoNodesField` and `## Meta.relation_shapes` entries (`shipped (0.0.9)`) with Index rows and "Relay" / "Type generation" Browse-by-category entries; extend [Relay Node integration][glossary-relay-node-integration] / [`DjangoConnectionField`][glossary-djangoconnectionfield] cross-references. **These two entries do not exist at spec-authoring time and creating them is out of scope for the [`docs/SPECS/NEXT.md`][next] flow** (see [Risks and open questions](#risks-and-open-questions)) — Slice 7 of the build creates them.
- [x] [`docs/README.md`][docs-readme]: add the root node/nodes fields, the relation-as-Connection upgrade, and the `testing.relay` helpers to the shipped-surface list; update the "Coming next" `0.0.9` line.
- [x] [`docs/TREE.md`][tree]: add [`relay.py`][relay-toplevel] and `testing/relay.py` to the current-layout tree, plus the new test files.
- [x] [`TODAY.md`][today]: products is **not** activated (deferred to [`TODO-BETA-051-0.1.5`][kanban]) — touch only the `0.0.9` `GlobalID` note if it references root `node(id:)` as unshipped (the "nothing decodes a GlobalID until root `node(id:)` ships" sentence becomes stale when this card lands); keep the file products-centric.
- [x] [`README.md`][readme]: update the status paragraph's newest-shipped-surface line.
- [x] [`GOAL.md`][goal]: flip the astronomy typed-node showcase fields `galaxy: GalaxyNode` and `celestial_body: CelestialBodyNode` to `… | None`, so the `1.0.0` showcase models the supported nullable-by-contract spelling rather than the non-optional shape [Decision 5](#decision-5--null-for-invisible-rows-graphqlerror-for-malformed-ids) leaves unsupported ([Decision 4](#decision-4--djangonodefield--djangonodesfield-a-bare-interface-form-and-a-typed-form)).
- [x] [`CHANGELOG.md`][changelog]: `### Added` bullets under `[Unreleased]` for `DjangoNodeField` / `DjangoNodesField`, `Meta.relation_shapes` + the relation-as-Connection upgrade, and `testing.relay`. **This is the per-card CHANGELOG-edit permission grant** ([`AGENTS.md`][agents] withholds it by default); the Slice 7 maintainer prompt must name this edit explicitly. No version-heading promotion (per [Decision 13](#decision-13--version-bumps-are-owned-by-the-joint-009-cut)).
- [x] [`KANBAN.md`][kanban]: move [`WIP-ALPHA-032-0.0.9`][kanban] to the Done column with the next `DONE-NNN-0.0.9` id; confirm the card's spec reference points at [`docs/spec-032-full_relay-0_0_9.md`][spec-032] (this document). (`KANBAN.md` is a generated export — the edit is a `SpecDoc` / card-column DB change re-rendered via `scripts/build_kanban_md.py`.)
- [x] **No version-file edits in this card.** Leave `pyproject.toml`, [`__version__`][package-init], [`tests/base/test_init.py::test_version`][test-base-init], and `uv.lock` to the joint `0.0.9` cut per [Decision 13](#decision-13--version-bumps-are-owned-by-the-joint-009-cut).

### Notes for Worker 1 (spec reconciliation)

Pre-logged by the planning pass for the final-verification spawn (no spec edits made at planning per the read-only rule):

1. **CHANGELOG `### Changed` amendment** (step 6): the spec's grant names only `### Added` bullets, but the `[Unreleased]` `### Changed` bullet carries the same stale latent-break sentence the spec orders fixed in `TODAY.md` — extend the spec's Slice-7 CHANGELOG bullet wording at final verification to record the one-sentence amendment.
2. **Two net-new terms need spec links** for `check_spec_glossary` (Worker 1-owned spec edit, step 21); the checker is expected-red on exactly those two terms between Worker 2's build pass and final verification.
3. **Public-exports glossary doc** (step 13): adds `DjangoNodeField`/`DjangoNodesField` (this card) and repairs pre-existing `DjangoConnection`/`DjangoConnectionField` drift (DONE-030 wrap miss). Spec names "Public exports" nowhere — record as a coherence addition.
4. **`DjangoConnectionField` strictness sentence** (step 11): the shipped glossary body asserts strictness-`"raise"` surfaces nested-connection N+1s — false per spec Revision 6 P2; corrected in the same body edit the spec's cross-reference extension already authorizes.
5. **Card DoD re-sectioning** (step 14): card 32's DoD items were imported under `other`; moving them to `definition_of_done` + ticking matches the done-card convention worker-0.md mandates and the DONE-029/030/031 precedent.
6. **Target-tree additions in `docs/TREE.md`** (step 4): beyond the literal "current-layout trees" wording, for internal coherence of the file.

---

## Build report (Worker 2)

### Files touched

- `GOAL.md` — flipped the two astronomy showcase fields to `GalaxyNode | None` / `CelestialBodyNode | None` per the staged block; removed the Slice-7 TODO block. No other claims changed.
- `README.md` — Status paragraph re-led with the full Relay story (`0.0.9`) as the newest shipped surface (root node/nodes via `DjangoNodeField` / `DjangoNodesField`, relation-as-Connection + `Meta.relation_shapes`, `testing.relay` helpers, using the plan's canonical phrasings); the prior GlobalID-default and `DjangoConnectionField` sentences demoted into the alongside-chain, not deleted; opening `**0.0.8, single-maintainer, alpha-quality.**` untouched (Decision 13). TODO block removed.
- `docs/README.md` — three shipped-surface bullets appended after the `DjangoConnectionField` bullet (root fields / relation upgrade / `testing.relay`, sibling "(new in `0.0.9`)" style with ref-style glossary links); "Coming next" `0.0.9` line shrunk to the `033` remainder per the plan's example wording; `[glossary-djangonodefield]` and `[glossary-metarelation_shapes]` defs added alphabetically under `<!-- docs/ -->`. TODO block removed.
- `docs/TREE.md` — current package tree: `relay.py` added after `connection.py`; `testing/relay.py` added (box chars adjusted). Current tests tree: `test_relay_connection.py` / `test_relay_node_field.py` added among the flat files, `tests/testing/test_relay.py` added. Target trees (planner-authorized coherence extension): `relay.py` after `connection.py`, `relay.py` under target `testing/`, the two flat test files after `test_connection.py`, and a `tests/testing/` entry carrying `test_relay.py`. TODO block removed.
- `TODAY.md` — ONLY the breaking-change note's latent-break sentence replaced with the canonical "lands **live** alongside its consumer" sentence; the three-step migration sequence and products framing byte-identical. TODO block removed.
- `CHANGELOG.md` (THE EXPLICIT PER-CARD GRANT) — three `### Added` bullets prepended under `[Unreleased]` with the plan's canonical texts verbatim (root fields / relation-as-Connection + `Meta.relation_shapes` / `testing.relay`); the `### Changed` first bullet's stale latent-break sentence amended per the planner-authorized one-sentence amendment; `[glossary-get_queryset-visibility-hook]` def added (all other refs the bullets use already existed); NO version-heading promotion; `## [0.0.8]` untouched. TODO block removed.
- `docs/spec-032-full_relay-0_0_9-terms.csv` — `DjangoNodesField` row added after the `DjangoNodeField` row; `Meta.relation_shapes` row added after the `Meta.connection` row (anchor + term pinned by plan; notes per plan suggestion).
- `examples/fakeshop/db.sqlite3` — all Part-B ORM edits (one scripted `manage.py shell` pass, transaction-wrapped, check-before-create idempotence guards): `djangonodefield` flipped to shipped + body rewritten; `djangonodesfield` / `metarelation_shapes` terms created at the plan's pre-verified neighbor placements (24/20 `djangonodesfield`; 49/45 `meta.relation_shapes` tie-band); `relay-node-integration` / `djangoconnectionfield` bodies extended (+ the strictness-sentence correction); see-also `GlossaryTermLink` mirror rows appended; `relay` (10) and `type-generation` (16) category memberships renumbered via the +1000 temp band; public-exports `BoardDoc` gained `DjangoConnection` / `DjangoConnectionField` / `DjangoNodeField` / `DjangoNodesField`; card-32 CardItem text fixes (orders 40, 59 → `spec-032-full_relay-0_0_9.md`); the 11 DoD items re-sectioned from `other` 40–50 to `definition_of_done` 0–10 with `is_complete=True`; one bootstrap `CardGlossaryTerm` (relay-node-integration); `SpecDoc` confirmed (NOT created — pre-existing row verified); status flipped to done via ORM `card.save()` → rendered id `DONE-032-0.0.9`; `import_spec_terms` reconciled the full 40-term link set.
- `KANBAN.md` / `KANBAN.html` / `docs/GLOSSARY.md` — regenerated (never hand-edited) via the three build scripts; the two rendered-file-only staged TODO anchors disappeared in regeneration as planned.

### Tests added or updated

- None, per the plan ("Test additions / updates: None" — all-docs wrap slice; test contracts shipped with Slices 1–6).

### Validation run

- `uv run ruff format .` — pass (241 files unchanged; no `.py` edits this slice).
- `uv run ruff check --fix .` — pass (no violations).
- `git status --short` after both ruff runs — classification:
  - **Slice-intended (this pass):** `CHANGELOG.md`, `GOAL.md`, `README.md`, `TODAY.md`, `docs/README.md`, `docs/TREE.md`, `docs/spec-032-full_relay-0_0_9-terms.csv`, `examples/fakeshop/db.sqlite3`, `KANBAN.md`, `KANBAN.html`, `docs/GLOSSARY.md` — exactly the plan step-20 expected set.
  - **Slices 1–6 baseline (untouched by this pass):** the 9 modified `django_strawberry_framework/**.py` files, `examples/fakeshop/apps/library/schema.py`, `examples/fakeshop/test_query/test_library_api.py`, `examples/fakeshop/tests/test_inspect_django_type.py`, and the 7 modified test files incl. `tests/base/test_init.py` — verified identical to the pass-start snapshot.
  - **Build-cycle artifacts:** `docs/spec-032-full_relay-0_0_9.md` (modified in the Slices 1–6 baseline; NOT edited by Worker 2), the 8 pre-flight `D docs/builder/*` deletions, the untracked `bld-slice-*.md` artifacts + `build-032-*.md` plan, and this artifact.
  - No unrelated tool churn; nothing reverted.
- Slice-mandated verification commands (plan steps 17–19), all from repo root:
  - `uv run python examples/fakeshop/manage.py import_spec_terms` — "Imported glossary terms for 32 done card(s)."
  - `uv run python examples/fakeshop/manage.py import_spec_terms --check` — "OK: 32 done cards have glossary links."
  - `uv run python examples/fakeshop/manage.py check` — "System check identified no issues (0 silenced)."
  - Double-regenerate byte-clean: md5 of `KANBAN.md` / `KANBAN.html` / `docs/GLOSSARY.md` identical across a second run of all three build scripts (DB ⇄ rendered lockstep proven).
  - `KANBAN.md`: `### [DONE-032-0.0.9 …]` appears exactly once, in `## Done` (newest-first top slot); `WIP-ALPHA-032` heading count 0; the card's `#### Definition of done` renders 11 ticked `- [x]` items; Spec line points at `docs/spec-032-full_relay-0_0_9.md`.
  - `git diff docs/GLOSSARY.md` — 9 hunks, all within the planned change-set (public-exports additions, Index flip + 2 new rows, 2 Browse-line changes, `DjangoNodeField` rewrite, 2 new entries, the 2 cross-ref body extensions + strictness correction, staged-TODO removal). Nothing else.
  - TODO-anchor grep: all eight staged Slice-7 anchors gone (GOAL/README/TODAY/CHANGELOG/docs-README/TREE removed by edit; GLOSSARY/KANBAN removed by regeneration). Remaining `TODO(spec-032` matches: `docs/feedback.md` (descriptive scratch, untouched per plan), `docs/builder/bld-*` artifacts (scratch), and the spec's own Revision-history/Implementation-plan prose at `docs/spec-032-full_relay-0_0_9.md:13/29/512` (descriptive mentions of the anchor convention, not staged anchors — pre-existing, spec is Worker 1 custody).
  - `scripts/check_spec_glossary.py --spec docs/spec-032-full_relay-0_0_9.md` — **expected-RED on exactly the two net-new terms** (`DjangoNodesField`, `Meta.relation_shapes` flagged spec-unlinked), exit 1. This is the planned interim state until Worker 1's final-verification spec edit (plan step 21) adds the body links; per the plan, Worker 3 should not treat it as a build defect.
- No `pytest` run (plan instructs none for Worker 2; Worker 1's final verification owns the focused `tests/base/test_init.py` run). No version-file edits: `pyproject.toml`, `__version__` (still `0.0.8`), `tests/base/test_init.py::test_version`, `uv.lock` all untouched by this pass (Decision 13).

### Implementation notes

- ORM work ran as ONE scripted `manage.py shell < script` pass (discretion item) wrapped in `transaction.atomic()` with `get_or_create` / membership-assert guards, so a partial failure rolls back whole and a re-run is a no-op.
- `docs/TREE.md` current-tests-tree placement: the two new flat test files went after `test_registry.py` (the files' actual `ls`-alphabetical slot: `test_registry` < `test_relay_*` < `testing/`) rather than literally "beside `test_connection.py`" as the plan's navigational hint said; `tests/testing/test_relay.py` likewise sits before `test_wrap.py` alphabetically. Content pinned by the plan is unchanged.
- Target tests tree had no `tests/testing/` directory at all; added a minimal `testing/` entry carrying only `test_relay.py` (the plan's "three test files") rather than back-filling `test_wrap.py` — the missing mirror for the shipped `testing/_wrap.py` is pre-existing drift outside this slice.
- `DjangoConnectionField` body: the relation-as-Connection cross-reference sentence was appended at the end of the first (machinery) paragraph — the plan pinned the sentence, not its position; the strictness correction lives in the second paragraph where the stale sentence was.
- See-also `GlossaryTermLink` mirror rows for the rewritten `djangonodefield` entry: the two pre-existing rows (orders 0–1) were kept and the two net-new targets appended at orders 2–3 per the plan's "appended order values / no renumbering" rule, so mirror-row order intentionally differs from the rendered See-also label order on that one entry (renderer reads the body, not the link rows).
- Public-exports BoardDoc one-liners for the four added symbols are new prose (the plan pinned the symbol set, not descriptions); kept to the sibling list's "— what it is" single-clause shape.
- CSV `notes` column wording taken verbatim from the plan's suggested text (discretion item).
- The optional current-layout `__init__.py` comment-parenthetical refresh in `docs/TREE.md` (discretion item) was NOT taken — minimal-diff bias for a wrap slice; the stale parenthetical predates this card.

### Notes for Worker 3

- No shadow files used (`review_inspect.py` skip recorded in the plan; no `.py` gained logic).
- Review is a read-through of the doc diffs against the plan's canonical phrasings + re-running the step-19 commands. The three canonical CHANGELOG `### Added` bullets and the two new glossary bodies were transcribed from the plan verbatim (one mechanical adaptation: the plan's block-quoted body text is stored in the DB without the `>` markers).
- `scripts/check_spec_glossary.py` exit 1 on exactly `djangonodesfield` / `metarelation_shapes` is the planned expected-RED window — not a defect (plan step 7 note).
- The `git diff KANBAN.md` is large (~300 lines) because the card block physically moved from WIP to the top of Done and the progress table recomputed (32/57 done, 56.1%); all of it is renderer output from the DB flip.

### Notes for Worker 1 (spec reconciliation)

(Adding to the planning pass's pre-logged items 1–6, which all still stand and were implemented as planned.)

7. **Stale `WIP-ALPHA-032-0.0.9` reference inside another card's rendered body** — `KANBAN.md:186` (card 033's dependency note) still says "the fakeshop products connections-only conversion tracked under `WIP-ALPHA-032-0.0.9`". That CardItem text lives on card 033 (not card 32), was not in this slice's named fix class (plan step 14 named only card 32's two spec-filename items), and the card-id it cites looks doubly stale (the products conversion is `TODO-BETA-051-0.1.5`). Left untouched; flagging for a Worker 1 decision (DB text fix candidate for final verification or the 033 wrap).
8. **Board-preamble cohort sentence** — `KANBAN.md:64` reads "the Relay connection cohort is in progress as four WIP cards — `DONE-030-…`, `DONE-031-…`, `DONE-032-…`, and `WIP-ALPHA-033-…`": the "four WIP cards" framing predates this slice (it already listed two DONE ids) and the rendered ids update automatically; the prose wording is BoardDoc text outside this slice's scope. Flagging only because this flip made the mismatch one card larger.
9. **TREE.md alphabetical placement + minimal target `tests/testing/` entry** — see Implementation notes; mechanically obvious, recorded here per the drift rule so final verification can confirm or adjust.

## Review (Worker 3)

Review performed 2026-06-11. All-docs wrap slice: review was a read-through of every per-file diff against the plan's canonical phrasings plus an independent re-run of the plan step-17–19 verification commands and a read-only ORM probe of the DB invariants. `review_inspect.py`: **skipped — recorded reason:** no `.py` file gained logic this slice (the slice-intended diff is `*.md` + the terms CSV + `db.sqlite3` row data only; verified via `git status` classification against the build report). No temp tests created (nothing behavioral to probe; verification is command-based per the plan).

### High:

None.

### Medium:

None.

### Low:

#### Pre-existing CHANGELOG link rot: `[spec-orders]` def points at a moved file

`CHANGELOG.md:281` (`[spec-orders]: docs/spec-028-orders-0_0_8.md`) targets a file archived to `docs/SPECS/spec-028-orders-0_0_8.md`. **Verified pre-existing at HEAD** (`git show HEAD:CHANGELOG.md` carries the identical def at line 291; the slice diff does not touch it), so it is NOT a defect of this slice — but the slice deliberately updated `CHANGELOG.md`, and the review's link-target sweep is what surfaced it. Disposition: not chargeable to Worker 2 (the slice's CHANGELOG grant covers the `### Added` bullets + the one amended sentence + one new def, all clean); escalated under Notes for Worker 1 as a one-line def re-relativization candidate (`docs/SPECS/spec-028-orders-0_0_8.md`) for final verification or the next archive sweep.

### DRY findings

- None new. The slice's whole design is anti-duplication: every cross-surface restatement of the Relay contract was transcribed from ONE pinned canonical source (plan steps 5/6/8–11), and I verified the load-bearing copies **character-for-character programmatically** — the three CHANGELOG `### Added` bullets, the three glossary bodies (`djangonodefield` rewrite, `djangonodesfield`, `metarelation_shapes`), the two cross-ref body extension sentences + the strictness correction, the TODAY/CHANGELOG latent-break replacement pair (the two copies are in lockstep as the plan required), and both CSV rows — all MATCH, zero paraphrase drift.
- Rendered-file duplication (Index rows, Browse lines, See-also tails appearing in both `db.sqlite3` and `docs/GLOSSARY.md`) is the generated-docs architecture, not a DRY defect; authorship was DB-only (see generated-files check below).

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` shows ONLY the accepted Slice-2 change: `from .relay import DjangoNodeField, DjangoNodesField`, the two `__all__` entries (alphabetical slot), and the staged Slice-2 TODO removal — spec-authorized (spec DoD item 4 / Slice-2 checklist). `__version__ = "0.0.8"` unchanged. **No NEW change from Slice 7** (per the standing posture: the diff is permanently non-empty since Slice 2; the check is "no new change", which holds).

### CHANGELOG sanity

Read the full `[Unreleased]` entry end-to-end post-diff:

- **Version line NOT promoted** (Decision 13): no `## [0.0.9]` heading exists; bullets live under `## [Unreleased]`; `## [0.0.8]` and everything below byte-untouched (diff confirms). `pyproject.toml` / `__version__` / `uv.lock` diffs empty (`tests/base/test_init.py` diff is the accepted Slice-2 `__all__` pin only; `test_version` untouched).
- **Headings spec-authorized**: the slice adds bullets under the existing `### Added` only (the spec's named grant) plus the one-sentence `### Changed` amendment the plan explicitly authorized and pre-logged for Worker 1's grant-extension (planning note 1). No new heading introduced.
- **Wording**: all three `### Added` bullets compared programmatically against the plan's canonical texts — character-for-character MATCH. The `### Changed` amendment matches the plan's canonical replacement sentence exactly and stays in lockstep with TODAY.md's copy.
- **Nothing over/understated**: bullet claims cross-checked against shipped behavior from Slices 2/3/5 review passes (null-for-hidden/missing/uncoercible, whole-field fail on malformed/wrong-type in batch, per-type batching + duplicates, finalization ledger error, the `"connection"`/`"list"` suppression semantics, the secondary-emitter decode-to-primary asymmetry, the testing-subpackage export gate). The new `[glossary-get_queryset-visibility-hook]` def resolves (heading exists in `docs/GLOSSARY.md`), placed alphabetically in the `<!-- docs/ -->` group; all other refs the bullets use had pre-existing defs (verified by a full use→def resolution sweep; the three apparent "missing defs" the sweep flagged are inline-code false positives at lines 65/103/133, and the one broken def path is the pre-existing `[spec-orders]` rot recorded under Low).

### Documentation / release sanity

- **Version strings / statuses / card IDs**: README opener stays `**\`0.0.8\`, single-maintainer, alpha-quality.**`; GLOSSARY header stays `Current package version: \`0.0.8\``; the three flipped/new glossary entries read `shipped (\`0.0.9\`)` in Status line AND Index rows; `Connection-aware optimizer planning` correctly remains `planned for \`0.0.9\`` (card 033). All card IDs in deliberately-updated files use `DONE-032-0.0.9`.
- **KANBAN movement**: `### [DONE-032-0.0.9 …]` heading appears exactly once, inside `## Done` (top slot); the `## In progress` column carries only `WIP-ALPHA-033-0.0.9`; zero `WIP-ALPHA-032` headings remain. DB probe: card 32 `status.key="done"`, 40 glossary links (the 38+2 CSV rows), `definition_of_done` section = 11 items at orders 0–10 all `is_complete=True`, zero leftover items at `other` 40–50; the rendered DoD shows 11 ticked `- [x]`; Spec line + spec-map row + CardItem text fixes all point at `docs/spec-032-full_relay-0_0_9.md`. Progress table recomputed to 32/57 (56.1%) consistently.
- **Generated files NOT hand-edited**: independently re-ran all three build scripts; `git diff --stat` and the GLOSSARY hunk count (9) are identical before/after — the DB and the three rendered files are in byte-lockstep, so every rendered change is renderer output of the DB edits. `git diff docs/GLOSSARY.md` is exactly the planned change-set (public-exports x4, Index flip + 2 new rows, 2 Browse lines, `DjangoNodeField` rewrite + TODO removal, 2 new entries, relay-node-integration + `DjangoConnectionField` extensions + strictness correction). `import_spec_terms --check` → "OK: 32 done cards have glossary links."; `manage.py check` → no issues.
- **Expected-RED confirmed exact**: `check_spec_glossary.py --spec …` exits 1 flagging precisely `DjangoNodesField` and `Meta.relation_shapes` as spec-unlinked — nothing else red. Matches the plan's recorded interim state (Worker 1's step-21 spec edit closes it); not a build defect.
- **Link-target spot check on every NEW def**: `docs/README.md` `[glossary-djangonodefield]` / `[glossary-metarelation_shapes]` and `CHANGELOG.md` `[glossary-get_queryset-visibility-hook]` all resolve to existing `docs/GLOSSARY.md` headings; defs sit alphabetically under the correct `<!-- docs/ -->` canonical group header (convention upheld). In-page anchors used by the new glossary bodies (`#djangonodesfield`, `#metarelation_shapes`, `#get_queryset-visibility-hook`, `#configurationerror`, `#metaconnection`, `#metaglobalid_strategy`, `#relay-node-integration`, `#djangoconnectionfield`, `#djangotype`, `#strictness-mode`, `#connection-aware-optimizer-planning`) all have matching headings. Full use→def sweep over all six edited markdown files: zero genuinely missing defs, zero broken def paths introduced.
- **Verbatim drop-ins**: see DRY findings — all pinned texts diffed character-for-character against the plan's canonical sources; all MATCH. No fenced-code drop-ins with matching inner/outer backtick counts in this slice.
- **No stale wording**: the stale "latent — nothing decodes a `GlobalID` until root `node(id:)` ships (`WIP-ALPHA-032-0.0.9`)" claim is gone from BOTH carriers (TODAY.md + CHANGELOG `### Changed`); grep confirms no `break is **latent**` remains; the `DjangoNodeField` "planned for `0.0.9`" Index row and entry body are flipped; the docs/README "Coming next" `0.0.9` line correctly shrinks the remainder to 033. All eight staged `TODO(spec-032 … Slice 7)` anchors are gone (six by edit, two by regeneration); remaining `TODO(spec-032` matches are only `docs/feedback.md` (descriptive scratch, plan says leave), `docs/builder/bld-*` artifacts (scratch), and the spec's own descriptive prose (Worker 1 custody) — exactly the build report's claim.
- **DB invariants (read-only ORM probe)**: `djangonodesfield` at `entry_order=24,index_order=20,title_sort="djangonodesfield"` (neighbor-tie after `djangonodefield`); `metarelation_shapes` at `49/45,"meta.relation_shapes"` (the `Meta.primary` tie band); `relay` category = 10 members and `type-generation` = 16 members in exactly the plan's pinned orders; See-also `GlossaryTermLink` mirror rows present for all five touched/created entries with net-new targets appended (orders 2–3 on `djangonodefield`, 4 on `relay-node-integration` / `djangoconnectionfield`) per the plan's appended-order/no-renumber rule — the rendered tail order diverges from row order on those entries by design (renderer reads the body; Worker 2's implementation note covers it).

### What looks solid

- The verbatim-transcription discipline: every canonical phrasing landed byte-exact across five surfaces with zero paraphrase drift — programmatically provable, and proven.
- The DB-backed procedure was followed to the letter: confirm-don't-create on the pre-existing `SpecDoc`, the +1000 temp-band renumber, the bootstrap `CardGlossaryTerm` before the status flip, ORM-only edits, and a regeneration that is independently reproducible byte-for-byte from the shipped DB.
- The DoD re-sectioning (11 items, `other` 40–50 → `definition_of_done` 0–10, ticked) matches the DONE-029/030/031 precedent exactly and renders correctly.
- Worker 2's Notes for Worker 1 items 7–9 are accurate as written — I verified the stale `WIP-ALPHA-032` reference in card 033's body (`KANBAN.md:186`), the "four WIP cards" preamble drift (`KANBAN.md:64`), and the TREE.md alphabetical placements (correct `ls`-order, content pinned by the plan unchanged).

### Temp test verification

- None created. All-docs slice; verification was command-based (the step-17–19 re-runs above) plus two throwaway read-only Python probes run inline from stdin (verbatim text comparison; link-def resolution sweep) and one read-only `manage.py shell` ORM inspection — nothing written to `docs/builder/temp-tests/slice-7/`, so there is no disposition to record beyond "no temp tests".

### Notes for Worker 1 (spec reconciliation)

- The planning pass's pre-logged items 1–6 and Worker 2's items 7–9 all still stand; I independently verified 7–9 (see What looks solid).
- **New (Low above):** `CHANGELOG.md` bottom-block def `[spec-orders]: docs/spec-028-orders-0_0_8.md` is pre-existing link rot (target archived to `docs/SPECS/`; verified identical at HEAD). One-line re-relativization candidate for final verification or the next spec author's Step-8 sweep — outside this slice's CHANGELOG grant, so left untouched.
- Reminder of the two Worker 1-owned step-21/22 items: the spec link-def edit that turns `check_spec_glossary` green on the two net-new terms (expected `OK: 40 terms`), and the spec line-5 / line-9 status-sentence refresh. The expected-RED window is exactly as the plan recorded.

### Review outcome

`review-accepted` — zero High/Medium findings; the single Low is a verified pre-existing defect with a recorded disposition (escalated to Worker 1, not chargeable to this slice). All 9 spec slice checklist boxes verified landed (no over-tick, no silent omission); CHANGELOG sanity and Documentation / release sanity both performed in full; public-surface check performed; generated files proven regeneration-pure.

## Final verification (Worker 1)

Performed 2026-06-11 (fresh spawn; on-disk artifacts only).

- **Part C executed (plan steps 21–22).** Spec edits made (full list under `### Spec changes made (Worker 1 only)`): the two link defs + four `[glossary-djangotype]` retargets + one `DjangoNodesField` body use landed, and `uv run python scripts/check_spec_glossary.py --spec docs/spec-032-full_relay-0_0_9.md` now exits **0** with `OK: 40 terms - all have glossary entries and at least one spec link.` — the recorded expected-RED window is closed. Spec line 5 refreshed to "all seven slices implemented … build complete pending the cross-slice integration pass and the final test-run gate"; the line-9 predecessor staleness fixed. Step 22 confirmed: no spec archival in this card (the Slice 7 checklist names none; the spec stays at `docs/spec-032-full_relay-0_0_9.md` for the next author's Step-8 sweep).
- **Spec slice checklist audit (9 boxes, all ticked):** every box independently re-verified against the on-disk state, not just the build/review reports — GOAL.md both showcase fields now `… | None` (lines 148/151); README.md status paragraph re-led with the full Relay story, `**0.0.8, single-maintainer, alpha-quality.**` opener intact; docs/README.md three "(new in `0.0.9`)" bullets + the shrunk-to-033 "Coming next" line; docs/TREE.md carries `relay.py` x2 trees + `testing/relay.py` x2 + the three test files in both tests trees; TODAY.md + CHANGELOG `### Changed` both carry the canonical lands-live sentence with zero `latent` remnants; CHANGELOG `### Added` three bullets present, no `## [0.0.9]` heading anywhere; KANBAN `DONE-032-0.0.9` heading in Done, 11 ticked DoD items, spec line correct; terms CSV rows 5/12 present; version files untouched (`__version__ = "0.0.8"`, `pyproject.toml`/`uv.lock` diff-empty). No over-tick, no silent omission, no deferral needed.
- **DRY check (Slices 1–7):** no new duplication. The wrap slice's cross-surface restatements were all transcribed from single pinned canonical sources (Worker 3 verified character-for-character; spot-confirmed). The standing integration ledger carries unchanged (gate-tail 3x, `.Meta.interfaces ` prefix, fifth-guard wording, test GraphQL literals, Worker 3's Slice-2 stale-docstring Low) — all owned by the integration pass, none worsened by this slice.
- **Escalation rulings** (the build report's items 7–9 + the review's Low):
  1. **Card 033 body stale citation (`KANBAN.md` line 186, CardItem text on card 033)** — "the fakeshop products connections-only conversion tracked under `WIP-ALPHA-032-0.0.9`" is hardcoded stale text, wrong twice (the id now renders `DONE-032-0.0.9`, and the products conversion is tracked under `TODO-BETA-051-0.1.5` (fakeshop activation), per spec line 115 / TODAY.md / KANBAN card 051). **Ruling: recorded deferral — NOT in-scope for this slice** (the spec's Slice-7 contract names only card 32's wrap; this is card-033 content, pre-existing drift). Dispatch to Worker 2 at this build's **cross-slice integration pass** (in-cycle, ahead of the joint cut — the established Worker-2-at-integration channel). Exact fix: ORM-edit the card-033 CardItem to "…tracked under `TODO-BETA-051-0.1.5` (fakeshop activation)…", then regenerate all three docs. Fallback owner if integration cannot carry DB edits: card 033's own wrap slice (same `0.0.9` cut — nothing releases with the stale text either way).
  2. **Board-preamble "four WIP cards" prose (`KANBAN.md` line 64, BoardDoc text)** — pre-existing drift (it already listed two DONE ids before this slice; the flip made it three). **Ruling: recorded deferral, same channel as item 1** (one ORM pass fixes both). Exact fix: reword the cohort sentence to reflect three done + one in progress, e.g. "…the Relay connection cohort is nearly complete — `DONE-030-0.0.9` (…), `DONE-031-0.0.9` (…), and `DONE-032-0.0.9` (…) have shipped; `WIP-ALPHA-033-0.0.9` (connection-aware optimizer planning) remains in progress." Because neither item is in this slice's contract and both have a guaranteed in-cycle owner, this does **not** trigger `revision-needed`.
  3. **TREE.md alphabetical placement + minimal target `tests/testing/` entry** — **Ruling: confirmed correct as built.** The plan's "beside `test_connection.py`" was a navigational hint; true `ls`-alphabetical order is the file's convention. The minimal target `testing/` entry (only `test_relay.py`, no back-filled `test_wrap.py` mirror) is accepted; the missing `test_wrap.py` mirror is pre-existing drift — noted for the integration pass as an optional one-line back-fill, not required.
  4. **Pre-existing CHANGELOG `[spec-orders]` link rot (line 281)** — verified: `docs/spec-028-orders-0_0_8.md` does not exist; the file lives at `docs/SPECS/spec-028-orders-0_0_8.md`; identical def at HEAD (pre-existing, a prior Step-8 sweep miss). **Ruling: fix in-cycle, but not by this pass** — it is outside the spec's Slice-7 CHANGELOG grant and AGENTS.md withholds CHANGELOG edits absent explicit instruction; Worker 1 performing it ad hoc would breach the same discipline the grant exists to protect. Dispatched to Worker 2 at the integration pass alongside items 1–2 (exact fix: re-relativize the def to `docs/SPECS/spec-028-orders-0_0_8.md` — one line, deterministic), rather than relying on the next spec author's future Step-8 sweep. This honors the root-cause posture (the rot is fixed before this cycle hands off) without scope-creeping the wrap slice.
- **Commands run (all from repo root, all green):**
  - `uv run python examples/fakeshop/manage.py check` — "System check identified no issues (0 silenced)."
  - `uv run python examples/fakeshop/manage.py import_spec_terms --check` — "OK: 32 done cards have glossary links."
  - `uv run pytest tests/testing/ examples/fakeshop/apps/library/tests/ tests/base/test_init.py --no-cov` — **23 passed** (includes the plan-mandated `tests/base/test_init.py` pin: `test_version` still `0.0.8`, `__all__` pin green).
  - `uv run python scripts/check_spec_glossary.py --spec docs/spec-032-full_relay-0_0_9.md` — exit 0, "OK: 40 terms".
  - TODO-anchor grep: remaining `TODO(spec-032` mentions are only `docs/feedback.md` (descriptive scratch), `docs/builder/bld-*` artifacts, and the spec's own descriptive prose — zero staged anchors anywhere.

### Summary

Slice 7 shipped the all-docs card-completion wrap for `DONE-032-0.0.9`: the six plain-markdown doc surfaces updated from pinned canonical phrasings (GOAL/README/docs-README/TREE/TODAY/CHANGELOG under the explicit per-card grant, no version promotion), the two net-new glossary entries (`DjangoNodesField`, `Meta.relation_shapes`) plus the `DjangoNodeField` shipped-flip authored DB-side and regenerated byte-clean, the card flipped to Done with its 11-item DoD re-sectioned and ticked, and the terms CSV extended to 40 terms. This final pass closed the planned expected-RED `check_spec_glossary` window with the Worker 1-owned spec link edits, reconciled the spec (status line, predecessor staleness, grant extension, and the four pre-logged coherence recordings), audited all 9 checklist boxes against the diff, and dispatched the three out-of-scope escalations (card-033 stale citation, board-preamble cohort prose, CHANGELOG `[spec-orders]` rot) to Worker 2 at the in-cycle integration pass with exact fix specs. Status: **final-accepted**.

### Spec changes made (Worker 1 only)

All in `docs/spec-032-full_relay-0_0_9.md`, this pass:

1. Link-definitions block: added `[glossary-djangonodesfield]: GLOSSARY.md#djangonodesfield` and `[glossary-metarelation_shapes]: GLOSSARY.md#metarelation_shapes` alphabetically in the `<!-- docs/ -->` group — the two net-new terms now resolve (checker green, 40 terms).
2. Lines 5/57/149/259: retargeted the four `[`Meta.relation_shapes`][glossary-djangotype]` body uses to `[glossary-metarelation_shapes]` (the placeholder def predated the entry's existence).
3. Line 5 status: "Slices 1–6 implemented …; Slice 7 not started" → "all seven slices implemented … build complete pending the cross-slice integration pass and the final test-run gate" (+ Slice-7 parenthetical: card is `DONE-032-0.0.9`).
4. Line 9 predecessor sentence: the stale "does **not** yet carry `DjangoNodesField` or `Meta.relation_shapes`" claim rewritten to past-tense spec-authoring-time framing; gains the required `[glossary-djangonodesfield]` body use.
5. Slice-7 checklist CHANGELOG bullet (line ~118) + Doc-updates CHANGELOG bullet (line ~620): grant extension recorded — the one-sentence `### Changed` latent-break amendment is now named in the grant (planning note 1).
6. Doc-updates TREE bullet (line ~616): target-tree additions recorded as the planner-authorized coherence extension (planning note 6).
7. Doc-updates GLOSSARY bullet (line ~613): recorded the `DjangoConnectionField` stale-strictness-sentence correction (Revision 6 P2) and the Public-exports board-doc additions incl. the `DONE-030` `DjangoConnection`/`DjangoConnectionField` drift repair (planning notes 3–4).
8. Doc-updates KANBAN bullet (line ~622): recorded the DoD re-sectioning (`other` 40–50 → `definition_of_done` 0–10, ticked, per the done-card convention) and the two spec-filename CardItem text fixes (planning note 5).
