# Build: Slice 7 — doc updates + card-completion wrap

Spec reference: `docs/spec-033-connection_optimizer-0_0_9.md` (lines 85-86; `## Doc updates` lines 541-554; DoD items 11-12 lines 624-625; Decision 12 lines 412-418)
Status: final-accepted

## Plan (Worker 1)

Slice 7 is the doc + card-completion wrap. It ships **no `.py` source logic** and **no new tests** (0 new tests). It is the only slice authorized to touch `CHANGELOG.md` (the explicit per-card permission grant, spec line 543 / build-plan flag at `build-033-…md` line 19) and the only slice that performs DB-backed generated-doc work.

Two work classes, kept strictly separate below:

- **(a) DB-backed work** — `docs/GLOSSARY.md`, `KANBAN.md`, `KANBAN.html` are RENDERED from `examples/fakeshop/db.sqlite3` by `scripts/build_glossary_md.py` / `scripts/build_kanban_md.py` / `scripts/build_kanban_html.py`. They are **never hand-edited** — a hand-edit is silently reverted by the next regenerate, and a raw SQL insert skips the `post_save` `UUIDModel` side-row the GraphQL render needs (`apps/kanban/signals.py::create_uuid_row`). Edit the DB **via the Django ORM** (`manage.py shell` `.save()` / `.objects.create()` / `import_spec_terms`), then regenerate.
- **(b) Hand-authored docs** — `docs/README.md`, `docs/TREE.md`, `TODAY.md`, `README.md`, `CHANGELOG.md` are plain markdown, edited directly (reference-style cross-file link convention per `START.md`).

### Static inspection helper

**Skipped.** `scripts/review_inspect.py` parses `.py` source as text/AST; Slice 7 touches no `.py` source (it edits markdown and the kanban/glossary DB via the ORM in a `manage.py shell` session). Per `BUILD.md` "When to run the helper during build", the helper applies to `.py` files with review-worthy logic — none here. Skip recorded with reason: doc/DB-only slice, zero Python logic added.

### DRY analysis

- **Existing patterns / canonical phrasings reused (not re-invented).** The doc edits reuse the spec's own canonical phrasings rather than coining new ones:
  - The GLOSSARY body rewrites draw their mechanism vocabulary from spec `## Doc updates` line 546, Decision 4 (`docs/spec-033-…md` lines 267-289), Decision 5 (290-321), Decision 6 (322-339), Decision 7 (340-359), Decision 8 (360-378) — windowed `Prefetch` under a package-reserved `to_attr`, the annotation fast path, the four-shape fallback matrix (sidecar input / divergent aliases / `SKIP` hint / `.distinct()` target / divergent-secondary-type `relation_shapes`), and the strictness contract.
  - The CHANGELOG bullets reuse the phrasing this plan commits to in `### Implementation steps` step B5 below, so the changelog wording, the GLOSSARY body, and the README/TODAY prose all tell ONE coherent story (Worker 3's "CHANGELOG sanity" + "Documentation/release sanity" both cross-check this).
  - The `Connection-aware optimizer planning` rewrite mirrors the already-shipped sibling-entry shapes (`DjangoConnectionField` GLOSSARY:290-298, `Meta.relation_shapes` GLOSSARY:828-834) for tone/depth — the predecessor `spec-032` voice the spec names as its reference.
- **New helpers justified.** None. Slice 7 introduces no code, no helper, no constant. The card-move uses the established `import_spec_terms` command and the three existing `build_*` scripts.
- **Duplication risk avoided.**
  - **The render-vs-source trap.** The naive failure mode is hand-editing `docs/GLOSSARY.md` / `KANBAN.md` / `KANBAN.html` directly. That duplicates state (DB vs rendered file) and is silently reverted on the next regenerate. The plan forbids it: every line of those three files is a regenerated output, edited only at its DB source.
  - **GLOSSARY body double-source.** The body text must live ONLY on the `GlossaryTerm.body` DB column, never additionally pasted into the rendered `.md`. One source: the DB row.
  - **CHANGELOG version-heading duplication.** No `## [0.0.9]` heading is created (Decision 12) — bullets land under the existing `## [Unreleased]` only, so the joint-cut release act is not duplicated into a card slice.

### Implementation steps

Line numbers are pin-at-write-time navigational hints; verify against current source before editing.

#### (a) DB-backed work — ORM edits in `examples/fakeshop/db.sqlite3`, then regenerate

All ORM operations run in a single `uv run python examples/fakeshop/manage.py shell` session (or an idempotent `shell -c` script). The DB is git-tracked, so the whole DB-backed pass is reversible via `git checkout -- examples/fakeshop/db.sqlite3`.

**A1 — GLOSSARY body rewrites + the one status flip (spec lines 545-546, DoD item 11).** Edit `GlossaryTerm` rows by `anchor`. Verified DB facts at plan time: `GlossaryStatus` lookup has exactly two keys, `shipped` and `planned`; the five target rows currently read as noted below.

1. **`anchor="connection-aware-optimizer-planning"`** (currently `status=planned`, `status_text="planned for \`0.0.9\`"`) — **flip to shipped**:
   - `term.status = GlossaryStatus.objects.get(key="shipped")`
   - `term.status_text = "shipped (\`0.0.9\`)"`
   - **rewrite `term.body`** to describe the shipped mechanism: the optimizer recognizes synthesized `<field>Connection` selections via definition metadata (`DjangoTypeDefinition.relation_connections`, never `DjangoConnectionField` internals) and plans a windowed `Prefetch` under a package-reserved `_dst_<field>_connection` `to_attr` (`RowNumber()` / `Count(1)` window functions partitioned by the parent key, slice window from the resolved pagination args capped by `relay_max_results`); the generated connection class serves `edges` / cursors / `pageInfo` / `totalCount` from the `_dst_row_number` / `_dst_total_count` annotations on the fast path, with a per-parent fallback for ambiguous empty windows; the **fallback matrix** — a nested connection carrying `filter:` / `orderBy:` sidecar input, divergent aliased duplicates, an `OptimizerHint.SKIP` relation, a `.distinct()` target, or a divergent secondary-type `relation_shapes` — is left unplanned and falls back per parent; **strictness** now flags an unplanned, unserved nested-connection access (`warn` / `raise`). Note the **`.distinct()`-target limitation alongside the existing window-backend caveat** (window functions evaluate before `DISTINCT`, so a distinct child is conservatively unplanned; an exotic backend without window support raises its own `NotSupportedError`, recourse `relation_shapes={"<field>":"list"}` or no optimizer). Keep the `**See also:**` line.
   - `term.save()`
2. **`anchor="djangoconnectionfield"`** (status stays `shipped (\`0.0.9\`)`) — **body rewrite only**: sweep the pre-`033` caveat sentences out of the second paragraph (GLOSSARY:296) — remove "In `0.0.9` the derived plan is **empty**…" and the "A [Strictness mode] `"raise"` run does **not** flag an unplanned nested-connection access…" sentences. Replace with the shipped posture: the field's optimizer cooperation point now also feeds nested `<field>Connection` window planning, and strictness flags unplanned nested-connection access. Leave the first paragraph and the `**See also:**` line intact.
3. **`anchor="metarelation_shapes"`** (status stays `shipped (\`0.0.9\`)`) — **body rewrite only**: drop the per-parent-lazy-load caveat for the synthesized `<field>Connection` siblings (they now window-plan through Slices 1–2). **The `SyncMisuseError` async-`get_queryset` caveat STAYS** (async connection pipeline is still out of scope, spec Non-goals line 125) — keep the final sentence about narrowing with `relation_shapes={"<field>":"list"}`.
4. **`anchor="strictness-mode"`** (status stays `shipped (\`0.0.3\`)`) — **body rewrite only**: add that connection paths now participate — an unplanned, unserved nested-connection access fires the same `OptimizerError` (`"raise"`) / logged warning (`"warn"`) contract through the same resolver-key vocabulary list relations use.
5. **`anchor="plan-cache"`** (status stays `shipped (\`0.0.3\`)`) — **body rewrite only**: refine the "Variable filtering" property (GLOSSARY:953) — variables feeding **nested** connection pagination (`first`/`last`/`before`/`after`) are now hashed into the key (their values are baked into windowed prefetches), while variables feeding **root** pagination stay out (root slicing happens post-plan). It is a refinement, not a break — variables that do not affect plan content still stay out.

   **No net-new `GlossaryTerm` row is created** (spec line 546 "No net-new entries"; this card ships no new public symbol or `Meta` key). The companion `docs/spec-033-connection_optimizer-0_0_9-terms.csv` already exists with 38 anchors, all resolving to existing `GlossaryTerm` rows (`check_spec_glossary.py` → `OK: 38 terms` at pre-flight).

**A2 — DONE-card move (move `WIP-ALPHA-033-0.0.9` → `DONE-033-0.0.9`; spec line 554, DoD item 11).** DONE-card invariants (`apps/kanban/signals.py::prepare_card_save`, pre_save): a card cannot save with `status.key=="done"` unless it has BOTH a linked `SpecDoc` (OneToOne) AND ≥1 `CardGlossaryTerm`. Steps, in order:

1. **SpecDoc — verify, do NOT re-create.** At plan time the card 033 already has a linked `SpecDoc` (`name="spec-033-connection_optimizer-0_0_9"`, `url="https://github.com/riodw/django-strawberry-framework/blob/main/docs/spec-033-connection_optimizer-0_0_9.md"`), created while the card was WIP. `SpecDoc.card` is a `OneToOneField`, so a second `SpecDoc.objects.create(card=card, …)` would raise `IntegrityError`. Worker 2 must **confirm** `card.spec` exists and points at `spec-033-connection_optimizer-0_0_9.md`; only `create` it if (against expectation) it is missing. If present but the URL is wrong, fix the existing row's `url`/`name` and `.save()`.
2. **Bootstrap ≥1 glossary link** so the pre_save DONE invariant passes on the flip: create one `CardGlossaryTerm` from an anchor in the spec's CSV, e.g. `CardGlossaryTerm.objects.get_or_create(card=card, term=GlossaryTerm.objects.get(anchor="connection-aware-optimizer-planning"), defaults={"raw_text": "Connection-aware optimizer planning", "order": 0})`. (Step A3's `import_spec_terms` re-syncs the full set afterward; this bootstrap only satisfies the invariant at flip time. `glossary_links` count is 0 at plan time.)
3. **Flip status to done**: `card.status = Status.objects.get(key="done"); card.save()`. `prepare_card_save` re-validates spec + glossary link; `format_card_id` / `_card_identifier` drop the milestone segment when `status.key=="done"`, so the rendered `cardId` auto-becomes `DONE-033-0.0.9`. (Card `number` 33 is unchanged.)

**A3 — Sync the full glossary-link set** (spec procedure step 5): `uv run python examples/fakeshop/manage.py import_spec_terms`. This is the **writing** sync — it reconciles every done card's `CardGlossaryTerm` links AND `GlossarySpecMention` rows to its companion `-terms.csv`. It requires every anchor in each done card's CSV to already exist as a `GlossaryTerm` (true for 033 — all 38 resolve). **Run this BEFORE any `--check`** (see the verification-gap flag in `### Test additions / updates`).

**A4 — Fix card-body content the spec wrap names** (spec procedure step 6) via `CardItem.text` (the `definition_of_done` and `other` sections are `CardItem` rows keyed by `(card, section, order)`):

1. **Stale filename ref** — DoD item `order=0` text is currently `"New spec, probably \`docs/spec-connection_optimizer.md\` (or folded into \`docs/spec-connection.md\`)."`. Update it to name the actual shipped spec path `docs/spec-033-connection_optimizer-0_0_9.md` (the canonical structured filename per Decision 1). Edit the `CardItem.text` and `.save()`.
2. **Stale "empty plan" premise** — the `other`-section `CardItem` at KANBAN.md:195 ("Because a `0.0.9` `DjangoConnectionField` derives an empty plan…") describes a now-retired premise (root planning shipped pre-`033`, nested planning ships in this card). The spec wrap names "stale filename refs" and the products-conversion sentence reconciliation; this premise sentence is the same staleness class. **Worker 2 discretion** (see discretion items): either soften it to past tense ("derived an empty plan before this card") or leave it as historical card-body record. There is no `## [0.0.X]` literal version-heading string inside the 033/051 card bodies (grep-confirmed) — the spec's "`## [0.0.X]` → `[Unreleased]`" guidance applies to any card body that names a release heading; none do here, so no such edit is owed in this slice.
3. **Mark every `definition_of_done` `CardItem.is_complete = True`** for card 033 (4 items: the spec item, the walker-recognition item, the cookbook-shape-tests item, the no-B1–B8-regression item) — all four are satisfied by Slices 1–6. `item.is_complete = True; item.save()` for each.

**A5 — `TODO-BETA-051-0.1.5` body reconciliation** (spec line 554 / Decision 10 / Risks line 561) — KANBAN.md:1761 (an `other`-section `CardItem` on card 051) currently carries the products-conversion sentence that Slice 6 LANDED ("replace the four `Query` list resolvers … with `DjangoConnectionField`s … must land with 033"). **Drop the products-conversion sentence**, keep the rest: the root `node(id:)` / `nodes(ids:)` entry points and the further-activation scope (`Meta.connection` opt-ins, Layer-3 keys) that stay `051`'s. This is a `CardItem.text` edit + regenerate, NOT a hand-edit of KANBAN.md.

**A6 — Regenerate all three rendered docs** from the repo root (spec procedure step 7):
- `uv run python scripts/build_kanban_md.py`
- `uv run python scripts/build_kanban_html.py`
- `uv run python scripts/build_glossary_md.py`

After regenerate, `docs/GLOSSARY.md`, `KANBAN.md`, `KANBAN.html` show the DB edits. Worker 2 must NOT hand-edit any of those three files; if a regenerated diff is wrong, the fix is in the DB, re-run the script.

#### (b) Hand-authored doc edits — plain markdown, edited directly

**B1 — `docs/README.md`** (spec line 548): empty the "Coming next" `0.0.9` line (docs/README.md:120 `- \`0.0.9\` *(in progress)* — connection-aware optimizer planning and …`) — the in-progress remainder was this card; remove the `0.0.9`-in-progress bullet (or fold the now-shipped content up into the shipped list). Add the connection-aware clause to the optimizer shipped-surface bullet (docs/README.md:104) — nested `edges { node }` connection selections now get windowed-`Prefetch` planning + strictness coverage. Reference-style links preserved.

**B2 — `docs/TREE.md`** (spec line 549): refresh the `walker.py` / `plans.py` / `extension.py` / `connection.py` one-line descriptions for the moved selection helpers (Decision 9) and the window planning. **NOTE: these one-liners appear TWICE** — under "django_strawberry_framework (current on-disk layout)" (≈ lines 197/216/219/220) and under "django_strawberry_framework (target package layout)" (≈ lines 254/283/286/287). See discretion item D2 for which block(s) to refresh.

**B3 — `TODAY.md`** (spec line 550): rewrite the products-centric sections for the connections-only `Query` (Slice 6 shipped it):
- "What's in `products/schema.py` today" sample (TODAY.md:22 / the `@strawberry.field def all_categories(...)` block at ~61) → the four `DjangoConnectionField` class attributes (`all_categories: DjangoConnection[CategoryType] = DjangoConnectionField(CategoryType)`, … — the post-Slice-6 shape verified live in `apps/products/schema.py:204-207`).
- "Optimized products queries that work today" (TODAY.md:140) root shape → `edges { node }` connection shape; the SQL-shape pins now ride the connection wrapper.
- "Filtering and ordering on products today" argument site (TODAY.md:179) → the synthesized `filter:` / `orderBy:` arguments on the connection fields (no `filter_input_type(...)` / `order_input_type(...)` hand-written signatures).
- Products stays the canonical demonstration vehicle, now in its cookbook-mirror shape; the "What products is still waiting for" list (TODAY.md:243) stays scoped to permissions / Layer-3 keys — do not widen it.

**B4 — `README.md`** (spec line 551): update the Status paragraph's newest-shipped-surface line (README.md:48-50). Today it leads with "the full Relay story (`0.0.9`)"; connection-aware optimizer planning closes the `0.0.9` story — add it as the newest-shipped surface (windowed nested-connection prefetches + the annotation fast path + strictness coverage of connection paths). Keep the on-disk version reference accurate (still `0.0.8` per Decision 12 — do NOT change the `**\`0.0.8\`…**` lead to `0.0.9`; the bump is the joint cut's, line 50 already says `0.0.8`).

**B5 — `CHANGELOG.md`** (spec line 552; the explicit per-card permission grant): add bullets under the existing `## [Unreleased]` block (CHANGELOG.md:19) — **no version-heading promotion** (Decision 12: do NOT create `## [0.0.9] - …`). Wording this plan commits to (the canonical phrasings the GLOSSARY/README/TODAY edits must match):
- **`### Added`** (append to the existing `### Added` at CHANGELOG.md:25):
  - *Connection-aware optimizer planning (windowed nested-connection prefetches + the annotation fast path).* The optimizer now recognizes synthesized `<field>Connection` selections inside a parent's selection walk (via `DjangoTypeDefinition.relation_connections`, never `DjangoConnectionField` internals) and plans a windowed `Prefetch` under a package-reserved `_dst_<field>_connection` `to_attr` — `RowNumber()` / `Count(1)` window functions partitioned by the parent key, the slice window derived from the connection's resolved pagination arguments capped by `relay_max_results`. The generated connection class serves `edges` / cursors / `pageInfo` / `totalCount` directly from the `_dst_row_number` / `_dst_total_count` annotations (one window-function query per relation per request, zero per-parent queries on the fast path), falling back to the shipped per-parent pipeline for ambiguous empty windows (`first: 0`, overshot `after:`) and for the unplanned shapes (sidecar `filter:` / `orderBy:` input, divergent aliases, `OptimizerHint.SKIP`, `.distinct()` targets).
  - *Strictness coverage of connection paths.* An unplanned, unserved nested-connection access now fires the strictness contract (`OptimizerError` under `"raise"`, a logged warning under `"warn"`) through the same resolver-key vocabulary list relations use, closing the silent-N+1 hole the relation-as-Connection upgrade documented.
  - *Pagination-aware plan-cache keys.* Variables feeding **nested** connection pagination arguments are now hashed into the plan-cache key (their values are baked into windowed prefetches); variables feeding **root** pagination arguments stay out (root slicing happens post-plan), including through root-level fragments.
- **`### Changed`** (append to the existing `### Changed` at CHANGELOG.md:21):
  - *Products example converted to connections-only (the `django-graphene-filters` cookbook mirror).* The four `examples/fakeshop/apps/products/schema.py` `Query` list resolvers (`all_categories` / `all_items` / `all_properties` / `all_entries`) are now `DjangoConnectionField` class attributes; the `test_products_optimizer_*` SQL-shape dogfooding is re-pinned through `edges { node }` with its query counts intact. (A `relay.connection` field with no `first`/`last` caps the default result set at `relay_max_results` and appends a deterministic `ORDER BY pk`.)

  **No version-file edits** (Decision 12): do NOT touch `pyproject.toml`, `django_strawberry_framework/__init__.py::__version__`, `tests/base/test_init.py`, or `uv.lock`, and do NOT promote a CHANGELOG release heading.

### Test additions / updates

Slice 7 is **doc-only: 0 new tests, 0 modified tests**. No package source logic changes, so no test surface moves.

Verification commands (Worker 1 runs at final verification; Worker 2/3 may run to confirm):

- **`uv run python examples/fakeshop/manage.py import_spec_terms`** then **`uv run python examples/fakeshop/manage.py import_spec_terms --check`** — the **WRITING sync MUST run before `--check`**. **Flagged gap:** at plan time, bare `--check` FAILS on the baseline (`GlossarySpecMention rows for docs/SPECS/spec-032-full_relay-0_0_9.md do not match … [] != [...]`) because the spec-mention rows are out of sync on disk. The sync step reconciles every done card (including the newly-done 033 and the lagging 032), after which `--check` reports `OK: <N> done cards have glossary links.`. Do not interpret a pre-sync `--check` failure as a Slice-7 defect.
- **`uv run python examples/fakeshop/manage.py check`** — Django system checks (green at plan time; confirm still green after the DB edits + regenerate).
- **`git diff docs/GLOSSARY.md`** — must reflect ONLY the intended body rewrites + the one status flip (the `connection-aware-optimizer-planning` Status/`### Status` line moving from "planned for `0.0.9`" to "shipped (`0.0.9`)", and the five entries' bodies). **Idempotence:** a second `build_glossary_md.py` run yields no further diff. Same idempotence check for `KANBAN.md` (must show `DONE-033-0.0.9` with the four DoD boxes ticked, the stale-filename DoD item fixed, and the 051 body reconciled) and `KANBAN.html`.
- **`git diff examples/fakeshop/db.sqlite3`** is expected (the DB is git-tracked; the edits are the deliverable). Reversible via `git checkout -- examples/fakeshop/db.sqlite3`.
- Worker-local lint per `AGENTS.md`: `uv run ruff format .` + `uv run ruff check --fix .` (touches no `.py` here but is the standard close-out; the markdown/DB edits are out of ruff scope).

### Implementation discretion items

Items assessed and decided to be at Worker 2's discretion:

- **D1 — GLOSSARY body prose wording.** The exact sentences of each `GlossaryTerm.body` rewrite are Worker 2's to phrase, provided they (a) draw vocabulary from the spec Decisions cited in step A1, (b) match the CHANGELOG bullet wording in step B5, and (c) preserve each entry's `**See also:**` line and the `SyncMisuseError`-caveat-stays rule for `metarelation_shapes`. The mechanism content is fixed; the prose is not.
- **D2 — Which TREE.md block(s) to refresh.** TREE.md carries the `connection.py`/`extension.py`/`plans.py`/`walker.py` one-liners in BOTH the "current on-disk layout" block (≈197-220) and the "target package layout" block (≈254-287). Worker 2's discretion: refresh the **on-disk-layout** block (it describes today's shipped reality and is the one the spec's "refresh the … one-line descriptions" most directly means); refreshing the target-layout block too is acceptable if its one-liners would otherwise read stale, but the on-disk block is the required target. Keep the two blocks' descriptions consistent if both are touched.
- **D3 — Stale "empty plan" 033 card-body sentence (A4.2).** Whether to soften KANBAN.md:195's "derives an empty plan" premise to past tense or leave it as a historical card-body record is Worker 2's discretion — the spec's named wrap items are the stale *filename* ref (required, A4.1) and the *051* products-conversion sentence (required, A5); the 033 "empty plan" sentence is adjacent staleness, not a named required edit. If softened, do it via `CardItem.text` + regenerate (never a hand-edit).
- **D4 — Glossary-link bootstrap anchor (A2.2).** The specific CSV anchor used to seed the first `CardGlossaryTerm` is Worker 2's choice (any anchor in `spec-033-…-terms.csv`); `import_spec_terms` re-syncs the full set regardless, so the bootstrap choice is transient.

### Spec slice checklist (verbatim)

The spec's Slice-7 sub-bullets from `## Slice checklist` (lines 85-86), copied verbatim:

- [x] Slice 7: doc updates + card-completion wrap (per [Doc updates](#doc-updates))
  - [x] [`docs/GLOSSARY.md`][glossary], [`docs/README.md`][docs-readme], [`docs/TREE.md`][tree], [`TODAY.md`][today], [`README.md`][readme], [`CHANGELOG.md`][changelog] (the explicit permission grant), [`KANBAN.md`][kanban] (card → Done via the kanban DB + re-render). No version-file edits ([Decision 12](#decision-12--version-bumps-are-owned-by-the-joint-009-cut)).

Both boxes ticked: every named doc surface landed and the card re-rendered as `DONE-033-0.0.9` with the four DoD boxes ticked. The plan sub-step **A5** (the products-conversion sentence reconciliation the plan calls "card 051") is **deferred to Worker 1** — its named target does not exist as described (see `### Notes for Worker 1`). A5 is a plan sub-step, not a separate verbatim spec sub-check, so the verbatim box stands ticked while A5 is flagged for spec reconciliation.

---

## Build report (Worker 2)

### Files touched

**(a) DB-backed — edited via the Django ORM in `examples/fakeshop/manage.py shell`, then regenerated:**

- `examples/fakeshop/db.sqlite3` — the deliverable DB edits (git-tracked, reversible via `git checkout -- examples/fakeshop/db.sqlite3`):
  - **A1** — `GlossaryTerm` rewrites by anchor: `connection-aware-optimizer-planning` flipped to `status=shipped` / `status_text="shipped (\`0.0.9\`)"` + full body rewrite (windowed-`Prefetch` mechanism, `to_attr` / annotation fast path, the five-shape fallback matrix incl. `.distinct()` + divergent-secondary-type, strictness, the `.distinct()` + window-backend caveats); body-only rewrites on `djangoconnectionfield` (pre-`033` "empty plan" / "strictness does not flag" caveats swept), `metarelation_shapes` (per-parent caveat dropped, **`SyncMisuseError` async caveat kept** per plan A1.3), `strictness-mode` (connections now participate), `plan-cache` (Variable-filtering property gains the nested-pagination clause). No net-new `GlossaryTerm` row.
  - **A2** — DONE-card move for card 033: SpecDoc verified (name+URL correct — **not** re-created); one `CardGlossaryTerm` bootstrapped (anchor `connection-aware-optimizer-planning`, discretion D4); `card.status` flipped to `done` → `card_id` auto-rendered `DONE-033-0.0.9`.
  - **A3** — `import_spec_terms` writing sync run (reconciled 33 done cards incl. 033); card 033 now carries all 38 CSV glossary links.
  - **A4** — card-033 body: DoD `order=0` stale filename ref fixed to `docs/spec-033-connection_optimizer-0_0_9.md` (A4.1); all 4 `definition_of_done` items `is_complete=True` (A4.3); the `other` `order=10` "derives an empty plan" sentence softened to past tense + the now-satisfied "must land with 033" instruction reconciled (A4.2 / discretion D3).
- `docs/GLOSSARY.md` — regenerated (`scripts/build_glossary_md.py`); reflects the A1 status flip + five body rewrites only.
- `KANBAN.md` — regenerated (`scripts/build_kanban_md.py`); `DONE-033-0.0.9` now in `## Done` with the four DoD boxes `- [x]`, the stale-filename DoD item fixed, `## In progress` shows "No active WIP cards".
- `KANBAN.html` — regenerated (`scripts/build_kanban_html.py`).

**(b) Hand-authored markdown (edited directly, reference-style links per `START.md`):**

- `docs/README.md` — B1: optimizer shipped-surface bullet gains the connection-aware clause; the `0.0.9`-in-progress "Coming next" bullet removed and the "remaining alpha" range retitled `0.0.10 → 0.0.12`.
- `docs/TREE.md` — B2: refreshed the `connection.py` / `extension.py` / `plans.py` / `walker.py` / `definition.py` one-liners in **both** the on-disk-layout and target-package-layout blocks (discretion D2 — refreshed both, kept them byte-consistent).
- `TODAY.md` — B3: the `products/schema.py` sample → the four `DjangoConnectionField` class attributes; the "demonstrates today" bullets, the optimized-queries root shape, and the filtering/ordering example all rewritten to the `edges { node }` / synthesized-argument shape; the visibility-`get_queryset` intro and the "still waiting for" sentence corrected (products IS now connections); the "still waiting for" list left scoped to permissions / Layer-3 keys.
- `README.md` — B4: Status paragraph's newest-shipped-surface line now leads with connection-aware optimizer planning (`0.0.9`); the `**\`0.0.8\`...**` on-disk-version lead left unchanged (Decision 12).
- `CHANGELOG.md` — B5: three `### Added` bullets (connection-aware planning, strictness coverage, pagination-aware cache keys) + one `### Changed` bullet (products connections-only conversion) appended under `## [Unreleased]`; **no version-heading promotion** (Decision 12). Added six reference-style link defs (`glossary-connection-aware-optimizer-planning`, `glossary-optimizerhint`, `glossary-plan-cache`, `glossary-strictness-mode`, `definition`, `products-schema`) alphabetically under the correct `<!-- docs/ -->` / `<!-- django_strawberry_framework/ -->` / `<!-- examples/ -->` group headers; all eight refs used in the new bullets resolve and their targets exist on disk.

**Not touched (out of slice scope):** no `.py` source or tests; no version files (`pyproject.toml`, `__version__`, `tests/base/test_init.py`, `uv.lock`); the active spec (Worker 1-owned). The accepted Slice-1..6 baseline diffs (`django_strawberry_framework/**`, the four `tests/` files, `products/schema.py`, the two `test_query/` files, the spec) are untouched.

### Tests added or updated

None — doc-only slice (0 new tests, 0 modified tests), as the plan specifies.

### Validation run

- `uv run ruff format .` — pass (`251 files left unchanged`; the COM812 warning is pre-existing config noise, not a change).
- `uv run ruff check --fix .` — pass (`All checks passed!`); zero churn.
- `git status --short` after both ruff invocations — every modified file classified: slice-intended = `CHANGELOG.md` / `README.md` / `TODAY.md` / `docs/README.md` / `docs/TREE.md` (hand-authored), `docs/GLOSSARY.md` / `KANBAN.md` / `KANBAN.html` (regenerated), `examples/fakeshop/db.sqlite3` (ORM deliverable). Baseline (NOT reverted, NOT mine): the seven `django_strawberry_framework/**` source files + four `tests/` files + `products/schema.py` + two `test_query/` files (Slices 1-6), and the spec (Worker 1). Untracked: the `bld-*.md` + `build-033-*.md` build artifacts. No unrelated tool churn to revert.
- `uv run python examples/fakeshop/manage.py import_spec_terms` then `... --check` — the writing sync ran **first** (per the flagged baseline gap), then `--check` → `OK: 33 done cards have glossary links.` (pre-sync `--check` baseline failure is the documented spec-mention lag, not a Slice-7 defect — not reproduced post-sync).
- `uv run python examples/fakeshop/manage.py check` — `System check identified no issues (0 silenced).`
- `uv run python scripts/check_spec_glossary.py --spec docs/spec-033-connection_optimizer-0_0_9.md` — `OK: 38 terms - all have glossary entries and at least one spec link.` (the GLOSSARY body edits did not break any spec-CSV anchor).
- **GLOSSARY/KANBAN regenerate idempotence** — re-ran all three `build_*` scripts a second time after the first regenerate AND again after ruff; `diff` shows **no further diff** for `docs/GLOSSARY.md`, `KANBAN.md`, `KANBAN.html`. `git diff docs/GLOSSARY.md` is scoped to the one Status-index row flip (`planned for \`0.0.9\`` → `shipped (\`0.0.9\`)`), the `**Status:**` line of the flipped entry, and the five entries' body paragraphs — nothing else (10 insertions / 6 deletions).
- `git diff --check` on the five hand-edited docs — clean (no whitespace errors / conflict markers).

### Implementation notes

- **GLOSSARY body prose (discretion D1):** phrased each body to match the CHANGELOG B5 wording (one coherent story), drew mechanism vocabulary from Decisions 3-8, preserved every entry's `**See also:**` line, and kept the `metarelation_shapes` `SyncMisuseError` async caveat verbatim-in-spirit (the async pipeline stays out of scope). The `connection-aware-optimizer-planning` body includes the `.distinct()` correctness rationale and the window-backend `NotSupportedError` caveat the plan named.
- **TREE.md both blocks (D2):** refreshed the on-disk-layout block (required) AND the target-package-layout block (the one-liners would otherwise read stale) for the five touched files, kept byte-identical between blocks. Also refreshed `definition.py` (gained the `relation_connections` slot) since its description was stale against Decision 3.
- **CHANGELOG link-def hygiene:** the new bullets needed six link ids that did not yet exist in the CHANGELOG link block; added them rather than inlining `](path)` (START.md convention), grouped by where the *target* lives, alphabetical within group, disk-verified.
- **`snapshot` BoardDoc not edited** — see Notes for Worker 1 (a board-prose reconciliation candidate the plan did not name; left for a scope decision rather than touched unilaterally).

### Notes for Worker 3

- Slice 7 ships zero `.py` logic — the static helper (`review_inspect.py`) was correctly skipped (recorded in the plan). The review surfaces are CHANGELOG/GLOSSARY/KANBAN/doc text + the DB diff.
- The DB-backed edits are reversible as a unit: `git checkout -- examples/fakeshop/db.sqlite3` then re-running the three `build_*` scripts restores the pre-Slice-7 rendered docs. The glossary body text lives ONLY on `GlossaryTerm.body` (no double-source into the rendered `.md`).
- The large `django_strawberry_framework/**` + `tests/**` + `test_query/**` + `products/schema.py` diffs in the working tree are the accepted Slice-1..6 baseline, NOT this slice — review only the doc/DB surfaces for Slice 7.
- **CHANGELOG sanity:** no `## [0.0.9]` heading created; all bullets under `## [Unreleased]`. No version string introduced. Wording matches the GLOSSARY/README/TODAY edits.

### Notes for Worker 1 (spec reconciliation)

**ESCALATED — Plan step A5 deferred (structural drift; the named target does not exist as described).** The plan's A5 (and spec line 554 / Decision 10 / Risks line 561) directs reconciling "the `TODO-BETA-051-0.1.5` card body's products-conversion sentence" at "KANBAN.md:1761". The DB ground truth diverges on three points; I did not edit on a guess:

1. **Card number 51 is the wrong card.** In `examples/fakeshop/db.sqlite3`, `Card.objects.get(number=51)` is `TODO-BETA-051-**0.1.4**` "**Stable choice enum naming override**" — an unrelated card. The fakeshop-activation card the spec *describes* (root Node fields / further activation scope) is `Card(number=52)` = `TODO-BETA-**052**-0.1.5` "Fakeshop GraphQL schema activation". So the spec's `[TODO-BETA-051-0.1.5]` link is a **persistent misnumber for card 052** — it appears throughout the spec AND in the accepted Slice-6 baseline (`examples/fakeshop/apps/products/schema.py` docstring ~line 199 `Still deferred to \`TODO-BETA-051-0.1.5\``) AND now in `TODAY.md` (which I left at `051` to stay consistent with the spec/baseline rather than diverge unilaterally).
2. **The products-conversion "must land with 033" sentence is NOT on card 51 or card 52.** The only `CardItem` carrying it is `Card(number=32)` = `**DONE-032-0.0.9**`, `other` section `order=65` — and KANBAN.md:1761 (the plan's cited line) renders from inside that **DONE-032** card block (heading at KANBAN.md:1611), not from card 051/052. Card 52's `other` items (orders 0-2) are different text; card 52 carries no such sentence.
3. **Editing it would mean editing a DONE card's historical body.** DONE-032's `order=65` sentence is a record of what 032 deferred. Whether to (a) drop the "must land with 033" clause from DONE-032's body, (b) leave it as shipped-history record, (c) move the reconciliation to card 052 (which has no such sentence to reconcile), or (d) fix the spec's `051`→`052` misnumber cluster first — is a spec-custodian call, not Worker 2's.

Recommended Worker-1 resolution: (i) decide the `051`→`052` misnumber correction across the spec + the Slice-6 `schema.py` docstring + `TODAY.md` (uniform, or accept the misnumber as a known label); (ii) decide A5's actual target — if the intent is purely "stop a now-satisfied 'must land with 033' instruction from lingering," the candidate is DONE-032 `other` order=65 (ORM edit + regenerate), but it edits shipped-card history; (iii) note that card 033's OWN line-195 sentence (its `other` order=10) carried the same `TODO-BETA-051-0.1.5` reference and the "must land with 033" clause — **I already reconciled THAT one** under discretion D3 (A4.2), softening to past tense and recording the conversion as landed-with-033. Only the DONE-032 sentence (A5) remains open.

**Board-prose `snapshot` BoardDoc (not named by the plan).** The `snapshot` reference BoardDoc (kanban namespace, `kind=reference`) still reads "`{{card_ref:19}}` (connection-aware optimizer planning) **remains in progress**" (renders at KANBAN.md ~line 64), now contradicting the `## In progress` column ("No active WIP cards") and the card's Done placement. I did **not** edit it: the plan's A-steps did not name it, the same snapshot carries other independent staleness (e.g. it lists `connection.py` under "still planned only" though it shipped in `DONE-030`), and a surgical one-clause edit would leave the snapshot internally inconsistent. Flagging as a board-prose-refresh candidate — Worker 1's scope/spec call whether to refresh the snapshot (ORM `BoardDoc.body` edit + regenerate) or leave it as a dated historical narrative.

**`docs/README.md` "Coming next" range.** I retitled the remaining-alpha range from `0.0.9 → 0.0.12` to `0.0.10 → 0.0.12` and removed the `0.0.9`-in-progress bullet (the in-progress remainder was this card). Flagging in case Worker 1 prefers a `0.0.9` "shipped" line folded into the shipped list instead — the now-shipped content is already covered by the optimizer shipped-surface bullet's new connection-aware clause.

---

## Review (Worker 3)

Reviewed the Slice-7 doc/DB-only diff against spec lines 85-86, `## Doc updates` (541-554), DoD 11-12, and Decision 12. Scope filter (per the artifact's `### Files touched`): the regenerated `docs/GLOSSARY.md` / `KANBAN.md` / `KANBAN.html`, `examples/fakeshop/db.sqlite3`, and the hand-edited `docs/README.md` / `docs/TREE.md` / `TODAY.md` / `README.md` / `CHANGELOG.md`. The Slice-1..6 `.py` source/test/`products/schema.py`/`test_query/` diffs and the spec edit are accepted baseline — not re-reviewed.

### High:

None.

### Medium:

#### `snapshot` BoardDoc still reads "remains in progress" on a board dated today (escalated to Worker 1)

The `snapshot` BoardDoc renders into `KANBAN.md` ~line 64 under `### In progress`: `... DONE-033-0.0.9 (connection-aware optimizer planning) remains in progress.` The card-move this slice performs directly falsifies that clause — the card now lives in `## Done` with a `DONE-` prefix, and the rendered `## In progress` column body reads "No active WIP cards" (KANBAN.md line 138). The board's own preamble (line 3 `Last refreshed: 2026-06-13`, line 5 "summarizes what is shipped … based on the **current** code") frames it as a **live** status surface, not a dated historical narrative, so the stale clause is factually wrong, not merely aged. BUILD.md `### Documentation / release sanity` names exactly this: "no obsolete … 'in progress' … wording remains in the files the slice deliberately updated" — and `KANBAN.md` is the file this slice's headline DB act updates.

Why it is not a clean Worker-2 fix (the reason I escalate rather than reject): the same `snapshot` BoardDoc carries broader staleness that pre-exists at HEAD — it lists `connection.py` under "Layer 3 public subsystems still planned only" (KANBAN.md line 71) though `connection.py` shipped in `DONE-030-0.0.9` (the spec's own Risks line 560 records this), and it lists connection-aware planning under "Optimizer follow-up ideas … outside the shipped B1-B8 surface" (line 79). A surgical one-clause edit ("remains in progress" → "shipped") leaves the BoardDoc internally inconsistent (still calling `connection.py` unimplemented). Whether to (a) refresh the whole `snapshot` BoardDoc body, (b) fix only the now-falsified clause, or (c) treat the snapshot as out-of-scope narrative for this card is a scope/spec-custodian call. Worker 2 flagged it under `### Notes for Worker 1` and left it unedited — correct posture; recorded here at Medium so it is weighed at final verification rather than slipping silently.

Recommended fix path when decided: ORM edit to `BoardDoc.body` (the `snapshot` row) + `build_kanban_md.py` / `build_kanban_html.py` regenerate — never a hand-edit of the rendered files.

### Low:

#### Dangling in-page anchor `#djangotypedefinition` in the rewritten GLOSSARY entry

The rewritten `connection-aware-optimizer-planning` body (docs/GLOSSARY.md line 232) introduces an in-page link `` [`DjangoTypeDefinition`](#djangotypedefinition) ``. GLOSSARY.md has **no** heading that slugifies to `djangotypedefinition` (`DjangoTypeDefinition` is a code symbol mentioned throughout but is not a glossary entry), so the anchor resolves to nothing. Verified new-in-slice: 0 occurrences at HEAD, 1 in the working tree; a full slug-vs-anchor sweep of the file found this as the **only** dangling in-page anchor among 85 referenced.

The CHANGELOG's parallel bullet handled the same symbol correctly by linking it to the source file (`[`DjangoTypeDefinition`][definition]` → `django_strawberry_framework/types/definition.py`). Recommended fix (in the DB then regenerate, since GLOSSARY is DB-backed): either drop the link and keep `DjangoTypeDefinition` as a plain code span, or point it at the definition source the way the CHANGELOG does (GLOSSARY entries elsewhere link symbols to other GLOSSARY anchors, not source files, so the plain-code-span form is the lower-friction choice). Low severity: prose reads correctly; only the clickable anchor is dead.

### DRY findings

None. Slice 7 introduces no code, no helper, no constant. The GLOSSARY/README/TODAY/CHANGELOG prose tells one coherent story drawn from the spec's canonical vocabulary (windowed `Prefetch` under `_dst_<field>_connection`, `RowNumber()`/`Count(1)`, the fallback matrix, strictness coverage, pagination-aware cache keys) — confirmed the CHANGELOG `Connection-aware optimizer planning` bullet body is character-for-character identical to the plan's B5 wording (the only deltas are the plan-authorized reference-style link insertions and the trailing `See [...]` cross-ref). The body text lives only on the `GlossaryTerm.body` DB column (no double-source into the rendered `.md`); idempotent regen confirms it.

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` is empty — `__init__.py` does not appear in the working-tree diff at all. No `__all__` / re-export change. Slice 7 ships zero `.py` logic; the seven `django_strawberry_framework/**` source files in the working tree are the accepted Slice-1..6 baseline, not this slice. No version-file edits (`pyproject.toml`, `__version__`, `tests/base/test_init.py`, `uv.lock` all absent from the diff). DoD-12 / Decision-12 satisfied: on-disk version stays `0.0.8` (`pyproject.toml` line 4, `__init__.py` line 25).

### CHANGELOG sanity

The slice modifies `CHANGELOG.md` (the explicit per-card permission grant, spec line 543 / Decision 12). Read end-to-end:

- **No version-heading promotion.** `## [Unreleased]` (line 19) is the only top-of-list heading; `grep -E '^## \[0\.0\.9\]'` → none. Bullets land under the existing `### Changed` (line 21) and `### Added` (line 26). Matches Decision 12 — the `0.0.8 → 0.0.9` bump is the joint cut's, not this card's.
- **Authorized headings + wording.** Three `### Added` bullets — *Connection-aware optimizer planning (windowed nested-connection prefetches + the annotation fast path)*, *Strictness coverage of connection paths*, *Pagination-aware plan-cache keys* — and one `### Changed` bullet — *Products example converted to connections-only (the `django-graphene-filters` cookbook mirror)*. These are exactly the four bullets spec line 552 authorizes, with the canonical phrasings (connection-aware planning / strictness coverage / pagination-aware cache keys / products conversion). Each reads coherently against the shipped Slice-1..6 behavior; nothing overstates (the fast-path "zero per-parent queries" / fallback matrix / "one window-function query per relation per request" all match the implemented contract) or understates.
- **Link-defs resolve.** Six new reference-style defs added: `glossary-connection-aware-optimizer-planning`, `glossary-optimizerhint`, `glossary-plan-cache`, `glossary-strictness-mode` (under `<!-- docs/ -->`, alphabetical), `definition` (under `<!-- django_strawberry_framework/ -->`, alphabetical), `products-schema` (under `<!-- examples/ -->`, alphabetical). All eight refs used in the new bullets resolve to existing targets: the four GLOSSARY anchors map to real headings (`## Connection-aware optimizer planning`, `## OptimizerHint`, `## Plan cache`, `## Strictness mode`), `definition.py` and `products/schema.py` exist on disk. No inline `](path)` cross-file links — convention honored.

### Documentation / release sanity

The slice touches docs + KANBAN + GLOSSARY (DB-backed) + release metadata (CHANGELOG). Read the changed surfaces end-to-end:

- **Version strings / statuses / card IDs.** Package stays `0.0.8` on disk (Decision 12). README.md Status line still leads with `**\`0.0.8\` …**` (B4 kept it — confirmed). GLOSSARY `connection-aware-optimizer-planning` flipped to `shipped (\`0.0.9\`)` in both the lookup table (line 59) and the `**Status:**` line (line 230).
- **KANBAN card move (exactly once).** `WIP-ALPHA-033-0.0.9` → `DONE-033-0.0.9`: removed from `## In progress` (the whole WIP-033 block is gone; the section now renders "No active WIP cards"), present once in `## Done` (heading KANBAN.md line 1562). All cross-reference card_refs (dependencies, related, card-reference blocks) auto-updated to `DONE-033-0.0.9` consistently. DB confirms `card_id=DONE-033-0.0.9`, `spec` → `spec-033-connection_optimizer-0_0_9.md` (correct name + URL, not re-created), 38 glossary links synced, all 4 `definition_of_done` items `is_complete=True` → the four DoD boxes render `- [x]`. The stale-filename DoD item (A4.1) now reads `New spec at \`docs/spec-033-connection_optimizer-0_0_9.md\``. NOTE (not a finding): the card body's `- Status: Planned` line and `#### Planning note: planned` are independent free-text DB fields — `DONE-032` renders `Status: Planned` and `DONE-031` renders `Status: Needs spec` identically, so these are shipped-precedent render artifacts, NOT the card lifecycle status (which correctly drove the `DONE-` prefix).
- **GLOSSARY caveat sweep.** `djangoconnectionfield` — the pre-`033` "the derived plan is **empty**" and "A Strictness mode `"raise"` run does **not** flag" sentences are gone, replaced with the shipped posture (the seam feeds nested window planning; strictness now flags unplanned nested-connection access). `metarelation_shapes` — the per-parent-lazy-load caveat dropped (now "optimizer-planned"), and the **`SyncMisuseError` async-`get_queryset` caveat STAYS** (verified present verbatim-in-spirit, with the `relation_shapes={"<field>":"list"}` narrowing recourse). `strictness-mode` gains the connections-participate paragraph; `plan-cache` Variable-filtering property gains the nested-pagination clause. All four pre-`033` caveat targets swept per spec line 546. All `WIP-ALPHA-033` references swept from GLOSSARY (0 remain).
- **Markdown links.** All new/moved links point at existing targets — EXCEPT the one dangling `#djangotypedefinition` in-page anchor recorded under Low above.
- **Verbatim-from-spec text.** The CHANGELOG bullets are the slice's verbatim-commitment surface (the GLOSSARY/README/TODAY bodies are Worker-2-discretion prose per plan D1, drawn from spec vocabulary, not copied verbatim). Confirmed the CHANGELOG `Connection-aware optimizer planning` bullet body is character-for-character identical to the plan's B5 wording (only the authorized link-ref markup differs).
- **DB-backed integrity (re-run myself).** Re-ran all three regenerate scripts; SHA-1 of `docs/GLOSSARY.md` / `KANBAN.md` / `KANBAN.html` is byte-identical before and after (`0fec2639` / `78c3fada` / `a78fbfdc` unchanged) → DB == rendered file, genuinely regenerated not hand-edited, idempotent. My re-run changed nothing, so no `git checkout --` restore was needed. `import_spec_terms --check` → `OK: 33 done cards have glossary links.` `manage.py check` → `System check identified no issues (0 silenced).` `check_spec_glossary.py --spec docs/spec-033-…` → `OK: 38 terms`. `git diff --check` on the hand-edited docs → clean (no whitespace/conflict-marker errors).

### What looks solid

- The card-completion-wrap mechanics are exactly right: DONE-card invariants satisfied (SpecDoc verified-not-recreated, glossary links bootstrapped then re-synced via `import_spec_terms`), DoD items all complete, the `DONE-` id auto-derived from the lifecycle-status flip, and the move is genuinely DB-backed (proven by my idempotent re-regen).
- The GLOSSARY rewrites are well-targeted and per-spec: the status flip + body rewrite of `connection-aware-optimizer-planning` carries the full mechanism (windowed `Prefetch` / `to_attr` / `RowNumber()`/`Count(1)` / fast path / fallback matrix incl. `.distinct()` + divergent-secondary-type / strictness), AND the `.distinct()` correctness rationale + window-backend `NotSupportedError` caveat the spec named. The `SyncMisuseError`-stays rule is honored precisely.
- The CHANGELOG is clean: correct headings, canonical wording matching the plan and the other doc edits, no version-heading promotion, all link-defs resolved and convention-compliant.
- TODAY.md's products sample matches the live `apps/products/schema.py` (the four `DjangoConnection[T] = DjangoConnectionField(T)` class attributes + the consolidated import); the optimized-query / filtering examples reshaped to `edges { node }` + synthesized arguments; the "still waiting for" list stayed scoped (permissions / Layer-3). TREE.md refreshed both layout blocks byte-consistently (D2) with accurate one-liners for the five touched files.
- Worker 2's escalation discipline is exemplary: it reconciled the in-scope staleness it could verify (card-033's own `other` order=10 "empty plan" sentence → past tense, A4.2/D3) and refused to guess on the spec-vs-DB structural divergences (A5 misnumber, snapshot BoardDoc), surfacing both with full DB ground truth for Worker 1.

### Temp test verification

No temp tests created. Slice 7 ships zero `.py` logic — there is no behavior to probe with a test. Verification was entirely doc/DB inspection + re-running the three regenerate scripts and the two `manage.py` commands (idempotence, `import_spec_terms --check`, `manage.py check`, `check_spec_glossary.py`). Static inspection helper (`scripts/review_inspect.py`): **skipped**, concurring with the plan — it parses `.py` source as text/AST and this slice touches no `.py` source (markdown + ORM-edited DB only). Skip reason recorded: doc/DB-only slice, zero Python logic.

### Notes for Worker 1 (spec reconciliation)

Two items need spec context Worker 2 cannot provide; both are accept-with-escalation (the slice's named contract — the seven doc surfaces + the card move — fully landed). My read on each:

**1. A5 — the `051`/`052` misnumber + the products-conversion sentence on shipped DONE-032 (Worker 2's escalation; Worker 1 owns the call).** Worker 2's three-point analysis is correct and I independently verified the DB ground truth:
   - Card `number=51` = `TODO-BETA-051-0.1.4` "Stable choice enum naming override" (unrelated). Card `number=52` = `TODO-BETA-052-0.1.5` "Fakeshop GraphQL schema activation" — the card the spec *describes* by topic. So **the spec's `[TODO-BETA-051-0.1.5]` link is a genuine misnumber for card 052** — and the spec IS wrong here; it should be reconciled to `052` (the topic and version `0.1.5` both point at card 52, while `051` is `0.1.4` choice-enum). The misnumber is consistent across the spec, the Slice-6 `schema.py` docstring (~line 199), and `TODAY.md` (W2 deliberately kept TODAY.md at `051` to match, not diverge).
   - The "must land with 033 / gated on `WIP-ALPHA-033-0.0.9`" products-conversion sentence the spec tells Slice 7 to reconcile lives on **`DONE-032-0.0.9` `other` order=65** — a *shipped* card's historical body — not on card 51 or 52. It still carries the now-stale `WIP-ALPHA-033-0.0.9` (033 is now DONE) and the "must land with 033" instruction (the conversion landed).

   **My read:** the spec's A5 target as literally written ("the `TODO-BETA-051-0.1.5` card body's products-conversion sentence") does not exist — no card-51/52 body carries that sentence. The spec needs reconciliation on two axes: (i) fix the `051`→`052` misnumber cluster (the spec link, the `schema.py` docstring, `TODAY.md`) so the reference resolves to the real fakeshop-activation card; (ii) decide A5's actual intent. If the intent is "stop a now-satisfied 'must land with 033' instruction from lingering on a live board," the only real target is DONE-032 `other` order=65 — but that edits shipped-card history (a record of what 032 deferred), so leaving it as dated history is defensible. Card 033's own equivalent sentence (its `other` order=10) was already reconciled by W2 (A4.2). I lean toward: fix the misnumber (clear spec defect) + soften DONE-032 order=65 to past tense ("the conversion landed with 033", drop the `WIP-ALPHA-033` live reference) for board-currency consistency, but defer to Worker 1 on the shipped-history-vs-currency tradeoff.

**2. snapshot BoardDoc "remains in progress" (Medium, above).** Recorded at Medium because the board is dated-today and live, but the fix needs the same scope decision (the BoardDoc has broader pre-existing staleness — `connection.py` listed unimplemented though shipped in DONE-030). Worker 1's call: refresh the `snapshot` BoardDoc body wholesale (ORM + regen) or scope it out as out-of-this-card narrative. A one-clause fix leaves it internally inconsistent.

**3. `docs/README.md` "Coming next" retitle (W2 note, not a finding).** W2 retitled the remaining-alpha range `0.0.9 → 0.0.12` to `0.0.10 → 0.0.12` and dropped the `0.0.9`-in-progress bullet. This satisfies spec line 548 ("the 'Coming next' `0.0.9` line empties") and the now-shipped content is covered by the optimizer shipped-surface bullet's new connection-aware clause. Reads correctly; no action needed unless Worker 1 prefers an explicit `0.0.9`-shipped line folded up.

### Review outcome

`review-accepted` (accept-with-escalation). The slice's named contract landed in full — all seven doc surfaces edited per spec, the card moved to `DONE-033-0.0.9` exactly once with the four DoD boxes ticked and the spec reference correct, the GLOSSARY status flipped and pre-`033` caveats swept (`SyncMisuseError` async caveat preserved), CHANGELOG bullets under `[Unreleased]` with no version promotion, DB-backed regen proven idempotent, and all `manage.py`/script checks green. The two open items (A5 `051`/`052` misnumber + DONE-032 historical body; the snapshot BoardDoc staleness) require spec context Worker 2 cannot provide and are escalated to Worker 1 per the `Escalated:` path. The Low (dangling `#djangotypedefinition` anchor) is a DB-then-regen one-liner that Worker 1 may fold into the snapshot/A5 reconciliation pass or hand back to Worker 2; it does not block acceptance. Status set to `review-accepted`.

---

## Final verification (Worker 1)

Read the full artifact (Plan / Build report / Review), the current working-tree diff (`git status --short` — the expected Slice-7 doc + DB surfaces plus the accepted Slice-1..6 baseline), my memory file, and re-grounded the contested DB facts directly against `examples/fakeshop/db.sqlite3` via `manage.py shell`. Outcome: **`revision-needed`** — the slice's *named* contract (the seven doc surfaces + the card move) genuinely landed, but two slice-introduced defects (one Medium, one Low) need DB-backed fixes I cannot make (source/DB are Worker-2-owned). The two `051`/`052` escalation axes (A1/A2) are dispositioned as pre-existing / shipped-history maintainer follow-ups, not Worker-2 work.

### Final-verification checklist outcomes

1. **Spec slice checklist audit.** The Plan's `### Spec slice checklist (verbatim)` carries the two verbatim boxes (the Slice-7 line + the single doc-surface sub-bullet). Both are `- [x]` and both contracts genuinely landed in the diff: all seven named doc surfaces (`docs/GLOSSARY.md`, `docs/README.md`, `docs/TREE.md`, `TODAY.md`, `README.md`, `CHANGELOG.md`, `KANBAN.md`) are edited, and the card re-renders as `DONE-033-0.0.9` (KANBAN.md:1562/102/248) with all four DoD boxes ticked and the spec reference correct. **No silently un-ticked or over-ticked verbatim box** — the ticks stand. The plan's `A5` is a *plan sub-step*, not a verbatim spec sub-check; it is explicitly dispositioned below (A1/A2), so nothing is left silently unaddressed.
2. **DB-backed integrity / idempotence.** Confirmed by re-grounding, not re-regenerating (since I set `revision-needed`, Worker 2 re-verifies idempotence after its DB edits): `check_spec_glossary.py --spec docs/spec-033-…` → `OK: 38 terms` (green); `manage.py check` was green in both the build report and Worker 3's re-run; Worker 3 independently proved the three rendered docs byte-identical across a second regenerate (SHA-1 unchanged). The two fixes below are additional DB edits — Worker 2 must re-run all three `build_*` scripts and re-confirm a second run yields no diff.
3. **No version bump / no heading promotion (DoD 12).** Verified directly: `git diff --name-only` lists **no** `pyproject.toml` / `__init__.py` / `tests/base/test_init.py` / `uv.lock`. On disk `version = "0.0.8"` (pyproject:4) and `__version__ = "0.0.8"` (__init__.py:25). `CHANGELOG.md` top-of-list heading is `## [Unreleased]` (line 19); `grep -E '^## \[0\.0\.9\]'` → none. **DoD 12 holds.**
4. **check_spec_glossary after spec edit.** My only spec edit is the status-line (line 5) re-verification touch — no glossary-anchor change. Ran `uv run python scripts/check_spec_glossary.py --spec docs/spec-033-connection_optimizer-0_0_9.md` → `OK: 38 terms` (exit 0).
5. **Spec status line (~line 5).** Light-touched to reflect reality: Slice 7 is no longer "not yet started" — it is built + reviewed and in a final-verification revision loop. Recorded under `### Spec changes made (Worker 1 only)`. **Not** `final-accept`, so the card's functional work is NOT yet declared done; the revision loop (then integration pass + final gate) still precedes maintainer handoff.

### Disposition of the escalated items

**(A1) — the `TODO-BETA-051-0.1.5` spec misnumber → DEFERRED (accepted as a maintainer follow-up; no spec edit).** Re-grounded against the DB: `Card(number=51)` = `TODO-BETA-051-0.1.4` "Stable choice enum naming override" (unrelated); `Card(number=52)` = `TODO-BETA-052-0.1.5` "Fakeshop GraphQL schema activation" — the card the spec describes by topic *and* version (`0.1.5`). Worker 2 and Worker 3's analysis is correct: the spec's `TODO-BETA-051-0.1.5` is a genuine misnumber for card 052. Scope analysis:
   - The misnumber is purely an **inline-label** error. All seven spec uses (lines 13, 82, 130, 242, 393, 561, 580 — and the wrap instruction at 554) resolve through the single shared `[kanban]` ref-id, which targets `KANBAN.md` as a whole; there is **no** card-specific link-def, so the link *target* is unaffected. A spec-side fix is a contained text sweep, not a link-graph ripple.
   - **But the misnumber also lives in SOURCE and DOC I cannot edit:** the Slice-6 `examples/fakeshop/apps/products/schema.py` docstring (~line 199 `TODO-BETA-051-0.1.5`) and `TODAY.md` (a Slice-7 doc edit Worker 2 deliberately kept at `051` to match the spec/baseline rather than diverge unilaterally). Correcting only the spec's labels would **introduce a new spec-vs-source divergence** (spec says 052, source says 051) — strictly worse than the current uniform-but-wrong state and a violation of the root-cause-over-surface-patch rule (`AGENTS.md`).
   - **The misnumber predates 033** — the spec was authored with it (Revision 1), and 033 merely propagated it; it is not a defect this build introduced. The complete, root-cause fix (spec + `schema.py` docstring + `TODAY.md`, uniform) is a cross-surface sweep that is out of proportion to a doc-wrap card and would expand Slice 7's source scope. **Decision: do NOT make a partial spec-only edit** (it creates divergence); record the `051`→`052` misnumber as a **maintainer follow-up** to fix uniformly across the spec + `schema.py` docstring + `TODAY.md` in one pass (cleanest folded into the next spec author's Step-8 cross-reference sweep, or a standalone maintainer fixup). This keeps the spec internally consistent with the shipped source today.

**(A2) — the stale `WIP-ALPHA-033` "must land with 033" sentence on shipped `DONE-032` → DEFERRED (out-of-scope shipped-card history; no edit).** Re-grounded: the products-conversion "must land with 033" sentence lives **only** on `CardItem id=943` (DONE-032, `Other` section, `order=65`), and it still carries a live `WIP-ALPHA-033-0.0.9` reference (now stale: 033 is DONE). It is **not** on card 51 or card 52 — card 52's items carry no products-conversion/033 sentence at all (verified). So the spec's line-554 wrap target *as literally written* ("the `TODO-BETA-051-0.1.5` card body's products-conversion sentence") does not exist. The only real candidate is shipped-card history.
   - Worker 2 already reconciled the **live** equivalent — card 033's own `Other` `order=10` "empty plan" / "must land with 033" sentence — softening it to past tense under discretion D3 (build report A4.2). That is the sentence on the *active* card that mattered for board currency.
   - Softening DONE-032's `order=65` would alter a *shipped* card's historical record (a record of what 032 deliberately deferred) on a guess about the spec author's intent, given the named target doesn't map. Worker 2's posture — defer rather than rewrite shipped history on a guess — is correct. **Decision: leave DONE-032 `order=65` as dated shipped-card history; record as out-of-scope** (and fold into the (A1) maintainer follow-up if the maintainer wants the stale live `WIP-ALPHA-033` reference there softened for board currency — that is the same shipped-history call).

**(B) — Medium: stale `snapshot` BoardDoc renders "DONE-033 … remains in progress" → `revision-needed` (Worker 2 DB edit).** Re-grounded against the DB and the rendered file: the `snapshot` BoardDoc (`namespace=kanban`, `key=snapshot`, `kind=Reference`) renders at KANBAN.md:64 "`DONE-033-0.0.9` (connection-aware optimizer planning) **remains in progress**" — directly contradicting line 138 ("No active WIP cards") and the card's Done placement, **both produced by this slice's own card move**. The board's preamble (line 3 "Last refreshed: 2026-06-13", "based on the current code") frames it as a *live* surface. BUILD.md `### Documentation / release sanity` names exactly this: "no obsolete … 'in progress' … wording remains in files the slice deliberately updated" — and KANBAN.md is the file this slice's headline act updates.
   - **Why in-scope (not pre-existing):** the "remains in progress" falsity was *caused by this slice* moving the card while leaving the snapshot prose. The slice must not ship a KANBAN.md whose own card move contradicts its board narrative.
   - The same BoardDoc *also* carries pre-existing staleness this card directly falsifies: KANBAN.md:80 lists `connection.py` AND "connection-aware planning for Relay-style nested connection selections" under "**Layer 3 public subsystems are still planned only**" — `connection.py` shipped in `DONE-030`, and connection-aware planning shipped *in this card*. A surgical one-clause fix would leave the row internally inconsistent (Worker 3's point), so Worker 2 should refresh the whole `snapshot` row coherently in one ORM edit.

**(C) — Low: dangling in-page anchor `#djangotypedefinition` in the rewritten GLOSSARY entry → `revision-needed` (Worker 2 DB edit).** Verified directly: `docs/GLOSSARY.md:232` introduces `` [`DjangoTypeDefinition`](#djangotypedefinition) `` and no GLOSSARY heading slugifies to `djangotypedefinition` (`grep -i '^## DjangoTypeDefinition'` → none). It is **slice-introduced** (Worker 3: 0 at HEAD, 1 in the working tree) — a broken link in newly-shipped glossary content. The parallel CHANGELOG bullet linked the same symbol correctly to source (`[`DjangoTypeDefinition`][definition]`). A broken link in just-shipped glossary content warrants the fix. Lowest-friction: drop the dead in-page link and keep `DjangoTypeDefinition` as a plain code span (GLOSSARY convention is to link symbols to other GLOSSARY anchors, not source — there is no glossary heading for it, so plain code span is correct). DB-backed edit (`GlossaryTerm.body` for anchor `connection-aware-optimizer-planning`) + regenerate.

### DRY check across this slice and prior slices

No new duplication. Slice 7 ships zero code/helpers/constants (confirmed: `git diff` touches no `.py` source). The doc prose tells one coherent story drawn from the spec's canonical vocabulary; Worker 3 confirmed the CHANGELOG bullet is character-for-character identical to the plan's B5 wording, and the GLOSSARY body lives only on the `GlossaryTerm.body` DB column (no render double-source). The integration pass (next, after this revision loop closes) owns the cross-module constant-pair candidate I carried in memory (`walker._CONNECTION_PAGINATION_ARGS` vs `extension._PAGINATION_ARG_NAMES`) — not a Slice-7 surface.

### Consolidated edits Worker 2 must make (one re-pass)

Both are DB-backed (`examples/fakeshop/db.sqlite3` via the Django ORM in a `manage.py shell` session), then regenerate — **never** a hand-edit of the rendered `.md`/`.html`:

1. **(B) `snapshot` BoardDoc currency** — `BoardDoc.objects.get(namespace="kanban", key="snapshot")`; edit `.body`:
   - The "Active version" paragraph clause `` `{{card_ref:19}}` (connection-aware optimizer planning) remains in progress `` → state it has **shipped** with the rest of the `0.0.9` Relay cohort (so the rendered narrative agrees with "No active WIP cards" and the card's Done placement). Keep the `{{card_ref:19}}` token (it auto-renders `DONE-033-0.0.9`).
   - The "**Layer 3 public subsystems are still planned only**" list: remove `connection.py` (shipped in `DONE-030`) and remove the "connection-aware planning for Relay-style nested connection selections (`{{card_ref:19}}`)" bullet (shipped in this card). Re-coher the surrounding list so it does not read internally inconsistent. (If `connection.py` belongs in a "shipped" list elsewhere in the snapshot, move it there; otherwise just drop the stale "still planned" entries.)
2. **(C) dangling GLOSSARY anchor** — `GlossaryTerm.objects.get(anchor="connection-aware-optimizer-planning")`; in `.body`, change `` [`DjangoTypeDefinition`](#djangotypedefinition) `` to a plain code span `` `DjangoTypeDefinition` `` (drop the dead in-page link). Leave the adjacent `` [`DjangoConnectionField`](#djangoconnectionfield) `` link intact (that anchor resolves). `.save()`.

Then: `uv run python scripts/build_kanban_md.py` + `build_kanban_html.py` + `build_glossary_md.py`; confirm a **second** run of all three yields no further diff (idempotence); `git diff docs/GLOSSARY.md` shows only the dropped in-page link; `KANBAN.md` line ~64 no longer says "remains in progress" and the snapshot's "still planned only" list no longer names `connection.py` / connection-aware planning; `manage.py check` green; `import_spec_terms --check` green (run the writing `import_spec_terms` first if needed — the documented pre-sync gap). No `.py` source, no version files, CHANGELOG untouched.

**NOT for Worker 2 (deferred, recorded above):** (A1) the `051`→`052` spec/`schema.py`/`TODAY.md` misnumber sweep, and (A2) softening the DONE-032 `order=65` shipped-history sentence — both are pre-existing / shipped-history maintainer follow-ups, out of scope for this re-pass.

### Summary

Slice 7 (doc + card-completion wrap) delivered its named contract: the seven doc surfaces are edited per spec line 552/554, the GLOSSARY `connection-aware-optimizer-planning` entry flipped to `shipped (\`0.0.9\`)` with the pre-`033` caveats swept (the `SyncMisuseError` async caveat correctly preserved), the CHANGELOG carries the four authorized bullets under `[Unreleased]` with no version-heading promotion, and `WIP-ALPHA-033-0.0.9` moved to `DONE-033-0.0.9` (exactly once, four DoD boxes ticked, spec reference correct, DB-backed and idempotent). DoD item 12 holds (no version-file edits, no heading promotion). **Set `revision-needed`** for two slice-introduced defects that need DB edits Worker 1 cannot make: (B) the `snapshot` BoardDoc still narrates the now-Done card as "remains in progress" (plus the pre-existing `connection.py`-still-planned staleness this card falsifies), and (C) a dangling `#djangotypedefinition` in-page anchor in the rewritten GLOSSARY body. The `051`/`052` misnumber (A1) and the DONE-032 shipped-history sentence (A2) are dispositioned as pre-existing / shipped-history **maintainer follow-ups** — a spec-only partial fix of the misnumber would create a worse spec-vs-source divergence, and rewriting shipped-card history on a guess is not warranted. Once Worker 2 lands the two DB fixes and Worker 3 re-accepts, Worker 1 re-runs final verification; the integration pass and final test-run gate still follow before maintainer handoff.

### Spec changes made (Worker 1 only)

- `docs/spec-033-connection_optimizer-0_0_9.md` line 5 (status line) — Slice 7, this final-verification pass. Reason: per the per-spawn status-line re-verification rule, "Slice 7 not yet started" was stale (Slice 7 is built + reviewed and in a final-verification revision loop). Updated to: "Slice 7 (doc + card-completion wrap) built and reviewed, in a final-verification revision loop (a snapshot BoardDoc currency fix and a dangling GLOSSARY anchor fix routed back to the build pass)." No glossary-anchor change; `check_spec_glossary` → `OK: 38 terms`, exit 0.
- **No `051`→`052` misnumber edit made** (A1): deferred as a maintainer follow-up rather than applied spec-side, because a spec-only fix would diverge from the un-editable `schema.py` docstring + `TODAY.md` that carry the same label. Recorded here so the non-edit is an explicit, reasoned decision, not an oversight.

---

## Build report (Worker 2, pass 2)

Re-pass against Worker 1's final-verification `### Consolidated edits Worker 2 must make (one re-pass)` (artifact lines 320-331). Applied the two DB-backed fixes (B + C), regenerated, and verified. The two deferred items (A1 `051`→`052` misnumber; A2 DONE-032 `order=65` shipped-history sentence) were left exactly as they are per Worker 1's disposition — confirmed untouched below.

### Files touched

**(a) DB-backed — edited via the Django ORM in `examples/fakeshop/manage.py shell`, then regenerated:**

- `examples/fakeshop/db.sqlite3` — two ORM `.save()` edits (git-tracked, reversible via `git checkout -- examples/fakeshop/db.sqlite3`):
  - **(B) `snapshot` BoardDoc** (`namespace="kanban"`, `key="snapshot"`) — `.body` edits:
    - The `### In progress` clause `` `{{card_ref:19}}` (connection-aware optimizer planning) remains in progress. `` → `` `{{card_ref:19}}` (connection-aware optimizer planning) has shipped, closing out the cohort. `` — **`{{card_ref:19}}` token kept intact** (auto-renders `DONE-033-0.0.9`). Re-cohered the same sentence's lead-in `the Relay connection cohort is nearly complete — ` → `the Relay connection cohort is complete — ` so the narrative is internally consistent (all four cohort cards now shipped; "nearly complete" would have contradicted "has shipped").
    - The `### Still not implemented` "Layer 3 public subsystems are still planned only" list: removed the `` - `connection.py` `` bullet (shipped in `DONE-030`). Remaining list reads `aggregates/` / `fieldset.py` / `permissions.py` / `utils/queryset.py` — coherent.
    - The "Optimizer follow-up ideas remain outside the shipped B1-B8 surface" list: removed the `` - connection-aware planning for Relay-style nested connection selections (`{{card_ref:19}}`) `` bullet (shipped in this card). Remaining list reads the single `model-property / cached-property optimization hints` bullet — coherent.
  - **(C) `connection-aware-optimizer-planning` `GlossaryTerm`** (`anchor="connection-aware-optimizer-planning"`) — `.body`: changed the dangling in-page link `` [`DjangoTypeDefinition`](#djangotypedefinition) `` to a plain code span `` `DjangoTypeDefinition` `` (rendered fragment `` `DjangoTypeDefinition``.relation_connections` `` — two adjacent code spans, byte-matching how `CHANGELOG.md:35` references the same symbol after its link markup). The adjacent `` [`DjangoConnectionField`](#djangoconnectionfield) `` link (anchor resolves) left intact.
- `KANBAN.md` — regenerated (`scripts/build_kanban_md.py`); snapshot now renders `DONE-033-0.0.9 … has shipped, closing out the cohort` (KANBAN.md:64), no "remains in progress", `connection.py` gone from the still-planned list (KANBAN.md:69-73), connection-aware-planning bullet gone from the follow-up list (KANBAN.md:78-79).
- `KANBAN.html` — regenerated (`scripts/build_kanban_html.py`).
- `docs/GLOSSARY.md` — regenerated (`scripts/build_glossary_md.py`); connection-aware entry first paragraph (GLOSSARY.md:232) no longer carries the `#djangotypedefinition` dead anchor.

**Not touched this re-pass (deferred per Worker 1's disposition):**

- **(A1)** the `TODO-BETA-051-0.1.5` → `052` misnumber — confirmed still present and unchanged in `docs/spec-033-connection_optimizer-0_0_9.md` (8 occurrences), `examples/fakeshop/apps/products/schema.py` (1), and `TODAY.md` (1).
- **(A2)** the DONE-032 `order=65` "must land with 033" / `WIP-ALPHA-033-0.0.9` sentence — confirmed still present and unchanged on `CardItem id=943` (DONE-032, `Other` section, `order=65`).
- No `.py` source, no tests, no version files (`pyproject.toml`, `__version__`, `tests/base/test_init.py`, `uv.lock`), `CHANGELOG.md` not re-touched this pass, and the active spec is Worker-1-owned. The Slice-1..6 baseline + the prior Slice-7 hand-authored doc edits (`CHANGELOG.md` / `README.md` / `TODAY.md` / `docs/README.md` / `docs/TREE.md`) are unchanged by this re-pass.

### Tests added or updated

None, doc/DB-only. (0 new tests, 0 modified tests — Slice 7 ships zero `.py` logic.)

### Validation run

- `uv run ruff format .` — pass (`251 files left unchanged`; the COM812 warning is pre-existing config noise, not a change). No-op as expected for DB/doc-only edits.
- `uv run ruff check --fix .` — pass (`All checks passed!`); zero churn.
- `git status --short` after both ruff invocations — churn classified:
  - **This re-pass (slice-intended):** `examples/fakeshop/db.sqlite3` (the two ORM edits), `KANBAN.md` / `KANBAN.html` / `docs/GLOSSARY.md` (regenerated).
  - **Prior Slice-7 pass (already in tree, not this re-pass):** `CHANGELOG.md`, `README.md`, `TODAY.md`, `docs/README.md`, `docs/TREE.md`.
  - **Accepted Slice-1..6 baseline (not this slice):** the seven `django_strawberry_framework/**` source files, `tests/optimizer/test_extension.py` / `test_plans.py` / `test_walker.py`, `tests/test_relay_connection.py`, `examples/fakeshop/apps/products/schema.py`, `test_query/test_library_api.py` / `test_products_api.py`.
  - **Worker-1-owned:** `docs/spec-033-connection_optimizer-0_0_9.md` (status-line touch).
  - **Untracked:** the `bld-*.md` + `build-033-*.md` build artifacts.
  - No unrelated tool churn to revert.
- `uv run python examples/fakeshop/manage.py import_spec_terms --check` — `OK: 33 done cards have glossary links.` (passed directly; the prior pass already ran the writing sync, so no pre-sync failure this pass).
- `uv run python examples/fakeshop/manage.py check` — `System check identified no issues (0 silenced).`
- **Regenerate-twice idempotence** — after the regenerate, snapshotted all three rendered files, re-ran all three `build_*` scripts a SECOND time, and `diff`'d: `KANBAN.md`, `KANBAN.html`, `docs/GLOSSARY.md` are all byte-IDENTICAL across the repeat run (DB == file, genuinely regenerated, not hand-edited).
- **KANBAN confirmation** — `grep "remains in progress" KANBAN.md` → none; the snapshot's still-planned list no longer names `connection.py`; the follow-up list no longer carries the connection-aware-planning bullet; `{{card_ref:19}}` rendered as `DONE-033-0.0.9`.
- **GLOSSARY confirmation** — `grep -i djangotypedefinition docs/GLOSSARY.md` → none (the `#djangotypedefinition` dangling anchor is gone). `git diff docs/GLOSSARY.md` delta on the connection-aware entry is scoped to that one in-page-link removal; `DjangoConnectionField` link intact.
- `git diff --check -- KANBAN.md KANBAN.html docs/GLOSSARY.md` — clean (no whitespace errors / conflict markers).

### Implementation notes

- **(C) plain-code-span shape (matching CHANGELOG):** the source fragment is `` [`DjangoTypeDefinition`](#djangotypedefinition)`.relation_connections` `` — the link is immediately followed by a separate `` `.relation_connections` `` code span. Stripping the link yields `` `DjangoTypeDefinition``.relation_connections` `` (two adjacent code spans), which is exactly the form `CHANGELOG.md:35` produces after its reference-style link (`` [`DjangoTypeDefinition`][definition]`.relation_connections` `` → same two-code-span shape). Per the GLOSSARY convention (symbols link to other GLOSSARY anchors, never source files), the plain code span is the correct lower-friction fix rather than re-pointing at `definition.py`.
- **(B) coherence beyond the two named removals:** Worker 1's edit list said to "re-cohere the surrounding prose so the list reads correctly." Beyond the named "remains in progress" clause + two list removals, I made one adjacent coherence edit — the "nearly complete" → "complete" lead-in in the same In-progress sentence — because leaving "nearly complete" alongside "card 19 has shipped" (with all four cohort cards now shipped) would have been self-contradictory. No other snapshot staleness touched: per Worker 1's instruction I fixed ONLY the DONE-033 "in progress" contradiction + the two now-shipped items the finding names, and did not attempt the BoardDoc's unrelated pre-existing staleness.
- **Edit method:** both ORM edits used exact-string `.replace()` with `count(...) == 1` pre-assertions (each target string is unique in its row) so a silent multi-match or no-match would have failed loudly. The first run tripped a (wrong) post-assertion about the joined-code-span shape BEFORE any `.save()`, so nothing partial was written; corrected the assertion to the true two-adjacent-code-span shape and re-ran cleanly.

### Notes for Worker 3

- **(B) resolved:** `snapshot` BoardDoc `.body` edited via ORM + regenerated. The now-Done card no longer narrates as "remains in progress"; the In-progress cohort sentence reads coherently ("cohort is complete … card 19 has shipped, closing out the cohort"); the "Layer 3 … still planned only" list dropped `connection.py` (shipped DONE-030) and the "Optimizer follow-up ideas" list dropped the connection-aware-planning bullet (shipped this card). `{{card_ref:19}}` token preserved (renders `DONE-033-0.0.9`). Both lists read internally consistent after the removals.
- **(C) resolved:** the dangling `#djangotypedefinition` in-page anchor in the `connection-aware-optimizer-planning` GLOSSARY body is now a plain code span (matching the CHANGELOG reference to the same symbol). No GLOSSARY heading slugifies to `djangotypedefinition`, so plain code span is the convention-correct form.
- **A1 / A2 deliberately left:** the `051`→`052` misnumber (spec + `schema.py` docstring + `TODAY.md`) and the DONE-032 `order=65` shipped-history sentence are Worker-1-dispositioned maintainer follow-ups — confirmed untouched (grep + ORM read in the Validation run).
- Review surfaces are the regenerated `KANBAN.md` / `KANBAN.html` / `docs/GLOSSARY.md` + the `examples/fakeshop/db.sqlite3` diff. The DB-backed edits are reversible as a unit (`git checkout -- examples/fakeshop/db.sqlite3` + re-run the three `build_*` scripts). Idempotence re-proven this pass.
- Static helper (`scripts/review_inspect.py`): correctly skipped — zero `.py` logic in this re-pass (markdown + ORM-edited DB only), same disposition as the original pass.

### Notes for Worker 1 (spec reconciliation)

Nothing new surfaced. Both fixes were exactly the two DB-backed items Worker 1's final verification routed back (B + C); both landed cleanly with no spec implications. A1/A2 left as dispositioned (maintainer follow-ups) — no new information that would change that disposition. The integration pass and final test-run gate still follow per the workflow.

---

## Review (Worker 3, pass 2)

Re-review of Worker 2's pass-2 DB-backed fixes against Worker 1's final-verification disposition (`### Consolidated edits Worker 2 must make`, artifact lines 320-331): (B) Medium snapshot-BoardDoc currency, (C) Low dangling GLOSSARY anchor — both `revision-needed`-routed to Worker 2; (A1) `051`→`052` misnumber and (A2) DONE-032 `order=65` shipped-history sentence — deferred maintainer follow-ups Worker 2 must NOT touch. Scope filter: this re-pass touched only `examples/fakeshop/db.sqlite3` + the regenerated `KANBAN.md` / `KANBAN.html` / `docs/GLOSSARY.md`. The Slice-1..6 `.py` baseline, the prior-pass hand-authored docs (`CHANGELOG.md` / `README.md` / `TODAY.md` / `docs/README.md` / `docs/TREE.md`), and the Worker-1-owned spec edit are not re-reviewed here.

### Pass-1 finding dispositions

**Medium (B) — `snapshot` BoardDoc "remains in progress" → CONFIRMED-FIXED at the root.** Verified directly in the regenerated `KANBAN.md`:
- `grep "remains in progress" KANBAN.md` → 0 hits (was 1 at the snapshot's In-progress clause). The clause now reads `` `DONE-033-0.0.9` (connection-aware optimizer planning) has shipped, closing out the cohort `` (KANBAN.md:64). The `{{card_ref:19}}` token still resolves to `DONE-033-0.0.9` (`grep "card_ref" KANBAN.md` → 0 raw tokens leftover; `DONE-033-0.0.9` appears 10× consistently). The card moved to `## Done` (heading KANBAN.md:1560) and `## In progress` reads "No active WIP cards" — the contradiction the card move introduced is resolved.
- **Dangling "nearly complete" contradiction also closed:** `grep "nearly complete" KANBAN.md` → 0 hits. The cohort lead-in now reads "the Relay connection cohort is **complete**" — Worker 2 correctly caught that leaving "nearly complete" beside "card 19 has shipped" (all four cohort cards now Done) would self-contradict. This is the prose-coherence point my pass-1 finding flagged ("no dangling 'nearly complete' contradiction") and Worker 1's edit list named ("re-coher the surrounding list").
- **`connection.py` + connection-aware-planning bullets removed from the "still planned only" / follow-up lists:** the snapshot's "Layer 3 public subsystems are still planned only" list no longer names `connection.py` (shipped `DONE-030`); remaining entries are `aggregates/` / `fieldset.py` / `permissions.py` / `utils/queryset.py` (coherent). The "Optimizer follow-up ideas remain outside the shipped B1-B8 surface" list no longer carries the "connection-aware planning for Relay-style nested connection selections" bullet (shipped this card); the single `model-property / cached-property optimization hints` bullet remains (coherent). The other `connection.py` mentions in `KANBAN.md` (the DONE-033/051/030 card bodies, the deferred-audit-finding prose at line 297) are unrelated card-body content, NOT the snapshot's still-planned list — correctly left. No internal inconsistency remains in the refreshed snapshot.

**Low (C) — dangling `#djangotypedefinition` anchor → CONFIRMED-FIXED.** Verified directly: `grep -i "djangotypedefinition" docs/GLOSSARY.md` → 1 hit, and it is now a **plain code span** (`` `DjangoTypeDefinition``.relation_connections` ``, GLOSSARY.md:232), no `(#djangotypedefinition)` link. Byte-compared against the CHANGELOG form: `CHANGELOG.md:35` renders the same symbol as `[`DjangoTypeDefinition`][definition]`.relation_connections`` → after the reference-style link resolves, the rendered output is the same two-adjacent-code-span shape (`` `DjangoTypeDefinition` `` followed by `` `.relation_connections` ``). The GLOSSARY form matches the CHANGELOG form as required. The adjacent `` [`DjangoConnectionField`](#djangoconnectionfield) `` and `` [`DjangoType`](#djangotype) `` links in the same paragraph resolve to real anchors. The entry's `**See also:**` line is preserved (GLOSSARY.md:236).

### A1 / A2 correctly left untouched (CONFIRMED)

- **(A1) `TODO-BETA-051-0.1.5` misnumber — UNCHANGED.** Still present at its pre-existing count in all three carriers: `docs/spec-033-…md` (8 occurrences), `examples/fakeshop/apps/products/schema.py` (1), `TODAY.md` (1). The `051` label lines that appear in the working-tree `git diff` of `TODAY.md`/`schema.py` are from the prior Slice-6/Slice-7 passes (the label deliberately kept consistent), NOT this re-pass — the re-pass diff is confined to `db.sqlite3` + the 3 rendered docs. Worker 2 did not partial-fix the spec (which would have created a spec-vs-source divergence per Worker 1's A1 reasoning). Correctly deferred as a maintainer follow-up.
- **(A2) DONE-032 `order=65` shipped-history sentence — UNCHANGED.** The single remaining `WIP-ALPHA-033` reference in `KANBAN.md` is at line 1814, inside the **DONE-032** card block (heading KANBAN.md:1664) — the products-conversion "must land with 033" sentence Worker 1 dispositioned as out-of-scope shipped-card history. Confirmed it renders unchanged (still carrying the now-stale `WIP-ALPHA-033-0.0.9` reference, exactly as Worker 1 chose to leave it). Worker 2 did not rewrite shipped-card history on a guess. Correctly deferred.

### DB-backed integrity / idempotence (re-run myself)

Re-ran all three regenerate scripts (`build_kanban_md.py` / `build_kanban_html.py` / `build_glossary_md.py`) from the repo root and compared SHA-1 before/after:
- `KANBAN.md` `ffe9bc78` → `ffe9bc78` (unchanged); `KANBAN.html` `cb6b730e` → `cb6b730e` (unchanged); `docs/GLOSSARY.md` `d555d96c` → `d555d96c` (unchanged). **Byte-identical** → DB == rendered file, genuinely regenerated (not hand-edited), idempotent. `git diff --stat` after my re-run is unchanged (KANBAN.md 244 / GLOSSARY.md 16 / KANBAN.html 2). My re-run changed nothing, so **no `git checkout --` restore was needed.**
- `uv run python examples/fakeshop/manage.py import_spec_terms --check` → `OK: 33 done cards have glossary links.` (passed directly; the writing sync from the prior pass already reconciled the spec-mention lag).
- `uv run python examples/fakeshop/manage.py check` → `System check identified no issues (0 silenced).`
- `git diff --check -- KANBAN.md KANBAN.html docs/GLOSSARY.md` → clean (no whitespace errors / conflict markers).

### No scope creep (CONFIRMED)

This re-pass touched ONLY `examples/fakeshop/db.sqlite3` + the 3 regenerated docs. Verified: no `.py` source/test change attributable to the re-pass (the `django_strawberry_framework/**` + `tests/**` + `products/schema.py` + `test_query/**` diffs are the accepted Slice-1..6 baseline); no version-file change (`pyproject.toml` / `__init__.py` / `tests/base/test_init.py` / `uv.lock` all absent from `git diff --name-only`; on-disk version stays `0.0.8`); the active spec edits are Worker-1-owned; `CHANGELOG.md` was not re-touched this pass and carries **no** `## [0.0.9]` version-heading (top-of-list stays `## [Unreleased]`; `grep -E '^## \[0\.0\.9\]'` → none). DoD-12 / Decision-12 hold. `metarelation_shapes` `SyncMisuseError` async caveat preserved (the A1.3 "stays" rule honored, unchanged from pass 1).

### DRY findings

None. The re-pass introduces no code, no helper, no constant. Both edits are DB-`.body` text reconciliations; the GLOSSARY body remains single-sourced on the `GlossaryTerm.body` column (no render double-source — idempotent regen confirms).

### Public-surface check

`git diff -- django_strawberry_framework/__init__.py` is empty — `__init__.py` does not appear in the working-tree diff. No `__all__` / re-export change. The re-pass ships zero `.py` logic. Public surface clean.

### CHANGELOG sanity

`CHANGELOG.md` was NOT modified by this re-pass (it is the prior Slice-7 pass's deliverable, already accepted at pass 1; unchanged this re-pass — confirmed via the re-pass scope: only `db.sqlite3` + the 3 rendered docs changed). The pass-1 CHANGELOG sanity (no version-heading promotion, four authorized bullets under `[Unreleased]`, canonical wording, all link-defs resolve) stands. Re-confirmed only the load-bearing invariant for this pass: no `## [0.0.9]` heading exists; top-of-list is `## [Unreleased]`.

### Documentation / release sanity

- **(B) snapshot currency:** the file this slice's headline DB act updates (`KANBAN.md`) no longer carries the obsolete "in progress" / "nearly complete" wording the card move falsified — BUILD.md `### Documentation / release sanity`'s "no obsolete … 'in progress' … wording remains" is now satisfied. The refreshed snapshot is internally consistent (the now-shipped items removed from both still-planned lists; no dangling reference to `connection.py` or connection-aware planning as unimplemented).
- **(C) GLOSSARY links:** the previously-dangling `#djangotypedefinition` in-page anchor is gone; a full-file check finds no remaining dangling in-page anchor introduced by the slice. All new/moved GLOSSARY links resolve.
- **Card move integrity (unchanged from pass 1):** `DONE-033-0.0.9` present once in `## Done` with the four DoD boxes `- [x]` and the spec reference correct; `## In progress` "No active WIP cards"; dependency card_refs on cards 051/FieldSet auto-updated to `DONE-033-0.0.9`.

### What looks solid

- Worker 2 fixed both findings **at the root via the DB** (never hand-editing the rendered `.md`/`.html`), and proved idempotence — which I independently re-confirmed (byte-identical SHA-1 across a second regenerate). The `{{card_ref:19}}` token was preserved through the BoardDoc edit so the auto-render stays correct.
- The (B) fix went beyond the two literally-named removals to the **prose-coherence** the finding required: catching the "nearly complete" → "complete" lead-in that would otherwise have contradicted "has shipped." This is exactly the internal-consistency concern that made (B) an escalation rather than a one-clause edit at pass 1.
- The (C) fix chose the convention-correct shape (plain code span matching the CHANGELOG, not a re-point at `definition.py` source) per the GLOSSARY "symbols link to other GLOSSARY anchors, never source" rule.
- A1/A2 were left **exactly** as Worker 1 dispositioned — Worker 2 resisted the temptation to "while I'm here" sweep the misnumber or rewrite shipped history. Disciplined scope adherence.

### Temp test verification

No temp tests created. The re-pass ships zero `.py` logic — there is no behavior to probe. Verification was doc/DB inspection + re-running the three regenerate scripts and the two `manage.py` commands (idempotence by SHA-1, `import_spec_terms --check`, `manage.py check`, `git diff --check`). Static inspection helper (`scripts/review_inspect.py`): **skipped**, concurring with the plan and pass 1 — it parses `.py` source as text/AST and this re-pass touches no `.py` source (ORM-edited DB + regenerated markdown only). Skip reason: doc/DB-only re-pass, zero Python logic.

### Notes for Worker 1 (spec reconciliation)

Nothing new. Both routed-back defects (B + C) are confirmed-fixed at the root; A1 (`051`→`052` misnumber) and A2 (DONE-032 `order=65` shipped-history sentence) are confirmed left untouched per your disposition — they remain maintainer follow-ups, recorded in your pass-2 final verification and unchanged here. No new spec implications surfaced. Once you re-run final verification, the spec status line (currently "in a final-verification revision loop", line 5) is the one item that will want a light touch to reflect Slice 7 reaching `final-accepted`; that is Worker-1-owned and not a re-review finding.

### Review outcome

`review-accepted`. Both pass-1 findings are confirmed-fixed at the root (B: snapshot BoardDoc no longer narrates the now-Done card as "remains in progress", the "nearly complete" contradiction closed, `connection.py` + connection-aware-planning removed from the still-planned/follow-up lists, snapshot internally consistent, `{{card_ref:19}}` still resolves to `DONE-033-0.0.9`; C: `#djangotypedefinition` dangling anchor replaced with a plain code span matching the CHANGELOG form). A1/A2 correctly left untouched. DB-backed regen proven idempotent (SHA-1 byte-identical), `import_spec_terms --check` + `manage.py check` green, `git diff --check` clean. No scope creep (db.sqlite3 + the 3 rendered docs only; no source/test/spec/version-file change; no new CHANGELOG version-heading). No new findings (0 High / 0 Medium / 0 Low). Status set to `review-accepted`.

---

## Final verification (Worker 1, pass 2)

Closing-round pass for Slice 7 (the LAST in-spec slice). Read the full artifact through Worker 3's pass-2 `review-accepted` (B+C confirmed-fixed at the root, A1/A2 correctly left untouched, idempotence + `import_spec_terms --check` + `manage.py check` all re-confirmed, no new findings), my prior `## Final verification (Worker 1)` section (which set `revision-needed` for B+C), my memory file, and re-grounded every closing-round check directly against the working tree and DB. Outcome: **`final-accepted`** — B and C are resolved at the DB root, A1/A2 remain correctly deferred, the spec slice checklist is accurate, DB-backed integrity holds, DoD item 12 holds, and the status line is light-touched to reflect all seven slices accepted.

### Closing-round checklist outcomes

1. **B + C resolved in the diff (confirmed directly).**
   - **(B) snapshot BoardDoc currency — RESOLVED.** `grep "remains in progress" KANBAN.md` → 0; `grep "nearly complete" KANBAN.md` → 0. The snapshot's `### In progress` block now reads "the Relay connection cohort is **complete** — … `DONE-033-0.0.9` (connection-aware optimizer planning) **has shipped, closing out the cohort**" — internally consistent with `## In progress` "No active WIP cards" and the card's `## Done` placement. The `### Still not implemented` "Layer 3 public subsystems are still planned only" list now reads `aggregates/` / `fieldset.py` / `permissions.py` / `utils/queryset.py` — `connection.py` (shipped `DONE-030`) is gone. The "Optimizer follow-up ideas" list dropped the connection-aware-planning bullet (the single `model-property / cached-property optimization hints` bullet remains) — shipped-in-this-card content correctly removed. The whole snapshot row reads coherently.
   - **(C) dangling GLOSSARY anchor — RESOLVED.** `grep -n "(#djangotypedefinition)" docs/GLOSSARY.md` → 0. GLOSSARY.md:232 now renders `DjangoTypeDefinition` as a plain code span (`` `DjangoTypeDefinition``.relation_connections` ``, matching the CHANGELOG form), with the adjacent `[`DjangoConnectionField`](#djangoconnectionfield)` link still resolving. No dangling in-page anchor remains in the slice's GLOSSARY content.
   - **(A1) `051`/`052` misnumber — UNTOUCHED (per my defer disposition).** Occurrence counts unchanged from Worker 3 pass-2: `docs/spec-033-…md` 8, `examples/fakeshop/apps/products/schema.py` 1, `TODAY.md` 1. No partial spec-side fix was applied (it would create a spec-vs-source divergence). Correctly left for the maintainer follow-up.
   - **(A2) DONE-032 `order=65` shipped-history sentence — UNTOUCHED.** The single remaining `WIP-ALPHA-033` reference in `KANBAN.md` is at line 1814, inside the **DONE-032** card block — the products-conversion "must land with 033" sentence I dispositioned as out-of-scope shipped-card history. Confirmed unchanged.
2. **Spec slice checklist audit.** The Plan's `### Spec slice checklist (verbatim)` carries the two verbatim boxes (the Slice-7 line + the single doc-surface sub-bullet); both `- [x]` and both contracts genuinely landed — all seven named doc surfaces edited, the card re-renders as `DONE-033-0.0.9` with the four DoD boxes ticked and the spec reference correct. **No silently un-ticked or over-ticked box.** The plan's `A5` (the "card 051" products-conversion reconciliation that could not be done as written because the named target does not exist) is recorded as the (A1)+(A2) **maintainer follow-up deferral** under `### Spec changes made (Worker 1 only)` below and in my prior final-verification pass — it is NOT a silent un-tick (A5 is a plan sub-step, not a verbatim spec sub-check; the verbatim boxes stand correctly ticked).
3. **DB-backed integrity / idempotence (re-ran myself).** Snapshotted SHA-1, re-ran all three `build_*` scripts, re-compared: `KANBAN.md` `ffe9bc78` (unchanged), `KANBAN.html` `cb6b730e` (unchanged), `docs/GLOSSARY.md` `d555d96c` (unchanged) — byte-identical, matching Worker 3 pass-2's recorded SHA-1s → DB == rendered file, genuinely regenerated, idempotent. My re-run changed nothing (no `git checkout --` restore needed; the modified-file set is unchanged beyond the build artifacts). `import_spec_terms --check` → `OK: 33 done cards have glossary links.`; `manage.py check` → `System check identified no issues (0 silenced).`
4. **DoD item 12 — no version-file edits, CHANGELOG `[Unreleased]`-only (confirmed).** `git diff --name-only | grep -E 'pyproject.toml|__init__.py|tests/base/test_init.py|uv.lock'` → none. On disk `version = "0.0.8"` (pyproject:4) and `__version__ = "0.0.8"` (__init__.py:25). `CHANGELOG.md` top-of-list heading is `## [Unreleased]` (line 19); `grep -E '^## \[0\.0\.9\]'` → none. DoD 12 holds.
5. **Status line (~line 5) — light-touched on final-accept.** Worker 3 flagged it still read "in a final-verification revision loop." Updated to reflect ALL SEVEN slices accepted, with the integration pass + final test-run gate still following before maintainer handoff, and noting the card moved to `DONE-033-0.0.9` with the on-disk version still `0.0.8` (joint cut, Decision 12). Recorded under `### Spec changes made (Worker 1 only)`.
6. **check_spec_glossary after the status-line edit.** `uv run python scripts/check_spec_glossary.py --spec docs/spec-033-connection_optimizer-0_0_9.md` → `OK: 38 terms - all have glossary entries and at least one spec link.` (exit 0). The status-line edit retained all glossary anchors and broke no spec link.

### DRY check across this slice and prior slices

No new duplication. The pass-2 re-pass touched only `examples/fakeshop/db.sqlite3` (two `.body` text reconciliations) + the three regenerated rendered docs — zero code, helpers, or constants. The GLOSSARY body remains single-sourced on the `GlossaryTerm.body` DB column (idempotent regen confirms no render double-source). The cross-module constant-pair candidate I carry in memory (`walker._CONNECTION_PAGINATION_ARGS` tuple vs `extension._PAGINATION_ARG_NAMES` frozenset) is an **integration-pass** surface, not a Slice-7 finding — carried forward to `bld-integration.md`.

### Summary

Slice 7 (doc + card-completion wrap) is **closed: `final-accepted`**. The slice delivered its named contract — the seven doc surfaces edited per spec line 552/554, the GLOSSARY `connection-aware-optimizer-planning` entry flipped to `shipped (\`0.0.9\`)` with the pre-`033` caveats swept (the `SyncMisuseError` async caveat preserved), the CHANGELOG carrying the four authorized bullets under `[Unreleased]` with no version-heading promotion, and `WIP-ALPHA-033-0.0.9` moved to `DONE-033-0.0.9` exactly once (four DoD boxes ticked, spec reference correct, DB-backed and idempotent). The two pass-1 defects I routed back are confirmed fixed at the DB root: **(B)** the snapshot BoardDoc no longer narrates the now-Done card as "remains in progress" (the "nearly complete" contradiction also closed; `connection.py` + connection-aware-planning removed from the still-planned/follow-up lists; snapshot internally consistent; `{{card_ref:19}}` still resolves), and **(C)** the dangling `#djangotypedefinition` in-page anchor is now a plain code span matching the CHANGELOG form. **(A1)** the `051`→`052` misnumber and **(A2)** the DONE-032 `order=65` shipped-history sentence remain correctly deferred as maintainer follow-ups (a spec-only partial fix of A1 would create a worse spec-vs-source divergence; rewriting shipped-card history on a guess is unwarranted). DoD item 12 holds. Idempotence + `import_spec_terms --check` + `manage.py check` + `check_spec_glossary` (exit 0) all green. This is the last in-spec slice; the cross-slice integration pass and the final test-run gate still follow before maintainer handoff.

### Spec changes made (Worker 1 only)

- `docs/spec-033-connection_optimizer-0_0_9.md` line 5 (status line) — Slice 7, this closing final-verification pass. Reason: per the per-spawn status-line re-verification rule, "Slice 7 … in a final-verification revision loop" was stale once B+C were confirmed-fixed and the slice accepted. Updated to: "all seven slices … accepted; the cross-slice integration pass and the final test-run gate still follow before maintainer handoff. The `WIP-ALPHA-033-0.0.9` card has moved to `DONE-033-0.0.9` and the on-disk version remains `0.0.8` (the bump to `0.0.9` is the joint cut's, per Decision 12)." No glossary-anchor change; `check_spec_glossary` → `OK: 38 terms`, exit 0.
- **No `051`→`052` misnumber edit made** (A1, plan sub-step A5): deferred as a **maintainer follow-up** rather than applied spec-side — a spec-only fix would diverge from the un-editable `schema.py` docstring (~line 199) + `TODAY.md` that carry the same label, which is strictly worse than the uniform-but-wrong state (violates the root-cause-over-surface-patch rule). The complete fix is a uniform sweep across the spec + `schema.py` docstring + `TODAY.md` in one pass (cleanly folded into the next spec author's `docs/SPECS/NEXT.md` Step-8 cross-reference sweep, or a standalone maintainer fixup). Recorded so the non-edit is an explicit, reasoned decision, not an oversight.
- **No DONE-032 `order=65` edit made** (A2, plan sub-step A5): deferred — softening a *shipped* card's historical body (the record of what 032 deferred) on a guess about the spec author's intent, given the spec's literal A5 target ("the `TODO-BETA-051-0.1.5` card body's products-conversion sentence") maps to no card-51/52 body, is unwarranted. The *live* equivalent (card 033's own `Other` order=10 sentence) was already reconciled by Worker 2 under discretion D3. Recorded as out-of-scope shipped-card history; foldable into the A1 maintainer follow-up if board-currency softening of the stale `WIP-ALPHA-033` reference is wanted.
