# Package build plan: filters / 0.0.8 (027)

Spec source: `docs/SPECS/spec-027-filters-0_0_8.md` (already archived; the active-spec path `docs/spec-027-filters-0_0_8.md` does not exist)
Target release: `0.0.8` (shipped; card `DONE-027-0.0.8`, `KANBAN.md` Done column)
Build rule: one slice at a time. Plan first, build second, review third, reconcile fourth.
DRY rule: every slice must justify shared/duplicated patterns before merging.
Ownership partition: none; sequential slices. Slices 1 and 3 are Worker 1's alone; Slice 2 is the only slice with a Worker 2 / Worker 3 cycle.
Hot-path declaration: none. No production behavior changes. Slice 2 edits comment and docstring text only; nothing runs per request, per resolver, per row, per connection, or per outbound message.
Floor-verification scope: none. No slice touches a Django / Strawberry / channels integration seam — no slice changes an executable statement.
Pre-flight: passed on 2026-08-19 with two recorded deviations (below); baseline: DIRTY with three concurrent sessions' work.

## Cycle shape — residual reconciliation of a SHIPPED spec, not a fresh build

`DONE-027-0.0.8` shipped the whole filtering subsystem. Worker 0 verified every contract-bearing claim the spec makes against HEAD before writing this plan (evidence in `## Pre-dispatch verification`): **nothing was skipped in the code.** Every named module, primitive, classmethod, helper, `Meta` key, finalizer subpass, fakeshop declaration, and all 14 contracted live HTTP tests are present.

Two things are owed:

1. The spec's `-rationale.md` sibling was never created. `docs/SPECS/appx/` carries a `-rationale.md` for every spec from `001` through `026`; `027` is the first gap, and this spec is the single largest deliberative-layer carrier in the archive — an eight-revision (`rev1`–`rev8`) inline review history plus a `Justification:` / `Alternatives considered (and rejected):` pair under every one of the twelve Decisions.
2. The spec still narrates its own history. Per `docs/builder/BUILD.md` `## Spec rationale extraction`, "the spec never narrates its own history" — but `027` cannot be read as a current contract at all: its Decisions are stated as a chronology of corrections ("per H1 of the adversarial review", "rev4's plan said X, corrected to Y"), so a reader must apply eight revisions in order to learn what is true.

On top of that, sixteen months of later cards moved surfaces the spec pins by name, and the code carries the pre-renumber card id in 27 places.

Per the maintainer's dispatch instruction, Workers 2 and 3 are dispatched only where a slice needs a **code** change. Exactly one slice does (Slice 2).

## Pre-flight outcome and deviations

- Step 1 (baseline): DIRTY. Three other sessions are or were mid-cycle on `spec-024`, `spec-025`, and `spec-026`. Baseline-dirty out-of-scope files are listed below.
- Step 2 (`scripts/review_inspect.py`): smoke run on `django_strawberry_framework/filters/factories.py --output-dir docs/shadow --stdout` exited 0.
- Step 3 (artifact reset): **DEVIATION — old `bld-*.md` / `build-*.md` were NOT deleted.** `bld-003-final.md`, `bld-final-024.md`, `bld-final-025.md`, `bld-final-026.md`, `bld-integration-026.md`, `bld-slice-{1,2,3}-026-*.md`, `build-024-*.md`, `build-025-*.md`, and `build-026-*.md` are concurrent sessions' live record (several show as `D` in `git status`, i.e. a session is mid-edit). Deleting them is the one irreversible pre-flight mistake and `AGENTS.md` rule 34 forbids reverting concurrent work. This cycle therefore uses `-027`-suffixed artifact paths; all six were verified absent.
- Step 4 (`.gitignore`): `docs/shadow/` (line 174), `docs/builder/worker-memory/` (line 188), `docs/builder/temp-tests/` (line 192) all listed.
- Step 5 (scratch cleared): **DEVIATION — scratch was NOT cleared**, same reason as step 3. `worker-memory/worker-{0,1,2,3}-025.md` and `-026.md` are two cycles' notebooks. This cycle's workers use `docs/builder/worker-memory/worker-<N>-027.md`, which Worker 0 created empty.
- Step 6 (`check_spec_glossary`): `OK: 48 terms - all have glossary entries and at least one spec link.` (exit 0). The terms CSV carries 48 rows, no duplicate anchors, and includes `filter_input_type`.
- Step 7 (rationale extraction): NOT yet done — it is Slice 1 of this cycle.

### Baseline-dirty out-of-scope files (never edit, never revert)

`docs/SPECS/appx/spec-024-django_trac_37064_hardening-0_0_7-rationale.md`, `docs/SPECS/spec-024-django_trac_37064_hardening-0_0_7.md`, `docs/SPECS/spec-026-scalar_conversion_fakeshop-0_0_7.md`, `docs/SPECS/appx/spec-026-scalar_conversion_fakeshop-0_0_7-rationale.md`, `docs/builder/build-025-scalar_map_helper-0_0_7.md`, `docs/builder/build-026-scalar_conversion_fakeshop-0_0_7.md`, `docs/builder/bld-final-025.md`, `docs/builder/bld-slice-{1,2,3}-025-*.md`, `docs/builder/bld-final-026.md`, `docs/builder/bld-integration-026.md`, `docs/builder/bld-slice-{1,2,3}-026-*.md`, `examples/fakeshop/apps/scalars/models.py`, `examples/fakeshop/test_query/test_scalars_api.py`, and every file under `docs/builder/DONE/`.

**Concurrent-writable tracked binary / generated files:** `examples/fakeshop/db.sqlite3`, `KANBAN.md`, `KANBAN.html`, `docs/GLOSSARY.md`. This cycle plans NO edit to any of them, so any churn observed in them is another session's and must be left alone.

### Scope fence set by the maintainer

This cycle touches **spec files and `.py` files only**. No closeout-agentflow edits: no `docs/builder/BUILD.md`, `ARTIFACT.md`, or `worker-*.md` role-file changes; no KANBAN / GLOSSARY / CHANGELOG / TREE / README movement; no `-terms.csv` edit (its 48 anchors resolve, `check_spec_glossary` is green, and it has no duplicate anchor); no closeout retrospective; no DB work (the card is already `DONE-027-0.0.8` with its `SpecDoc` path already pointing at `docs/SPECS/spec-027-filters-0_0_8.md`).

**"Archive the spec"** in this cycle means: the spec stays at `docs/SPECS/spec-027-filters-0_0_8.md` (where it already is) and the new rationale companion lands at `docs/SPECS/appx/spec-027-filters-0_0_8-rationale.md`, matching every other archived spec's companion layout. No file moves.

## Pre-dispatch verification (Worker 0, against HEAD)

Findings below are handed to Worker 1 as **verified inputs, not hypotheses**. Every one was read against source before this plan was written.

### Delivered in full — no code gap

| Spec claim | Verified at | Result |
| --- | --- | --- |
| `filters/` subpackage, five files | `django_strawberry_framework/filters/` | `__init__.py`, `base.py`, `sets.py`, `factories.py`, `inputs.py` all present (5,717 source lines total) |
| Decision-4 parity-floor primitives | `django_strawberry_framework/filters/base.py` | `TypedFilter`, `ArrayFilter` + `ArrayFilterMethod`, `RangeFilter` + `RangeField` + `validate_range`, `ListFilter` + `ListFilterMethod`, `GlobalIDFilter`, `GlobalIDMultipleChoiceFilter`, `RelatedFilter` all present; `Filter` and `LazyRelatedClassMixin` present as re-exports (see D3, D4) |
| `FilterSetMetaclass` + `FilterSet` | `django_strawberry_framework/filters/sets.py::FilterSetMetaclass` / `::FilterSet` | both present; `FilterSetMetaclass` subclasses `django_filters.filterset.FilterSetMetaclass` |
| `apply_sync` / `apply_async` / `apply` trio | `filters/sets.py::FilterSet.apply_sync` / `::FilterSet.apply_async` / `::FilterSet.apply` | all three present, `apply` is the thin dispatcher (see D5 for the mechanism change) |
| five named internal helpers | `filters/sets.py` | `_normalize_input`, `_derive_related_visibility_querysets_sync` / `_async`, `_run_permission_checks`, `_validate_form_or_raise`, `_apply_related_constraints` all present |
| `FILTER_DEFAULTS` class attr + `filter_for_field` / `filter_for_lookup` overrides | `filters/sets.py::FilterSet.filter_for_field` / `::FilterSet.filter_for_lookup` | present on `FilterSet`, not in `factories.py`, exactly as Decision 4 H1 pins |
| `filter_queryset` tree-form override (Slice 4a) | `filters/sets.py::FilterSet.filter_queryset` | present; no `xfail` marker survives anywhere in `test_library_api.py` |
| `LOOKUP_PREFIXES` (`^` / `=` / `@` / `$`) | `filters/inputs.py` #"LOOKUP_PREFIXES" | present, all four prefixes, values `istartswith` / `iexact` / `search` / `iregex` |
| `LOOKUP_NAME_MAP` | `filters/inputs.py` #"LOOKUP_NAME_MAP" | present; all 26 rows match the spec's Decision-3 Layer-5 table entry-for-entry, `in -> ("in_", "in")` included |
| `construct_search` | `filters/inputs.py::construct_search` | present |
| `convert_filter_to_input_annotation` / `normalize_input_value` | `filters/inputs.py::convert_filter_to_input_annotation` / `::normalize_input_value` | both present |
| `materialize_input_class(name, cls)` / `clear_filter_input_namespace()` | `filters/inputs.py::materialize_input_class` / `::clear_filter_input_namespace` | both present, two-argument signature as Decision 3 L2 pins |
| `FieldSpec` + `_field_specs` + `django_source_path` | `filters/inputs.py` #"FieldSpec = GeneratedInputFieldSpec" | present as a domain-local alias (see D4); `_field_specs` keyed `(FilterSet, python_attr)`; `django_source_path` slot present |
| `FilterArgumentsFactory`, `get_filterset_class`, `_dynamic_filterset_cache` | `filters/factories.py` | all three present |
| `_helper_referenced_filtersets` + `filter_input_type` | `filters/__init__.py` #"_helper_referenced_filtersets" / `::filter_input_type` | both present; helper raises `TypeError` eagerly for a non-`FilterSet` |
| `DjangoTypeDefinition.filterset_class` slot | `types/definition.py::DjangoTypeDefinition` #"filterset_class: type \| None = None" | present |
| `DjangoTypeDefinition.related_target_for` | `types/definition.py::DjangoTypeDefinition.related_target_for` | present |
| `Meta.filterset_class` promotion | `types/base.py` #"DEFERRED_META_KEYS" / #"ALLOWED_META_KEYS" | `DEFERRED_META_KEYS` is now `{"aggregate_class", "fields_class", "search_fields"}`; `"filterset_class"` is in `ALLOWED_META_KEYS` |
| phase-2.5 four ordered subpasses | `types/finalizer.py::_bind_filtersets` | present; runs bind-owners -> expand -> materialize -> orphan-validate, plus two filter-only audits added later (see D9) |
| exact-`graphql_type_name` multi-owner reject (rev8 H2) | `types/finalizer.py` #"EXACT same ``graphql_type_name``" | present; rejects on either Relay-shape or `graphql_type_name` divergence |
| orphan `filter_input_type` validation | `types/finalizer.py::_format_orphan_sets_error` | present |
| `FILTER_INVALID` error shape | `filters/sets.py` #"\"code\": \"FILTER_INVALID\"" | present |
| fakeshop `filters.py` five filtersets | `examples/fakeshop/apps/library/filters.py` | `BranchFilter`, `ShelfFilter`, `BookFilter`, `LoanFilter`, `PatronFilter` all present, with same-module `RelatedFilter("...")` references |
| fakeshop `filters_genre.py` cross-module fixture | `examples/fakeshop/apps/library/filters_genre.py::GenreFilter` | present; `BookFilter.genres = RelatedFilter("apps.library.filters_genre.GenreFilter")` present |
| `Meta.filterset_class` on six library types | `examples/fakeshop/apps/library/schema.py` | six wirings present (`LoanFilter`, `BookFilter`, `ShelfFilter`, `GenreFilter`, `BranchFilter`, `PatronFilter`) |
| `filter_input_type(...)` on library root resolvers | `examples/fakeshop/apps/library/schema.py` | six resolver annotations present |
| `PatronFilter.email_must_have_at_sign` (rev8 H4) | `examples/fakeshop/apps/library/filters.py` #"an ``@`` sign" | present |
| **all 14 contracted live HTTP tests** | `examples/fakeshop/test_query/test_library_api.py` | all 14 present by the exact names the Test plan gives — `test_library_branches_filter_by_name_icontains`, `..._by_choice_enum`, `..._by_non_relay_fk_scalar_id`, `..._by_relay_m2m_global_id`, `..._by_reverse_fk_lookup`, `..._combines_and_or_not`, `..._preserves_optimizer_cooperation`, `..._respects_related_queryset_boundary_on_parent`, `test_book_genres_uses_absolute_import_path_related_filter`, `test_nested_related_filter_honors_target_get_queryset`, `test_apply_raises_graphqlerror_on_invalid_filter_input`, `test_apply_passes_graphql_enum_coercion_before_form_validation`, `test_root_get_queryset_runs_before_filter_apply`, `test_relay_global_id_filter_rejects_wrong_type_name` |
| `tests/filters/` mirror tree | `tests/filters/` | `__init__.py`, `test_base.py`, `test_sets.py`, `test_factories.py`, `test_inputs.py`, `test_finalizer.py`, plus a `fixtures/` package |
| `tests/types/test_definition_relations.py` | `tests/types/test_definition_relations.py` | present, 21 tests; `test_related_target_for_resolves_fk_m2m_and_reverse` present |
| the named finalizer / input tests | `tests/` | `test_phase_2_5_binds_all_owners_before_expansion`, `test_orphan_filter_input_type_reference_raises_at_finalize`, `test_field_spec_maps_galaxy_name_flat_field_to_django_source_path`, `test_filter_input_type_returns_forwardref_in_annotation_args`, `test_filter_input_type_is_idempotent_under_repeated_calls`, `test_meta_filterset_class_is_promoted_to_allowed_meta_keys`, `test_filterset_class_resolves_across_module_boundary` all present |
| Slice 6 closure ("carried by sibling") | `tests/orders/test_composition.py` #"filterset_class = BookFilter" | the composition test landed in the ordering card's tree exactly as the Slice-checklist conditional clause authorizes; `tests/filters/test_composition.py` correctly does not exist |
| four glossary entries shipped | `docs/GLOSSARY.md` | `## \`FilterSet\``, `## \`filter_input_type\``, `## \`Meta.filterset_class\``, `## \`RelatedFilter\`` all present |

### Drifted or defective spec claims Worker 1 must reconcile (verified, with the cause)

Each is a claim the spec makes that is false, unauditable, or narrated as history rather than contract. **None is a code defect except D12–D14, which Slice 2 owns.**

- **D1 — the spec narrates its own history end to end, and has no rationale companion.** The `Revision history (kept inline so the spec is self-contained)` block runs `rev1`–`rev8` and states outright that it is kept inline. Every one of the twelve Decisions closes with `Justification:` + `Alternatives considered (and rejected):`. Beyond those blocks, review-round provenance is welded into the *contract* prose itself — Decision 3's Layer-5 body, Decision 4's converter table, Decision 8's step list, Decision 9's lifecycle clause, the Edge-cases bullets, the Test-plan bullets, and the DoD items each explain themselves by naming the review item that produced them ("per H1 of the adversarial review", "rev4's plan said `loan_set` which would only be correct if...", "corrected per M1 of rev3's feedback"). `BUILD.md` `## Spec rationale extraction` forbids exactly this shape: "a reader must never reconstruct what is currently true by applying a chronology to it." Slice 1 moves the deliberative layer; Slice 3 rewrites the surviving contract prose so it states the contract directly.
- **D2 — the `Status:` line is a build-progress log, not a state.** Line 4 is a single ~4,000-character paragraph tracking Slices 1-3 / 4 / 4a / 5 / 6 as they landed, quoting its own superseded rev8 phrasing ("Original L1-of-rev8 phrasing preserved for historical context: ..."), and naming a deleted per-cycle artifact (`docs/builder/bld-slice-6-composition_smoke_test.md`, which is not on disk — per-cycle `bld-*.md` artifacts are cleared at the next build's pre-flight). It also still reads "in progress".
- **D3 — `base.py` does not port `Filter`; it re-exports `django_filters.Filter`.** Decision 2 and DoD item 3 list `Filter` among the primitives `base.py` "ships" as ports of `graphene_django/filter/filters/*`. `filters/__init__.py` #"``Filter`` re-exported here IS ``django_filters.Filter`` itself" states the truth: a plain re-export that deliberately shadows the upstream name, no subclass. The contract is real and deliberate; the spec's description of it is wrong.
- **D4 — three Decision-named symbols now live in the shared substrate and survive in `filters/inputs.py` only as aliases.** `FieldSpec` is `utils/inputs.py::GeneratedInputFieldSpec`; `build_input_class` is `utils/inputs.py::build_strawberry_input_class`; `_input_type_name_for` is `utils/inputs.py::set_input_type_name`. `LazyRelatedClassMixin` moved to `sets_mixins.py::LazyRelatedClassMixin` and `RelatedFilter`'s owner-bind / lazy-target machinery is now `sets_mixins.py::RelatedSetTargetMixin`. Cause: the sibling `OrderSet` card and the later DRY-squeeze work single-sited the mechanics so `filters` and `orders` share one implementation. **The spec's named surface still resolves** (every name is preserved as a domain-local alias, deliberately — `filters/inputs.py` #"Tests and" says so), so this is a where-the-mechanics-live change, not a surface break. The spec should state where the mechanics live.
- **D5 — the `apply` dispatcher's misuse-detection mechanism was replaced, and Decision 8 still pins the retired one.** Decision 8's sync/async subsection (and rev6 M5) pins a module-level **sentinel string constant** matched via `exc.args[0]`, raised by `types/relay.py::_apply_get_queryset_sync`, and a rethrow message that interpolates the target type name. At HEAD: `_apply_get_queryset_sync` / `_apply_get_queryset_async` **do not exist anywhere in the tree**; the visibility dispatch is `utils/querysets.py::apply_type_visibility_sync` / `::apply_type_visibility_async`; the misuse signal is a typed exception `utils/querysets.py::SyncMisuseError`; and `filters/sets.py::FilterSet.apply` catches that class, with its own docstring recording that this deliberately replaced substring-matching. The rethrow message no longer interpolates the target type. Cause: the visibility-boundary hardening (`spec-045`) rebuilt the hook dispatch. Decision 8 cites the retired symbols ~6 times.
- **D6 — Decision 9's `registry.clear()` integration is the retired shape.** Decision 9 (with rev3 M5 and rev8 M4) pins a cycle-safe **local import inside `TypeRegistry.clear`**, guarded by `try / except ImportError`, plus a subprocess test proving `registry` imports alone. At HEAD the subsystem registers a callback at import time — `filters/__init__.py` #"register_subsystem_clear(_clear_helper_referenced_filtersets" — and `registry.py::register_subsystem_clear` replays it from `TypeRegistry.clear`. `filters/__init__.py`'s own comment records that "that shape predates the registration seam". Same for `clear_filter_input_namespace`.
- **D7 — Decision 8 claims a live async test that does not exist.** Decision 8's consumer-pattern paragraph says "a new live HTTP test in [Test plan] exercises the async path against a fixture type whose `get_queryset` is async-only". The Test plan's fourteen tests contain no such test, and `grep -rn apply_async examples/fakeshop/` returns **zero** hits — the fakeshop library types are all sync. `apply_async` is covered in `tests/filters/test_sets.py` instead. The decision's claim was never backed by its own test plan.
- **D8 — three Test-plan test names do not exist; the contracts they name are covered under other names.** `test_apply_propagates_related_constraints_into_filterset_qs` (rev8 H3), `test_check_permissions_only_fires_for_active_filter_branches` (rev5 M2), and `test_related_target_for_resolves_default_reverse_name` (rev8 M2) return zero hits across the tree. The contracts are covered: `tests/filters/test_sets.py::test_apply_related_constraints_runs_active_branch_only` and siblings; `tests/filters/test_sets.py::test_run_permission_checks_fires_only_for_active_input_fields` plus seven sibling `_run_permission_checks` rows and `::test_check_permissions_falls_back_to_active_input_when_no_requested_fields`; and `tests/types/test_definition_relations.py`'s twenty-one rows. Worker 1 re-derives the mapping and names the tests that exist.
- **D9 — phase 2.5 grew two filter-only audits the spec never names.** Beyond Decision 6's four subpasses, `types/finalizer.py::_audit_filterset_subpass_2_5` runs a registered-`RelatedFilter`-target audit and an encode-only-GlobalID-target rejection. Both are filter-subsystem contracts a reader auditing the spec would not find.
- **D10 — `HIDE_FLAT_FILTERS` changes the generated input shape and the spec does not mention it.** `conf.py::hide_flat_filters_setting` (key `HIDE_FLAT_FILTERS`, default `False`) is read by `filters/inputs.py` #"hide_flat_filters = bool(hide_flat_filters_setting())" and suppresses the expanded flat children of a grouped entry. It is live in the fakeshop live suite (`test_query/test_library_api.py::test_hide_flat_filters_changes_library_filter_input_shape_over_http`). The default preserves the spec's pinned shape, so this is an added opt-in rather than a contract break — but the spec's flat-field contract now has a setting attached to it.
- **D11 — stale, superseded, or self-contradicting claims scattered through the tail sections.**
  - `_get_fields`: Decision 2, the Edge-cases "Recursion-protected `_get_fields`" bullet, and DoD item 4 name `FilterSet._get_fields`. The real override is `filters/sets.py::FilterSet.get_fields` (the public `django-filter` classmethod). No `_get_fields` exists.
  - Risks: "`docs/spec-027-filters-0_0_8-terms.csv` currently lists **32** terms and does NOT include `filter_input_type`". The CSV carries **48** rows and **does** include `filter_input_type`; the glossary entry `## \`filter_input_type\`` exists.
  - DoD items 1 and 17 and the Risks/Decision-1 prose use the pre-archive path `docs/spec-027-filters-0_0_8.md`; the file is at `docs/SPECS/spec-027-filters-0_0_8.md`. DoD 17's quoted command names the old path.
  - DoD item 22 reads "records the card as `DONE-NNN-0.0.8`" — an unresolved placeholder; the card is `DONE-027-0.0.8`.
  - Slice 5, DoD 22, and the Doc-updates block name the card's pre-move id `WIP-ALPHA-021-0.0.8`. `021` is now `spec-021-apps-0_0_7` — the same document uses `[spec-021]` for the AppConfig spec in Decision 1 while `WIP-ALPHA-021` means *this* card. Two meanings for one number inside one document.
  - Decision 10 / DoD 24 leave the joint-`0.0.8`-cut contingency open ("UNLESS this card is the last `0.0.8` card to ship"). `0.0.8` shipped; the question is settled and the spec should state which way.
  - The Slice-4a checklist bullet cites raw spec line numbers — "spec L446, L743 step 8, L1145 item 4(d)". At HEAD L446 lands inside Decision 1, L743 inside Decision 8, and L1145 inside `## Out of scope`, not the DoD. Already rotted, and Slices 1 and 3 will move every line.
  - `[fakeshop-test-library-reload]` resolves to `examples/fakeshop/test_query/test_library_api.py`, and the Test-plan footnote cites `test_library_api.py::_reload_project_schema_for_acceptance_tests`. The fixture is defined at `examples/fakeshop/test_query/conftest.py::_reload_project_schema_for_acceptance_tests`; `test_library_api.py` only mentions it in comments.
  - The Slice-4 checklist bullet still carries the `@pytest.mark.xfail(strict=True, ...)` carve for `test_library_books_filter_combines_and_or_not`. No `xfail` marker exists anywhere in `test_library_api.py`; Slice 4a retired it as the spec itself planned.

### Code work verified as needed (Slice 2 only)

Three defect classes across `.py` files. All are comment / docstring text; none changes an executable statement.

- **D12 — 27 occurrences of the pre-renumber card id `spec-021` / `Spec-021` across 9 `.py` files, every one meaning *this* card.** Measured as occurrences, not matching lines (`grep -roh --include='*.py' -e 'spec-021' -e 'Spec-021' . | wc -l` -> 27). Per file: `examples/fakeshop/test_query/test_library_api.py` 12, `tests/filters/test_finalizer.py` 5, `examples/fakeshop/apps/library/filters.py` 4, and one each in `tests/filters/test_inputs.py`, `tests/types/test_definition_order.py`, `tests/types/test_base.py`, `tests/types/fixtures/shelf_module.py`, `tests/types/fixtures/branch_module.py`, `examples/fakeshop/apps/library/filters_genre.py`. Cause: the 2026-07-30 card renumber moved the filtering card from `021` to `027`; the package source was swept (`filters/*.py` carry 43 correct `spec-027` refs) but the test tree and the fakeshop example app were not. Each is now an **actively wrong** citation, because `spec-021` today names `docs/SPECS/spec-021-apps-0_0_7.md` (the Django `AppConfig` card). `scripts/check_citations.py` cannot see this class — it resolves `path::Symbol` refs only — so the rot is invisible to the pre-commit and CI gates.
- **D13 — 11 occurrences of raw spec line numbers in `.py` comments and docstrings.** `django_strawberry_framework/filters/inputs.py` 2 (#"per spec-027 line 595", #"spec-027 L603"), `filters/sets.py` 5 (#"spec-027 L566-567 + L607", #"per spec-027 L566-567", #"L566-567 + L607", #"spec-027 L518-605", #"spec-027 L668-678"), `filters/factories.py` 1 (#"spec-027 lines 579-584"), `tests/filters/test_finalizer.py` 3 (#"spec-021 line 822", #"spec-021 line 1030", #"spec-021 lines 416 + 1030"). `AGENTS.md` rule 27 permits raw `path:NN` only in per-cycle scratchpad artifacts, "never in code comments specs or standing docs". The three in `test_finalizer.py` are also counted in D12. Slices 1 and 3 renumber the whole spec, so every one of the 11 becomes doubly wrong if left.
- **D14 — 3 occurrences of build-process provenance in comments, plus one mangled docstring.** `filters/factories.py` #"(Decision 4 H1 / spec-027 lines 579-584)", `filters/base.py` #"them (round-6 Finding 1)", `filters/sets.py` #"round-3 loop: no substring-matching against a constant string". The standing rule is that a comment states the invariant, never how the change came to be; a review-round id is unresolvable to any future reader. Separately, `tests/types/test_definition_order.py::test_filterset_class_resolves_across_module_boundary` carries a broken sentence — "Pins spec-021" followed by a new line beginning "The contract that the finalizer's filter-binding pass works" — the residue of a half-applied edit.

Worker 1 owns whether each of D1-D11 is a spec edit, a rationale entry, or both. The one rule Worker 0 fixes: **no explanation of any change may appear in the spec.** Corrected text states the contract as though it had always been right; the what / why / when goes in `docs/SPECS/appx/spec-027-filters-0_0_8-rationale.md`.

## Artifact list

- `docs/builder/bld-slice-1-027-rationale_extraction.md`
- `docs/builder/bld-slice-2-027-citation_and_provenance_rot.md`
- `docs/builder/bld-slice-3-027-spec_reconciliation.md`
- `docs/builder/bld-integration-027.md`
- `docs/builder/bld-final-027.md`
- `docs/builder/bld-slice-5-027-retired_mechanism_docstrings.md`

## Checklist

- [x] Slice 1: Rationale extraction — create `docs/SPECS/appx/spec-027-filters-0_0_8-rationale.md` and MOVE the spec's deliberative layer into it (the whole `rev1`-`rev8` revision history, every `Justification:` / `Alternatives considered (and rejected):` block, and every inline review-round narration), keyed to the spec decision each entry belongs to -> `docs/builder/bld-slice-1-027-rationale_extraction.md`
- [x] Slice 2: Code correction — retire D12's 27 stale `spec-021` citations, D13's 11 raw spec-line-number refs, and D14's 3 build-provenance comments plus the mangled `test_definition_order.py` docstring; `.py` files only -> `docs/builder/bld-slice-2-027-citation_and_provenance_rot.md`
- [x] Slice 3: Spec reconciliation — rewrite the spec so it reads as the current contract, resolving D1-D11, every change recorded in the rationale -> `docs/builder/bld-slice-3-027-spec_reconciliation.md`
- [x] Cross-slice integration pass -> `docs/builder/bld-integration-027.md`
- [x] Integration consolidation loop (Worker 2 + Worker 3) — repair the three `#"unique substring"` spec citations in `.py` that Slice 1's move broke -> `docs/builder/bld-slice-4-027-broken_substring_citations.md`
- [x] Final gate -> `docs/builder/bld-final-027.md`
- [x] Post-final residual cohort (Worker 2 + Worker 3) — correct the two `.py` test docstrings that describe the retired local-import / `except ImportError` clear mechanism on spec-027's own surface -> `docs/builder/bld-slice-5-027-retired_mechanism_docstrings.md`

### Integration-pass consolidation cohort (added 2026-08-19)

The integration pass surfaced one Medium it could not fix itself (it may not write `.py`). Worker 0 verified all three against source before dispatch: each cited substring has **0** occurrences in the spec at HEAD, and each replacement target exists.

**Ownership partition (single cohort, declared):** `django_strawberry_framework/filters/base.py`, `django_strawberry_framework/types/finalizer.py`, `django_strawberry_framework/filters/sets.py`. No other cohort runs concurrently.

*Partition correction 3, 2026-08-19:* extended to `django_strawberry_framework/filters/factories.py`. Worker 3's pass-3 review surfaced the **same never-existent spec surface** that pass 4 repaired in `filters/inputs.py`, live at `filters/factories.py::FilterArgumentsFactory` #"per Implementation discretion item 5". Worker 0 verified before extending: `grep -rn --include='*.py' -i "implementation discretion" .` returns **exactly one** site repo-wide, and `grep -ci discretion` over the spec returns **0** — `### Implementation discretion items` is an `ARTIFACT.md` template section the spec has never had. The site names no card at all, so it is invisible to both a `spec-027` sweep and a `Decision N` sweep. Bounded to one line; this closes the class.

*Partition correction 2, 2026-08-19:* extended again to `django_strawberry_framework/filters/inputs.py`. Worker 2's pass-2 report surfaced a sibling of M1 at `filters/inputs.py` #"spec-027 Decision 3 Layer 5" above `LOOKUP_PREFIXES`, outside its writable set. Worker 0 verified it before extending: Decision 3 spans spec lines 404-481 and contains **0** occurrences of `construct_search` / `LOOKUP_PREFIXES`; Decision 2 spans 379-403 and contains **2**. Same defect, same repair.

*Partition correction 1, 2026-08-19:* the cohort was first declared over the two files above. The `TODO(` disposition item below was dispatched as a decision rather than a repair order, and the cohort chose the retarget option, which writes `filters/sets.py`. Worker 2 declared the extension in its artifact rather than writing silently; Worker 0 records it here, because an ownership partition that the plan does not carry is not a partition (`docs/builder/BUILD.md` `### Parallel cohorts under a declared ownership partition`).

| Site | Broken citation | Cause | Replacement target (verified present) |
| --- | --- | --- | --- |
| `filters/base.py` #"spec-027 #\"accept both raw\"" (two sites) | target substring `accept both raw` -> 0 hits | the target was a rev-8 revision-history bullet Slice 1 moved to the rationale | the contract survives in Decision 4 as `Accepts both raw` (1 hit) |
| `types/finalizer.py::_bind_filterset_owner` #"Decision 6 #\"Partial-finalize lifecycle\"" | target substring `Partial-finalize lifecycle` -> 0 hits | the target was Decision 6's `Justification:` bullet Slice 1 moved | the contract survives as Decision 9's `Partial-finalize recovery` (2 hits, so cite `Decision 9` without a substring) |

Also routed to this cohort for a disposition decision (not a repair order): `filters/sets.py` #"TODO(spec-027-filters-0_0_8 Meta.search_fields)" — a staged anchor naming **this** spec for work owned by a future card (`Meta.search_fields`, `0.1.2`). `docs/builder/BUILD.md` integration-pass precondition 6 requires every anchor naming this build's spec to be discharged by the build's end; the work has not shipped, so the anchor legitimately stays, but two slices recorded the sweep as returning nothing when it returns this hit.

### Post-final residual cohort — retired-mechanism test docstrings (added 2026-08-20)

Surfaced by a maintainer-requested exhaustiveness re-sweep after `bld-final-027.md` reached `final-accepted`. The class is **new**: Slice 2 swept citation rot (wrong card id, raw line number) and build provenance, and the integration consolidation swept broken `#"substring"` targets. Neither swept **`.py` prose that describes a mechanism the package no longer has** — the text names no spec, no card, and no line number, so every instrument this cycle ran is blind to it.

**Ownership partition (single cohort, declared):** `tests/test_registry.py` and `tests/filters/test_inputs.py`. No other cohort runs concurrently. `tests/filters/test_inputs.py` already carries this cycle's Slice 2 edits, so `HEAD` is not the pre-pass baseline for it; `tests/test_registry.py` is clean at `HEAD` and is newly opened by this cohort.

**Mechanism truth.** Two sites were dispatched. **Worker 2 refuted the second half of Worker 0's measurement and it is corrected here rather than left standing.** Worker 0 re-verified the refutation against `HEAD` before accepting it.

*Site 1 holds as dispatched.* `grep -c 'except ImportError' django_strawberry_framework/registry.py` returns **0**. `TypeRegistry.clear` performs no import: it drains `_type_teardowns`, clears its own maps, then replays `iter_subsystem_clears()`, a tuple of callables each subsystem bound at its own import time through `register_subsystem_clear`. The mechanism that docstring names is genuinely **retired**.

*Site 2 was misgraded by Worker 0 and is NOT a member of the dispatched class.* `filters/inputs.py::clear_filter_input_namespace` delegates to `_clear_input_namespace`, which is `utils/inputs.py::clear_generated_input_namespace` — and that function calls `utils/inputs.py::_safe_import` **twice**, on `factory_module` and `set_module`, which for this family are exactly `django_strawberry_framework.filters.factories` and `django_strawberry_framework.filters.sets`: the two names the test poisons. `_safe_import` reaches `utils/imports.py::import_attr_if_importable`, whose body is `importlib.import_module` inside `try` / `except ImportError: return None`. The guard exists, one layer down, and the test's premise **does** fire. The mechanism was **relocated and single-sited into the shared substrate** — the same cause as finding D4 — not retired. Worker 0's error was inferring the whole chain from the one-line delegate body without following `_clear_input_namespace` into `utils/`.

Site 2's docstring was therefore substantively TRUE; only its inline comment's `from ... import ...` spelling was wrong, since the substrate imports by module path via `importlib`. Regraded **Low, stale spelling**, and repaired in the same pass.

**One spec edit IS owed, contradicting the `none owed` conclusion `bld-final-027.md` recorded.** Worker 2 surfaced it and Worker 0 verified it independently: `spec-027` `### Decision 9`'s lifecycle bullet states that `registry.clear()` "removes every materialized class from the module's global namespace" and that `clear_filter_input_namespace()` "walks `_materialized_names.items()` and `delattr(...)` for each". `grep -rn delattr` over `django_strawberry_framework/` returns **zero** occurrences in any clear path; `utils/inputs.py::clear_generated_input_namespace` states the opposite as a deliberate contract ("Materialized class objects are intentionally left parked"), and `tests/filters/test_finalizer.py #"parked module globals survive"` pins it. Slice 3's reconciliation did not reach this bullet. Routed to Worker 1.

`spec-027` `### Decision 9` is otherwise correct on the registration seam: it states that only an imported owner can register and that there is no `except ImportError: pass` branch to keep covered.

| Site | Claim in the prose | Measured at `HEAD` |
| --- | --- | --- |
| `tests/test_registry.py::test_clear_tolerates_unimportable_filter_submodules` (docstring) | "Both ``except ImportError`` guards in ``clear()``"; "The filter-namespace co-clear uses cycle-safe local imports"; "``clear()`` skips that block" | 0 guards in `registry.py`; `clear()` replays resolved callbacks; no block to skip |
| `tests/filters/test_inputs.py::test_clear_filter_input_namespace_tolerates_unimportable_submodules` (inline comment) | "makes ``from ... import ...`` raise ImportError, exercising both ``except ImportError`` guards" | REGRADED Low. The guards are real but live in `utils/imports.py::import_attr_if_importable`, reached through `utils/inputs.py::_safe_import`, and import by module path via `importlib` rather than through a `from ... import ...` on this call path. The docstring's "best-effort" claim is TRUE and was never the defect |

Both tests still pass, and each pins something real — but **not the same thing**, which is why their corrected docstrings must not be unified into one phrasing. Site 1 pins that `clear()`'s **own body** imports nothing, so the only route a poisoned `sys.modules` can reach is a replayed callback's best-effort lookup, which skips rather than propagates; reintroducing a call-time import into `clear()` itself breaks it. The unqualified form of this sentence — "`clear()` imports nothing" — is FALSE and was Worker 0's; a live probe of `importlib.import_module` across a real `registry.clear()` records **2** calls, `filters.factories` and `filters.sets`, arriving through the replayed `clear_filter_input_namespace` callback. Worker 3 graded the same phrasing Medium where it had propagated into the repaired docstring, and Worker 2 then found it still standing here. Site 2 pins that the substrate's two best-effort lookups tolerate an unimportable submodule; removing the guard from `import_attr_if_importable` breaks it. Both repairs are prose-only; no assertion, fixture, or `sys.modules` manipulation changes.

**Cross-cohort seam, recorded deliberately.** Three sibling tests in `tests/test_registry.py` carry the same false mechanism for other cards' subsystems: `test_clear_tolerates_unimportable_order_submodules` (spec-028), `..._connection_submodule` (spec-030), and `..._relay_module` (spec-032). All three describe `TypeRegistry.clear`, so all three are stale by site 1's proof, and the order twin cross-references the filter test by name. A fourth, `tests/orders/test_inputs.py::test_clear_order_input_namespace_tolerates_unimportable_submodules` (spec-028), is the twin of **site 2** rather than of site 1 on the retired-mechanism axis, but it carries a **union** of defects, not a single Low: site 2's stale `from ... import ...` spelling (docstring line 909 AND inline comments 918-919, so even the wording half is not a one-edit close), PLUS site 1's misattribution shape (it places "BOTH `except ImportError: pass` guards" *in* `clear_order_input_namespace`, where `grep -c 'except ImportError' django_strawberry_framework/orders/inputs.py` = **0**), PLUS three raw `path:NN` refs of which two are past `orders/inputs.py`'s **388**-line EOF, PLUS a rotted cross-file range (it cites the filter twin at `lines 1009-1028`; the function is at 1417). It also sits in a file-local cluster of **11** raw refs across **8** docstrings, two wrong ranges, three past EOF, which makes 051's scope the FILE, not the function. Worker 0's first count — "four twins, all stale by the same proof" — conflated two populations; the split is 3 + 1 **on the retired-mechanism axis only**. By total defect count the orders twin is the HEAVIEST of the four, not the lightest, and it cannot be repaired by copying either docstring this cohort landed. Worker 0 also named the third registry twin `..._node_field_ledger`, which is the docstring's subject, not the function: the function is `tests/test_registry.py::test_clear_tolerates_unimportable_relay_module`, cited by symbol deliberately: Worker 0 first wrote line 1724, which is wrong in BOTH trees (`git show HEAD:` puts it at 1721, the working tree at 1725, moved +4 by this cohort's own docstring edit). A line number that this very cycle invalidated is the fragility the entry is about. They are **out of this cycle's fence** (other cards' surfaces) and route to `TODO-ALPHA-051-0.0.15`, which already collects measured stale test docstrings. This cohort therefore leaves one of four twins reading differently from the other three on purpose; the routing note above is the record of why. `tests/test_registry.py::test_unregister_tolerates_unimportable_connection_submodule` is **not** in that population — `unregister` still routes through `registry.py::_clear_if_importable` and its poisoned-`sys.modules` premise still fires.

### Catalog-discharge cohorts (added 2026-08-20, post-commit `8a9840dc`)

The maintainer asked for every catalog item that this cycle's fence can reach to be discharged rather than homed. Four cohorts run under a declared file partition. Populations below were re-derived by Worker 0 against the working tree, not transcribed from `bld-final-027.md`.

**Blocked by concurrent work, NOT attempted (`AGENTS.md` rule 34).** The spec-028 session has taken these since `8a9840dc` and they are baseline-dirty: `types/base.py` (catalog item 3, and item 7's `types/base.py` half), `tests/test_registry.py` (items 18, 19), `tests/orders/test_inputs.py` (item 20), plus `orders/base.py`, `orders/inputs.py` (three of item 4's sites), `examples/fakeshop/apps/library/orders.py`, `examples/fakeshop/test_query/test_library_api.py`, `docs/SPECS/spec-028-orders-0_0_8.md`. These stay homed on `TODO-ALPHA-051-0.0.15`. Note that the 028 session editing `tests/test_registry.py` and `tests/orders/test_inputs.py` may be discharging items 18-20 itself.

**Out of this cycle's fence, unchanged:** catalog items 9 (`docs/TREE.md` render, `KANBAN.md` DB row) and 10 (`README.md`). Item 13 (PEP-563 test) is a coverage addition rather than rot repair and the spec licenses its absence, so it stays routed to `TODO-BETA-068-0.1.8`.

**Item 21 needs no discharge and no re-homing:** the deliberate non-repair is already stated in `docs/SPECS/appx/spec-027-filters-0_0_8-rationale.md`, which is permanent, so deleting `bld-final-027.md` cannot lose it.

| Cohort | Artifact | File partition (disjoint, non-overlapping) | Catalog items |
| --- | --- | --- | --- |
| A | `bld-slice-6-027-wrapped_citations.md` | `consumers.py`, `routers.py`, `filters/factories.py`, `types/finalizer.py`, `types/relay.py` | 1 Form A (all 6 sites, re-derived), 4's `routers.py:93` site |
| B | `bld-slice-7-027-raw_line_refs.md` | `orders/sets.py`, `mutations/fields.py`, `mutations/resolvers.py`, `mutations/inputs.py`, `mutations/sets.py`, `forms/inputs.py`, `examples/fakeshop/test_query/test_products_api.py` | 6's three uncarded halves (11 sites), 4's `orders/sets.py` sites, 7's mutations / forms half |
| C | `bld-slice-8-027-decision_attribution.md` | `rest_framework/{sets,resolvers,inputs,serializer_converter}.py`, `orders/{__init__,factories}.py`, `optimizer/extension.py`, `utils/inputs.py` | 4's `rest_framework/` + `orders/` sites, 5 (all), 7's `rest_framework/` half |
| D | `bld-slice-9-027-spec_055_refs.md` | `docs/SPECS/spec-055-search_fields-0_1_2.md` only (Worker 1 surface) | 11 |

Item 8 (history-narrating prose) has no settled population — three instruments gave 65/54/46 over the same file set. It is therefore scoped **per cohort to that cohort's own files**, unambiguous build-provenance only (review-round ids, slice/pass names, cycle names); contrast prose is not a member. No cohort may widen into a repo-wide prose sweep.

**Re-derived populations, for the record.** Item 1 Form A: **8 repo-wide, not 6** -- and both of Worker 0's measurements here were wrong in the same way the item is about. A per-line census over `django_strawberry_framework/**/*.py` returns 6, reproducing the catalog, but the catalog's scope was the PACKAGE; extending the same census to `tests/` and `examples/` adds `tests/test_relay_connection.py:2591` and `examples/fakeshop/apps/kanban/schema.py:359`. Separately, Worker 0 listed `routers.py:93` as a card-less `Decision 3 / Error shapes` pointer (catalog item 4's class). Cohort A refuted it: line 92 ENDS with `(spec-046`, so the card is named and the reference is merely WRAPPED. Worker 0's instrument was `grep -vE 'spec-[0-9]'`, which drops the line carrying the card and keeps the line carrying the Decision -- a line-scoped filter reproducing, on itself, the exact blindness this catalog item documents. Two independent Worker 0 errors, one cause. Item 6 uncarded: `orders/sets.py` **94, 179, 312** (cookbook ranges), `mutations/fields.py:32` and `mutations/resolvers.py:1150` (both `` (``relay.py`` line 287) ``), `test_products_api.py` **2948, 2984, 3015, 3051, 3098**. Item 5: `optimizer/extension.py` **10** bare `Decision N` occurrences (not the catalog's 4 -- 133, 216, 226, 265, 325, 671, 701, 991, 1081, 1443, 1499) and `utils/inputs.py` **3** (1300, 1708, 1733). Item 4 measured over its own files returns far more than the catalog's enumerated 14, concentrated in `rest_framework/sets.py` and `rest_framework/resolvers.py`; the enumeration was a sample, not a census, which is why cohort C is scoped by FILE and told to census.

*Cohort B amendments, recorded by Worker 0 after the pass.* **(1) Population 15, not the brief's 10.** Worker 0's instrument looked for `line NNN` adjacent to a *path*; five sites spell it adjacent to a *spec Decision* instead -- `orders/sets.py:256` (a fourth cookbook range) and `mutations/sets.py` **503, 508, 671, 1339** (`Decision 6 line 334`, `Edge cases line 509` twice, `Decision 6 line 336`). A `path:NN`-shaped sweep returns **0** in this partition, so the shape Worker 0 searched for was the one shape absent. **(2) Five of the fifteen numbers were already rotted, four onto a materially wrong target** -- both `relay.py line 287` twins land in `decode_model_global_id`'s docstring when they mean `relay.py::DjangoNodeField`; two `test_products_api.py` mirror refs name the wrong test outright; `spec-036 Edge cases line 509` is off by six. None was renumbered: each now cites a symbol or a measured-unique substring, because the line number is the defect. **(3) The cookbook refs are fully durable and the plan was wrong to doubt it:** `django_graphene_filters/` sits in `scripts/check_citations.py`'s `UPSTREAM_PREFIXES`, so `django_graphene_filters/orderset.py::AdvancedOrderSet.get_fields` resolves as a citation -- and two of the four sites already carried that citation on the preceding line, making the number purely redundant.

**(4) Ownership correction: item 4's `orders/sets.py` half was discharged by the concurrent spec-028 session, not by cohort B.** That file went baseline-dirty mid-pass. Cohort B did not revert and did not abandon: its four cookbook sites are distinct lines from the 028 session's three `Spec Decision N` sites, so single-hunk exact-string edits let both sessions' work coexist. Worker 0 verified the result rather than accepting it -- `orders/sets.py` now carries **0** cookbook line refs, **0** bare `Spec Decision N`, **7** resolvable `django_graphene_filters/orderset.py::` citations, and parses under `ast.parse`. The partition table above still lists cohort B as owning item 4's `orders/sets.py` sites; it did not.

*Cohort C amendments, recorded by Worker 0 after the pass.* **The brief's figures were wrong in both directions and the cause is instrument scope.** Cohort C censused BLOCK-scoped (whole docstrings and comment runs) rather than line-scoped: **80** `Decision N` occurrences across its eight files, of which **18** were genuine orphans, **0** after the pass. A line-scoped instrument over the same files reports 46 "bare" hits, of which **28 (61%) are adjacent-line or wrapped false positives** -- the class Worker 0 manufactured at `routers.py:93` and warned the cohort about, now quantified. The same blindness runs the other way: the line instrument MISSED three real orphans, `optimizer/extension.py:1124` (a `Decision` / `11` wrapped across the break) and `utils/inputs.py:400` and `:1441`. Worker 0's own figures were wrong twice over -- `optimizer/extension.py` was stated as "10" while eleven lines were listed, three of which are not members; `utils/inputs.py` is **4** orphans and a DIFFERENT set than the three named, because line 1300 is not an orphan at all (it carries the `spec-027 / spec-028` shared-Decision precedent the brief told the cohort to look for).

**Strongest attribution in the pass, by disproof rather than by resemblance.** Five `optimizer/extension.py` sites sat in blocks naming `spec-035` and were repaired to `spec-030 Decision 11`. The disproof: `grep -c '^### Decision ' docs/SPECS/spec-035-optimizer_hardening-0_0_10.md` returns **9**, so spec-035 cannot own a Decision 11; `spec-030-connection_field-0_0_9.md` has 14, and its `### Decision 11 - The connection field owns its optimizer cooperation point` states the code's exact claims. Both counts re-derived by Worker 0. An attribution naming a real document that stops short of the cited number is strictly worse than a bare number, because it reads as resolvable.

**Six sites deliberately left unresolved**, which is a successful outcome, not a shortfall: `SR-3` (0 hits repo-wide), `spec-039 H4` (0 in that spec), `D1` (0 anywhere), `H3` x2 and `H5` x3 (two live candidates each with mismatched subjects), and `rest_framework/sets.py:685` (block-resolvable, but the 9-character insert would breach the file's 99-char limit and force a reflow -- and a reflow is how this cycle created a wrapped citation once already).

### Two unmeasured in-fence classes surfaced by cohort C

Neither existed in `bld-final-027.md`'s catalog; both are new.

- **Review-round ids in code that the rationale extraction orphaned.** `spec-027`'s `H1` has **0** occurrences in the spec and **27** in `docs/SPECS/appx/spec-027-filters-0_0_8-rationale.md` (Worker 0 re-derived both). Slice 2 retired build-provenance clauses, but an `H1` / `M1` / `H4` style id that once resolved INTO the spec now resolves nowhere in it. Cohort C repaired the one instance in its partition (`orders/factories.py:9` -> `spec-027 Decision 4`). **The population outside cohort C's eight files has never been measured.**
- **A hyphenated `Decision-N` spelling that no census in this cycle has ever covered.** `grep -rnE 'Decision-[0-9]' django_strawberry_framework/ --include='*.py'` returns **39** occurrences. Every sweep in this cycle, and every sweep the inherited catalog was built from, matched `Decision N` with a space. Some unknown subset of the 39 are orphans.

*Cohort E amendments, recorded by Worker 0 after the pass.* **The `.py` half of catalog item 1 is now closed at 0 repo-wide**, measured over all **425** tracked `.py` files via a `git archive HEAD` scratch tree, with a control run confirming the instrument still finds both originals in the `HEAD` copies. The repo has **0** `.pyi` files, so `*.py` is the whole Python surface. Neither of cohort E's two sites was a reflow-only case: `tests/test_relay_connection.py:2591` was **mis-paired** -- its substring resolves once, but inside `_resolver_identities_for`, not the cited `_plan_connection_relation`, so a hit-count check alone grades it "resolves" and passes a wrong citation; and `examples/fakeshop/apps/kanban/schema.py:359` was **non-unique at 3 hits** with the two bare occurrences byte-identical at identical indentation, so "extend until unique" was structurally unavailable and the repair had to move to a different symbol.

**Item 8's population in cohort E's two files was 6, not the 0 cohort A measured in its five** -- four `Revision N P<n>` review-round ids and two `Implementation step N`, the latter resolving only to an `ARTIFACT.md` / `worker-1.md` TEMPLATE section, which is the same never-existent-surface class this cycle's integration consolidation closed in `filters/factories.py`. The two cohorts do not contradict each other; they measured disjoint files.

### Three further in-fence classes surfaced by cohort E

- **19 wrapped `#"substring"` citations in SPEC files, which the fence covers.** Re-derived by Worker 0: `docs/SPECS/` **15** (`NEXT.md` 143; `spec-037` 15, 486, 1122, 1655; `spec-039` 2347; `spec-040` 734, 782, 884, 1795, 1898, 1951; `spec-041` 673; `spec-045` 294; `spec-046` 409) and `docs/SPECS/appx/` **4** (`spec-001-rationale` 669, `spec-009-rationale` 280, `spec-015-rationale` 680 and 883). `scripts/check_citations.py` puts `docs/` out of scope by design, so no instrument has ever seen these. The remaining 7 markdown sites are `docs/builder/` per-cycle artifacts and stay out of fence.
- **`spec-033`'s Revision 3 finding (4) claims this hygiene sweep was already performed on its relation-connection surface. It was not** -- cohort E found six live sites in exactly the file that spec nominates. A spec asserting completed work that is measurably incomplete is a false claim on a spec surface.
- **`docs/SPECS/appx/spec-015-relay_interfaces-0_0_5-rationale.md` prescribes a `git grep 'spec-<NNN> #"'` sweep as the standing method** -- and that command is line-scoped, so it carries the exact blind spot it exists to close. The companion also claims a retired citation "resolves inside this companion" when the phrase occurs there **twice**, failing rule 27's uniqueness requirement.

**Held pending cohort F, not dropped:** the `workstream B/C/D` vocabulary. Worker 0 measured **12** occurrences across `django_strawberry_framework/**/*.py` (cohort E reported 8 from its own narrower view) and **0** in the entire `docs/` tree -- the partition those comments name is documented nowhere, while live `optimizer/walker.py` code cross-references it. Cohort F is writing `.py` files right now, so dispatching this concurrently would collide; it is sequenced behind F rather than deferred.

*Cohort G amendments, recorded by Worker 0 after the pass.* **18 in-fence sites discharged, and Worker 0's list of 19 had two wrong members in opposite directions.**

- **`docs/SPECS/NEXT.md:143` is not a citation at all.** The line reads ``run `rg -n "^##|^###" <path>``` -- the `#"` is the tail of a grep pattern, preceded by `#`. A string-literal false positive; the file needed no edit.
- **`docs/SPECS/appx/spec-004-optimizer_beyond-0_0_3-rationale.md:1252` is a real wrapped citation that Worker 0's census could not see, and the cause is a defect in the instrument.** That line carries TWO citations: the first (`#"the plan is finalized before handoff"`) closes on the line, the second (`#"plan immutability, the`) wraps onto 1253. Worker 0's census used `line.find('#"')` and tested only the FIRST occurrence per line, so any line whose earlier citation closes hides every later one. **A census of this class must scan every `#"` on the line, not the first.** This is the fourth distinct scope or method error in measuring this one population, after package-only globbing, the first-occurrence bug here, and the two membership errors above.

**Two of the four zero-hit retargets were caused by the target's own text wrapping mid-phrase**, so no single line ever carried the cited string -- the same defect on the target side as on the citing side. Seven further zero-hit sites were reflowed with text unchanged and reported rather than rewritten, because their targets were removed by the very cards those specs describe (`spec-037`'s own shipping removed the seam comment it pins; commit `48f9f65d` retired the string-tuple subsystem-clear ledger `spec-040` 782/1898/1951 describe). Cohort G also extended to `spec-039:357`, an UNWRAPPED occurrence of the same zero-hit substring, on the ground that leaving one spec carrying two spellings of one citation -- one resolving, one not -- is the half-reconciled defect this cycle keeps finding.

### Four further in-fence spec defects, verified by Worker 0, not yet dispatched

- **`spec-040` `### Decision 9`'s mechanism is falsified at HEAD.** Line 783 states that declaration registries are "hand-rowed in `TypeRegistry.clear()` only" and explicitly **not** `register_subsystem_clear`. Measured: `django_strawberry_framework/forms/sets.py:130` registers `clear_form_mutation_registry` (owner `forms.declarations`) and `auth/mutations.py:167` registers `clear_auth_mutation_registry` (owner `auth.declarations`), both through that exact seam. Commit `48f9f65d` moved the distinction the spec is drawing onto the `before_bind` flag: the contract holds, the mechanism named does not. Same defect class as spec-027's own Decision 9 `delattr` claim, which this cycle repaired.
- **`spec-033`'s opener and `Status:` line describe a shipped card as unshipped.** Line 3 reads "Planned for `0.0.9` (card `WIP-ALPHA-033-0.0.9`). **This spec is an open build plan, not a shipped record.** The card is the only card in the `## In progress` column"; line 5 reads `Status: in progress`. `KANBAN.md` carries `DONE-033-0.0.9` at 11 sites. This is worse than the Revision-3 claim cohort G repaired in the same file.
- **`spec-037`'s opener says "Planned for `0.0.11`" on a DONE card.** Same class, smaller blast radius.
- **`docs/SPECS/appx/spec-004-optimizer_beyond-0_0_3-rationale.md:1252`** -- the last wrapped citation in the two spec directories, outside cohort G's writable set.

*Cohort F amendments, recorded by Worker 0 after the pass.* **Both dispatched populations were wrong, and the second was wrong in kind, not just in size.**

- **Class 2, hyphenated `Decision-N`: 78 occurrences with 50 block-orphans, not 39.** Worker 0's 39 was exactly right for `django_strawberry_framework/` and exactly HALF the fence -- the other half sits in `tests/` and `examples/`, which the sweep never globbed. The same package-only scoping error that undercounted the wrapped-citation population. Post-pass: 39 occurrences, 11 orphans. Worker 0 re-derived the 39 independently.
- **Class 1's stated cause is real but RARE, and the dispatch named the wrong dominant population.** Only **2 sites repo-wide** were orphaned by a rationale move (`orders/factories.py:9`, closed by cohort C, and `tests/test_views.py:2144`'s `M1` -- `spec-046` carries it 0 times, its companion 4). The population 13x larger is **26 occurrences of review-round ids that resolve in NO document at all**: 19 `Finding N` (`grep -ci finding` over `spec-038` returns **0**, likewise `spec-036` and `spec-039`, none of which has a companion), 4 `spec-036 L3-1` / `M3-1` (spec-036's real tag families are `AR-M#` / `CR-#` / `DRY-#`), plus `H4` and `SR-3`. **Structural cause, which is the durable finding:** rationale companions exist only for `spec-001`-`028` and `044`-`048`, while code overwhelmingly cites `029`-`043`. Those ids were never extractable -- they never resolved.
- **Worker 0's spelling guesses were empty.** The dispatch named `round-N` and `finding N`; both have **0** occurrences. The real dominant spellings are `Finding N` (19) and `Revision N` (18). `Decision` itself has five spellings: spaced 1189, hyphenated 78, `DN` 36, newline-wrapped 7, `Spec Decision` 1.
- Line-scoped versus block-scoped on class 2: 70 bare hits, **20 false positives (29%)**, 0 misses -- a second independent measurement of the line-scoped instrument's error rate after cohort C's 61%.

### The largest remaining in-fence population, and Worker 0's stopping decision

**Spaced `Decision N` in `.py`: 1238 occurrences (Worker 0 re-derived; cohort F measured 1230), of which cohort F puts 257 at block-orphan.** Cohort C's 18 was over 8 files; this is the fence-wide figure. Concentrations: `tests/test_routers.py` **26** plus a newline-wrapped `Decision` / `19` at 1647, `connection.py` **20**, `tests/test_permissions.py` **19**.

Worker 0 dispatches the three concentrations (**65 of 257**) and **stops there deliberately**. The remaining ~192 are spread thinly across many files, and both cohort C and cohort F demonstrated that this class cannot be swept -- each site needs its owning spec established by measurement, C left 6 of 18 and F left 26 sites unresolved precisely because they could not be grounded. Forcing the long tail in one pass would manufacture confidently wrong card ids, which every cohort in this cycle has identified as strictly worse than a bare number: a wrong id sends the reader to a real document that says something else, and it gets copied. The tail is a `TODO-ALPHA-052-0.1.0` sweep, not a slice.

**Also routed to cohort I:** `tests/optimizer/test_walker.py:2426` and `:2455` cite `spec-033 Decision 11 ... / Revision 3` and `spec-033 Decision 6 / Revision 3`. Cohort G rewrote spec-033's Revision-3 finding into a normative contract -- a comment cites a spec, card, or symbol path, never a review artifact or a `Revision N` id -- so these two sites now contradict a contract stated on their own card's spec. The contract is sound; the code is what moves.

*Cohorts I and H (follow-up) amendments, recorded by Worker 0.*

**A SIXTH spelling of the card-reference token, which every census in this cycle missed: capital-S `Spec-NNN`.** Cohort I found it; Worker 0 re-derived **52** occurrences in `.py` and **166** in tracked `docs/` (`git grep`). Every block-scoped census run in this cycle -- cohort C's, cohort F's, and cohort I's own first pass -- matched `spec-NNN` case-sensitively, so each over-counted orphans and under-counted resolved sites. In `tests/test_routers.py`, **13 of 27** apparent orphans name their card in capitalized form, including the wrapped site at `:1647` that Worker 0's brief nominated as the worked example: its docstring opens `"""Spec-046 row 28:`, so it was never an orphan. **Fence-wide correction: the spaced-`Decision N` orphan population is 243 case-insensitive, not the 257 Worker 0 recorded, which was the case-sensitive figure.**

Worker 0's `docs/` figure of 182 was also methodologically wrong, for a reason worth keeping: it was a working-tree `grep -r`, which counts this cycle's own **untracked in-flight `bld-*` artifacts**. A population measured over a tree that includes the measuring cycle's own output is not a population. Tracked-only (`git grep`) is the reproducible figure, and the ~16-occurrence gap is entirely those artifacts.

**Cohort I's strongest attributions were all disproofs by heading count**, which is now the established method for this class: `spec-046` is the only spec in the repo with >=16 `### Decision` headings (19), uniquely fixing the `Decision 19` and `Decision 16` sites; `spec-041` has 11, killing it for `Decision 12`; `spec-045` has 8, killing it for all of `test_permissions.py`'s D9-D12. It also **declined a renumber it could not ground**: at `tests/test_routers.py:3877` the card is certain but a per-decision-block scan puts 17 of 18 `lease` occurrences under `spec-046 Decision 16` and **0** under Decision 11, making the cited number an author slip -- a claim about intent, not a citation repair. It added the card and escalated the number.

**The `spec-033` quotation was a phantom, not a drift.** Its line 106 quoted a `connection.py` docstring fragment, and cohort H measured the spec's exact text as matching **0** occurrences in the code at HEAD *and* in the tree -- so the misquotation predated the renumber entirely ("will use" where the code says "uses"). The second quoted fragment was worse: `"Deliberately ABSENT"` has **zero** occurrences in every `.py` file in the repo (Worker 0 re-derived), because this card's own Slice 4 wired the strictness the docstring used to disclaim. The two sentinel names it cites (`DST_OPTIMIZER_STRICTNESS`, `DST_OPTIMIZER_PLANNED`) **are** still live symbols in `optimizer/_context.py:35-37`, which is precisely what let a quotation of vanished text survive every skim. Cohort H found and fixed a second site (line 371) asserting the same vanished block, on the ground that repairing 106 alone would leave the spec asserting the block twice and quoting it once.

**Cohort H's own `grep -c` error, recorded because it is the canonical trap.** Its first board count of `DONE-033-0.0.9` was 11; the real figure is **15**. `grep -c` counts matching LINES, and four occurrences share a line -- the exact failure `BUILD.md` `## Claims are proven mechanically` names in words, reproduced in an artifact that quotes the rule elsewhere. Worker 0 made the identical error earlier in this cycle counting `(name, input_class)` occurrences.

**Still open, needing a `.py`-side owner:** `django_strawberry_framework/types/finalizer.py:660` names the retired `WIP-ALPHA-033-0.0.9`. No spec quotes it, so no Worker 1 surface can catch it. It is in cohort K's writable set.

### Closeout of the catalog-discharge cohorts (Worker 0, 2026-08-20)

**Recorded deviation, at the maintainer's explicit direction to finish and take shortcuts.** Slices 6-16 went from `built` to `final-accepted` WITHOUT a Worker 3 review round or a Worker 1 final-verification pass. No independent reviewer read any of them. Each cohort ran its own scoped validation and its own executable-token-identity proof with a challenged instrument, and Worker 0 re-verified the following mechanically across the whole cohort before advancing the statuses -- but per-finding attribution correctness rests on each builder's own evidence, unreviewed. The prior review rounds in this cycle each found a real defect, including one Medium that was a NEW false claim installed by a repair, so the residual risk here is a wrong card-id attribution surviving into the record.

Verified by Worker 0 across all 50 changed `.py` files at once:

| Check | Result |
| --- | --- |
| Full suite, no `--cov*` | **6179 passed, 40 skipped**, exit 0 -- byte-identical to the pre-cycle baseline |
| Executable-token identity vs `HEAD`, all 50 `.py` | **0 divergent** (comment/docstring text only, tokenized, statement-position strings dropped, compared element-wise) |
| `scripts/check_citations.py` | `OK: 782 citations resolve (705 in 422 .py files, 77 in KANBAN.md)` -- up from 743 at `8a9840dc`, entirely `.py` refs the cohorts made resolvable |
| `scripts/check_trailing_commas.py --check`, all 82 changed paths | exit 0 |
| Wrapped-citation census, every `#"` per line over `.py` + `docs/SPECS/` + `appx/` | **0 real** -- the 2 remaining hits are `grep`/`rg` patterns quoted inside prose (`NEXT.md:143`, `appx/spec-021-apps-0_0_7-rationale.md:61`), the string-literal false-positive class, confirmed by reading both |

**Open and deliberately not fixed**, carried for `TODO-ALPHA-051-0.0.15` / `TODO-ALPHA-052-0.1.0`: the ~243-site case-insensitive spaced-`Decision N` orphan tail; 52 capital-S `Spec-NNN` occurrences in `.py` and 166 in tracked `docs/`; 56 stale `TODO-ALPHA-`/`TODO-BETA-` occurrences across specs 034-039; 7 wrapped citations in `docs/builder/` artifacts; `types/relay.py:680`'s retired `WIP-ALPHA-032-0.0.9`, where the SPEC is correct and the code is the defect; and the five-spec population (029-032, 034-036, 038-040) still carrying its deliberative layer inline with no rationale companion.

**One cross-cohort disagreement resolved against a decision Worker 0 had endorsed:** cohort H kept `spec-033:564`'s pre-move board id as a decided non-edit and Worker 0 agreed in writing. Cohort J then measured the archive's five reconciled shipped records (020, 021, 022, 025, 027) and found **zero** carrying a pre-move id -- the spelling survives only in unreconciled specs. Cohort J's position is the evidenced one; the `spec-033` site is left as cohort H wrote it and is the minority spelling in the archive.
