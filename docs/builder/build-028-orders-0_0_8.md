# Package build plan: orders / 0.0.8 (028)

Spec source: `docs/SPECS/spec-028-orders-0_0_8.md` (already archived; the active-spec path `docs/spec-028-orders-0_0_8.md` does not exist)
Target release: `0.0.8` (shipped; card `DONE-028-0.0.8`, `KANBAN.md` Done column)
Build rule: one slice at a time. Plan first, build second, review third, reconcile fourth.
DRY rule: every slice must justify shared/duplicated patterns before merging.
Ownership partition: none; sequential slices. Slices 1 and 3 are Worker 1's alone; Slice 2 is the only slice with a Worker 2 / Worker 3 cycle.
Hot-path declaration: none. No production behavior changes. Slice 2 edits comment and docstring text only; nothing runs per request, per resolver, per row, per connection, or per outbound message.
Floor-verification scope: none. No slice touches a Django / Strawberry / channels integration seam — no slice changes an executable statement.
Pre-flight: passed on 2026-08-20 with two recorded deviations (below); baseline: DIRTY with the prior `spec-027` cycle's uncommitted work.

**HEAD moved mid-cycle, 2026-08-20.** The maintainer committed the `spec-027` residual cycle as `8a9840dc` ("docs(specs): complete the spec-027 record and reconcile its Decision 9 claims") while Slice 2's review pass was running, and that commit **swept in three of Slice 2's own files** — `tests/types/test_base.py`, `tests/test_registry.py`, `examples/fakeshop/test_query/test_library_api.py` — each carrying both cycles' hunks. Nothing was lost or reverted (verified against `git show HEAD:<path>`; every Slice-2 postcondition still holds in the working tree, and `scripts/check_citations.py` reads `OK: 758 citations resolve`). Two consequences every later pass must apply: **`git diff HEAD -- <path>` is now empty for those three paths**, so Slice 2's work in them is read with `git show 8a9840dc -- <path>` or `git diff 5c6fdd71 -- <path>`; and three `spec-028` hunks now sit in committed history under a `spec-027` commit message that does not mention them. The second is the maintainer's to know about at handoff, not a worker's to fix — nothing is amended, re-staged, or force-pushed.

## Cycle shape — residual reconciliation of a SHIPPED spec, not a fresh build

`DONE-028-0.0.8` shipped the whole ordering subsystem. Worker 0 verified every contract-bearing claim the spec makes against HEAD before writing this plan (evidence in `## Pre-dispatch verification`): **nothing was skipped in the code.** Every named module, primitive, classmethod, helper, `Meta` key, finalizer subpass, fakeshop declaration, and all 14 contracted live HTTP tests are present — the surface has since *grown* (a 15th live order test, two more fakeshop ordersets, path pre-validation, an async-gate rejection) rather than shrunk. Exactly one spec-named symbol was deliberately **deleted** post-ship (`OrderSet.check_permissions`, `9e864f59`) and exactly one was **never** shipped (`ORDER_BY_ARG`, which nothing needs); both are recorded below with their evidence.

Three things are owed:

1. The spec's `-rationale.md` sibling was never created. `docs/SPECS/appx/` carries a `-rationale.md` for every spec from `001` through `027`; `028` is the gap. This spec is the second-largest deliberative-layer carrier in the archive after `027` — a seven-revision (`rev1`–`rev7`) inline review history plus a `Justification:` / `Alternatives considered (and rejected):` pair under every one of the thirteen Decisions.
2. The spec still narrates its own history. Per `docs/builder/BUILD.md` `## Spec rationale extraction`, "the spec never narrates its own history" — but `028` cannot be read as a current contract at all: its Decisions are stated as a chronology of corrections ("per H1 of the rev-1 adversarial review", "corrected in rev7 — the named test was not shipped", "per Worker 1 Slice-4 final-verification reconciliation"), so a reader must apply seven revisions in order to learn what is true.
3. Fourteen months of later cards moved surfaces the spec pins by name, and four parallel sites still pin a contract a later card retired.

Per the maintainer's dispatch instruction, Workers 2 and 3 are dispatched only where a slice needs a **code** change. Exactly one slice does (Slice 2).

## Pre-flight outcome and deviations

- Step 1 (baseline): DIRTY. The prior `spec-027` residual cycle's work is uncommitted (21 modified files + 8 untracked). Baseline-dirty out-of-scope files are listed below.
- Step 2 (`scripts/review_inspect.py`): smoke run on `django_strawberry_framework/orders/factories.py --output-dir docs/shadow --stdout` exited 0.
- Step 3 (artifact reset): **DEVIATION — old `bld-*.md` / `build-*.md` were NOT deleted.** `bld-003-final.md`, `bld-final-027.md`, `bld-integration-027.md`, `bld-slice-{1,2,3,4}-027-*.md`, and `build-027-filters-0_0_8.md` are the prior cycle's **untracked and uncommitted** record — the maintainer has not committed them, so deleting them destroys the only copy, which `worker-0.md` `## Pre-flight procedure` step 3 names as the one irreversible pre-flight mistake. This cycle therefore uses `-028`-suffixed artifact paths; all six were verified absent.
- Step 4 (`.gitignore`): `docs/shadow/` (line 174), `docs/builder/worker-memory/` (line 188), `docs/builder/temp-tests/` (line 192) all listed.
- Step 5 (scratch cleared): **DEVIATION — scratch was NOT cleared**, same reason as step 3. `worker-memory/worker-{0,1,2,3}-{025,026,027}.md` are three cycles' notebooks and `docs/shadow/` holds the 027 cycle's overviews. This cycle's workers use `docs/builder/worker-memory/worker-<N>-028.md`, which Worker 0 created empty. `docs/builder/temp-tests/` is already empty.
- Step 6 (`check_spec_glossary`): `OK: 44 terms - all have glossary entries and at least one spec link.` (exit 0). The terms CSV carries 44 rows, no duplicate anchors.
- Step 7 (rationale extraction): NOT yet done — it is Slice 1 of this cycle.

### Baseline-dirty out-of-scope files (never revert; two are in-scope to EDIT)

Dirty at baseline from the `spec-027` cycle: `django_strawberry_framework/filters/{base,factories,inputs,sets}.py`, `django_strawberry_framework/types/finalizer.py`, `django_strawberry_framework/utils/inputs.py`, `docs/SPECS/spec-027-filters-0_0_8.md`, `examples/fakeshop/apps/kanban/filters.py`, `examples/fakeshop/apps/library/{filters,filters_genre}.py`, `examples/fakeshop/apps/products/filters.py`, `examples/fakeshop/test_query/{test_library_api,test_products_api}.py`, `tests/filters/{test_factories,test_finalizer,test_inputs,test_sets}.py`, `tests/types/fixtures/{branch_module,shelf_module}.py`, `tests/types/{test_base,test_definition_order}.py`, plus untracked `docs/SPECS/appx/spec-027-filters-0_0_8-rationale.md`, `docs/builder/bld-*-027*.md`, and `docs/builder/build-027-filters-0_0_8.md`.

**Two of those files are in Slice 2's writable set** — `tests/types/test_base.py` (finding C1) and `examples/fakeshop/test_query/test_library_api.py` (findings C3, C5). Slice 2 edits named lines in them additively and **never reverts a 027 hunk**. Every other file above is untouched by this cycle. Verify with `git diff HEAD -- <path>`, never `git diff -- <path>` (a concurrent `add -A` makes the latter read clean).

**Concurrent-writable tracked binary / generated files:** `examples/fakeshop/db.sqlite3`, `KANBAN.md`, `KANBAN.html`, `docs/GLOSSARY.md`. This cycle plans NO edit to any of them, so any churn observed in them is another session's and must be left alone.

### Scope fence set by the maintainer

This cycle touches **spec files and `.py` files only**. No closeout-agentflow edits: no `docs/builder/BUILD.md`, `ARTIFACT.md`, or `worker-*.md` role-file changes; no KANBAN / GLOSSARY / CHANGELOG / TREE / README movement; no `-terms.csv` edit (its 44 anchors resolve, `check_spec_glossary` is green, and it has no duplicate anchor); no closeout retrospective; no DB work (the card is already `DONE-028-0.0.8`).

**"Archive the spec"** in this cycle means: the spec stays at `docs/SPECS/spec-028-orders-0_0_8.md` (where it already is) and the new rationale companion lands at `docs/SPECS/appx/spec-028-orders-0_0_8-rationale.md`, matching every other archived spec's companion layout. No file moves.

## Pre-dispatch verification (Worker 0, against HEAD)

Findings below are handed to Worker 1 as **verified inputs, not hypotheses**. Every one was read against source before this plan was written.

### Delivered in full — no code gap

| Spec claim | Verified at | Result |
| --- | --- | --- |
| `orders/` subpackage, five files | `django_strawberry_framework/orders/` | `__init__.py`, `base.py`, `sets.py`, `factories.py`, `inputs.py` all present |
| `RelatedOrder` + `bind_orderset` + `.orderset` property | `django_strawberry_framework/orders/base.py::RelatedOrder` | present; `bind_orderset`, the `orderset` getter and setter all present |
| `OrderSetMetaclass` + `OrderSet` | `orders/sets.py::OrderSetMetaclass` / `::OrderSet` | both present; `OrderSet(ClassBasedTypeNameMixin, ActiveInputPermissionMixin, metaclass=OrderSetMetaclass)` |
| `_owner_definition` / `_expanded_fields` / `_is_expanding_fields` slots | `orders/sets.py::OrderSet` | all three present with the spec's declared defaults (`None`, `None`, `False`) |
| `get_fields` + `get_flat_orders` | `orders/sets.py::OrderSet.get_fields` / `::OrderSet.get_flat_orders` | both present |
| `apply_sync` / `apply_async` pair, **no** `apply()` dispatcher | `orders/sets.py::OrderSet.apply_sync` / `::OrderSet.apply_async` | both present, `apply_async` is an `async def` returning `models.QuerySet`; no `apply(...)` dispatcher exists (Decision 2 H1 honored) |
| `Ordering` six-member enum + `resolve()` | `orders/inputs.py::Ordering` / `::Ordering.resolve` | all six members present; `resolve` returns `F(value).asc/desc(nulls_first=…, nulls_last=…)` with the True-or-None sentinel semantics Decision 5 pins |
| `INPUTS_MODULE_PATH` | `orders/inputs.py` #"INPUTS_MODULE_PATH: str" | present, value `"django_strawberry_framework.orders.inputs"` |
| `_input_type_name_for` / `build_input_class` / `FieldSpec` | `orders/inputs.py` #"_input_type_name_for = set_input_type_name" | all three present as domain-local aliases over the shared substrate (see D5) |
| `_build_input_fields` / `convert_order_field_to_input_annotation` / `normalize_input_value` | `orders/inputs.py::_build_input_fields` / `::convert_order_field_to_input_annotation` / `::normalize_input_value` | all three present with the spec's declared signatures |
| `materialize_input_class(name, input_cls)` / `clear_order_input_namespace()` | `orders/inputs.py::materialize_input_class` / `::clear_order_input_namespace` | both present, two-argument signature as Decision 9 pins |
| `_materialized_names` / `_field_specs` ledgers | `orders/inputs.py` #"_materialized_names: dict[str, type]" | both present; `_field_specs` keyed `(OrderSet, python_attr)` |
| `_get_concrete_field_names_for_order` M2M-exclusion divergence | `orders/inputs.py::_get_concrete_field_names_for_order` | present; the deliberate `and not getattr(f, "many_to_many", False)` clause Decision 3 pins is there |
| `OrderArgumentsFactory` + `input_object_types` + `_type_orderset_registry` | `orders/factories.py::OrderArgumentsFactory` | all three present |
| `get_orderset_class` + `_dynamic_orderset_cache`, **no caller** | `orders/factories.py::get_orderset_class` / #"_dynamic_orderset_cache" | both present; the only non-test references are inside `factories.py` itself, exactly as Decision 12 states |
| `_helper_referenced_ordersets` + `order_input_type` | `orders/__init__.py` #"_helper_referenced_ordersets" / `::order_input_type` | both present; the helper returns the element annotation and raises for a non-`OrderSet` (via the shared builder — see D5) |
| `orders/__init__.py.__all__` three-tier contract | `orders/__init__.py` #"__all__: tuple[str, ...]" | exactly `OrderSet`, `OrderSetMetaclass`, `Ordering`, `RelatedOrder`, `order_input_type`; `OrderArgumentsFactory` deliberately absent, as DoD 2 pins |
| `DjangoTypeDefinition.orderset_class` slot | `types/definition.py::DjangoTypeDefinition` #"orderset_class: type \| None = None" | present |
| `Meta.orderset_class` promotion | `types/base.py` #"DEFERRED_META_KEYS" / #"ALLOWED_META_KEYS" | `DEFERRED_META_KEYS` is `{"aggregate_class", "fields_class", "search_fields"}`; `"orderset_class"` is in `ALLOWED_META_KEYS` |
| `_validate_orderset_class` with the **local** `OrderSet` import | `types/base.py::_validate_orderset_class` | present; carries the in-function `from ..orders.sets import OrderSet` with the "Do NOT hoist to module top" comment DoD 9 requires |
| phase-2.5 `_bind_ordersets()` four ordered subpasses | `types/finalizer.py::_bind_ordersets` | present; called from `finalize_django_types` immediately after `_bind_filtersets()`; bind-owners → expand → orphan-validate → materialize |
| `_bind_orderset_owner` three checks | `types/finalizer.py::_bind_orderset_owner` | present, with `_format_owner_orderset_model_mismatch_error` naming all four entities |
| subpass 2's explicit `.orderset` walk | `types/finalizer.py::_expand_orderset` | present; calls `get_fields()` then walks `related_orders.values()` reading `.orderset`, exactly as Decision 6 subpass 2 pins as load-bearing |
| order side does NOT enforce own-PK Relay identity | `tests/orders/test_finalizer.py` #"does NOT enforce own-PK Relay identity" | present; Decision 6(b)'s narrower-by-one-axis claim is pinned |
| `registry.clear()` co-clears both order ledgers | `django_strawberry_framework/registry.py::TypeRegistry.clear` | co-clear happens — but through the registration seam, not the shape Decision 9 pins (see **D3**) |
| fakeshop `orders.py` five ordersets | `examples/fakeshop/apps/library/orders.py` | `BranchOrder`, `ShelfOrder`, `BookOrder`, `LoanOrder`, `PatronOrder` all present with same-module `RelatedOrder("...")` references (plus two more — see **D15**) |
| fakeshop `orders_genre.py` cross-module fixture | `examples/fakeshop/apps/library/orders_genre.py::GenreOrder` | present; `BookOrder.genres = RelatedOrder("apps.library.orders_genre.GenreOrder")` present |
| `BranchOrder.check_name_permission` / `check_shelves_permission` | `examples/fakeshop/apps/library/orders.py::BranchOrder.check_name_permission` / `::BranchOrder.check_shelves_permission` | both present, both raising `GraphQLError(..., extensions={"code": "ORDER_PERMISSION_DENIED"})` |
| `Meta.orderset_class` on the library types | `examples/fakeshop/apps/library/schema.py` | eight wirings present (`Loan`, `Book`, `Shelf`, `Genre`, `Branch`, `Patron`, `Issue`, `Periodical`) — DoD 14 names six (see **D15**) |
| `list[order_input_type(...)] \| None` on library root resolvers | `examples/fakeshop/apps/library/schema.py` | six resolver annotations present, each chaining `get_queryset` → `Filter.apply_sync` → `Order.apply_sync` exactly as Decision 8 steps 1-3 pin |
| **all 14 contracted live HTTP tests** | `examples/fakeshop/test_query/test_library_api.py` | 13 present under the exact names the Test plan gives; the 14th ships parametrized under a different name (see **D12**). Names verified: `test_library_branches_order_by_name_asc`, `..._order_by_forward_fk_relation`, `..._order_by_reverse_fk_relation`, `..._order_by_m2m_absolute_import_path`, `test_library_books_filter_and_order_compose`, `..._order_preserves_optimizer_cooperation`, `test_root_get_queryset_runs_before_order_apply`, `test_order_check_permission_denies_for_active_field`, `..._quiet_for_inactive_field`, `..._denies_active_related_branch`, `test_library_books_order_by_multi_field_priority`, `..._order_by_flat_shorthand_path`, `test_library_branches_order_empty_list_and_null_direction_no_op` |
| `tests/orders/` seven files | `tests/orders/` | `__init__.py`, `test_base.py`, `test_sets.py`, `test_factories.py`, `test_inputs.py`, `test_finalizer.py`, `test_composition.py` — 7 files, 149 test functions |
| the named finalizer / inputs / registry tests | `tests/` | `test_phase_2_5_binds_all_owners_before_expansion`, `test_phase_2_5_rejects_orderset_wired_to_unrelated_owner_model`, `test_phase_2_5_unresolved_related_order_raises_at_finalize`, `test_phase_2_5_configuration_error_during_expansion_propagates_by_identity`, `test_orphan_order_input_type_reference_raises_at_finalize`, `test_registry_clear_works_without_orders_imported` (still a real subprocess test), `test_clear_tolerates_unimportable_order_submodules`, `test_order_input_type_returns_element_annotation_for_orderset_subclass`, `test_order_input_type_is_idempotent_under_repeated_calls`, `test_orderset_apply_async_runs_check_permission_in_sync_to_async`, `test_orderset_request_from_info_reads_context_request_attribute`, `test_orderset_request_from_info_reads_bare_httprequest_context`, `test_orderset_check_permission_dedups_repeated_list_entries`, `test_meta_orderset_class_is_promoted_to_allowed_meta_keys` all present |
| Decision 12's post-ship symbols | multiple | `connection.py::_synthesized_signature` / `::_pipeline_sync` / `::_pipeline_async`, `orders/sets.py::OrderSet._resolve_order_expressions`, `utils/relations.py::path_traverses_to_many`, `utils/inputs.py::make_dynamic_set_getter` all present — Decision 12 is the one Decision already reconciled to HEAD |
| five glossary entries shipped | `docs/GLOSSARY.md` | `## \`OrderSet\``, `## \`Ordering\``, `## \`order_input_type\``, `## \`RelatedOrder\``, `## \`Meta.orderset_class\`` all present at `shipped (0.0.8)` |

### Drifted or defective spec claims Worker 1 must reconcile (verified, with the cause)

Each is a claim the spec makes that is false, unauditable, or narrated as history rather than contract. **None is a code defect except C1–C6, which Slice 2 owns.**

- **D1 — the spec narrates its own history end to end, and has no rationale companion.** The `Revision history (kept inline so the spec is self-contained)` block runs seven `Revision N` entries. All thirteen Decisions close with `Justification:` + `Alternatives considered (and rejected):` (13 of each, measured). Beyond those blocks, review provenance is welded into the *contract* prose: 90 occurrences of `adversarial review`, 160 `rev-N` references, 89 `per <ID> of` citations (`per B4 of the rev-3 adversarial review`, `per H1 of the rev-1 adversarial review`, …), and 8 `Worker 1` references (`per Worker 1 Slice-4 final-verification reconciliation`) appear across Decisions 2, 3, 5, 6, 8, 9, 11, 12, 13, the Slice checklist, Edge cases, the Test plan, Doc updates, and the DoD. `BUILD.md` `## Spec rationale extraction` forbids exactly this shape: "a reader must never reconstruct what is currently true by applying a chronology to it." Slice 1 moves the deliberative layer; Slice 3 rewrites the surviving contract prose so it states the contract directly. Spec is 289,080 bytes at entry.
- **D2 — the `Status:` line is a build-progress log, not a state.** Line 4 is a single 4,300-character paragraph tracking Slices 1-6 as they landed, narrating two superseded maintainer-review corrections and quoting its own disproved diagnosis ("was empirically disproved: the maintainer made the glossary tests pass and the same nine lines stayed uncovered"), attributing the full-suite gate to a named pass, and closing "Awaiting maintainer commit." It also enumerates four independent closing pieces (a)-(d), three of which belong to other cards (a filter async-path coverage gap, a kanban/glossary fixture fix).
- **D3 — Decision 9's `registry.clear()` integration is the retired shape, and its code block is 59 lines of Python that no longer exists.** Decision 9's `Import-cycle-safe integration` bullet and the fenced `registry.py` block pin `try: from .orders.inputs import clear_order_input_namespace / except ImportError: pass / else: …` blocks **inside** `TypeRegistry.clear`, with the same claim made for the filter side. At HEAD: `registry.py::register_subsystem_clear` / `::iter_subsystem_clears` are the seam; `orders/inputs.py` registers `clear_order_input_namespace` (owner `orders.input_namespace`, `before_bind=True`) and `orders/__init__.py` registers `_clear_helper_referenced_ordersets` (owner `orders.helper_references`), both at import time; `TypeRegistry.clear` carries **no** `except ImportError` guard for either subsystem — it replays `for clear in iter_subsystem_clears(): clear()`. `orders/__init__.py`'s own comment records that "that shape predates the registration seam". Decision 9's `clear_order_input_namespace` clause is also now indirect: the helper is a thin wrapper over `utils/inputs.py::make_set_input_namespace`'s heavy clear, and it explicitly does **not** touch `_helper_referenced_ordersets` (that ledger clears through its own row) — which the spec's two-separate-blocks rationale still describes correctly in intent but wrongly in mechanism.
- **D4 — `OrderSet.check_permissions` was deliberately DELETED post-ship, and the spec still names it plus a test that never existed.** Decision 8 step 6's parenthetical, Decision 2's `sets.py` bullet, the Borrowing-posture bullet (`orderset.py::AdvancedOrderSet.check_permissions` → "port verbatim"), and DoD item 4(e) all list `check_permissions` on `OrderSet`. It shipped in `11d9fbe0` and was removed by `9e864f59` ("Finish REVIEW of 0.0.9"), which in the same diff rewrote the module docstring from "the `check_permissions` instance method + the classmethod pipeline" to "the classmethod permission pipeline" — i.e. the deletion was deliberate, a dead cookbook-compat delegate retired. `grep -rn 'def check_permissions' django_strawberry_framework/` returns exactly one hit, `filters/sets.py::FilterSet.check_permissions`. The test Decision 8 step 6 cites, `tests/orders/test_sets.py::test_orderset_check_permissions_instance_method_delegates`, has **0** occurrences anywhere but inside the spec itself.
- **D5 — the order side's mechanics moved into the shared set-family substrate; `orders/*` keeps the spec-named surface as aliases, wrappers, or inherited members.** Cause: the sibling filter card plus the later DRY-squeeze work single-sited both set families. Per mechanism:
  - `RelatedOrder(RelatedSetTargetMixin)` — `sets_mixins.py::RelatedSetTargetMixin` (itself a `LazyRelatedClassMixin` subclass) owns `_bind_owner` / `_resolved_target` / `_set_target`. Decision 2, Decision 3 Layer 2, and DoD 3 name `LazyRelatedClassMixin` as the direct base.
  - `_run_permission_checks`, `_request_from_info`, `_active_permission_field_paths`, `_iter_active_related_branches`, `_invoke_permission_method`, `_extract_branch_value`, and the `_fired` dedup map all live on `sets_mixins.py::ActiveInputPermissionMixin` (delegating to `utils/permissions.py::invoke_permission_method`); `OrderSet` inherits them and configures via a class-level `ActiveInputPermissionAttrs`. Decision 8 step 6 and its dedup-contract paragraph attribute all of them to the order side.
  - metaclass collection is `sets_mixins.py::collect_related_declarations`; the Layer-4 cache/guard is `::expanded_once` / `::should_cache_expansion` plus a class-level `SetLifecycleAttrs`. Decision 3 Layers 3 and 4 describe the `__dict__` guard directly.
  - `orders/inputs.py`'s `FieldSpec`, `build_input_class`, `_input_type_name_for`, `_iter_orderset_subclasses` are one-line aliases of `utils/inputs.py::GeneratedInputFieldSpec` / `::build_strawberry_input_class` / `::set_input_type_name` / `::iter_set_subclasses`; `materialize_input_class` and `clear_order_input_namespace` are thin wrappers over `utils/inputs.py::make_set_input_namespace`.
  - `OrderArgumentsFactory(GeneratedInputArgumentsFactory)` — the BFS lives on the shared base and the subclass declares `_build_input_triples` plus class-level config (`_collision_registry_attr`, `_factory_label`, `_family_label`, `_rename_noun`, `_related_attr`, `_related_target_attr`); `arguments` is the base's property. Decision 3 Layer 5 and Decision 6 subpass 4 name `_ensure_built` and `_build_class_type`, which have **0** occurrences under `orders/`.
  **Every spec-named name still resolves from `orders.*`**, deliberately, so this is a where-the-mechanics-live change and not a surface break — except `_ensure_built` / `_build_class_type`, which are gone. The spec should state where the mechanics live.
- **D6 — `apply_async`'s thread boundary is now a shared helper.** Decision 8's `apply_async permission-hook dispatch` paragraph (and the rev-2 N7 / rev-7 B1 breadcrumbs that produced it) pin `await sync_to_async(cls._run_permission_checks, thread_sensitive=True)(input_value, request)` literally, plus the `thread_sensitive=True` rationale. At HEAD `orders/sets.py::OrderSet.apply_async` calls `await run_in_one_sync_boundary(cls._run_permission_checks, input_value, request)` from `utils/querysets.py`. The behavioral claim survives; the named mechanism does not.
- **D7 — the permission gate now REJECTS an `async def check_<field>_permission`, and the spec names no such rejection.** `utils/permissions.py::invoke_permission_method` runs the gate's return through `reject_async_in_sync_context`, raising `SyncMisuseError`, because an un-awaited coroutine is truthy and an intended denial would otherwise silently become an authorization **bypass**. Decision 8 step 6, the `Error shapes` list, and the User-facing-API `check_*_permission` examples describe only the `GraphQLError` denial path.
- **D8 — order paths are now pre-validated, which falsifies two spec claims.** `7000d920` added `utils/relations.py::classify_path` calls in both `orders/sets.py::OrderSet._expand_meta_fields` (over every `Meta.fields` entry) and `::OrderSet._resolve_order_expressions` (over every resolved path), each raising `ConfigurationError` with the path and model named. Consequences:
  - the Edge-case bullet "Order that raises a Django ORM error at queryset-translation time … the framework does not pre-validate the backend's supported expressions" is now false for a bad path — it *is* pre-validated, as a `ConfigurationError` rather than a Django `FieldError`. Pinned by `tests/orders/test_sets.py::test_orderset_resolve_order_expressions_rejects_unknown_order_path`.
  - the `Error shapes` bullet "Invalid `Meta.fields` member … `ConfigurationError` at type-creation time" names the wrong time. Decision 3 Layer 3 is explicit that the metaclass "does not expand" and defers to `get_fields()`, so the raise lands at finalize phase-2.5 subpass 2, not at type creation. Pinned by `::test_orderset_meta_fields_rejects_unknown_order_path`.
- **D9 — the order path resolves against the QUERYSET's model, not `Meta.model`, and a model-less `OrderSet` is legal.** `orders/sets.py::OrderSet._apply_orderings` calls `_resolve_order_expressions(flat_orders, model=queryset.model)`. Pinned by `tests/orders/test_sets.py::test_modelless_orderset_uses_queryset_model_for_to_many_order` and `::test_queryset_model_overrides_conflicting_orderset_meta_model` (landed via `ae6ac9ab` "fix(orders): use queryset model for fanout"). Decision 8 step 7 and DoD 4(b) describe only `queryset.order_by(*expressions)` from `Meta.model`-derived paths, and Decision 3 treats `Meta.model` as mandatory throughout.
- **D10 — the to-many correction landed in ONE of five parallel sites.** The row-preserving `Min`/`Max` aggregate (per `spec-030-connection_field-0_0_9` P1-B) is correctly stated in the Slice-4 checklist bullet, in Decision 12, and in the Non-goals DISTINCT-ON bullet. Four sites still pin the retired JOIN-multiplicity contract, measured on the `multiplicity|multiplied|denormalized` vocabulary:
  - the Test-plan `test_library_branches_order_by_reverse_fk_relation` bullet — "assert the response carries Alpha three times", "Pinning the multiplicity (rather than seeding one-shelf-per-branch to dodge it) catches a future regression where the runtime accidentally `.distinct()`s the queryset", and "The `RelatedOrder` GLOSSARY entry calls out this multiplicity";
  - Decision 13's capability list — "reverse-FK relation order with denormalized-multiplicity asserted (per M5 of the rev-1 adversarial review)";
  - the Implementation-plan Slice-4 row — "reverse-FK relation with denormalized-multiplicity asserted (per M5 of rev1)";
  - the KANBAN past-tense body quoted in Doc updates — "reverse-FK with denormalized-multiplicity-pinned".
  The shipped test asserts `names == ["Alpha", "Beta"]` and its docstring states the old shape "silently corrupted cursors / `totalCount` on a connection". This is the cycle's dominant defect class: a later card's correction applied at the checklist and one Decision, and never swept across the Test plan, the other Decision, the plan table, or the quoted card body.
- **D11 — the GLOSSARY claims are false in both directions.**
  - Decision 8 step 4 asserts "The GLOSSARY entry for `OrderSet` and the `RelatedOrder` entry both call this out (Slice 5 doc-update bullet)" about the position-side-channel leak. Neither entry mentions a side channel, a leak, or a position inference. The rev-2 H4 breadcrumb repeats the claim.
  - The Test-plan bullet asserts "The `RelatedOrder` GLOSSARY entry calls out this multiplicity". It does not — and must not, because the multiplicity no longer occurs.
  - Conversely the shipped `## \`OrderSet\`` entry documents three contracts the spec's Doc-updates block does not name: the `Min`/`Max` row-preserving aggregate, the root connection's deterministic pk tiebreaker over the grouped queryset, and the deliberate nested-relation-connection `orderBy:` bypass of window/lateral planning. `docs/GLOSSARY.md` is DB-generated and **out of this cycle's scope to edit**; only the spec's claims *about* it are in scope.
- **D12 — the live-test count and one live-test name are stale.** "14" is asserted at fourteen sites across the spec (Status line, rev-3 R2/R3/R4 breadcrumbs, Slice-4 checklist, Decision 13 twice, the Implementation-plan row, the Test-plan subsection header, the `tests/orders/test_inputs.py` cross-reference, the closing "All 14 new live HTTP tests" sentence, the KANBAN body, the CHANGELOG bullet, and DoD 15). At HEAD `test_library_api.py`'s spec-028 section (the banner at line 1738 through the `spec-029` banner at 2482) carries **16** test functions: the 14 plus **two** post-ship additions the spec names nowhere — `test_library_branches_order_by_scalar_then_to_many_aggregate_no_multiplication` (a live mixed scalar-plus-to-many-aggregate GROUP BY case) and `test_library_genres_connection_pages_by_to_many_aggregate` (the connection-paging half of the same contract). **Worker 0 first measured this population as 15 with `grep -cE '^def test_.*order'` and that vocabulary was wrong** — the second addition carries no `order` token in its name. Worker 1 must count by the section's line range, not by a name pattern, and must state whether it is counting test *functions* or test *rows*: `test_library_books_order_by_subtitle_null_positioning` is parametrized over four directions, so the row count exceeds the function count. Separately, `test_library_books_order_by_subtitle_desc_nulls_last` has **0** occurrences; the NULLS-positioning contract ships parametrized as `test_library_books_order_by_subtitle_null_positioning` (four directions, `DESC_NULLS_LAST` among them), and a second unnamed nullable-subtitle contract test exists at `test_library_choice_enum_and_nullable_subtitle_are_deliberate_http_contracts`.
- **D13 — four spec-named package permission tests do not exist; their contracts moved to the shared substrate.** `test_orderset_check_permission_active_relatedorder_branch_fires_parent_gate`, `..._fires_child_gate`, `test_orderset_check_permission_quiet_for_inactive_field`, and `test_orderset_check_permission_denies_for_active_field` each return **0** occurrences repo-wide. The double-dispatch-plus-dedup contract is pinned once, family-neutrally, at `tests/utils/test_permissions.py::test_run_active_input_permission_checks_double_dispatch_and_dedup`; the family wiring is pinned by `tests/test_sets_mixins.py` (five tests, including `::test_permission_facade_methods_are_single_sourced_on_the_mixin`); the order-side residue is `tests/orders/test_sets.py::test_orderset_check_permission_dedups_repeated_list_entries`, `::test_orderset_inactive_input_does_not_resolve_lazy_related_target`, and four `_active_permission_field_paths` rows. All three live gate tests exist. Also `test_registry_clear_works_without_orders_imported` lives in `tests/orders/test_inputs.py`, not `tests/orders/test_finalizer.py` where the Test plan places it. Worker 1 re-derives the mapping and names the tests that exist.
- **D14 — `ORDER_BY_ARG = "orderBy"` was never shipped.** Decision 5's justification bullet ("the module-level constant in `factories.py` is `ORDER_BY_ARG = \"orderBy\"`") and the Borrowing-posture strawberry-django bullet ("Constant name in the package: `ORDER_BY_ARG = \"orderBy\"`") both assert it. `git log --oneline -S'ORDER_BY_ARG' --all` hits only the two spec-draft commits (`649a813a`, `c8be7ec9`) and checkpoint refs — the constant has never existed in any `.py`. Nothing needs it: Strawberry derives `orderBy` from the resolver's `order_by` parameter. **Worker 0's recommendation, Worker 1's call:** correct the spec rather than ship a dead constant, which is the same YAGNI judgement Decision 2 already records for the dropped `apply()` dispatcher and Decision 12 for Layer 6.
- **D15 — fakeshop's order graph grew past the spec's five ordersets and six wirings.** `examples/fakeshop/apps/library/orders.py` ships **seven** ordersets — the five named plus `PeriodicalOrder` and `IssueOrder`, whose docstring identifies them as the keyset-cursor `orderBy:` substrate ("a root `orderBy: {title: ASC}` page mints value cursors fingerprinted to THAT order"). `schema.py` carries **eight** `Meta.orderset_class` wirings (`Loan`, `Book`, `Shelf`, `Genre`, `Branch`, `Patron`, `Issue`, `Periodical`) against DoD 14's six. `orders_genre.py::GenreOrder` additionally declares `books = RelatedOrder("apps.library.orders.BookOrder")`, a second absolute-import-path form; `BookOrder` declares `loans` and `ShelfOrder` declares `books`, neither named by the spec. Six root resolvers carry `order_input_type(...)`, matching. Slice 4 and DoD 13-14 should state the shipped shape.
- **D16 — stale, superseded, or self-contradicting claims scattered through the tail sections.**
  - **Slice 6 test count contradicts itself three ways.** The Slice-6 checklist says "One package-internal test under `tests/orders/test_composition.py`"; the Implementation-plan row 6 says `New tests = 1`; the Status line says "two test functions per Slice 6 plan-licensed split". HEAD has two: `test_filter_and_order_compose_through_finalizer_and_apply_pipelines` and `test_filter_and_order_share_lazy_related_class_mixin_via_neutral_module`.
  - **Three Key-glossary-references bullets still read `planned for 0.0.8`** as present fact — `OrderSet`, `RelatedOrder`, and `Meta.orderset_class` — contradicting the header's shipped-state model. (The other eleven `planned for 0.0.8` occurrences sit in the Predecessors line, the Pre-implementation baseline, and Slice-5 / Doc-updates *flip-this* instructions, where the phrasing is correct.)
  - **The pre-archive path `docs/spec-028-orders-0_0_8.md` appears 12 times across 9 lines** (Decision 1's body, rev-5's no-rename bullet, the Slice-5 KANBAN bullet, the KANBAN past-tense body twice, the CHANGELOG bullet, the Risks terms-CSV bullet, DoD 1, DoD 17 twice, DoD 22). The file is at `docs/SPECS/spec-028-orders-0_0_8.md`; DoD 17's quoted `check_spec_glossary` command names the old path and would fail as written.
  - **Two raw intra-document line-range references survive** in the rev-3 R-bullets and in Decision 3's `"__all__"` paragraph, and Slices 1 and 3 will move every line under them.
  - **The `docs/TREE.md` Doc-updates bullet names a section that no longer exists** — "Update the 'Test layout going forward' section's `tests/orders/` enumeration". `docs/TREE.md`'s headings are `## Test layout` and `### Target test shape`.
  - **The Pre-implementation-baseline `docs/TREE.md` bullet** says the target-layout section "already names the directory and the five planned files"; at HEAD `orders/` sits in the current-on-disk block with four described files (`build_tree_md.py` omits `__init__.py`).
  - **Decision 10's closing sentence** ("Release-heading promotion … had not happened as of this spec's writing") is a dated claim about `CHANGELOG.md` state that a reader will read as current. `CHANGELOG.md` is out of this cycle's scope to edit; the claim is in scope to date or drop.
  - **The Test-plan preamble says "Tests live in two trees"** and the `tests/orders/` subsection says "Five files mirror the source layout", then lists five and adds `test_finalizer.py` and `test_composition.py` in separate blocks — while Decision 2, Decision 13, and DoD 11 all say seven. Seven is correct at HEAD.
  - **`[fakeshop-test-library-reload]` and `[fakeshop-test-library]` resolve to the same path**, and the Test plan cites `test_library_api.py::_reload_project_schema_for_acceptance_tests` as the fixture's home; the fixture is defined at `examples/fakeshop/test_query/conftest.py`. (Same defect the 027 cycle found in its own spec — verify before repairing.)

Worker 1 owns whether each of D1-D16 is a spec edit, a rationale entry, or both. The one rule Worker 0 fixes: **no explanation of any change may appear in the spec.** Corrected text states the contract as though it had always been right; the what / why / when goes in `docs/SPECS/appx/spec-028-orders-0_0_8-rationale.md`.

### Code work verified as needed (Slice 2 only)

Six defect sites across `.py` files. All are comment / docstring text; none changes an executable statement.

- **C1 — 2 occurrences of the review-item id `(spec-028 N3)`.** `django_strawberry_framework/types/base.py` #"(spec-028 N3)" and `tests/types/test_base.py` #"(spec-028 N3)". `N3` is rev-1 review Nit 3; a review-item id is unresolvable to any future reader, and after Slice 1 moves the revision history it is unresolvable from the spec at all. Measured as occurrences: `grep -roh --include='*.py' '(spec-028 N3)' . | wc -l` → 2. Replace with the invariant (the cycle-safe local `OrderSet` import) or a durable `spec-028 Decision 7` / DoD-item pointer.
- **C2 — 2 raw spec-line-number citations, both ALREADY rotted.** `django_strawberry_framework/orders/inputs.py` #"per spec-028 Decision 3 line 452" — spec line 452 lands inside Decision 2's `Justification:` list, not Decision 3. `tests/orders/test_inputs.py` #"spec-028 Decision 5 lines 525-532" — lines 525-532 land inside Decision 4's `NOT in the parity floor` list, not Decision 5's enum block. `AGENTS.md` rule 27 permits raw `path:NN` only in per-cycle scratchpad artifacts, "never in code comments specs or standing docs", and Slices 1 and 3 renumber the whole spec.
- **C3 — 9 `Test <N>` ordinal citations into a spec Test plan that has no numbered list.** `examples/fakeshop/apps/library/orders.py` 7 lines (`Test 9` ×2, `Test 10` ×2, `Test 11` ×3, `Tests 3, 7, 8, and 12`) and `examples/fakeshop/test_query/test_library_api.py` 2 lines (`Test 9`, `Test 1`). The spec's `### examples/fakeshop/test_query/test_library_api.py (extend)` section is a bulleted list, so no ordinal resolves; D12 also shows the population is now 15, so any ordinal a reader counted out would be wrong. Retarget each to the test's actual name.
- **C4 — 1 wrong-layer citation.** `django_strawberry_framework/orders/base.py` #"Layer 2 of the spec-028 six-layer plan" — Decision 3 puts `RelatedOrder`'s lazy class references at **Layer 1** and the shared module-fallback resolution at Layer 2. The module ships `RelatedOrder`, so the citation names the wrong layer. (`orders/factories.py` #"Layer 5 of the spec-028 six-layer pipeline" is correct and stays.)
- **C5 — 1 stale count banner.** `examples/fakeshop/test_query/test_library_api.py` #"(spec-028 - 14 acceptance tests)" — the section the banner heads (lines 1738-2481) carries **16** test functions at HEAD, one of them parametrized over four directions (D12). Re-measure by line range before writing a number; the name-pattern grep that produced Worker 0's first reading of 15 missed a test whose name carries no `order` token.
- **C6 — 1 docstring describing code that no longer exists.** `tests/test_registry.py::test_clear_tolerates_unimportable_order_submodules` opens "Both order-side `except ImportError` guards in `clear()` are best-effort" and states the co-clear "uses cycle-safe local imports per spec-028 Decision 9". Per D3 `TypeRegistry.clear` has no `except ImportError` at HEAD; it replays already-resolved callables from `iter_subsystem_clears()`. The docstring must describe what the test now proves. **Worker 3 additionally judges whether the test still pins anything at all** — poisoning `sys.modules` cannot reach a callback whose function object was captured at import time — and records the judgement rather than silently accepting or silently deleting; a boundary-less test is a finding for the maintainer, not a worker's deletion call.

#### Census correction, recorded 2026-08-20 after Slice 2's planning pass

Worker 1's planning pass re-measured every class against the post-Slice-1 tree and the input contract above **undercounted two classes and missed two more**. The corrected census is the one in `docs/builder/bld-slice-2-028-citation_and_provenance_rot.md` `### Census correction`; Worker 0 records the deltas here so this plan does not read as the authority it no longer is:

- **C2 is 5 occurrences over 4 lines, not 2.** Two extra sites say only `Spec line`, with no `spec-028` token, so the anchored grep Worker 0 used could not see them — and **one of those is wrapped across two source lines** (`per spec line` / newline / `1039)`), invisible to any single-line pattern whatsoever. One of the extra sites is provably *false* rather than merely unresolvable: it claims the spec names `name: DESC` where the spec says `city: DESC`, so its repair is a deletion. This is the same defect shape the plan's `### Standing hazard` names, arriving in the opposite direction — a citation a move would break, versus a citation a grep cannot find.
- **C3 is 10 occurrences over 9 lines, not 9** (one line carries two), and one of the ordinal claims is substantively wrong: `Tests 3, 7, 8, and 12` asserts four live tests use the nested `shelf:` order input; `grep '{ shelf: {'` proves it is **two**. The repair therefore corrects a count as well as retargeting a citation.
- **C7 (new) — 8 raw `path:NN` citations in `tests/orders/test_inputs.py`.** Three point **past the 388-line EOF** of the file they cite, the bare `inputs.py` basename they use resolves to six files in this repo, and the expressions they quote have since moved into `utils/` or been reshaped. Replacing them with `path::Symbol` makes them gate-enforced, since `scripts/check_citations.py` is fail-closed on first-party `.py` refs.
- **C8 (new) — 1 malformed and wrong-target citation.** `examples/fakeshop/apps/library/orders.py` #"(spec-028 + Decision 11)" — Decision 11 is the consumer helper; the sentence is about Decision 3's Layer 2 lazy resolution.

Every new site lies inside the eight paths already authorized below, so **the ownership partition is unchanged** and no correction to it is owed. `django_strawberry_framework/orders/factories.py`'s `Layer 5` citation was verified correct and is explicitly excluded.

Slice 2's writable set is exactly: `django_strawberry_framework/types/base.py`, `django_strawberry_framework/orders/base.py`, `django_strawberry_framework/orders/inputs.py`, `tests/types/test_base.py`, `tests/orders/test_inputs.py`, `tests/test_registry.py`, `examples/fakeshop/apps/library/orders.py`, `examples/fakeshop/test_query/test_library_api.py`. Two of those eight are baseline-dirty from the 027 cycle (see the baseline-dirty list); edits are additive to named lines and no 027 hunk is reverted.

**Not a code defect — verified and rejected:** `examples/fakeshop/test_query/test_glossary_api.py` uses the pre-archive string `"docs/spec-028-orders-0_0_8.md"` at three sites, but they are self-consistent *fixture data* (the test creates a `GlossarySpecMention` with that `spec_path` and then queries for it), not a documentary reference to the spec's location. Changing it buys nothing and risks the DB-backed glossary suite. Left alone deliberately.

### Partition correction 1, 2026-08-20 — Slice 2 extended after the review pass

Worker 3's review closed 0 High and 2 Medium, neither a defect in the diff: both say the **population** of a repaired class is larger than the plan scoped. Worker 0 re-verified both against source before authorizing anything, and the readings differ from Worker 3's in one direction each — so the extension is written against Worker 0's measurements, not the review's.

- **M1 — raw line-number citations in `tests/orders/`: 10 occurrences over 10 lines** (`grep -rnE --include='*.py' '\b(lines?|L)[ ]?[0-9]{2,4}\b' tests/orders/`), not the 12 the review reported and not the 8 the plan scoped. Nine sit in `tests/orders/test_sets.py` (6) and `tests/orders/test_factories.py` (3); the tenth is `tests/orders/test_composition.py` #"``AGENTS.md`` line 8 carve-out", which cites a **standing doc's** line, not a source file's. **Five are actively wrong, not merely un-navigable**: `test_sets.py` cites `line 526`, `532`, `535`, `571`, and `579` inside `django_strawberry_framework/orders/sets.py`, which is **496 lines long** — every one points past EOF. The remaining four cite the upstream cookbook by line, which is unversioned and outside this repo.
- **M2 — bare `Spec Decision N` / `Spec DoD N` citations: 16 occurrences**, of which **15 mean spec-028** (`grep -rnE --include='*.py' 'Spec (Decision|DoD) [0-9]'`; the sixteenth, in `tests/types/test_relay_interfaces.py`, names `spec-015` explicitly and is correct). Not the 11 the review reported. Per file: `orders/inputs.py` 6, `orders/sets.py` 5, `utils/inputs.py` 2, `orders/__init__.py` 1, `orders/factories.py` 1, `tests/orders/test_composition.py` 1. **They carry no `spec-028` token, so all three durable-form censuses are blind to them** — `spec-028` 67, `spec-028 Decision N` 38, `spec-028 test plan` 6 all miss every one. That makes them a genuine **precondition for Slice 3**, which is about to rewrite every `### Decision N` heading's prose: a heading rename would break fifteen citations with no gate watching and no census showing the loss.

Both classes are the **partial claim fix** this cycle exists to close: Slice 2 repaired 8 of 18 raw-line-ref sites in this card's own test tree and 0 of 15 unqualified Decision refs. `AGENTS.md` forbids defer-the-real-fix sequencing, so the partition is extended rather than the residue catalogued.

**Slice 2's writable set gains five paths** (single cohort throughout, so nothing serializes against it):

9. `django_strawberry_framework/orders/sets.py`
10. `django_strawberry_framework/orders/factories.py` — **for M2 only.** Its `Layer 5 of the spec-028 six-layer pipeline` citation was verified correct by three passes and stays byte-identical.
11. `django_strawberry_framework/orders/__init__.py`
12. `django_strawberry_framework/utils/inputs.py`
13. `tests/orders/test_composition.py`

plus `tests/orders/test_sets.py` and `tests/orders/test_factories.py`. `django_strawberry_framework/utils/inputs.py` is shared substrate serving both set families, so its two sites are qualified rather than re-attributed — the sentence must stay true for the filter family too.

The three Low findings (L1's over-broad best-effort clause, L2's build-report overstatement about the banner naming both additions, L3's closed helper-trigger misquote) and Worker 3's DRY existence challenge over triple-pinned single-sited contracts are Worker 1's to disposition at final verification.

### Partition correction 2, 2026-08-20 — Slice 2 is SIXTEEN paths, and correction 1's own numbers were wrong

Worker 1's final verification set `Status: revision-needed`, re-measured both Medium populations, and **corrected Worker 0's authorization in three places**. Recorded here because an ownership partition the plan does not carry is not a partition, and because the corrections are the cycle's own standing lesson landing on the dispatcher:

- **M1 is 23 sites in `tests/orders/`, not 10 and not 12** — Worker 0 and Worker 3 had each measured a *disjoint* class and reported it as the total. Worker 0's 10 was the prose `line NN` class (real count 11); Worker 3's 12 was the `path:NN` class. Worker 0's per-file split was also wrong (`test_sets.py` carries 7 prose sites, not 6), the `tests/orders/test_composition.py` #"``AGENTS.md`` line 8" site is an eleventh the `[0-9]{2,4}` pattern structurally cannot match rather than "the tenth", only **2** sites cite the cookbook rather than 4, and the past-EOF count is **9**, not 5. The coordinate rot is also the smaller half of the defect: **all 12 `path:NN` refs point at wrong content**, because the guards they cite moved into `utils/{inputs,permissions,input_values}.py` under the D5 squeeze, `orders/base.py:82` is a blank line, and three assert a sync/async split that `_apply_orderings` collapsed.
- **M2 is 17 occurrences / 16 in-family, not 16 / 15.** Worker 3's count was right and **Worker 0's single-line "correction" missed a wrapped site** — `django_strawberry_framework/orders/sets.py` #"(Spec Decision" continues `8 step 6` on the next line. That is the **third** time in this cycle a citation population was under-measured by exactly its wrapped-site count, and the first time it happened inside the authorization itself. The standing rule now has a dispatcher-facing corollary: **any citation census Worker 0 states must be measured on a whitespace-flattened probe before it is written down**, never on a single-line grep.
- **The writable set is SIXTEEN paths, not the "thirteen" the final-verification section labels it.** Worker 1's own enumeration is right and its label is a miscount: the original 8 + the 5 Worker 0 authorized + `tests/orders/test_sets.py` + `tests/orders/test_factories.py` + `tests/orders/test_base.py` = **16**. Worker 0 verified all sixteen exist on disk. The count matters because a builder handed a wrong total may stop mid-flight when it finds the fourteenth file it needs.

**Slice 2's authorized writable set, enumerated (16 paths, all verified present):** `django_strawberry_framework/types/base.py`, `django_strawberry_framework/orders/base.py`, `django_strawberry_framework/orders/inputs.py`, `django_strawberry_framework/orders/sets.py`, `django_strawberry_framework/orders/factories.py` (**C12/M2 only** — its `Layer 5` citation stays byte-identical), `django_strawberry_framework/orders/__init__.py`, `django_strawberry_framework/utils/inputs.py`, `tests/types/test_base.py`, `tests/orders/test_inputs.py`, `tests/orders/test_sets.py`, `tests/orders/test_factories.py`, `tests/orders/test_base.py`, `tests/orders/test_composition.py`, `tests/test_registry.py`, `examples/fakeshop/apps/library/orders.py`, `examples/fakeshop/test_query/test_library_api.py`.

Six new or re-opened checklist boxes (C5 un-ticked as an over-tick, plus C6c, C9, C10, C11, C12) are specified as steps 19-24 in the artifact. Worker 1's other rulings: the ninth finding becomes **C9** and lands here because Slice 3 cannot edit source; **L1 widens** to narrow the non-operative best-effort clause in *both* registry twins (Worker 1 verified the filter twin is identically non-operative, so accuracy and the paired-repair constraint are not in tension) while the connection and relay twins stay fenced to `spec-030` / `spec-032`; **L3 confirmed closed**; and the **DRY existence challenge is deferred** — every resolution changes executable statements, which would forfeit the zero-boundary entitlement the slice's whole verification rests on, and both halves sit outside the writable set. `graphql_camel_name`'s unpinned empty-core branch is flagged as a card in its own right for `bld-final-028.md`'s `### Deferred work catalog`.

### Integration-pass consolidation cohort, 2026-08-20

The integration pass set `Status: revision-needed` on one Medium it may not fix itself (it does not write `.py`). Worker 0 verified it against source before dispatch.

**The finding.** `tests/orders/test_inputs.py` cites the same spec Decision two ways, and one citation resolves to nothing. Line 62 reads `"""All six members from spec-028 Decision 5 are present."""`; line 74, twelve lines below in a sibling test, reads `"""Members carry string values matching their names (Decision 5)."""`; line 681 reads `# Module global is left parked (parking is load-bearing per Decision 9).` in a function carrying no `spec-028` anchor at all. Verified: the file's only `spec-028` tokens are at lines 62 and 516, and 516 is 165 lines away on a different topic, so **line 681 is not anaphora — it is unresolvable**, and line 74's bare form is inconsistent with its own sibling. That is 2 of 18 sites of **C12's own class, inside C12's own file** — the partial claim fix this cycle exists to close.

Slice 2's C2 step 5 rationale actually *named* the line-74 neighbour and normalized on the wrong axis: it made the pair consistent in dropping the raw line number and left them inconsistent in carrying the spec id. **The DRY deferral does not shelter this.** Unlike the deferred existence challenge, this is docstring text, so it costs the zero-boundary entitlement nothing — which is the whole reason the deferral was granted there and is refused here.

**Decided, not skipped:** `django_strawberry_framework/utils/inputs.py:1301`'s bare `Decision 9` is genuine anaphora four lines under the full form and stays byte-identical.

**Ownership partition (single cohort, declared):** `tests/orders/test_inputs.py`. No other cohort runs concurrently, and the concurrent `spec-027` cycle has fully committed (HEAD `5447c9eb`), so this is the first pass of this cycle with a quiet tree.

**Artifact:** `docs/builder/bld-slice-4-028-decision_citation_consistency.md`.

**Where the regress stops — Worker 0's definitive measurement, 2026-08-20.** The cohort's review escalated one further site (`tests/orders/test_finalizer.py` #"Decision 6 -- first-bind model compatibility", a section banner 320 lines from its nearest `spec-028` token) as structurally the same shape as the 165-line site just repaired, and warned that "2 of 18" invites the wrong conclusion at family scope. Worker 0 measured the whole population before authorizing anything further, keyed on **anchor presence** rather than distance:

- **Unprefixed `Decision N` / `DoD N` occurrences tree-wide in `.py`: 442.** A bare `Decision N` inside a module whose docstring already names its spec is the **repo-wide convention**, not a defect. Qualifying the orders family further would diverge it from every other subsystem — the opposite of the consistency this cohort was dispatched for.
- **Occurrences in a file carrying NO `spec-NNN` anchor anywhere: 16, and none of them is in the orders family.** They sit in `permissions.py` (7), `management/commands/inspect_django_type.py`, `optimizer/hints.py`, `tests/optimizer/test_selections.py`, `tests/test_apps.py` (3), and `examples/fakeshop/apps/products/filters.py` (2) — all other cards' surfaces.
- **The orders family is at 2 unprefixed occurrences, both in `tests/orders/test_finalizer.py`, and that file carries 3 `spec-028` anchors.** So they are in-file anaphora, merely distant — the same species as `utils/inputs.py:1301`, which this cohort explicitly decided to leave. Every other file in the family reads **0 unprefixed against 2-20 anchors**.

**Disposition: deferred, with the measurement as the reason so it cannot be re-fought.** The distance-based reading makes every long file look defective; the anchor-based reading shows the family is clean and the residue is a repo-wide convention plus 16 sites belonging to other cards. Catalogued for the final gate, not looped.

#### The integration pass's own findings, for the final gate to carry

- **Slice 3's protect-list was under-measured 3.3x, by CASE.** All three of its instruments were case-sensitive and there are **15** `Spec-028` (capital-S) occurrences in `.py` — the case-insensitive census is **106**, not 91. `## Test plan` carries **20** dependents, not 6; the heading-bearing total is **85**, not 71. Worker 0 confirmed both numbers. The spelling is a coherent positional convention (sentence-initial only: 0 mid-sentence capitals, 0 lowercase docstring-openers), so the **voice is fine and only the instrument failed** — no repair is owed, but the `check_citations.py` gate-extension proposal gains a fourth clause. This is the **tenth** under-measurement of the cycle, through a door none of the nine before it used.
- **The code-side citation question closes clean.** A resolver over all **106** `spec-028`-family citations in `.py` found **0 unresolved** against the rewritten spec, on three provably-different instruments (line-scoped `[ \t]` = 106, flattened = 106, join-aware with `\n` inside the pattern = 0). All 13 `### Decision N` headings, `## Test plan`, `## Definition of done` with item 4's `(a)`-`(e)` sub-lettering, and `## Edge cases and constraints` are present; both `spec-028 DoD 4(c)` refs resolve. **This pass is the only gate that exists for that class** — `check_citations.py` resolves `path::Symbol` only and `docs/` is outside its scope.
- **Worker 0's committed/dirty split was wrong in shape, not only in digits.** 11 of the sixteen have hunks in `5c6fdd71..HEAD`, 7 are dirty, and **2 are MIXED** — `tests/test_registry.py` and `examples/fakeshop/test_query/test_library_api.py` carry earlier passes committed and later passes dirty, so **neither diff reading alone shows Slice 2's work in them.** Read the current file for any present-state claim.
- **Staged anchors: 0 in source or tests, but 8 tree-wide rather than 4.** Two are `TODO-ALPHA-028-0.0.11` rows in `spec-025-…-terms.csv` — a **pre-renumber** id that shipped as `DONE-037`, so the card-id form false-positives whenever a renumber recycles a number. Out of scope (not a `.py`, and another card's CSV), catalogued.
- **`review_inspect.py` obligation is vacuous by entitlement**, not satisfied by sampling: zero review-worthy logic changed anywhere in the cycle. All sixteen files carry a run or a recorded skip.
- **The shadow `Repeated string literals` section is truncated** (`226 more not shown`), so the comparison `BUILD.md` calls essential here cannot be performed from the overview alone. Recomputed from AST: **56** cross-file literals, not the 17 shown. All are field names, `Meta` keys, wire aliases, or fixtures; none new, none a consolidation candidate.
- **Imports: one-way direction holds.** `utils/inputs.py` imports nothing from `orders/` or `filters/`; `orders/inputs.py → .sets` is `TYPE_CHECKING`-only so the apparent cycle is not one at runtime; `types/base.py → orders/sets` is in-function only.
- **Deferred-work consolidation: 25 items, 20 transcribable as written, 5 flagged as needing restatement** before the final gate can carry them (S2-2 needs the case clause; S2-7's inventory is stale in both directions and 2 of its 14 hits are not citations; S2-11 names 3 of 54 tree-wide review-item ids, 2 of them wrapped; S2-12 is now a closed live event rather than a risk; S2-13 needs the 3-commits / 11-paths / 2-mixed shape). Plus one item no slice named: `docs/GLOSSARY.md`'s `## RelatedOrder` entry narrates its own history ("not `filters.base` as named in earlier revisions") — a DB-generated file, so out of this cycle's scope to edit and catalogued for the maintainer.
- The pass recorded **two of its own instruments as unfalsifiable**: an `endswith("")` test that is True for every string, which made its capitalization check a constant, and a `grep -c` line count that inflated 9 rationale subsections to 10 and nearly produced a false count defect against Slice 3. Both self-caught.

## Artifact list

- `docs/builder/bld-slice-1-028-rationale_extraction.md`
- `docs/builder/bld-slice-2-028-citation_and_provenance_rot.md`
- `docs/builder/bld-slice-3-028-spec_reconciliation.md`
- `docs/builder/bld-slice-4-028-decision_citation_consistency.md`
- `docs/builder/bld-integration-028.md`
- `docs/builder/bld-final-028.md`

## Checklist

- [x] Slice 1: Rationale extraction — create `docs/SPECS/appx/spec-028-orders-0_0_8-rationale.md` and MOVE the spec's deliberative layer into it (the whole `Revision 1`-`Revision 7` history, all 13 `Justification:` / `Alternatives considered (and rejected):` blocks, and every inline review-round narration), keyed to the spec decision each entry belongs to -> `docs/builder/bld-slice-1-028-rationale_extraction.md`
- [x] Slice 2: Code correction — retire C1's 2 `(spec-028 N3)` provenance refs, C2's 5 raw spec-line-number refs (one wrapped across two lines), C3's 10 `Test <N>` ordinal citations plus the wrong four-vs-two count inside one of them, C4's wrong-layer citation, C5's stale 14-test banner, C6's false `except ImportError` docstring, C7's 8 raw `path:NN` refs, and C8's malformed wrong-target citation; `.py` files only -> `docs/builder/bld-slice-2-028-citation_and_provenance_rot.md`
- [x] Slice 3: Spec reconciliation — rewrite the spec so it reads as the current contract, resolving D1-D16, every change recorded in the rationale -> `docs/builder/bld-slice-3-028-spec_reconciliation.md`
- [x] Cross-slice integration pass -> `docs/builder/bld-integration-028.md`
- [x] Integration consolidation loop (Worker 2 + Worker 3) - normalize the two bare `Decision N` citations in `tests/orders/test_inputs.py` that the integration pass surfaced -> `docs/builder/bld-slice-4-028-decision_citation_consistency.md`
- [x] Final gate -> `docs/builder/bld-final-028.md`

### Standing hazard for Slices 1 and 3 (carried from the 027 cycle)

A slice that MOVES spec text breaks every `path #"substring"` citation aimed into the moved region, and **no gate sees it**: `scripts/check_citations.py` resolves `path::Symbol` refs only, and `docs/` is out of its scope. Worker 0 measured the entry-state population: **0** `spec-028 #"…"` substring citations exist in `.py`, so the move has no substring cohort to break today. But the 68 `spec-028` occurrences across `django_strawberry_framework/` + `tests/` + `examples/` include 37 `spec-028 Decision N` references and 5 `spec-028 test plan` references, which survive a move only if Slice 1 leaves every `### Decision N` heading and the `## Test plan` heading in place, and Slice 3 leaves them addressable. Slice 1 and Slice 3 each re-run the sweep as a **postcondition**, not only a precondition, and the integration pass re-runs it across the whole cycle's diff.
