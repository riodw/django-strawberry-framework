# Package build plan: form_mutations / 0.0.12 (038)

Spec source: `docs/SPECS/spec-038-form_mutations-0_0_12.md`
Target release: `0.0.12`
Date created: 2026-06-23
Build rule: one slice at a time. Plan first (Worker 1), build second (Worker 2), review third (Worker 3), reconcile fourth (Worker 1 final verification). Do not start the next slice until the current slice's artifact reaches `final-accepted`.
DRY rule: every plan, every implementation, every review pass answers first — is this the maximally DRY shape that stays readable? Worker 1 plans for DRY; Worker 3 enforces DRY; Worker 1 re-checks DRY across slices at the integration pass.

Pre-flight: passed on 2026-06-23; baseline: one unrelated dirty file (`docs/feedback.md`, out of scope — see below); cleanup: old artifacts removed (build-037 + 6 bld-*.md from the 037 cycle), `worker-memory/` cleared and re-seeded with four empty files, `docs/shadow/` cleared (held only the pre-flight smoke-test output), `docs/builder/temp-tests/` empty. Checks: `git status --short` reviewed; `scripts/review_inspect.py django_strawberry_framework/mutations/fields.py --output-dir docs/shadow --stdout` exits 0; `.gitignore` lists `docs/builder/worker-memory/`, `docs/shadow/`, `docs/builder/temp-tests/`; `scripts/check_spec_glossary.py --spec docs/SPECS/spec-038-form_mutations-0_0_12.md` reports `OK: 31 terms` (exit 0).

## Baseline-dirty out-of-scope files (workers do NOT edit, do NOT revert)

- `docs/feedback.md` — one-line edit pre-existing at build start (renames a `resolvers.py` symbol reference `run_pipeline_async` → `resolve_mutation_async` in a feedback doc). Unrelated to form mutations; presumptively the maintainer's in-progress work (AGENTS.md). No build slice touches this file. Leave it exactly as found.
- `examples/fakeshop/db.sqlite3` — **CONCURRENT-WRITER, out of scope (discovered during Slice 1).** Clean at pre-flight, but a concurrent process (maintainer board work or a watcher) intermittently rewrites it with **semantic kanban data**: card #38's FK columns changed, and `kanban_boarddoccardreference` / `kanban_uuidmodel` rows for cards 027 / 028 differ vs HEAD (confirmed by SQL-dump diff; hash changed between two read-only diagnostic calls). Per AGENTS.md rule 34 (concurrent mods are presumptively another dev's in-progress work — never auto-revert), **Worker 0 and all workers leave db.sqlite3 in whatever state the concurrent writer leaves it** and do NOT `git checkout` it for Slices 1–3. (Worker 0 reverted it twice early in Slice 1 before recognizing the pattern; the external process re-applied each time, so no permanent harm.) Slices 4 (FileField migration) and 5 (kanban card move) DO make intentional db.sqlite3 changes and must coordinate with the concurrent state — see the Slice 5 coordination flag below.

## Build-wide context flags

- **Version-bump owner = THIS card (Slice 5).** `038` is the lone `0.0.12` card (`039` / `040` target `0.0.13`); there is no joint cut to defer to. Slice 5 aligns the version quintet — `pyproject.toml`, `__version__` in `__init__.py`, `tests/base/test_init.py::test_version`, the `docs/GLOSSARY.md` package-version line, and `uv.lock` if it carries the package version — from `0.0.11` to `0.0.12` (spec Decision 14). The bump moves only in Slice 5, never earlier.
- **CHANGELOG.md edit — AUTHORIZED (maintainer, 2026-06-23).** AGENTS.md #"Do not update CHANGELOG.md unless explicitly instructed" reserves CHANGELOG edits for an explicit maintainer instruction. At the Slice-5 coordination pause the maintainer **explicitly authorized** adding the `[Unreleased]`→`0.0.12` form-mutations entry to `CHANGELOG.md` per the spec's Doc-updates section. Slice 5 DOES edit `CHANGELOG.md`; Worker 3 runs the CHANGELOG sanity check.
- **Slice-5 DB writes — apply on top of concurrent work, DO NOT undo it (maintainer, 2026-06-23).** The maintainer directed: run Slice-5's DB-backed steps (KANBAN `TODO-ALPHA-038`→`DONE` card move; GLOSSARY symbol promotion to `shipped (0.0.12)`; GLOSSARY package-version line) against the CURRENT `examples/fakeshop/db.sqlite3` (which carries the concurrent writer's uncommitted kanban edits) WITHOUT reverting/undoing any of that concurrent work. Consequence: the regenerated `KANBAN.md`/`KANBAN.html`/`docs/GLOSSARY.md` will reflect BOTH the concurrent edits AND the card move — the maintainer reconciles at commit. **Verification adjustment:** the worker-0.md "git diff docs/GLOSSARY.md clean" check is NOT applicable (the DB legitimately diverged from HEAD via concurrent work); instead prove MY DB writes regenerate deterministically via the **two-consecutive-regenerate byte-stability** check, and confirm card 038 shows `DONE` + both glossary symbols show `shipped (0.0.12)` in the rendered output. Workers never commit — hand the regenerated docs + `db.sqlite3` to the maintainer.
- **Generated docs are DB-backed (Slice 5 card wrap + GLOSSARY).** `KANBAN.md`, `KANBAN.html`, and `docs/GLOSSARY.md` are rendered from the kanban/glossary tables in `examples/fakeshop/db.sqlite3`. Editing them means **edit the DB via the Django ORM, then regenerate** (`scripts/build_kanban_md.py` / `build_kanban_html.py` / `build_glossary_md.py`); never hand-edit the rendered markdown. The card move (`TODO-ALPHA-038-0.0.12` → `DONE-NNN-0.0.12`) follows the worker-0.md "Closing out a kanban card" DB-backed procedure (DONE-card invariants, `import_spec_terms`, byte-clean-regenerate verification).
- **Slice 5 DB-concurrency coordination (discovered Slice 1).** A concurrent process is intermittently editing `examples/fakeshop/db.sqlite3` kanban data (see baseline-dirty list above). Because Slice 5's card move + glossary promotion operate on this same DB, Worker 0 must **flag the concurrency to the maintainer before Slice 5 runs** and confirm the DB baseline is stable / theirs to reconcile. The worker-0.md verification step (`git diff docs/GLOSSARY.md` clean after regenerate; byte-stable across two regenerates) may surface drift introduced by the concurrent writer — that is a maintainer-coordination point, not a Slice-5 build defect.
- **Spec path discrepancy (Worker 1 reconciliation candidate).** The active spec lives at `docs/SPECS/spec-038-form_mutations-0_0_12.md` (its reference-style link defs resolve from `docs/SPECS/`, confirming that home), but the spec prose self-references `docs/spec-038-form_mutations-0_0_12.md` (in `docs/`, not `docs/SPECS/`) in Decision 1 and Definition-of-done item 1, and the DoD spec-glossary command names that `docs/` path. This is a spec-internal inconsistency. It is NOT a blocker (the real file is the `docs/SPECS/` one and all plans/builds/reviews use that path). Worker 1 may reconcile the spec self-references during a slice that touches the spec, or record it as a maintainer/next-author follow-up. Workers other than Worker 1 must not edit the spec.
- **036 surface generalization is a real, named delta — not a no-op reuse.** Slices 2 and 3 refactor the shipped `036` mutation surface (`mutations/sets.py` `_validate_meta` / `build_input` / `input_type_name` / `input_module_path` / `resolve_*` seams; `mutations/fields.py` target-check + `_resolve` dispatch + `data:` lazy-ref derivation; `types/finalizer.py` phase-2.5 wiring; `registry.py` co-clear; the shared `make_declaration_registry` helper). Every seam must default to today's model behavior — the no-model-flavor-regression gate (spec Decision 5 / 6 / 13, Definition-of-done item 6). Staged-but-not-implemented seams follow the AGENTS.md `TODO(spec-038 Slice N)` anchor discipline, removed in the slice that ships them.
- **Coverage is the maintainer's gate.** No worker runs `pytest` with `--cov*` flags. Worker 1's final test-run gate runs `uv run pytest --no-cov` once (the explicit `--no-cov` opts out of `pytest.ini`'s auto-applied `--cov`).

## Artifact list

- `docs/builder/bld-slice-1-form_converter_inputs.md`
- `docs/builder/bld-slice-2-form_bases_meta_validation.md`
- `docs/builder/bld-slice-3-resolver_pipeline_field_exposure.md`
- `docs/builder/bld-slice-4-products_live_form_surface.md`
- `docs/builder/bld-slice-5-docs_version_cut.md` (Slice 5a — version quintet ex-GLOSSARY-line + plain docs + CHANGELOG)
- `docs/builder/bld-slice-5b-glossary_kanban_db.md` (Slice 5b — DB-backed GLOSSARY promote/correct + version-line BoardDoc + KANBAN card move + regenerate)
- `docs/builder/bld-integration.md`
- `docs/builder/bld-final.md`

## Checklist

- [x] Slice 1: form-field → Strawberry input mapping + the form-derived input generator (`forms/converter.py` + `forms/inputs.py` + `forms/__init__.py`; spec Decision 7) -> `docs/builder/bld-slice-1-form_converter_inputs.md`
- [x] Slice 2: the `DjangoFormMutation` / `DjangoModelFormMutation` bases + `Meta` validation + the phase-2.5 bind (`forms/sets.py`, `mutations/sets.py`, `types/finalizer.py`, `registry.py`, `__init__.py`; spec Decision 5 / 6) -> `docs/builder/bld-slice-2-form_bases_meta_validation.md`
- [x] Slice 3: the form resolver pipeline + `DjangoMutationField` exposure (`forms/resolvers.py`, `mutations/resolvers.py`, `mutations/fields.py`; spec Decision 8 / 9) -> `docs/builder/bld-slice-3-resolver_pipeline_field_exposure.md`
- [x] Slice 4: the products live form surface (`examples/fakeshop/apps/products/forms.py`, `products/schema.py`, `test_products_api.py`; spec Decision 12) -> `docs/builder/bld-slice-4-products_live_form_surface.md`
- [x] Slice 5a: version quintet (ex-GLOSSARY-line) + plain docs (README/docs-README/GOAL/TODAY/TREE) + CHANGELOG (maintainer-authorized) (Doc updates / spec Decision 14) -> `docs/builder/bld-slice-5-docs_version_cut.md`
- [x] Slice 5b: DB-backed GLOSSARY promote/correct (Decision 6) + GLOSSARY version-line BoardDoc + KANBAN `WIP-ALPHA-038`→`DONE-038-0.0.12` card move + regenerate all three docs (Doc updates / spec Decision 14; apply on top of concurrent DB work, do not undo) -> `docs/builder/bld-slice-5b-glossary_kanban_db.md`
  - Note: Worker 1 split Slice 5 into 5a/5b as a build-execution decision (recorded in `bld-slice-5-docs_version_cut.md` `### Spec changes made`); the spec's `## Slice checklist` keeps one Slice 5 (full contract lands across 5a+5b). The spec's CHANGELOG sub-bullet condition ("only if the maintainer prompt requests it") is now MET — CHANGELOG is in 5a.
- [x] Cross-slice integration pass -> `docs/builder/bld-integration.md`
- [x] Final test-run gate -> `docs/builder/bld-final.md`
