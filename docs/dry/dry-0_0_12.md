# DRY consolidation plan: 0.0.12

Source: `docs/builder/`
Generated: 2026-06-24
Workflow: `docs/dry/DRY.md`

One finding at a time. Worker 0 dispatches > Worker 1 triages > Worker 2 implements > Worker 1 verifies > Worker 0 ticks.

## Findings

## Build: Slice 1 — form-field → Strawberry input mapping + the form-derived input generator

_Source: `docs/builder/bld-slice-1-form_converter_inputs.md`_

**Existing patterns reused (cite file:line):**

- [x] `django_strawberry_framework/utils/inputs.py:66-100` — `build_strawberry_input_class(name, field_specs)`: the single-sited `(python_attr, annotation, field_kwargs)`-triple → `@strawberry.input` constructor. `forms/inputs.py` builds its triples and calls this, exactly as `mutations/inputs.py:536` (`build_mutation_input`) and `orders/inputs.py` do. No hand-rolled `setattr`/`type(...)` in `forms/inputs.py`.
  - **Investigation (Worker 1):** Already addressed. `forms/inputs.py:64-65` imports `build_strawberry_input_class` from `..utils.inputs`, and `build_form_input_class` builds its `(python_attr, annotation, field_kwargs)` triples and calls `input_cls = build_strawberry_input_class(type_name, triples)` at `forms/inputs.py:463`. No hand-rolled `setattr`/`type(name, (), ...)` class construction exists in the module (the lone `setattr` token at `forms/inputs.py:144` is a docstring reference to `materialize_form_input_class`'s module-global pin, not a class build). The reused helper still lives at `utils/inputs.py:112-146` (the cited `:66-100` has drifted because `normalize_field_name_sequence` was inserted ahead of it). The reuse the finding recaps is in place; no consolidation remains.
- [x] `django_strawberry_framework/utils/inputs.py:103-139` — `materialize_generated_input_class(name, cls, *, module_path, family_label, ledger)`: the module-global pin + idempotent-dedupe + distinct-class collision raise. Its ledger gives the Decision 7 finalize-time `ConfigurationError` (two distinct shapes on one name; two different form classes sharing a `__name__`) **for free** — `forms/inputs.py` wraps it as a thin `materialize_form_input_class(name, cls)` with `module_path="django_strawberry_framework.forms.inputs"`, `family_label="DjangoFormMutation"`, and a module-level `_materialized_names: dict[str, type] = {}` ledger, byte-for-byte the wrapper shape `mutations/inputs.py:106-125` (`materialize_mutation_input_class`) uses.
  - **Investigation (Worker 1):** Already addressed. `forms/inputs.py` already imports `materialize_generated_input_class` from `..utils.inputs` (`forms/inputs.py:67`) and exposes `materialize_form_input_class(name, input_cls)` (`forms/inputs.py:113-134`) as the prescribed thin wrapper: it forwards `module_path=INPUTS_MODULE_PATH`, `family_label="DjangoFormMutation"`, and `ledger=_materialized_names` (the module-level `_materialized_names: dict[str, type] = {}` at `forms/inputs.py:110`) to the shared helper, byte-for-byte mirroring `mutations/inputs.py::materialize_mutation_input_class` (`mutations/inputs.py:106-126`). No bespoke module-global pin, `(name, cls)` dedupe, or distinct-class collision check exists in `forms/inputs.py` — the Decision 7 finalize-time `ConfigurationError` (distinct shapes on one name; two form classes sharing a `__name__`) comes free from the shared ledger's raise. The shared helper still lives in `utils/inputs.py` but the cited `:103-139` has drifted to `materialize_generated_input_class` at `utils/inputs.py:149-185` (the spec-038 integration-pass `normalize_field_name_sequence` and `graphql_camel_name` were inserted ahead of it). No consolidation remains. (The "Re-deriving the collision raise" duplication-risk bullet below records the same in-place reuse.)
- [x] `django_strawberry_framework/utils/inputs.py:53-63` — `graphql_camel_name(name)`: `category_id` → `categoryId`. The form converter's relation branch and every scalar field's GraphQL alias route through this (same call site shape as `mutations/inputs.py:285`, `:492`, `:496`).
  - **Investigation (Worker 1):** Already addressed. The cited helper is live and correctly located at `utils/inputs.py:53-63`. `forms/inputs.py:66` imports `graphql_camel_name` from `..utils.inputs`, and `build_form_input_field_spec` routes every non-relation alias through it directly: the ModelForm file branch (`forms/inputs.py:376`), ModelForm scalar branch (`:381`), model-less file branch (`:391`), model-less relation branch (`:395`), and model-less scalar branch (`:398`) each call `graphql_camel_name(python_attr)`. The ModelForm relation branch (`:369-372`) defers to `relation_input_annotation(column, ...)`, which itself computes `graphql_name = graphql_camel_name(python_attr)` at `mutations/inputs.py:285` and returns it — so the relation alias also routes through the single-sited helper, just transitively. No re-spelled camelCase logic (`.title()` / `.capitalize()` / manual `_`-join) exists anywhere in `forms/inputs.py`; grep over `forms/` + `mutations/inputs.py` finds only `graphql_camel_name` call sites plus the unrelated `_pascalize_token`'s `.capitalize()` (`mutations/inputs.py:323`, the token-name scheme, not alias camel-casing). The reuse the finding recaps is in place; no consolidation remains. The mutation-side call sites the finding cites for shape parity (`mutations/inputs.py:285`, `:492`, `:496`) are all current.
- [x] `django_strawberry_framework/mutations/inputs.py:249-286` — `relation_input_annotation(field, *, related_primary_type)`: the FK/O2O → `<field>_id` + M2M → `list[<id>]` annotation strategy, **keyed on a `models.Field`** with the related model's primary `DjangoType` resolved via `registry.get(field.related_model)`. **Reused only for a `ModelForm` field that has a backing column** (Decision 7 lines 1208-1214): the converter resolves the column via `form_class._meta.model._meta.get_field(name)` and hands that `models.Field` to `relation_input_annotation`. This gives the Relay-`GlobalID`-vs-raw-pk id type and the `categoryId`/`category_id` naming identical to the read side and the `036` model-driven input — no parallel relation table.
  - **Investigation (Worker 1):** Already addressed. The reuse the finding recaps is in place, not a residual duplication. `forms/inputs.py:58` imports `relation_input_annotation` from `..mutations.inputs`, and `build_form_input_field_spec` routes every ModelForm relation column through it: at `forms/inputs.py:368-373` the backed-column branch tests `getattr(column, "is_relation", False)` and calls `relation_input_annotation(column, related_primary_type=registry.get(column.related_model))`, then sets `kind = RELATION_MULTI if column.many_to_many else RELATION_SINGLE`. The `column` is the resolved `models.Field` from `model._meta.get_field(name)` (`forms/inputs.py:311`), so the FK/O2O→`<field>_id` + M2M→`list[<id>]` strategy, the Relay-`GlobalID`-vs-raw-pk id type, and the `categoryId`/`category_id` alias are all owned single-sited by `mutations/inputs.py::relation_input_annotation` (unchanged at `:249-286`). No parallel relation annotation table exists in `forms/`: the only other relation path is `_model_less_relation_annotation(name, field)` (`forms/inputs.py:394`), which is the spec-sanctioned column-less divergence (a plain `Form` field has no Django column and so no read-side equivalent), not a copy of the column-keyed strategy. No consolidation remains.
- [x] `django_strawberry_framework/types/converters.py:272-395` — `convert_scalar(field, type_name, *, force_nullable)` / `:249-269` `scalar_for_field(field)` / `:493-570` `convert_choices_to_enum(field, type_name)`: the read-side scalar/enum registry, **keyed on `models.Field` subclasses (MRO walk), not `forms.Field`** (Decision 7 lines 1208-1213). **Reused only for a `ModelForm` field with a backing column** — resolve the column and route it through `convert_scalar` (which itself delegates to `convert_choices_to_enum` when `field.choices` is set), so a `ChoiceField` over model `choices` resolves to the **same** generated enum the read `DjangoType` synthesizes (the symmetric wire contract). `force_nullable=False` suppresses the column's own null-widening so the form generator owns required-ness via `field.required` — same tri-state use as `mutations/inputs.py:301`.
  - **Investigation (Worker 1):** Already addressed. The reuse described by this finding is in place in current source and was not a parallel table. The `ModelForm`-backed-column scalar path is `forms/inputs.py:382` — `convert_scalar(column, type_name, force_nullable=False)` — inside `_field_triple_and_spec` (`forms/inputs.py:339-399`), reached only when `_model_column_for` (`:295-313`, via `model._meta.get_field(name)`) resolves a `models.Field`; column-less fields take the `else` branch into `convert_form_field` (`:387`). `convert_scalar` (`types/converters.py:272-395`) delegates to `convert_choices_to_enum` when `field.choices` is set (`:391-392`), and `convert_choices_to_enum` (`:493-570`) caches on `(field.model, field.name)` via `registry.get_enum`/`register_enum` (`:545-547`, `:569`), so a `ChoiceField` over model `choices` resolves to the SAME enum object the read `DjangoType` synthesizes — the symmetric wire contract, no `forms/`-side scalar/enum table. The `convert_form_field` registry in `forms/converter.py:134-235` is the genuinely net-new model-less table (its own docstring lines 11-17 affirm it is NOT a parallel copy). The two key spaces (`models.Field` read-side, `forms.Field` model-less) stay strictly separated. No code change warranted.
- [x] `django_strawberry_framework/scalars.py` (`Upload`, lines ~25-38) — the `0.0.11`/spec-037 `Upload` scalar. `forms.FileField` / `forms.ImageField` map to it, identical to the `mutations/inputs.py:493` file-column branch (`annotation = Upload`).
  - **Investigation (Worker 1):** Already addressed. The reuse this finding asserts is in place in current source: `forms/inputs.py:61` imports `from ..scalars import Upload` (the same import `mutations/inputs.py:48` uses) and assigns the shared `Upload` scalar at both the model-backed file-column branch (`forms/inputs.py:377`, `annotation = Upload`) and the column-less `FILE`-kind branch (`forms/inputs.py:392`), byte-for-byte the `mutations/inputs.py:493` (`annotation = Upload`) strategy. `grep -rn "strawberry.scalar|NewType|file_uploads" django_strawberry_framework/forms/` returns nothing — no re-declared upload scalar exists in `forms/`. The finding is a correct recap of a pattern already reused, not a residual duplication; nothing to consolidate.
- [x] `django_strawberry_framework/mutations/sets.py:271-299` — `_normalize_field_sequence(value, *, label)`: the bare-string / duplicate-name fail-loud normalizer for `Meta.fields` / `Meta.exclude`. The spec (Slice 1 sub-bullet 2, lines 310-312; Decision 7 lines 1350-1356) says "mirroring `036`'s `_normalize_field_sequence`". This function is **module-private to `mutations/sets.py`** and its messages name "DjangoMutation". See "New helpers justified" below for the resolution (do NOT import the private symbol cross-package, and do NOT duplicate its body).
  - **Investigation (Worker 1):** Already addressed — and the deferred integration-pass consolidation has since landed. The form code never imports the private `036` symbol and never duplicates its body: option (c). `forms/inputs.py::normalize_form_field_sequence(value, *, label)` (`forms/inputs.py:174-195`) is the spec-mandated form-flavor near-twin, and `resolve_effective_form_fields` (`:198-223`) calls it for both `fields` and `exclude` (`:222-223`); the `form_class.base_fields` field-existence basis is checked separately in `resolve_effective_form_fields`, exactly as the spec required. Beyond what the bullet asked for, the "viable consolidation … out of scope for this slice" that #5 flagged for the integration pass has already been performed: both flavors now delegate to a single shared param-driven normalizer `utils/inputs.py::normalize_field_name_sequence(value, *, label, flavor)` (`utils/inputs.py:66`), keyed only by a `flavor` label (mirroring `make_declaration_registry`'s flavor-parameterized wording). `normalize_form_field_sequence` passes `flavor="DjangoFormMutation / DjangoModelFormMutation"` (`:191-195`); the `036` model flavor `mutations/sets.py::_normalize_field_sequence` (now at `:320-334`, drifted from the bullet's cited `:271-299`) is itself a thin shim passing `flavor="DjangoMutation"` (`:334`); both import the shared helper (`forms/inputs.py:68`, `mutations/sets.py:52`). The shared helper's docstring and `forms/inputs.py:178-180` both credit this as "spec-038 integration pass, Finding I1". The bare-string / duplicate-name `ConfigurationError` wording is preserved byte-identical per flavor via the interpolated `flavor`. No body duplication, no cross-package private import, no residual opportunity.
- [x] `django_strawberry_framework/types/relay.py:53` — `implements_relay_node(type_cls)`: used transitively inside `relation_input_annotation`; the converter never calls it directly (the reuse is through `relation_input_annotation`).
  - **Investigation (Worker 1):** Not real. The bullet's stated fact is wrong about current source — the form converter DOES call `implements_relay_node` directly: `forms/inputs.py:330` (`_model_less_relation_annotation`) does `if primary is not None and implements_relay_node(primary):`, importing it at `forms/inputs.py:63`. The model-less path's docstring (`:319-326`) explains why it cannot route through `relation_input_annotation` (that helper is `models.Field`-keyed and a plain-`Form` `ModelChoiceField` has no column), so it re-derives the Relay-`GlobalID`-vs-raw-pk id type inline. But this is not a consolidation opportunity: `implements_relay_node` is ALREADY the one shared one-line helper (`types/relay.py:53`, `return issubclass(type_cls, relay.Node)`) — every site across `forms/`, `mutations/`, `filters/`, `registry.py`, and `types/finalizer.py` imports that single definition; there is no duplicated copy of the predicate to fold up. The larger shape the bullet gestures at (the `if implements_relay_node(...): relay.GlobalID else scalar_for_field(...pk)` id-scalar block shared by `mutations/inputs.py:274-277` and `forms/inputs.py:330-333`) is a real N=2 near-twin, but it is a DIFFERENT finding than the one stated, and the Slice-1 source artifact already assessed and deferred it (`bld-slice-1-form_converter_inputs.md` lines 420, 436: the model-less id-type basis is "a clean reconciliation note, not a finding" and broader consolidation "is the integration pass's call once both flavors are accepted"). Nothing to do for this bullet as worded.
- [x] Test-shape precedent: `tests/mutations/test_inputs.py:91` (`{f.python_name: f for f in input_cls.__strawberry_definition__.fields}`), `:94` (`_is_optional` via `strawberry.types.base.StrawberryOptional`), the `.default is UNSET` / `.graphql_name` assertions, and the `_make_relay_target` / `_make_non_relay_target` fixtures (`:258-290`). `tests/forms/test_converter.py` / `test_inputs.py` mirror these helpers verbatim in spirit — the converter/input tests resolve required-ness and id-type the same way.
  - **Investigation (Worker 1):** Not real. This is a recap of acceptable parallel test-shape, not a consolidation opportunity. The duplication is genuine — `_field_map`, `_is_optional`, `_inner_type`, `_make_relay_target`, `_make_non_relay_target`, and `_unique_app_label` are copied near-verbatim between `tests/mutations/test_inputs.py` (`#"_field_map"`, `#"_is_optional"`, `#"_make_relay_target"`) and `tests/forms/test_inputs.py` (`#"_field_map"`, `#"_make_relay_target"`) — but per-tree test fixtures are the standing convention here, not an anomaly to fold up. Three signals confirm sharing them cross-file is undesirable: (1) AGENTS.md test-placement mandates per-tree isolation ("each app carries its OWN coverage so deleting an app loses only that app's tests") and pins `tests/base/` to exactly `test_init.py` / `test_conf.py` with "no new files added", so there is no sanctioned cross-tree shared-helper home — `tests/testing/` is tests *of* the public `testing` module, not a fixture library, and nothing in `tests/` imports a sibling tree's helper. (2) `_unique_app_label` deliberately *diverges* per file — the namespace prefix differs (`test_mutation_inputs__` vs `test_form_inputs__` vs `tests/types/test_converters.py`'s `test_choice_enums`/`test_force_nullable`) precisely to keep each tree's Django app-label registrations collision-free; a shared helper would couple those namespaces. (3) The same per-file pattern already recurs independently across `tests/mutations/`, `tests/forms/`, and `tests/types/` without anyone consolidating it — it is the intentional sibling design, not duplication waiting on a helper. The assertion idioms (`.default is UNSET`, `.graphql_name`, `StrawberryOptional`) are one-liners reading Strawberry's public field API; naming them once cross-tree would hide behavior the per-test reader wants visible. No code change warranted.

**New helpers justified (single responsibility each):**

1. `forms/converter.py::convert_form_field(field)` — **the genuinely net-new machinery** (Decision 7 lines 1214-1219): the `forms.Field`-keyed → Strawberry-annotation + required-ness registry, *for the model-less case* (a plain `Form` field — `captcha`, `confirm_email` — has no Django column and so no read-side equivalent). Single responsibility: given a `forms.Field` instance, return `(annotation, required)`. This is **not** a parallel copy of the `convert_scalar` table: the `ModelForm`-backed-column path defers to `convert_scalar`/`relation_input_annotation` (above); only the no-column path uses this table. It is the explicit spec guard against the over-DRY-into-drift trap (lines 1217-1219, and the rejected Alternative at lines 1427-1429). Fail-loud dispatch detail under Implementation steps.
2. `forms/converter.py` reverse-map record — the per-generated-input-field `(input_attr, graphql_name) → (form_field_name, kind)` metadata, `kind ∈ {scalar, relation_single, relation_multi, file}` (Decision 7 lines 1239-1260, P1). Single responsibility: let Slice 3's resolver build a **form-field-keyed** payload (`{"category": pk}`, never `{"category_id": pk}`). A small frozen dataclass (e.g. `FormInputFieldSpec(input_attr, graphql_name, form_field_name, kind)`) — structurally a sibling of `utils/inputs.py:38-50`'s `GeneratedInputFieldSpec`, but with a `form_field_name` + `kind` instead of `django_source_path` (the form needs the form-field key and the decode kind, not an ORM lookup path). Worker 2 may instead reuse `GeneratedInputFieldSpec` if a clean mapping fits; see Implementation discretion items.
3. `forms/inputs.py::build_form_input_class(form_class, operation_kind, *, fields, exclude)` (name at W2 discretion) — builds **one** `@strawberry.input` from the form's declared fields for a given operation kind, returning `(input_cls, type_name, field_specs)`. Single responsibility: walk the effective form-field set, produce triples, call `build_strawberry_input_class`. The create vs partial requiredness split lives here.
4. `forms/inputs.py` form-shape identity / name helper — `form_input_type_name(form_class, operation_kind, effective_field_names, *, full_field_names)` mirroring `mutations/inputs.py:326-359` `mutation_input_type_name`: canonical `<FormClass.__name__>Input` / `<FormClass.__name__>PartialInput` for the full effective shape, deterministic shape-derived token name for a narrowing. The token scheme (`_pascalize_token`) is in `mutations/inputs.py:304-323` — see "Duplication risk avoided" for how to reuse rather than re-spell.
5. `forms/inputs.py::normalize_form_field_sequence(value, *, label)` — the form-flavored `_normalize_field_sequence`. Justified because the `036` original (`mutations/sets.py:271`) is module-private and its `ConfigurationError` text hard-codes "DjangoMutation"; the form flavor needs "DjangoFormMutation / DjangoModelFormMutation" wording and validation against `form_class.base_fields` (not model columns). This is a thin, intentional near-twin — its *body* (bare-string raise, duplicate raise) is small; the divergence is the message wording + the field-existence basis. Note for Worker 3: this is a **deliberate** near-copy the spec mandates ("mirroring `036`'s `_normalize_field_sequence`"); the alternative (extract a shared param-driven normalizer into `utils/`) is a viable consolidation but is **out of scope for this slice** — flag it for the integration pass, do not pre-extract here (the `036` symbol is private and only one other family would consume it, so the shared-extraction call is the integration pass's to make once both sites exist and are accepted).
6. `forms/inputs.py::get_form_fields(form_class)` / the discovery helper reading `form_class.base_fields` — Decision 7 lines 1398-1411 (P2). Single responsibility: return the `dict[str, forms.Field]` the input derives from, with **no instantiation** (so a kwarg-requiring form still has a discoverable shape). The spec names an overridable classmethod `get_form_fields(cls)` on the base (default `form_class.base_fields`); since the base does not exist until Slice 2, this slice ships the **discovery function** that Slice 2's base method will delegate to (default = read `base_fields`). Worker 2: a module-level `get_form_fields(form_class)` returning `dict(form_class.base_fields)` is the Slice-1 surface; do not add a base classmethod.

**Duplication risk avoided:**

- [x] **A parallel scalar/enum table.** The naive implementation would re-list `CharField → str`, `ChoiceField → enum`, etc. for `ModelForm` fields, drifting from `types/converters.py`. Prevented by routing every `ModelForm`-backed-column field through `convert_scalar`/`convert_choices_to_enum`/`relation_input_annotation` (resolve the column via `model._meta.get_field(name)`); `convert_form_field`'s own table is reached **only** for fields with no backing column. The spec elevates this to a tested guard (the `ChoiceField`-over-model-`choices` symmetric-enum edge case, lines 1900-1903; the rejected Alternative, lines 1427-1429).
  - **Investigation (Worker 1):** Already addressed (risk genuinely avoided — no consolidation work needed). `forms/converter.py::_SCALAR_FORM_FIELDS` (lines 134-146) holds only `forms.Field`-keyed scalar entries; it contains no `models.Field` types and no enum logic — it is NOT a parallel copy of the read-side `convert_scalar`/`convert_choices_to_enum` tables. The branch split is exactly as the finding claims: `forms/inputs.py::_field_triple_and_spec` (lines 364-398) routes a column-backed field (`column is not None`) through `relation_input_annotation` (relation), `Upload` (file), or `convert_scalar(column, type_name, force_nullable=False)` (scalar — which itself delegates to `convert_choices_to_enum` when the column has `choices`); `convert_form_field` is called **only** in the `else` (column-less) arm. The column is resolved at `_model_column_for` via `model._meta.get_field(name)` (line 311, wrapped in `FieldDoesNotExist` → returns `None`). The symmetric-enum guard is pinned by `tests/forms/test_inputs.py::test_choices_modelform_field_resolves_to_read_side_enum` (lines 355-388), which asserts the form input's enum `is` the same object `convert_choices_to_enum` returns for the read side. Nothing to consolidate; the two key spaces (`forms.Field` vs `models.Field`) are kept strictly separate by design.
- [x] **Re-spelling the injective token-name scheme.** `forms/inputs.py`'s narrowed-shape name must be injective per field set exactly as `mutation_input_type_name` is. Risk: copying the `_pascalize_token` + sorted-concat logic into `forms/inputs.py` (two copies of a subtle injectivity-critical algorithm). Prevention: `_pascalize_token` (`mutations/inputs.py:304`) is module-private but importable; reuse the token primitive. Worker 2 chooses between (a) importing `_pascalize_token` from `mutations.inputs` (a one-symbol cross-module reach — acceptable, the function is pure and named in the spec's name-scheme reference) or (b) flagging it for an integration-pass lift into `utils/inputs.py` alongside `graphql_camel_name`. Recommended for this slice: import `_pascalize_token` (and reuse `CREATE`/`PARTIAL` operation-kind constants from `mutations.inputs` if the create/partial split keys on them), and record the cross-module import in `### Notes for Worker 3` so the integration pass can decide whether to promote both into `utils/inputs.py`. Do NOT re-implement the token scheme.
  - **Investigation (Worker 1):** Already addressed (prevention applied as prescribed). `forms/inputs.py:54-59` imports `_pascalize_token` (plus `relation_input_annotation`, `CREATE`, `PARTIAL`) from `..mutations.inputs`; `form_input_type_name` (`forms/inputs.py:260-292`) reuses the primitive at line 291 — `"".join(_pascalize_token(name) for name in sorted(effective_field_names))` — the sorted-concat shape, with no re-spelling of the algorithm. The single `_pascalize_token` definition still lives only at `mutations/inputs.py:304`; grep finds no copy in `forms/`. Worker 2 chose option (a) (cross-module import) and recorded the integration-pass lift candidate in `### Notes for Worker 3` of the source artifact, exactly as the finding prescribed. No consolidation work remains for this cycle; the `utils/inputs.py` lift of the shared token primitive is correctly deferred to the integration pass (both flavors must exist + be accepted first).
- [x] **Re-deriving the collision raise.** The two collision cases (distinct shapes on one name; two forms sharing `__name__`) must raise. Risk: writing a bespoke name-collision check in `forms/inputs.py`. Prevention: it comes free from `materialize_generated_input_class`'s ledger (lines 1343-1348) — the form flavor must route every materialization through `materialize_form_input_class` and never maintain its own name set.
  - **Investigation (Worker 1):** Already addressed. `forms/inputs.py` holds exactly one name set, `_materialized_names: dict[str, type] = {}` (`forms/inputs.py:110`), and it is never read or written directly — it is handed to the shared helper as `ledger=_materialized_names` inside `materialize_form_input_class(name, input_cls)` (`forms/inputs.py:113-134`), the prescribed thin wrapper over `materialize_generated_input_class` (imported from `..utils.inputs` at `forms/inputs.py:67`; the helper now lives at `utils/inputs.py:149-185`, drifted from the finding's cited `:103-139`). The pin/dedupe/distinct-class collision raise all live in that shared helper, so both Decision 7 cases (distinct shapes on one name; two form classes sharing a `__name__`) raise from the shared ledger for free. No bespoke collision check, no second name set: the build functions `build_form_input_class` (`forms/inputs.py:410-464`) and `build_form_inputs` (`:472-524`) return UNMATERIALIZED classes and never touch any name registry; `materialize_form_input_class` is the sole materialization route. Form-class-specific code uses `form_class.__name__` only for the canonical type-name string and `ConfigurationError` messages, never as a collision key. No consolidation remains.
- [x] **`forms.Field` vs `models.Field` confusion.** The converter is keyed on `forms.Field`; the reused converters are keyed on `models.Field`. Risk: passing a `forms.Field` to `convert_scalar` (silent wrong dispatch / raise). Prevention: the `ModelForm`-overlap path **resolves the model column first** (`model._meta.get_field(name)` → a `models.Field`) before calling `convert_scalar`/`relation_input_annotation`; the `forms.Field` only ever reaches `convert_form_field`. Worker 2 keeps the two key spaces strictly separated.
  - **Investigation (Worker 1):** Already addressed (risk genuinely avoided — no consolidation work needed). The two key spaces are kept strictly separate exactly as the finding prescribes. `forms/inputs.py::build_form_input_class` resolves the model column FIRST at the call site (`column = _model_column_for(form_class, name)`, line 446) before dispatch, then hands `(name, field, column, type_name)` to `_field_triple_and_spec` (line 447). `_model_column_for` (lines 295-313) is the sole `model._meta.get_field(name)` site (line 311, wrapped in `FieldDoesNotExist` → `None`); a plain `Form` (no `_meta.model`) or a column-less extra field yields `None`. `_field_triple_and_spec` (lines 339-398) branches on `column is not None`: the `models.Field` arm routes ONLY the resolved column object through `relation_input_annotation` (line 369) / `convert_scalar` (line 382) / `Upload`; `convert_form_field(field)` — the `forms.Field`-keyed table — is called ONLY in the `else` (column-less) arm (line 387). No `forms.Field` ever reaches `convert_scalar`/`relation_input_annotation`, and no `models.Field` ever reaches `convert_form_field`. The one model-less relation helper, `_model_less_relation_annotation` (lines 316-336), derives the related model from `field.queryset.model` and `scalar_for_field(related_model._meta.pk)` — still a `models.Field` (the pk column), never the `forms.Field`. Nothing to consolidate.

---

## Build: Slice 2 — the `DjangoFormMutation` / `DjangoModelFormMutation` bases + `Meta` validation + the phase-2.5 bind

_Source: `docs/builder/bld-slice-2-form_bases_meta_validation.md`_

**Existing patterns reused (cite file:line — pin-at-write-time):**

- [x] `django_strawberry_framework/mutations/sets.py::_validate_mutation_meta` (lines 347-443) — the 97-line class-creation validator (the helper's flagged hotspot at line 347, 10 branches). The refactor **moves this body verbatim** into a `DjangoMutation._validate_meta(cls, meta)` classmethod (the model base keeps every line, incl. its `"DjangoMutation {name}.Meta..."` wording — the shadow's `13x DjangoMutation` literal stays in the model path). The module-level `_validate_mutation_meta` becomes a thin shim `return cls._validate_meta(meta)` OR the metaclass calls `new_class._validate_meta(meta)` directly — see Implementation discretion. This is the seam: the form override supplies its own body.
  - **Investigation (Worker 1):** Already addressed (in the Slice-2 build itself, baseline `2b34ec89`). The seam landed correctly: `DjangoMutation._validate_meta(cls, meta)` is now an overridable `@classmethod` (`mutations/sets.py:455-561`) carrying the relocated 0.0.11 body — the model-path wording `"DjangoMutation {name}.Meta..."` is preserved verbatim (`:492`, `:497`, `:503`, `:511`, `:523`), the `13x DjangoMutation` literal stays in the model path. The module-level `_validate_mutation_meta` function is removed (no shim); it survives only as a prose reference in a comment (`:91`, `:462`). The metaclass `DjangoMutationMetaclass.__new__` dispatches via `new_class._mutation_meta = new_class._validate_meta(meta)` (`:407`). The body was relocated, not re-typed (only `mutation_cls`→`cls` and `name = cls.__name__`), so model validation behavior is unchanged. The form overrides exist and supply their own bodies: `DjangoModelFormMutation._validate_meta` (`forms/sets.py:323`) and the plain `DjangoFormMutation._validate_meta` (`forms/sets.py:583`). Nothing left to consolidate.
- [x] `django_strawberry_framework/mutations/sets.py::DjangoMutationMetaclass.__new__` (lines 457-473) — the metaclass calls `_validate_mutation_meta(new_class, meta)` (line 471) and `register_mutation(new_class)` (line 472). The refactor changes line 471 to `new_class._validate_meta(meta)` (dispatching to the overridable classmethod), so the **same metaclass** validates a `DjangoModelFormMutation` (a `DjangoMutation` subclass) through the form override — no second metaclass needed for the ModelForm flavor. The plain `DjangoFormMutation` needs its OWN metaclass (it is not a `DjangoMutation` subclass) — modeled on this one.
  - **Investigation (Worker 1):** Already addressed (landed in the Slice-2 build itself). The metaclass reuse is exactly as the finding describes — there is no second metaclass for the ModelForm flavor and no consolidation opportunity in the plain-form metaclass. `DjangoMutationMetaclass.__new__` (`mutations/sets.py:393-409`; cited `:457-473` has drifted) dispatches validation via `new_class._mutation_meta = new_class._validate_meta(meta)` (`:407`) then `register_mutation(new_class)` (`:408`) — the line-471 change the finding prescribed is in place. `DjangoModelFormMutation(DjangoMutation)` (`forms/sets.py:290`) inherits this SAME metaclass and supplies only the `_validate_meta` form override (`forms/sets.py:323`), so the model metaclass validates it through the override — no ModelForm-specific metaclass exists. The plain `DjangoFormMutation` (`forms/sets.py:546`) is NOT a `DjangoMutation` subclass and gets its OWN `DjangoFormMutationMetaclass` (`forms/sets.py:515-543`), structurally modeled on `DjangoMutationMetaclass.__new__` (same `super().__new__` → `meta is None` abstract-base skip → `new_class._validate_meta(meta)` → register shape) but differing in the load-bearing way the finding sanctions: it calls the disjoint `register_form_mutation` (`:542`) instead of `register_mutation`, because the plain form rides its own declaration registry / `bind_form_mutations()`, not the model quad. The two metaclasses are a deliberate same-shape sibling pair (~16 mechanical lines each), not duplication to fold up — merging them would force the model-less flavor onto the model registry, the exact over-DRY trap Decision 6 (lines 1163-1174) rejects. Nothing to consolidate.
- [x] `django_strawberry_framework/mutations/sets.py::register_mutation` / `clear_mutation_registry` / `iter_mutations` (lines 126-156) + the module-global `_mutation_registry: list[type]` (line 107) — the four-part declaration-registry quad (identity-dedup append, post-`mark_finalized()` reject, `.clear()`, ordered `tuple(...)` snapshot). Decision 13 (lines 1779-1793) mandates factoring the **shared mechanics** into `make_declaration_registry(label)` that BOTH `mutations/sets.py` and `forms/sets.py` instantiate, **keeping the two ledgers disjoint** (different `bind_*` bodies, different `registry.clear()` rows). Verified mechanically identical to what the plain-form registry needs.
  - **Investigation (Worker 1):** Already addressed. The `make_declaration_registry(label)` factory landed at `mutations/sets.py:139-179` and folds all four mechanics into the callables it closes over a fresh private `store: list[type]`: identity-dedup append (`if declaration_cls not in store: store.append(...)`, `:170-171`), post-finalize reject (`if registry.is_finalized(): raise ConfigurationError(f"Cannot declare {label} {declaration_cls.__name__} after finalization; ...")`, `:165-169` — `label` interpolated as Decision 13 prescribes), `.clear()` (`:173-174`), and ordered `tuple(store)` snapshot (`:176-177`). It returns a `DeclarationRegistry` NamedTuple `(register, clear, iter_, store)` (`:122-136`, `:179`). BOTH modules instantiate it over disjoint lists: `mutations/sets.py:192-196` (`make_declaration_registry("DjangoMutation")`) and `forms/sets.py:112-116` (`make_declaration_registry("DjangoFormMutation")`, importing the factory at `forms/sets.py:60`). The public names survive — `register_mutation` / `clear_mutation_registry` / `iter_mutations` are reassigned from the model quad (`mutations/sets.py:193-195`; consumed by the metaclass at `:408`, the bind at `:1038`, registry.py co-clear, and tests), and the form flavor exposes the parallel disjoint `register_form_mutation` / `clear_form_mutation_registry` / `iter_form_mutations` (`forms/sets.py:113-115`; metaclass at `:542`, bind at `:824`). The two `store` lists never merge. No body duplication, no hand-written registry quad remains. (The cited `:126-156` / `_mutation_registry` line 107 have drifted to the `make_declaration_registry`/assignment block at `:139-196`.)
- [x] `django_strawberry_framework/mutations/sets.py::_materialize_input_for` (lines 603-669) — builds the model-column `<Model>Input` / `<Model>PartialInput` (or merges a consumer `input_class`), routing through `mutation_input_shape` + `build_mutation_input` + `materialize_mutation_input_class`, caching by shape identity in `_shape_build_cache` (line 123). This becomes the **default body of `DjangoMutation.build_input(meta, primary_type)`** (the model flavor's seam default), called from `_bind_mutation` (line 869) instead of the direct function call. The form override calls the `forms/inputs.py` generator instead.
  - **Investigation (Worker 1):** Already addressed (in the Slice-2 build itself). The `build_input` seam landed exactly as the finding prescribes. `DjangoMutation.build_input(cls, meta, primary_type)` is an overridable `@classmethod` (`mutations/sets.py:570-582`) whose model default delegates to `_materialize_input_for(cls.__name__, meta, primary_type)` (`:582`) — the relocated model-column body still lives in `_materialize_input_for` (`:740-805`), routing through `mutation_input_shape` (`:788`) + `build_mutation_input` (`:796`) + `materialize_mutation_input_class` (`:805`), caching by `shape.cache_key` in `_shape_build_cache` (`:794-804`, dict at `:119`). `_bind_mutation` calls the seam (`input_cls = mutation_cls.build_input(meta, primary_type)`, `:1009`) instead of a direct function call, with the cited `:603-669`/`:869` having drifted to `:740-805`/`:1009`. The form overrides exist and call the `forms/inputs.py` generator instead: `DjangoModelFormMutation.build_input` (`forms/sets.py:413-448`, via `build_form_inputs` + `materialize_form_input_class`) and the plain `DjangoFormMutation.build_input` (`forms/sets.py:674-697`). Nothing left to consolidate.
- [x] `django_strawberry_framework/mutations/sets.py::_bind_mutation` (lines 857-885) — resolves the primary type (`_resolve_primary_type`, line 867), materializes the input (line 869 → now `mutation_cls.build_input(meta, primary_type)`), builds + materializes the `<Name>Payload` (lines 871-881), stashes `_primary_type` / `_input_class` / `_payload_type_name` (lines 883-885). For the `DjangoModelFormMutation` flavor this binds **unchanged except** the input-materialization step routes through the `build_input` seam (Decision 13 lines 1752-1765). `bind_mutations()` (lines 888-899) drains the `DjangoMutation` registry and clears `_shape_build_cache` — the ModelForm flavor rides it for free.
  - **Investigation (Worker 1):** Already addressed (in the Slice-2 build itself). `_bind_mutation` is reused for the `DjangoModelFormMutation` flavor exactly as the finding prescribes — UNCHANGED except the input step routes through the `build_input` seam. In current source `_bind_mutation` (`mutations/sets.py:994-1025`) resolves `_resolve_primary_type(mutation_cls, meta.model)` (`:1004`), materializes the input via the seam `input_cls = mutation_cls.build_input(meta, primary_type)` (`:1009`, the one prescribed change — was a direct `_materialize_input_for` call), builds + materializes `<Name>Payload` via `build_payload_type` + `materialize_mutation_input_class` (`:1011-1021`), and stashes `_primary_type` / `_input_class` / `_payload_type_name` (`:1023-1025`). `DjangoModelFormMutation` (`forms/sets.py:290`) subclasses `DjangoMutation` and defines NO `_bind_mutation` and NO metaclass `__new__` of its own — it rides `DjangoMutationMetaclass.__new__` (`sets.py:393`), which calls `new_class._validate_meta(meta)` + `register_mutation(new_class)` into the SAME `_mutation_declaration_registry` (`sets.py:193`). `bind_mutations()` (`:1028-1039`) clears `_shape_build_cache` (`:1037`) and drains `iter_mutations()` (`:1038`) calling the inherited `_bind_mutation` for each — so the ModelForm flavor binds for free; its only divergence is its `build_input` override (`forms/sets.py:413`) feeding the form-derived input. Cited line numbers drifted (`:857-885`→`:994-1025`; `:867`→`:1004`; `:869`→`:1009`; `:871-881`→`:1011-1021`; `:883-885`→`:1023-1025`; `:888-899`→`:1028-1039`), but the structure matches the finding verbatim. Nothing to consolidate.
- [x] `django_strawberry_framework/mutations/fields.py::_input_type_name` (lines 95-124) + `_lazy_ref` (lines 127-136) + `INPUTS_MODULE_PATH` import (line 62) — the model-column `data:` lazy-ref derivation. Decision 5 axis 3 (lines 1008-1017) makes the name + module overridable seams `input_type_name(meta)` / `input_module_path` the factory consults. **This slice defines those seam classmethods on `DjangoMutation` (model defaults = today's `_input_type_name` body + `INPUTS_MODULE_PATH`)**; the *fields.py rewiring to consult them* is Slice 3 (spec Slice 3 sub-bullet 2). So the model default `DjangoMutation.input_type_name(meta)` returns exactly what `fields.py::_input_type_name(meta)` returns today, and `DjangoMutation.input_module_path` is exactly `mutations.inputs.INPUTS_MODULE_PATH` — keep them single-sourced (see Duplication risk avoided).
  - **Investigation (Worker 1):** Already addressed (staged duplication resolved by Slice 3). The seams are in place and single-sourced: `DjangoMutation.input_type_name(meta)` (`mutations/sets.py:584-615`, the relocated body delegating to `mutation_input_type_name(...)`) and the `input_module_path: str = INPUTS_MODULE_PATH` class attribute (`mutations/sets.py:568`). The transient `fields.py::_input_type_name` twin is gone — `fields.py` now consults the seam directly at `mutations/fields.py:184` (`data_ann = _lazy_ref(mutation_cls.input_type_name(meta), mutation_cls.input_module_path)`), so there is one name-derivation body. Grep finds no `_input_type_name` symbol anywhere in `mutations/` (the remaining `_input_type_name_for` hits are the unrelated filters/orders helper) and no `TODO(spec-038` anchor remains in source. The sets.py docstring (`:596-598`) records that Slice 3 deleted the twin. No consolidation work remains.
- [x] `django_strawberry_framework/mutations/resolvers.py::resolve_mutation_sync` (line 1133) / `resolve_mutation_async` (line 1149) — the model resolver entry points `fields.py::_resolve` calls today (fields.py lines 211-213). The `DjangoMutation.resolve_sync` / `resolve_async` classmethod defaults delegate to these (so the model dispatch is unchanged when Slice 3 rewires `fields.py::_resolve` to call `mutation_cls.resolve_sync`).
  - **Investigation (Worker 1):** Already addressed (the seam landed in the Slice-2 build; Slice 3 then rewired `fields.py` onto it). The delegating seams are in place exactly as the finding prescribes and there is no duplication to consolidate. `DjangoMutation.resolve_sync(cls, info, *, data, id)` (`mutations/sets.py:617-636`) and `resolve_async` (`:638-654`) are overridable `@classmethod`s whose **model defaults delegate** to the existing resolver entry points via a function-local import — `from .resolvers import resolve_mutation_sync; return resolve_mutation_sync(cls, info, data=data, id=id)` (`:634-636`) and the async mirror `resolve_mutation_async(cls, info, data=data, id=id)` (`:652-654`). The resolver bodies live once at `resolvers.py:1147` (`resolve_mutation_sync`) / `:1163` (`resolve_mutation_async`) — the cited `:1133`/`:1149` have drifted but the entry points are intact and unduplicated. `mutations/fields.py::_resolve` is already wired to call the seam (`mutation_cls.resolve_async(info, **call_kwargs)` / `mutation_cls.resolve_sync(...)`, `fields.py:232-233`), not the resolver functions directly, so the model dispatch routes seam→delegate→resolver with the body single-sourced in `resolvers.py`. The form flavors override these seams (the form pipeline, spec-038 Slice 3), confirming the seam is genuinely overridable. Nothing left to consolidate.
- [x] `django_strawberry_framework/forms/inputs.py::build_form_inputs` (lines 483-535) + `clear_form_input_namespace` (lines 136-154) + `FORM` (line 93) + `materialize_form_input_class` (lines 112-133) + `INPUTS_MODULE_PATH` (line 84) — the Slice-1 form-input generator + ledger. `forms/sets.py`'s `build_input` overrides (both flavors) call `build_form_inputs(form_class, operation_kind=..., fields=, exclude=, guard_required=...)` and `materialize_form_input_class`; `bind_form_mutations()` drains the plain-form registry and calls each plain form's `build_input` + a model-less payload build. The Slice-1 `clear_form_input_namespace()` is wired into `registry.clear()` **this slice** (Slice 1 left the `# wired into registry.clear() in Slice 2` note, `forms/inputs.py` lines 105-106 / 152).
  - **Investigation (Worker 1):** Already addressed. The reuse this finding recaps is in place in current source — all three asserted wirings landed in the Slice-2 build, with no duplication or leftover unwired note. (1) Both `build_input` overrides call the Slice-1 generator + ledger: `DjangoModelFormMutation.build_input` (`forms/sets.py:413`) routes through `build_form_inputs` (via `_cached_build_form_input`, which calls `build_form_inputs` at `forms/sets.py:180`; imported `forms/sets.py:66`) and calls `materialize_form_input_class(input_cls.__name__, input_cls)` (`forms/sets.py:448`); the plain `DjangoFormMutation.build_input` (`forms/sets.py:674`) likewise calls `materialize_form_input_class` (`forms/sets.py:697`). Both share the Slice-1 `FORM` sentinel / `INPUTS_MODULE_PATH` and never hand-roll a class build. (2) `bind_form_mutations()` (`forms/sets.py:811`) drains the disjoint plain-form registry — `for mutation_cls in iter_form_mutations(): _bind_form_mutation(mutation_cls)` (`forms/sets.py:824-825`) — and each `_bind_form_mutation` (`forms/sets.py:787`) materializes the form-derived input via the `build_input` seam (`forms/sets.py:797`) plus the model-less `{ ok errors }` payload. (3) `clear_form_input_namespace` is now wired into the clear path: `registry.py::TypeRegistry.clear` has the `_clear_if_importable("django_strawberry_framework.forms.inputs", "clear_form_input_namespace", ...)` co-clear row (`registry.py:543-547`), byte-identical in shape to the mutation co-clears above it, alongside the `clear_form_mutation_registry` row (`registry.py:548-552`). The Slice-1 "wired in Slice 2" note (`forms/inputs.py:106`, `:152-153`) now accurately describes a wiring that exists — not a leftover unwired TODO; the registry comment (`registry.py:538`) confirms "the Slice-1 ledger finally wired here". (Beyond what this bullet asserts, the Slice-2 build added a *third* form co-clear row, `clear_form_shape_build_cache` at `registry.py:553-557`, for the form input-shape cache the build report flagged as in-contract drift — not in conflict.) No consolidation remains.
- [x] `django_strawberry_framework/registry.py::clear` (lines 468-544) — the `_clear_if_importable(module_path, attr_name, action)` co-clear idiom (lines 34-50). The two new co-clear rows (`forms.inputs::clear_form_input_namespace` and `forms.sets::clear_form_mutation_registry`) append after the existing mutation co-clears (lines 518-534), byte-identical shape to lines 525-534.
  - **Investigation (Worker 1):** Already addressed (landed in the Slice-2 build itself). Both new co-clear rows route through the existing `_clear_if_importable(module_path, attr_name, action)` helper (`registry.py:34-50`) and are byte-identical in shape to the mutation co-clears: `_clear_if_importable("django_strawberry_framework.forms.inputs", "clear_form_input_namespace", lambda clear: clear())` (`registry.py:543-547`) and `_clear_if_importable("django_strawberry_framework.forms.sets", "clear_form_mutation_registry", lambda clear: clear())` (`registry.py:548-552`), appended immediately after the mutation co-clears (`:525-534`). No hand-rolled clear logic, no bespoke try/except, no duplicated body — the same single-sited cycle-safe idiom is reused in place. (Cited `:468-544`/`:518-534`/`:34-50` drifted slightly to `:543-552`/`:525-534`/`:34-50`, but the structure matches the finding verbatim.) Beyond what this bullet asserts, the build added a third adjacent form co-clear row, `clear_form_shape_build_cache` (`:553-557`), also via the same idiom — consistent, not in conflict. Nothing to consolidate.
- [x] `django_strawberry_framework/types/finalizer.py::finalize_django_types` (lines 695-712) — the phase-2.5 bind window: the function-local `from ..mutations.sets import bind_mutations` (line 708) then `bind_mutations()` / `_bind_filtersets()` / `_bind_ordersets()` (lines 710-712). `bind_form_mutations()` is added with the **same function-local-import + call** shape, alongside `bind_mutations()` (Decision 13 lines 1773-1777 — a single named finalizer edit, no new public entry point). Placement: after `bind_mutations()` (the plain-form bind is independent of the model bind; both run before Phase 3 / `strawberry.type`).
  - **Investigation (Worker 1):** Already addressed (landed in the Slice-2 build itself). `bind_form_mutations()` is wired into the phase-2.5 bind window of `finalize_django_types` with the SAME function-local-import + call shape as `bind_mutations()`, placed immediately after it — no new public entry point. In current source the model bind is `from ..mutations.sets import bind_mutations` (`types/finalizer.py:708`) + `bind_mutations()` (`:710`); the form bind mirrors it byte-for-byte in shape: `from ..forms.sets import bind_form_mutations` (`:721`) + `bind_form_mutations()` (`:723`), sitting between `bind_mutations()` and `_bind_filtersets()` / `_bind_ordersets()` (`:724-725`), so both bind passes complete before the Phase-3 `strawberry.type(...)` loop (`:727-731`). `forms/sets.py::bind_form_mutations` (`:811`) exists and drains the disjoint plain-form registry, matching the finding's premise. Cited `:695-712` (import `:708`, calls `:710-712`) have drifted to `:695-725` (import `:721`, call `:723`), but the structure matches the finding verbatim. Nothing to consolidate.
- [x] `django_strawberry_framework/__init__.py` (lines 19-24, 38-62) — the `from .mutations import (...)` re-export block + `__all__` tuple. Add `DjangoFormMutation` / `DjangoModelFormMutation` from `.forms` (a new `from .forms import (...)` line, mirroring the mutations block) and two `__all__` entries (alphabetical). Do NOT touch `__version__` (line 36) — Slice 5.
  - **Investigation (Worker 1):** Already addressed. The Slice-2 build already shipped this re-export. `django_strawberry_framework/__init__.py:18` carries `from .forms import DjangoFormMutation, DjangoModelFormMutation  # noqa: E402` (mirroring the `.mutations` block style); `__all__` lists `"DjangoFormMutation"` (line 44) and `"DjangoModelFormMutation"` (line 47), both alphabetically placed; `__version__ = "0.0.12"` (line 37) is untouched. `forms/__init__.py:29` re-exports both bases (with its own `__all__`), so the package-root import resolves. No duplication or inconsistency — this finding was completed in the source cycle, not a pending DRY edit.
- [x] `django_strawberry_framework/types/base.py` `DEFERRED_META_KEYS` / `ALLOWED_META_KEYS` — **read-only, byte-unchanged** (Decision 13 lines 1795-1800; DoD item 3). A form-mutation `Meta` is its own namespace; do not import or extend these sets.
  - **Investigation (Worker 1):** Not real (no opportunity — confirmed as designed). `git diff <baseline> -- django_strawberry_framework/types/base.py` is empty, so `DEFERRED_META_KEYS` (`types/base.py:63`) and `ALLOWED_META_KEYS` (`:67`) are byte-unchanged. The form code keeps its own disjoint namespace: `forms/sets.py` defines `_ALLOWED_MODELFORM_META_KEYS` (`:81`) and `_ALLOWED_PLAIN_FORM_META_KEYS` (`:90`), and both `_validate_meta` overrides validate against those (`:350`, `:628`) — there is NO `from ..types.base import` / `types.base` reference anywhere under `forms/` (grep: none), so the form `Meta` validation neither imports nor extends the base sets. This is the intended sibling-namespace design (Decision 6: a form `Meta` adds `form_class`, drops `model`, and the `DjangoType`-flavor `DEFERRED_META_KEYS` deferrals do not apply), correctly N=2-divergent rather than duplicated. Nothing to consolidate.
- [x] Test-shape precedent: `tests/mutations/test_sets.py` (the `Meta`-validation matrix shape, the registry-isolation autouse fixture, the `pytest.raises(ConfigurationError, match=...)` assertions, the `bind_mutations()` + finalize harness). `tests/forms/test_sets.py` mirrors these. `tests/forms/test_inputs.py` (Slice 1) already carries the form fixtures (`ModelForm` over products `Item`, plain `Form`, kwarg-requiring form, `_make_relay_target` / `_make_non_relay_target`).
  - **Investigation (Worker 1):** Not real (intentional parallel test-shape; no consolidation warranted under AGENTS.md test-placement). This finding recaps a *precedent to follow*, not a defect, and there is no shareable seam. (1) The `tests/mutations/test_sets.py` mirror is cross-tree — AGENTS.md "no cross-tree helper imports" forbids `tests/forms/` importing from `tests/mutations/`, so the matrix shape / autouse-fixture shape / `pytest.raises(ConfigurationError, match=...)` idiom are deliberately re-typed per tree; the mutation file is the model the forms file was written against, not a helper to extract. (2) Within `tests/forms/` the apparent fixture overlap between `test_sets.py` and `test_inputs.py` is sibling divergence at N=2, not duplication: the two autouse fixtures differ by design and document it in their docstrings (`test_inputs.py:65` `_isolate_registry_and_ledger` clears `clear_form_input_namespace()` explicitly because Slice 1 did NOT wire it into `registry.clear()`; `test_sets.py:71` `_isolate_registry` relies on the Slice-2 co-clear and calls only `registry.clear()`); `_item_model_form` diverges (`test_inputs.py:193` adds a non-model required `confirm = forms.BooleanField` for its input-shape assertions; `test_sets.py:95` is plain); `_contact_form` differs in fields and is a reusable helper only in `test_sets.py:106` (`message`), while `test_inputs.py`'s `ContactForm`s are inline-in-test with different fields. (3) The one near-identical pair — `_unique_app_label` + `_app_label_counter` (`test_sets.py:87-92`, `test_inputs.py:81-86`) — deliberately uses a per-module label prefix (`test_form_sets__` vs `test_form_inputs__`) so emitted app-labels stay diagnosable to their source file and the two `itertools.count` counters don't interleave; hoisting to a `tests/forms/conftest.py` would erase that namespacing for a ~3-line save and is premature abstraction. The `_make_relay_target`/`_make_non_relay_target` helpers the finding names live only in `test_inputs.py` (input-shape concerns) and aren't referenced by `test_sets.py`. Nothing to consolidate.

**New helpers justified (single responsibility each):**

1. `mutations/sets.py::make_declaration_registry(label)` — the Decision-13 shared-mechanics factory. Single responsibility: given a human label (`"DjangoMutation"` / `"DjangoFormMutation"`), create a **fresh private `list[type]`** and return the bound `(register, clear, iter)` callables over it (identity-dedup append + post-`mark_finalized()` reject in `register`, `.clear()` in `clear`, ordered `tuple(...)` in `iter`). The `label` is interpolated into the post-finalize reject message (`f"Cannot declare {label} {cls.__name__} after finalization; ..."`). `mutations/sets.py` instantiates it for `register_mutation` / `clear_mutation_registry` / `iter_mutations` (replacing the three hand-written functions + `_mutation_registry`); `forms/sets.py` instantiates a **second, disjoint** one for `register_form_mutation` / `clear_form_mutation_registry` / `iter_form_mutations`. Single-sources the dedup/reject/clear logic without merging the storage (the over-DRY trap Decision 13 names). Lives in `mutations/sets.py` (the existing owner of the quad); `forms/sets.py` imports it. Return shape at W2 discretion (a small frozen tuple, a tiny dataclass, or a namedtuple — see discretion items); `mutations/fields.py` imports `iter_mutations` today only transitively, but the public names `register_mutation` / `clear_mutation_registry` / `iter_mutations` must survive (registry.py line 531 + the metaclass + tests reference them), so the factory's returned callables are **assigned to those module-level names**.

2. `forms/sets.py::DjangoModelFormMutation` — the `ModelForm` base subclassing `DjangoMutation`. Single responsibility: supply the four form seam overrides (`_resolve_model` → `Meta.form_class._meta.model`; `_validate_meta` → the form-flavor validator; `build_input` → the `forms/inputs.py` generator + `materialize_form_input_class`; `input_type_name` / `input_module_path` → the form name + `forms.inputs.INPUTS_MODULE_PATH`; `resolve_sync` / `resolve_async` → the Slice-3-anchored stubs). It inherits the metaclass, `register_mutation`, `_bind_mutation`, `check_permission`, and the `DjangoModelPermission` default unchanged.

3. `forms/sets.py::DjangoFormMutation` + `DjangoFormMutationMetaclass` — the model-less plain-`Form` sibling (its OWN metaclass + declaration registry + bind). Single responsibility: a lighter base that shares the form pipeline (Slice 3) and converter but carries no model / no `DjangoType` object slot. Its metaclass validates `Meta` via the plain-form `_validate_meta` and calls `register_form_mutation`. Its `build_input` materializes the model-less input; `bind_form_mutations()` drains its registry and builds each plain form's input + the pinned `ok`/`errors` payload.

4. `forms/sets.py::bind_form_mutations()` — the phase-2.5 entry point for the plain-form registry. Single responsibility: drain `iter_form_mutations()`, and for each materialize its form-derived input (via the flavor `build_input`) + its **pinned two-field payload** (`ok: Boolean!` + `errors: [FieldError!]!` — no DjangoType object slot, Decision 6 lines 1111-1128). NOTE: the payload *builder* for the model-less `ok`/`errors` shape — does the plain-form payload reuse `build_payload_type(object_type=None)` or a dedicated model-less builder? See Implementation steps + the spec-reconciliation note: `build_payload_type` (mutations/inputs.py line 549) takes `object_type` + `object_slot`; a model-less payload has neither. This slice needs a model-less payload build path. Resolve in-plan (below).

5. `forms/sets.py::_validate_form_mutation_meta` (or two flavor classmethod bodies) — the form-flavor `_validate_meta`. Single responsibility: the form allowed-key validation matrix (Decision 6 / spec Slice 2 lines 344-370). A shared core (presence of `form_class`, `fields`/`exclude` normalize + mutual exclusion via the Slice-1 `resolve_effective_form_fields` machinery or `normalize_form_field_sequence`) plus the two per-base divergences (plain: `issubclass(ModelForm)` reject FIRST → require `forms.Form`, reject ANY `operation`, `"form"` sentinel; modelform: require `forms.ModelForm`, `operation ∈ {"create","update"}`, reject `"delete"`). W2 decides one shared helper with a flag vs two classmethod bodies (see discretion).

**Duplication risk avoided:**

- [x] **A second copy of the 97-line `_validate_mutation_meta` body.** The model `_validate_meta` MUST be the relocated body, not a re-typed near-copy — the no-regression gate depends on byte-identity. The form override is a genuinely different matrix (different keys, different `operation` rule, the `form_class`-before-`_resolve_model` ordering), so it is a *new* body, not a near-copy of the model one. Prevent: relocate (cut/paste) the model body into the classmethod; do not retype it. Worker 3 diffs the relocated body against the pre-refactor `_validate_mutation_meta` to confirm the model path is unchanged.
  - **Investigation (Worker 1):** Already addressed (prevented-risk honored, no duplication present). The model validator is the relocated single body: `django_strawberry_framework/mutations/sets.py::DjangoMutation._validate_meta` (the metaclass at `DjangoMutationMetaclass.__new__` calls `new_class._validate_meta(meta)`); the module-level `_validate_mutation_meta` is GONE (`grep` finds no `def _validate_mutation_meta`, only a doc-comment reference). Diffing the current classmethod's executable statements against the pre-refactor module-level body (`f55c63c5^`) is identical line-for-line modulo the `mutation_cls`→`cls` + `name = cls.__name__` rename and a cosmetic comment line-rewrap — no logic change, byte-behavior-identical, no second copy. The two form validators are genuinely distinct matrices, not near-copies of the model one: `forms/sets.py::DjangoModelFormMutation._validate_meta` (allowed-keys `_ALLOWED_MODELFORM_META_KEYS` adding `form_class`; require `forms.ModelForm` subclass BEFORE `_resolve_model`; `operation ∈ {"create","update"}` rejecting `"delete"`; snapshot carries `form_class`) and `forms/sets.py::DjangoFormMutation._validate_meta` (allowed-keys `_ALLOWED_PLAIN_FORM_META_KEYS`; rejects ANY `operation` FIRST with a targeted message; `forms.ModelForm`-first reject naming `DjangoModelFormMutation`; then `forms.Form` gate; snapshot `model=None` + `FORM` sentinel). The three share only thin helpers (`_require_form_class`, `_resolve_effective_form_field_names`, `_validate_permission_classes`), not a re-spelled validation body. Nothing to consolidate.
- [x] **Re-spelling the declaration-registry quad.** The plain-form registry's four functions are mechanically identical to `register_mutation` & co. Prevent: `make_declaration_registry(label)` single-sources them (Decision 13). Do NOT clone the four bodies into `forms/sets.py`; instantiate the factory.
  - **Investigation (Worker 1):** Already addressed. The dedup/reject/clear/iter mechanics are single-sourced in `mutations/sets.py::make_declaration_registry(label)` (sets.py:139-179, returning the `DeclarationRegistry` NamedTuple at sets.py:122-136); `forms/sets.py:112-116` instantiates it (`_form_mutation_declaration_registry = make_declaration_registry("DjangoFormMutation")`) and binds `register_form_mutation` / `clear_form_mutation_registry` / `iter_form_mutations` / `_form_mutation_registry` to its `.register`/`.clear`/`.iter_`/`.store` handles. No quad bodies are cloned into `forms/sets.py` — the form ledger reuses the factory over a disjoint backing list, exactly as the finding's prevention prescribes (the model flavor does the same at sets.py:192-196). Built this way in the Slice-2 commit; no further consolidation needed.
- [x] **Re-deriving the form input name / module in two places.** Slice 3's `fields.py` will consult `mutation_cls.input_type_name(meta)` / `input_module_path`. The model default must be single-sourced with today's `fields.py::_input_type_name`: rather than copy that body into `DjangoMutation.input_type_name`, **move** the `_input_type_name` body to the `DjangoMutation.input_type_name` classmethod (model default) and have `fields.py` call the classmethod in Slice 3 (so there is one body). For THIS slice, the cleanest move is: define `DjangoMutation.input_type_name(meta)` with the `_input_type_name` body and `input_module_path = mutations.inputs.INPUTS_MODULE_PATH`; leave `fields.py::_input_type_name` in place (Slice 3 removes it when it rewires `_synthesized_mutation_signature` to consult the seam). Flag the transient duplication (`_input_type_name` body exists in both `fields.py` and the new classmethod between Slice 2 and Slice 3) as a **staged, Slice-3-resolved** item — do NOT leave it permanently. Worker 2 anchors the `fields.py::_input_type_name` with a `TODO(spec-038 Slice 3)` note that Slice 3 deletes. (See Implementation discretion — W2 may instead have the Slice-2 classmethod *delegate* to `fields.py::_input_type_name` to avoid the copy, but that creates a `sets.py → fields.py` import edge that does not exist today and that `fields.py` imports `sets.py` already, risking a cycle; recommended: relocate the body to `sets.py` and have Slice 3 point `fields.py` at it.)
  - **Investigation (Worker 1):** Already addressed (single-sourced; staged duplication resolved by Slice 3). The model name-derivation body lives once on the seam at `django_strawberry_framework/mutations/sets.py:585 DjangoMutation.input_type_name(cls, meta)` (delegating to `mutations/inputs.py::mutation_input_type_name`, with `input_module_path = INPUTS_MODULE_PATH` at sets.py:568). `mutations/fields.py::_synthesized_mutation_signature` consults the seam at fields.py:184 (`_lazy_ref(mutation_cls.input_type_name(meta), mutation_cls.input_module_path)`); the form bases override it via `forms/inputs.py::form_input_type_name`. No leftover twin: `grep` finds NO `def _input_type_name` anywhere in the package, and none in `mutations/`/`fields.py` (the `_input_type_name_for` hits are the unrelated filters/orders FilterSet/OrderSet helper, a different symbol). No `TODO(spec-038` anchor survives anywhere in the source tree. The transient duplication was the Slice-2→Slice-3 staged item; the Slice-3 rewire deleted the `fields.py` twin (confirmed by the sets.py:596-598 docstring) — there is now one body, exactly as the finding prescribed.
- [x] **A model-less branch leaking into `_bind_mutation`.** Decision 6 (lines 1163-1174) explicitly rejects relaxing the model requirement to fold the plain form into `bind_mutations`. Prevent: the plain form gets its OWN `bind_form_mutations()`; `_bind_mutation` stays model-only (no `if model is None` branch).
  - **Investigation (Worker 1):** Already addressed (risk genuinely avoided — no consolidation work needed). `_bind_mutation` (`mutations/sets.py:994-1025`) is unconditionally model-only: it does `primary_type = _resolve_primary_type(mutation_cls, meta.model)` (`:1004`) with NO `if model is None` / model-less guard, and grep over `mutations/sets.py` finds no model-less or plain-`DjangoFormMutation` bind branch (the lone `if model is None:` at `:495` is inside `DjangoMutation._validate_meta`, not the bind). The `ModelForm` flavor rides this for free because it IS a `DjangoMutation` subclass with `meta.model` resolvable; the input step only swaps the materialization via the `build_input` seam (`:1009`), not the model requirement. The plain form gets its OWN bind exactly as prescribed: `forms/sets.py::bind_form_mutations()` (`:811-825`) clears `_form_shape_build_cache` then drains the DISJOINT plain-form ledger via `iter_form_mutations()` (`:824`), calling `_bind_form_mutation` (`:784`) per class — it never touches `iter_mutations()`/`_bind_mutation` and never imports `bind_mutations`. `bind_mutations()` (`mutations/sets.py:1028-1039`) drains only `iter_mutations()`. The two bind paths and their ledgers stay fully separate; nothing to consolidate.
- [x] **The form `_validate_meta` re-implementing the Slice-1 narrowing validation.** `Meta.fields`/`Meta.exclude` mutual-exclusion + bare-string/duplicate/unknown-name fail-loud already lives in `forms/inputs.py::resolve_effective_form_fields` / `normalize_form_field_sequence` (Slice 1, lines 173-268). The form `_validate_meta` reuses those (it validates `form_class.base_fields`), not a re-spelled copy.
  - **Investigation (Worker 1):** Already addressed (risk genuinely avoided — no consolidation work needed). Both `_validate_meta` overrides delegate the narrowing fail-loud to a single thin wrapper, `forms/sets.py::_resolve_effective_form_field_names(form_class, *, fields, exclude)` (`forms/sets.py:209-224`), whose entire body is `resolve_effective_form_fields(form_class, fields=fields, exclude=exclude)` — the Slice-1 machinery (mutual exclusion, bare-string/duplicate rejection, unknown-name rejection against `form_class.base_fields`, empty-set guard). The `DjangoModelFormMutation._validate_meta` calls it at `forms/sets.py:384`; the plain `DjangoFormMutation._validate_meta` calls it at `forms/sets.py:655`. Neither override re-spells the mutual-exclusion / fail-loud logic — there is no hand-written `if fields and exclude`, no bare-string/duplicate/unknown-name loop, and no second `base_fields` membership check anywhere in `forms/sets.py` (grep confirms the only narrowing call sites route through `_resolve_effective_form_field_names`, which in turn imports `resolve_effective_form_fields` from `.inputs` at `forms/sets.py:70`). The wrapper exists only to coerce the return to a `tuple` and document the delegation; it is not a copy. Built this way in the Slice-2 commit, exactly as the finding's prevention prescribed; nothing to consolidate.

---

## Build: Slice 3 — the form resolver pipeline + `DjangoMutationField` exposure

_Source: `docs/builder/bld-slice-3-resolver_pipeline_field_exposure.md`_

**Existing patterns reused (cite file:line — pin-at-write-time):**

- [x] `django_strawberry_framework/mutations/resolvers.py::_locate_instance` (now `locate_instance`, lines 564-585) — the visibility-scoped `update` locate (`apply_type_visibility_sync(target_type, initial_queryset(target_type), info).get(pk=node_id)`, `DoesNotExist` → `None`). **Called by name** from `forms/resolvers.py`'s update branch (Decision 8 step 2 / helper-reuse paragraph lines 1583-1606). A security contract — must not be re-implemented.
  - **Investigation (Worker 1):** Already addressed. The Slice-3 underscore-drop promotion (New helpers justified #1) has landed: the helper is now the public `locate_instance(target_type, node_id, info)` at `mutations/resolvers.py:564-585`, carrying the single visibility-scoped body — `apply_type_visibility_sync(target_type, initial_queryset(target_type), info, _MUTATION_ASYNC_RECOURSE).get(pk=node_id)` with `DoesNotExist` → `None` (`:576-585`). It is the ONE shared implementation: the `036` model pipeline calls it at `mutations/resolvers.py:933` and `:986`, and the form update branch calls it BY NAME at `forms/resolvers.py:397` (`instance = locate_instance(primary_type, node_id, info)`), importing it from `..mutations.resolvers` at `forms/resolvers.py:101`. No re-implementation of the visibility query exists in `forms/resolvers.py` (grep for `locate_instance` finds only the import + the one call; grep for a bespoke `apply_type_visibility_sync(...).get(pk=` in `forms/` finds nothing). The security contract is preserved single-sited; the finding's bullet cited the pre-promotion private name `_locate_instance` (corrected in place to `locate_instance`). Nothing to consolidate.
- [x] `django_strawberry_framework/mutations/resolvers.py::_coerce_lookup_id` (now `coerce_lookup_id`, lines 1080-1114) — the server-side `id:` decode + type-check against the target model (`decode_model_global_id` → `(pk, None)` or `(None, FieldError)`), reused for the `ModelForm` update's top-level `id:` (Decision 8 step 2 lines 1475-1486). The `id:` arrives as a raw GlobalID **string** (the `node(id: ID!)` contract), distinct from a relation `data:` field's Strawberry-coerced `GlobalID`.
  - **Investigation (Worker 1):** Already addressed. The Slice-3 underscore-drop promotion (New helpers justified #1) has landed: the helper is the public `coerce_lookup_id(id, target_type)` at `mutations/resolvers.py:1080-1114`, carrying the single decode body — `decode_model_global_id(id, model_for(target_type))` mapping `DECODE_FAILED`/`WRONG_MODEL` → `_invalid_lookup_id_error()`, `UNCOERCIBLE_PK` → `not_found_error()`, else `(result.pk, None)`. It is the ONE shared implementation: the `036` model pipeline calls it at `mutations/resolvers.py:930` (create/n-a) and `:983` (update/delete locate), and the form update branch calls it BY NAME at `forms/resolvers.py:394` (`node_id, id_error = coerce_lookup_id(id, primary_type)`), importing it from `..mutations.resolvers` at `forms/resolvers.py:100`. No bespoke `decode_model_global_id(id, ...)` top-level `id:` decode exists in `forms/`. The finding cited the pre-promotion private name `_coerce_lookup_id` (corrected in place to `coerce_lookup_id`). Nothing to consolidate.
- [x] `django_strawberry_framework/mutations/resolvers.py::_not_found_error` (now `not_found_error`, lines 1117-1119) — the `id`-keyed not-found `FieldError` (no existence leak). Reused for both the `coerce_lookup_id` UNCOERCIBLE_PK return shape and the `locate_instance` miss.
  - **Investigation (Worker 1):** Already addressed. Promoted alongside `coerce_lookup_id` to the public `not_found_error()` at `mutations/resolvers.py:1117-1119` (`FieldError(field="id", messages=["No matching row found."])`). Single-sited and reused everywhere: `coerce_lookup_id`'s `UNCOERCIBLE_PK` return (`:1113`), the model-pipeline locate miss (`:935`, `:988`), and the form-pipeline locate miss (`forms/resolvers.py:399`), imported from `..mutations.resolvers` at `forms/resolvers.py:102`. No parallel not-found `FieldError` literal in `forms/`. The finding cited the pre-promotion private names `_not_found_error`/`_coerce_lookup_id`/`_locate_instance` (corrected in place). Nothing to consolidate.
- [x] `django_strawberry_framework/mutations/resolvers.py::authorize_or_raise` (lines 997-1036) — the write-auth gate (`check_permission` → `False` → top-level `GraphQLError`, async-in-sync rejected). Reused for both flavors (before validation for `create`, after the locate for `update`). NOTE: it constructs `mutation_cls()` and reads `mutation_cls._primary_type.__name__` for the denial message — for the **plain form** `_primary_type is None` (Slice 2 `DjangoFormMutation._primary_type = None`), so the form pipeline must call `authorize_or_raise` only via a path that does not dereference `_primary_type` for the plain flavor, OR the plain pipeline supplies its own auth-denial message. See Implementation discretion item "plain-form auth message".
  - **Investigation (Worker 1):** Already addressed. The gate is the single shared public helper `mutations/resolvers.py::authorize_or_raise` (promoted from `_authorize_or_raise` per Slice 3 underscore-drop), called by all three `036` model sites (`resolvers.py:899/937/990`) AND both form pipelines (`forms/resolvers.py:405` ModelForm body, `forms/resolvers.py:448` plain-form body via `import` at `forms/resolvers.py:98`) — no re-implemented gate exists. The plain-form `_primary_type is None` case is handled inside the one helper at `resolvers.py:1035` with `target_name = getattr(mutation_cls._primary_type, "__name__", mutation_cls.__name__)`: on a plain `DjangoFormMutation`, `_primary_type` is `None`, `None` has no `__name__` attribute, so `getattr(..., default)` returns the fallback `mutation_cls.__name__` — crash-free, no `_primary_type.__name__` dereference, no duplicated message path. Both wrinkles flagged by the finding are resolved in current source; nothing for Worker 2/future Worker 1 to consolidate.
- [x] `django_strawberry_framework/mutations/resolvers.py::refetch_optimized` (lines 734-775) — the by-pk-without-visibility optimizer re-fetch (`force_load=False` for create/update; the G2 gate keeps `select_related`/`prefetch_related`, suppresses `.only(...)` because the op is a MUTATION; routes through `apply_connection_optimization` with `mutation_payload_child_selections(slot)`). Reused unchanged by the `ModelForm` re-fetch (Decision 9 lines 1642-1664). Decision 9 explicitly says this comes for free — do NOT add new optimizer code.
  - **Investigation (Worker 1):** Already addressed. The re-fetch is the ONE shared helper, promoted by the Slice-3 underscore-drop (New helpers #1) from `_refetch_optimized` to the public `refetch_optimized(target_type, pk, info, *, force_load)` at `mutations/resolvers.py:734-775` — its single body still owns the by-pk-without-visibility query (`initial_queryset(target_type).filter(pk=pk)`, `:765`), the slot-aware optimizer routing (`apply_connection_optimization(..., selection_extractor=mutation_payload_child_selections(slot))`, `:766-771`), and the `force_load` split. The `036` model pipeline calls it at `resolvers.py:872` (`force_load=False`, create/update) and `:992` (`force_load=True`, delete snapshot). The `ModelForm` re-fetch calls it BY NAME — `obj = refetch_optimized(primary_type, form.instance.pk, info, force_load=False)` (`forms/resolvers.py:429`), importing it from `..mutations.resolvers` (`forms/resolvers.py:103`). No optimizer code exists in `forms/`: grep for `apply_connection_optimization` / `mutation_payload_child_selections` / a bespoke `.filter(pk=...)` optimizer plan in `forms/resolvers.py` finds only the docstring note and the one `refetch_optimized` call, never a re-implementation. Decision 9's "comes for free" landed exactly: the G2 gate (keep `select_related`/`prefetch_related`, suppress `.only(...)` under MUTATION) is inherited by call, no new optimizer code. The finding cited the pre-promotion private name `_refetch_optimized` (corrected in place to `refetch_optimized`); line range `734-775` is unchanged. Nothing to consolidate.
- [x] `django_strawberry_framework/mutations/resolvers.py::build_payload` (lines 778-792) + `mutations/inputs.py::payload_object_slot` (line 539) — the uniform-slot envelope return (`payload_cls(**{slot: obj, "errors": errors})`). Reused by the `ModelForm` flavor. The plain form has NO object slot (a `{ ok errors }` payload), so it does NOT call `build_payload` (which keys on `slot`); it instantiates its pinned two-field payload directly with `ok=` + `errors=`. See Implementation discretion "plain-form payload instantiation".
  - **Investigation (Worker 1):** Already addressed (in the Slice-3 build itself). The helper was promoted `_build_payload` → `build_payload` (the underscore-drop, Decision 8 #1) and lives at `mutations/resolvers.py:778-792` with the exact uniform-slot body `payload_cls(**{slot: obj, "errors": errors})`; `payload_object_slot` is already-public at `mutations/inputs.py:539`. The ModelForm body reuses it by call — `forms/resolvers.py::_run_modelform_pipeline_sync` recomputes `slot = payload_object_slot(primary_type)` (`forms/resolvers.py:387`) and routes every envelope return through `build_payload(payload_cls, slot, …)` (`:396`, `:399`, `:409`, `:423`, `:427`, `:430`), imported at `:99`. The plain form has NO object slot and correctly does NOT call `build_payload`: `_run_plain_form_pipeline_sync` (`forms/resolvers.py:433-470`) instantiates its pinned two-field payload directly as `payload_cls(ok=…, errors=…)` (`:458`, `:464`, `:468`, `:470`), with the docstring at `:436-438` stating "no object slot … instantiated directly (NOT via `build_payload`, which keys on `slot`)". No re-implementation of the slot logic on the plain path and no duplicate envelope builder — exactly the finding's prescription. Nothing to consolidate.
- [x] `django_strawberry_framework/mutations/resolvers.py::validation_error_to_field_errors` (lines 673-691) — the `ValidationError` → `FieldError` envelope mapper (uses `exc.error_dict`, keys `NON_FIELD_ERRORS` → `NON_FIELD_ERROR_KEY` = `"__all__"`). **Reused per Decision 8 step 4 (lines 1545-1556)**: the form pipeline calls `validation_error_to_field_errors(ValidationError(form.errors.as_data()))` — `form.errors.as_data()` yields the `{field: [ValidationError, …]}` shape the mapper's `error_dict` branch already consumes, so the form's `NON_FIELD_ERRORS` bucket lands on `"__all__"` byte-identically to a model `full_clean()` failure. No parallel mapper.
  - **Investigation (Worker 1):** Already addressed. The mapper is the single shared public helper `validation_error_to_field_errors` at `mutations/resolvers.py:673`, and the form pipeline's `_form_errors_to_field_errors` at `forms/resolvers.py:361-373` reuses it via `validation_error_to_field_errors(ValidationError(form.errors.as_data()))` — no parallel form-errors mapper exists. (Corrected the leading symbol name to drop the now-stale underscore; it is already promoted in source.)
- [x] `django_strawberry_framework/mutations/resolvers.py::save_or_field_errors` (lines 1061-1077) — `save()` the instance; map a race `IntegrityError` to the `"__all__"` envelope else `None` (Major-2). **Reused per Decision 8 step 5 (lines 1564-1576, P1)**: the form `form.save()` (ModelForm) and the plain-form `perform_mutate` default-save path run through this so a post-validation `IntegrityError` returns the same null-object/`{ok:false}` `FieldError` envelope, never a top-level `GraphQLError`. NOTE: `_save_or_field_errors(instance)` calls `instance.save()` — the form pipeline needs to wrap `form.save()` (no `instance` until the form saves) / `perform_mutate`, so the promotion must expose a callable-wrapping form (see "New helpers justified" #2 / Implementation discretion).
  - **Investigation (Worker 1):** Already addressed. The "New helpers justified" #1 wrinkle option (b) has landed verbatim: the helper is the public, callable-wrapping `save_or_field_errors(save_callable)` at `mutations/resolvers.py:1061-1077` — its body is `try: save_callable() except IntegrityError: return _integrity_error_field_errors()` else `return None`, the ONE `IntegrityError` → envelope catch (the still-private `_integrity_error_field_errors` at `:694` is the single message-policy source, not promoted). All three save paths pass a zero-arg callable into it: the `036` model pipeline at `mutations/resolvers.py:867` (`save_or_field_errors(instance.save)`), the form ModelForm branch at `forms/resolvers.py:425` (`save_or_field_errors(form.save)`, imported at `:105`), and the plain-form branch at `forms/resolvers.py:466` (`save_or_field_errors(lambda: instance.perform_mutate(form, info))`). No second `IntegrityError` catch exists in `forms/resolvers.py` (grep for `IntegrityError` there finds only docstring references). The finding cited the pre-promotion private name `_save_or_field_errors` (lines 1057-1063); corrected in place to `save_or_field_errors` (lines 1061-1077). Nothing to consolidate.
- [x] `django_strawberry_framework/mutations/resolvers.py::_run_pipeline_sync` (lines 795-829) + `resolve_mutation_sync`/`resolve_mutation_async` (lines 1133-1170) — the `with transaction.atomic():` body + the async `sync_to_async(_run_pipeline_sync, thread_sensitive=True)` wrapper. The form pipeline MIRRORS this exact boundary shape (one `transaction.atomic()`, async runs the sync body in one `sync_to_async(thread_sensitive=True)`, spec Decision 8 lines 1583-1626) — it is a structural parallel, NOT a call (the form body differs), so it is a deliberate same-shape sibling, not duplication to consolidate (the `036` body dispatches on `meta.operation` to model branches the form pipeline does not share).
  - **Investigation (Worker 1):** Not real. Confirmed a same-shape sibling, not extractable duplication. Current source: the model boundary is `_run_pipeline_sync` (resolvers.py:795-829, `with transaction.atomic():` then `meta.operation` dispatch to `_run_create`/`_run_update`/`_run_delete`), entered by `resolve_mutation_sync` (:1147-1160) / `resolve_mutation_async` (:1163-1184). The form boundary is `_run_form_pipeline_sync` (forms/resolvers.py:473-488, branches on `_primary_type is None` to `_run_plain_form_pipeline_sync` :433-470 / `_run_modelform_pipeline_sync` :376-430, each with its OWN `with transaction.atomic():`), entered by `resolve_form_sync` (:491-506) / `resolve_form_async` (:509-530). The only byte-identical text across the two modules is the async wrapper expression `await sync_to_async(<sync_body>, thread_sensitive=True)(mutation_cls, info, data, id)` (and the trivial sync alias `return <sync_body>(mutation_cls, info, data, id)`) — differing solely in which sync body is named. Extracting a shared `_run_in_thread(sync_callable, …)` would consolidate one `sync_to_async(...)(...)` call expression parameterized only by callee: premature abstraction at N=2 that hides the spec-036 AR-M4 / Decision-8 thread-safety contract each module documents in its own docstring. The `transaction.atomic()` boundary cannot be lifted — its body is entirely divergent dispatch (operation branches vs. primary-type branch). The shape parallel is deliberate (Decision 8 lines 1583-1626 directs the form path to mirror the `036` boundary), and the four cross-module-consumed pipeline *helpers* that the form bodies genuinely reuse (`coerce_lookup_id`/`locate_instance`/`authorize_or_raise`/`build_payload`/`save_or_field_errors`/`refetch_optimized`/`not_found_error`) are already shared by call (the preceding `- [x]` findings). Nothing left to consolidate here.
- [x] `django_strawberry_framework/mutations/resolvers.py::_coerce_relation_pk_or_none` (lines 492-517) — coerce a raw pk through the target's pk field (`to_python` then `run_validators`; uncoercible/out-of-range → `None`). **A primitive the form relation decoder reuses for the raw-pk branch** (Decision 7 lines 1296-1306 / Decision 8 step 1 lines 1456-1474), so an uncoercible raw pk never reaches the `pk__in` query as a raw backend error.
  - **Investigation (Worker 1):** Already addressed. `_coerce_relation_pk_or_none` is the single shared primitive at `mutations/resolvers.py:492-517` (still private — a primitive, not a promoted helper; the `036` `_decode_relation_id_set` consumes it same-module at `:549`). The form relation decoder reuses it BY NAME: `forms/resolvers.py` imports `_coerce_relation_pk_or_none` from `..mutations.resolvers` (`:97`) and calls it on the raw-pk branch of `_decode_form_relation_single` — `pk = _coerce_relation_pk_or_none(related_model, value)` at `:216`, with `None` → field-keyed `_relation_field_error(graphql_name)` (`:217-218`) before the visible-object query (`:220`), so an uncoercible/out-of-range raw pk never reaches the backend query. No re-implementation in `forms/` (grep finds only the import, the one call site, and docstring references at `:30`/`:198`). Exactly the finding's "primitive reused for the raw-pk branch" shape; nothing to consolidate.
- [x] `django_strawberry_framework/relay.py::decode_model_global_id` (lines 202-225) + `GlobalIDDecode` (lines 174-186) — decode + type-check a `GlobalID` against the expected model, returning `DecodeResult(status, pk, resolved_type)`. **The Relay-branch primitive the form relation decoder reuses** (Decision 7 lines 1296-1306): a non-`OK` status → field-keyed `FieldError`; an `OK` status yields the coerced pk the visibility query then resolves to the visible object.
  - **Investigation (Worker 1):** Already addressed. Slice 3 has landed; the primitive is reused BY NAME, not re-implemented. `decode_model_global_id` + `GlobalIDDecode` stay the single source at `relay.py::decode_model_global_id` / `relay.py::GlobalIDDecode`. The form relation decoder `forms/resolvers.py::_decode_form_relation_single` imports `from ..relay import GlobalIDDecode, decode_model_global_id` (`forms/resolvers.py` #"from ..relay import GlobalIDDecode") and on the `isinstance(value, relay.GlobalID)` branch calls `result = decode_model_global_id(value, related_model)` then `if result.status is not GlobalIDDecode.OK: return None, _relation_field_error(graphql_name)`, else takes `result.pk` — exactly the non-`OK` → field-keyed `FieldError` / `OK` → coerced-pk shape the finding prescribes. The same primitive is reused by the `036` model pipeline (`mutations/resolvers.py::_decode_relation_id_set` #"result = decode_model_global_id(value, expected_model)" and `coerce_lookup_id` #"result = decode_model_global_id(id, target_model)"). No bespoke GlobalID decode + model-check + pk-coercion body exists in `forms/`. Nothing to consolidate.
- [x] `django_strawberry_framework/utils/querysets.py::apply_type_visibility_sync` (line 150) / `initial_queryset` (line 108) / `model_for` (line 94) / `reject_async_in_sync_context` (line 58) — the visibility-query + sync-misuse-discipline primitives. The form relation decoder runs `apply_type_visibility_sync(related_type, initial_queryset(related_type), info, <recourse>)` on **every** branch (the net-new code below); the same primitives `_relation_visibility_error` (resolvers.py lines 445-489) already uses, so the visibility query shape is identical — but the form decoder must return the **visible object** (to apply `to_field_name`), not just confirm membership, so it cannot call `_relation_visibility_error` directly (which returns only an error/None). See "New helpers justified" #3.
  - **Investigation (Worker 1):** Already addressed. The form relation decoder reuses the `utils/querysets.py` primitives, not a parallel visibility query. `forms/resolvers.py` imports `apply_type_visibility_sync, initial_queryset` from `..utils.querysets` (`:108`) and the object-returning visibility query lives single-sited in `_visible_related_object(related_model, pk, info)` (`forms/resolvers.py:137-169`), which runs `apply_type_visibility_sync(related_type, initial_queryset(related_type), info, _FORM_ASYNC_RECOURSE).filter(pk=pk).first()` (`:163-169`) — byte-identical query shape to `mutations/resolvers.py::_relation_visibility_error` (`:480-482`), as the finding predicted. As predicted, it does NOT call `_relation_visibility_error` (which returns error/None, not the object); it returns the visible object so `_to_form_key_value` can apply `to_field_name`. The sync-misuse discipline is inherited through `apply_type_visibility_sync` (an async `get_queryset` raises `SyncMisuseError` with the surface-specific `_FORM_ASYNC_RECOURSE`); `model_for`/`reject_async_in_sync_context` are inside that primitive, not duplicated. Grep confirms no bespoke `apply_type_visibility_sync`/`initial_queryset` outside `forms/resolvers.py` and no re-implemented visibility query in `forms/`. Nothing to consolidate.
- [x] `django_strawberry_framework/registry.py::registry.get` (line 221) — resolve the related model's primary `DjangoType` (`registry.get(related_model)`). The decode basis MUST AGREE with the Slice-1 input id basis (carry-forward + Decision 7 lines 1205-1218): `column.related_model` for a **model-backed** relation (the `ModelForm` path, `forms/inputs.py::_field_triple_and_spec` line 382), `field.queryset.model` for a **model-less** plain-`Form` relation (`forms/inputs.py::_model_less_relation_annotation` line 339). The decoder resolves the same related primary type by the same basis so the input id type and the decode visibility query agree.
  - **Investigation (Worker 1):** Already addressed. Slice 3 has landed and the decode reuses `registry.get` from ONE site, keyed on a related-model basis that agrees with the Slice-1 input basis. The decoder resolves the related model uniformly off the form field — `related_model = form_field.queryset.model` at `forms/resolvers.py::_decode_form_relation_single` (`:209`) — then `_visible_related_object(related_model, pk, info)` (`:220`) runs the single `from ..registry import registry; related_type = registry.get(related_model)` (`:156-158`) before the visibility query `apply_type_visibility_sync(related_type, initial_queryset(related_type), info, _FORM_ASYNC_RECOURSE).filter(pk=pk).first()` (`:163-169`). This single `form_field.queryset.model` basis agrees in value with the Slice-1 two-branch split the finding cites: for a model-backed `ModelForm` relation the field is a `ModelChoiceField`/`ModelMultipleChoiceField` whose `.queryset.model` is exactly `column.related_model` (Django builds it from `_default_manager`), so it equals the basis at `forms/inputs.py::_field_triple_and_spec` (`registry.get(column.related_model)`, `:371`); for a model-less plain-`Form` relation it is the same `field.queryset.model` basis as `forms/inputs.py::_model_less_relation_annotation` (`related_model = field.queryset.model; registry.get(related_model)`, `:328-329`). The input id type (`GlobalID` vs raw-pk scalar) and the decode visibility query therefore target the same primary `DjangoType`. There is no divergent or duplicated decode basis to consolidate — the decode is *more* single-sourced than the input side (one uniform `form_field.queryset.model` basis and one `registry.get` call, vs. the input side's two generation-time call sites which legitimately key on `column.related_model` / `field.queryset.model` because they answer a different question — which model's primary determines the id scalar). The `inputs.py` two-site split is intentional sibling code (model-backed routes through the `models.Field`-keyed `relation_input_annotation`, model-less cannot reach it), not duplication. Nothing to consolidate.
- [x] `django_strawberry_framework/forms/converter.py` — the four `kind` constants (`SCALAR` / `RELATION_SINGLE` / `RELATION_MULTI` / `FILE`, lines 58-61) + the `FormInputFieldSpec(input_attr, graphql_name, form_field_name, kind)` reverse-map record (lines 64-87). **The decode `kind`-split addresses these constants as the single source of truth** (no bare-string `kind` literals in `forms/resolvers.py`).
  - **Investigation (Worker 1):** Already addressed. The four `kind` constants and the `FormInputFieldSpec` record are the single source of truth at `converter.py:58-61` / `64-87`, and the decode `kind`-split in `forms/resolvers.py` addresses them by import — `from .converter import FILE, RELATION_MULTI, RELATION_SINGLE, FormInputFieldSpec` (`forms/resolvers.py:109`). The `_decode_form_data` split keys every branch on `spec.kind` against the imported constants: `RELATION_SINGLE` / `RELATION_MULTI` route to the relation decoders (`:293-307`), `FILE` routes to `provided_files` (`:308-309`), and `SCALAR` is the residual `else` (`:310-311`) — legitimately the one un-imported constant since it is the fall-through, not a compared literal. `_non_file_form_field_names` likewise filters on `spec.kind != FILE` (`:325`). Grep confirms ZERO bare-string kind literals (`"scalar"` / `"relation_single"` / `"relation_multi"` / `"file"`) in `forms/resolvers.py`, and no parallel spec class — the reverse map is consumed as `FormInputFieldSpec` instances off the bind-stashed `mutation_cls._input_field_specs` (`:277-279`). Exactly the finding's prescription; nothing to consolidate.
- [x] `django_strawberry_framework/forms/inputs.py::build_form_input_class` (lines 421-475) returns `(input_cls, field_specs)` where `field_specs: list[FormInputFieldSpec]` is the per-field reverse map. **Slice 2's bind discards `field_specs`** (`DjangoModelFormMutation.build_input` / `DjangoFormMutation.build_input` return only `input_cls`, `forms/sets.py` lines 330-366 / 579-602). The decode NEEDS the spec list keyed by input attr — so Slice 3 must **stash the `field_specs` on the bound mutation** (e.g. `mutation_cls._input_field_specs`) at bind time. See "New helpers justified" #4 (the bind-stash seam) — this is the load-bearing plumbing that lets the decode reach the reverse map.
  - **Investigation (Worker 1):** Already addressed (the bind-stash seam landed exactly as #4 option (a) prescribed; the decode reads it, nothing re-derived at resolve). `build_form_input_class` still returns `(input_cls, field_specs)` with `field_specs: list[FormInputFieldSpec]` (`forms/inputs.py:410-464`). The specs are now threaded through the cache and stashed at bind, NOT discarded: `_cached_build_form_input` (`forms/sets.py:143-188`) caches the `(input_cls, field_specs)` pair (cache value typed `tuple[type, list]`, `_form_shape_build_cache` at `:135`) so the reverse map survives the per-shape dedupe; both `build_input` overrides receive `(input_cls, field_specs)` and assign `cls._input_field_specs = field_specs` after materialize — `DjangoModelFormMutation.build_input` (`forms/sets.py:441-449`, declared attr `_input_field_specs: list | None = None` at `:410`) and `DjangoFormMutation.build_input` (`forms/sets.py:690-698`, declared attr at `:577`). The decode reads the stash: `_decode_form_data` builds `spec_by_attr = {spec.input_attr: spec for spec in mutation_cls._input_field_specs}` (`forms/resolvers.py:277-279`) and routes by `spec.kind`; `_non_file_form_field_names` likewise iterates `mutation_cls._input_field_specs` (`:325`). No `build_form_input_class` / `build_form_inputs` call exists at resolve time (grep over `forms/resolvers.py` finds neither) — the resolver only reads the bound-time stash. The finding's "Slice 2's bind discards `field_specs`" premise was the pre-Slice-3 state; the plumbing is in place. The cited `build_form_input_class` line range `421-475` drifted to `410-464`, and the discarding `build_input` overrides the finding cites at `forms/sets.py` lines `330-366` / `579-602` are now the stashing overrides at `:413-450` / `:674-699`. Nothing to consolidate.
- [x] `django_strawberry_framework/mutations/fields.py::_resolve` (lines 226-233) / `_synthesized_mutation_signature` (lines 137-192) / `_validate_mutation_target` (lines 64-98) — the three generalization sites. Each already has its Slice-2 seam default: `DjangoMutation.input_type_name(meta)` (= the relocated `_input_type_name` body) + `DjangoMutation.input_module_path` (= `INPUTS_MODULE_PATH`) + `DjangoMutation.resolve_sync`/`resolve_async` (= delegate to `mutations/resolvers.py`). The generalization re-points `fields.py` at the seams; the model default is byte-behavior-identical.
  - **Investigation (Worker 1):** Already addressed. Slice 3 landed all three generalizations in current source; `fields.py` consults the seams on every flavor and the model default is byte-behavior-identical. (i) `_validate_mutation_target` (`fields.py:64-98`) is duck-typed via `_has_mutation_protocol` (`:101-118`) — it checks `hasattr(_mutation_meta)` + callable `resolve_sync`/`resolve_async` + callable `input_type_name` + non-`None` `input_module_path`, NOT `issubclass(DjangoMutation)`, so the plain model-less `DjangoFormMutation` (not a `DjangoMutation` subclass) and `DjangoModelFormMutation` both pass alongside the `036` `DjangoMutation`; no form-base import (would close the `forms/sets.py → mutations/sets.py` load cycle). (ii) `_synthesized_mutation_signature` (`:137-192`) builds the `data:` lazy-ref via `_lazy_ref(mutation_cls.input_type_name(meta), mutation_cls.input_module_path)` (`:184`) — the per-flavor seam, not a hardcoded name — while the PAYLOAD-return ref stays pinned to `INPUTS_MODULE_PATH` (`:190`, the Decision-5 namespace divergence); `id`/`data` arg gating keys on `meta.operation` (`update`/`delete` get `id`; non-`delete` gets `data`, so the `"form"` sentinel gets `data` only). (iii) `_resolve` (`:226-233`) dispatches through `mutation_cls.resolve_async`/`resolve_sync` (`:232-233`) and gates the `id=` kwarg on `takes_id = meta.operation != "form"` (`:224`) so the plain form's `(info, *, data)` seam is never passed an `id`. The model-flavor seam defaults are present and byte-identical: `DjangoMutation.input_module_path = INPUTS_MODULE_PATH` (`mutations/sets.py:568`) and `input_type_name(meta)` delegating to `mutation_input_type_name` (`mutations/sets.py:585-610`, the relocated `_input_type_name` body — the transient twin deleted), with the form flavors overriding to `FORMS_INPUTS_MODULE_PATH` / `form_input_type_name` (`forms/sets.py:405/456`, `:580/753`). No duplicated or forked field machinery exists — `fields.py` has ONE `_resolve` / ONE `_synthesized_mutation_signature` / ONE `_validate_mutation_target`, each parameterized by the seam, accepting both model and form targets. The finding cited pre-Slice-3 line ranges (`_resolve` 217-222, `_synthesized_mutation_signature` 148-191, `_validate_mutation_target` 70-92); corrected in place to current ranges. Nothing to consolidate.
- [x] `tests/mutations/test_fields.py` (the `_Query` / `CategoryT` / `ItemT` / `CreateItem` / `UpdateItem` / `DeleteItem` fixtures, lines 50-104; the registry-isolation harness; `test_non_mutation_target_raises_at_construction` line 233) — extend with a `DjangoModelFormMutation` + plain `DjangoFormMutation` target-accept test. `tests/forms/test_resolvers.py` (new) reuses the Slice-1/Slice-2 form fixtures (the `ModelForm` over products `Item`, plain `Form`, `_make_relay_target`/`_make_non_relay_target`, kwarg-requiring form) + the `tests/mutations/test_resolvers.py` resolver-harness shape (the `info` stub, the `_AllowAll`/deny permission fixtures, the mocked-`save()` `IntegrityError` test at line 614).
  - **Investigation (Worker 1):** Not real. This is a TEST-PLACEMENT finding, and AGENTS.md mandates exactly the parallel-per-tree shape it describes: "tests/ … per-tree" fixtures with **no cross-tree helper imports** (each app/tree "carries its OWN coverage"). Grep confirms zero `from tests…`/cross-tree imports in `tests/forms/test_resolvers.py`, `tests/mutations/test_fields.py`, or `tests/mutations/test_resolvers.py`; the only conftest is the global `tests/conftest.py` (the async-sqlite ResourceWarning cleanup, unrelated). The "reused-shape" items the finding lists are deliberately re-spelled per tree, not importable duplication: the `_isolate_registry` autouse fixture is byte-near-identical but carries a tree-specific docstring in each of the three files; `_AllowAll`/`_Query`/`_schema` recur with tree-local docstrings; and the unique-name helper legitimately diverges (`_category_name`/`_Cat-` in mutations vs the generic `_uniq` in forms). Both target files already landed in Slice 3 — `tests/mutations/test_fields.py::test_generalized_target_accepts_modelform_and_plain_form_family` (:260) extends the fixtures and `test_non_mutation_target_raises_at_construction` (:233) still pins the negative case; `tests/forms/test_resolvers.py` (1451 lines) re-spells the resolver-harness shape (its own `_AllowAll`/`_DenyAll`, info stub, the mocked-`save()` `IntegrityError` envelope tests at :763/:793) rather than importing it. The finding's own `_Source:_` recap frames this as "reuses … shape" / "extend with," i.e. intentional parallel extension, not extraction. Promoting any of these shapes to a shared importable helper would *violate* the AGENTS.md per-tree rule (and couple `tests/forms` to `tests/mutations`); there is nothing to consolidate without breaking the test-placement contract.

**New helpers justified (single responsibility each):**

1. **The helper-promotion mechanism — CHOSEN: drop the leading underscore on the reused subset in `mutations/resolvers.py` (the lighter edit), NOT a `mutations/_pipeline.py` lift.** Decision 8 (lines 1596-1606) offers two: (a) the lighter edit — drop the `_` on exactly the reused subset in `mutations/resolvers.py`; (b) the cleaner edit — lift them to a neutral `mutations/_pipeline.py` both modules import (mirroring the `0.0.9` set-family `utils/` lift). **Recommendation and decision: the underscore-drop.** Rationale: (i) the reused subset (`_locate_instance`, `_coerce_lookup_id`, `_not_found_error`, `_authorize_or_raise`, `_refetch_optimized`, `_build_payload`, `_validation_error_to_field_errors`, `_save_or_field_errors`) ALL still live in `mutations/resolvers.py` and are still called by the `036` model pipeline IN THE SAME MODULE — a `_pipeline.py` lift would move them out from under their primary same-module callers (`_run_create` / `_run_update` / `_run_delete` / `_validate_save_assign_refetch_payload`), forcing `mutations/resolvers.py` to import its own extracted helpers back, which reads worse, not better; (ii) the `0.0.9` set-family lift moved shared scaffold that had NO single owning module (two sibling families both needed it equally) — here `mutations/resolvers.py` is the unambiguous owner and `forms/resolvers.py` is a clean downstream consumer (a one-way `forms → mutations` import edge that already exists via `forms/sets.py` importing `mutations/sets.py`), so the directional dependency is honest; (iii) the underscore-drop is a rename-only diff (no moved bodies, no new module, no new import cycle risk), so the no-model-flavor-regression audit is a trivial "same body, public name" check. The promoted public names: `locate_instance`, `coerce_lookup_id`, `not_found_error`, `authorize_or_raise`, `refetch_optimized`, `build_payload`, `validation_error_to_field_errors`, `save_or_field_errors` (Worker 2: rename in place, update the `036` same-module call sites, keep `payload_object_slot` already-public from `mutations/inputs.py`). The module-private helpers the `036` model pipeline alone uses (`_decode_relations`, `_run_create`, `_unprovided_exclude`, `_integrity_error_field_errors`, etc.) STAY underscore-private — only the cross-module-consumed subset is promoted. Single responsibility: expose the reused-by-call surface without a new module.

   **`_save_or_field_errors` promotion wrinkle (Implementation discretion, resolved in-plan).** `_save_or_field_errors(instance)` today calls `instance.save()`. The form pipeline must wrap `form.save()` (ModelForm) and the plain-form `perform_mutate` save — neither is `instance.save()`. Two clean options: (a) promote it as-is (`save_or_field_errors(instance)`) and have `forms/resolvers.py` pass the form's saved instance pathway through a thin local that catches `IntegrityError` around the `form.save()` / `perform_mutate` call and returns `_integrity_error_field_errors()` — but that re-implements the catch, the exact duplication Decision 8 forbids; (b) **generalize the promoted helper to wrap a zero-arg callable** — `save_or_field_errors(save_callable)` runs `save_callable()` and maps `IntegrityError`, with the `036` model call site passing `instance.save` and the form call sites passing `form.save` / a `perform_mutate` bound method. **Recommendation: (b)** — it keeps ONE `IntegrityError` → envelope catch (the `036` `_integrity_error_field_errors` message policy, single-sourced) reused by all three save paths, which is precisely the "share, do not re-implement" Decision 8 mandates. `_integrity_error_field_errors` itself need NOT be promoted (it stays private; only `save_or_field_errors` is the public seam). Worker 2: if generalizing to a callable reads as churn on the `036` call site, the fallback is (a) with a SHARED `integrity_error_field_errors` promotion so the form catch reuses the message builder — but (b) is preferred (one catch, not two). Recorded as a discretion item with a strong recommendation.

2. **`forms/resolvers.py::resolve_form_sync` / `resolve_form_async` (the public entry points)** — the sync + async pipeline entries the Slice-2 `forms/sets.py` `resolve_sync`/`resolve_async` overrides delegate to (the model resolver's `resolve_mutation_sync`/`resolve_mutation_async` parallel). Single responsibility: the `with transaction.atomic():` form body (sync) + the `sync_to_async(thread_sensitive=True)` wrapper (async). The `ModelForm` flavor's entry takes `(mutation_cls, info, *, data, id)`; the plain flavor's takes `(mutation_cls, info, *, data)` (no `id` — `forms/sets.py::DjangoFormMutation.resolve_sync(cls, info, *, data)` has no `id` param). Worker 2 decides one entry pair that branches on `mutation_cls._primary_type is None` (plain) vs not, or two entry pairs — see Implementation discretion. The form body: decode → (update only) locate → authorize → construct+validate-once → write → (ModelForm) re-fetch → return payload.

3. **`forms/resolvers.py::_decode_form_relation_single` / `_decode_form_relation_multi` (the dedicated form relation decoder — the genuinely NET-NEW security code)** — Decision 7 lines 1286-1319 / Decision 8 step 1 lines 1456-1474, P1. Single responsibility: given a relation id (or id list), the related primary type, and `info`, return the **form-key value** (the visible object's `to_field_name` value) or a field-keyed `FieldError`. The body for each id: (i) if `isinstance(value, relay.GlobalID)` → `decode_model_global_id(value, related_model)`; a non-`OK` status → `FieldError` on the field's GraphQL name (the `036` AR-H4 contract); else take `result.pk`; (ii) else (raw pk) → `_coerce_relation_pk_or_none(related_model, value)`; `None` → field-keyed `FieldError`; (iii) **for BOTH branches**, resolve the **visible object** through the related primary `DjangoType.get_queryset` — `apply_type_visibility_sync(related_type, initial_queryset(related_type), info, <recourse>).filter(pk=<coerced pk>).first()`; a `None` (hidden / unseeable) → the SAME field-keyed `FieldError`, no existence leak (this is the visibility-on-every-branch that closes the raw-pk gap `_decode_relation_id_set` leaves, Decision 7 lines 1286-1306); (iv) convert the object to the form key via `to_field_name`: `obj.serializable_value(field.to_field_name)` when the form field's `to_field_name` is set, else `obj.pk` (Decision 7 lines 1308-1319 / Edge case lines 1949-1954, P2). The multi decoder maps this per element and returns the list under the form field name. This is **NOT** `_decode_relation_id_set` (which skips visibility on the raw-pk branch); it reuses the `036` *primitives* (`decode_model_global_id`, `_coerce_relation_pk_or_none`, `apply_type_visibility_sync`/`initial_queryset`) but resolves the OBJECT and visibility-checks every branch. Resolve the related model by the Slice-1-agreeing basis: for a model-backed relation, `mutation_cls`'s form `form_class._meta.model._meta.get_field(form_field_name).related_model`; for a model-less relation, the form field's `field.queryset.model`. Worker 2: the form field is reachable via `get_form_fields(form_class)[form_field_name]`, which carries both the `to_field_name` and (for model-less) the `queryset.model` — single-source the related-model basis off the form field + the optional backing column, mirroring `forms/inputs.py::_model_column_for` / `_model_less_relation_annotation`. A `SyncMisuseError` from an `async def get_queryset` propagates (the same discipline the `036` `_relation_visibility_error` inherits via `apply_type_visibility_sync`).

4. **`forms/resolvers.py::_decode_form_data` (the decode `kind`-split — NET-NEW)** — Decision 8 step 1 lines 1456-1474. Single responsibility: walk the bound input dataclass's provided fields (`UNSET` stripped), and using the per-field `FormInputFieldSpec` reverse map (the bind-stashed `mutation_cls._input_field_specs`), produce TWO form-field-keyed dicts: `provided_data` (scalars with the choice-enum member unwrapped to its raw value via the reused `036` `_raw_choice_value` discipline — Edge case lines 1926-1929; and relation values from the decoder #3, landed under `form_field_name`) and `provided_files` (the `FILE`-kind uploaded value, NEVER in `data=`). The `kind`-split (`SCALAR` / `RELATION_SINGLE` / `RELATION_MULTI` / `FILE`) is the single point routing form-field names, visibility-checked relation values, and file uploads each to the right place. Returns `(provided_data, provided_files, error)`; a relation decode `FieldError` short-circuits. This is net-new because the `036` `_decode_relations` keys on MODEL attrs (`<field>_id` → pk for `setattr`/`Model(**…)`) and never splits files out — the form decode keys on FORM-field names and a bound Django form reads files from `files=`, not `data=`.

   **The bind-stash seam (#4 plumbing).** `build_form_input_class` returns `(input_cls, field_specs)`. The Slice-2 `build_input` overrides discard `field_specs`. Slice 3 must thread them onto the mutation: either (a) `build_input` also stashes `mutation_cls._input_field_specs = field_specs` (cleaner — the bind already stashes `_input_class` / `_payload_type_name`), or (b) the resolver re-derives the spec list by re-calling `build_form_input_class` at resolve time (wasteful, re-builds a class). **Recommendation: (a)** — stash the specs at bind. Worker 2: thread the `field_specs` out of `_cached_build_form_input` / `build_input` and assign `mutation_cls._input_field_specs` in `_bind_mutation` (model-flavor path, for `DjangoModelFormMutation`) and `_bind_form_mutation` (plain path). NOTE: `_cached_build_form_input` (`forms/sets.py` lines 133-174) currently caches only `input_cls`, discarding specs — extend the cache value to `(input_cls, field_specs)` so the specs survive the dedupe. This is a `forms/sets.py` edit beyond the four stubs — flag it (it IS in the slice's spirit: the decode reverse map is the P1 contract).

5. **`forms/resolvers.py` partial-update reconstruction (NET-NEW)** — Decision 8 step 4 lines 1516-1544, P1. Single responsibility: for a `ModelForm` `update`, reconstruct the full bound payload from the located instance overlaid with the provided fields: `data = {**model_to_dict(instance, fields=<the form's non-file field names>), **provided_data}`, `files = provided_files` only. `model_to_dict` (from `django.forms.models`) supplies FK as pk under the form field name and M2M as `[pk]` — so an omitted scalar/FK/M2M is preserved; an omitted file is preserved via the bound `form_class(instance=…)`'s `initial` (never re-supplied, never cleared). The `<the form's non-file fields>` filter excludes file fields (a file column's `model_to_dict` value is not a re-bindable `data=` value). The required-extra-non-model-field rule (Edge case lines 1945-1948, P2) is already encoded in the Slice-1 `<FormClass>PartialInput` (a required non-model extra field stays required in the input), so a required extra is always in `provided_data`; an optional one may be omitted. This is net-new (the `036` update does `setattr` on the located instance, not a `model_to_dict` payload reconstruction).

6. **`forms/resolvers.py` form construction via `get_form_kwargs` / `get_form` + the plain-form `perform_mutate`** — Decision 8 step 4 lines 1496-1513 / Decision 6 lines 1114-1131. Single responsibility: the overridable `get_form_kwargs(self, info, *, data, files, instance=None) -> dict` hook (default `{"data": data, "files": files}` + `"instance": instance` when non-`None`) and the coarser `get_form(self, info, *, data, files, instance=None)` hook (default `form_class(**self.get_form_kwargs(...))`), plus `perform_mutate(self, form, info) -> None` (default calls `form.save()` when present, else no-op). **These hooks land on the form bases in `forms/sets.py`** (they are instance methods a consumer overrides), not in `forms/resolvers.py` — the resolver CALLS `mutation_cls().get_form(...)` / `mutation_cls().perform_mutate(...)`. Worker 2: add `get_form_kwargs` / `get_form` / `perform_mutate` to the appropriate bases (`get_form_kwargs` / `get_form` on both flavors; `perform_mutate` on `DjangoFormMutation` per Decision 6 — the `ModelForm` flavor writes via `form.save()` directly). This is a `forms/sets.py` addition beyond the four stubs — flag it (the spec names these hooks as the construction seam the pipeline uses).

**The `get_form_kwargs` waiver wiring (carry-forward (b)).** Slice 2 ships `build_input` passing `guard_required=True` always with two `TODO(spec-038 Slice 3)` anchors (`forms/sets.py` `DjangoModelFormMutation.build_input` lines 350-354, `DjangoFormMutation.build_input` lines 591-592). Slice 3 wires the waiver: `guard_required = not _get_form_kwargs_overridden(cls)` where `_get_form_kwargs_overridden` checks whether the concrete mutation overrides `get_form_kwargs` / `get_form` (Decision 7 lines 1388-1394 — the guard can't know which fields the override injects, so it trusts the override). Worker 2: a `cls.get_form_kwargs is not <base>.get_form_kwargs` (or `get_form`) identity check on the base default is the simplest detection; remove both `TODO(spec-038 Slice 3)` anchors when wired. The Slice-1 guard already takes the `guard_required` parameter, so this is a one-line flip per `build_input` plus the detection helper.

**Duplication risk avoided:**

- [x] **Re-implementing the `036` locate / authorize / re-fetch / payload / validation-mapper / save-mapper.** The naive form pipeline would re-spell each. Prevented by promoting the subset to public names (#1) and calling them; the `036` model pipeline and the form pipeline share ONE copy. Worker 3 confirms `forms/resolvers.py` has no body that re-implements a promoted helper.
  - **Investigation (Worker 1):** Already addressed. `forms/resolvers.py` re-implements none of the six promoted helpers — it imports and calls the single `mutations/resolvers.py` copy of each. Five are imported at the module top (`forms/resolvers.py:96-106`: `authorize_or_raise`, `build_payload`, `locate_instance`, `refetch_optimized`, `save_or_field_errors` — defined at `mutations/resolvers.py:997/778/564/734/1061`); `validation_error_to_field_errors` (`mutations/resolvers.py:673`) is imported locally inside `_form_errors_to_field_errors` (`forms/resolvers.py:371`). The pipeline bodies call them directly: `_run_modelform_pipeline_sync` (`forms/resolvers.py:376-430`) uses `locate_instance` → `authorize_or_raise` → `save_or_field_errors(form.save)` → `refetch_optimized` → `build_payload`, and `_run_plain_form_pipeline_sync` (:433-470) reuses `authorize_or_raise` / `save_or_field_errors`. `_form_errors_to_field_errors` (:361-373) is a thin adapter (`validation_error_to_field_errors(ValidationError(form.errors.as_data()))`), not a parallel mapper body. Nothing to consolidate.
- [x] **Re-implementing the relation visibility query.** The form decoder needs the visible OBJECT (for `to_field_name`), which `_relation_visibility_error` does not return, so it cannot call that helper — but it MUST reuse the same primitives (`apply_type_visibility_sync` / `initial_queryset` / `registry.get` / `decode_model_global_id` / `_coerce_relation_pk_or_none`) so the query shape and coercion are identical. Prevented: the decoder (#3) is keyed on those primitives, not a parallel query. The net-new part is genuinely net-new (object-returning + visibility-on-every-branch), NOT a copy.
  - **Investigation (Worker 1):** Already addressed (duplication risk was avoided as designed; nothing to consolidate). `forms/resolvers.py` keys the decoder on the shared `036` primitives with no parallel visibility query: `registry.get` (line 158), `apply_type_visibility_sync(related_type, initial_queryset(related_type), info, recourse)` (lines 163-168), `decode_model_global_id` + `GlobalIDDecode.OK` (lines 211-212), and `_coerce_relation_pk_or_none` (line 216). The visibility query lives once in `_visible_related_object` (lines 156-169), which returns the visible OBJECT (`queryset.filter(pk=pk).first()`) so `_to_form_key_value` can apply `to_field_name` — exactly the reason it cannot call `_relation_visibility_error` (error/None only). `_relation_visibility_error` is correctly NOT imported or called. Both relation branches (GlobalID and raw-pk) route through the one `_visible_related_object`, closing the raw-pk gap. The object-returning + visibility-on-every-branch body is genuinely net-new, not a copy of the `036` query. No source change warranted.
- [x] **A second `IntegrityError` → envelope catch.** Prevented by the callable-wrapping `save_or_field_errors` (#1 wrinkle, option b) — one catch, three save paths.
  - **Investigation (Worker 1):** Already addressed (duplication risk avoided as designed; nothing to consolidate). There is exactly ONE `except IntegrityError:` statement in the whole package — `save_or_field_errors` at `mutations/resolvers.py:1073-1077` (`try: save_callable()` / `except IntegrityError: return _integrity_error_field_errors()`). It wraps a zero-arg callable so one catch serves all three save paths: the `036` model pipeline passes `instance.save`, and the two form pipelines route through the same helper — `_run_modelform_pipeline_sync` passes `form.save` (`forms/resolvers.py:425`) and `_run_plain_form_pipeline_sync` passes `lambda: instance.perform_mutate(form, info)` (`forms/resolvers.py:466`). `forms/resolvers.py` has NO `except IntegrityError`; its only two `IntegrityError` mentions (lines 58, 440) are docstrings confirming the reuse. `_integrity_error_field_errors` (`mutations/resolvers.py:695`) is the single message-policy source. No source change warranted.
- [x] **A parallel `kind` literal set.** Prevented: the decode addresses the `forms/converter.py` `SCALAR`/`RELATION_SINGLE`/`RELATION_MULTI`/`FILE` constants, not bare strings.
  - **Investigation (Worker 1):** Already addressed (duplication risk avoided as designed; nothing to consolidate). The four kind values are defined exactly once as named module constants in `forms/converter.py:58-61` (`SCALAR = "scalar"`, `RELATION_SINGLE = "relation_single"`, `RELATION_MULTI = "relation_multi"`, `FILE = "file"`). `forms/resolvers.py` imports them (`from .converter import FILE, RELATION_MULTI, RELATION_SINGLE, FormInputFieldSpec`, line 109) and the kind-split decode references them symbolically: `spec.kind in (RELATION_SINGLE, RELATION_MULTI)` (line 293), `spec.kind == RELATION_MULTI` (line 296), `spec.kind == FILE` (line 308), and `spec.kind != FILE` (line 325). Grep for bare-string kind literals (`"scalar"`/`"relation_single"`/`"relation_multi"`/`"file"`) in `forms/resolvers.py` returns zero matches; the only `kind`-word occurrences are docstrings. No parallel literal set exists. No source change warranted.
- [x] **Re-deriving the form input name / module in `fields.py`.** The `fields.py` generalization consults `mutation_cls.input_type_name(meta)` / `input_module_path` (the Slice-2 seams), deleting the transient `_input_type_name` twin — so there is ONE name-derivation body (the model default on `DjangoMutation`, overridden by the form bases). Prevented: do NOT keep `_input_type_name` "just in case".
  - **Investigation (Worker 1):** Already addressed. Slice 3 deleted the transient twin and rewired `fields.py` to the seam. `mutations/fields.py::_synthesized_mutation_signature` builds the `data:` ref via `_lazy_ref(mutation_cls.input_type_name(meta), mutation_cls.input_module_path)` (`mutations/fields.py:184`) — one name-derivation entry point through the seam. No `_input_type_name` definition or call survives anywhere in `mutations/` (grep for `def _input_type_name`/`_input_type_name(` in `mutations/` matches only the unrelated `mutation_input_type_name`; the bare `_input_type_name` twin is gone). The single name-derivation body is the model default `DjangoMutation.input_type_name` at `mutations/sets.py:585` (delegating to `mutations/inputs.py::mutation_input_type_name`) plus `input_module_path = INPUTS_MODULE_PATH` at `mutations/sets.py:568`, overridden by the form bases at `forms/sets.py:456` and `:753` (`form_input_type_name`) with `input_module_path = FORMS_INPUTS_MODULE_PATH` at `forms/sets.py:405`/`:580`. The remaining `_input_type_name_for` symbols (`filters/inputs.py:193`, `orders/inputs.py:131`) are an unrelated filter/order-set generator, not the form/mutation twin. The bld artifact confirms the deletion (`bld-slice-3…:8`, `:90`, `:239`). Nothing to consolidate.
- [x] **Re-building the input class to recover `field_specs`.** Prevented by the bind-stash seam (#4) — the specs are stashed once at bind, read at resolve.
  - **Investigation (Worker 1):** Already addressed (the risk was avoided as designed — there is no resolve-time rebuild). The `field_specs` are produced once at bind: `_cached_build_form_input` (`forms/sets.py:143-188`) calls `build_form_input_class` / `build_form_inputs` and caches the `(input_cls, field_specs)` pair in `_form_shape_build_cache` (`:135`, `:187`), and both `build_input` overrides assign the recovered specs onto the bound class — `DjangoModelFormMutation.build_input` does `cls._input_field_specs = field_specs` (`forms/sets.py:449`, declared attr `:410`) and the plain `DjangoFormMutation.build_input` does the same (`forms/sets.py:698`, declared attr `:577`). At resolve time the decoder only reads that stash: `_decode_form_data` builds `spec_by_attr` from `mutation_cls._input_field_specs` (`forms/resolvers.py:278`) and `_non_file_form_field_names` iterates the same stash (`:325`). Grep over `forms/resolvers.py` finds NO call to `build_form_input_class` / `build_form_inputs` and no `@strawberry.input` / class construction — the resolver never rebuilds the input class to recover the reverse map. The seam is exactly #4 option (a); this risk-avoided bullet records the same in-place plumbing the #4 finding above already confirmed. Nothing to consolidate.

---

## Build: Slice 4 — the products live form surface

_Source: `docs/builder/bld-slice-4-products_live_form_surface.md`_

- [x] **Existing patterns reused (cite file:line).**
  - **Seeding (AGENTS.md non-negotiable):** every catalog/auth test opens with
    `seed_data(N)` / `create_users(N)` / `seed_cascade_split()` from
    `apps.products.services` (services.py `seed_data` line 147, `create_users` line
    259, `seed_cascade_split` line 423). Never hand-roll `Category`/`Item`/`User`.
  - **The reload fixture + HTTP helpers** in `test_products_api.py`:
    `_reload_products_project_schema` (line 42) + the autouse
    `_reload_project_schema_for_acceptance_tests` (line 73); `_post_graphql` (line 79);
    `_assert_graphql_data` (line 91); `_global_id` (line 108); `_login` (line 1788);
    `_login_with_perm` (line 112); `_staff_client` (line 100). The new form tests reuse
    ALL of these verbatim — the `apps.products.schema` reload already covers the new
    form mutations once they are wired into `products/schema.py::Mutation`, so **no new
    fixture is needed.**
  - **The model-driven mutation tests are the exact assertion-shape templates to
    mirror** for the form flavor (same envelope, same wire contract):
    - relation-visibility (hidden `Category` → `FieldError` on `categoryId`):
      `test_create_item_relation_id_for_hidden_category_is_field_error` (line 694) — the
      P1 invariant template, including the visible-public-category success contrast.
    - `categoryId` wrong-type → field-keyed error:
      `test_create_item_wrong_type_global_id_on_category_id_is_field_error` (line 584).
    - `"__all__"` sentinel on the unique constraint:
      `test_create_item_unique_constraint_envelope_uses_all_sentinel` (line 388).
    - partial-update collision on a one-field change:
      `test_update_item_partial_collision_on_unique_constraint_changing_only_name`
      (line 424) — the right-path partial-update template.
    - write-auth (anonymous denied / missing-perm denied):
      `test_create_item_anonymous_is_denied_top_level_error_no_write` (line 458),
      `test_create_item_missing_model_perm_is_denied_no_write` (line 493).
    - visibility-scoped update not-found:
      `test_visibility_scoped_update_delete_hidden_private_row_is_not_found` (line 528).
  - **The raw multipart-upload transport pattern** is fully demonstrated in
    `examples/fakeshop/test_query/test_uploads_api.py::test_multipart_create_uploads_real_files_over_http`
    (lines 204-261): the GraphQL-multipart `operations` / `map` / numbered-file POST
    via `django.test.Client`, `SimpleUploadedFile`, `override_settings(MEDIA_ROOT=...)`,
    `client.force_login`. **The new products form-backed `Upload` test copies this
    transport shape** (the `{operations, map, "0": SimpleUploadedFile(...)}` POST body),
    pointing `map` at `variables.data.<fileField>`. Cite this file:line as the reused
    pattern.
  - **The `Mutation` wiring + lazy-payload discipline** is shown in
    `apps/products/schema.py` (existing `CreateItem`/`UpdateItem`/... line 232-272 +
    the `@strawberry.type class Mutation` line 256) and `apps/scalars/schema.py`
    (`CreateMediaSpecimen` line 286 + its `Mutation` line 301). The form mutations are
    added the same way: declare the `DjangoModelFormMutation`/`DjangoFormMutation`
    subclass, expose via `DjangoMutationField`, add to the products `Mutation` type.
    `config/schema.py` already composes `Mutation(ProductsMutation, ScalarsMutation)`
    (line 35) and calls `finalize_django_types()` (line 50) — the form bind
    (`bind_form_mutations()`, Slice 2) already runs inside that finalize, so **NO
    `config/schema.py` edit is needed** as long as the form mutations land on the
    existing `apps.products.schema.Mutation` (confirmed below).
  - **The `get_form_kwargs(info, *, data, files, instance=None)` override seam** ships
    on both form bases (`forms/sets.py` `_default_get_form_kwargs` + the `get_form_kwargs`
    class attr at lines 454/701). The P2 `user`-injection form overrides it to add
    `user=<request user>`; the override ALSO waives the create-required guard
    (`_form_kwargs_overridden`, forms/sets.py line 227, consumed at lines 446/695).
  - **Investigation (Worker 1):** Already addressed (reuse-in-place; no consolidation
    opportunity). This is an "existing patterns reused" recap and every asserted reuse is
    verified live: `seed_data`/`create_users`/`seed_cascade_split` exist in
    `apps/products/services.py` and the new `*_via_form` tests open with them per the
    AGENTS.md seed-helper rule; the `_post_graphql`/`_assert_graphql_data`/`_global_id`/
    `_login`/`_login_with_perm`/`_staff_client` helpers and the autouse reload fixture
    exist in `test_products_api.py` and the new form tests route through them verbatim;
    the form mutations (`CreateItemViaForm`/`UpdateItemViaForm`/`CreateItemWithFileViaForm`/
    `CreateStampedItemViaForm`/`SubmitContact`) land on the existing
    `apps/products/schema.py::Mutation`, so no `config/schema.py` edit; the
    `get_form_kwargs` seam (`_default_get_form_kwargs` + `_form_kwargs_overridden` waiver)
    ships on both bases in `forms/sets.py`; and the single multipart test
    (`test_create_item_with_file_via_form_multipart_upload_over_http`) inlines the
    `{operations, map, "0": SimpleUploadedFile}` transport rather than extracting a
    cross-file helper, exactly as the "New helpers justified" bullet prescribed (per the
    AGENTS.md per-tree-fixture discipline a shared upload helper would wrongly couple
    `test_products_api.py` to `test_uploads_api.py`). Nothing to consolidate.

- [x] **New helpers justified.** **None in package source.** In `products/forms.py` (new
  example module) the only net-new constructs are the consumer forms themselves
  (`ItemModelForm`, a plain contact/action `Form`, the kwarg-requiring form, and the
  file-form). In `test_products_api.py` the new tests reuse every existing helper; the
  only candidate new local helper is a tiny multipart-POST wrapper, but it is a
  near-copy of `test_uploads_api.py`'s inline body — **do NOT extract a cross-file
  shared helper** (the two files have independent reload fixtures and live in the same
  tier; a shared upload helper would couple `test_products_api.py` to
  `test_uploads_api.py`). Worker 2 may inline the multipart POST in the single products
  upload test, mirroring `test_uploads_api.py`'s inline shape. If Worker 2 finds 2+
  multipart tests land in `test_products_api.py`, a file-LOCAL `_post_multipart(...)`
  helper is justified then (named condition for later extraction).
  - **Investigation (Worker 1):** Not real (deferred condition unfired). Verified in
    current source: `test_products_api.py` contains exactly **one** multipart-upload test,
    `test_create_item_with_file_via_form_multipart_upload_over_http` (line 2536), with a
    single inline `{operations, map, "0": SimpleUploadedFile(...)}` POST body (the only
    `"operations"`/`"map"`/`SimpleUploadedFile` occurrence in a test body, lines 2582-2584).
    The file-LOCAL `_post_multipart(...)` helper is justified ONLY at 2+ multipart tests; at
    N=1 there is nothing to consolidate, so extracting now would be premature abstraction. As
    the finding directs, the cross-file shared multipart helper is correctly NOT pursued (the
    two files keep independent reload fixtures in the same tier per AGENTS.md fixture
    discipline). Disposition stands as the finding prescribed: inline at N=1, revisit only if a
    second multipart test lands.

- [x] **Duplication risk avoided.** The naive risk is **copying `test_uploads_api.py`
  wholesale** (its `_png_bytes`, its read-side SDL introspection tests, its
  `CreateMediaSpecimen` model-driven path). The products upload test needs ONLY the
  multipart-transport skeleton against a **form-backed** mutation — not the read-side or
  the Pillow image machinery. Plan: a `FileField` (not `ImageField`) so no Pillow / no
  `_png_bytes`; a plain text `SimpleUploadedFile`; assert the file landed + `errors ==
  []`, nothing about width/height. Second risk: **re-deriving the wire-contract query
  strings** — instead, define module-level form-mutation query-string constants
  alongside the existing `_CREATE_ITEM` (line 142) etc., named distinctly
  (`_CREATE_ITEM_VIA_FORM`, ...), so the form field-name and `errors { field messages }`
  envelope are spelled once.
  - **Investigation (Worker 1):** Already addressed (risk avoided in current source; no
    consolidation opportunity). Both prescribed preventions are in place in
    `examples/fakeshop/test_query/test_products_api.py`. (1) The upload test
    `test_create_item_with_file_via_form_multipart_upload_over_http` (line 2536) is
    minimal, not a wholesale copy of `test_uploads_api.py`: it targets the form-backed
    `FileField` `Item.attachment` (models.py line 63), uses a plain-text
    `SimpleUploadedFile("doc.txt", b"form upload bytes")`, and asserts `errors == []` +
    the row/file landed — no `_png_bytes`, no Pillow/PIL, no image-dimension checks, no
    read-side SDL/introspection, no `CreateMediaSpecimen` path (grep for
    `_png_bytes|__schema|introspection|Pillow|PIL` in the file hits only the docstring at
    line 2547 disclaiming Pillow). (2) The repeated wire-contract query strings are
    module-level constants spelled once — `_CREATE_ITEM_VIA_FORM` (line 2102),
    `_UPDATE_ITEM_VIA_FORM` (line 2106), `_CREATE_STAMPED_ITEM_VIA_FORM` (line 2110),
    `_SUBMIT_CONTACT` (line 2114) — named distinctly from the `036` `_CREATE_ITEM` (line
    144) and reused across the form tests. The one inline mutation string (line 2560, in
    the upload test) is single-use, so naming it would be premature. No DRY action needed.

---

## Build: Slice 5a — version quintet (ex-GLOSSARY-line) + plain docs + CHANGELOG

_Source: `docs/builder/bld-slice-5-docs_version_cut.md`_

- [x] **Existing patterns reused.**
  - The version quintet is the **exact same five-site edit** `spec-037` Decision 10 performed for the `0.0.11` cut (spec lines 1844-1851): `pyproject.toml [project].version` (`pyproject.toml` line 4), `__version__` in `django_strawberry_framework/__init__.py` (line 37), `tests/base/test_init.py::test_version` (the `assert __version__ == "0.0.11"` at line 19), the `docs/GLOSSARY.md` package-version line (DB-backed — **carved to 5b**), and `uv.lock` (the package's own `version = "0.0.11"` at line 218, directly under `name = "django-strawberry-framework"` at line 217). No new mechanism — mirror the prior cut. **5a edits four of the five sites; the DB-backed GLOSSARY package-version line is 5b.**
  - The CHANGELOG entry reuses the established `## [0.0.X] - DATE` + `### Added` / `### Changed` shape (the `[0.0.11]` block at `CHANGELOG.md` lines 19-34 is the template); canonical phrasings crib from `docs/README.md` line 123-124 and the spec's Goals/Decision-6 prose.
  - **Investigation (Worker 1):** Not real. This is a docs/version-string slice with no package logic — the bullet recaps reuse of an established *editing procedure*, not a code-duplication opportunity. The "version quintet" is a manual five-site string bump (one literal that must agree across `pyproject.toml`, `__init__.py::__version__`, `tests/base/test_init.py::test_version`, `uv.lock`, and the DB-backed GLOSSARY line); AGENTS.md rule 31 already mandates the agreement, the per-cut test pins it, and there is no runtime call site to route through a helper — a "version constant" helper would be premature abstraction across heterogeneous file formats (`.toml`/`.py`/`.lock`/DB row). The CHANGELOG `## [0.0.X] - DATE` + `### Added`/`### Changed` shape is a one-off prose template, not repeated logic. Sanity-checked the four 5a sites: all read `0.0.12` and agree (`pyproject.toml` line 4, `__init__.py` line 37, `test_init.py` line 19, `uv.lock` line 218). No consolidation to perform.
- [x] **New helpers justified.** None. Slice 5a touches no package logic — it is version strings, prose edits, and a CHANGELOG entry. No helper, module, or constant is created.
  - **Pre-triage (Worker 0):** Skipped — bullet states "None ... Slice 5a touches no package logic ... No helper, module, or constant is created" (no-opportunity recap, no code inspection required).
- [x] **Duplication risk avoided.**
  - **Version-string drift** (AGENTS.md rule 31: `pyproject.toml` and `__init__.py` MUST match). The plan pins all four 5a sites in one list and the final-verify greps `0.0.11` across them to prove none was missed (the fifth site, the GLOSSARY package-version line, is 5b's responsibility — 5a final-verification defers it with a recorded reason).
  - **Re-flowing prose into stale version tags.** The plain-doc edits are status/version flips; the risk is leaving an old-version "coming soon" string alongside a flipped one. The plan pins each old string and its replacement; Worker 3's documentation-sanity check (BUILD.md lines 322-333) backstops "no obsolete coming soon / planned / old-version wording remains".
  - **Investigation (Worker 1):** Not real. This is a risk-avoided recap for a docs/version-string slice with no package logic, not a code-duplication opportunity. Both sub-risks are prevented by per-site pinning + a final grep, not by extracting a shared helper: version-string drift is a manual multi-format string bump (`.toml`/`.py`/`.lock`/DB) already mandated to agree by AGENTS.md rule 31 and pinned by `tests/base/test_init.py::test_version` — no runtime call site exists to route through a helper, and a "version constant" across heterogeneous file formats would be premature abstraction; the stale-prose risk is a one-off old-string→replacement edit, not repeated logic. Sanity-checked the four 5a sites: no stray `0.0.11` remains and all read `0.0.12` (`pyproject.toml` line 4, `__init__.py` line 37, `test_init.py` line 19, `uv.lock` package version). No consolidation to perform.

---

## Build: Slice 5b — DB-backed GLOSSARY promote/correct + version-line BoardDoc + KANBAN card move + regenerate

_Source: `docs/builder/bld-slice-5b-glossary_kanban_db.md`_

- [x] **Existing patterns reused.**
  - The DB-backed card-close + glossary-promote is the **standard worker-0.md "Closing out a kanban card" procedure** (embedded verbatim in the build-plan flags and reproduced in `### Implementation steps` below). It reuses the shipped render scripts (`scripts/build_kanban_md.py`, `scripts/build_kanban_html.py`, `scripts/build_glossary_md.py`) and the shipped `manage.py import_spec_terms` (with `--check`) — no new tooling.
  - The GLOSSARY Index row + the Mutations browse-by-category row are **generated** from `GlossaryTerm.statusText` and `GlossaryCategoryMembership` rows respectively (`scripts/build_glossary_md.py::render_index` / `render_browse`); flipping `status_text` flips the Index row for free. **Verified:** both form symbols ALREADY carry `GlossaryCategoryMembership` rows in the `mutations` category (`djangoformmutation` order 2, `djangomodelformmutation` order 3) — so "add to the Mutations browse-by-category row" is **already satisfied in the DB**; 5b VERIFIES this, it does not re-add (avoid the `(category, order)` unique collision a blind add would risk).
  - The package-version line is the SAME `0.0.11` value 5a bumps elsewhere; here it lives in the `docs/GLOSSARY.md`-rendered `BoardDoc` (namespace=`glossary`, key=`status-legend`), NOT a `build_glossary_md.py` literal. The version edit reuses the version-quintet bump shape (string `0.0.11` → `0.0.12`), only the carrier is a DB row + regenerate rather than a source-file edit.
  - **Investigation (Worker 1):** Not real. This is a recap of reuse-of-established-procedure with no consolidation opportunity. Slice 5b is a DB-row + regenerate slice with no package logic: it ran the standard worker-0.md "Closing out a kanban card" procedure against the shipped render scripts (`scripts/build_kanban_md.py`, `scripts/build_kanban_html.py`, `scripts/build_glossary_md.py`) and the shipped `manage.py import_spec_terms --check`, promoting/correcting two existing `GlossaryTerm` rows, moving the `status-legend` `BoardDoc` version line, and moving the card — adding no helper, module, constant, or literal. Confirmed against the source artifact's `### DRY analysis`: every cited reuse is a pre-existing tool/procedure (no new tooling), the version bump rides the existing quintet-bump shape, and the Mutations-category memberships were verified-already-present rather than re-added. Nothing repeats across call sites and there is nothing to extract.
- [x] **New helpers justified.** None. Slice 5b touches no package logic — it is DB row edits via the ORM + three regenerates. No helper, module, or constant is created.
  - **Pre-triage (Worker 0):** Skipped — bullet states "None ... Slice 5b touches no package logic ... No helper, module, or constant is created" (no-opportunity recap, no code inspection required).
- [x] **Duplication risk avoided.**
  - **Hand-editing a generated doc.** `KANBAN.md` / `KANBAN.html` / `docs/GLOSSARY.md` are DB-rendered; a hand-edit is silently reverted by the next regenerate. 5b edits the DB via the ORM then regenerates — NEVER touches the rendered markdown directly. The GLOSSARY package-version line is a `BoardDoc` body, NOT a script literal (verified: `scripts/build_glossary_md.py` has no version string; the line is rendered from the `namespace='glossary', key='status-legend'` `BoardDoc`, pk≈40).
  - **Raw SQL skips the side-row.** All DB writes go through the Django ORM (`.save()` / `.objects.create(...)`), NEVER raw SQL — kanban/glossary rows need the `post_save` `UUIDModel` side-row the render's `uuid { id }` query reads; a raw insert skips it and the render breaks.
  - **Re-seeding already-present terms.** All 31 terms-CSV anchors already exist as `GlossaryTerm` rows (verified) and both form symbols already have Mutations-category memberships — so the worker-0 procedure's "seed net-new terms" + "add category memberships" steps are **no-ops here**; 5b only PROMOTES + CORRECTS the two existing rows, appends two Public-exports bullets, moves the version-line `BoardDoc`, and moves the card. The plan calls these out as verify-not-create to stop Worker 2 duplicating rows.
  - **Investigation (Worker 1):** Not real. This is a risk-avoided recap for a DB-row + regenerate slice with no package logic, not a code-duplication opportunity — mirroring the two sibling 5b findings already disposed `not-real` above. All three sub-risks are process guardrails, not consolidation targets: (1) "hand-editing a generated doc" is a procedure rule (edit the DB via ORM then run the shipped render scripts, never touch `KANBAN.md`/`KANBAN.html`/`docs/GLOSSARY.md` directly) — verified the version line is a `BoardDoc` body (`namespace='glossary', key='status-legend'`), not a `scripts/build_glossary_md.py` literal, so there is nothing repeated to extract; (2) "raw SQL skips the side-row" is the standing ORM-only invariant that lets the `post_save` `UUIDModel` side-row fire for the render's `uuid { id }` query — a constraint on *how* the one-off DB writes are made, no call site to route through a helper; (3) "re-seeding already-present terms" is a verify-not-create caution against duplicating rows that already exist, the opposite of a duplication-extraction opportunity. No new tooling, helper, module, constant, or literal is introduced and nothing repeats across call sites. Nothing to consolidate.

## Final test-run gate

Run by Worker 2 after all 63 findings closed `- [x]` (every finding dispositioned `already-addressed` or `not-real`; no source/test code changed this cycle). All commands run with coverage flags forbidden (`--no-cov` on pytest); ruff commands read-only (no `--fix`).

1. `uv run pytest --no-cov` — **PASS**. 2367 passed, 4 skipped, 4 xfailed in 130.11s.
2. `uv run python examples/fakeshop/manage.py check` — **PASS**. System check identified no issues (0 silenced).
3. `uv run python examples/fakeshop/manage.py makemigrations --check --dry-run` — **PASS**. No changes detected.
4. `uv run ruff format --check .` — **PASS**. 277 files already formatted. (Benign `COM812`/formatter-conflict config warning only; not a failure.)
5. `uv run ruff check .` — **PASS**. All checks passed!
6. `git diff --check` — **PASS**. No whitespace errors or conflict markers (exit 0).

**Gate result: PASS end-to-end.** All six commands passed. No findings routed back. Working-tree changes remain limited to `docs/dry/dry-0_0_12.md` and the pre-existing dirty `docs/SPECS/spec-*.md` edits (out of scope, maintainer's).
