# DRY consolidation plan: 0.0.6

Source: `docs/builder/`
Generated: 2026-05-18
Workflow: `docs/dry/DRY.md`

One finding at a time. Worker 0 dispatches > Worker 1 triages > Worker 2 implements > Worker 1 verifies > Worker 0 ticks.

Pre-flight: passed on 2026-05-18; baseline: DRY workflow doc reshuffle uncommitted — `docs/DRY.md` → `docs/dry/DRY.md`, `scripts/export_dry_review.py` → `docs/dry/export_dry_review.py`, new `docs/dry/worker-{0,1,2}.md` role briefs, `.gitignore` extended with `docs/dry/worker-memory/`, `docs/feedback.md` edits; all expected for this cycle's setup. Build cycle `build-014-meta_primary-0_0_6` is `final-accepted` per `docs/builder/bld-final.md`.

## Findings

All 28 findings extracted from the six `build-014-meta_primary-0_0_6` slice artifacts were disposed during triage as `already-addressed` or `not-real`. Every finding was a status report describing DRY discipline already executed during the final-accepted build cycle (existing patterns reused, single-call-site helpers, intentionally avoided duplication, no-op verification gates, or doc-consistency contracts already honored). Worker 1 walked the cited source for each and confirmed no residual consolidation opportunity for Worker 2.

Per-slice dispatch outcomes:

- **Slice 1 — Registry multi-type storage + primary tracking** (3 findings, all `already-addressed`): `_check_mutable` / `_already_registered` reuse confirmed at `registry.py:50-326`; three new accessors (`primary_for` / `types_for` / `models_with_multiple_types`) live as single-expression methods at `:184-206`; `register_with_definition` rollback inline at `:244-257`; two inline primary-collision messages preserved (no `_check_primary_collision` helper); `fresh_registry` / `_isolate_global_registry` fixtures unchanged.
- **Slice 2 — `Meta.primary` recognition** (3 findings, all `already-addressed`): `"primary"` in `ALLOWED_META_KEYS` at `types/base.py:64`; inline `getattr → isinstance → raise` validation at `:399-401`; plumb at `:93` → `DjangoTypeDefinition(primary=primary)` at `:133` → `register_with_definition(..., primary=primary)` at `:135`; `primary: bool = False` dataclass field at `definition.py:32`; stale test at `tests/types/test_base.py:68-82` rewritten per option (a) (`CategoryTypeA`/`CategoryTypeB` both `Meta.primary = True`, `match="already declared primary"`).
- **Slice 3 — Cross-type ambiguity audit at finalization** (3 findings, all `already-addressed`): `audit_primary_ambiguity()` at `finalizer.py:66-86` (single call site at `:107`, after `is_finalized()` guard); `_format_ambiguity_error()` sibling at `:45-63` mirroring `_format_unresolved_targets_error()` at `:21-42`; audit uses `registry.models_with_multiple_types()` + `registry.primary_for()` + `registry.types_for()` only (no `_types` scan, no `definition.primary` read); six regression tests across `tests/test_registry.py` and `tests/types/test_definition_order.py` including the `monkeypatch.setattr` spy pattern at `tests/test_registry.py:1095`.
- **Slice 4 — Consumer-site updates (relation conversion + optimizer)** (15 findings: 8 `already-addressed`, 7 `not-real`): registry helper trio centralized at `registry.py:137-206`; `consumer_authored_fields` short-circuits preserved at `types/base.py:621-622, :644-650`; eager-bind branch removed (every auto-synthesized relation lands on `PendingRelation` + `PendingRelationAnnotation` at `:641-642`); `_resolve_field_map(model, source_type=None)` is the single `_meta.get_fields()` access at `walker.py:83-109`; `_OriginAndModel` NamedTuple at `extension.py:371-386` next to `_resolve_model_from_return_type:389`; deduped warning sink with `seen: set[...]` at `extension.py:644-677`; five-element cache-key tuple constructed only at `_build_cache_key`'s return (`:736`); no `definition.primary` routing reads anywhere in source; no "primary-only" iterator added; no shared multi-type test factory introduced (inline declarations + autouse `_isolate_registry` mirror).
- **Slice 5 — Atomic version-bump quintet** (3 findings, all `already-addressed`): five sites all at `0.0.6` (`pyproject.toml:4`, `django_strawberry_framework/__init__.py:25`, `tests/base/test_init.py:11`, `docs/FEATURES.md:20`, `uv.lock`); no stale `0.0.5` strings at the bump sites (the surviving `0.0.5` markers in `docs/FEATURES.md` are historical "shipped (`0.0.5`)" badges); zero source-code changes per the slice's no-op verification gate.
- **Slice 6 — Docs, KANBAN, CHANGELOG, archive** (4 findings, all `already-addressed`): `## Meta.primary` entry at `docs/FEATURES.md:651-668` with shipped (`0.0.6`) badge in the Index at `:85`; `DONE-014-0.0.6` in `KANBAN.md ## Done` at `:1611`; `[Unreleased]` `Added`/`Changed` entries in `CHANGELOG.md:25-33`; `docs/README.md:101` "Shipped today (`0.0.6`)" bullet; `TODAY.md:263-265` "available but not currently demonstrated in fakeshop" line; three-helper vocabulary (`primary_for` / `types_for` / `models_with_multiple_types`) consistent across the three sites that describe the registry surface; one-line `DjangoType` bullet at `docs/FEATURES.md:384` pointing at `#metaprimary` (full contract not duplicated).

No findings remain pending Worker 2 implementation. Cycle closeout proceeds directly to the final test-run gate.

## Final test-run gate

- `uv run pytest --no-cov` — **pass.** 683 passed, 3 skipped, 3 warnings in 25.22s across all three test trees.
- `uv run python examples/fakeshop/manage.py check` — **pass.** System check identified no issues (0 silenced).
- `uv run python examples/fakeshop/manage.py makemigrations --check --dry-run` — **pass.** "No changes detected" — model state is migration-consistent.
- `uv run ruff format --check .` — **pass.** 100 files already formatted (the standing COM812-vs-formatter compatibility warning is informational and unrelated).
- `uv run ruff check .` — **pass.** All checks passed.
- `git diff --check` — **pass.** Exit 0, no whitespace errors or conflict markers in the working tree (only the expected baseline DRY-workflow doc reshuffle present).

**Pass.**
