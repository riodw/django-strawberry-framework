# DRY consolidation plan: 0.0.6

Source: `docs/review/`
Generated: 2026-05-20
Workflow: `docs/dry/DRY.md`

One finding at a time. Worker 0 dispatches > Worker 1 triages > Worker 2 implements > Worker 1 verifies > Worker 0 ticks.

## Findings

## Review: `django_strawberry_framework/types/relay.py`

_Source: `docs/review/rev-types__relay.md`_

- [ ] Extract `_model_for(type_cls) -> type[models.Model]` collapsing `cls.__django_strawberry_definition__.model` reads at `types/relay.py:78`, `134`, `297`. `_initial_queryset` (`types/relay.py:264-271`) already absorbs the QuerySet-variant call site; a sibling `_model_for` would single-source the model-only reads and make the "type_cls is a registered DjangoType with a definition" contract visible in one named place. Three sites today, with `install_is_type_of` being the structural outlier (class-creation phase, not finalize phase).
  - **Triage (Worker 1, 2026-05-20):** Real. No `_model_for` helper exists yet (`grep "def _model_for" types/relay.py` returns nothing); the three direct `cls.__django_strawberry_definition__.model` reads still live inline at `relay.py:78` (inside `install_is_type_of`), `:134` (inside `_check_composite_pk_for_relay_node`), and `:304` (inside `_order_nodes` — drifted +7 lines from the artifact-cited `:297` due to the L1 Phase-2.5 inline comment + L3/L4 docstring expansions that landed in the source review cycle's comment pass). The source cycle's Fix report explicitly defers L5 (`_model_for` extraction) to a follow-up — this DRY cycle IS that follow-up. Recommended consolidation shape, mirroring `_initial_queryset` (`relay.py:271-278`):
    ```python
    def _model_for(cls: type) -> type[models.Model]:
        """Return the registered model for ``cls.__django_strawberry_definition__``.

        Centralizes the ``cls.__django_strawberry_definition__.model``
        lookup so model-only reads share one source of truth with the
        queryset-variant lookup in ``_initial_queryset``.
        """
        return cls.__django_strawberry_definition__.model
    ```
    Place it immediately before `_initial_queryset` at `relay.py:271` so the two centralizing helpers sit together (model-only above, model+`.all()` below); `_initial_queryset` can itself be rewritten as `return _model_for(cls)._default_manager.all()` so the two helpers form a single canonical chain rather than parallel reads. Parameter name: `cls` (matches `_initial_queryset`'s existing signature) — even though the first call site at `:78` uses `type_cls` locally inside `install_is_type_of`, passing it through as `_model_for(type_cls)` is the natural call shape; the helper's own parameter name does not need to match every caller's local. Type annotation: `cls: type` (matches `_initial_queryset`) — no stricter typing today; the contract is "any class with `__django_strawberry_definition__` attribute" which `_initial_queryset` also assumes without a Protocol. Error handling: `_initial_queryset` does NOT guard against a missing `__django_strawberry_definition__`; it lets `AttributeError` surface raw, because the call-site contract is "type_cls was already registered through `__init_subclass__`." `_model_for` should match — no custom `raise` wrapping, just direct attribute access. The three call sites are equivalent in contract (each runs inside a function whose docstring or surrounding context already establishes "this is a registered DjangoType"); the "structural outlier" framing in the artifact refers to lifecycle phase (class-creation vs finalize), NOT to whether the definition is guaranteed present. Specifically: `install_is_type_of` is called from `types/base.py:246` immediately after `cls.__django_strawberry_definition__ = definition` is set on `:245`, so the attribute is guaranteed present at that exact call site even though the broader Phase-2.5 finalizer has not yet run. The helper does NOT need to handle a not-yet-registered case. Worker 2 must update all three sites to use `_model_for(...)` and may optionally refactor `_initial_queryset` to call `_model_for` internally as described (recommended; it makes the centralization gesture explicit). Tests: existing coverage at `tests/types/test_relay_*.py` already exercises all three call sites end-to-end (interface dispatch via `install_is_type_of`, composite-pk gate via `_check_composite_pk_for_relay_node`, ordered bulk-fetch via `_order_nodes`); a focused unit test for `_model_for` itself is OPTIONAL — no behavior change, just a one-line read — but a single assertion that `_model_for(SomeRegisteredDjangoType)` returns the declared model would pin the contract cheaply. The folder-pass aggregator that bundled this with two unrelated extractions (`_collect_consumer_authored_fields`, `_assert_no_relay_id_collision`, `_apply_id_filter_and_materialize`) was triaged `not-real` for compound-trigger reasons; this per-file finding stands on its own and is the actionable extraction. AGENTS.md ack: no behavior change, so the existing live-HTTP test coverage continues to pin the contract; Worker 2 should run `uv run ruff format .` and `uv run ruff check --fix .` after edits and verify the three call sites all now read through `_model_for`.

  disposition: real

  - **Implementation (Worker 2, 2026-05-20):** Added `_model_for(cls: type) -> type[models.Model]` at `django_strawberry_framework/types/relay.py:271`, immediately before `_initial_queryset`; body is `return cls.__django_strawberry_definition__.model` with no custom error handling (mirrors `_initial_queryset`'s contract — raw `AttributeError` surfaces for unregistered callers). Updated the three call sites to read through the helper: `install_is_type_of` at `relay.py:78`, `_check_composite_pk_for_relay_node` at `relay.py:134`, and `_order_nodes` at `relay.py:316`; also refactored `_initial_queryset` to `return _model_for(cls)._default_manager.all()` so the two centralizing helpers form a single canonical chain. Added focused unit test `test_model_for_returns_registered_model` in `tests/types/test_relay_interfaces.py` (after `test_apply_interfaces_wraps_typeerror_as_configuration_error`) pinning `_model_for(CategoryNode) is Category` and `is CategoryNode.__django_strawberry_definition__.model`. `uv run ruff format .` reported 101 files unchanged; `uv run ruff check --fix .` reported all checks passed. `git status --short` shows only `django_strawberry_framework/types/relay.py` and `tests/types/test_relay_interfaces.py` modified (plus the untracked plan file) — no unrelated drift.

  - **Verification (Worker 1, 2026-05-20):** Verified. `_model_for(cls: type) -> type[models.Model]` landed at `django_strawberry_framework/types/relay.py:271-280` immediately before `_initial_queryset` (line 283), body is the single-line `return cls.__django_strawberry_definition__.model` matching the Triage contract verbatim. All three previously-direct reads now route through the helper: `install_is_type_of` (`relay.py:78` — `model = _model_for(type_cls)`), `_check_composite_pk_for_relay_node` (`relay.py:134` — `model = _model_for(type_cls)`), `_order_nodes` (`relay.py:316` — `model = _model_for(cls)`); `_initial_queryset` (`relay.py:290`) was additionally refactored to `return _model_for(cls)._default_manager.all()`, preserving the `.all()` chain byte-identical except for the leading attribute access — exactly the optional refactor the Triage recommended ("makes the centralization gesture explicit"). `grep -n "__django_strawberry_definition__\.model" django_strawberry_framework/types/relay.py` returns four lines: :186 (docstring inside `_resolve_id_default` discussing the alternative-keying choice), :274 + :286 (docstrings on `_model_for` and `_initial_queryset` describing what the helpers centralize), and :280 (the helper body itself) — zero non-helper code reads remain. Focused tests `uv run pytest --no-cov tests/types/test_relay_interfaces.py -x` pass 63/63 including the new `test_model_for_returns_registered_model` at `tests/types/test_relay_interfaces.py:1089` (correct test tree per AGENTS.md test-placement rules; located in `tests/types/`, asserting `_model_for(CategoryNode) is Category` AND `is CategoryNode.__django_strawberry_definition__.model`, double-pinning both the return identity and the canonical-attribute equivalence). `uv run ruff format --check .` reports "101 files already formatted"; `uv run ruff check .` reports "All checks passed!". `git status --short` shows only the two expected modifications plus the untracked plan file — no unrelated drift.

---

## Review: `django_strawberry_framework/types/resolvers.py`

_Source: `docs/review/rev-types__resolvers.md`_

- [ ] None — `types/resolvers.py`'s DRY surface is already tracked elsewhere: the `FieldMeta.from_relation_like` consolidation is the Medium M1 finding above, the `field_meta.related_model` vs `field.related_model` read is Low L2, and the three resolver-closure `_check_n1` head repetition is load-bearing parallel (each tail diverges and centralising would force a hot-path branch on `kind`).

---

## Review: `django_strawberry_framework/utils/` (folder pass)

_Source: `docs/review/rev-utils.md`_

- [ ] None — the three `utils/` siblings are pure leaf modules with zero cross-imports inside the folder and exactly one canonical consumer per direction; the `_NODEID_STRING_RE` carry-forward (move-to-`utils/typing.py`-when-a-second-site-lands) is already tracked in `rev-utils__typing.md`'s DRY analysis, and the `queryset/` submodule charter in `__init__.py:13-14` is the only other forward-extension gate.

---

## Review: `django_strawberry_framework/utils/relations.py`

_Source: `docs/review/rev-utils__relations.md`_

- [ ] None — `utils/relations.py` is the canonical leaf for Django-relation-shape classification; its string-literal repetitions in the `Literal[...]` alias and classifier `return`s are structurally unavoidable given Python's closed-set typing shape, and the prior cycle (`rev-optimizer__field_meta.md` M1) eliminated the only known external copy of `{"many", "reverse_many_to_one"}`.

---

## Review: `django_strawberry_framework/utils/strings.py`

_Source: `docs/review/rev-utils__strings.md`_

- [ ] None — `utils/strings.py` is a pure-stdlib leaf module with two short case-conversion helpers; the docstring already names the only deferred extension condition ("a third style would land here").

---

## Review: `django_strawberry_framework/utils/typing.py`

_Source: `docs/review/rev-utils__typing.md`_

- [ ] Defer until a second stringified-annotation parse site lands in the optimizer: extend `utils/typing.py` with the `_NODEID_STRING_RE` regex helper currently carried in `worker-memory/worker-1.md`. No second site has landed; the two-function split (`unwrap_graphql_type` peel-all vs `unwrap_return_type` peel-one) stays correct factoring today.
