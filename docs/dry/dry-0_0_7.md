# DRY consolidation plan: 0.0.7

Source: `docs/builder/`
Generated: 2026-05-27
Workflow: `docs/dry/DRY.md`

Pre-flight: passed; baseline: working-tree change limited to the prior `docs/dry/dry-0_0_7.md` being deleted in preparation for this regeneration (now overwritten by the fresh plan). `pyproject.toml` and `django_strawberry_framework/__init__.py` both pin `0.0.7`. `export_dry_review.py --help` runs.

One finding at a time. Worker 0 dispatches > Worker 1 triages > Worker 2 implements > Worker 1 verifies > Worker 0 ticks.

## Findings

## Build: Slice 1 — Helper module + `BigInt` redefinition

_Source: `docs/builder/bld-slice-1-helper_module_and_bigint_redefinition.md`_

- [x] **Existing patterns reused.**
  - `django_strawberry_framework/scalars.py::_parse_bigint` and `django_strawberry_framework/scalars.py::_serialize_bigint` stay verbatim. The strict-parser and strict-serializer functions are pure (no Strawberry coupling) and continue to back the migrated `_BIGINT_SCALAR_DEFINITION`. No changes to body, signature, or docstring.
  - `django_strawberry_framework/scalars.py` #`"_BIGINT_STRING_PATTERN"` regex constant stays as-is — referenced by `_parse_bigint` only.
  - `django_strawberry_framework/scalars.py` module docstring (lines 1–11) stays as-is — describes the BigInt wire-format contract, which Decision 3 preserves verbatim.
  - The existing `from typing import Any, NewType` import is preserved (both names remain in use: `Any` annotates `_parse_bigint(value: Any)`, `_serialize_bigint(value: Any)`, and now also `**config_kwargs: Any`; `NewType` builds the bare `BigInt = NewType("BigInt", int)`).
  - The existing `import strawberry` import stays — `strawberry.scalar(name=..., serialize=..., parse_value=...)` is the no-warning overload entry point at `.venv/lib/python3.10/site-packages/strawberry/types/scalar.py::scalar #"if cls is None and name is not None"`.
  - `django_strawberry_framework/__init__.py` already groups scalar re-exports on one line (`from .scalars import BigInt`). The plan widens the existing line rather than adding a new import statement, preserving the file's "one import per source module" convention.
  - **Investigation (Worker 1):** Not real. This bullet is a descriptive recap of patterns the closed Slice 1 build deliberately preserved verbatim (the pure `_parse_bigint` / `_serialize_bigint` pair at `django_strawberry_framework/scalars.py::_parse_bigint` and `::_serialize_bigint`, the `_BIGINT_STRING_PATTERN` regex at `scalars.py #"_BIGINT_STRING_PATTERN"`, the module docstring at lines 1–11, the `from typing import Any, NewType` and `import strawberry` imports, and the widened single-line `from .scalars import BigInt, strawberry_config` re-export at `django_strawberry_framework/__init__.py` #`"from .scalars import"`). Every claim verifies against current source post-Slice-1 (`scalars.py` and `__init__.py` read exactly as the bullet describes). No extraction, helper, or consolidation is proposed — the bullet is a preservation inventory, not a new DRY opportunity. The source BUILD cycle is `final-accepted`; there is no work for Slice C/D/E to do here.
- [x] **New helpers justified.**
  - **Investigation (Worker 1):** Already addressed. All three helpers ship in `django_strawberry_framework/scalars.py` with the prescribed single-responsibility shape at the BUILD cycle close: `_BIGINT_SCALAR_DEFINITION: ScalarDefinition` at `scalars.py:84-88` (built via `strawberry.scalar(name="BigInt", serialize=_serialize_bigint, parse_value=_parse_bigint)` no-warning overload), `_PACKAGE_SCALAR_MAP: dict[object, ScalarDefinition] = {BigInt: _BIGINT_SCALAR_DEFINITION}` at `scalars.py:90-92`, and `strawberry_config(*, extra_scalar_map=None, **config_kwargs) -> StrawberryConfig` factory at `scalars.py:95-130` (merges `_PACKAGE_SCALAR_MAP` with `extra_scalar_map`, rejects direct `scalar_map=` kwarg and package-key collisions per spec-020 Decision 4, forwards remaining kwargs to `StrawberryConfig(...)`). The finding is the source artifact's descriptive recap of the slice's three new symbols and contains no consolidation proposal beyond what the closed BUILD cycle already shipped.
  - One new public symbol: `strawberry_config` factory in `django_strawberry_framework/scalars.py`. Single responsibility: build a fresh `StrawberryConfig` instance whose `scalar_map` is the package's defaults merged with the caller's `extra_scalar_map`, while forwarding every non-`scalar_map` kwarg to the upstream `StrawberryConfig(...)` constructor. Call sites: consumer `strawberry.Schema(query=..., config=strawberry_config(), extensions=[...])` invocations (one per consumer schema, plus the fakeshop migration in Slice 3, plus the two Slice 2 integration tests in `tests/test_scalars.py`).
  - One new module-level data structure: `_PACKAGE_SCALAR_MAP: dict[object, ScalarDefinition]`. Single responsibility: the canonical mapping of package-defined scalar `NewType`s (today only `BigInt`) to their `ScalarDefinition`s. The factory reads it; future package scalars (`Upload` in `TODO-ALPHA-028-0.0.11`) join by appending a new entry. Justified because the alternative (inline literal dict inside the factory body) would force a fresh `strawberry.scalar(...)` call per `strawberry_config()` invocation, multiplying `ScalarDefinition` allocations and frustrating the "single source of truth for package scalars" reading.
  - One new module-level definition: `_BIGINT_SCALAR_DEFINITION: ScalarDefinition` produced by the no-warning `strawberry.scalar(name="BigInt", serialize=_serialize_bigint, parse_value=_parse_bigint)` overload. Single responsibility: the canonical `ScalarDefinition` for `BigInt`. Referenced by `_PACKAGE_SCALAR_MAP`. Justified because building the definition inline inside `_PACKAGE_SCALAR_MAP = {BigInt: strawberry.scalar(...)}` would obscure the definition; the named module-level binding makes the symbol greppable.
- [x] **Duplication risk avoided.**
  - **Investigation (Worker 1):** Already addressed. This is a "what the shipped code avoided" recap, not a forward-looking duplication opportunity. Verified against current `django_strawberry_framework/scalars.py` (baseline `ec4b98c93da406bf9a9e566866deb5488d32b7d3`): (1) `BigInt = NewType("BigInt", int)` is bare at `scalars.py:82` and `_BIGINT_SCALAR_DEFINITION` uses the no-warning `strawberry.scalar(name=..., serialize=..., parse_value=...)` overload at `scalars.py:84-88` — no deprecated re-wrap; (2) `merged: dict[object, ScalarDefinition] = dict(_PACKAGE_SCALAR_MAP)` then `merged.update(extra)` at `scalars.py:128-129` builds a fresh dict per call — `_PACKAGE_SCALAR_MAP` is never mutated; (3) `import warnings` and the `warnings.catch_warnings()` block are both gone from `scalars.py` (`grep -n warnings django_strawberry_framework/scalars.py` returns nothing); (4) the explicit `if "scalar_map" in config_kwargs: raise ValueError(...)` rejection lives at `scalars.py:115-118`; (5) no pre-validation of `extra_scalar_map` key shape — keys flow through `dict()` copy and merge, Strawberry's own contract owns validation. All five naive-implementation pitfalls listed in the finding are avoided by construction in the FINAL-ACCEPTED Slice 1 build. Nothing remains to consolidate.
  - The naive implementation could re-wrap `BigInt` via `strawberry.scalar(NewType("BigInt", int), name=..., ...)` (the deprecated overload). Decision 3 forbids this; the spec's pinned shape uses the `cls is None and name is not None` branch at `.venv/lib/python3.10/site-packages/strawberry/types/scalar.py::scalar #"if cls is None and name is not None"`, which returns a `ScalarDefinition` directly without invoking the deprecation-emitting `wrap()` body.
  - The naive implementation could build the merged `scalar_map` by `_PACKAGE_SCALAR_MAP.update(extra)`, which would mutate the module-level dict across calls. The spec's pinned shape (spec lines 297–298) constructs a fresh `merged: dict[object, ScalarDefinition] = dict(_PACKAGE_SCALAR_MAP)` then `merged.update(extra)`, so module state is never mutated and each call returns an independent `StrawberryConfig`. Worker 2 follows the pinned shape verbatim.
  - The naive implementation could leave the `warnings.catch_warnings()` suppression block in place "defensively." Decision 6 forbids this; the block (current `scalars.py` lines 80–103) is removed wholesale, along with the now-unused `import warnings`.
  - The naive implementation could route `extra_scalar_map` through `**config_kwargs` (i.e., accept `scalar_map=` as a kwarg). Decision 2 / Decision 4 require a hard `ValueError` at `if "scalar_map" in config_kwargs`. Worker 2 mirrors the pinned shape's explicit check.
  - The naive implementation could pre-validate the `extra_scalar_map` key shape. Spec edge-cases section (lines 437) and Error shapes (spec lines 200–203) deliberately disclaim this — Strawberry's own `Mapping[object, ScalarDefinition]` contract owns key validation; the helper does not duplicate it.
- [x] **Helper not required for this planning pass.** `scripts/review_inspect.py` is **skipped**: `django_strawberry_framework/scalars.py` is 103 source lines (verified via `wc -l`), below the 150-line threshold pinned in `docs/builder/BUILD.md` "When to run the helper during build"; the file is not under `django_strawberry_framework/optimizer/` or `django_strawberry_framework/types/`. The threshold criterion governs Worker 1's planning helper requirement; both conditions miss, so the helper is not required. Worker 3 will run the helper at review time per its own threshold rules (30+ new logic lines in any file under `django_strawberry_framework/`).
  - **Investigation (Worker 1):** Not real. This finding is a descriptive recap of a planning-time tooling decision in the closed BUILD cycle (Worker 1 declined to run `scripts/review_inspect.py` because `django_strawberry_framework/scalars.py` was under the 150-line threshold and outside the `optimizer/`/`types/` directories); it asserts no extraction, no helper opportunity, and no behavior change in current source — there is nothing in current source to consolidate.

---

## Build: Slice 2 — Tests

_Source: `docs/builder/bld-slice-2-tests.md`_

- [x] **Existing patterns reused.**
  - `tests/test_scalars.py` lines 1-16 — module docstring + existing import block (`subprocess`, `sys`, `Decimal`, `pytest`, `_parse_bigint`/`_serialize_bigint`). Slice 2 widens this block (adds `NewType` from `typing`; adds `strawberry`, `StrawberryConfig`, `ScalarDefinition`, top-level `BigInt`, top-level `strawberry_config`) rather than re-emitting a fresh import block. The existing `import pytest` is reused by the new `pytest.raises(ValueError | TypeError)` blocks. The existing `from django_strawberry_framework import BigInt` use inside `test_bigint_is_importable_from_top_level` (lines 212-221) is preserved as a runtime symbol fetch; the new tests promote `BigInt` to a top-of-file import so the factory and integration tests can reference it without re-importing.
  - `tests/test_scalars.py` lines 18-20, 46-48, 83-85, 120-122, 207-209, 224-226 — banner-comment style (`# ----` / `# Section name` / `# ----`). The new "`strawberry_config()` factory" section uses the same banner shape so the file reads consistently. The factory tests, the `**config_kwargs` passthrough tests, and the two integration tests live under three sub-banners inside that section, mirroring spec lines 475-501.
  - `tests/test_scalars.py::test_bigint_is_importable_from_top_level #"from django_strawberry_framework import BigInt"` — confirms `BigInt` re-exports cleanly today. Slice 2 leans on the same re-export and adds a parallel `strawberry_config` import; no new top-level export shape to validate.
  - `tests/test_scalars.py::test_package_import_does_not_emit_strawberry_deprecation_warning` (lines 229-252) — left UNCHANGED per spec line 45 (the subprocess-with-`-W error::DeprecationWarning` regression now pins the post-migration no-leak contract via the new registration shape rather than via the removed suppression block).
  - `tests/base/test_init.py::test_public_api_surface_is_pinned` (lines 30-44) — Slice 2 appends exactly one element (`"strawberry_config"`) to the pinned tuple at line 44, after `"finalize_django_types"`. Matches the spec line 448 final tuple shape character-for-character. The surrounding test's pinned-tuple structure (one entry per line, trailing comma per COM812 / AGENTS.md line 17) is preserved verbatim.
  - `tests/types/test_converters.py::_introspect_field_type`, `_walk_introspected_type` (lines 463-489) — helper functions reused by every migrated test in the BigInt section. No edit to either helper; the schema-construction line is the only delta inside each test.
  - `tests/types/test_converters.py` BigInt section in-function model declaration pattern (`class BigIntOwner(models.Model): ... class Meta: managed = False, app_label = "test_bigint"`) — reused verbatim per test; Slice 2 does not alter model declarations, only the `strawberry.Schema(query=Query)` → `strawberry.Schema(query=Query, config=strawberry_config())` line.
  - `strawberry.scalar(name="CustomScalar", serialize=str, parse_value=str)` — the spec-pinned shape (spec line 481) for a throwaway consumer scalar used by the merge / passthrough / collision / non-mutation tests. Declared locally per test so each test stays a single pytest item per Decision 7 (spec lines 366-367, "Single pytest item per test; no `pytest.mark.parametrize` fan-out").
  - **Investigation (Worker 1):** Already addressed. This bullet is a preservation inventory of patterns Slice 2 (FINAL-ACCEPTED BUILD) deliberately kept verbatim, not a forward-looking DRY opportunity. Every claim verifies against current source: (1) `tests/test_scalars.py::test_package_import_does_not_emit_strawberry_deprecation_warning` is present at `tests/test_scalars.py:238` unchanged (subprocess + `-W error::DeprecationWarning` shape intact); (2) `_walk_introspected_type` and `_introspect_field_type` live at `tests/types/test_converters.py:463` and `:477` with the same docstrings/signatures the finding cites, and the migrated 10 BigInt-section tests call them unchanged (e.g., `tests/types/test_converters.py:547`, `:579`, `:608`); (3) `tests/base/test_init.py::test_public_api_surface_is_pinned` (file is now 45 lines) keeps the one-entry-per-line + trailing-comma + COM812 shape with `"strawberry_config"` appended last after `"finalize_django_types"`; (4) the triple-line `# ---------------------------------------------------------------------------` banner style is preserved across `tests/test_scalars.py` (banners at lines 27/29, 55/57, 92/94, 129/131, 216/218, 233/235, plus the three new sub-section banners at 264/266, 339/341, 398/400); (5) `tests/test_scalars.py`'s import block widening, the in-test `CustomScalar = NewType(...)` / `custom_def = strawberry.scalar(...)` declarations, and the unchanged BigInt section in-function model-declaration pattern (`class Meta: managed = False, app_label = "test_bigint"`) all read as the bullet describes. No extraction, helper, or consolidation is proposed — the bullet recaps what Slice 2 preserved. The source BUILD cycle is `final-accepted`; there is no work for Slice C/D/E to do here.

- [x] **New helpers justified.**
  - **No module-level test helper extracted.** The eight scalar-map factory tests, the five passthrough tests, and the two integration tests are each short (≤ 10 lines of body) and assert on different facets of the same factory call. A shared `_call_factory(...)` helper would obscure the test boundaries. Per Decision 7's no-`parametrize` policy (spec lines 366-367) and the established `tests/test_scalars.py` style (every existing test is a flat `def test_...():` with inline asserts), no helper is justified.
  - **A locally-declared `CustomScalar = NewType("CustomScalar", str)` and `custom_def = strawberry.scalar(name="CustomScalar", serialize=str, parse_value=str)` pair recurs across four tests** (`test_strawberry_config_merges_extra_scalar_map`, `test_strawberry_config_extra_scalar_map_does_not_mutate_caller_dict`, `test_strawberry_config_independent_call_returns_independent_instance`, `test_strawberry_config_combines_extra_scalar_map_and_config_kwargs`). Each test declares its own pair inside the test body (no module-level fixture) per Decision 7's single-pytest-item-per-test rule. The local declaration is two lines (`CustomScalar = NewType(...)`; `custom_def = strawberry.scalar(...)`) and keeps each test self-contained; consolidating into a `@pytest.fixture` would scatter the test's data across files and frustrate single-test-failure debugging. Worker 2 has discretion to choose between (a) declaring the pair at module level once (DRY-leaning) and (b) declaring it inside each test body (locality-leaning); the spec does not pin either, but the existing file style favors locality. Recommendation: in-test declaration.
  - **Investigation (Worker 1):** Already addressed. The shipped Slice 2 tests in `tests/test_scalars.py` follow the recommendation: no `_call_factory(...)` module-level helper exists, and the `CustomScalar = NewType("CustomScalar", str)` / `custom_def = strawberry.scalar(name="CustomScalar", serialize=str, parse_value=str)` pair is declared in-test at lines 298-299, 309-310, 329-330, and 365-366 (one pair per test body). Every `def test_...():` in the file is flat with inline asserts; no shared call-factory or fixture was introduced. The recurring two-line pair is intentional locality per Decision 7 and the file style, so no further consolidation is warranted.

- [x] **Duplication risk avoided.**
  - The naive implementation could pin the `ValueError` collision message by exact-string equality. Spec line 483 pins substring assertions (`"BigInt"` AND `"cannot redeclare"`), NOT exact equality, because Worker 2 in Slice 1 split the recourse sentence into two adjacent implicit-concatenation literals for the 110-char line budget (`bld-slice-1-helper_module_and_bigint_redefinition.md` Implementation notes). The runtime message is character-identical to the spec's pinned shape, but a future cosmetic re-wrap should not require a Slice 2 test edit. Worker 2 asserts on substrings only.
  - The naive implementation could assert on `result.auto_camel_case` for `test_strawberry_config_forwards_auto_camel_case_kwarg`. The spec at line 490 EXPLICITLY corrects this — `auto_camel_case` is a dataclass `InitVar` on `StrawberryConfig` (verified at `.venv/lib/python3.10/site-packages/strawberry/schema/config.py::StrawberryConfig #"auto_camel_case: InitVar[bool]"`), and `__post_init__` applies the value to `name_converter.auto_camel_case`, leaving `cfg.auto_camel_case` itself as `None`. Worker 2 asserts on `result.name_converter.auto_camel_case is False` / `is True`, NOT on `result.auto_camel_case`.
  - The naive implementation could parametrize the eight scalar-map tests into one `pytest.mark.parametrize(...)`-driven test. Decision 7 (spec lines 366-367) explicitly forbids parametrize fan-out so the pytest item count matches collection output unambiguously and aligns with the precedent in spec-018 / spec-019. Worker 2 writes 15 flat `def test_...():` functions.
  - The naive implementation could add a third integration test for the `test_bigint_in_input_position_with_null_via_strawberry_config_schema` case. The spec at lines 500-501 pins exactly TWO integration tests (`test_bigint_serializes_int_via_strawberry_config_schema`, `test_bigint_parses_decimal_string_via_strawberry_config_schema`). The null-input behavior is already pinned by `tests/types/test_converters.py::test_bigint_in_input_position_with_null_via_schema_execution` (one of the 10 sites migrated in this slice), so duplicating it in `tests/test_scalars.py` would be defense-in-depth on top of an already-defense-in-depth migration. Worker 2 writes exactly two integration tests.
  - The naive implementation could migrate every `strawberry.Schema(query=Query)` site in `tests/types/test_converters.py` (the file has 30+ sites; see line counts via `grep -n "strawberry.Schema(query=Query)"`). Spec line 47 pins exactly 10 sites — the BigInt-resolving subset of the BigInt-section banner; sites whose schemas resolve to upstream `Int` (`test_big_auto_field_still_maps_to_int` at line 615) or to JSONField / Choice-enum / Relation / Boolean scalars (lines 777+) are explicitly NOT migrated. Worker 2 follows the spec's pinned site list verbatim and does not touch any other `strawberry.Schema(query=Query)` line in the file.
  - The naive implementation could update the existing `test_package_import_does_not_emit_strawberry_deprecation_warning` to point at the new registration path or rename it. Spec line 45 pins that this test is UNCHANGED (the subprocess shape still validates the post-migration no-leak contract). Worker 2 does not touch this test.
  - The naive implementation could rewrite the `tests/test_scalars.py` module docstring from scratch. Spec line 48 pins "preserve the existing delegation sentence" and "APPEND" the new sentence; the suggested rewrite preserves the schema-execution-delegation context and adds the integration-tests acknowledgement. Worker 2 keeps the existing first paragraph verbatim and appends the new sentence per the spec's suggested wording (Worker 2 has discretion on exact phrasing per Decision 7's "Worker 2 has discretion on exact phrasing" allowance).
  - **Investigation (Worker 1):** Already addressed. All seven naive-implementation pitfalls were sidestepped by the Slice 2 build itself. Verified against current source: (1) the collision-message test asserts substrings only at `tests/test_scalars.py:322-324` (`"BigInt" in message` AND `"cannot redeclare" in message`), no exact-string equality; (2) `test_strawberry_config_forwards_auto_camel_case_kwarg` at `tests/test_scalars.py:350,352` asserts on `result.name_converter.auto_camel_case`, not `result.auto_camel_case`; (3) `grep "pytest.mark.parametrize"` returns zero hits in the new tests — the file has 43 flat `def test_...():` functions; (4) exactly two integration tests exist (`test_bigint_serializes_int_via_strawberry_config_schema` at line 403, `test_bigint_parses_decimal_string_via_strawberry_config_schema` at line 418) — no third null-input integration test; (5) `grep "strawberry.Schema(query=Query" tests/types/test_converters.py` shows `config=strawberry_config()` on exactly the 10 BigInt-resolving sites (lines 546, 578, 607, 670, 685, 702, 719, 734, 749, 766) and bare `strawberry.Schema(query=Query)` at line 639 (`test_big_auto_field_still_maps_to_int`, resolves to upstream `Int`) and at every site from line 812 onward (the JSONField / Choice-enum / Relation / Boolean sections); (6) `test_package_import_does_not_emit_strawberry_deprecation_warning` at `tests/test_scalars.py:238` is untouched and retains its `-W error::DeprecationWarning` subprocess shape; (7) the module docstring at `tests/test_scalars.py:1-12` preserves the existing "Wire-level / schema-execution behavior lives in ``tests/types/test_converters.py``..." sentence verbatim and appends the new sentence naming both integration tests by full function name. The build report's "Tests added or updated" section and "Implementation notes" already record each of these as deliberate Slice 2 decisions; no DRY action remains.

- [x] **Static inspection helper.** Run on three files this slice extends:
  - **Investigation (Worker 1):** Not real. Planning-time meta-record of helper runs (`scripts/review_inspect.py` ran on `tests/test_scalars.py` and `tests/types/test_converters.py`, skipped on `tests/base/test_init.py` per BUILD.md threshold); the bullet reports overview outputs already written to `docs/shadow/` and proposes no source-code consolidation.
  - `tests/test_scalars.py` (252 source lines, no logic — pure test file): ran helper at planning; overview at `docs/shadow/tests__test_scalars.overview.md`. No control-flow hotspots, no Django/ORM markers, 2 repeated string literals (the `int64_max` / `int64_min` decimal strings, already justified by spec-013 wire-format pins). The helper output confirms the existing file's style is flat `def test_...():` with inline asserts — Slice 2's new tests follow the same shape.
  - `tests/types/test_converters.py` (1735 source lines, over the 150-line threshold; under `tests/types/` which is NOT the same as `django_strawberry_framework/types/`, so the path-based trigger does NOT apply — only the line-count threshold). Ran the helper at planning; overview at `docs/shadow/tests__types__test_converters.overview.md`. No new control-flow hotspots introduced by the migration (the 10 affected sites are leaf-level `strawberry.Schema(query=Query)` line edits inside existing test bodies). No new Django/ORM markers, no new TODOs.
  - `tests/base/test_init.py` (44 source lines, below the 150-line threshold) — helper SKIPPED per BUILD.md "When to run the helper during build" criteria. The one-line edit is a single-element append to the pinned tuple; no logic added.

---

## Build: Slice 3 — Example-app migration

_Source: `docs/builder/bld-slice-3-example_app_migration.md`_

- [x] **Existing patterns reused.**
  - **Investigation (Worker 1):** Already addressed. The Slice 3 migration is in current source at `examples/fakeshop/config/schema.py`: line 16 widens the existing single-line `from django_strawberry_framework import ...` to add `strawberry_config` as the third symbol (alphabetical, 106 chars, single line preserved); lines 26–30 widen the sole `strawberry.Schema(...)` call to insert `config=strawberry_config(),` between `query=Query,` and `extensions=[DjangoOptimizerExtension()],` with trailing comma preserved per COM812. No new helper, no new module, no new symbol — the finding is a pure recap of the in-place widen that shipped in Slice 3 (build-final-accepted at `docs/builder/bld-slice-3-example_app_migration.md`). The per-app schemas at `examples/fakeshop/apps/library/schema.py` and `examples/fakeshop/apps/products/schema.py` remain untouched per Decision 9. Nothing to consolidate.
  - `examples/fakeshop/config/schema.py #"strawberry.Schema(query=Query"` (current lines 26–29) is the file's sole `strawberry.Schema(...)` construction site. The migration widens the existing call in place; no new helper, no new module, no new symbol introduced.
  - `examples/fakeshop/config/schema.py #"from django_strawberry_framework import DjangoOptimizerExtension, finalize_django_types"` (current line 16) is the file's existing one-line import block from `django_strawberry_framework`. The migration widens this single line to add `strawberry_config`; no new import statement is added.
  - The `strawberry_config` symbol itself ships from `django_strawberry_framework/scalars.py` (final-accepted in Slice 1) and is re-exported from `django_strawberry_framework/__init__.py` (final-accepted in Slice 1); the per-call factory shape is exercised by 15 new tests in `tests/test_scalars.py` and the 10 migrated converter-table sites in `tests/types/test_converters.py` (final-accepted in Slice 2). Slice 3 reuses the symbol verbatim — no public-surface change.
- [x] **New helpers justified.**
  - None. The slice is a two-edit migration (one import widen, one constructor-call widen) on a single file. No helper or shared utility is introduced or warranted; the file is the sole project-level schema-construction site and there is no parallel call site to consolidate against.
  - **Pre-triage (Worker 0):** Skipped — opens with "None. The slice is a two-edit migration … No helper or shared utility is introduced or warranted".
- [x] **Duplication risk avoided.**
  - **Investigation (Worker 1):** Already addressed. Slice 3 (commit `b1a6d01`) modified exactly one file (`examples/fakeshop/config/schema.py`, +2/-1) per `git show --stat b1a6d01 -- examples/fakeshop/`. Verified the four claimed-untouched targets: (1) `grep -rn "strawberry.Schema(" examples/fakeshop/` returns the three sites the bullet names — `config/schema.py:26`, `test_query/test_multi_db.py:142`, `test_query/README.md:13`; (2) `examples/fakeshop/apps/library/schema.py` and `examples/fakeshop/apps/products/schema.py` carry only `@strawberry.type` / `@strawberry.field` / `from strawberry.types import Info` usage (no `strawberry.Schema(` call) per `grep -n "strawberry\." …`; (3) `test_query/test_multi_db.py:142` and `test_query/README.md:13` are unchanged in `b1a6d01` (only `config/schema.py` appears in the commit's file list). All four "naive pitfall avoided" claims hold; nothing to consolidate.
  - The naive implementation could touch the per-app schemas at `examples/fakeshop/apps/library/schema.py` and `examples/fakeshop/apps/products/schema.py` (the "audit-only" targets) under the assumption that they also need migration. Decision 9 (spec lines 402–417) and DoD item 8 (spec line 647) explicitly forbid this — both per-app files declare only `@strawberry.type class Query` with `@strawberry.field` resolvers and do NOT construct `strawberry.Schema(...)`. Verified pre-plan: `grep -rn "strawberry.Schema(" examples/fakeshop/` returns exactly three sites — `config/schema.py:26` (the slice's target), `test_query/test_multi_db.py:142` (a test-local schema construction outside Slice 3 scope), and `test_query/README.md:13` (a docs reference, not source). The per-app schemas contain only `@strawberry.field` / `@strawberry.type` decorator usage (verified via `grep -n "strawberry\." examples/fakeshop/apps/library/schema.py` and `examples/fakeshop/apps/products/schema.py`); neither carries a `strawberry.Schema(` call.
  - The naive implementation could change the import statement style — e.g., split the existing single-line `from django_strawberry_framework import DjangoOptimizerExtension, finalize_django_types` into two lines, or reorder symbols by something other than alphabetical. Slice 3 preserves the existing single-line shape and adds `strawberry_config` alphabetically (after `finalize_django_types`) so ruff format's import sorter does not rewrap the block. Worker 2's discretion on the exact in-line ordering — see `### Implementation discretion items` below.
  - The naive implementation could touch the `examples/fakeshop/test_query/test_multi_db.py` test-local `strawberry.Schema(...)` site at line 142 because it constructs a schema. That site is **out of scope** for Slice 3 — Decision 9 names exactly one site (`examples/fakeshop/config/schema.py`). The test-local schema in `test_multi_db.py` is a per-test fixture, not the project schema. Worker 2 does not edit it.
  - The naive implementation could update `examples/fakeshop/test_query/README.md` line 13 (which describes the project schema's construction shape in prose). Slice 3 does NOT modify that file — Slice 4 owns all docs updates (per the spec's slice boundary at lines 52–58); the `test_query/README.md` mention is not in Slice 4's scope either (only `docs/README.md`, `docs/GLOSSARY.md`, `GOAL.md`, `TODAY.md`, and the terms CSV are named). The `test_query/README.md` line will remain unchanged through the build; if it drifts post-cut, that is a future-card concern.
- [x] **Static inspection helper.** Skipped at planning time. Per `docs/builder/BUILD.md` "When to run the helper during build", Worker 1 must run the helper when the plan adds logic to any existing `.py` file with at least 150 source lines OR any file under `django_strawberry_framework/optimizer/` or `django_strawberry_framework/types/`. For Slice 3:
  - `examples/fakeshop/config/schema.py` is 29 source lines (verified via `wc -l`), well below the 150-line threshold; it is not under `optimizer/` or `types/`. Skip reason: file is 29 source lines, below the 150-line threshold; not under `optimizer/` or `types/`.
  - The two audit-only files (`examples/fakeshop/apps/library/schema.py`, `examples/fakeshop/apps/products/schema.py`) are NOT modified by the slice, so the "adds logic to" trigger does not fire. Skip reason: audit-only; no logic added.
  - Worker 3 may run the helper at review time per its own threshold rules; Worker 1's planning-pass skip does not bind Worker 3.
  - **Investigation (Worker 1):** Not real. This bullet is a meta-record of a process decision (Worker 1 in the closed BUILD cycle skipped `scripts/review_inspect.py` per documented threshold rules), not a DRY opportunity — no source duplication, no repeated literal, no helper to extract; the two threshold conditions (`examples/fakeshop/config/schema.py` at 29 source lines below the 150-line bar, not under `optimizer/`/`types/`; audit-only files unmodified) are correctly applied and the cited file/rules still hold in current source.

---

## Build: Slice 4 — Docs

_Source: `docs/builder/bld-slice-4-docs.md`_

- [x] Existing patterns reused: the `strawberry.Schema(query=Query, config=strawberry_config(), extensions=[DjangoOptimizerExtension()])` shape established by Slice 3 in `examples/fakeshop/config/schema.py` (Worker 1's Slice 3 final-verification memory line: "the canonical post-migration consumer contract"). Every doc-side example aligns to that kwarg order: `query=`, `config=`, `extensions=`. Slice 2 already migrated 10 sites in `tests/types/test_converters.py` and 2 integration tests in `tests/test_scalars.py` to the same shape; Slice 4 repeats the pattern on five doc surfaces (`docs/README.md`, `docs/GLOSSARY.md`, `GOAL.md`, `TODAY.md`, plus the CSV).
  - **Investigation (Worker 1):** Already addressed. Walked every Slice-4-named site against the live tree at SHA `b1a6d01ff8eea9b4dd6b9d0d425451181cc6000b`: `docs/README.md` Quick start (lines 45-49, multi-line), Schema setup boundary "Recommended" (line 138, single-line), and "Wrong order" anti-example (line 144, single-line) all carry `query=Query, config=strawberry_config(), extensions=[DjangoOptimizerExtension()]` in that kwarg order; `docs/GLOSSARY.md` `## BigInt scalar` paragraph (line 179, inline) and the new `## strawberry_config` entry's three code blocks (lines 985-989, 996-999, 1005-1008) all use the same order (the second/third blocks legitimately omit `extensions=` because they demonstrate `extra_scalar_map=` / passthrough kwargs); `GOAL.md` line 155 carries `query=Query, config=strawberry_config()` single-line (no `extensions=`, consistent with the pre-Slice-4 showcase shape per spec line 565); `TODAY.md` lines 137-141 carry the full multi-line trio mirroring `docs/README.md` Quick start; and `docs/spec-020-scalar_map_helper-0_0_7-terms.csv` line 18 carries the `strawberry_config` row that brings the term count to `OK: 17 terms`. No per-doc drift in kwarg order; the recap was a process-level DRY observation, not a remaining opportunity. (Note: `docs/GLOSSARY.md` lines 349 and 446 — `## DjangoOptimizerExtension` and `## finalize_django_types` entries — still carry the legacy `strawberry.Schema(query=Query, extensions=[DjangoOptimizerExtension()])` shape without `config=strawberry_config()`; those entries are NOT in Slice 4's spec-pinned scope (the spec named only the `## BigInt scalar` entry update, new `## strawberry_config` entry, Public exports bullet, and Index row) and so are out of scope for this finding's disposition.)
- [x] New helpers justified: none. Slice 4 ships zero Python source. The repetition of `config=strawberry_config()` across consumer-facing docs IS the demonstration consumers must read; Decision 2 in the spec already rejected a `dst.Schema(...)` wrapper to consolidate it. No new helper warranted.
  - **Pre-triage (Worker 0):** Skipped — bullet opens with "none. Slice 4 ships zero Python source … No new helper warranted".
- [x] Duplication risk avoided: the only DRY hazard is per-doc drift in (a) kwarg order, (b) whether the helper is on its own line vs inline, (c) the exact GLOSSARY-entry body. Steps 1-4 below pin the verbatim spec body for the GLOSSARY entry and the kwarg order Slice 3 already shipped to fakeshop.
  - **Investigation (Worker 1):** Already addressed. Walked the five Slice-4 doc surfaces against HEAD `b1a6d01ff8eea9b4dd6b9d0d425451181cc6000b` (matches finding baseline `5d84d47b57a08694194dfd1c49a3e94dbe154210` for the docs touched here): (a) kwarg order `query=`, `config=strawberry_config()`, `extensions=[...]` is consistent at every site — `docs/README.md` Quick start (lines 45-49 multi-line), Schema setup boundary Recommended (line 138 single-line) and Wrong-order anti-example (line 144 single-line), `docs/GLOSSARY.md` `## BigInt scalar` paragraph (line 179 inline) and `## strawberry_config` entry's three code blocks (lines 985-989, 996-999, 1005-1008; the second and third legitimately omit `extensions=` because they showcase `extra_scalar_map=` / passthrough kwargs), `GOAL.md` line 155 (`query=Query, config=strawberry_config()` single-line, no `extensions=` per spec line 565), and `TODAY.md` lines 137-141 (multi-line trio mirroring `docs/README.md` Quick start). (b) Inline-vs-own-line shape is preserved per pre-Slice-4 layout at every site (multi-line at Quick start / TODAY / GLOSSARY's first block; single-line at Schema setup boundary Recommended / Wrong-order to preserve the anti-example contrast; single-line at GOAL astronomy). (c) GLOSSARY-entry body verbatim check: stripped the spec's `    > ` blockquote markers from `docs/spec-020-scalar_map_helper-0_0_7.md` lines 516-551 and `diff`'d against `docs/GLOSSARY.md` lines 978-1013 — byte-identical. The recap bullet was a process-level Slice-4 DRY observation that the implementation already pinned; no remaining opportunity.
- [x] Helper skip reason: Slice 4 touches only Markdown / CSV. No Python logic added, no `.py` files modified. Per `docs/builder/BUILD.md` "When to run the helper during build", Worker 1 runs the helper only when the plan adds logic to a Python file >=150 source lines OR any file under `optimizer/` / `types/`. None of those apply here. Helper skipped; recorded for the cross-slice integration pass.
  - **Investigation (Worker 1):** Not real. Meta-record about why `scripts/review_inspect.py` was skipped during Slice 4 — the slice ships zero Python (`bld-slice-4-docs.md` `### Files touched` lists only `docs/README.md`, `docs/GLOSSARY.md`, `GOAL.md`, `TODAY.md`, and `docs/spec-020-scalar_map_helper-0_0_7-terms.csv`; `git diff 5466d2b94f46e7ff7778419d22917995a3e6b1c2 -- '*.py'` is empty). The BUILD.md trigger rule (helper runs only for `.py` files >=150 SLOC or `optimizer/` / `types/` touches) did not fire, so the skip was correct. No source-code consolidation is implied — the bullet is documenting the rule application, not asserting duplication to consolidate.
- [x] Pre-Slice-4 CSV term count: `uv run python scripts/check_spec_glossary.py --spec docs/spec-020-scalar_map_helper-0_0_7.md` -> `OK: 16 terms — all have glossary entries and at least one spec link.` Post-Slice-4 expected: `OK: 17 terms` (per spec DoD item 9a). The new row this slice adds is for the term `strawberry_config`; the script run post-Slice-4 confirms the row count rose by exactly one.
  - **Investigation (Worker 1):** Already addressed. Verification artifact — bullet records the pre-Slice-4 baseline (16 terms) against the post-Slice-4 expected count (17 terms), no source consolidation implied. Confirmed against HEAD `b1a6d01ff8eea9b4dd6b9d0d425451181cc6000b`: `uv run python scripts/check_spec_glossary.py --spec docs/spec-020-scalar_map_helper-0_0_7.md` reports `OK: 17 terms — all have glossary entries and at least one spec link.`; `docs/spec-020-scalar_map_helper-0_0_7-terms.csv` is 18 lines (1 header + 17 rows) with the final row reading `strawberry_config,strawberry_config,The factory function this card introduces; entry created in Slice 4.`. Post-Slice-4 state matches expectation exactly; nothing to consolidate.

---

## Build: Slice 5 — KANBAN + CHANGELOG

_Source: `docs/builder/bld-slice-5-kanban_and_changelog.md`_

- [x] **Existing patterns reused.** The canonical text drop-ins for this slice are pre-pinned verbatim in the spec's `## Doc updates` section: the past-tense Done body at `docs/spec-020-scalar_map_helper-0_0_7.md` line 577 (the long paragraph beginning "Pinned the package-defined scalar registration path…"), the `### Added` paragraph at spec line 584, the `### Changed` Breaking-change paragraph with before/after diff at spec lines 587-595, and the `### Removed` paragraph at spec line 598. Worker 2 copies these verbatim — no paraphrasing. The KANBAN past-tense Done-body wording also already exists as a precedent in the `DONE-016-0.0.7` / `DONE-017-0.0.7` / `DONE-019-0.0.7` cards' Done bodies (`KANBAN.md::DONE-016-0.0.7`, `KANBAN.md::DONE-017-0.0.7`, `KANBAN.md::DONE-019-0.0.7`) — the past-tense shape, the per-slice file list, the spec citation, and the version-bump-deferred sentence are the same pattern Worker 2 mirrors.
  - **Investigation (Worker 1):** Already addressed. The spec's verbatim drop-ins landed in the working tree at the cited locations: `KANBAN.md` line 1675 carries the new `### DONE-047-0.0.7 — Warning-free scalar registration via \`StrawberryConfig.scalar_map\`` heading with the past-tense Done body from spec line 577 (line 1677) plus the `Build plan:` citation paragraph (line 1679); zero `WIP-ALPHA-020-0.0.7` headings remain and `grep -c "docs/spec-scalar_map_helper.md" KANBAN.md` = 0 confirms the stale-spec-path bullet was dropped wholesale via deletion-as-move. `CHANGELOG.md` `[Unreleased]` now carries `### Added` (line 20 with the spec-line-584 paragraph at line 21), the appended `### Changed` Breaking-change bullet (lines 27-35 with the inner triple-backtick `diff` fence), and `### Removed` (line 40 with the spec-line-598 paragraph at line 41); ordering is `Added → Changed → Fixed → Removed` per the spec's explicit placement rule. `grep -c "Migration to a" CHANGELOG.md` = 0 confirms the `[0.0.6] ### Notes` removal. The Done-body shape (past-tense paragraph + `Spec:` citation + `Build plan:` citation; no `Parity:` line per discretion item 4) mirrors the `DONE-019-0.0.7` precedent as the finding describes. Worker 3's review pass already verified byte-identical match against the spec drop-ins (`bld-slice-5-kanban_and_changelog.md` lines 153-158); no DRY action needed.
- [x] **New helpers justified.** None. Slice 5 is Markdown-only.
  - **Pre-triage (Worker 0):** Skipped — bullet opens with "None. Slice 5 is Markdown-only."
- [x] **Duplication risk avoided.** The past-tense Done body (in `KANBAN.md`) and the `[Unreleased] ### Changed` Breaking-change paragraph (in `CHANGELOG.md`) both describe the same migration. The repetition is intentional — `KANBAN.md` tells the historical card story (what shipped, which files, which spec); `CHANGELOG.md` tells the consumer-facing release story (what breaks for consumers, exact migration diff). Not a DRY violation per BUILD.md severity definitions; both surfaces are required by spec DoD items 15 and 16.
  - **Investigation (Worker 1):** Not real. The bullet itself disclaims the duplication as intentional sibling content with distinct audiences (historical card story in `KANBAN.md::DONE-047-0.0.7` vs consumer-facing release story in `CHANGELOG.md::[Unreleased] ### Changed`); both surfaces are mandated by spec DoD items 15 and 16 and were independently confirmed non-violating in Worker 3's `bld-slice-5-kanban_and_changelog.md` `### DRY findings` block ("None. The deliberate duplication … is justified by the plan's DRY analysis and is mandated by DoD items 15 and 16"). No code-side consolidation applies — both targets are Markdown release surfaces, not shared logic.

**Static inspection helper:** SKIPPED — Slice 5 touches only Markdown (`KANBAN.md`, `CHANGELOG.md`). `scripts/review_inspect.py` operates on Python source; not applicable here. Recorded per BUILD.md "Worker 1 and Worker 3 may skip the helper for files where the artifact will be a 'no review-worthy logic' disposition … The skip must be recorded explicitly with a short reason."

---

## Final test-run gate

1. `uv run pytest --no-cov` — **pass**. 817 passed, 3 skipped, 6 warnings in 27.68s; no failures across `tests/`, `examples/fakeshop/tests/`, or `examples/fakeshop/test_query/`.
2. `uv run python examples/fakeshop/manage.py check` — **pass**. "System check identified no issues (0 silenced)."
3. `uv run python examples/fakeshop/manage.py makemigrations --check --dry-run` — **pass**. "No changes detected" — example-app migrations stay in sync with models.
4. `uv run ruff format --check .` — **pass**. "118 files already formatted" (the standing `COM812`-vs-formatter advisory warning is unrelated to this cycle).
5. `uv run ruff check .` — **pass**. "All checks passed!"
6. `git diff --check` — **pass**. Exit 0; no whitespace errors or conflict markers in the working-tree diff (docs-only changes under `docs/dry/dry-0_0_7.md`).

All six gate steps passed; cycle is ready for maintainer closeout.
