# DRY consolidation plan: 0.0.8

Source: `docs/builder/`
Generated: 2026-06-03
Workflow: `docs/dry/DRY.md`
Pre-flight: passed on 2026-06-03; exporter smoke: pass; plan path freed by maintainer ("clear all old artifacts and start from scratch"); prior half-done dry-0_0_8.md removed and build worker-memory reset. Baseline: EXPLICIT (working tree carries pre-existing unrelated maintainer changes — CHANGELOG.md, KANBAN.md, docs/builder/bld-final.md, docs/builder/build-028-orders-0_0_8.md, docs/feedback.md, docs/spec-028-orders-0_0_8.md, and four test files). Per-finding `git stash create` baselines isolate each finding's diff from this dirty tree. Source cycle (spec-028-orders-0_0_8 build) treated as closed by maintainer invocation.

One finding at a time. Worker 0 dispatches > Worker 1 triages > Worker 2 implements > Worker 1 verifies > Worker 0 ticks.

## Findings

## Build: Cross-slice integration pass

_Source: `docs/builder/bld-integration.md`_

The integration-pass DRY scan walks the seven inputs required by `docs/builder/BUILD.md` "Cross-slice integration pass":

1. Every prior `docs/builder/bld-slice-*.md` artifact read in slice order. All six artifacts are at `Status: final-accepted`; every per-slice `What looks solid` / `DRY findings` / `Notes for Worker 1` block was walked.
2. `scripts/review_inspect.py --all --output-dir docs/shadow` re-ran at the start of this pass (43 package `.py` files refreshed). Per-file `Repeated string literals` sections cross-walked.
3. `Imports` sections cross-walked to confirm one-way dependency direction.
4. Naming-symmetry scan walked every order-side / filter-side / type-side pair Slices 1-3 introduced.
5. The five deferred items Worker 0 surfaced (collision-message wording, `_isolate_registry` duplication, DONE-027 "carried by sibling" closure, `_field_type_suffix` dead surface, Slice 3 `_validate_optimizer_hints` parked follow-up) all addressed below.

---

## Build: Slice 1 — Foundation — module layout + Order / RelatedOrder primitives + OrderSet metaclass

_Source: `docs/builder/bld-slice-1-foundation.md`_

The Slice 1 work is intentionally a class-substitution mirror of the shipped Filtering subsystem; the DRY question is which pieces port verbatim, which get re-imported through the neutral home, and which are genuinely new. Every cite below uses the symbol-qualified `path::Symbol` form per `AGENTS.md` #"Source references in docs and code comments" — these are standing-doc references, not per-cycle scratchpad refs.

- [x] **Existing patterns reused (verbatim sibling-import, no copy).**
  - `django_strawberry_framework/sets_mixins.py::LazyRelatedClassMixin` — the cookbook's Layer-2 resolution mixin. `orders/base.py::RelatedOrder` inherits from it via `from ..sets_mixins import LazyRelatedClassMixin` (NOT from `filters/base.py`, per Spec Revision 4 H1 and the build-plan's "Shared mixins live in the neutral module" build-wide flag). The shipped `django_strawberry_framework/filters/base.py::RelatedFilter` already uses this exact import shape (`from ..sets_mixins import LazyRelatedClassMixin`), so the order side mirrors that import line one-for-one.
  - `django_strawberry_framework/sets_mixins.py::ClassBasedTypeNameMixin` — supplies the `{cls.__name__}InputType` naming rule shared across the set family. `orders/sets.py::OrderSet` inherits this mixin (same shape `django_strawberry_framework/filters/sets.py::FilterSet` uses at the `class FilterSet(ClassBasedTypeNameMixin, filterset.BaseFilterSet, metaclass=FilterSetMetaclass)` declaration head). `OrderSet` does NOT override `_field_type_suffix` — the cookbook's upstream `django_graphene_filters/orderset.py::AdvancedOrderSet` keeps the default `"InputType"` for both root and per-field suffixes (verbatim from upstream lines 57-58). This is the divergence from `FilterSet`, which overrides `_field_type_suffix = "FilterInputType"`. The default keeps `BookOrder` → `BookOrderInputType` (root) and any future per-field-bag use → `BookOrderShelfInputType` (consistent with the cookbook).
  - `django_strawberry_framework/exceptions.py::ConfigurationError` — raised by `_input_type_name_for` when a field path collapses to empty (via the `ClassBasedTypeNameMixin.type_name_for` guard) and by `materialize_input_class` (Slice 3 wiring will land the latter call site, but the Slice 1 namespace plumbing already imports the exception so the contract is pinned).
  - `django_strawberry_framework/filters/inputs.py::INPUTS_MODULE_PATH` shape — the order side's `INPUTS_MODULE_PATH` is its own module string `"django_strawberry_framework.orders.inputs"`, but the symbol name and the use-pattern (single hoisted constant referenced by every `strawberry.lazy(...)` call site AND by `materialize_input_class`'s `sys.modules[INPUTS_MODULE_PATH]` lookup) is copied verbatim from `django_strawberry_framework/filters/inputs.py::INPUTS_MODULE_PATH`. Per Spec Decision 2, the constant is hoisted at the top of `orders/inputs.py`.
  - `django_strawberry_framework/filters/inputs.py::_input_type_name_for` shape — the order side's `_input_type_name_for` delegates to `orderset_class.type_name_for()` (the inherited `ClassBasedTypeNameMixin` method); same one-line implementation as the filter side. Reusing the mixin keeps both subsystems pinned to one naming derivation.
  - `django_strawberry_framework/filters/inputs.py::_materialized_names` shape — Slice 1 ships `_materialized_names: dict[str, type]` at module scope of `orders/inputs.py` per Spec Decision 9 M1 of rev3 (the value is the materialized **input class**, NOT the source `OrderSet`; source-class collision tracking lives in Slice 2's `OrderArgumentsFactory._type_orderset_registry`). Same key-shape and lifetime contract as the filter side's ledger — different module, disjoint namespace per Spec Decision 9 line 703.
  - `django_strawberry_framework/filters/inputs.py::materialize_input_class` shape — Slice 1 ships the order-side `materialize_input_class(name: str, input_cls: type) -> None` body modelled on the filter side's idempotent `(name, cls)` check + `ConfigurationError` on collision + `sys.modules[INPUTS_MODULE_PATH]` setattr. The function is **shipped in Slice 1 but called only by Slice 3's `_bind_ordersets()` materialize subpass**; Slice 1 lands the namespace plumbing so Slice 2's `OrderArgumentsFactory` and Slice 3's finalizer wiring can both depend on a stable, tested helper.
  - `django_graphene_filters/orders.py::BaseRelatedOrder` (cookbook source — `~/projects/django-graphene-filters/django_graphene_filters/orders.py` lines 8-29) — ported to `orders/base.py::RelatedOrder` per Spec Decision 2 with `BaseRelatedOrder` + `RelatedOrder` collapsed into a single consumer-facing class (mirroring the filter side's Decision-2 single-symbol collapse at `django_strawberry_framework/filters/base.py::RelatedFilter`). `__init__`, `bind_orderset`, `.orderset` property + setter — all body-equivalent to upstream lines 11-28.
  - `django_graphene_filters/orderset.py::OrderSetMetaclass` (cookbook source — `~/projects/django-graphene-filters/django_graphene_filters/orderset.py` lines 14-43) — ported to `orders/sets.py::OrderSetMetaclass`. The upstream metaclass's inherited-then-overridden `related_orders` OrderedDict shape is preserved verbatim (cookbook lines 30-40); the only adaptation is replacing the cookbook's `orders.BaseRelatedOrder` import with the local `from .base import RelatedOrder` (Spec Decision 2 collapses `BaseRelatedOrder` and `RelatedOrder` into the one shipped class).
  - `django_graphene_filters/orderset.py::AdvancedOrderSet` (cookbook lines 46-58 for the class head; the body is Slice 2's domain) — ported to `orders/sets.py::OrderSet`. Slice 1 ships the class head + the `_owner_definition` slot + `_expanded_fields` / `_is_expanding_fields` cache slots + the `Meta` validation + the cookbook's `get_fields` (Layer 4 expansion, cookbook lines 265-285) only. The cookbook's `__init__`-driven application (lines 60-84), the `check_permissions` body (lines 86-113), the `get_flat_orders` body (lines 115-170), and the `apply_distinct` branches (lines 172-263) ALL move to Slice 2 or live as no-ops in Slice 1 because `OrderArgumentsFactory` (the BFS consumer of `get_fields()`) lands in Slice 2. Slice 1 ships only the class slots and the `Meta`-driven primitive shape so Slice 2's BFS has a target to consume.
  - **Investigation (Worker 1):** Already addressed. This is a shipped-build DRY self-report; every cited reuse exists in the live source exactly as described (no pending extraction). Confirmed against `HEAD`:
    - Sibling-import of `LazyRelatedClassMixin`: `django_strawberry_framework/orders/base.py::RelatedOrder` head plus the `from ..sets_mixins import LazyRelatedClassMixin` line — mirrors `django_strawberry_framework/filters/base.py #"from ..sets_mixins import LazyRelatedClassMixin"` (the filter side declares `class RelatedFilter(LazyRelatedClassMixin, ModelChoiceFilter)`). No `class LazyRelatedClassMixin` redefinition in `orders/`.
    - Sibling-import of `ClassBasedTypeNameMixin`: `django_strawberry_framework/orders/sets.py::OrderSet` declares `class OrderSet(ClassBasedTypeNameMixin, metaclass=OrderSetMetaclass)` and does NOT override `_field_type_suffix` (the inherited `django_strawberry_framework/sets_mixins.py::ClassBasedTypeNameMixin #"_field_type_suffix: str = \"InputType\""` default is kept), the named divergence from `django_strawberry_framework/filters/sets.py::FilterSet #"_field_type_suffix: str = \"FilterInputType\""`. Matches cookbook `~/projects/django-graphene-filters/django_graphene_filters/orderset.py::AdvancedOrderSet` lines 57-58 (`_root_type_suffix = "InputType"` / `_field_type_suffix = "InputType"`).
    - `ConfigurationError`: imported and raised in `django_strawberry_framework/orders/inputs.py::materialize_input_class` (collision branch); also imported in `orders/sets.py`.
    - Hoisted `INPUTS_MODULE_PATH`: `django_strawberry_framework/orders/inputs.py #"INPUTS_MODULE_PATH: str = \"django_strawberry_framework.orders.inputs\""` — own module string, same hoisted-constant shape as `django_strawberry_framework/filters/inputs.py #"INPUTS_MODULE_PATH: str = \"django_strawberry_framework.filters.inputs\""`, referenced by `materialize_input_class`'s `sys.modules[INPUTS_MODULE_PATH]` lookup and the `strawberry.lazy(INPUTS_MODULE_PATH)` call sites.
    - `_input_type_name_for`: `django_strawberry_framework/orders/inputs.py::_input_type_name_for` is the one-line `return orderset_class.type_name_for()` delegate, mirroring `django_strawberry_framework/filters/inputs.py::_input_type_name_for`.
    - `_materialized_names` ledger: `django_strawberry_framework/orders/inputs.py #"_materialized_names: dict[str, type] = {}"`, disjoint from the filter side's `django_strawberry_framework/filters/inputs.py #"_materialized_names: dict[str, type] = {}"`.
    - `materialize_input_class`: `django_strawberry_framework/orders/inputs.py::materialize_input_class` is the `(name, input_cls)`-idempotent + `ConfigurationError`-on-collision + `setattr(sys.modules[INPUTS_MODULE_PATH], ...)` body modelled on `django_strawberry_framework/filters/inputs.py::materialize_input_class`.
    - Cookbook ports: `orders/base.py::RelatedOrder` collapses `~/projects/django-graphene-filters/django_graphene_filters/orders.py::BaseRelatedOrder` (lines 8-29) + `::RelatedOrder` (lines 31-40) into one class with `field_name` relaxed to optional; `orders/sets.py::OrderSetMetaclass` is the verbatim port of cookbook `orderset.py::OrderSetMetaclass` lines 14-43 (`from .base import RelatedOrder` is the only local adaptation).
    - **Reconciliation note:** the artifact described the Slice-1-scoped subset; the shipped `orders/inputs.py` and `orders/sets.py` now also carry the Slice 2/3 surface (adapters, `apply_*`, `clear_order_input_namespace`, the `_helper_referenced_ordersets` ledger in `orders/__init__.py`). That later surface does not contradict any Slice 1 reuse claim — the foundation pieces are intact within the fully-shipped modules. No stale symbol names or moved anchors required correcting in the `- [ ]` text. No pending consolidation.

- [x] **New helpers justified (none for Slice 1).** Slice 1 is the foundation pass — every helper named in the Slice 1 contract is either a port from the cookbook or a sibling-import from the neutral `sets_mixins.py`. The genuinely new modules (`orders/factories.py::OrderArgumentsFactory`, `orders/inputs.py::convert_order_field_to_input_annotation`, `orders/inputs.py::normalize_input_value`) all belong to Slice 2 per the spec's Slice 2 checklist. Slice 1 ships zero net-new shared helpers across the package.
  - **Pre-triage (Worker 0):** Skipped — no-opportunity recap: "Slice 1 ships zero net-new shared helpers across the package" (the new-helper dimension is vacuous when zero new helpers exist; the genuinely-new modules are dispatched under Slice 2).

- [x] **Duplication risk avoided (three risks named explicitly).**
  - **Risk: copy-paste `LazyRelatedClassMixin` into `orders/base.py`.** Naive implementation would replicate the resolution algorithm in two places, silently bifurcating behaviour the first time a maintainer fixes one and forgets the other. **Avoidance:** Slice 1 imports `LazyRelatedClassMixin` from `..sets_mixins` (the explicit build-plan rule); Worker 3 should flag any `class LazyRelatedClassMixin` definition inside `orders/base.py` as a High finding. Spec Revision 4 H1 already retired the alternative; the rejected-alternative prose at Spec Decision 2's third alternative is the historical breadcrumb.
  - **Risk: re-deriving `<Name>OrderInputType` inline at every call site.** The filter side hoisted `_input_type_name_for` to one helper specifically to dodge this exact bifurcation (filter spec-027 M3 of rev1). **Avoidance:** Slice 1 ships `orders/inputs.py::_input_type_name_for(orderset_class)` as a one-line `orderset_class.type_name_for()` delegate at module scope, with the same call surface as the filter side's helper. Slice 2's `OrderArgumentsFactory` and Slice 3's `order_input_type` consumer helper both call it.
  - **Risk: `_helper_referenced_ordersets` migrating to `orders/inputs.py` for "convenience" with the namespace plumbing.** Per Spec Decision 2 M9 of rev1 AND N-new-2 of rev2, the ledger lives in `orders/__init__.py` co-located with its only writer (`order_input_type`, which is the public helper that hosts the writer). Putting it in `inputs.py` would force the import-cycle dance the filter side already designed against. **Avoidance:** Slice 1 does NOT ship `_helper_referenced_ordersets` in `inputs.py`; the ledger is **planned for** `orders/__init__.py` but the writer (`order_input_type`) does not exist until Slice 2 (per Spec line 850-853 — `order_input_type` is part of the Slice 2 consumer-helper surface; Slice 1 ships only the re-exports the helper will need, namely the namespace plumbing in `inputs.py`). For Slice 1, `orders/__init__.py` carries only the `__all__` skeleton + re-exports of `RelatedOrder`, `OrderSet`, `OrderSetMetaclass`, `Ordering`. The ledger is added to `__init__.py` in Slice 2 alongside `order_input_type`. Worker 3 should NOT flag the ledger's absence in Slice 1.
  - **Investigation (Worker 1):** Already addressed. All three named avoidances hold in the fully-shipped source (the ledger and `order_input_type` writer that Slice 1 deferred to Slice 2 have since landed; the foundation-pass avoidances survived). Confirmed against `HEAD`:
    - **Risk 1 (no copy-paste mixin):** `django_strawberry_framework/orders/base.py #"from ..sets_mixins import LazyRelatedClassMixin"` then `django_strawberry_framework/orders/base.py::RelatedOrder` declares `class RelatedOrder(LazyRelatedClassMixin)` — sibling-import, no redefinition. `grep -rn "class LazyRelatedClassMixin" django_strawberry_framework/orders/` returns empty; the single definition lives at `django_strawberry_framework/sets_mixins.py::LazyRelatedClassMixin` and is also sibling-imported by `django_strawberry_framework/filters/base.py #"from ..sets_mixins import LazyRelatedClassMixin"`. The avoidance holds.
    - **Risk 2 (single `_input_type_name_for` delegate, no inline re-derivation):** `django_strawberry_framework/orders/inputs.py::_input_type_name_for` is the one-line `return orderset_class.type_name_for()` delegate (no inline `f"{...}InputType"` formula). Every order-side call site routes through it: `orders/inputs.py::_build_input_fields #"target_name = _input_type_name_for(target_os)"`, `orders/factories.py::OrderArgumentsFactory` (three sites — `#"self.order_input_type_name = _input_type_name_for(orderset_class)"`, `#"target_name = _input_type_name_for(os_class)"`, `#"type_name = _input_type_name_for(os_class)"`), and `orders/__init__.py::order_input_type #"name = _input_type_name_for(orderset_class)"`. The `{cls.__name__}{suffix}` formula itself lives exactly once, at `django_strawberry_framework/sets_mixins.py::ClassBasedTypeNameMixin.type_name_for`. The only textual `InputType` occurrences inside `orders/` are docstrings/comments, not executable re-derivations. The avoidance holds.
    - **Risk 3 (ledger in `__init__.py`, not `inputs.py`):** `django_strawberry_framework/orders/__init__.py #"_helper_referenced_ordersets: set[type[OrderSet]] = set()"` defines the ledger, and its sole writer is co-located at `orders/__init__.py::order_input_type #"_helper_referenced_ordersets.add(orderset_class)"`. `orders/inputs.py` never defines or writes the ledger — it references it only in module docstring/comment lines (`#"plus the separate \`\`_helper_referenced_ordersets.clear()\`\` block"` and the `clear_order_input_namespace` note `#"Does NOT touch \`\`_helper_referenced_ordersets\`\`"`), confirming the deliberate split that dodges the import-cycle dance. The avoidance holds.
    - **Reconciliation note:** the artifact framed Risk 2/3 in Slice-1-deferred terms (`order_input_type` and the ledger "do not exist until Slice 2"). The shipped source now carries the Slice 2 surface, so the writer and ledger are present — this strengthens rather than contradicts the avoidance claims. No stale symbol names or moved anchors required correcting in the `- [ ]` text. No pending consolidation.

---

## Build: Slice 2 — Factories — OrderArgumentsFactory BFS + inputs adapters + apply_sync/async

_Source: `docs/builder/bld-slice-2-factories.md`_

Slice 2 is a one-for-one mirror of the shipped Filtering subsystem's BFS factory + adapter quartet + resolver-facing classmethod pair, with the cookbook's order-side simplifications layered on top. Every cite below uses the symbol-qualified `path::Symbol` form per `AGENTS.md` #"Source references in docs and code comments" plus inline raw `path:NN` line refs (allowed per `AGENTS.md` since this is a `bld-*.md` scratchpad).

- [x] **Existing patterns reused (verbatim sibling-import / structural mirror, no copy).**
  - `django_strawberry_framework/filters/factories.py::FilterArgumentsFactory` (filters/factories.py:56-182) — the BFS class is ported with the substitutions `FilterSet`→`OrderSet`, `RelatedFilter`→`RelatedOrder`, `related_filters`→`related_orders`, `_type_filterset_registry`→`_type_orderset_registry`, `_input_type_name_for(filterset_class)`→`_input_type_name_for(orderset_class)` (the latter delegates to the same `ClassBasedTypeNameMixin.type_name_for()` on the order side). The cookbook source at `~/projects/django-graphene-filters/django_graphene_filters/order_arguments_factory.py::OrderArgumentsFactory` (lines 35-141) is the original this filter port itself mirrors; the two BFS bodies are structurally identical (queue `pending`, `seen` set, name-collision check at `_type_orderset_registry`, build via `_build_class_type`, enqueue every `related_orders` target). Slice 2 ports the filter side's shape directly (the closer mirror) rather than the cookbook (the upstream) because the filter side already encoded the package-specific decisions (FifO queue order, collision-error wording, the `existing_owner is not None and existing_owner is not fs_class` guard) the order side mirrors per the build-plan's "Filter subsystem is shipped … every divergence is a finding" rule.
  - `django_strawberry_framework/filters/inputs.py::_build_input_fields` (filters/inputs.py:649-780) — the adapter that walks `<Set>.get_fields()` and emits `(python_attr, annotation, field_kwargs)` triples for `build_input_class`. The order side's `_build_input_fields(orderset_cls, owner_definition)` ports the filter shape with three intentional simplifications named in Spec Decision 8: (a) NO per-field operator-bag class build (every leaf annotation is `Ordering | None`, NOT a `<Owner>OrderFooInputType` bag class); (b) NO `_build_logic_fields` (no `and_` / `or_` / `not_` operator bag — the cookbook's `AdvancedOrderSet` has no analogue); (c) NO `HIDE_FLAT_FILTERS` / `is_expanded_child` skip (the order side does not expose flat `*__lookup` paths through `django-filter` expansion — flat shorthand paths come only from `Meta.fields = ["shelf__code"]` per Spec Edge cases "**`Meta.fields = ["shelf__code"]`**" line 980, and the runtime normalizer reconstructs them via `FieldSpec.django_source_path`). The grouping-by-top-level-name step (filters/inputs.py:666-690) reduces to a one-pass walk on the order side because every `get_fields()` entry is either a `RelatedOrder` (related) or `None` (leaf) — no operator-bag flattening. `FieldSpec` (filters/inputs.py:118-160) shape ports verbatim with the same `(python_attr, graphql_name, django_source_path)` fields and the same `(orderset_cls, python_attr)` key shape; the per-field provenance map `_field_specs: dict[tuple[type, str], FieldSpec]` is the runtime path `normalize_input_value` consults at apply time. Spec Decision 9 Test plan line 1010 names this map explicitly ("walks nested `RelatedOrder` input and produces flat `(field_path, Ordering)` tuples (including the `shelf__code` path for `RelatedOrder("ShelfOrder", field_name="shelf")` branches)").
  - `django_strawberry_framework/filters/inputs.py::build_input_class` (filters/inputs.py:590-621) — the `type(name, (), namespace)` + `strawberry.input(cls)` decoration shape is reused verbatim. The same per-field `strawberry.field(default=None, name=<graphql_alias>)` pattern handles the python-attr → GraphQL alias mapping for the order-side flat-shorthand path (`shelf__code` → python attr `shelf_code` → GraphQL `shelfCode`). Slice 2's `orders/inputs.py::build_input_class` is a direct port; no order-specific divergence is justified.
  - `django_strawberry_framework/filters/inputs.py::convert_filter_to_input_annotation` (filters/inputs.py:341-405) — the order-side equivalent `convert_order_field_to_input_annotation(model_field, owner_definition) -> Any` is genuinely simpler than the filter side: the order side has NO operator-bag / branch-by-filter-class shape (no `RangeFilter` / `ListFilter` / `ChoiceFilter` / `GlobalIDFilter` branches). For ordering, the converter ALWAYS returns the union `Ordering | None` regardless of the model field type — Spec Decision 5 + Spec Test plan line 1010 ("`convert_order_field_to_input_annotation` always returns `Ordering | None` for leaf fields (verified table-driven across scalar / choice / FK / PK / `BigIntegerField` types — ordering does NOT differentiate the value type, only the direction)"). The function still takes `(model_field, owner_definition)` for signature symmetry with the filter side AND so future work (e.g., DISTINCT ON in `0.0.9`) can extend it without changing the call sites — but the body has one expression: `return Ordering | None`. The `model_field` / `owner_definition` arguments are unused at the body level today, kept in the signature deliberately. Worker 2 may add a `# noqa: ARG001` if ruff complains; otherwise the docstring spelling out the future-extension reason satisfies the linter's "argument used in docstring" check.
  - `django_strawberry_framework/filters/inputs.py::normalize_input_value` (filters/inputs.py:408-456) — the order-side `normalize_input_value(order_set_class, raw_value)` is again simpler than the filter side: no filter-class dispatch, no `BaseCSVFilter` / `GlobalIDFilter` / `RangeFilter` / `ListFilter` branches. The order side's function walks the Strawberry input dataclass (whose attributes are either `Ordering | None` for leaves or another `<Target>OrderInputType | None` for `RelatedOrder` branches) and produces a flat `list[tuple[str, Ordering]]` of `(field_path, direction)` tuples that the apply pipeline turns into `OrderBy` expressions via `Ordering.resolve(field_path)`. The recursive descent follows the cookbook's `~/projects/django-graphene-filters/django_graphene_filters/orderset.py::AdvancedOrderSet.get_flat_orders` (cookbook lines 115-170) algorithm with two adaptations: (a) input is a Strawberry dataclass instance, NOT a `list[dict]` (the cookbook accepts the raw GraphQL `list[Mapping]` shape; Slice 2's normalizer accepts the already-deserialized Strawberry input); (b) leaf direction is an `Ordering` enum member (NOT a string `"asc"` / `"desc"` like the cookbook). The function also accepts an empty list / `None` direction per Spec Decision 13 / Spec Edge case "Empty `orderBy:` input (`orderBy: []`)" line 987 and "`orderBy: [{ name: null }]`" line 988 — returns an empty list, skipping any leaf whose direction is `None`.
  - `django_strawberry_framework/filters/sets.py::FilterSet.apply_sync` / `apply_async` (filters/sets.py:1543-1619) — the resolver-facing classmethod pair on the order side mirrors the filter side's shape verbatim with the order-side simplifications enumerated in Spec Decision 8 (the 8-step pipeline). Specifically, `OrderSet.apply_sync(input_value, queryset, info)` does the following in the order Spec Decision 8 pins:
    1. `data = cls._normalize_input(input_value)` — calls `orders.inputs.normalize_input_value(cls, input_value)` to produce the flat `[(field_path, Ordering), ...]` list. Empty list returns the queryset unchanged.
    2. `request = cls._request_from_info(info)` — ports `FilterSet._request_from_info` (filters/sets.py:768-790) verbatim with the wording adapted (`"FilterSet.apply"` → `"OrderSet.apply"` in the `ConfigurationError` messages).
    3. `cls._run_permission_checks(input_value, request)` — ports the filter side's `_run_permission_checks` (filters/sets.py:1007-1141) verbatim with the order-side simplification that there are NO `and` / `or` / `not` logical-branch recursion paths (no operator bag on the order side per Spec Decision 8 line 686). The active-branch double-dispatch (parent's `check_<branch>_permission` for active `RelatedOrder` branches AND the child orderset's per-field gates) ports verbatim per Spec Decision 8 step 6 "Active-branch double-dispatch (per H3 of `docs/feedback.md` rev3, mirroring the filter side's `_run_permission_checks`)". The `_fired: dict[type, set[str]]` dedup map ports verbatim — same shape, different module.
    4. `flat_orders = cls.get_flat_orders(data)` — ports `~/projects/django-graphene-filters/django_graphene_filters/orderset.py::AdvancedOrderSet.get_flat_orders` (cookbook lines 115-170) as a classmethod on `OrderSet`. The cookbook returns `(flat_orders: list[str], distinct_fields: list[str])`; Slice 2's port returns ONLY the flat orders (DISTINCT ON is deferred to `0.0.9` per Spec Decision 12; the `distinct_fields` half is dropped). The returned list is `[(field_path, Ordering), ...]` (tuple-form, not the cookbook's string-prefix form) because Slice 2 produces `OrderBy` expressions via `Ordering.resolve(field_path)` rather than bare `"-name"` strings (Spec Decision 5).
    5. `expressions = [direction.resolve(field_path) for field_path, direction in flat_orders if direction is not None]` — the thin post-pass per Spec Decision 8 step 7 "Expression-conversion layering note" pinned in the prose ("the conversion lives in the apply pipeline layer (not inside `get_flat_orders`)"). Skip-when-`direction is None` handles the `orderBy: [{ name: null }]` null-direction edge case.
    6. `return queryset.order_by(*expressions) if expressions else queryset` — the empty-list / all-null no-op contracts per Spec Edge cases lines 987-988.

    `OrderSet.apply_async(input_value, queryset, info) -> QuerySet` is an `async def` mirror; per Spec Decision 8's sync/async-split + N7-of-rev1 caveat AND per the carry-forward "no async-only `get_queryset` re-derivation" — the order side has NO `_derive_related_visibility_querysets_async` step (the filter side's lines 1602-1606 do not port). The order side's `apply_async` body is:
    ```
    data = cls._normalize_input(input_value)
    request = cls._request_from_info(info)
    await sync_to_async(cls._run_permission_checks, thread_sensitive=True)(input_value, request)
    flat_orders = cls.get_flat_orders(data)
    expressions = [direction.resolve(field_path) for field_path, direction in flat_orders if direction is not None]
    return queryset.order_by(*expressions) if expressions else queryset
    ```
    The `sync_to_async(thread_sensitive=True)` wrap around `_run_permission_checks` matches the filter side's lines 1612-1615 verbatim (Spec Decision 8's sync/async-split section quotes the N7-of-rev1 caveat). The `get_flat_orders` and `.order_by(...)` calls do NOT wrap in `sync_to_async` because they are pure-Python parsing + a queryset-method call that does no I/O (Spec Decision 8 step 7 line 670 "the apply pipeline does a thin post-pass that walks `get_flat_orders`'s output" + the N7-of-rev1 docstring caveat names this rationale explicitly).
  - `django_strawberry_framework/sets_mixins.py::LazyRelatedClassMixin` / `ClassBasedTypeNameMixin` — both are already in use by Slice 1's `RelatedOrder` and `OrderSet` respectively. Slice 2 adds NO new sibling-import dependencies; the `orders/factories.py` module imports `from .sets import OrderSet`, `from .base import RelatedOrder`, and `from .inputs import _build_input_fields, _input_type_name_for, build_input_class` — all intra-subpackage imports.
  - `django_strawberry_framework/exceptions.py::ConfigurationError` — Slice 1 already imports this in `orders/inputs.py::materialize_input_class`; Slice 2 imports it in `orders/factories.py` for the `_type_orderset_registry` collision-detection path AND in `orders/inputs.py` for the `_get_concrete_field_names_for_order` model-validation path (if a `Meta.model` declaration references a property instead of a field, the helper raises with the same error wording the cookbook uses).
  - `django_strawberry_framework/filters/__init__.py::_helper_referenced_filtersets` (filters/__init__.py — module scope; the ledger is the set the filter side maintains for `filter_input_type` orphan tracking) — `orders/__init__.py::_helper_referenced_ordersets: set[type[OrderSet]] = set()` is the order-side equivalent. Per Spec Decision 2 N-new-2 of rev2: the ledger is co-located with its only writer (`order_input_type` lives in `__init__.py`). Slice 2 lands BOTH the ledger AND the `order_input_type` helper. Per Spec Decision 11 + Spec Decision 11's code block (spec lines 813-861), the helper signature is `order_input_type(orderset_class: type["OrderSet"]) -> object`; the body validates eagerly (`TypeError` for non-`OrderSet`), records into `_helper_referenced_ordersets`, and returns `Annotated[name, strawberry.lazy(INPUTS_MODULE_PATH)]` (the **element type** per B1 of rev3).
  - `django_strawberry_framework/filters/inputs.py::clear_filter_input_namespace` (filters/inputs.py:882-959) — `orders/inputs.py::clear_order_input_namespace()` ports verbatim per Spec Decision 9 / Revision 4 B2 ("**Exact behavior per B2 of `docs/feedback.md` rev3 (mirrors `clear_filter_input_namespace` verbatim)**"). The five clear steps land identically: `_materialized_names.clear()`, `_field_specs.clear()` (the order-side adapter map populated by `_build_input_fields`), `OrderArgumentsFactory.input_object_types.clear()` + `_type_orderset_registry.clear()` (local-import-guarded), every `OrderSet` subclass's `_owner_definition` / `_expanded_fields` / `_is_expanding_fields` `delattr`-when-in-`__dict__` (local-import-guarded). Module globals stay parked per B2 of rev3 (parking is load-bearing — `materialize_input_class` overwrites via `setattr` on the next finalize). Slice 2 ships `clear_order_input_namespace` AND a Slice-2-local `_iter_orderset_subclasses(root)` helper mirroring `filters/inputs.py::_iter_filterset_subclasses` (filters/inputs.py:962-986) verbatim. The Slice 1 TODO anchor at `orders/inputs.py` bottom names this clear helper as Slice 2's deliverable (the carry-forward to Slice 3 in worker-memory said "Slice 3"; the carry-forward is wrong — Spec Decision 9's lifecycle clause + Spec Test plan line 1010 + Spec Slice 5 DoD item 6 all place `clear_order_input_namespace` in Slice 2 because Slice 3's `registry.clear()` integration NEEDS the helper to exist before the local-import block can call it. Worker 1 final-verification at Slice 2's close should update the Slice 1 worker-memory carry-forward to reflect Slice 2 as the actual home.).
  - **Investigation (Worker 1):** Already addressed. This finding is a recap of a verbatim sibling-import / structural-mirror port, and every cited mirror exists in shipped source at `HEAD` — there is no pending consolidation. Confirmed each cited reuse against the shipped order subsystem and its filter-side originals:
    - **BFS factory port:** `django_strawberry_framework/orders/factories.py::OrderArgumentsFactory._ensure_built` mirrors `django_strawberry_framework/filters/factories.py::FilterArgumentsFactory._ensure_built` — same FIFO `#"os_class = pending.pop(0)"` queue, `seen` set, `_type_orderset_registry` collision check with the `existing_owner is not None and existing_owner is not os_class` guard, and `_build_class_type` with the operator-bag layer removed. The collision wording reuses the filter shape verbatim (`#"OrderArgumentsFactory: input type name {target_name!r} is claimed"`). `_input_type_name_for` is the one-line delegate to `ClassBasedTypeNameMixin.type_name_for()` (`orders/inputs.py::_input_type_name_for #"return orderset_class.type_name_for()"`).
    - **Adapter quartet:** `orders/inputs.py::_build_input_fields`, `orders/inputs.py::build_input_class`, `orders/inputs.py::convert_order_field_to_input_annotation` (one expr `#"return Ordering | None"`, args `del`-d as reserved), and `orders/inputs.py::normalize_input_value` all ship and mirror their `filters/inputs.py` twins (`FilterArgumentsFactory` line anchors in the `- [ ]` text have drifted from the artifact's pin-at-write-time hints, but the symbols all resolve — see filter-side `_build_input_fields`/`build_input_class`/`convert_filter_to_input_annotation`/`normalize_input_value`). `FieldSpec` + `_field_specs: dict[tuple[type[OrderSet], str], FieldSpec]` ship at `orders/inputs.py::FieldSpec` / `orders/inputs.py #"_field_specs: dict[tuple[type[OrderSet], str], FieldSpec] = {}"`.
    - **Resolver pair + permission shape:** `orders/sets.py::OrderSet.apply_sync` / `orders/sets.py::OrderSet.apply_async` ship; `apply_async` wraps only `_run_permission_checks` in `sync_to_async(thread_sensitive=True)` and has NO `_derive_related_visibility_querysets_async` step (matches the carry-forward). `orders/sets.py::OrderSet._run_permission_checks` carries the same `_fired: dict[type, set[str]]` dedup (`#"class_fired = _fired.setdefault(cls, set())"`) and active-branch double-dispatch (parent `check_<branch>_permission` + child `_run_permission_checks`) as the filter side, with no `and`/`or`/`not` operator-bag recursion. `_request_from_info`, `_invoke_permission_method`, `_iter_active_related_branches`, `_active_permission_field_paths` all ship.
    - **Ledger + helper:** `orders/__init__.py #"_helper_referenced_ordersets: set[type[OrderSet]] = set()"` (ledger) and `orders/__init__.py::order_input_type` (its only writer, returning `Annotated[name, strawberry.lazy(INPUTS_MODULE_PATH)]` element-type) both ship co-located, mirroring `filters/__init__.py`.
    - **Clear pair:** `orders/inputs.py::clear_order_input_namespace` and `orders/inputs.py::_iter_orderset_subclasses` both ship, mirroring the filter-side `clear_filter_input_namespace` / `_iter_filterset_subclasses`.
    - **Flagged-claim verification (`clear_order_input_namespace` home):** confirmed the helper is **defined exactly once** at `orders/inputs.py::clear_order_input_namespace` (Slice-2 inputs module); `grep -rn "def clear_order_input_namespace" django_strawberry_framework/` returns that single site. Slice 3's `django_strawberry_framework/registry.py::TypeRegistry.clear #"from .orders.inputs import clear_order_input_namespace"` only **imports and calls** it (wiring, not re-shipping). The worker-memory "Slice 3" carry-forward the finding flags as wrong is indeed wrong: the helper ships in Slice 2, exactly as the finding argues.
    - **Reconciliation note (per `DRY.md` "Spec / artifact reconciliation"):** the source artifact `docs/builder/bld-slice-2-factories.md` is accurate as to substance; only its inline raw `path:NN` line anchors (e.g. `filters/factories.py:56-182`, `filters/inputs.py:649-780`, `filters/sets.py:1007-1141`) have drifted as the filter modules grew (e.g. filter `_run_permission_checks` is now at `filters/sets.py:1058`, `apply_sync` at `:1625`). Per the closed-cycle rule the artifact is left untouched; the `- [ ]` text's symbol names are all current, so no in-place text correction was needed. No actionable consolidation remains.

- [x] **New helpers justified (only one — and it lives inside `orders/inputs.py`).**
  - `orders/inputs.py::_get_concrete_field_names_for_order(model)` — returns the list of every column-backed model field name per Spec Revision 4 B4 ("`[f.name for f in model._meta.get_fields() if hasattr(f, "column") and not getattr(f, "many_to_many", False)]`"). Single responsibility: produce the column-leaf field-name list for `OrderSet.get_fields("__all__")` to consume. The helper is a one-line list-comprehension; it could be inlined into `OrderSet._expand_meta_fields`, but extracting it (a) keeps the empirical-vs-documented Django M2M divergence rationale at the single site that needs it (the helper's docstring is where Spec Revision 4 B4's "Empirically against Django 6.0.5" reasoning lives); (b) gives the test suite a single import target for the `__all__` parity tests (the cookbook divergence test asserts `genres` is NOT in the output AND `shelf` IS in the output — easier with one importable helper than with a private `OrderSet` classmethod). Spec Decision 3 line 452 names this helper at this exact path. Worker 3's review-time helper trigger (≥30 lines new logic in `inputs.py`) fires automatically; Worker 3 will run `scripts/review_inspect.py` on the post-Slice-2 `inputs.py`.
  - **Investigation (Worker 1):** Already addressed. The single justified helper ships as described and its extraction is sound — no pending consolidation. Confirmed against shipped source at `HEAD`:
    - **Helper exists with the prescribed shape.** `orders/inputs.py::_get_concrete_field_names_for_order(model)` is the exact one-line comprehension `[f.name for f in model._meta.get_fields() if hasattr(f, "column") and not getattr(f, "many_to_many", False)]` (signature `(model: Any) -> list[str]`). The Django M2M-divergence rationale lives in its docstring exactly per claim (a): names the cookbook's `get_concrete_field_names`, the empirical "Django 6.0.5 `ManyToManyField` exposes `.column = None`" finding, and the deliberate `not f.many_to_many` clause.
    - **Single import target confirmed (claim b).** `grep` shows exactly one `def`; it is imported via local-import at `orders/sets.py::OrderSet._expand_meta_fields #"from .inputs import _get_concrete_field_names_for_order"` (the production caller, local-import to dodge the `sets ↔ inputs` cycle) and imported directly by the three parity tests in `tests/orders/test_inputs.py` (`..._includes_forward_fk_column` / `..._excludes_m2m_managers` / `..._excludes_reverse_fk`). The single-importable-helper-vs-private-classmethod argument holds.
    - **It is the ONLY net-new shared helper in Slice 2.** Every other Slice-2 addition to `orders/inputs.py` (`_build_input_fields`, `build_input_class`, `normalize_input_value`, `convert_order_field_to_input_annotation`, `_iter_orderset_subclasses`, `clear_order_input_namespace`, `FieldSpec`/`_field_specs`) is a verbatim port/mirror of a `filters/inputs.py` original — all covered by the preceding "Existing patterns reused" finding, not net-new logic. The lone exception, `_camel_case`, is a 4-line local string-format utility (not a shared cross-module helper and not surfaced in the source artifact's helper inventory), so the "exactly one" claim stands.
    - **Extraction justified per AGENTS.md (single responsibility, not premature).** The helper has one clear reason to exist beyond line-count reduction: it co-locates the M2M-divergence rationale at its single consuming site and gives the `__all__` parity tests one importable target instead of a private classmethod. Inlining would scatter the rationale and force the divergence tests through `OrderSet.get_fields("__all__")` setup. No re-abstraction is warranted.

- [x] **Duplication risk avoided (three risks named explicitly).**
  - **Risk: copy-paste `_run_permission_checks` body into `orders/sets.py`.** Naive implementation would duplicate the ~140-line `_run_permission_checks` body (filters/sets.py:1007-1141) into `orders/sets.py`, silently bifurcating the dedup contract the first time a maintainer fixes one and forgets the other. **Avoidance:** Slice 2 does NOT call the filter side's `_run_permission_checks` (the filter version walks `and` / `or` / `not` operator-bag branches the order side does not have), but Slice 2's order-side `_run_permission_checks` shares the SAME `_fired: dict[type, set[str]]` shape AND the SAME `_invoke_permission_method` helper shape AND the SAME `_active_permission_field_paths(input_value) -> list[str]` shape AND the SAME `_iter_active_related_branches(input_value)` shape (each adapted to the order side's input dataclass instead of the filter side's `_normalize_input` output dict). The cookbook source at `~/projects/django-graphene-filters/django_graphene_filters/orderset.py::AdvancedOrderSet.check_permissions` (cookbook lines 86-113) is shorter than the filter side's because the cookbook has no `and` / `or` / `not` branches; Slice 2's port matches the cookbook's shape verbatim but uses the filter side's per-field naming convention (`check_<field_path>_permission`) for cross-subsystem consistency. Worker 3 should flag any divergence from the filter side's dedup-map shape or method-name format string as a Medium finding. The duplication is in shape only (the body is shorter and the input format differs); no `from ..filters.sets import _run_permission_checks` import or shared helper is justified — the filter side's helper is keyed on `RelatedFilter` / `_iter_active_related_branches`'s filter-side return type, which is not type-compatible with the order side's `RelatedOrder` / order-side `_iter_active_related_branches`.
  - **Risk: re-implementing the BFS walker in `orders/factories.py` instead of mirroring `FilterArgumentsFactory`.** Naive implementation would write the BFS from first principles (LIFO vs FIFO queue, error message wording, collision check vs duplicate-build skip), silently diverging from the filter side. **Avoidance:** Slice 2's `OrderArgumentsFactory._ensure_built` body is a near-line-for-line port of `filters/factories.py::FilterArgumentsFactory._ensure_built` (filters/factories.py:133-169) with the substitutions named in the DRY analysis above. The collision-error wording reuses the filter side's exact message shape with `FilterSet`→`OrderSet`: `"OrderArgumentsFactory: input type name {target_name!r} is claimed by two distinct OrderSet classes: ..."`. The FIFO queue (`pending.pop(0)` per filters/factories.py:145) is preserved — even though `pending.pop()` (LIFO, the cookbook's choice at order_arguments_factory.py:88) produces the same set of reachable classes for a finite graph, the filter side chose FIFO for predictable build order in nested cycles AND Worker 3 should treat any divergence as a Medium finding. Worker 2 records the choice in build notes either way so Worker 3 doesn't ask.
  - **Risk: parking the `_helper_referenced_ordersets` ledger in `orders/inputs.py` for "convenience" with `clear_order_input_namespace`.** Per Spec Decision 2 N-new-2 of rev2 AND Spec Decision 9's "**`_helper_referenced_ordersets.clear()` stays as a SEPARATE `registry.clear()` block** (lives in `orders/__init__.py` per Decision 2), NOT inside `clear_order_input_namespace()`. Two-block layout matches the filter side..." (spec line 775). The ledger lives in `orders/__init__.py` co-located with its only writer (`order_input_type`); `clear_order_input_namespace()` in `inputs.py` does NOT touch the ledger — Slice 3's `registry.clear()` carries TWO separate blocks per the spec line 775 contract. **Avoidance:** Slice 2's `orders/inputs.py::clear_order_input_namespace` body does NOT import `_helper_referenced_ordersets` and does NOT clear it. The ledger clear lives only in Slice 3's `registry.clear()` block (the second of the two order-side blocks per Spec Decision 9 line 764-769). Worker 3 should flag any cross-coupling between `clear_order_input_namespace` and `_helper_referenced_ordersets` as a High finding (it would entrench duplicated logic at exactly the boundary Spec Decision 9 N-new-2 forbids).
  - **Investigation (Worker 1):** Already addressed — all three named avoidances hold in shipped source; this is a confirmation of intentional shape-mirror-without-shared-import, not a live duplication to consolidate. (1) `django_strawberry_framework/orders/sets.py::OrderSet._run_permission_checks` exists with the filter-side dedup/dispatch shape — `_fired: dict[type, set[str]]` param, `class_fired = _fired.setdefault(cls, set())`, mirrored `_invoke_permission_method` (with `method_name = f"check_{field_path.replace('__', '_')}_permission"`), `_active_permission_field_paths`, and `_iter_active_related_branches`; a grep of `orders/sets.py` for any `import` of `filters` returns nothing, so there is NO `from ..filters.sets import _run_permission_checks` or shared helper (shape-only mirror, type-incompatible across `RelatedFilter`/`RelatedOrder` as the finding states). (2) `django_strawberry_framework/orders/factories.py::OrderArgumentsFactory._ensure_built` uses FIFO `pending.pop(0)` #"os_class = pending.pop(0)" and the mirrored collision wording #"is claimed " ("OrderArgumentsFactory: input type name {target_name!r} is claimed by two distinct OrderSet classes: …"), raised via `ConfigurationError`, with the `existing_owner is not None and existing_owner is not os_class` guard. (3) `django_strawberry_framework/orders/inputs.py::clear_order_input_namespace` clears `_materialized_names` / `_field_specs` / factory caches / per-subclass binding only and explicitly does NOT touch `_helper_referenced_ordersets` (docstring #"Does NOT touch"); the ledger lives in `django_strawberry_framework/orders/__init__.py #"_helper_referenced_ordersets: set"` co-located with its only writer `order_input_type`, preserving the two-separate-block layout for Slice 3's `registry.clear()`. Returns `already-addressed`.

- [x] **`scripts/review_inspect.py` planning-time disposition (record per `BUILD.md`).** Ran the helper on the **current** `orders/sets.py` (199 source lines — over the 150-line trigger) AND `orders/inputs.py` (169 source lines — over the 150-line trigger) on 2026-06-01. Both shadow overviews land at `docs/shadow/django_strawberry_framework__orders__sets.overview.md` and `docs/shadow/django_strawberry_framework__orders__inputs.overview.md`. Hotspots / DRY observations:
  - **`orders/sets.py` shadow scan.** One control-flow hotspot: `OrderSet.get_fields` (51 lines, 6 branch nodes). Slice 2 ADDs `apply_sync` / `apply_async` / `get_flat_orders` / `check_permissions` / `_normalize_input` / `_request_from_info` / `_run_permission_checks` / `_invoke_permission_method` / `_iter_active_related_branches` / `_active_permission_field_paths` — call them ~7 new methods, ~150-200 source lines total (rough estimate against the filter side's 1543-1619 + 1007-1170 = ~230 lines). After Slice 2, `orders/sets.py` is likely ~350-400 source lines AND has 6-8 control-flow hotspots; Worker 3's review-time helper trigger (≥30 lines new logic in `django_strawberry_framework/`) fires automatically. **Repeated string literals from the current shadow:** `"related_orders"` (3x) and `"_expanded_fields"` (2x) — same as the filter side's `"related_filters"` / `"_expanded_filters"` pattern; both are class-attribute names referenced symmetrically across the metaclass walk and the `get_fields` cache gate. Not consolidable; no finding.
  - **`orders/inputs.py` shadow scan.** Zero control-flow hotspots in the Slice 1 surface; Slice 2 ADDs `_build_input_fields` (~50 lines per filter side analogue's shape, simpler), `convert_order_field_to_input_annotation` (~10 lines), `normalize_input_value` (~25 lines per cookbook `get_flat_orders` shape), `build_input_class` (~25 lines, port of filter side verbatim), `_get_concrete_field_names_for_order` (~3 lines), `_iter_orderset_subclasses` (~15 lines, port of filter side verbatim), `clear_order_input_namespace` (~30 lines, port of filter side verbatim), and `FieldSpec` (dataclass + `_field_specs: dict[tuple[type, str], FieldSpec]`, ~25 lines). After Slice 2, `orders/inputs.py` is likely ~350-400 source lines AND has ~3-5 control-flow hotspots; the Worker 3 helper trigger fires. **Zero repeated string literals in the current shadow.** Slice 2 may introduce new repeated literals (`"related_orders"`, `"_owner_definition"`, etc. shared between `_build_input_fields` and `clear_order_input_namespace`); Worker 3's helper rerun at review time catches them.
  - **No `optimizer/` or `types/` files touched.** Slice 2 lands NOTHING under `django_strawberry_framework/optimizer/` or `django_strawberry_framework/types/` — those are Slice 3's domain (`types/finalizer.py` + `types/base.py` + `types/definition.py`). Verified by walking the spec's Slice 2 file list AND the build-plan's per-slice scoping. Worker 1's planning-pass helper trigger ("plan adds logic to any file under `optimizer/` or `types/`") does NOT fire for Slice 2.
  - **Investigation (Worker 1):** Already addressed. The "not consolidable; no finding" disposition holds against the FINAL shipped source (`orders/sets.py` grew 199→580 lines, `orders/inputs.py` 169→488). Grep counts: `orders/sets.py` uses `"related_orders"` 5x (`sets.py:67,152,161,286,334`) and `"_expanded_fields"` 3x (`sets.py:135,146,147`); `orders/inputs.py` uses `"related_orders"` 1x (`inputs.py::_build_input_fields #"getattr(orderset_cls, \"related_orders\""`, line 333), `"_owner_definition"` 1x and `"_expanded_fields"` 1x (both in the single `clear_order_input_namespace` clear tuple at `inputs.py:486`). Every occurrence is a literal attribute *name* fed to `getattr(...)` / `cls.__dict__.get(...)` / `delattr(...)` — the string IS the runtime attribute name, so a module constant would introduce name-vs-attribute indirection that hurts readability without removing real duplication. Parity bar confirmed: the shipped filter side treats the symmetric literals identically as raw inline strings and never extracts a constant — `"related_filters"` appears 7x in `filters/sets.py` (`sets.py:301,316,646,831,1243` + the `new_class.related_filters` assignment) + 1x in `filters/inputs.py:665`; `"_expanded_filters"` 2x (`sets.py:291,292`); and the `clear_filter_input_namespace` clear tuple `("_owner_definition", "_expanded_filters", "_is_expanding_filters")` at `filters/inputs.py:957` is the exact analogue of the order side's `inputs.py:486` tuple. The finding's predicted cross-method literal duplication did NOT materialize — `"related_orders"` and `"_owner_definition"` each appear exactly once in `inputs.py` (different methods, single use each), so there is nothing shared to factor out. No un-consolidated logic or literal duplication remains; matches filter-side treatment. Returns `already-addressed`.

---

## Build: Slice 3 — Wiring — Meta.orderset_class promotion + finalizer phase 2.5 binding

_Source: `docs/builder/bld-slice-3-wiring.md`_

Slice 3 is a one-for-one mirror of the shipped Filtering subsystem's `_bind_filtersets()` umbrella helper + `_bind_filterset_owner()` binding + `_validate_filterset_class()` validator + the `registry.clear()` namespace-clear blocks, with the order-side simplifications named in Spec Decision 6 (no own-PK Relay-identity axis on the order side because `ORDER BY id` against any model uses the column, not the GraphQL ID type). Every cite below uses the symbol-qualified `path::Symbol` form per `AGENTS.md` #"Source references in docs and code comments" plus inline raw `path:NN` line refs (allowed per `AGENTS.md` since this is a `bld-*.md` scratchpad).

- [x] **Existing patterns reused (verbatim structural mirror with substitutions).**
  - `django_strawberry_framework/types/base.py::_validate_filterset_class` (base.py:83-106) — the function this slice ports as `_validate_orderset_class(meta, orderset_class)`. Substitutions: `filterset_class`→`orderset_class`, `FilterSet`→`OrderSet`, `Meta.filterset_class`→`Meta.orderset_class`, `from ..filters.sets import FilterSet`→`from ..orders.sets import OrderSet`. Same `if filterset_class is None: return None` short-circuit; same `isinstance(filterset_class, type) and issubclass(filterset_class, FilterSet)` shape; same `ConfigurationError` message structure (`f"{meta.model.__name__}.Meta.orderset_class must be an OrderSet subclass; got {orderset_class!r}"`). **Local in-function import** of `OrderSet` from `..orders.sets` is the critical contract per Spec N3 of rev1 + DoD item 9 — dodges the `types → orders → types` module-load cycle. Worker 2 MUST NOT hoist this import to module top.
  - `django_strawberry_framework/types/base.py::DEFERRED_META_KEYS` (base.py:48-55) and `ALLOWED_META_KEYS` (base.py:57-69) — the two frozensets to surgically edit. Drop `"orderset_class"` from `DEFERRED_META_KEYS`; add `"orderset_class"` to `ALLOWED_META_KEYS` (alphabetized — between `"optimizer_hints"` and `"primary"`). Mirrors the same one-line `"filterset_class"` move applied in `DONE-027-0.0.8`.
  - `django_strawberry_framework/types/base.py::_ValidatedMeta` (base.py:571-590) — the `NamedTuple` snapshot of validated Meta keys. Add `orderset_class: type | None` as a new slot immediately after `filterset_class: type | None` (preserves the spec-named ordering at base.py:587-590 TODO anchor).
  - `django_strawberry_framework/types/base.py::_validate_meta` (base.py:593-679) — the validator dispatch. Add one call `orderset_class = _validate_orderset_class(meta, getattr(meta, "orderset_class", None))` immediately after the existing `filterset_class = _validate_filterset_class(...)` call at base.py:665; include `orderset_class=orderset_class` in the `_ValidatedMeta(...)` constructor at base.py:670-679. Mirrors the filter side's one-line addition pattern verbatim.
  - `django_strawberry_framework/types/base.py::DjangoType.__init_subclass__` (base.py:281-303) — the `DjangoTypeDefinition(...)` constructor call. Add `orderset_class=validated.orderset_class` immediately after the existing `filterset_class=validated.filterset_class,` at base.py:299 (where the TODO anchor at base.py:300-302 currently parks the work). Same pattern the filter side applied.
  - `django_strawberry_framework/types/definition.py::DjangoTypeDefinition` (definition.py:14-194) — the dataclass. Add `orderset_class: type | None = None` immediately after `filterset_class: type | None = None` at definition.py:91 (the TODO anchor at definition.py:92-98 currently parks the slot). The class-docstring's bullet listing `filterset_class` semantics (definition.py:49-56) grows a parallel `orderset_class` bullet — Worker 2 may keep this terse: "`orderset_class` is the per-owner `OrderSet` sidecar populated by `DjangoType.__init_subclass__` from `Meta.orderset_class` once promoted out of `DEFERRED_META_KEYS`; consumed by `finalize_django_types()` phase 2.5 to bind the owning `DjangoTypeDefinition` on the OrderSet and to materialize the generated Strawberry input class as a module global of `django_strawberry_framework.orders.inputs`." Mirrors the filter side's bullet at definition.py:49-56 one-for-one.
  - `django_strawberry_framework/types/finalizer.py::_bind_filterset_owner` (finalizer.py:278-384) — the per-orderset binding helper. Slice 3 ports the FIRST-bind model-compatibility branch (finalizer.py:316-336) and the IDEMPOTENT re-bind branch (finalizer.py:337-338); per Spec Decision 6 second-paragraph rationale ("The filter side's separate own-PK Relay-identity check is **NOT** ported — the order side has no equivalent (`ORDER BY id` against any model uses the column, not the GraphQL ID type), so the order-side check is narrower than the filter side's by one axis"), the order side's `_bind_orderset_owner` SKIPS the Axis-1 own-PK Relay-identity check at finalizer.py:339-350. Worker 2 keeps the Axis-2 related-target-agreement check (finalizer.py:351-384) for multi-owner reuse — adapted to walk `getattr(orderset_cls, "related_orders", {})` instead of `getattr(filterset_cls, "related_filters", {})`. The result: the order-side helper is ~70 lines instead of the filter side's 107 (one axis dropped, same idempotent + first-bind structure).
  - `django_strawberry_framework/types/finalizer.py::_bind_filtersets` (finalizer.py:503-622) — the four-subpass umbrella helper. Slice 3 ports verbatim with the substitutions `filtersets`→`ordersets`, `filterset`→`orderset`, `FilterArgumentsFactory`→`OrderArgumentsFactory`, `filters.factories`→`orders.factories`, `filters.inputs`→`orders.inputs`, `_helper_referenced_filtersets`→`_helper_referenced_ordersets`, `filterset_class`→`orderset_class`, `get_filters()`→`get_fields()` (Layer-4 expansion method name differs by subsystem). The **four-subpass order is preserved verbatim per Spec B1 of rev2**: (1) bind owners; (2) expand fields; (3) orphan-validate; (4) materialize. The ordering matches the shipped filter side (finalizer.py docstring lines 19-30 + the umbrella body at finalizer.py:548-622) NOT the spec-027 rev8 H1 prescription which had the order inverted. The `ImportError`-rewrap pattern at finalizer.py:569-577 ports verbatim with the same `repr(exc)` + `from exc` `__cause__`-preservation contract.
  - `django_strawberry_framework/types/finalizer.py::_format_orphan_filtersets_error` (finalizer.py:462-484) — the orphan-error message formatter. Slice 3 ports as `_format_orphan_ordersets_error(orphans)` with the substitutions `filter_input_type`→`order_input_type`, `Meta.filterset_class`→`Meta.orderset_class`, `FilterSet`→`OrderSet`. Same single-orphan-vs-multi-orphan branching at finalizer.py:471-484. Same sorting by `cls.__module__.cls.__qualname__`.
  - `django_strawberry_framework/types/finalizer.py::_format_owner_model_mismatch_error` (finalizer.py:440-459) — the first-bind model-mismatch formatter. Slice 3 ports as `_format_owner_orderset_model_mismatch_error(orderset_cls, owner)` with the same four names in the message (owner type, owner model, orderset class, orderset model). Same `Meta.filterset_class`→`Meta.orderset_class`, `FilterSet`→`OrderSet` substitutions. Per Spec Decision 6's `test_phase_2_5_rejects_orderset_wired_to_unrelated_owner_model` test contract (spec line 603), the message must name all four entities.
  - `django_strawberry_framework/types/finalizer.py::_format_owner_mismatch_error` (finalizer.py:387-412) — the diverging-target multi-owner error formatter. Slice 3 ports as `_format_owner_ordersets_mismatch_error(orderset_cls, previous, new, field_name, prev_target, new_target)` with the `FilterSet`→`OrderSet` substitution. Drives the Axis-2 related-target-agreement raise.
  - `django_strawberry_framework/types/finalizer.py::finalize_django_types` (finalizer.py:145-275) — the orchestrator. Slice 3 adds ONE line: `_bind_ordersets()` at finalizer.py:260 (where the TODO anchor at finalizer.py:260-267 currently parks the work). Placed AFTER `_bind_filtersets()` at finalizer.py:259 and BEFORE the Phase 3 `strawberry.type(...)` loop at finalizer.py:269. The placement matches the spec's "immediately after the existing `_bind_filtersets()` umbrella helper and before phase 3's `strawberry.type` decoration" contract verbatim.
  - `django_strawberry_framework/registry.py::TypeRegistry.clear` (registry.py:395-442) — the test-only clear method. Slice 3 adds TWO new blocks at registry.py:434-442 (where the TODO anchor currently parks the work). Block 1: `from .orders.inputs import clear_order_input_namespace` + call (mirrors the filter side's block at registry.py:420-425). Block 2: `from .orders import _helper_referenced_ordersets` + `.clear()` call (mirrors the filter side's block at registry.py:427-432). **Both blocks use the `except ImportError: pass` + `else:` shape — NOT `return` on the last block** per Spec M-core-4 of rev1 + Spec B2 of rev1 (a future card adding a fifth clear phase, e.g., aggregates 0.1.3, must not be silently skipped on a partial-load environment). The shipped TypeRegistry field names used by the existing block 1 of the model-state clear at registry.py:404-410 (`_types`, `_primaries`, `_models`, `_enums`, `_definitions`, `_pending`, `_finalized`) are verbatim from `TypeRegistry.__init__` at registry.py:44-50 — Slice 3's additions DO NOT touch those field clears. **Per Spec B2+B3 of rev2 + the prompt: the phantom `_types_by_model` / `_primary_types` names from rev1 were errors; the shipped/correct names are the seven listed above.**
  - `django_strawberry_framework/orders/inputs.py::clear_order_input_namespace` (orders/inputs.py:417-488) — **already shipped in Slice 2 per the worker-1 memory Slice 2 final-verification entry.** Slice 3 ONLY wires `registry.clear()` to call it. The helper itself is not re-shipped; the Slice 3 plan's `_bind_ordersets()` materialize subpass calls `materialize_input_class` directly (not `clear_order_input_namespace`).
  - `django_strawberry_framework/orders/__init__.py::_helper_referenced_ordersets` (orders/__init__.py:39) — **already shipped in Slice 2.** Slice 3 ONLY wires `registry.clear()` to call `.clear()` on it.
  - `django_strawberry_framework/orders/factories.py::OrderArgumentsFactory` (orders/factories.py:31-174) — **already shipped in Slice 2.** Slice 3's materialize subpass calls `OrderArgumentsFactory(orderset_cls).arguments` (the property triggers `_ensure_built` per orders/factories.py:103-113) then iterates `factory.input_object_types.items()` and calls `materialize_input_class(name, input_cls)` for each. The exact call shape matches the filter side's subpass 4 at finalizer.py:614-622 verbatim.
  - **Investigation (Worker 1):** Already addressed. Every cited mirror exists verbatim in shipped source; this finding is a faithful recap of completed Slice 3 wiring, not a pending consolidation. Concrete confirmations (verified by symbol, line anchors drifted as modules grew): (1) **LOAD-BEARING local import** — `types/base.py::_validate_orderset_class` ships at base.py:94-119 with `from ..orders.sets import OrderSet` as an in-function import at base.py:112 under the `# In-function import: dodges the 'types -> orders -> types' module-load cycle. Do NOT hoist to module top.` comment; module-top imports (base.py:40-46) do NOT include `OrderSet`, so the contract holds. Same `if orderset_class is None: return None` short-circuit + `isinstance(...) and issubclass(..., OrderSet)` shape + `ConfigurationError` message as the filter sibling. (2) **LOAD-BEARING two separate registry blocks** — `registry.py::TypeRegistry.clear` ships TWO independent order-side blocks at registry.py:441-453: block 1 `from .orders.inputs import clear_order_input_namespace` (441-446) and block 2 `from .orders import _helper_referenced_ordersets` + `.clear()` (448-453), each using `except ImportError: pass` + `else:` (NOT `return`), under the explicit comment that the `pass`+`else` shape protects a future fifth phase. Mirrors the filter side's two blocks at registry.py:420-432 verbatim. (3) `DEFERRED_META_KEYS` (base.py:48-50) no longer contains `"orderset_class"`; `ALLOWED_META_KEYS` (base.py:52-65) carries `"orderset_class"` at base.py:62, alphabetized between `"optimizer_hints"` and `"primary"`. (4) `_ValidatedMeta.orderset_class: type | None` at base.py:602 (after `filterset_class` at 601); `_validate_meta` dispatch at base.py:678 + constructor arg at base.py:687; `DjangoType.__init_subclass__` constructor arg `orderset_class=validated.orderset_class` at base.py:317. (5) `DjangoTypeDefinition.orderset_class: type | None = None` at definition.py:94 (after `filterset_class` at 93) with the parallel docstring bullet at definition.py:56-58. (6) `finalizer.py::_bind_orderset_owner` at 478-549 keeps Axis-2 related-target agreement walking `related_orders` (517-549) and intentionally SKIPS the Axis-1 own-PK check (docstring 488-492). (7) `_bind_ordersets()` at 630-736 runs the four subpasses in the **shipped order** bind→expand→orphan-validate→materialize (subpass 1 at 677-686, subpass 2 at 688-714 with the `ImportError`→`ConfigurationError` `from exc` rewrap + `repr(exc)` generic rewrap, subpass 3 orphan-validate at 716-725 explicitly BEFORE materialize, subpass 4 at 727-736). (8) Three formatters present: `_format_owner_ordersets_mismatch_error` (552-575), `_format_owner_orderset_model_mismatch_error` (578-603, names all four entities), `_format_orphan_ordersets_error` (606-627, single-vs-multi branching). (9) `finalize_django_types` calls `_bind_ordersets()` at finalizer.py:258, immediately AFTER `_bind_filtersets()` (257) and BEFORE the Phase 3 `strawberry.type(...)` loop (260-264). Slice-2 deps (`orders/__init__.py::_helper_referenced_ordersets`, `orders/sets.py::OrderSet`/`related_orders`/`get_fields`, `orders/factories.py::OrderArgumentsFactory`/`input_object_types`/`arguments`/`_ensure_built`) all present and wired exactly as the finding describes. No reconciliation needed — the artifact neither overstated nor understated the duplication; it is a structural mirror by design and correctly justified as such.

- [x] **New helpers justified (five new finalizer-side helpers; one new validator).** The five new helpers in `types/finalizer.py` are all sibling formatters / binding shapes mirroring the filter side; none can be consolidated into one because each carries subsystem-specific wording (`FilterSet`/`filter_input_type` vs `OrderSet`/`order_input_type`). Worker 1's planning-pass DRY review concluded the wording-divergence weighs against a shared formatter — the consumer-visible error names the relevant subsystem so a future maintainer reading the error knows which sidecar is broken. The one new validator helper (`_validate_orderset_class`) is the single canonical place where the local-import-against-cycle contract lives; same justification as the shipped `_validate_filterset_class`.
  - `types/finalizer.py::_bind_orderset_owner(orderset_cls, definition) -> None` — per-orderset binding. Single responsibility: enforce first-bind model compatibility + Axis-2 related-target agreement (multi-owner reuse) + idempotent re-bind. Does NOT enforce Axis-1 own-PK Relay-identity (Spec Decision 6 explicit rationale: not an owner-dependent axis on the order side).
  - `types/finalizer.py::_bind_ordersets() -> None` — the umbrella four-subpass helper. Single responsibility: run the four subpasses in the **shipped order** (bind → expand → orphan-validate → materialize) for every wired orderset.
  - `types/finalizer.py::_format_owner_orderset_model_mismatch_error(orderset_cls, owner) -> str` — first-bind model mismatch error. Single responsibility: name all four entities (owner type, owner model, orderset class, orderset model) per Spec Decision 6 / H2 of rev3.
  - `types/finalizer.py::_format_owner_ordersets_mismatch_error(orderset_cls, previous, new, field_name, prev_target, new_target) -> str` — diverging-target multi-owner reuse error. Single responsibility: name both owners + offending field + both resolved target type names.
  - `types/finalizer.py::_format_orphan_ordersets_error(orphans) -> str` — orphan `order_input_type` reference error. Single responsibility: single-orphan vs multi-orphan branching per the filter side's pattern; suggestion text mentions `Meta.orderset_class` so consumers see the fix.
  - `types/base.py::_validate_orderset_class(meta, orderset_class) -> type | None` — `Meta.orderset_class` validator. Single responsibility: short-circuit when absent, raise `ConfigurationError` for non-`OrderSet` values, return the class otherwise. **Local in-function import** of `OrderSet` is the load-bearing contract.
  - **Investigation (Worker 1):** Already addressed. All six helpers exist as described, each single-responsibility, and the "not consolidable into a shared formatter" judgment holds per `AGENTS.md` rule #4 (intentional sibling design diverging in consumer-visible wording at N=2 — the highest-quality fix is the legible per-subsystem error, not a noun-parameterized formatter). Verified by symbol (line anchors drifted as the module grew): (1) `types/base.py::_validate_orderset_class` ships at base.py:94 with the load-bearing local `from ..orders.sets import OrderSet` at base.py:112 under the "Do NOT hoist to module top" comment; module-top imports omit `OrderSet`; filter sibling `_validate_filterset_class` at base.py:68 kept. (2) `types/finalizer.py::_bind_orderset_owner` at finalizer.py:478 — first-bind model-compat (494-513) + Axis-2 related-target agreement (516-549) + idempotent re-bind (514-515); Axis-1 own-PK check intentionally skipped (docstring 488-492). (3) `_bind_ordersets` umbrella at finalizer.py:630 (four subpasses bind→expand→orphan-validate→materialize); filter sibling `_bind_filtersets` at finalizer.py:739 kept. (4) Three order formatters — `_format_owner_ordersets_mismatch_error` (552), `_format_owner_orderset_model_mismatch_error` (578, names all four entities), `_format_orphan_ordersets_error` (606, single-vs-multi branching) — each embeds `OrderSet`/`order_input_type`/`Meta.orderset_class` literally in the user-facing message string. The filter siblings (`_format_owner_mismatch_error` at 378, `_format_owner_model_mismatch_error` at 431, `_format_orphan_filtersets_error` at 453) embed `FilterSet`/`filter_input_type`/`Meta.filterset_class` and are kept independently. A shared formatter would have to parameterize every subsystem noun, obscuring the consumer-visible wording whose whole purpose is to name the broken sidecar — the wording divergence genuinely blocks consolidation, and the filter side independently keeps its own siblings, confirming this is sibling-by-design (N=2), not duplication. No consolidation the source missed; no reconciliation needed (the artifact correctly justifies the six helpers).

- [x] **Duplication risk avoided (four risks named explicitly).**
  - **Risk: copy-paste the entire `_bind_filtersets` body into `_bind_ordersets`.** Naive implementation would duplicate the ~120-line body (finalizer.py:503-622) into `_bind_ordersets`, silently bifurcating the four-subpass discipline the first time a maintainer fixes one (e.g., adds a fifth subpass) and forgets the other. **Avoidance:** Slice 3's `_bind_ordersets` IS a structural mirror — same subpass discipline, same `ImportError`-rewrap shape, same `repr(exc)` + `from exc` cause-preservation, same `wired_set = set(wired)` orphan-set construction, same `factory = OrderArgumentsFactory(orderset_cls); _ = factory.arguments; for name, input_cls in factory.input_object_types.items(): materialize_input_class(name, input_cls)` materialize loop. The duplication is in shape only (the body is shorter — no Axis-1 own-PK check in `_bind_orderset_owner`); no `from ..filters.finalizer import _bind_filtersets` import or shared helper is justified — the filter side's helper is keyed on `FilterSet` / `filter_input_type` / `_helper_referenced_filtersets` / `FilterArgumentsFactory`, none of which are type-compatible with the order-side equivalents. Worker 3 should flag any divergence from the filter side's subpass ordering as a High finding.
  - **Risk: re-ordering the four subpasses (e.g., materialize before orphan-validate).** Naive implementation might follow the spec-027 rev8 H1 prescription (which inverted subpasses 3 and 4) instead of the shipped filter side's order. **Avoidance:** Spec Decision 6 explicit pin: "the shipped filter side at `django_strawberry_framework/types/finalizer.py::_bind_filtersets` documents this rationale explicitly in its docstring (subpass 3 — orphan validation); this card mirrors the shipped order, not the spec-027 rev8 H1 prescription which inverted subpasses 3 and 4." Per Spec B1 of rev2 ("the **shipped** filter side at `_bind_filtersets` is the authoritative shape") — bind → expand → orphan-validate → materialize, in that order. Worker 3 should flag any deviation as a High finding.
  - **Risk: hoisting `from ..orders.sets import OrderSet` to module top of `types/base.py`.** Naive implementation would add `from ..orders.sets import OrderSet` at the top of `types/base.py` for the validator, mirroring the existing imports. **Avoidance:** Spec N3 of rev1 + DoD item 9 explicit: the import MUST be local-in-function inside `_validate_orderset_class`. The filter side at base.py:99 already demonstrates the pattern (`# In-function import: dodges the 'types -> filters -> types' module-load cycle. Do NOT hoist to module top.`). The cycle: `types/base.py` imports `..registry` → `registry.py` does NOT import `types` at module top (uses TYPE_CHECKING) → BUT `orders/sets.py` imports `from ..types.definition import DjangoTypeDefinition` under TYPE_CHECKING; `orders/factories.py` does NOT import types at module top. The dodge is defensive: Worker 1 verified the cycle is structurally inert today, but the local import keeps the contract robust against any future `orders/*` module growing a top-level `from ..types import ...` line. **Worker 3 MUST flag any top-level `from ..orders.sets import OrderSet` in `types/base.py` as a High finding** — pinned by `test_validate_orderset_class_uses_local_import`.
  - **Risk: collapsing `clear_order_input_namespace()` and `_helper_referenced_ordersets.clear()` into a single `registry.clear()` block.** Naive implementation might bundle both clears under one `try: from .orders import clear_order_input_namespace, _helper_referenced_ordersets` import block. **Avoidance:** Spec Decision 9 line 775 + the prompt's Slice 2 carry-forward both pin: TWO separate `try / except ImportError / else` blocks — block 1 for `clear_order_input_namespace`, block 2 for `_helper_referenced_ordersets.clear()`. The two-block layout matches the filter side at registry.py:420-432 verbatim. Each block uses the `except ImportError: pass` + `else:` shape so a partial-load environment never short-circuits a later cleanup phase (per Spec B2 of rev1 + M-core-4 of rev1 — a future fifth phase, e.g., aggregates 0.1.3, must not be silently skipped). Worker 3 should flag any single-block collapse as a High finding.
  - **Investigation (Worker 1):** Already addressed. All four named avoidances hold in shipped source; this finding is a faithful recap of completed Slice 3 wiring, not a pending consolidation. Verified by symbol (line anchors drifted as modules grew): (1) **No copy-paste / no shared-helper import** — `types/finalizer.py::_bind_ordersets` ships at finalizer.py:630 as a structural mirror of `_bind_filtersets` (finalizer.py:739): it imports the order-side surface locally (`from ..orders import _helper_referenced_ordersets` / `..orders.factories import OrderArgumentsFactory` / `..orders.inputs import materialize_input_class` at 673-675), NOT `from ..filters.finalizer import _bind_filtersets`; it carries the same `wired_set = set(wired)` orphan-set construction (719), the same `ImportError`→`ConfigurationError` `from exc` rewrap + generic `repr(exc)` rewrap (699-714), and the same `factory.arguments` + `materialize_input_class(name, input_cls)` loop (727-736). The body is shorter only because `_bind_orderset_owner` (478-549) drops the Axis-1 own-PK check the filter sibling `_bind_filterset_owner` carries (filterset has `implements_relay_node` Axis-1 at finalizer.py:330-337; the orderset helper goes straight from first-bind model-compat (494-513) + idempotent re-bind (514-515) into the Axis-2 related-target loop (516-549), with the skip documented in its docstring at 488-492). (2) **Subpass order preserved** — `_bind_ordersets` runs bind→expand→orphan-validate→materialize in the shipped filter order (Subpass 1 at finalizer.py:677, Subpass 2 at 688, Subpass 3 orphan-validate at 716 explicitly BEFORE materialize, Subpass 4 at 727), matching `_bind_filtersets` (784/795/831/850), NOT the inverted spec-027 rev8 H1 prescription. `finalize_django_types` calls `_bind_ordersets()` at finalizer.py:258, immediately after `_bind_filtersets()` (257) and before the Phase 3 `strawberry.type(...)` loop (263). (3) **Local import not hoisted** — `types/base.py::_validate_orderset_class` (base.py:94) imports `from ..orders.sets import OrderSet` in-function at base.py:112 under the `# ... Do NOT hoist to module top.` comment (111); the module-top import block (base.py:28-46) does NOT include `OrderSet`. (4) **Two separate registry blocks** — `registry.py::TypeRegistry.clear` ships block 1 `from .orders.inputs import clear_order_input_namespace` (registry.py:441-446) and block 2 `from .orders import _helper_referenced_ordersets` + `.clear()` (448-453) as independent blocks, each `except ImportError: pass` + `else:` (no `return`), under the explicit comment (434-440) that the `pass`+`else` shape protects a future fifth phase; mirrors the filter side's two blocks at registry.py:420-432. No reconciliation needed — the artifact neither overstated nor understated; all four avoidances are correct as shipped.

- [x] **`scripts/review_inspect.py` planning-time disposition (record per `BUILD.md`).** Ran the helper on the **current** files Slice 3 will modify on 2026-06-01: `types/base.py` (952 lines), `types/definition.py` (194 lines), `types/finalizer.py` (622 lines), `registry.py` (445 lines). All four are over the 150-line trigger AND under `types/` (which fires the second helper-trigger condition); the helper run is required.
  - **`types/base.py` shadow scan.** Three hotspots already pre-Slice-3: `DjangoType.__init_subclass__` (>100 lines), `_validate_meta` (~85 lines), `_build_annotations` (~140 lines). Slice 3 ADDS one ~20-line `_validate_orderset_class` function + 3 small line edits (the `DEFERRED_META_KEYS` / `ALLOWED_META_KEYS` set membership flip, the `_validate_meta` call addition, the `_ValidatedMeta` field addition, the `DjangoTypeDefinition(...)` argument addition). Repeated string literals in the current shadow: `"optimizer_hints"` (4x), `"description"` (2x), `"filterset_class"` (2x), `"interfaces"` (2x). Slice 3 adds `"orderset_class"` references (2x — in the validator + in the `_validate_meta` call). Not a consolidation candidate; the filter side's pattern is to have `Meta.<key>` as repeated string literal across validator + dispatcher.
  - **`types/definition.py` shadow scan.** No control-flow hotspots; zero repeated string literals. Slice 3 ADDs one slot line (`orderset_class: type | None = None`) + one docstring bullet update. Worker 3's review-time trigger fires (file is under `types/`).
  - **`types/finalizer.py` shadow scan.** Three control-flow hotspots: `finalize_django_types` (131 lines, 15 branches), `_bind_filterset_owner` (107 lines, 15 branches), `_bind_filtersets` (120 lines, 11 branches). Slice 3 ADDs five new helpers (`_bind_orderset_owner`, `_bind_ordersets`, `_format_owner_orderset_model_mismatch_error`, `_format_owner_ordersets_mismatch_error`, `_format_orphan_ordersets_error`) plus a one-line `_bind_ordersets()` call in `finalize_django_types`. Estimated post-Slice-3 line count: ~800 source lines, ~6 control-flow hotspots. Repeated string literals to watch for cross-file dup at integration pass: `"FilterSet"` (3x) and `"Cannot finalize Django types: filterset"` (2x) — Slice 3 will add the parallel `"OrderSet"` and `"Cannot finalize Django types: orderset"` strings. These are subsystem-noun strings (NOT shared-formatter candidates) per the wording-divergence rationale above.
  - **`registry.py` shadow scan.** Zero repeated string literals; the helper found no control-flow hotspots. Slice 3 ADDS ~14 lines (two new try/except/else blocks) at the bottom of `TypeRegistry.clear`. The `from .orders.inputs import ...` and `from .orders import ...` lines are new but mirror the existing `from .filters.inputs import ...` and `from .filters import ...` lines at registry.py:421 + 428 verbatim.
  - **Cross-file repeated-literal observation.** Comparing the `Repeated string literals` sections across the four shadows: the shared symbols at integration pass to watch for are (a) `"filterset_class"` and the future `"orderset_class"` appearing in BOTH `types/base.py` AND `types/definition.py` AND `types/finalizer.py` — these are NOT consolidation candidates (each is a slot / dict-key reference, not a free-form literal); (b) `"Cannot finalize Django types: filterset"` in `types/finalizer.py` (and the future `"Cannot finalize Django types: orderset"`) — both subsystem-noun-specific, NOT a consolidation candidate. Worker 1's integration-pass DRY scan must walk these explicitly; the planning-pass disposition is "no new cross-file dups introduced; the existing subsystem-noun strings stay legible by intent."
  - **Investigation (Worker 1):** Already addressed. Confirmed against FINAL shipped source — the planning-pass disposition holds with full filter-side parity; no un-consolidated duplication remains that the filter side does not also leave inline. Evidence: (1) `types/base.py::_validate_orderset_class` shipped at base.py:94 mirroring `_validate_filterset_class` (base.py:68) with the local in-function `OrderSet` import intact; the `"orderset_class"` literal appears at exactly two sites — `ALLOWED_META_KEYS` (base.py:62) and the `getattr(meta, "orderset_class", None)` in `_validate_meta` (base.py:678) — byte-for-byte parity with the filter side's `"filterset_class"` at base.py:57 + base.py:677; neither side names the key as a constant. (2) `types/definition.py` shipped one dataclass slot `orderset_class: type | None = None` (definition.py:94) + one docstring bullet (definition.py:56-60) mirroring `filterset_class` (definition.py:93 / 49-55); nothing consolidable. (3) `types/finalizer.py` shipped the five order helpers (`_bind_orderset_owner` finalizer.py:478, `_format_owner_ordersets_mismatch_error` :552, `_format_owner_orderset_model_mismatch_error` :578, `_format_orphan_ordersets_error` :606, `_bind_ordersets` :630) as siblings of the five filter helpers (`_bind_filterset_owner` :269, `_format_owner_mismatch_error` :378, `_format_owner_model_mismatch_error` :431, `_format_orphan_filtersets_error` :453, `_bind_filtersets` :739); the `"Cannot finalize Django types: orderset"` strings (finalizer.py:704, :712) carry subsystem-divergent suffixes ("related-order target" / "raised during expansion") and the filter side keeps the parallel `"...filterset"` strings inline too (finalizer.py:811, :827) — no shared formatter on either side, so leaving the order strings inline is parity, not a gap. (4) `registry.py::TypeRegistry.clear` shipped the two order try/except/else blocks (registry.py:441-453) as a verbatim mirror of the two filter blocks (registry.py:420-432); the per-block local import is the documented cycle-safe contract and the filter side routes through no helper either, so the four-block shape is intentional inline parity, not a pending extraction. No parity gap (no helper the filter side has and the order side lacks). Return `already-addressed`.

---

## Build: Slice 4 — Live HTTP coverage — 14 live /graphql tests + fakeshop orders + schema wiring

_Source: `docs/builder/bld-slice-4-live_http.md`_

Slice 4 is the consumer-facing mirror of the shipped Filtering subsystem's fakeshop wiring (`DONE-027-0.0.8` per the existing `apps.library.filters` / `apps.library.filters_genre` / `apps.library.schema` corpus) with the substitutions Filter→Order and `filterset_class`→`orderset_class`. The order subsystem itself (`django_strawberry_framework/orders/`) is fully shipped through Slices 1-3; Slice 4 lands NOTHING under `django_strawberry_framework/` — it is a pure example-app + test-corpus slice. Every cite below uses the symbol-qualified `path::Symbol` form per `AGENTS.md` #"Source references in docs and code comments" plus inline raw `path:NN` line refs (allowed per `AGENTS.md` since this is a `bld-*.md` scratchpad).

- [x] **Existing patterns reused (verbatim structural mirror with one substitution: Filter→Order).**
  - `examples/fakeshop/apps/library/filters.py` (entire file, lines 1-161) — the per-`DjangoType` filterset declaration shape Slice 4 mirrors with `orders.py`. Five-of-six owners declared (`BranchFilter`, `ShelfFilter`, `BookFilter`, `LoanFilter`, `PatronFilter`) — the same five owners Slice 4's `orders.py` must declare (`BranchOrder`, `ShelfOrder`, `BookOrder`, `LoanOrder`, `PatronOrder`). The sixth library `DjangoType` (`GenreType`) gets its filterset in a separate module (`filters_genre.py`) — Slice 4 mirrors this split with `orders_genre.py` (`GenreOrder`). The `MembershipCardType` is intentionally unwired on the filter side and stays unwired on the order side (per the schema.py:13-21 TODO anchor: "keep `MembershipCardType` unwired unless a live order test needs it" — no Slice-4 test names it, so it stays out).
  - `examples/fakeshop/apps/library/filters.py::BookFilter` (filters.py:75-98) — declares `shelf = RelatedFilter("ShelfFilter", field_name="shelf")` (same-module unqualified-name resolution; Layer-2 module-fallback branch), `genres = RelatedFilter("apps.library.filters_genre.GenreFilter", field_name="genres")` (cross-module absolute-import path; Layer-2 `import_string` first-attempt branch), `loans = RelatedFilter("LoanFilter", field_name="loans")`. Slice 4's `BookOrder` mirrors this verbatim — `shelf = RelatedOrder("ShelfOrder", field_name="shelf")`, `genres = RelatedOrder("apps.library.orders_genre.GenreOrder", field_name="genres")`, `loans = RelatedOrder("LoanOrder", field_name="loans")` — so both Layer-2 lazy-resolution branches are exercised end to end on the order side. The `genres` declaration's absolute-import path is the load-bearing fixture for Test 5 (M2M absolute-import-path).
  - `examples/fakeshop/apps/library/filters.py::BranchFilter` (filters.py:42-57) — declares `shelves = RelatedFilter("ShelfFilter", field_name="shelves", queryset=models.Shelf.objects.filter(topic="permanent collection"))`. **The order side drops the `queryset=` parameter** (Spec Decision 8 final paragraph: "the cookbook's `RelatedOrder` accepts only `orderset` and `field_name`"; `RelatedOrder` has no `queryset` kwarg per Spec Decision 2 + Decision 8 step 4 second paragraph). Slice 4's `BranchOrder.shelves` is `RelatedOrder("ShelfOrder", field_name="shelves")` — same two args, no queryset constraint. Test 4 (reverse-FK multiplicity) and Test 12 (active-related-branch permission gate) both target `BranchOrder.shelves`.
  - `examples/fakeshop/apps/library/filters.py::ShelfFilter` (filters.py:60-72) — declares `branch = RelatedFilter("BranchFilter", field_name="branch")` and `books = RelatedFilter("BookFilter", field_name="books")`. Slice 4's `ShelfOrder` mirrors this with `branch = RelatedOrder("BranchOrder", field_name="branch")` and `books = RelatedOrder("BookOrder", field_name="books")` so multi-owner reuse (`ShelfOrder` is targeted by `BranchOrder.shelves` AND by `BookOrder.shelf`) exercises the Slice-3 Axis-2 related-target-agreement check end-to-end.
  - `examples/fakeshop/apps/library/filters.py::LoanFilter` (filters.py:101-109) — declares `book = RelatedFilter("BookFilter", field_name="book")` and `patron = RelatedFilter("PatronFilter", field_name="patron")`. Slice 4's `LoanOrder` mirrors this verbatim.
  - `examples/fakeshop/apps/library/filters.py::PatronFilter` (filters.py:112-151) — declares `loans = RelatedFilter("LoanFilter", field_name="loans")` PLUS the spec-021 H4-rev8 `email_must_have_at_sign` custom-filter validator. **The order side has NO equivalent custom-validator surface** — the cookbook's `AdvancedOrderSet` does not use forms and has no validator concept (Spec Decision 8 Justification bullet 2: "no form validation (no `BaseFilterSet.form.is_valid()` step — the cookbook's `AdvancedOrderSet` doesn't use forms)"). Slice 4's `PatronOrder` is therefore SIMPLER than `PatronFilter`: `loans = RelatedOrder("LoanOrder", field_name="loans")` + `class Meta: model = models.Patron; fields = ["id", "name"]` — no custom field, no `__init__` override.
  - `examples/fakeshop/apps/library/filters_genre.py` (entire file, lines 1-34) — the cross-module Layer-2 absolute-import fixture Slice 4 mirrors with `orders_genre.py`. `GenreFilter` declares `books = RelatedFilter("apps.library.filters.BookFilter", field_name="books")` — the cross-module-pointing-back-to-the-main-orders-module variant. Slice 4's `GenreOrder` mirrors this verbatim: `books = RelatedOrder("apps.library.orders.BookOrder", field_name="books")` so the absolute-import-path Layer-2 branch is exercised in BOTH directions (`BookOrder.genres` points to `orders_genre.GenreOrder`; `GenreOrder.books` points back to `orders.BookOrder`).
  - `examples/fakeshop/apps/library/schema.py` (entire file, lines 1-274) — the `DjangoType` + root-resolver corpus Slice 4 extends. `LoanType.Meta.filterset_class = filters.LoanFilter` at schema.py:59 is the per-type wiring template Slice 4 mirrors with `Meta.orderset_class = orders.LoanOrder` etc. Five of six library `DjangoType`s carry `filterset_class` today (`LoanType`/`BookType`/`ShelfType`/`GenreType`/`BranchType`/`PatronType`); the same five gain `orderset_class`. `MembershipCardType` (schema.py:111-116) stays unwired on both sides.
  - `examples/fakeshop/apps/library/schema.py::Query` (schema.py:178-272) — the root-resolver corpus Slice 4 extends. The six in-scope root resolvers (`all_library_branches` at schema.py:199, `all_library_shelves` at schema.py:210, `all_library_books` at schema.py:221, `all_library_genres` at schema.py:236, `all_library_patrons` at schema.py:247, `all_library_loans` at schema.py:262) all share the same shape: `def all_library_<Xs>(self, info: strawberry.Info, filter: filter_input_type(filters.<X>Filter) | None = None) -> list[<X>Type]: queryset = <X>Type.get_queryset(models.<X>.objects.order_by("id"), info); if filter is not None: queryset = filters.<X>Filter.apply_sync(filter, queryset, info); return queryset`. Slice 4 extends each to: `def all_library_<Xs>(self, info: strawberry.Info, filter: filter_input_type(filters.<X>Filter) | None = None, order_by: list[order_input_type(orders.<X>Order)] | None = None) -> list[<X>Type]: queryset = <X>Type.get_queryset(models.<X>.objects.order_by("id"), info); if filter is not None: queryset = filters.<X>Filter.apply_sync(filter, queryset, info); if order_by is not None: queryset = orders.<X>Order.apply_sync(order_by, queryset, info); return queryset`. The visibility→narrow→arrange chain matches Spec Decision 8 step ordering verbatim. `all_library_prefetched_books` (schema.py:233) and `all_library_membership_cards` (schema.py:259) stay unwired (no filter today, no order added — neither feeds a Slice-4 test). The `DjangoListField` resolvers (schema.py:190-197) stay unwired per the Spec Non-goals "`DjangoListField` orderBy-argument integration deferred to `0.0.9`" + the schema.py:182-188 TODO anchor.
  - `examples/fakeshop/apps/library/schema.py::BranchType.get_queryset` (schema.py:137-150) — the existing visibility hook hiding `city="restricted"` for anonymous users. Test 9 (root `get_queryset` honoring) reuses this hook verbatim — the test seeds one `city="Boston"` row and one `city="restricted"` row and asserts that anonymous `orderBy: [{ name: DESC }]` sees ONLY the `city="Boston"` row. Test 8 (filter+order composition + optimizer cooperation) ALSO reuses this hook for the staff-vs-anonymous halves where applicable.
  - `examples/fakeshop/apps/library/schema.py::ShelfType.get_queryset` (schema.py:83-97) — the existing visibility hook hiding `topic="secret"`. Not exercised directly by Slice 4 tests but stays unchanged (Slice 4 must not break the shipped filter-side test `test_nested_related_filter_honors_target_get_queryset` at test_library_api.py:1108).
  - `examples/fakeshop/test_query/test_library_api.py` (entire file, lines 1-1309) — the live-HTTP test corpus Slice 4 appends to. Reuse the shipped helpers verbatim:
    - `_reload_project_schema_for_acceptance_tests` autouse fixture (test_library_api.py:47-50) — fires per Slice-4 test; Slice 4's order binding runs through finalizer phase 2.5 on the reload, so the fixture continues to work unchanged.
    - `_post_graphql(query, *, client=None)` helper (test_library_api.py:76-82) — anonymous POST. Used by every Slice-4 test.
    - `_post_graphql_as_staff(query)` helper (test_library_api.py:676-687) — staff POST. Used by Slice-4 Tests 9, 10, 11, 12 (the four permission/visibility tests) for the staff-pass halves where appropriate.
    - `_assert_graphql_data(query, expected)` helper (test_library_api.py:85-89) — exact-match data assertion. Used by Slice-4 Tests 1, 3, 5, 7, 13 (the exact-order assertion tests).
    - `_seed_library_graph()` helper (test_library_api.py:53-67) — the canonical one-of-each-model seed. Slice 4 tests that need only a single ordered scalar list can call this directly; tests that need specific orderings declare their own per-test inline seeds (matching the existing filter-side test pattern at e.g. test_library_api.py:692-694 for `test_library_branches_filter_by_name_icontains`).
    - `_seed_branch_with_two_shelves(name="Override")` (test_library_api.py:70-73) — already-shipped multi-shelf seed. Slice-4 Test 4 (reverse-FK multiplicity) extends this pattern with a fresh `_seed_branches_with_varying_shelves()` helper (see "New helpers justified" below) that seeds a multi-shelf Alpha + a single-shelf Beta in one call.
    - `CaptureQueriesContext(connection)` from `django.db` (used by `test_library_books_filter_preserves_optimizer_cooperation` at test_library_api.py:1020) — Slice-4 Test 8 (optimizer cooperation) reuses this pattern verbatim with the new `orderBy:` argument added.
  - `examples/fakeshop/test_query/test_library_api.py::test_library_books_filter_by_choice_enum` (test_library_api.py:766-791) — the shipped reference for the lower-case `available` enum literal. Slice-4 Test 7 (filter+order composition) uses the same `circulationStatus: { exact: available }` shape and Spec Revision 4 H4 explicitly licenses this casing.
  - `examples/fakeshop/test_query/test_library_api.py::test_library_books_filter_preserves_optimizer_cooperation` (test_library_api.py:1001-1049) — the shipped reference for `assertNumQueries`-style assertion via `CaptureQueriesContext`. Slice-4 Test 8 mirrors this pattern (3 expected queries: root SELECT + `select_related("shelf")` JOIN + `prefetch_related("genres")` SELECT) with the added `orderBy: [{ title: ASC }]` argument.
  - `examples/fakeshop/test_query/test_library_api.py::test_root_get_queryset_runs_before_filter_apply` (test_library_api.py:1228-1265) — the shipped reference for "root `get_queryset` runs before X.apply" pattern. Slice-4 Test 9 mirrors this verbatim with `orderBy:` instead of `filter:`.
  - `examples/fakeshop/test_query/test_library_api.py::test_apply_raises_graphqlerror_on_invalid_filter_input` (test_library_api.py:1176-1199) — the shipped reference for "GraphQLError-with-extensions surfaces in `payload["errors"]`" pattern. Slice-4 Tests 10, 11, 12 reuse the `payload["errors"][0]["extensions"]["code"]` assertion shape with the order-side extension code `"ORDER_PERMISSION_DENIED"` (named in Spec test plan lines 1039-1041).
  - **Investigation (Worker 1):** Already addressed. Every cited mirror exists in shipped source; this finding is a faithful recap of completed Slice 4 fakeshop + schema wiring, not a pending consolidation. Verified by symbol (line anchors drifted as the modules grew): (1) **`orders.py` ships the five OrderSet declarations** — `BranchOrder` (orders.py::BranchOrder), `ShelfOrder`, `BookOrder`, `LoanOrder`, `PatronOrder` with `__all__` listing all five, mirroring `filters.py`'s five filtersets; `orders_genre.py::GenreOrder` is the sixth in its own module mirroring `filters_genre.py::GenreFilter`. (2) **Both Layer-2 lazy-resolution branches exercised** — same-module unqualified names (`RelatedOrder("ShelfOrder")`, `RelatedOrder("BranchOrder")`, etc.) AND cross-module absolute paths in BOTH directions: `BookOrder.genres = RelatedOrder("apps.library.orders_genre.GenreOrder", field_name="genres")` (orders.py::BookOrder #"apps.library.orders_genre.GenreOrder") and `GenreOrder.books = RelatedOrder("apps.library.orders.BookOrder", field_name="books")` (orders_genre.py::GenreOrder #"apps.library.orders.BookOrder"), mirroring `BookFilter.genres` / `GenreFilter.books`. (3) **`BranchOrder.shelves` DROPS `queryset=`** — `shelves = RelatedOrder("ShelfOrder", field_name="shelves")` (orders.py::BranchOrder #"shelves = RelatedOrder") carries only the two args, vs. the filter side's `BranchFilter.shelves` which passes `queryset=models.Shelf.objects.filter(topic="permanent collection")`. (4) **`PatronOrder` is simpler than `PatronFilter`** — `orders.py::PatronOrder` is just `loans = RelatedOrder(...)` + `Meta(model=Patron, fields=["id", "name"])`, with NO custom field, NO `__init__` override, NO validator, vs. `PatronFilter`'s `email_must_have_at_sign` CharFilter + `_validate_email_must_have_at_sign` + `filter_email_must_have_at_sign`. (5) **`Meta.fields` is a list, not a dict** — every OrderSet uses the list shape (`["id", "name", "city"]`, `["id", "title", "subtitle", "circulation_status", "shelf__code"]`, etc.); `BookOrder.Meta.fields` carries the `"shelf__code"` path-shorthand WITHOUT a bare `"shelf"` column leaf, alongside the explicit `RelatedOrder("ShelfOrder", field_name="shelf")` — exactly the layout Worker 1 pinned in the Duplication-risk bullet. (6) **`schema.py` wires the five DjangoTypes** — `orderset_class` lands on `LoanType` (schema.py:51), `BookType` (:70), `ShelfType` (:102), `GenreType` (:121), `BranchType` (:156), `PatronType` (:172) — that is six types carrying `orderset_class` (the same five-of-six-plus-Genre set that carry `filterset_class`); `MembershipCardType` (schema.py::MembershipCardType) stays unwired on BOTH sides (no `filterset_class`, no `orderset_class`). (7) **Six root resolvers gain `order_by:` + `apply_sync`** — `all_library_branches` (schema.py:193/198-199), `all_library_shelves` (:207/212-213), `all_library_books` (:221/226-227), `all_library_genres` (:239/244-245), `all_library_patrons` (:253/258-259), `all_library_loans` (:271/276-277) each take `order_by: list[order_input_type(orders.<X>Order)] | None = None` and run `orders.<X>Order.apply_sync(order_by, queryset, info)` INSIDE an `if order_by is not None:` block placed AFTER the filter-narrow step (visibility `get_queryset` → filter `apply_sync` → order `apply_sync`), matching Spec Decision 8 ordering; `all_library_prefetched_books` (:231) and `all_library_membership_cards` (:263) and the two `DjangoListField` resolvers (:179-186) stay unwired. (8) **Test helpers reused verbatim** — `test_library_api.py` reuses `_reload_project_schema_for_acceptance_tests` (:48), `_seed_library_graph` (:53), `_seed_branch_with_two_shelves` (:70), `_post_graphql` (:76), `_assert_graphql_data` (:85), `_post_graphql_as_staff` (:660), and `CaptureQueriesContext` (imported :12); the two new seed helpers `_seed_branches_with_varying_shelves` (:1298) and `_seed_books_with_nullable_subtitles` (:1314) landed; order tests assert the `"ORDER_PERMISSION_DENIED"` extension code (:1688, :1744). No reconciliation needed — the artifact neither overstated nor understated; all cited mirrors are correct as shipped. Return `already-addressed`.

- [x] **New helpers justified.** Exactly TWO new fakeshop seed helpers; one fresh fixture-style permission-mode toggle; one cross-test SQL-substring helper. None are required to land in slice 4 if Worker 2 prefers to inline them — but inlining at five sites of a five-test seed pattern is the duplication risk the planning pass exists to surface.
  - `examples/fakeshop/test_query/test_library_api.py::_seed_branches_with_varying_shelves()` (NEW; module-level helper). Single responsibility: seed one `Branch(name="Alpha", city="Boston")` with three shelves (`Shelf(code="A")`, `Shelf(code="C")`, `Shelf(code="E")`) AND one `Branch(name="Beta", city="Boston")` with one shelf (`Shelf(code="B")`). Returns nothing; tests query the live schema and assert against the response shape. Used by Slice-4 Test 4 (reverse-FK multiplicity) and may be reused by Test 12 (active-related-branch permission gate) if Worker 2 wants the same shape. **Why justified:** Test 4's denormalized multiplicity assertion is the load-bearing seed shape per Spec Revision 4 M5; a typo in the seed body (e.g., one extra shelf, mis-ordered codes) would silently flip the assertion meaning. A named helper at the top of the file makes the seed shape inspectable in one place.
  - `examples/fakeshop/test_query/test_library_api.py::_seed_books_with_nullable_subtitles()` (NEW; module-level helper). Single responsibility: seed at least one `Book(subtitle=None)` row AND at least one `Book(subtitle="A Short Subtitle")` row plus their owning `Branch` + `Shelf` per Spec Revision 4 B3. Used by Slice-4 Test 2 (NULLS positioning). **Why justified:** the NULLS-positioning contract is the only Slice-4 test that turns on row-content ordering of one specific field; an inline seed body would diffuse the contract. **Alternative considered:** Worker 2 may inline this if it stays under ~6 lines AND the test name carries the seed shape in its docstring — Worker 1 accepts either choice at Worker 2's discretion (see `### Implementation discretion items`).
  - **NOT a new helper:** the `BranchOrder.check_name_permission(request)` / `BranchOrder.check_shelves_permission(request)` gates are declared INSIDE `orders.py` as ordinary `OrderSet` per-field / per-branch `check_*_permission` methods. They are NOT helpers and live with their owning `OrderSet`. The gate body for both is the standard "raise `GraphQLError('staff only', extensions={'code': 'ORDER_PERMISSION_DENIED'})` for non-staff users; return None for staff users" pattern; the `request` argument carries the user via `getattr(request, "user", None)` per the same pattern `BranchType.get_queryset` uses at schema.py:145-150. **Worker 2 owns the precise gate-body wording** within these guardrails.
  - **Investigation (Worker 1):** Already addressed. Both seed helpers shipped as named module-level helpers with exactly the prescribed single-responsibility shapes (the discretionary inline option for the second was not taken — fine, the finding licensed either). (1) `examples/fakeshop/test_query/test_library_api.py::_seed_branches_with_varying_shelves` seeds `Branch(name="Alpha", city="Boston")` with three shelves (codes `A`/`C`/`E`, `topic="general"`) + `Branch(name="Beta", city="Boston")` with one shelf (code `B`), called once by Test 4 (`test_library_branches_order_by_shelves_reverse_fk_multiplicity`-family at test_library_api.py #"_seed_branches_with_varying_shelves()"). (2) `_seed_books_with_nullable_subtitles` seeds one Branch + one Shelf (`topic="general"`, so the `ShelfType.get_queryset` `topic="secret"` filter does not hide it) + `Book(subtitle=None, title="Null Title")` + `Book(subtitle="A Short Subtitle", title="Non-null Title")`, called once by Test 2 (NULLS positioning at test_library_api.py #"_seed_books_with_nullable_subtitles()"). Each helper is invoked at exactly ONE site — no copy-pasted seed shape across multiple tests, so no warranted consolidation was skipped (the duplication risk the planning pass flagged — inlining at five sites — did not materialize). The two `check_*_permission` gates are confirmed as ordinary `@classmethod` per-field/per-branch methods on `orders.py::BranchOrder` (NOT standalone helpers): `check_name_permission` (orders.py::BranchOrder.check_name_permission) and `check_shelves_permission` (orders.py::BranchOrder.check_shelves_permission), both raising `GraphQLError(..., extensions={"code": "ORDER_PERMISSION_DENIED"})` for non-staff via `getattr(request, "user", None)` + `getattr(user, "is_staff", False)`. The helper/inline decision is sound and no duplication remains. Return `already-addressed`.

- [x] **Duplication risk avoided (six risks named explicitly).**
  - **Risk: copy-pasting the entire test_library_api.py filter-side test bodies into Slice-4 tests.** Naive implementation would copy `test_library_branches_filter_by_name_icontains` (test_library_api.py:689-704), swap `filter:` → `orderBy:`, and produce 14 near-identical bodies. **Avoidance:** Slice 4 reuses the shipped helpers (`_post_graphql`, `_assert_graphql_data`, `_post_graphql_as_staff`, `CaptureQueriesContext`, `_reload_project_schema_for_acceptance_tests`) verbatim. Tests differ ONLY in (a) the GraphQL query body, (b) the seed shape, (c) the assertion shape. The shipped pattern at test_library_api.py:1001-1049 (CaptureQueriesContext + `assert response.status_code == 200` + `payload = response.json()` + `assert "errors" not in payload` + content assertions) IS the template; Worker 2 follows it verbatim per test.
  - **Risk: re-declaring the same `BookOrder.Meta.fields` shape in two different forms across tests.** Slice-4 Test 13 (flat-shorthand `shelfCode`) requires `BookOrder.Meta.fields = ["shelf__code"]`. Every OTHER Slice-4 `BookOrder`-related test (Tests 2, 3, 5, 7, 8, 14a) requires the full `BookOrder.Meta.fields` set including `subtitle`, `title`, `circulation_status`, and a same-module `RelatedOrder("ShelfOrder")` for nested-shape ordering. **A naive implementation would either (a) ship two parallel `BookOrder` classes, or (b) make `Meta.fields` a union which would render BOTH `shelfCode: Ordering` AND `shelf: ShelfOrderInputType` and confuse the consumer.** **Avoidance:** `BookOrder.Meta.fields` carries `["id", "title", "subtitle", "circulation_status", "shelf__code"]` (note the double-underscore shorthand for `shelf__code`) AND the explicit `RelatedOrder("ShelfOrder", field_name="shelf")` declaration which OVERRIDES the `shelf__code` flat-shorthand per Spec Decision 5 + the column-overrides-by-same-name-RelatedOrder contract from Spec Edge case "`Meta.fields = '__all__'`" final sentence. The actual rendered GraphQL surface: `shelf: ShelfOrderInputType` (nested-shape — wins over the flat shorthand because `RelatedOrder("ShelfOrder", field_name="shelf")` overrides the same Python-attr-name on the OrderSet) AND `shelfCode: Ordering` (flat-shorthand surface from the explicit `"shelf__code"` field in `Meta.fields` — note: `"shelf__code"` and `"shelf"` are DIFFERENT field-spec entries, and `"shelf__code"` produces a `shelfCode: Ordering` leaf via path-rendering per Spec Edge case `Meta.fields = ["shelf__code"]`). **Worker 2 must verify (potentially via a Slice-2/3 test reading) that both surfaces coexist on the same `OrderSet` without collision — if they do collide, Worker 2 escalates to Worker 1 under `### Notes for Worker 1 (spec reconciliation)`.** **The safer alternative Worker 1 prefers:** ship `BookOrder.Meta.fields = ["id", "title", "subtitle", "circulation_status", "shelf__code"]` (no `"shelf"` column leaf in the list) PLUS the explicit `RelatedOrder("ShelfOrder", field_name="shelf")` — the column-list does NOT include `"shelf"` so the only `shelf:` surface on the input type is the nested `ShelfOrderInputType`, AND the path-shorthand `"shelf__code"` produces the `shelfCode: Ordering` leaf for Test 13. This is the layout Worker 2 should ship; Worker 1 has assessed it and pins it here.
  - **Risk: re-declaring the same `Meta.fields` shape across `orders.py` and the shipped `filters.py`.** Naive implementation would import the filter side's `Meta.fields` dict-of-lists shape (`{"id": ["exact", "in"], "name": ["exact", "icontains"]}`) into the order side. **Avoidance:** the cookbook's `OrderSet.Meta.fields` accepts a LIST of column names (`["id", "name"]`) OR the string `"__all__"` per Spec Decision 3 — NOT a dict-of-lookups (ordering has no lookups; direction is the only modifier). Worker 2 ships `BranchOrder.Meta.fields = ["id", "name", "city"]` etc. The list shape is intentionally different from `BranchFilter.Meta.fields = {"id": ["exact", "in"], ...}`. **Worker 3 should flag any dict-shaped `Meta.fields` on an OrderSet as a High finding** — the dict shape is filter-side syntax.
  - **Risk: re-using the filter side's `Meta.model = models.Branch` import path with a different absolute-module-path style.** Naive implementation would import `from apps.library import models as library_models` in `orders.py` while `filters.py` imports `from apps.library import models`. **Avoidance:** Slice 4 mirrors the filter side's import shape verbatim. The top of `orders.py`: `from __future__ import annotations` + (blank line) + `from apps.library import models` + `from django_strawberry_framework.orders import OrderSet, RelatedOrder` (mirrors filters.py:18-26 line-by-line with the substitution `filters` → `orders` and `FilterSet, RelatedFilter` → `OrderSet, RelatedOrder`).
  - **Risk: Test 12 (active-related-branch permission) and Test 10/11 (active-input-only scalar permission) declaring the same `check_*_permission` gates twice on `BranchOrder`.** **Avoidance:** Both Tests 10/11 (scalar `check_name_permission`) AND Test 12 (relation-level `check_shelves_permission`) live on the SAME `BranchOrder` class. The two gates fire under different inputs per Spec H3-rev3 active-branch dispatch + Spec M6-rev1 active-input-only scope: Test 10's input `orderBy: [{ name: ASC }]` fires `check_name_permission` only; Test 11's input `orderBy: [{ city: ASC }]` fires NEITHER; Test 12's input `orderBy: [{ shelves: { code: ASC } }]` fires `check_shelves_permission` only; Test 12's second-half input `orderBy: [{ name: ASC }]` fires `check_name_permission` only (so Test 12 second half ALSO needs staff context, OR the test must declare the gates' parent class differently). **Worker 1 has assessed this:** Test 12's second-half input MUST be `orderBy: [{ city: ASC }]` (the unguarded field) to be quiet against `check_shelves_permission` AND `check_name_permission` together. Worker 2 ships Test 12 with `city: ASC` in the second half. **Re-confirmed:** the Spec test-plan line 1041 says "Re-issue `{ allLibraryBranches(orderBy: [{ name: ASC }]) { id } }` (the `shelves` branch is absent); assert no error AND a successful ordered response" — BUT this conflicts with Test 10's `check_name_permission` firing on the same input. **Worker 1's planning-pass resolution:** Test 12's second half uses `orderBy: [{ city: ASC }]` instead (no gated field, no gated branch). **This is a spec wording gap; Worker 1 will record this under `### Notes for Worker 1 (spec reconciliation)` and either edit the spec text at final verification OR leave as Worker-2-discretion if the spec author's intent allows.** See `### Notes for Worker 1 (spec reconciliation)` below for the exact wording.
  - **Risk: collapsing the `_seed_branches_with_varying_shelves` helper into the existing `_seed_branch_with_two_shelves` helper.** **Avoidance:** the existing helper at test_library_api.py:70-73 seeds two shelves per branch with `code="A-1"` and `code="B-2"`; Test 4's multiplicity-assertion requires three shelves with `code="A"`, `code="C"`, `code="E"` PLUS a single-shelf Beta with `code="B"`. The two seed shapes diverge on shelf-count AND shelf-code form. Worker 2 ships them as TWO separate helpers (the existing one stays unchanged; the new one is the Slice-4 addition).
  - **Investigation (Worker 1):** Already addressed. All six named avoidances hold in shipped source — this is a faithful recap of completed Slice-4 wiring, not a pending consolidation. Verified by symbol (line anchors drifted as the file grew): (1) **Helpers reused, no copy-pasted filter-side bodies** — the Slice-4 order tests reuse `CaptureQueriesContext` (imported at `test_library_api.py` #"from django.test.utils import CaptureQueriesContext"), `_post_graphql` (`test_library_api.py::_post_graphql`), `_assert_graphql_data` (`test_library_api.py::_assert_graphql_data`), `_post_graphql_as_staff` (`test_library_api.py::_post_graphql_as_staff`), and the `_reload_project_schema_for_acceptance_tests` autouse fixture verbatim; each order test differs only in query body / seed / assertion. (2) **`BookOrder.Meta.fields` ships the pinned layout with both surfaces coexisting** — `orders.py::BookOrder` declares `fields = ["id", "title", "subtitle", "circulation_status", "shelf__code"]` (NO bare `"shelf"` column leaf) PLUS `shelf = RelatedOrder("ShelfOrder", field_name="shelf")`. Both surfaces are exercised live without collision: `test_library_books_order_by_multi_field_priority` queries the nested `orderBy: [{ shelf: { code: ASC } }, { title: DESC }]` (the `RelatedOrder` surface) and `test_library_books_order_by_flat_shorthand_path` queries the flat `orderBy: [{ shelfCode: ASC }]` (the `"shelf__code"` path-shorthand surface) — both return HTTP 200 with `"errors" not in payload`, confirming `shelf: ShelfOrderInputType` and `shelfCode: Ordering` coexist on the same input type. The build-pass confirmed the coexistence (no escalation under `### Notes for Worker 1`); the artifact's "Worker 2 must verify" flag resolved cleanly. (3) **OrderSet `Meta.fields` use the LIST shape** — every shipped orderset uses a list (`BranchOrder` `["id", "name", "city"]`, `ShelfOrder` `["id", "code", "topic"]`, `BookOrder` as above, `LoanOrder` `["id", "note"]`, `PatronOrder` `["id", "name"]`), never the filter-side dict-of-lookups shape. (4) **`orders.py` mirrors the filter import style** — `orders.py` #"from apps.library import models" matches `filters.py` #"from apps.library import models" verbatim (no aliased `as library_models`). (5) **Both gates on the same `BranchOrder`; Test-12 reconciliation matches shipped** — `orders.py::BranchOrder.check_name_permission` and `orders.py::BranchOrder.check_shelves_permission` both live on `BranchOrder`, both raise `GraphQLError(..., extensions={"code": "ORDER_PERMISSION_DENIED"})` for non-staff. The reconciled quiet/second half ships as `orderBy: [{ city: ASC }]` (the unguarded scalar) in `test_library_api.py::test_order_check_permission_denies_active_related_branch` (denial half uses `orderBy: [{ shelves: { code: ASC } }]` asserting the `ORDER_PERMISSION_DENIED` code; quiet half uses `city: ASC` asserting no errors) — exactly the Worker-1 planning-pass reconciliation for Spec line 1041, which `bld-slice-4-live_http.md` records as having been applied to the spec text (`name` → `city`). (6) **`_seed_branches_with_varying_shelves` is a SEPARATE helper** — `test_library_api.py::_seed_branches_with_varying_shelves` (Alpha with shelves A/C/E + Beta with shelf B) is distinct from the unchanged `test_library_api.py::_seed_branch_with_two_shelves` (codes A-1/B-2); not collapsed.
    - **Reconciliation note (test-number label, not substance):** the finding's risk-#5 prose and the source artifact discuss the reconciliation as "Test 12's second half," but the build/spec renumbered the live-HTTP enumeration to 14 (the M7 no-op pair was combined into one test). The shipped test carrying the reconciled quiet half is named `test_order_check_permission_denies_active_related_branch` and is labeled **Slice-4 Test 11** in its docstring. The substance — quiet half uses `orderBy: [{ city: ASC }]`, the unguarded field — matches the finding exactly; only the test-number label drifted. No source change warranted; recorded here per `DRY.md` "Spec / artifact reconciliation" (the closed artifact is not edited).

- [x] **`scripts/review_inspect.py` planning-time disposition (record per `BUILD.md`).** Ran the helper on the **current** files Slice 4 will modify on 2026-06-01: `examples/fakeshop/apps/library/schema.py` (274 lines) and `examples/fakeshop/test_query/test_library_api.py` (1309 lines). Both files are over the 150-line trigger; `schema.py` is also under the broad fakeshop-app live-coverage path so the helper run is required by BUILD.md "When to run the helper during build".
  - **`schema.py` shadow scan.** 27 symbols, no control-flow hotspots; 2x `"is_staff"` repeated string literals (in `BranchType.get_queryset` + `ShelfType.get_queryset`). Slice 4 adds: (1) the `from apps.library import orders, orders_genre` import (one new line under the existing `from apps.library import filters, filters_genre, models` at schema.py:9); (2) the `from django_strawberry_framework.orders import order_input_type` import (one new line under the existing `from django_strawberry_framework.filters import filter_input_type` at schema.py:11); (3) `Meta.orderset_class = orders.<X>Order` on five DjangoType classes (5 added lines total); (4) the `order_by:` argument + `if order_by is not None:` block on six root resolvers (six resolvers × ~3 added lines = ~18 added lines); (5) removal of the two TODO anchors at schema.py:13-21 and schema.py:182-188. Estimated post-Slice-4 schema.py line count: ~310 source lines, still no hotspots. No new repeated-literal candidates.
  - **`test_library_api.py` shadow scan.** 12 imports, 56 symbols. The top repeated-string literals are: `"allLibraryBooks"` (13x), `"Speculative"` (8x), `"allLibraryBranches"` (8x), `"Foundation"` (8x), `"library_shelf"` (7x), `"GenreType"` (6x), `"Hyperion"` (6x), `"Andromeda Main"` (5x), `"Cambridge"` (5x). Slice 4 adds 14 new test functions plus 2 new module-level helpers (`_seed_branches_with_varying_shelves`, `_seed_books_with_nullable_subtitles`). Each test will reuse `allLibraryBranches` / `allLibraryBooks` so those counts will grow further (estimated +10 occurrences of `"allLibraryBranches"` and +6 of `"allLibraryBooks"`). **Not a consolidation candidate** — these are inline GraphQL query strings; the test corpus's hardcoded query strings are intentional per the "Live GraphQL HTTP tests" pattern, and consolidating into a `_query_template_format(...)` helper would obscure the actual GraphQL surface under test. Estimated post-Slice-4 test_library_api.py line count: ~1700-1800 source lines (14 tests × ~25-40 lines each + 2 helpers + the test docstrings). Worker 3's review-time helper trigger fires (file is >150 lines AND >50 added lines outside `django_strawberry_framework/`).
  - **New files NOT triggering the helper.** `examples/fakeshop/apps/library/orders.py` and `orders_genre.py` are NEW files containing ONLY `class <X>Order(OrderSet)` declarations (no functions, no module-level logic apart from `__all__` tuple and a per-class docstring). Per BUILD.md "Worker 3 must run the helper during review when ... unless it is a pure-class-definition module (only `class` declarations with docstrings, no logic)", these files DO NOT trigger the helper. Worker 1 records the skip-and-reason here per BUILD.md.
  - **Investigation (Worker 1):** Already addressed — the planning-time shadow-scan disposition holds against FINAL shipped source, and no genuine pending consolidation was left behind. Evidence: (1) **`schema.py` `"is_staff"` 2x holds** — exactly two occurrences, both in visibility `get_queryset` hooks (`examples/fakeshop/apps/library/schema.py` #"getattr(user, \"is_staff\", False)" at the `BranchType` and `ShelfType` hooks); file grew to 281 lines, still no control-flow hotspots, no new repeated-literal candidate from the Slice-4 `orderset_class` + `order_by:` additions. (2) **`test_library_api.py` inline-query-string disposition holds with filter-side parity** — `"allLibraryBooks"` now appears 42x and `"allLibraryBranches"` 40x; there is NO `_query_template_format` / `.format()`-based query-builder helper anywhere in the file (`grep` for `_query_template` / `def _query` / `query_template` returns empty). The order-side tests hardcode their GraphQL query bodies inline (e.g. `allLibraryBranches(orderBy: …)` / `allLibraryBooks(orderBy: …)`) EXACTLY as the already-shipped filter-side tests hardcode theirs (`allLibraryBranches(filter: …)` at lines 682/718/738/931/1045/1221/1236, `allLibraryBooks(filter: …)` at 766/801/968/1005/1079/1189, etc.). Consolidating the repeated query-name literals or templating the bodies would obscure the live GraphQL surface under test and would DIVERGE from the established filter-side pattern in the same file — so "NOT a consolidation candidate" is correct and parity-confirmed. (3) **`orders_genre.py` is a genuine pure-class module** — only `GenreOrder(OrderSet)` with a docstring + `Meta` + one `RelatedOrder` attribute + `__all__`; no logic. The Worker-3 review-time skip-and-reason is sound for it. **Reconciliation note (characterization imprecision, not a pending consolidation):** the finding's "no functions, no module-level logic" wording is literally accurate for `orders_genre.py` but NOT for `orders.py` as shipped — `orders.py::BranchOrder.check_name_permission` and `orders.py::BranchOrder.check_shelves_permission` are two `@classmethod` gates carrying real branch logic (`getattr(request, "user", None)` + `not getattr(user, "is_staff", False)` → `raise GraphQLError(..., extensions={"code": "ORDER_PERMISSION_DENIED"})`), so `orders.py` is strictly a class-definition-PLUS-permission-gate module rather than a logic-free pure-class module. This does NOT flip the helper disposition for DRY purposes: those two near-identical gate bodies (differing only in the message string) were already surfaced and dispositioned by the "New helpers justified" finding above (its Investigation sub-bullet confirms they are correctly ordinary `OrderSet` `check_*_permission` methods living with their owning OrderSet, NOT a missed helper), so no still-pending consolidation exists here. Per `DRY.md` "Spec / artifact reconciliation" the closed `bld-slice-4-live_http.md` artifact is not edited; the imprecision is recorded here. Return `already-addressed`.

---

## Build: Slice 5 — Docs + KANBAN + CHANGELOG status updates (no version bump)

_Source: `docs/builder/bld-slice-5-docs_kanban_changelog.md`_

Slice 5 is docs-only — five `.md` files plus `KANBAN.md` plus `CHANGELOG.md`. The DRY risk is the same risk the spec's M10 of rev1 already named: status strings, headline sentences, and the past-tense narrative get copy-pasted across `docs/GLOSSARY.md` entry bodies, the `docs/README.md` shipped-bullet list, the `README.md` shipped-bullet list, the `KANBAN.md` Done card body, the `CHANGELOG.md` Added bullet, and the `TODAY.md` shipped-capabilities enumeration — and the moment any two of them drift, a future reader cannot tell which is canonical. The spec's M10 resolution (the CHANGELOG bullet references the KANBAN past-tense body as the single source of truth and ships only a one-line headline summary) is the load-bearing DRY rule for this slice; every other content edit is **either** verbatim from the spec's Doc updates section (lines 1048-1092) **or** a small structural change (Index table cell, Browse-by-category line, TREE.md `[alpha]` → on-disk move) that has only one site by construction.

- [x] **Existing patterns reused (verbatim sibling-pattern carry-overs from DONE-027-0.0.8's Slice 5).**
  - `CHANGELOG.md` `[Unreleased]` `### Added` shape — the Filtering subsystem bullet at `CHANGELOG.md` line 21 IS the template the Ordering bullet sibling-mirrors. Slice 5 ships ONE Ordering bullet under the same `### Added` heading (the heading already exists; the Filtering subsystem bullet from `DONE-027-0.0.8` already lives there) and ONE `### Changed` bullet under the existing `### Changed` heading (line 23). The `[Unreleased]` heading at line 19 stays as `[Unreleased]` — promotion to `## [0.0.8]` is Spec Decision 10 territory and is explicitly forbidden in this card.
  - `docs/GLOSSARY.md` "Flip status" pattern — verbatim mirror of `DONE-027-0.0.8`'s Slice 5 flip of `FilterSet` / `RelatedFilter` / `filter_input_type` / `Meta.filterset_class` from `planned for 0.0.8` to `shipped (0.0.8)`. Five GLOSSARY entries (`OrderSet`, `Ordering`, `order_input_type`, `RelatedOrder`, `Meta.orderset_class`) take the same flip; the bodies update per the Spec Doc updates section's pinned text at lines 1050-1057.
  - `docs/GLOSSARY.md` Index table status column — Filtering subsystem rows already flipped to `shipped (0.0.8)` in DONE-027-0.0.8. Slice 5 flips the five Ordering rows at lines 91 / 97 / 98 / 99 / 105 of the current `docs/GLOSSARY.md`.
  - `docs/GLOSSARY.md` Browse-by-category Ordering line at line 130 already enumerates the five symbols — no body edit needed there beyond verifying every entry's anchor still resolves after the body edits (the anchors `#orderset` / `#ordering` / `#relatedorder` / `#order_input_type` / `#metaorderset_class` are stable; only the bodies change).
  - `docs/README.md` shipped-bullet list (line 97 onwards) — mirror of DONE-027-0.0.8's Slice 5 addition (the filter bullet at line 114 IS the template). Slice 5 adds one Ordering bullet alongside it; Filtering bullet stays untouched.
  - `README.md` Status paragraph (line 45-52) — DONE-027-0.0.8 left "Newest shipped surface: the filtering subsystem (`0.0.8`)" as the canonical phrasing. Slice 5 updates the paragraph to name Ordering as the newest shipped surface and demotes Filtering to "alongside the filter symbols promoted in DONE-027-0.0.8" (the spec's pinned wording at line 1068 carries this verbatim).
  - `docs/TREE.md` `[alpha]` → on-disk move — DONE-027-0.0.8 already moved `filters/` from the target-layout block to the current-on-disk block; Slice 5 does the same for `orders/`. The `orders/` directory listing in the target-layout block (lines 278-283 of the current `docs/TREE.md`) has a Slice 5 TODO anchor at line 283 that is removed by this slice; the matching `tests/orders/` listing in the target-shape block (lines 461-464) is already pinned and just needs to be promoted to the current-shape block at the same time the source listing moves.
  - `KANBAN.md` column-move pattern — every prior `DONE-NNN-X.Y.Z` card ships the same Done-column shape: a single past-tense bullet body, no future-tense / "planned" language anywhere in the card body. Slice 5 follows the same shape for the Ordering card; the spec pins the body text verbatim at line 1080.
  - `TODAY.md` "library" current-state paragraph at line 19 already carries the filter sibling-narration ("The new `BranchFilter` / `ShelfFilter` / `BookFilter` / `LoanFilter` / `PatronFilter` declarations at `examples/fakeshop/apps/library/filters.py` (plus `GenreFilter` at `filters_genre.py` for the cross-module absolute-import-path case) and the 14 live `/graphql/` HTTP filter tests at `examples/fakeshop/test_query/test_library_api.py` exercise the `FilterSet` / `RelatedFilter` / `Meta.filterset_class` wiring end-to-end through finalizer phase 2.5."). Slice 5 appends a parallel sentence naming the five orderset classes + `GenreOrder` cross-module fixture + 14 live order HTTP tests. The TODO anchor at lines 11-17 of `TODAY.md` is removed in the same edit.
  - **Investigation (Worker 1):** Already addressed — every cited verbatim sibling-pattern carry-over is present in the CURRENT standing docs (read against the working tree, including the pre-existing uncommitted maintainer edits to `CHANGELOG.md` / `KANBAN.md`, none reverted). Confirming locations: (1) **CHANGELOG** — `[Unreleased]` heading is NOT promoted (`CHANGELOG.md #"## [Unreleased]"`, still `[Unreleased]`, with `## [0.0.7] - 2026-05-27` the next versioned heading below); one Ordering bullet under `### Added` (`CHANGELOG.md #"Ordering subsystem.] [`OrderSet`"`); one Ordering bullet under `### Changed` (`CHANGELOG.md #"Meta.orderset_class`] is no longer in `DEFERRED_META_KEYS`"`). (2) **GLOSSARY** — all five entry-body `**Status:**` lines flipped to `shipped (0.0.8)` (`docs/GLOSSARY.md` `## Meta.orderset_class` / `## Ordering` / `## OrderSet` / `## order_input_type` / `## RelatedOrder`), all five Index-table status cells flipped (`docs/GLOSSARY.md #"[`Meta.orderset_class`](#metaorderset_class) | shipped"` plus the `Ordering` / `OrderSet` / `order_input_type` / `RelatedOrder` rows), and the Browse-by-category Ordering line intact with all five anchors (`docs/GLOSSARY.md #"- **Ordering:** [`OrderSet`](#orderset)"`). (3) **docs/README.md** — ordering shipped-bullet present (`docs/README.md #"ordering subsystem (new in `0.0.8`)"`, naming `OrderSet` / `RelatedOrder` / `Meta.orderset_class` / `Ordering` / `order_input_type`). (4) **README.md** — Status paragraph names ordering as the newest shipped surface (`README.md #"Newest shipped surface: the ordering subsystem"`, demoting Filtering to "alongside the filter symbols promoted in `DONE-027-0.0.8`'s Slice 5"). (5) **docs/TREE.md** — `orders/` lives in the current-on-disk block (`docs/TREE.md #"orders/                  # Ordering subsystem (Layer 3 read-side) — shipped 0.0.8"`), the intro enumerates it on-disk (`docs/TREE.md #"filters, orders, testing, and utility subpackages are on disk"`), `tests/orders/` mirror present (`docs/TREE.md #"orders/                  # mirrors django_strawberry_framework/orders/"`), and NO `orders/` source entry remains in the target-package-layout block. (6) **KANBAN** — past-tense `DONE-028-0.0.8` Ordering card shipped (snapshot `KANBAN.md #"the Ordering subsystem as `DONE-028-0.0.8`. No active WIP cards"`, spec-map row `KANBAN.md #"`DONE-028-0.0.8` — Ordering subsystem |"`); no `WIP-ALPHA-028` string remains. (7) **TODAY.md** — parallel ordering sentence naming the five orderset classes + `GenreOrder` + 14 live order HTTP tests + filter+order composition (`TODAY.md #"The new `BranchOrder` / `ShelfOrder` / `BookOrder` / `LoanOrder` / `PatronOrder` declarations"`). No Slice-5 TODO anchors leaked: `grep -rE "TODO\(spec-028-orders-0_0_8 Slice 5\)"` across the standing docs returns hits only in per-cycle scratchpad artifacts (`docs/dry/dry-0_0_8.md`, `docs/builder/bld-slice-5-docs_kanban_changelog.md`), which are exempt. **Reconciliation note (navigational-anchor drift, not a pending consolidation):** the finding's raw line anchors have drifted vs. current on-disk positions (GLOSSARY Index rows 91/97/98/99/105 → now 92/98/99/100/106; Browse line 130 → 132; TODAY paragraph "line 19" → now folded into the line-11 rich-demonstration paragraph) due to the maintainer's concurrent baseline-dirty edits; per AGENTS.md #27 + START.md "Temp artifact conventions" these are per-cycle-scratchpad navigational hints exempt from the symbol-path rule and every substring/symbol anchor still resolves, so no `- [ ]` text correction is warranted. This is docs-only verification with zero net-new helpers and no still-pending consolidation. Return `already-addressed`.

- [x] **New helpers justified (none).** Slice 5 ships zero net-new shared helpers. No `.py` files added or modified. Every doc edit is a single-site change to a `.md` file (or its source-of-truth SQLite row, for `KANBAN.md`).
  - **Pre-triage (Worker 0):** Skipped — no-opportunity recap: "Slice 5 ships zero net-new shared helpers. No `.py` files added or modified." (docs-only slice; the new-helper dimension is vacuous and code-inspection-free to dispose of).

- [x] **Duplication risk avoided (four risks named explicitly).**
  - **Risk: drift between the KANBAN past-tense body and the CHANGELOG bullet.** Naive implementation would copy-paste the full ~600-word past-tense narrative into both the `KANBAN.md` Done card AND the `CHANGELOG.md` `### Added` bullet, exactly the duplication the spec's M10 of rev1 already named. **Avoidance:** the spec's pinned CHANGELOG bullet at line 1087 is **explicitly** a one-line headline that references the KANBAN body as the single source of truth — `"Ordering subsystem — see the KANBAN past-tense body above for the canonical narrative … Headline: OrderSet / RelatedOrder / Meta.orderset_class shipped … 14 live HTTP tests in examples/fakeshop/test_query/test_library_api.py. Spec: docs/spec-028-orders-0_0_8.md."`. Worker 2 ships exactly that one-line headline; the long-form narrative lives only in `KANBAN.md`'s Done card. Worker 3's `diff` against the spec catches verbatim drift.
  - **Risk: docs/GLOSSARY.md entry body drift from the spec's pinned text.** The spec lines 1050-1056 each pin one entry-body update sentence with specific clauses (e.g., line 1051's `OrderSet` body mentions "active-input-only scope plus active-branch double-dispatch for `RelatedOrder` branches"; line 1052's `RelatedOrder` body mentions "sibling import from `sets_mixins.LazyRelatedClassMixin`" — wording that is load-bearing for the H1 of rev3 retraction). **Avoidance:** Worker 2 quotes each spec body sentence verbatim into the GLOSSARY entry, then layers ordering-specific detail beyond the pinned sentence only as the entry's overflow text. Worker 3 diffs the new GLOSSARY entry bodies against the spec's pinned text at lines 1050-1056 and flags any verbatim drift.
  - **Risk: shipped-bullet-list drift between `docs/README.md` and `README.md`.** The spec's Doc updates section at lines 1060 + 1068 names the same five symbols for both shipped lists (`OrderSet` / `RelatedOrder` / `order_input_type` / `Ordering` / `Meta.orderset_class` in `docs/README.md`; `OrderSet` / `RelatedOrder` / `order_input_type` / `Ordering` in `README.md`). M3 of rev3 explicitly requires `Ordering` in both lists. **Avoidance:** the plan pins both lists to enumerate the same four symbols in the same order (`OrderSet` first, then `RelatedOrder`, then `Ordering`, then `order_input_type`); `Meta.orderset_class` appears only in `docs/README.md` (per Spec line 1060 — the deeper `docs/README.md` shipped list is the only one to enumerate `Meta` keys; the top-level `README.md` status paragraph stays one tier higher and does not enumerate `Meta` keys). Worker 3 confirms `Ordering` appears in both shipped lists per M3 of rev3.
  - **Risk: TODO-anchor leakage post-Slice-5.** The spec's Slice 5 sub-bullets (lines 129-137) implicitly remove every `TODO(spec-028-orders-0_0_8 Slice 5)` anchor in the standing docs: `docs/GLOSSARY.md` line 705 (Meta.orderset_class entry TODO comment block), `docs/README.md` line 90 (shipped-list TODO comment block), `README.md` line 45 (Status paragraph TODO comment block), `docs/TREE.md` line 283 (orders/inputs.py target-layout TODO comment), `TODAY.md` line 11 (library current-state TODO comment block). Five anchors total. Worker 2 deletes all five during the Slice 5 doc-flip; Worker 3 verifies via `grep -rE "TODO\(spec-028-orders-0_0_8 Slice 5\)" docs/ README.md TODAY.md KANBAN.md CHANGELOG.md` returning empty.
  - **Investigation (Worker 1):** Already addressed — the duplication/drift the four named risks guard against is NOT present in the CURRENT standing docs (read against the working tree, including the maintainer's CHANGELOG/KANBAN restructure from commit `311f908`; nothing reverted). Per-risk evidence: **(1) KANBAN↔CHANGELOG single-source — outcome holds, mechanism changed.** The ~600-word past-tense narrative lives only in the `KANBAN.md` Done card `#### Other` section (`KANBAN.md #"Shipped the ordering subsystem in `0.0.8`."`, the full multi-clause body); the `CHANGELOG.md` `### Added` Ordering entry (`CHANGELOG.md #"**Ordering subsystem.** [`OrderSet`]"`) is a compact ~4-sentence summary, NOT a copy of that narrative — so no long-form duplication exists. **Reconciliation note (mechanism drift, not a pending consolidation):** the finding's specific avoidance mechanism ("the CHANGELOG bullet is explicitly a one-line headline that references the KANBAN past-tense body as the single source of truth … `see the KANBAN past-tense body above for the canonical narrative`") no longer matches the on-disk CHANGELOG: the maintainer's CHANGELOG restructure (commit `311f908`) replaced the spec-line-1087-pinned KANBAN-pointer headline with a standalone Keep-a-Changelog-style entry that does not reference KANBAN. The two surfaces are now independent descriptions at different altitudes (CHANGELOG = release-note summary; KANBAN = full narrative); the no-duplication DRY outcome holds, only the single-source pointer was retired. This is a closed-cycle artifact-vs-current reconciliation, not a duplication still pending fix. **(2) GLOSSARY body — holds.** All five Ordering entries flipped to `**Status:** shipped (`0.0.8`)` (`docs/GLOSSARY.md` `## Meta.orderset_class` / `## Ordering` / `## OrderSet` / `## order_input_type` / `## RelatedOrder`); bodies match the spec's pinned clauses, including the two load-bearing ones — `OrderSet` body carries "active-input-only scope plus active-branch double-dispatch for `RelatedOrder` branches" (`docs/GLOSSARY.md #"active-branch double-dispatch for [`RelatedOrder`]"`) and `RelatedOrder` body carries the sibling import (`docs/GLOSSARY.md #"sibling import from `sets_mixins.LazyRelatedClassMixin`"`). No verbatim drift. **(3) README-list parity — holds.** `Ordering` appears in BOTH shipped lists (`docs/README.md #"the public `Ordering` enum (six members with NULLS positioning)"` at the ordering-subsystem bullet; `README.md #"the public `Ordering` enum (six members with NULLS positioning)"` at the Status paragraph), satisfying M3 of rev3. **Reconciliation note (finding-recap inaccuracy):** the finding's sub-claim that `Meta.orderset_class` appears "only in `docs/README.md`" is contradicted by the shipped (and spec-line-91-pinned) `README.md` Status paragraph, which DOES name "`Meta.orderset_class` wiring" — this matches what Worker 2 reported shipping in `bld-slice-5-docs_kanban_changelog.md` and the spec-pinned README wording, so it is a recap imprecision in the finding text, not a doc-drift defect; a top-level README naming a `Meta` key in prose is not a DRY/duplication violation. **(4) TODO-anchor leakage — holds.** `grep -rnE "TODO\(spec-028-orders-0_0_8 Slice 5\)" docs/ README.md TODAY.md KANBAN.md CHANGELOG.md` returns exactly two hits, both in per-cycle scratchpad artifacts (`docs/dry/dry-0_0_8.md` line 266 and `docs/builder/bld-slice-5-docs_kanban_changelog.md` line 29) — exempt per AGENTS.md #"Source references in docs and code comments" + START.md "Temp artifact conventions". No standing doc carries the anchor. This is docs-only verification with zero net-new helpers and no still-pending consolidation. Return `already-addressed`.

---

## Build: Slice 6 — Cross-card composition smoke test with shipped Filtering subsystem

_Source: `docs/builder/bld-slice-6-composition_smoke.md`_

Slice 6 ships ONE new package-internal test file under `tests/orders/test_composition.py`. The file already exists as a 10-line stub TODO-pseudocode placeholder (`tests/orders/test_composition.py:1-10`) per the pre-Slice-1 scaffolding seeded by Slice 1; Slice 6 REPLACES the stub with the real test body. NOTHING under `django_strawberry_framework/` is touched (the order + filter subsystems are already shipped through Slices 1-5 of this card and `DONE-027-0.0.8`); NOTHING under `examples/fakeshop/` is touched (the cross-card composition is already exercised live by Slice 4's `test_library_books_filter_and_order_compose` at `examples/fakeshop/test_query/test_library_api.py:1500-1543`). Slice 6 is a pure package-internal test slice that pins the composition contract from inside the package's test boundary.

The Slice 4 live HTTP test `test_library_books_filter_and_order_compose` (test_library_api.py:1500-1543) already pins the **live consumer-visible behavior** of filter+order composition end-to-end (GraphQL surface accepts both arguments; the response is filtered AND ordered). Slice 6 MUST NOT duplicate that assertion altitude; per spec line 138-139 and AGENTS.md "Test through real usage and prefer the example project: any coverage line achievable via a real GraphQL query against fakeshop in examples/fakeshop/test_query/ MUST be earned that way", the live-HTTP path already owns the consumer-visible composition coverage. Slice 6 therefore pins a **complementary** package-internal angle that the live HTTP path does NOT (and arguably cannot) cleanly assert: the FINALIZER/REGISTRY composition contract that both factories' input types are materialized into their respective per-module namespaces in one `finalize_django_types()` pass, that both `_helper_referenced_*` ledgers register cleanly when both helpers are called for the same `DjangoType`, that the apply pipelines produce a queryset whose **SQL** carries both `WHERE` and `ORDER BY` clauses in the canonical chain order, and that the shared `sets_mixins.LazyRelatedClassMixin` resolves correctly when called from both subsystems on the same finalize.

- [x] **Existing patterns reused.**
  - `tests/orders/test_finalizer.py::_isolate_registry` autouse fixture (tests/orders/test_finalizer.py:51-65) — the canonical reset shape clearing `registry`, `_field_specs`, `_helper_referenced_ordersets`, `_materialized_names`, `OrderArgumentsFactory.input_object_types`, and `OrderArgumentsFactory._type_orderset_registry` around each test. Slice 6 reuses this shape **extended** to also reset the filter-side equivalents (`_helper_referenced_filtersets`, `_materialized_names` in `filters.inputs`, `FilterArgumentsFactory.input_object_types`, `FilterArgumentsFactory._type_filterset_registry`) so the composition test starts from a clean registry-state on BOTH subsystems. The filter-side reset pattern lives at `tests/filters/test_factories.py:42-48`.
  - `tests/orders/test_finalizer.py::test_phase_2_5_materializes_input_classes_into_module_globals` shape (tests/orders/test_finalizer.py:380-388) — the "assert `hasattr(inputs_module, '<X>OrderInputType')` AND `'<X>OrderInputType' in _materialized_names`" assertion pair Slice 6 mirrors for both `<X>OrderInputType` AND `<X>FilterInputType` against their respective inputs modules and ledgers (`django_strawberry_framework.orders.inputs` + `_materialized_names` on the order side; `django_strawberry_framework.filters.inputs` + the filter side's `_materialized_names` ledger).
  - `tests/orders/test_sets.py::_make_info` (tests/orders/test_sets.py:318-322) — the minimal `info`-shaped stub `SimpleNamespace(context=SimpleNamespace(request=HttpRequest()))` Slice 6 reuses verbatim for the apply-pipeline-into-queryset step. (Worker 2 may also factor the shared `_make_info` into a small helper local to the test file rather than re-import, at discretion — see `### Implementation discretion items`.)
  - `tests/orders/test_sets.py::test_orderset_apply_sync_applies_order_by_to_queryset` shape (tests/orders/test_sets.py:365-378) — the `list(result.query.order_by)` introspection pattern Slice 6 mirrors for the ORDER BY half of the composition assertion. The filter side's analogous pattern is `result.query.where` at `tests/filters/test_sets.py::test_apply_sync_filters_against_simple_scalar_input` (test_sets.py:845-855) — Slice 6 mirrors that pattern for the WHERE half.
  - `examples/fakeshop/apps/library/models` — Slice 6 reuses the fakeshop library models (`Book`, `Shelf`, `Branch`, `Genre`, etc.) as fixture models, NOT a test-only `apps.fake` model. The library models are already imported by every shipped `tests/orders/test_*.py` file (the `from apps.library import models as library_models` shape at e.g. tests/orders/test_factories.py:14 + tests/orders/test_finalizer.py:30) and by the filter-side `tests/filters/` tree. Reusing them keeps Slice 6 in the same test-boundary shape as every other `tests/orders/` test.
  - The shipped fakeshop library schema (`examples/fakeshop/apps/library/schema.py:55-71` for `BookType` carrying BOTH `filterset_class = filters.BookFilter` AND `orderset_class = orders.BookOrder`) is the canonical real-world example of the composition contract Slice 6 pins. Five library DjangoTypes (`LoanType`/`BookType`/`ShelfType`/`GenreType`/`BranchType`) currently carry both `filterset_class` and `orderset_class`; Slice 6's test does NOT need to instantiate any of them — it constructs a minimal local `DjangoType` from a library model so the registry-isolated fixture state is clean (the `_reload_project_schema_for_acceptance_tests` autouse path from `examples/fakeshop/test_query/test_library_api.py:47-50` is heavyweight and lives at the live-HTTP boundary, not the package-internal one).
  - **Investigation (Worker 1):** Already addressed. Every cited reuse pattern is present verbatim in the shipped `tests/orders/test_composition.py` (on-disk read, baseline `0e88dd7`). (1) `_isolate_registry::tests/orders/test_composition.py::_isolate_registry` (`#"def _isolate_registry"`, lines 88-124) mirrors the order-side six-clear sequence from `tests/orders/test_finalizer.py::_isolate_registry` (lines 51-65) and EXTENDS it with the filter-side equivalents — `_helper_referenced_filtersets.clear()`, `_filter_materialized_names.clear()` (filter-side `_materialized_names`), `FilterArgumentsFactory.input_object_types.clear()`, `FilterArgumentsFactory._type_filterset_registry.clear()` (composition `#"_helper_referenced_filtersets.clear()"` / `#"FilterArgumentsFactory._type_filterset_registry.clear()"`), exactly the four filter-side symbols the finding names. (2) `tests/orders/test_composition.py::_make_info` (`#"def _make_info"`, lines 127-136) is the same `SimpleNamespace(context=SimpleNamespace(request=HttpRequest()))` stub as `tests/orders/test_sets.py::_make_info` (lines 318-322), declared locally per the plan's discretion item. (3) ORDER BY introspection `order_by = list(queryset.query.order_by)` (composition `#"list(queryset.query.order_by)"`, line 219) mirrors `tests/orders/test_sets.py::test_orderset_apply_sync_applies_order_by_to_queryset` (`#"list(result.query.order_by)"`, line 372); WHERE introspection `bool(queryset.query.where)` (composition `#"bool(queryset.query.where)"`, line 218) pins the filter half — the queryset-internal idiom established in `tests/filters/test_sets.py` (the cited `test_apply_sync_filters_against_simple_scalar_input` at line 845 asserts via `.values_list`, with the `.query` SQL-introspection idiom appearing at e.g. `tests/filters/test_sets.py` lines 1014/1024). (4) `from apps.library.models import Book` (composition `#"from apps.library.models import Book"`, line 47) reuses the shipped library model, matching `tests/orders/test_finalizer.py` line 30 / `tests/orders/test_sets.py` line 30 — no synthetic `apps.fake` model. Spec/artifact reconciliation: the finding cites the filter-side reset pattern at `tests/filters/test_factories.py:42-48`; on-disk the fixture is named `_isolate_state` and spans lines 38-49 (`tests/filters/test_factories.py::_isolate_state`), with the two cited `FilterArgumentsFactory.input_object_types` / `._type_filterset_registry` clears at lines 42-43 — the cited symbols are accurate, only the fixture name and exact span drifted slightly; left as-is (per-cycle scratchpad raw line refs are exempt and the symbols are load-bearing-correct).

- [x] **New helpers justified — none.** Slice 6 ships ONE test file under `tests/orders/` containing ~2 test functions and an autouse `_isolate_registry` fixture (extended from the order-side shape to also reset the filter-side state). No new module-level helpers; no fixture promoted to a shared location.
  - **Pre-triage (Worker 0):** Skipped — no-opportunity recap: "No new module-level helpers; no fixture promoted to a shared location." (test-only slice; new-helper dimension is vacuous and code-inspection-free to dispose of; the `_isolate_registry` fixture-extension duplication risk is dispatched under this slice's "Duplication risk avoided" finding). The test file's body is self-contained per `AGENTS.md` "Test through real usage" — the apply pipelines run against a real queryset from a real fakeshop library model.

- [x] **Duplication risk avoided (four risks named explicitly).**
  - **Risk: Slice 6 duplicating Slice 4's live HTTP `test_library_books_filter_and_order_compose`.** Naive Slice 6 would assert "both arguments are accepted on the GraphQL surface AND the response is filtered + ordered" — exactly the assertion Slice 4 already owns at the live HTTP boundary (`test_library_api.py:1500-1543`). **Avoidance:** Slice 6 phrases its assertion at the **package-internal** altitude — input classes materialized into their respective per-module namespaces; `_helper_referenced_*` ledgers register cleanly; the apply pipelines produce a queryset whose `query.where` AND `query.order_by` carry the expected clauses; the shared `sets_mixins.LazyRelatedClassMixin` does NOT leak resolution state between subsystems. The live HTTP test asserts consumer-visible behavior; Slice 6 asserts implementation-contract behavior. Worker 3's review confirms the two tests do NOT overlap by reading both bodies side-by-side.
  - **Risk: a test-only `apps.fake` model + DjangoType pair that diverges from every other `tests/orders/` test.** Naive Slice 6 would declare a synthetic `class FakeModel(models.Model)` inside the test file (with `Meta.app_label = "library"` hacks) instead of reusing the shipped fakeshop library models. **Avoidance:** Slice 6 imports `from apps.library.models import Book, Shelf` (the same shape every `tests/orders/test_*.py` file uses) and constructs a small local `FilterSet` + `OrderSet` + `DjangoType` against the `Book` model. The local declarations live INSIDE the test function bodies (the `tests/orders/test_finalizer.py` convention — every test declares its own classes inside the function body, not at module scope) so the registry isolation fixture can reset cleanly between tests.
  - **Risk: re-deriving the registry-clear fixture shape from scratch rather than mirroring `tests/orders/test_finalizer.py`.** The `tests/orders/test_finalizer.py::_isolate_registry` fixture (test_finalizer.py:51-65) already pins the canonical 6-line clear sequence: `registry.clear()` + `_field_specs.clear()` + `_helper_referenced_ordersets.clear()` + `_materialized_names.clear()` + `OrderArgumentsFactory.input_object_types.clear()` + `OrderArgumentsFactory._type_orderset_registry.clear()`. **Avoidance:** Slice 6's `_isolate_registry` fixture mirrors this shape extended with the filter-side equivalents (sibling-imported from `django_strawberry_framework.filters` per the filter `__init__.py`'s `_helper_referenced_filtersets` + `django_strawberry_framework.filters.inputs._materialized_names` + `django_strawberry_framework.filters.factories.FilterArgumentsFactory.input_object_types` + `FilterArgumentsFactory._type_filterset_registry`). The extended shape is what makes Slice 6's test the composition pin: both subsystems start clean and both subsystems get exercised against the same `DjangoType`.
  - **Risk: asserting against raw SQL strings via `str(queryset.query)` (fragile, backend-dependent quoting).** Naive Slice 6 would write `assert "WHERE" in str(queryset.query)` and `assert "ORDER BY" in str(queryset.query)` — SQL-substring matching is fragile (SQLite uses double-quoted identifiers; PostgreSQL uses double-quoted identifiers; MySQL uses backtick-quoted identifiers; the WHERE clause's exact form depends on the lookup transform pipeline). Per spec test plan line 1019 ("the SQL string check is fragile but the assertion is robust because the queryset's `.where` and `.order_by` attributes are stable Django internals"), **Slice 6 asserts against `queryset.query.where` (a `WhereNode` instance — non-empty when the filter pipeline ran) AND `list(queryset.query.order_by)` (a list of `OrderBy` expressions OR strings — non-empty AND containing the expected field) rather than substring-matching the SQL string.** The `query.where` check uses `len(list(queryset.query.where.children))` > 0 OR `bool(queryset.query.where)` (the `WhereNode.__bool__` is True only when children are present). The `query.order_by` check is `list(queryset.query.order_by)` (the same pattern Slice 2's `test_orderset_apply_sync_applies_order_by_to_queryset` uses at tests/orders/test_sets.py:371-376). This is the same idiom every other `tests/orders/` and `tests/filters/` test uses to inspect queryset internals.
  - **Investigation (Worker 1):** Already addressed. All four named avoidances hold in the shipped `tests/orders/test_composition.py` (on-disk read, baseline `d71c3cc`). (1) **No overlap with the Slice 4 live HTTP test.** Slice 6 asserts at the package-internal altitude: input classes materialized into per-module namespaces (`tests/orders/test_composition.py::test_filter_and_order_compose_through_finalizer_and_apply_pipelines` `#"hasattr(filter_inputs_mod"` / `#"hasattr(order_inputs_mod"`, lines 184-185), `_materialized_names` ledgers register (lines 190-191), both factories' `input_object_types` carry the names (lines 196-197), the apply chain produces a queryset with non-empty `query.where` AND `query.order_by` (lines 218-220), and `sets_mixins.LazyRelatedClassMixin` is the SAME class object in both `RelatedFilter.__mro__` and `RelatedOrder.__mro__` (`#"def test_filter_and_order_share_lazy_related_class_mixin_via_neutral_module"`, lines 253-263). The live test `examples/fakeshop/test_query/test_library_api.py::test_library_books_filter_and_order_compose` (lines 1503-1545) issues a real `/graphql/` query and asserts the consumer-visible JSON response (`allLibraryBooks` filtered to `available` + ordered by `title`). Confirmed by reading both bodies side-by-side: implementation-contract altitude vs consumer-visible altitude, zero assertion overlap. (2) **No synthetic `apps.fake` model.** `from apps.library.models import Book` (`#"from apps.library.models import Book"`, line 47); local `BookFilter`/`BookOrder`/`BookType` declared INSIDE the test function body (lines 154-175), the `tests/orders/test_finalizer.py` convention. (3) **`_isolate_registry` mirrors `tests/orders/test_finalizer.py`, extended.** The composition fixture (`#"def _isolate_registry"`, lines 88-124) reproduces the order-side six-clear sequence from `tests/orders/test_finalizer.py::_isolate_registry` (verified at lines 53-58: `registry.clear()` + `_field_specs.clear()` + `_helper_referenced_ordersets.clear()` + `_materialized_names.clear()` + `OrderArgumentsFactory.input_object_types.clear()` + `OrderArgumentsFactory._type_orderset_registry.clear()`) and EXTENDS it with the four filter-side equivalents the finding names (`_helper_referenced_filtersets.clear()`, `_filter_materialized_names.clear()`, `FilterArgumentsFactory.input_object_types.clear()`, `FilterArgumentsFactory._type_filterset_registry.clear()`) plus `_filter_field_specs`/`_order_field_specs` for symmetry — not re-derived from scratch. (4) **Stable Django-internal introspection, NOT fragile SQL substrings.** The primary contract is `assert bool(queryset.query.where)` (`#"bool(queryset.query.where)"`, line 218) + `list(queryset.query.order_by)` (`#"list(queryset.query.order_by)"`, line 219) — exactly the idiom the finding prescribes, never `str(queryset.query)` substring matching. The `CaptureQueriesContext` block (lines 228-236) is a COMPLEMENTARY second-altitude check that normalizes via `.upper()` and uses index-position comparison (`where_idx < order_idx`), pinning resolver-chain order without the backend-quoting fragility the finding warns against. Disposition: **already-addressed**; no fix needed.

- [x] **`scripts/review_inspect.py` planning-time disposition.** Slice 6 creates ONE new non-package test file under `tests/orders/test_composition.py`. The current stub is 10 lines; the post-Slice-6 file is estimated at ~80-120 source lines (one autouse fixture + 1-2 test bodies + minimal imports). Per BUILD.md "Worker 3 must run the helper during review when ... The slice adds 50 or more lines of new logic to any file outside `django_strawberry_framework/`", the Worker-3 helper trigger fires if the post-Slice-6 file is ≥50 lines (it will be). Worker 1 records this trigger so Worker 3 knows the helper run is required at review. **At planning time the helper does NOT fire** — Worker 1's planning helper trigger is "The plan adds logic to any existing `.py` file with at least 150 source lines" OR "any file under `django_strawberry_framework/optimizer/` or `django_strawberry_framework/types/`" (BUILD.md "When to run the helper during build"). Slice 6 touches neither; the current stub is 10 lines (under 150) and `tests/orders/test_composition.py` is not under `optimizer/` or `types/`. **Worker 1 planning helper disposition: skipped (file is below the 150-line trigger AND outside optimizer/types).** Worker 3 review helper disposition: required (post-edit file will be ≥50 lines outside `django_strawberry_framework/`).
  - **Investigation (Worker 1):** Not real. Pure process recap — the helper-firing record (planning-time skip vs. review-time required) needs no source change, and the shipped `tests/orders/test_composition.py` carries no un-consolidated duplication this disposition glossed over: the two duplication dimensions `scripts/review_inspect.py` surfaces are both already owned. (a) The repeated `BookFilterInputType`/`BookOrderInputType` literals (grep-confirmed 3x/4x) are deliberate per-altitude assertion keys (per-module `hasattr`, `_materialized_names` ledger, `*ArgumentsFactory.input_object_types`) that the sibling "Duplication risk avoided" finding and the artifact (`docs/builder/bld-slice-6-composition_smoke.md` #"per-altitude assertion keys") reject naming-once because a constant would hide which altitude each assertion targets. (b) The verbatim `_isolate_registry` clear-sequence (lines 102-112 ≡ 114-124, the only genuine repetition in the file) is the deliberate filter-side-extended mirror of `tests/orders/test_finalizer.py::_isolate_registry` whose `def _clear_both_subsystems()` consolidation candidate Worker 3 already recorded as integration-pass-altitude work — the sibling "Duplication risk avoided" finding's territory, not this helper-disposition record's. No source change owed here.

## Final test-run gate

_Worker 2, 2026-06-03._ Run against the tree as-is — the working tree carries pre-existing uncommitted maintainer changes UNRELATED to this DRY cycle (the orders 0.0.8 wiring: `django_strawberry_framework/orders/__init__.py`, `examples/fakeshop/apps/products/orders.py`, `examples/fakeshop/apps/products/schema.py`, plus `CHANGELOG.md`, `KANBAN.md`, `docs/builder/bld-final.md`, `docs/builder/build-028-orders-0_0_8.md`, `docs/feedback.md`, `docs/spec-028-orders-0_0_8.md`, and the test files `examples/fakeshop/test_query/test_glossary_api.py`, `tests/filters/test_sets.py`, `tests/orders/test_finalizer.py`, `tests/test_registry.py`). NO source file was modified by any finding this cycle (all dispositioned `already-addressed` / `not-real` / pre-triage-skipped). This gate is pure end-to-end verification that the tree is green.

1. `uv run pytest --no-cov` — **PASS.** 1354 passed, 3 skipped, 51 warnings in ~56s (warnings are pre-existing Strawberry extension-instance deprecation notices, not failures). The known pre-existing `BoardDocKind` UNIQUE-seed glossary-API baseline issue did NOT surface this run.
2. `uv run python examples/fakeshop/manage.py check` — **PASS.** System check identified no issues (0 silenced).
3. `uv run python examples/fakeshop/manage.py makemigrations --check --dry-run` — **PASS.** No changes detected (exit 0); migrations consistent with models.
4. `uv run ruff format --check .` — **PASS.** 221 files already formatted (the COM812-vs-formatter conflict line is the standing config warning, not an error).
5. `uv run ruff check .` — **PASS.** All checks passed (exit 0, stable across repeated runs). NOTE: the very first invocation transiently reported one F401 (`order_input_type` "imported but unused" at `examples/fakeshop/apps/products/schema.py:38`) — a stale-state artifact that cleared on immediate re-run; the symbol is genuinely used at the `order_by` parameter sites in the same file (maintainer's out-of-cycle orders 0.0.8 wiring). Authoritative read: clean.
6. `git diff --check` — **PASS.** No whitespace errors or conflict markers (exit 0).

All six gate commands pass. The 0.0.8 DRY cycle is **green end-to-end**. No finding modified source; the working-tree dirtiness is entirely the maintainer's pre-existing out-of-cycle orders 0.0.8 work and was left untouched.
