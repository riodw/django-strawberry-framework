# DRY consolidation plan: 0.0.9

Source: `docs/builder/`
Generated: 2026-06-09
Workflow: `docs/dry/DRY.md`
Pre-flight: passed with conditions. A prior `dry-0_0_9.md` (spec-029/030 cycle, failing final gate) was deleted by the maintainer on instruction; path freed. Working tree dirty with spec-031 in-progress edits (maintainer's concurrent work, out-of-scope per AGENTS.md); untracked `bld-slice-*.md` builder artifacts are the DRY target. Export script ran cleanly; path was free. Source-cycle closure assumed from maintainer invocation. Per-finding baseline captured via `git stash create` before each Worker 1 dispatch.

One finding at a time. Worker 0 dispatches > Worker 1 triages > Worker 2 implements > Worker 1 verifies > Worker 0 ticks.

## Findings

## Build: Slice 1 — `Meta.globalid_strategy` net-new key + `RELAY_GLOBALID_STRATEGY` setting read + precedence resolver

_Source: `docs/builder/bld-slice-1-globalid_strategy_key.md`_

**Existing patterns reused.**
- [x] `types/base.py::_validate_connection` (base.py:145-188) is the verbatim structural model for the new `_validate_globalid_strategy(meta, value, relay_shaped)`: same `None`-short-circuit-returns-`None`, same `relay_shaped` gate with the same remediation-message style (`"...requires a Relay-Node-shaped type; add `relay.Node` to `Meta.interfaces`..."`, base.py:183-187), same return-the-normalized-value contract. Spec Decision 6 names this precedent explicitly.
  - **Investigation (Worker 1):** Already addressed. `_validate_globalid_strategy` (base.py:197-254) was implemented as part of Slice 1, explicitly structurally modeled on `_validate_connection`: same `None`-short-circuit (`value is None → return None`), same `relay_shaped` gate with identical remediation-message style (base.py:249-253 mirrors base.py:181-185), same return-the-normalized-value contract. The companion `_validate_globalid_callable` helper (base.py:257-278) extracts the arity/sync-ness logic so it is not duplicated across the `Meta` and setting paths. The `source` discriminator (`source="meta"` / `source="setting"`) delivers source-specific error text as specified by Decisions 6/7. The validator is wired into `_validate_meta` (base.py:864-868) receiving the already-computed `relay_shaped` bool (base.py:860), mirroring how `_validate_connection` receives it. All confirmed by the Slice 1 build report and Worker 3 review in the source artifact (`bld-slice-1-globalid_strategy_key.md`).
- [x] `types/base.py::_is_relay_shaped(cls, interfaces)` (base.py:349-360) is reused unchanged as the single-source Relay-shape predicate; the slice threads the already-computed `relay_shaped` bool (base.py:860) into the new validator exactly as `_validate_connection` receives it (base.py:863) — NOT recomputed.
  - **Investigation (Worker 1):** Already addressed. `_is_relay_shaped` exists at base.py:349-360 and is unchanged. `_validate_meta` computes `relay_shaped = _is_relay_shaped(cls, interfaces)` at base.py:860 and threads it into `_validate_globalid_strategy` at base.py:864-868, exactly mirroring how it passes `relay_shaped` to `_validate_connection` at base.py:863. The predicate is not recomputed. Confirmed by the build report and Worker 3 review in the source artifact.
- [x] The `connection` / `filterset_class` / `orderset_class` definition-slot pattern (base.py:484-487 → definition.py:122-125) is reused verbatim for the new `globalid_strategy` slot: validate in `_validate_meta`, bundle on `_ValidatedMeta`, pass `globalid_strategy=validated.globalid_strategy` at the `DjangoTypeDefinition(...)` call (base.py:466-488), declare a defaulted dataclass field on `DjangoTypeDefinition`.
  - **Investigation (Worker 1):** Already addressed. The full slot pattern is in place: `_ValidatedMeta` carries `globalid_strategy: str | Callable[..., str] | None` (base.py:774); `_validate_meta` validates and assigns it (base.py:864-868, base.py:900); `DjangoTypeDefinition` has `globalid_strategy: str | Callable[..., str] | None = None` at definition.py:125; the `__init_subclass__` call passes `globalid_strategy=validated.globalid_strategy` at base.py:487, mirroring the identical `connection=validated.connection` keyword at base.py:486. The cited line numbers in the original bullet have shifted with the implementation; corrected above.
- [x] `types/base.py`'s in-function-import cycle-dodge (the `from ..filters.sets import FilterSet` pattern at base.py:105 with its load-cycle comment at base.py:103-104, and the identical `OrderSet` pattern at base.py:131-133) is the reused pattern for `relay.py`'s `_resolve_globalid_strategy` reaching back into `base.py` for `_validate_globalid_strategy` (see "New helpers justified" — the inverse direction, same justification).
  - **Investigation (Worker 1):** Already addressed. `_resolve_globalid_strategy` (relay.py:324-372) uses in-function imports at relay.py:355-359 (`from ..conf import settings as conf_settings` and `from .base import DEFAULT_GLOBALID_STRATEGY, _validate_globalid_strategy`) with the explicit cycle-dodge comment (relay.py:349-354) citing the same `base.py FilterSet / OrderSet` precedent. The in-function import pattern at base.py:103-105 and base.py:131-133 is structurally identical to what relay.py now applies. The cited line numbers in the original bullet have shifted; corrected above.
- [x] `conf.py::settings` attribute access (conf.py:133-156) is reused as the setting read; the precedence helper reads it defensively (`getattr(conf.settings, "RELAY_GLOBALID_STRATEGY", None)`) so a missing key → `None` → fall through to the package default, honoring the "absent → package default" wording in spec line 87. `tests/filters/test_inputs.py` (test_inputs.py:890-988) demonstrates the `settings.DJANGO_STRAWBERRY_FRAMEWORK = {...}` test-override pattern the precedence test reuses.
  - **Investigation (Worker 1):** Already addressed. `_resolve_globalid_strategy` reads the setting defensively at relay.py:364 via `getattr(conf_settings, "RELAY_GLOBALID_STRATEGY", None)`, exactly as the finding prescribes. `conf.Settings.__getattr__` (conf.py:133-156) converts `KeyError` to `AttributeError`, so a missing key yields `None` and falls through to the `"model"` default. The precedence test (`test_resolve_globalid_strategy_precedence`, test_base.py:551-594) uses the pytest-django `settings` fixture in the same override-pattern the finding cites, confirming all three tiers and the unknown-setting failure mode.
- [x] `tests/types/test_base.py` autouse `_isolate_registry` (test_base.py:55-60) and the `connection` test bodies (test_base.py:307-411) are the reused fixture + assertion-shape model for the new tests.
  - **Investigation (Worker 1):** Already addressed. The autouse `_isolate_registry` fixture is present and unchanged at test_base.py:55-60. Ten new `spec-031 Slice 1` tests were added at test_base.py:419-594, following the exact fixture + assertion shape of the `connection` test block. The `_isolate_registry` autouse applies to all of them. The cited `connection` test line range has shifted to 307-411 with the additions; corrected above.

**New helpers justified.**
- [x] `types/base.py::_validate_globalid_strategy(meta, value, relay_shaped, *, source=...)` — single responsibility: validate ONE `globalid_strategy`-shaped value (a `Meta` value OR a setting value) and return the normalized form. It is the ONE validator both call sites share (the `Meta` path via `_validate_meta`, the setting path via `_resolve_globalid_strategy`), per spec Decisions 6/7's "one validator, two sources, source-specific error text" rule. Source-specific error text is achieved by a `source` discriminator (default the `Meta` framing naming the type via `meta.model.__name__`; the setting framing naming `RELAY_GLOBALID_STRATEGY`). The callable arity/sync-ness check (`inspect.signature` must accept the four positional params `(type_cls, model, root, info)`; `inspect.iscoroutinefunction` must be `False`) lives in this one helper so it is not duplicated across the `Meta` and setting paths. Worker 2 has discretion on the exact `source` parameter spelling (see Implementation discretion items).
  - **Investigation (Worker 1):** Already addressed. `_validate_globalid_strategy(meta, value, relay_shaped, *, source="meta")` exists at base.py:197-254 with the exact `source` discriminator shape. Its companion `_validate_globalid_callable(subject, value)` at base.py:257-277 holds the arity/sync logic once, called by `_validate_globalid_strategy` for the callable branch. The `_GLOBALID_CALLABLE_PARAMS` tuple at base.py:194 single-sources the four-param contract for both the `.bind(...)` check and the error text. Both the `Meta` path (`_validate_meta`, base.py:864-868) and the setting path (`_resolve_globalid_strategy`, relay.py:366-371) share this one validator.
- [x] `types/relay.py::_resolve_globalid_strategy(definition)` — single responsibility: apply the three-tier precedence (`definition.globalid_strategy` → `conf.settings.RELAY_GLOBALID_STRATEGY` → `"model"`) and return the resolved raw strategy (string or callable), validating the setting branch through `_validate_globalid_strategy`. It serves the Slice-2 `install_globalid_typename_resolver` call site (relay.py TODO at relay.py:319-323). Home is `types/relay.py` per the pinned comment (relay.py:319-321). The `relay_shaped` argument the validator expects is `True` for the setting branch (the resolver is only ever called for a Relay-Node-shaped type at finalization — the gate already passed at type creation), so the setting-path call passes `relay_shaped=True` and `source=` the setting framing.
  - **Investigation (Worker 1):** Already addressed. `_resolve_globalid_strategy(definition)` exists at relay.py:324-372 with the three-tier precedence implemented exactly: `definition.globalid_strategy` (Meta override, returned directly if not `None`) → `getattr(conf_settings, "RELAY_GLOBALID_STRATEGY", None)` (validated via `_validate_globalid_strategy(..., relay_shaped=True, source="setting")` if not `None`) → `DEFAULT_GLOBALID_STRATEGY` (`"model"`). The setting path passes `relay_shaped=True` and `meta=None` as specified. The original bullet's relay.py line references for the TODO anchor have shifted; corrected above.

**Duplication risk avoided.**
- [x] The callable arity/sync validation could be naively written twice (once for `Meta`, once for the setting). The plan forbids that: the single `_validate_globalid_strategy` is the only place the `inspect.signature`/`iscoroutinefunction` logic lives; `_resolve_globalid_strategy` calls it rather than re-implementing.
  - **Investigation (Worker 1):** Already addressed. The arity/sync logic lives exclusively in `_validate_globalid_callable` (base.py:257-277), called by `_validate_globalid_strategy` (base.py:238-240). `_resolve_globalid_strategy` calls `_validate_globalid_strategy` for the setting branch (relay.py:366-371) and does not re-implement any `inspect` logic. No duplication exists.
- [x] The valid-strategy string set `{"model", "type", "type+model"}` could become a parallel literal in the validator, the resolver, and later the encoder/decoder. This slice pins it as ONE named module-level constant in `types/base.py` (e.g. `STRING_GLOBALID_STRATEGIES = frozenset({"model", "type", "type+model"})`), exported for reuse so Slice 2's encoder and Slice 3's decoder reference the same source of truth rather than re-typing the set (build-plan DRY watch point, build-031 plan "DRY-first rule"). Worker 2 has discretion on the constant's exact name; the requirement is that it is named once and not duplicated.
  - **Investigation (Worker 1):** Already addressed. `STRING_GLOBALID_STRATEGIES = frozenset({"model", "type", "type+model"})` is defined at base.py:85. It is used by `_validate_globalid_strategy` (base.py:232, base.py:235, base.py:243) and is positioned for Slice 2/3 reuse. The set literal is not re-typed anywhere else in the current source.
- [x] The Relay-shape predicate is NOT recomputed: the slice reuses the `relay_shaped` bool `_validate_meta` already computes (base.py:860), exactly as `_validate_connection` does (base.py:863).
  - **Investigation (Worker 1):** Already addressed. `relay_shaped = _is_relay_shaped(cls, interfaces)` is computed once at base.py:860, passed to `_validate_connection` at base.py:863, and to `_validate_globalid_strategy` at base.py:866. Neither validator re-calls `_is_relay_shaped`; the bool is threaded in as prescribed. The original bullet's line numbers have shifted; corrected above.
- [x] The `"model"` default literal recurs in the resolver and (Slice 2) the encoder default. To keep one source of truth, plan a named default constant (e.g. `DEFAULT_GLOBALID_STRATEGY = "model"`) alongside the string-set constant in `types/base.py`. Worker 2 discretion on the name; the requirement is single-sourcing.
  - **Investigation (Worker 1):** Already addressed. `DEFAULT_GLOBALID_STRATEGY = "model"` is defined at base.py:86. It is imported in-function by `_resolve_globalid_strategy` (relay.py:357) and returned as the package default at relay.py:372. The bare `"model"` string literal does not appear at any other load-bearing site in the current source; the constant is positioned for Slice 2's encoder default per the plan.

---

## Build: Slice 2 — the encode seam (strategy-parameterized `resolve_typename` + four encoders + default flip + strategy-aware filter)

_Source: `docs/builder/bld-slice-2-encode_seam.md`_

**Existing patterns reused (cite file:line — pin-at-write-time hints).**

- [x] `types/relay.py::install_relay_node_resolvers` (relay.py:577-603) is the
  structural precedent for `install_globalid_typename_resolver`: the
  `existing.__func__ is relay.Node.<attr>.__func__` MRO-aware identity test
  (relay.py:598-603) is reused verbatim for `resolve_typename` against
  `relay.Node.resolve_typename.__func__`. `resolve_typename` is a `@classmethod`
  on `relay.Node` (strawberry `relay/types.py:493`), so it has `__func__` exactly
  like the four `resolve_*` defaults. The install path also reuses the
  `setattr(type_cls, attr, classmethod(default))` shape (relay.py:603).
  - **Investigation (Worker 1):** Already addressed. `install_relay_node_resolvers` (relay.py:861-887) is in place and unchanged. The `__func__` identity test idiom is reused — but rather than inlining the four-line idiom, Worker 2 extracted a named `_consumer_overrode_resolve_typename(type_cls)` predicate (relay.py:466-479) that performs the identical `existing.__func__ is relay.Node.resolve_typename.__func__` check for the `resolve_typename` case. `install_globalid_typename_resolver` (relay.py:482-536) calls `_consumer_overrode_resolve_typename` at step 1 and installs the framework closure via `setattr(type_cls, "resolve_typename", classmethod(resolve_typename))` (relay.py:557), mirroring `install_relay_node_resolvers`'s `setattr(type_cls, attr, classmethod(default_impl))` shape (relay.py:887). The structural precedent was correctly applied; the `_consumer_overrode_resolve_typename` extraction (a Worker-2 discretion item per the plan) makes the shared idiom even cleaner than a verbatim copy. Line references in the finding have drifted (relay.py grew with the Slice-2 additions); the canonical current locations are relay.py:861-887 for `install_relay_node_resolvers` and relay.py:466-479 + relay.py:482-536 for the new code.
- [x] `types/relay.py::_resolve_globalid_strategy(definition)` (relay.py:323-371,
  Slice 1) is REUSED unchanged to resolve the raw strategy in the no-override
  branch — `install_globalid_typename_resolver` calls it, does not re-implement
  the precedence.
  - **Investigation (Worker 1):** Already addressed. `_resolve_globalid_strategy(definition)` (relay.py:324-372) is present and its body is the Slice-1 implementation — not altered by Slice 2. `install_globalid_typename_resolver` calls it at relay.py:531 (`strategy = _resolve_globalid_strategy(definition)`) in the no-override branch (step 2) and does not re-implement any three-tier precedence logic. The finding is correctly described.
- [x] `types/relay.py::_RELAY_RESOLVER_DEFAULTS` (relay.py:566-574): the slice
  deliberately does NOT add `resolve_typename` to this table (spec Decision 10 —
  the table maps a name→one static default; the typename default is
  strategy-parameterized per type). The "single source of truth for resolver
  names" comment (relay.py:566-568) stays accurate.
  - **Investigation (Worker 1):** Already addressed. `_RELAY_RESOLVER_DEFAULTS` (relay.py:853-858 after Slice-2 additions) contains exactly the original four entries (`resolve_id`, `resolve_id_attr`, `resolve_node`, `resolve_nodes`) and `resolve_typename` was NOT added — the comment at relay.py:850-852 explicitly records this and states the invariant. The docstring of `install_relay_node_resolvers` (relay.py:862-880) also notes that `resolve_typename` is "deliberately NOT added to `_RELAY_RESOLVER_DEFAULTS`". The finding's intent is satisfied; line numbers have shifted but the substance is unchanged.
- [x] `types/finalizer.py::_audit_primary_ambiguity` (finalizer.py:117-142) is the
  structural precedent for the model-label-routing audit: iterate
  `registry.models_with_multiple_types()` (finalizer.py:136), collect offenders,
  sort deterministically by `model.__name__` (finalizer.py:141), raise one
  `ConfigurationError` built by a `_format_*_error` helper (finalizer.py:142). The
  audit's home is the Phase-2.5 TODO at finalizer.py:145-155.
  - **Investigation (Worker 1):** Already addressed. `_audit_primary_ambiguity` (finalizer.py:120-145) is in place and unchanged as the precedent. `_audit_model_label_routing` (finalizer.py:177-211) was implemented mirroring its shape exactly: iterates `registry.models_with_multiple_types()` (finalizer.py:200), collects offenders as a list of `(model, emitter_type, primary_strategy)` tuples, sorts by `model.__name__` (finalizer.py:210), and raises `ConfigurationError(_format_model_label_routing_error(offenders))` (finalizer.py:211) — parallel to `_audit_primary_ambiguity`'s `ConfigurationError(_format_ambiguity_error(offenders))` (finalizer.py:145). The `_first_model_label_emitter` helper (finalizer.py:214-226) single-sources the per-type strategy read inside the emitter scan. The finding's TODO anchor has been replaced by the shipped implementation; the line numbers have shifted throughout finalizer.py.
- [x] `types/finalizer.py` Phase 2.5 Relay loop (finalizer.py:256-274): the install
  call slots in after `install_relay_node_resolvers` at the TODO
  (finalizer.py:269-274), inside the existing
  `if implements_relay_node(type_cls):` block (finalizer.py:266-268).
  - **Investigation (Worker 1):** Already addressed. The Phase-2.5 Relay loop (finalizer.py:327-340) contains the `if implements_relay_node(type_cls):` block (finalizer.py:337) with `install_relay_node_resolvers(type_cls)` (finalizer.py:339) immediately followed by `install_globalid_typename_resolver(type_cls, definition)` (finalizer.py:340) — exactly the prescribed slotting. The TODO at finalizer.py:269-274 has been replaced. `install_globalid_typename_resolver` is imported at finalizer.py:65 in the existing `from .relay import (...)` block alongside `_accepts_model_label_decode`, `_emits_model_label`, and the pre-existing relay helpers. `definition` is in scope from the loop binding `for type_cls, definition in registry.iter_definitions()` (finalizer.py:327).
- [x] `types/definition.py` defaulted-field block (definition.py:104-123): the new
  `effective_globalid_strategy` field is added at the pinned TODO
  (definition.py:118-122), immediately after the raw `globalid_strategy` slot
  (definition.py:117), mirroring how `connection` / `globalid_strategy` were added.
  Its docstring stub already exists (definition.py:78-83).
  - **Investigation (Worker 1):** Already addressed. `effective_globalid_strategy: str | None = None` is present at definition.py:132, immediately after `globalid_strategy: str | Callable[..., str] | None = None` at definition.py:125 and before `finalized: bool = False` at definition.py:133. The docstring section (definition.py:78-91) fully describes the field — the five classification strings, the re-entrancy-sentinel role, and the `None` meaning — replacing what was a TODO stub. The original TODO (definition.py:118-122) and matching docstring stub (definition.py:78-83) have been replaced. Layout matches the prescribed pattern.
- [x] `filters/base.py::_decode_and_validate_global_id` (filters-base.py:207-229) and
  `_expected_global_id_type_name` (filters-base.py:169-204): the existing
  owner/target resolution (own-PK branch → owner definition; relation branch →
  `owner.related_target_for(head)` target definition; finalizer-base.py:191-204)
  is reused — the refactor swaps the final `.graphql_type_name` read for a
  strategy-aware acceptance check keyed on the resolved definition's
  `effective_globalid_strategy`. The existing `expected is None` node-id-only
  fallback (filters-base.py:224) is the precedent for the `callable`/`custom`
  fallback.
  - **Investigation (Worker 1):** Already addressed. `_expected_global_id_type_name` was renamed to `_target_definition_for(filter_instance)` (filters/base.py:179-217), which now returns a `DjangoTypeDefinition | None` rather than a string. The own-PK-vs-relation resolution logic (filters/base.py:204-216) is kept single-sited here, mirroring the prescribed shape. `_accepted_globalid_type_names(definition)` (filters/base.py:220-247) owns the per-strategy accepted-set logic, returning `None` for `callable`/`custom`/absent strategy — the node-id-only fallback. `_decode_and_validate_global_id` (filters/base.py:250-280) calls both helpers and branches on the accepted set; the `accepted is not None and decoded.type_name not in accepted` guard (filters/base.py:274) mirrors the old `expected is None` fallback shape. The finding's approach was fully realized via the filter-rename path (Worker-2 discretion item).
- [x] `definition.graphql_type_name` property (definition.py:136-147) is the single
  source for the `type`/`type+model` type-name slot, reused by the encoder and the
  filter's `type`-shape check.
  - **Investigation (Worker 1):** Already addressed. `graphql_type_name` (definition.py:146-157) is the single property returning `self.name if self.name is not None else self.origin.__name__`. The encoder (`encode_typename`, relay.py:463) reads `definition.graphql_type_name` for the `type`-strategy branch. The filter's `_accepted_globalid_type_names` (filters/base.py:245) reads `definition.graphql_type_name` for strategies in `TYPE_NAME_STRATEGIES`. No inline copy of the name derivation exists anywhere in the Slice-2 additions. The property's location has shifted (line numbers in the finding cite pre-Slice-1 positions) but the substance is unchanged.
- [x] `definition.model._meta.label_lower` is the Django-canonical model label
  (`"products.item"`) — the `model`/`type+model` slot and the filter's
  `model`-shape check.
  - **Investigation (Worker 1):** Already addressed. `definition.model._meta.label_lower` is read by `encode_typename` (relay.py:461) for `model`/`type+model` strategies and by `_accepted_globalid_type_names` (filters/base.py:244) for the `model`-label-acceptance set. No `f"{app}.{model}"` hand-rolled formatting appears anywhere in the Slice-2 additions. The single access point is `_meta.label_lower` as prescribed, consistent across the encoder and the filter.

**New helpers/constants justified (single responsibility each).**

- [x] `types/relay.py::encode_typename(definition, strategy, root, info)` — single
  responsibility: compute the type-name slot string for ONE resolved strategy.
  `model`/`type+model` → `definition.model._meta.label_lower`; `type` →
  `definition.graphql_type_name`; `callable` → the consumer callable's return
  (validated non-empty `str`). It serves the installed `resolve_typename` closure.
  (Note: for `type`, the framework installs NOTHING — Strawberry's default returns
  `info.path.typename`, byte-identical to `graphql_type_name` for the live
  schema — so `encode_typename` is only invoked from the framework closure for
  `model`/`type+model`/`callable`. Whether `encode_typename` is one dispatch
  function or the closures inline the per-strategy slot read is a Worker 2
  discretion item, provided the model-label / graphql-name reads are not
  duplicated across the install branches.)
  - **Investigation (Worker 1):** Already addressed. `encode_typename(definition, strategy, type_cls, root, info)` (relay.py:425-463) is the single dispatch function. Its three branches each read the right accessor exactly once: callable → `strategy(type_cls, definition.model, root, info)` with the non-empty-`str` guard (relay.py:451-459); `model`/`type+model` → `definition.model._meta.label_lower` (relay.py:461); `type` → `definition.graphql_type_name` (relay.py:463). The closure `_install_typename_closure` (relay.py:539-557) calls `encode_typename` for all three install-worthy strategies, so model-label and graphql-name reads are single-sited inside `encode_typename`. The actual signature has `type_cls` as a third positional argument (the bound `cls` from the classmethod), which is a deliberate discretion choice to pass it to the callable as the documented `(type_cls, model, root, info)` contract.
- [x] `types/relay.py::install_globalid_typename_resolver(type_cls, definition)` —
  single responsibility: classify the type's effective strategy, install the
  framework `resolve_typename` closure (or nothing), and record
  `definition.effective_globalid_strategy`. Re-entrant-safe.
  - **Investigation (Worker 1):** Already addressed. `install_globalid_typename_resolver(type_cls, definition)` (relay.py:482-536) implements the four ordered steps: step-0 re-entrancy guard (`if definition.effective_globalid_strategy is not None: return`, relay.py:517); step-1 override detection via `_consumer_overrode_resolve_typename` with both-declared conflict check keyed on `definition.globalid_strategy is not None` (relay.py:520-529); step-2 no-override resolve via `_resolve_globalid_strategy` with install-or-skip logic (relay.py:531-535); step-3 record `definition.effective_globalid_strategy = classification` (relay.py:536). The recording doubles as the step-0 sentinel as prescribed. Single responsibility confirmed — no ancillary logic is mixed in.
- [x] `types/relay.py::MODEL_LABEL_STRATEGIES` + `_emits_model_label` /
  `_accepts_model_label_decode` predicates — the ONE source of truth for the
  strategy→payload-shape mapping (see "Pinned single source of truth" above),
  reused by the encoder, the audit, the filter, and (Slice 3) the decoder.
  - **Investigation (Worker 1):** Already addressed. `MODEL_LABEL_STRATEGIES = frozenset({"model", "type+model"})` (relay.py:385), `_emits_model_label` (relay.py:389-396), and `_accepts_model_label_decode` (relay.py:399-409) are defined at the prescribed location. The module-top comment (relay.py:375-384) names all four consumers. Notably, Slice 2 also added a sibling `TYPE_NAME_STRATEGIES = frozenset({"type", "type+model"})` (relay.py:386) and `_accepts_type_name_decode` (relay.py:412-422) to complete the strategy→shape coverage — this was the forward-looking DRY note from Worker 3/Worker 1 Slice-3 planning (the bare tuple `("type", "type+model")` that would have become a parallel literal in Slice 3's decoder). Both frozensets are imported module-top in `filters/base.py` (filters/base.py:44) and the identity `filters.base.MODEL_LABEL_STRATEGIES is relay.MODEL_LABEL_STRATEGIES` is confirmed by Worker 3's runtime check. No parallel literals exist.
- [x] `types/finalizer.py::_audit_model_label_routing()` + a
  `_format_model_label_routing_error(...)` message builder — single
  responsibility: enforce the model-label-routing invariant for multi-type models.
  Modeled on `_audit_primary_ambiguity` + `_format_ambiguity_error`.
  - **Investigation (Worker 1):** Already addressed. `_format_model_label_routing_error(offenders)` (finalizer.py:148-174) and `_audit_model_label_routing()` (finalizer.py:177-211) are both present. `_format_model_label_routing_error` returns a string (not a bare inline `raise`) and its caller `_audit_model_label_routing` raises `ConfigurationError(_format_model_label_routing_error(offenders))` (finalizer.py:211) — exactly mirroring the `_format_ambiguity_error` / `_audit_primary_ambiguity` pattern. `_first_model_label_emitter` (finalizer.py:214-226) extracts the per-type strategy scan to keep the audit body readable. The single-responsibility is maintained; the placement at finalizer.py:346 (after Relay loop, before `_bind_filtersets`) is load-bearing-correct per spec Decision 10.

**Duplication risk avoided.**

- [x] The strategy→payload-shape membership could be naively re-typed as three+
  parallel literal sets (`{"model", "type+model"}` in the encoder, the audit, the
  filter, and Slice 3's decoder). The plan forbids that: `MODEL_LABEL_STRATEGIES`
  in `types/relay.py` is the one frozenset all four sites reference.
  - **Investigation (Worker 1):** Already addressed. `MODEL_LABEL_STRATEGIES` is defined once (relay.py:385) and consumed by `encode_typename` (relay.py:460), `_emits_model_label`/`_accepts_model_label_decode` (relay.py:396/409, which the finalizer audit reads), `_accepted_globalid_type_names` (filters/base.py:243 via the module-top import at filters/base.py:44), and (Slice 3) `decode_global_id` (relay.py:661 via `_accepts_model_label_decode`). No inline `{"model", "type+model"}` literal appears in any Slice-2 addition. The plan's central DRY watch point is satisfied.
- [x] The model-label string (`definition.model._meta.label_lower`) and the
  graphql-name string (`definition.graphql_type_name`) recur in the encoder AND
  the filter's per-strategy acceptance check. The plan keeps each read sited on
  the definition (the encoder computes the emit slot; the filter computes the
  expected-accept slot[s]) — both read the SAME two definition accessors, not
  re-derived label logic. No hand-rolled `f"{app}.{model}"` formatting anywhere
  (use `_meta.label_lower`).
  - **Investigation (Worker 1):** Already addressed. In the encoder, `encode_typename` reads `definition.model._meta.label_lower` (relay.py:461) and `definition.graphql_type_name` (relay.py:463). In the filter, `_accepted_globalid_type_names` reads `definition.model._meta.label_lower` (filters/base.py:244) and `definition.graphql_type_name` (filters/base.py:245). Both use the definition accessors; no hand-rolled `f"{app}.{model}"` formatting exists anywhere. The two reads (encoder vs filter) are independent lookup calls against the same definition object — no duplication of derivation logic, just two readers of the same source property.
- [x] The `__func__` identity test is NOT copied: `install_globalid_typename_resolver`
  uses the same idiom as `install_relay_node_resolvers` (relay.py:598-603). Worker 2
  should consider whether a tiny shared `_consumer_overrode(type_cls, attr)`
  helper is worth extracting vs. inlining the four-line idiom once more; Worker 1's
  assessment: a one-call-site inline is acceptable (the existing loop in
  `install_relay_node_resolvers` is structurally different — it iterates a table),
  but if Worker 2 sees a clean two-line predicate it may extract it. Discretion item.
  - **Investigation (Worker 1):** Already addressed. Worker 2 chose the extraction path: `_consumer_overrode_resolve_typename(type_cls)` (relay.py:466-479) is the dedicated predicate for the `resolve_typename` case. It is NOT a generic `_consumer_overrode(type_cls, attr)` helper (which would have altered `install_relay_node_resolvers` too) — it is a specific named predicate for the `resolve_typename` `__func__` test, called once by `install_globalid_typename_resolver`. `install_relay_node_resolvers` keeps its inline idiom unchanged (relay.py:882-886), which is structurally different (iterates a table). The four-line `__func__` idiom is not duplicated — it appears in `_consumer_overrode_resolve_typename` (relay.py:477-479) and in the `install_relay_node_resolvers` loop body (relay.py:882-886), but these are two structurally distinct use-sites (single override-detection call vs per-item iteration). The finding's "not copied" intent is satisfied.
- [x] The `ConfigurationError` message-builder pattern (`_format_*_error` returning a
  string, raised by the caller) is reused for the routing audit — not a bespoke
  inline `raise ConfigurationError(f"...")` that would diverge in style from the
  finalizer's other audit messages.
  - **Investigation (Worker 1):** Already addressed. `_format_model_label_routing_error(offenders)` (finalizer.py:148-174) returns a string; `_audit_model_label_routing` raises `ConfigurationError(_format_model_label_routing_error(offenders))` (finalizer.py:211). This exactly follows the `_format_ambiguity_error` / `_audit_primary_ambiguity` pattern (finalizer.py:98-117 / finalizer.py:145) and every other `_format_*` / audit pair in the finalizer. No bespoke inline `raise ConfigurationError(f"...")` was introduced. The message-builder convention is uniform across the finalizer module.

---

## Build: Slice 3 — the decode seam (`decode_global_id` resolve-then-enforce + `definition_for_graphql_name` + symmetry + transitional `type+model`)

_Source: `docs/builder/bld-slice-3-decode_seam.md`_

**Existing patterns reused (cite file:line — pin-at-write-time hints).**

- [x] **Decode reads the recorded `effective_globalid_strategy`, never re-resolves.** The
  durable contract is the finalization-stamped `definition.effective_globalid_strategy`
  field (`definition.py:132`, recorded by `install_globalid_typename_resolver` at
  relay.py:517). Decode Step-2 reads it directly — it does NOT call
  `_resolve_globalid_strategy` again (which re-runs the precedence + setting read and
  cannot tell a preserved consumer override (`custom`) from the framework closure). This
  is the spec's explicit DRY rule (spec line 35 Revision-2 note + Revision-5 cleanup,
  Decision 8 Step 2). Carried in worker-1 memory.
  - **Investigation (Worker 1):** Already addressed. `decode_global_id` (relay.py:560-673) reads `definition.effective_globalid_strategy` directly at relay.py:655 (`strategy = definition.effective_globalid_strategy if definition is not None else None`) and never calls `_resolve_globalid_strategy`. The Step-2 predicate dispatch (`_accepts_model_label_decode(strategy)` / `_accepts_type_name_decode(strategy)`) reads the recorded classification string; no precedence re-resolution occurs. The `custom` case is rejected at Step-2 (neither predicate includes it) rather than being misclassified. This satisfies the spec's explicit DRY rule (Decision 8 Step 2). Confirmed by the Slice-3 build report and Worker 3 + Worker 1 final verification in the source artifact.
- [x] **`MODEL_LABEL_STRATEGIES` predicates** (`relay.py:380-403`). Step-2's model-label
  acceptance is exactly `_accepts_model_label_decode(effective_strategy)` (relay.py:393)
  — `model` / `type+model` accept a model-label payload. Reused, not re-typed. (Slice 2
  already reserved this for the decoder; Worker 3 + Worker 1 confirmed it is the same
  frozenset object across module boundaries.)
  - **Investigation (Worker 1):** Already addressed. `MODEL_LABEL_STRATEGIES = frozenset({"model", "type+model"})` (relay.py:385) and `_accepts_model_label_decode` (relay.py:399-409) are in place from Slice 2 and reused unchanged by the Slice-3 decoder. `decode_global_id`'s Step-2 model-label branch calls `_accepts_model_label_decode(strategy)` (relay.py:662) — not an inlined membership test. The finding's line-number hint has drifted slightly (the predicate block now spans relay.py:385-422 after the Slice-3 `TYPE_NAME_STRATEGIES` / `_accepts_type_name_decode` additions), but the substance is correct and confirmed by the Worker 3 DRY review in the source artifact.
- [x] **`registry.get(model)`** (registry.py:190-209) is the primary-routing resolver the
  model-label branch reuses verbatim — it returns the declared primary
  (`_primaries[model]`, honoring `Meta.primary`), or the lone registered type, or
  `None`. This is the same resolver relation targets use
  (`DjangoTypeDefinition.related_target_for` → `registry.get`), so a decoded ID and a
  traversed relation land on the same type (spec Decision 8 justification).
  - **Investigation (Worker 1):** Already addressed. `registry.get(model)` (registry.py:190-209, unchanged) is called in `decode_global_id` at relay.py:644 (`target_type = registry.get(model)`) for the model-label branch, after `apps.get_model` resolves the app/model name. The `None` return is checked and raises `ConfigurationError` naming the resolution attempt (relay.py:645-649). The line numbers cited in the finding are accurate for the current source. `registry.get_definition(target_type)` is called at relay.py:650 to resolve the candidate definition once the type is established.
- [x] **`registry.iter_definitions()`** (registry.py:333-335) is the scan source for
  `definition_for_graphql_name`, mirroring the TODO pseudocode at registry.py:328 (note:
  the TODO pseudo-scans `self._definitions.items()` — `iter_definitions()` is the public
  spelling of that, which the spec sub-bullet names; use the public iterator, not the
  private dict). `registry.get_definition(type_cls)` (registry.py:315-317) reads the
  candidate definition once Step 1 resolves a `type_cls` via `registry.get`.
  - **Investigation (Worker 1):** Already addressed. `iter_definitions()` (registry.py:362-364) is the scan source used by `definition_for_graphql_name` (registry.py:347: `for type_cls, definition in self.iter_definitions()`). The TODO pseudocode that referenced `self._definitions.items()` has been replaced by the shipped method. `get_definition(type_cls)` (registry.py:315-317, unchanged) is called at relay.py:650 in the model-label branch after `registry.get` resolves the type. The line numbers in the finding have shifted slightly (the `get_definition` method is at registry.py:315-317 and `iter_definitions` is at registry.py:362-364 after the `definition_for_graphql_name` addition), but both helpers are in place and used as described.
- [x] **`implements_relay_node(type_cls)`** (relay.py:63-73) is the Relay-only-scan
  predicate `definition_for_graphql_name` applies (`registry.py` imports it in-function
  to dodge the `registry → relay → registry`-adjacent direction — see "Duplication risk
  avoided" import note). It also belt-and-suspenders pairs with Step-2's absent-`None`
  rejection (a non-Relay type's `effective_globalid_strategy` is never stamped).
  - **Investigation (Worker 1):** Already addressed. `implements_relay_node` (relay.py:64-74, `return issubclass(type_cls, relay.Node)`) is unchanged from Slice 2 and is imported in-function by `definition_for_graphql_name` at registry.py:343 (`from .types.relay import implements_relay_node`). The in-function import correctly dodges the `registry ↔ relay` module-load coupling. The Relay-only filter in the list comprehension (registry.py:348: `if implements_relay_node(type_cls) and ...`) is the gate that makes the non-Relay absent-`None` belt-and-suspenders work: non-Node types are excluded from the match list, so they never enter Step-2 as candidates. The cited line numbers (relay.py:63-73) are accurate.
- [x] **`relay.GlobalID.from_id(value)`** (strawberry `relay/types.py:113`, verified against
  the installed `0.316.0`) is the `str → GlobalID` parser. It wraps `from_base64` and
  re-raises any `ValueError` as `GlobalIDValueError` (`relay/types.py:70`, a subclass of
  `ValueError`). The `str` branch reuses this exactly as `filters/base.py:271` already
  does (`relay.GlobalID.from_id(value)`); catching `ValueError` (the superset spelling)
  suffices. There is **no** `GlobalID.from_base64` classmethod (module-level
  `from_base64` returns a raw tuple) — `from_id` is correct (spec Revision 4 P3
  correction).
  - **Investigation (Worker 1):** Already addressed. `decode_global_id` uses `relay.GlobalID.from_id(gid)` at relay.py:617 inside a `try/except ValueError` (relay.py:616-622), matching the spelling and error-catching approach described in the finding. The existing filter use at `filters/base.py:271` (`relay.GlobalID.from_id(value)`) is the identical expression — both sites consistently use `from_id`, not `from_base64`. The `except ValueError` superset is confirmed correct by the build report (Worker 3 verified `GlobalIDValueError ⊂ ValueError` against the installed strawberry). The finding's description is fully accurate.
- [x] **The `ConfigurationError`-message-builder fail-loud shape.** The spec names the
  `RelatedFilter`-style fail-loud message for an unqualified/unresolvable name. The
  decode errors follow that shape (name the resolution attempt: the offending
  `type_name` / label / strategy). One uniform `ConfigurationError` for every failure
  mode keeps the contract `032`'s arbitrary client input depends on.
  - **Investigation (Worker 1):** Already addressed. Every failure path in `decode_global_id` raises `ConfigurationError` naming the resolution attempt: the input-type gate names the unexpected type (relay.py:610-613); the parse failure quotes the malformed string (relay.py:619-621); the empty-slot check names both slots (relay.py:629-632); the unknown app/model label names the label (relay.py:640-643); the unregistered-model branch names the label (relay.py:646-649); the absent-`None` strategy branch names the candidate type-name (relay.py:657-660); the shape-enforcement failure names the `type_name`, shape, and strategy (relay.py:667-671). No `KeyError`, `ValueError`, or `AttributeError` leaks. This is precisely the `RelatedFilter`-style fail-loud contract described in the finding. `definition_for_graphql_name` likewise raises `ConfigurationError` on miss (registry.py:353-355) and ambiguity (registry.py:356-360) with the offending name named. No inline `raise ConfigurationError(f"...")` diverges from the pattern.
- [x] **Test scaffolding.** Both `tests/types/test_relay_interfaces.py` (autouse
  `registry.clear()` fixture at test_relay_interfaces.py:37-42; real fakeshop models
  `apps.products.models.{Category, Item}`; `finalize_django_types()`; the Slice-2
  `_emitted_typename`-style helpers) and `tests/test_registry.py` (autouse
  `_isolate_global_registry` at test_registry.py:34-39; `fresh_registry` fixture;
  `Category`/`Item`/`Property` models; the `register` / `primary` multi-type fixtures at
  test_registry.py:721-802) already carry the fixtures the new tests need. Decode
  round-trips build encoded IDs with `relay.to_base64(type_name, node_id)` (or
  `str(relay.GlobalID(...))`).
  - **Investigation (Worker 1):** Already addressed. Both fixture anchors are confirmed present: `_isolate_registry` autouse at test_relay_interfaces.py:38-43 (clears the global registry); `_isolate_global_registry` autouse at test_registry.py:34-39. The Slice-3 decode tests (test_relay_interfaces.py:1800-1991) and `definition_for_graphql_name` tests (test_registry.py:48-130) use `Category`/`Item` models and `finalize_django_types()` throughout. Round-trip tests use `str(relay.GlobalID(slot, node_id))` via the `_encoded_id` helper (test_relay_interfaces.py:1795-1797), never a raw payload. The finding's cited line-number hints for the `test_registry.py` multi-type fixtures have drifted (the `definition_for_graphql_name` block was added at the top of the file, pushing later tests down), but the fixtures themselves are present and used as described.

**New helpers / constants justified (single responsibility each).**

- [x] `registry.py::definition_for_graphql_name(name) -> DjangoTypeDefinition` — single
  responsibility: invert the `type`-strategy encode (which emits
  `definition.graphql_type_name`) by a unique `graphql_type_name` lookup over
  Relay-Node definitions, raising `ConfigurationError` on miss or ambiguity. It is the
  type-name half of Step 1. Keyed on `graphql_type_name` (NOT `type_cls.__name__`) so a
  `Meta.name`-renamed type round-trips (spec Decision 8 / feedback P1). Relay-Node scan
  only (a non-Node type can never be a `GlobalID` target — spec Revision 4 P1).
  - **Investigation (Worker 1):** Already addressed. `definition_for_graphql_name(self, name: str) -> DjangoTypeDefinition` is implemented at registry.py:319-360: it uses an in-function `implements_relay_node` import (registry.py:343), a list comprehension over `iter_definitions()` filtering on both `implements_relay_node(type_cls)` and `definition.graphql_type_name == name` (registry.py:345-349), returning the unique match or raising `ConfigurationError` on zero (registry.py:352-355) or two-or-more (registry.py:356-360) results. The key is `graphql_type_name`, not `type_cls.__name__`, confirmed by `test_definition_for_graphql_name_honors_meta_name` (test_registry.py:62-77). Single-responsibility is satisfied — the method does only the type-name inversion lookup.
- [x] `types/relay.py::decode_global_id(gid: relay.GlobalID | str) -> tuple[type, str]` —
  single responsibility: the input-gate + parse + resolve-then-enforce dispatch,
  returning `(target_type, node_id)` (`target_type` is `candidate.origin`, the
  `DjangoType` class; `node_id` is the parsed `gid.node_id` string). One uniform
  `ConfigurationError` for every failure.
  - **Investigation (Worker 1):** Already addressed. `decode_global_id(gid: relay.GlobalID | str) -> tuple[type, str]` is implemented at relay.py:560-673. It covers the full input-gate → parse → empty-slot → Step-1 resolution → Step-2 enforcement pipeline, returning `(target_type, node_id)` where `target_type` is `definition.origin` (relay.py:653/target_type) and `node_id` is `decoded.node_id` (relay.py:627). In-function `from ..registry import registry` (relay.py:607) handles the cycle dodge. All 21 decode tests in `tests/types/test_relay_interfaces.py` (lines 1800-1991) confirm single-responsibility — no ancillary logic mixed in. The helper is pinned by 21 permanent package tests per the source artifact.
- [x] `types/relay.py::TYPE_NAME_STRATEGIES = frozenset({"type", "type+model"})` + a
  predicate `_accepts_type_name_decode(effective_strategy) -> bool` (sibling of
  `_accepts_model_label_decode`) — single responsibility: the "which strategies accept a
  type-name payload" membership. Reused by BOTH decode Step-2 (type-name branch) AND
  `filters/base.py::_accepted_globalid_type_names` (refactoring its bare tuple at
  filters/base.py:245). This is the planned consolidation; without it the second site
  (the decoder) would make `{"type", "type+model"}` a parallel-literal defect (Worker 3
  Slice-2 DRY note + Worker 1 Slice-2 carry-forward).
  - **Investigation (Worker 1):** Already addressed. `TYPE_NAME_STRATEGIES = frozenset({"type", "type+model"})` (relay.py:386) and `_accepts_type_name_decode` (relay.py:412-422, `return effective_strategy in TYPE_NAME_STRATEGIES`) are present as the sibling of `MODEL_LABEL_STRATEGIES`/`_accepts_model_label_decode`. `decode_global_id`'s Step-2 no-dot branch calls `_accepts_type_name_decode(strategy)` (relay.py:664). `filters/base.py::_accepted_globalid_type_names` consumes `TYPE_NAME_STRATEGIES` directly via `if strategy in TYPE_NAME_STRATEGIES:` (filters/base.py:245), imported at module top (filters/base.py:44: `from ..types.relay import MODEL_LABEL_STRATEGIES, TYPE_NAME_STRATEGIES`). The Slice-2 bare tuple `("type", "type+model")` is gone. Confirmed by the Worker 3 DRY review in the source artifact: "confirmed `{"type", "type+model"}` is now spelled exactly once (`TYPE_NAME_STRATEGIES` at relay.py:386) and consumed by both the decoder and the filter."

**Duplication risk avoided.**

- [x] **The strategy→shape sets are NOT re-typed.** Step-2 enforcement maps shape→strategy
  via the two named predicates: a model-label payload is permitted iff
  `_accepts_model_label_decode(strategy)` (relay.py:393, reused); a type-name payload is
  permitted iff `_accepts_type_name_decode(strategy)` (new, sourced from
  `TYPE_NAME_STRATEGIES`). The decoder never inlines `{"model", "type+model"}` or
  `{"type", "type+model"}` — both memberships are single-sourced in `types/relay.py`.
  - **Investigation (Worker 1):** Already addressed. `decode_global_id`'s Step-2 uses a ternary `_accepts_model_label_decode(strategy) if is_model_label else _accepts_type_name_decode(strategy)` (relay.py:661-664) — both named predicates, no inlined set. `encode_typename` (relay.py:460: `if strategy in MODEL_LABEL_STRATEGIES`) and `_accepted_globalid_type_names` (filters/base.py:243-245) both reference the named constants. The Worker 3 final DRY check in the source artifact confirms via grep: no surviving `{"model", "type+model"}` or `{"type", "type+model"}` literal outside the single `frozenset` definitions at relay.py:385-386.
- [x] **`callable` / `custom` "no decode" falls out of the predicate math, no third
  literal.** Neither predicate includes `callable` or `custom`, so a `callable`/`custom`
  candidate's payload is permitted by neither shape → uniform rejection. No separate
  encode-only set is needed; the absence from both `MODEL_LABEL_STRATEGIES` and
  `TYPE_NAME_STRATEGIES` IS the encode-only contract. (Worker 2 may add a brief comment
  naming this, but must not introduce a `{"callable", "custom"}` literal.)
  - **Investigation (Worker 1):** Already addressed. No `{"callable", "custom"}` literal exists anywhere in the source. The source-of-truth comment at relay.py:375-384 explicitly states the absence contract: "``callable`` and ``custom`` are intentionally in neither set: they are encode-only in 0.0.9 (no decode path), so the decoder's 'no decode for these' contract IS their absence from both memberships — there is deliberately no ``{"callable", "custom"}`` literal." The tests `test_decode_callable_strategy_has_no_decode_path` and `test_decode_custom_override_type_has_no_decode_path` (test_relay_interfaces.py:1901-1937) pin the rejection as `ConfigurationError`. Confirmed by Worker 3 DRY verification in the source artifact.
- [x] **The filter refactor is a pure swap, not a behavior change.**
  `filters/base.py:245` `if strategy in ("type", "type+model"):` becomes
  `if _accepts_type_name_decode(strategy):` (or `if strategy in TYPE_NAME_STRATEGIES:`)
  — same membership, now single-sourced. The `model` branch at filters/base.py:243
  already uses `MODEL_LABEL_STRATEGIES`; this makes the two branches symmetric. No
  filter-behavior change, no new filter test needed beyond confirming the existing
  Slice-2 filter tests (`tests/filters/test_base.py`) still pass (Worker 2 re-runs them;
  the import + tuple swap is behavior-preserving). The shared mapping import direction
  (`filters → types`) is already established and acyclic (Slice 2, filters/base.py:44
  module-top `from ..types.relay import MODEL_LABEL_STRATEGIES`); add
  `TYPE_NAME_STRATEGIES` (and/or the predicate) to that same import.
  - **Investigation (Worker 1):** Already addressed. `filters/base.py:245` reads `if strategy in TYPE_NAME_STRATEGIES:` (not the bare tuple). The import at filters/base.py:44 is `from ..types.relay import MODEL_LABEL_STRATEGIES, TYPE_NAME_STRATEGIES` — both constants imported together in the same import statement. The two branches in `_accepted_globalid_type_names` (filters/base.py:243-246) are now symmetric: `MODEL_LABEL_STRATEGIES` for the model-label branch, `TYPE_NAME_STRATEGIES` for the type-name branch. The Worker 2 build report and Worker 1 final verification both note the Slice-2 filter suite (59 tests) passed unchanged after the swap, confirming behavior is preserved.
- [x] **`registry.py` import of `implements_relay_node` is in-function** to avoid coupling
  the registry module top to `types.relay`. `registry.py` already uses in-function
  imports for the same cycle-dodge reason (the `clear()` filter/order/connection imports
  at registry.py:434-479, and the module-top `TYPE_CHECKING`-only `DjangoTypeDefinition`
  import at registry.py:29). `definition_for_graphql_name` is only called at decode time
  (well after module load), so an in-function `from .types.relay import
  implements_relay_node` resolves cheaply. (Worker 2 discretion: if a module-top import
  is proven acyclic under Django setup it is acceptable, but in-function is the safe
  default given `registry.py` is imported very early.)
  - **Investigation (Worker 1):** Already addressed. The in-function import is at registry.py:343 (`from .types.relay import implements_relay_node`), inside `definition_for_graphql_name`. The module-top `TYPE_CHECKING`-only import at registry.py:28-29 (`from .types.definition import DjangoTypeDefinition`) is the established same-file precedent for the type-only convention. The finding's reference to `registry.py:434-479` for the in-function `clear()` imports has drifted (the `definition_for_graphql_name` addition pushes those lines down), but the convention itself is confirmed intact. Worker 2 chose in-function (the safe default) and not module-top, consistent with the finding's preference.
- [x] **No hand-rolled label parsing / formatting.** The model-label-vs-type-name shape
  discrimination is "does the `type_name` slot contain a dot" (spec Decision 8 Step 1:
  `"app_label.modelname"` contains a dot, a bare GraphQL type name does not). Use the
  dot test directly on the parsed `gid.type_name`; `apps.get_model` is the canonical
  `app_label.model → model` resolver (it accepts the dotted `"app.model"` string or the
  split pair — Worker 2 picks; `apps.get_model("app", "model")` after a single
  `split(".", 1)` is the explicit form). Do NOT re-derive labels with `f"{app}.{model}"`.
  - **Investigation (Worker 1):** Already addressed. `decode_global_id` uses `"." in type_name` (relay.py:634) as the dot test and `type_name.split(".", 1)` (relay.py:636) for the split, then `apps.get_model(app_label, model_name)` (relay.py:638) — exactly the prescribed two-arg explicit form. No `f"{app}.{model}"` re-derivation appears anywhere in the decode path; the decoder only splits the incoming `type_name`, it never formats a label. The `from django.apps import apps` import is at relay.py:29 (added in Slice 3). Worker 3 explicitly confirms in the shadow walk: "`definition.model._meta.label_lower` is not re-derived in decode (decode never formats a label; it splits the incoming `type_name`)."

---

## Build: Slice 4 — live HTTP coverage (model-label emitted IDs, the headline filter round-trip, the deterministic `type`-opt-out)

_Source: `docs/builder/bld-slice-4-live_http.md`_

**Existing patterns reused (cite file:line — pin-at-write-time hints).**

- [x] **The `_decode_global_id(global_id) -> (type_name, node_id)` test helper**
  (`test_library_api.py:672-681`): base64-decodes a `GlobalID` string and partitions
  on `":"`. REUSE this exact helper for the library emitted-ID assertions (the
  type-name slot just moves from `"GenreType"` to `"library.genre"` — same decode,
  new expectation). `test_products_api.py` has NO such helper today; its new
  emitted-ID test (a) should decode via `relay.GlobalID.from_id(id)` (already
  imported as `relay`) per the source-site TODO pseudocode at
  `test_products_api.py:104-105` (`relay.GlobalID.from_id(id).type_name == "products.item"`),
  rather than re-introducing a second base64 helper. ONE decode idiom per file; do
  not copy the library helper into products.
  - **Investigation (Worker 1):** Already addressed. `test_library_api.py:660-669` retains `_decode_global_id` and it is reused at lines 697 and 2171 for the library emitted-ID assertions, with expectations now comparing against `models.Genre._meta.label_lower` (not `"GenreType"`). `test_products_api.py` has no `_decode_global_id` at all; its new emitted-ID tests at lines 125 and 178 decode exclusively via `relay.GlobalID.from_id(emitted["id"])` — one decode idiom per file, exactly as mandated.
- [x] **The `_global_id(type_name, pk)` helper** (`test_products_api.py:96-97`):
  builds a `GlobalID` string. REUSE it unchanged — the SIGNATURE stays
  `(type_name, pk)`; only the call-site `type_name` argument moves from
  `"ItemType"`/`"CategoryType"`/`"EntryType"` to the model labels
  `"products.item"`/`"products.category"`/`"products.entry"`. Do NOT rename the
  helper or change its body. (The same applies to the inline
  `relay.GlobalID(type_name="CategoryType", node_id=...)` builders in the filter
  tests — only the `type_name` literal moves.)
  - **Investigation (Worker 1):** Already addressed. `test_products_api.py:105-106` defines `_global_id(type_name, pk)` with its signature and body unchanged. The call-site `type_name` arguments at lines 251, 254, and 257 now pass `models.Entry/Item/Category._meta.label_lower` rather than the old `"EntryType"`/`"ItemType"`/`"CategoryType"` literals. The inline `relay.GlobalID(...)` builders at lines 364, 404, and 536 likewise pass `models.Category._meta.label_lower` — no parallel builder was introduced.
- [x] **The library reload-as-callable-helper pattern**
  (`test_library_api.py:19-50`): `_reload_library_project_schema()` is a plain
  callable; the autouse `_reload_project_schema_for_acceptance_tests` fixture just
  calls it. This is the EXACT shape spec Risks P3 wants the products suite to adopt
  so the opt-out test can call the reload itself (inside an `override_settings`
  block). The products autouse fixture (`test_products_api.py:42-67`) currently
  inlines the reload body — Slice 4 extracts it into a callable named helper, and
  the autouse fixture calls it. The reload pattern is mandated by
  `examples/fakeshop/test_query/README.md` #"clear the global registry, reload
  ... then reload the project schema and URLconf" and `docs/TREE.md`.
  - **Investigation (Worker 1):** Already addressed. `test_library_api.py:19-50` defines `_reload_library_project_schema()` as a plain callable, and the autouse fixture at line 47-50 calls it in one line — exactly the mandated shape. `test_products_api.py:42-70` now mirrors that shape: `_reload_products_project_schema()` is the extracted callable and the autouse fixture at lines 73-76 calls it as a one-liner. Both files match the reload-as-callable pattern.
- [x] **The `override_settings(DJANGO_STRAWBERRY_FRAMEWORK={...})` precedent**: the
  Slice-2 callable-setting tests (`tests/types/test_relay_interfaces.py`) and the
  Slice-1 precedence test already drive `RELAY_GLOBALID_STRATEGY` through
  `override_settings` + a registry-clear + reload; `conf.settings` reads
  `DJANGO_STRAWBERRY_FRAMEWORK` from Django settings (`AGENTS.md` #"reads
  `DJANGO_STRAWBERRY_FRAMEWORK` from the consumer's settings dict"). `test_multi_db.py:52`
  already imports `override_settings` in this directory — the idiom is established
  here.
  - **Investigation (Worker 1):** Already addressed. `test_products_api.py:34` imports `override_settings` from `django.test`, and the opt-out test at lines 169-170 uses `with override_settings(DJANGO_STRAWBERRY_FRAMEWORK={"RELAY_GLOBALID_STRATEGY": "type"}):` wrapping the reload call. `test_multi_db.py:52` also imports `override_settings` in this directory, confirming the idiom was already established here before Slice 4.
- [x] **The `_post_graphql` / `_assert_graphql_data` HTTP helpers**
  (`test_products_api.py:70-85`, `test_library_api.py:76-89`): REUSE for all new
  tests; do not hand-roll a second `client.post("/graphql/", ...)`.
  - **Investigation (Worker 1):** Already addressed. All three new products tests (`test_emitted_globalid_is_model_anchored` at line 120, `test_globalid_filter_round_trip` at lines 142/149, and `test_type_strategy_opt_out_reproduces_type_name` at line 173) call `_post_graphql` or `_assert_graphql_data` exclusively. The only `client.post(...)` call in `test_products_api.py` is the definition body of `_post_graphql` itself at line 81 — no hand-rolled duplicate.

- [x] **`seed_data(N)` / `create_users(N)`** (`apps.products.services`): the mandated
  first-line seeders (`AGENTS.md` #"First line of every catalog/auth test:
  seed_data(N)"). The new products tests seed via `seed_data(1)`; library tests
  use inline `Model.objects.create` (the library app has no `services.py` —
  `AGENTS.md` #"Library acceptance tests use inline Model.objects.create").
  - **Investigation (Worker 1):** Already addressed. All three new products tests (`test_emitted_globalid_is_model_anchored:118`, `test_globalid_filter_round_trip:140`, `test_type_strategy_opt_out_reproduces_type_name:167`) call `seed_data(1)` as their first line. The library acceptance tests continue to use inline `Model.objects.create` (e.g. `test_library_relay_node_global_id_round_trips:680`, `test_library_books_filter_by_relay_m2m_global_id:885-892`) — no `seed_data` import or usage appears in `test_library_api.py`.

**New helpers justified (single responsibility each).**

- [x] **One extracted callable: `_reload_products_project_schema()`** in
  `test_products_api.py` — single responsibility: clear the registry, reload
  `apps.products.schema` → `config.schema` → `config.urls`, clear URL caches. It is
  the verbatim body of the current autouse fixture, lifted out so the opt-out test
  (c) can invoke the reload itself **after** applying `override_settings`. The
  autouse fixture (`_reload_project_schema_for_acceptance_tests`) becomes a one-line
  wrapper calling it — byte-for-byte the library shape. No NEW reload logic; just a
  refactor-to-callable mirroring `test_library_api.py:19-50`. This is the ONLY new
  helper, and it exists solely to satisfy the spec's "not a brittle import-order
  exercise" requirement.
  - **Investigation (Worker 1):** Already addressed. `test_products_api.py:42-70` defines `_reload_products_project_schema()` as a module-level callable with exactly the mandated body (`registry.clear()` → reload `apps.products.schema` → reload `config.schema` → reload `config.urls` + `clear_url_caches()`). The autouse fixture at lines 73-76 is a one-line wrapper. This is structurally identical to `test_library_api.py:19-50`. The callable is the only new helper added; no other helper was introduced.

**Duplication risk avoided.**

- [x] **Do NOT introduce a second products base64-decode helper.** Test (a) decodes the
  emitted products `id` via `relay.GlobalID.from_id(...)` (the source-site TODO's
  own pseudocode), not by copying `test_library_api.py::_decode_global_id`. The
  library file keeps its existing `_decode_global_id`; products uses the `relay`
  parse API it already imports. One decode idiom per file.
  - **Investigation (Worker 1):** Already addressed. `test_products_api.py` has no `_decode_global_id` definition or call anywhere in the file. The new emitted-ID tests at lines 125 and 178 both decode via `relay.GlobalID.from_id(emitted["id"])` reading `.type_name` and `.node_id`. The library file's `_decode_global_id` at line 660 is untouched and only used within `test_library_api.py`.
- [x] **Do NOT re-type the model-label string as a literal where the ORM object is in
  scope.** Worker 2 may assert against `models.Item._meta.label_lower` /
  `f"{models.Genre._meta.label_lower}"` rather than a hardcoded `"products.item"`
  in the body of a test that already holds the model — this keeps the expectation
  derived from the same source the encoder reads (mirrors the suite's existing
  "compare API == ORM" data-driven posture, `test_products_api.py:18-22`). A
  hardcoded label string is acceptable in a docstring or where no model object is
  bound, but the load-bearing assertion should derive it. Discretion item.
  - **Investigation (Worker 1):** Already addressed. Every load-bearing emitted-ID and filter-input assertion in `test_products_api.py` derives the label from `Model._meta.label_lower` rather than a hardcoded literal (e.g. `test_emitted_globalid_is_model_anchored:126` asserts `parsed.type_name == models.Item._meta.label_lower`; the depth-2 test at lines 251/254/257 passes `models.Entry/Item/Category._meta.label_lower` to `_global_id`; all three filter-input tests at lines 364/404/536 pass `models.Category._meta.label_lower`). The only capitalized type-name string in the file is `"ItemType"` at line 179, which is intentional — it is the assertion inside the `type`-strategy opt-out test confirming the GraphQL type name is reproduced under that strategy.
- [x] **Do NOT duplicate the opt-out reload.** The opt-out test (c) calls the SAME
  extracted `_reload_products_project_schema()` inside its `override_settings`
  block — it does not inline a third copy of the registry-clear/reload sequence.
  - **Investigation (Worker 1):** Already addressed. The only call to `registry.clear()` in `test_products_api.py` is inside `_reload_products_project_schema()` at line 54. The opt-out test `test_type_strategy_opt_out_reproduces_type_name` at line 172 calls `_reload_products_project_schema()` inside its `override_settings` block — it does not inline any reload steps. There are exactly three references to `_reload_products_project_schema` in the file: the definition (line 42), the autouse fixture call (line 76), and the opt-out test call (line 172).
- [x] **The `_global_id(type_name, pk)` builder stays one helper.** Every products
  filter-input test that builds a `GlobalID` routes through `_global_id` or the
  existing inline `relay.GlobalID(...)`; the model-label move is purely a
  `type_name`-argument change, never a parallel new builder.
  - **Investigation (Worker 1):** Already addressed. `test_products_api.py` has exactly one `GlobalID`-building helper definition: `_global_id` at line 105. The depth-2 emitted-ID test routes through it (lines 251/254/257). The filter-input tests use either `_global_id` or the inline `relay.GlobalID(...)` form (lines 364/404/536) — all three are the same `relay.GlobalID` call pattern with `type_name=models.Category._meta.label_lower`, not a new builder. No parallel GlobalID-building function was introduced.

---

## Build: Slice 5 — doc updates + card-completion wrap

_Source: `docs/builder/bld-slice-5-doc_card_wrap.md`_

- [x] **Existing patterns reused (cite path / line).**
  - **Canonical phrasings reused from the spec — do not re-invent prose.** The shipped-surface wording for every doc edit is derived from the spec, single-sourced:
    - The four-strategy semantics (`model` default / `type` opt-out / `type+model` transitional / callable encode-only) come from spec Decision 4 (`docs/spec-031-globalid_encoding-0_0_9.md` lines 318-329).
    - The precedence (`Meta.globalid_strategy` → `RELAY_GLOBALID_STRATEGY` → `"model"`) comes from Decision 5 (lines 342-344).
    - The `Meta.globalid_strategy` validation surface (net-new `ALLOWED_META_KEYS` key, callable arity/sync check, Relay-Node gating) comes from Decision 6 (lines 356-365).
    - The `RELAY_GLOBALID_STRATEGY` settings discipline (thin `conf.py` reader, validated at finalization, shared validator) comes from Decision 7 (lines 379-381).
    - The breaking-change posture + the **exact** `type+model`-first upgrade sequence + the not-a-rename-history-alias-map caveat come from Decision 9 (lines 436-454).
  - **One canonical breaking-change narrative, three render targets, no duplicated prose beyond what each needs.** The `type+model`-first upgrade sequence is the single load-bearing fact. It is written in full (3 ordered steps + the rename-ordering caveat) in `CHANGELOG.md` (the durable release record) and in `TODAY.md` (the products-centric playbook). `docs/GLOSSARY.md` (Relay Node integration body), `docs/README.md`, and `README.md` describe the model-anchored default + the opt-out and **link/point** to the detail rather than re-typing the full 3-step sequence — each says only what its altitude needs. Watch point for Worker 3: the sequence must read coherently in both CHANGELOG and TODAY but must not be a third near-copy elsewhere.
  - **GLOSSARY entry-body shape reused from the recent shipped-`0.0.9` precedents.** `` `Meta.connection` `` (`docs/GLOSSARY.md` lines 611-627) is the net-new-key-stored-on-definition precedent (Decision 6 cites it); `` `Meta.nullable_overrides` `` / `` `Meta.required_overrides` `` (lines 713-741, 799-807) are the companion-pair shipped-`0.0.9` precedent for two related entries that cross-reference each other. The new entries reuse this body shape (one-paragraph lead, a `python` Meta example, a `**See also:**` line) — not a new format.
  - **DB-seeding mechanics reused verbatim from `worker-0.md` "Closing out a kanban card"** (`docs/builder/worker-0.md` lines 159-187) — the GlossaryTerm/membership ORM steps, the SpecDoc/CardGlossaryTerm done-card invariant, the `import_spec_terms` CSV-anchor precondition, and the byte-clean regenerate verification. No new procedure is invented.
  - **Investigation (Worker 1):** Already addressed. All four sub-claims are satisfied by the shipped changes. (a) Canonical spec phrasings: CHANGELOG.md line 22's `### Changed` bullet and TODAY.md lines 209-215 carry the exact `type+model`-first sequence derived from Decision 9; `docs/README.md` line 80 and README.md lines 50-51 use the four-strategy summary (`model`/`type`/`type+model`/callable) without re-authoring prose. (b) Breaking-change narrative discipline: the body shape at `docs/GLOSSARY.md` lines 668-689 (`` `Meta.globalid_strategy` ``) follows the established `Meta.connection` / `Meta.nullable_overrides` pattern exactly — one-paragraph lead, a `python` Meta example, a `**See also:**` line — and `RELAY_GLOBALID_STRATEGY` at lines 1047-1061 follows the same shape. (c) GLOSSARY entry bodies match the companion-pair precedent: `Meta.required_overrides` (line 814) is a short inverse-direction companion that cross-links `Meta.nullable_overrides` for the full table, and `RELAY_GLOBALID_STRATEGY` (line 1047) is exactly the same shape — it cross-links `Meta.globalid_strategy` for the full strategy table. (d) DB-seeding mechanics: the build report (`bld-slice-5-doc_card_wrap.md` lines 160-168) confirms every generated-doc change went through the Django ORM shell + the three regenerate scripts, with the byte-clean GLOSSARY idempotency check confirming no hand-edit of the markdown.
- [x] **New helpers justified.** None. Slice 5 introduces no code and no shared helper. All "shared" content is the single-sourced spec phrasing above.
  - **Pre-triage (Worker 0):** Skipped — "None. Slice 5 introduces no code and no shared helper."
- [x] **Duplication risk avoided.** The naive failure mode is (a) writing the full upgrade sequence in 4+ files (fixed: full sequence only in CHANGELOG + TODAY, summary elsewhere), and (b) hand-editing the generated `GLOSSARY.md` / `KANBAN.md` markdown directly (fixed: every generated-doc change goes through the DB ORM then regenerate, per nature B). A third risk is duplicating the strategy list between the two new glossary entries — avoided by making `RELAY_GLOBALID_STRATEGY`'s body the project-wide-knob view that cross-links `` `Meta.globalid_strategy` `` for the full strategy table (mirroring how `Meta.required_overrides` defers to `Meta.nullable_overrides` for the full validation table).
  - **Investigation (Worker 1):** Already addressed. All three risk-avoidance sub-claims are verified. (a) Full 3-step upgrade sequence confinement: CHANGELOG.md (line 22) and TODAY.md (lines 209-215) each carry the complete ordered steps + rename-ordering caveat; `docs/README.md` line 80 gives a one-sentence four-strategy summary and links to `docs/GLOSSARY.md#metaglobalid_strategy`; README.md line 50 gives the same summary inline with no sequence steps; neither is a "third near-copy" — Worker 3's DRY findings in the build report explicitly confirmed no third near-copy exists (`bld-slice-5-doc_card_wrap.md` line 237). (b) Generated-doc ORM+regenerate discipline: the build report (lines 160-168) records that all DB changes went through `manage.py shell` ORM writes followed by `build_glossary_md.py`, `build_kanban_md.py`, and `build_kanban_html.py`; the byte-clean idempotency check (MD5 `51f5612a5a1ca2f1e34ec8f4817516d9`, build report line 192 and Worker 3 review line 260) proves no hand-edit of the rendered markdown survived. (c) Strategy list not duplicated: `docs/GLOSSARY.md` lines 668-689 (`` `Meta.globalid_strategy` ``) carries the full four-strategy table with descriptions; lines 1047-1061 (`RELAY_GLOBALID_STRATEGY`) deliberately omits the callable form and instead says "see `Meta.globalid_strategy` for the full strategy table and the callable form", mirroring exactly the `Meta.required_overrides`→`Meta.nullable_overrides` cross-link pattern at line 818.

---

## Final test-run gate

_Run by Worker 2 on 2026-06-10._

| Command | Result |
|---------|--------|
| `uv run pytest --no-cov` | ✅ pass (1516 passed, 3 skipped, 0 failures) |
| `uv run python examples/fakeshop/manage.py check` | ✅ pass |
| `uv run python examples/fakeshop/manage.py makemigrations --check --dry-run` | ✅ pass |
| `uv run ruff format --check .` | ✅ pass (235 files already formatted) |
| `uv run ruff check .` | ✅ pass |
| `git diff --check` | ✅ pass |

**Overall: PASS**
