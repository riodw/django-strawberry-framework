# DRY consolidation plan: 0.0.9

Source: `docs/builder/`
Generated: 2026-06-06
Workflow: `docs/dry/DRY.md`
Pre-flight: passed with explicit baseline. Source cycle closure assumed from maintainer invocation; `git status --short` showed pre-existing out-of-scope `docs/feedback.md`; exporter help passed; planned path `docs/dry/dry-0_0_9.md` was free.

One finding at a time. Worker 0 dispatches > Worker 1 triages > Worker 2 implements > Worker 1 verifies > Worker 0 ticks.

## Findings

## Build: Slice 1 — migrate `extensions=` construction sites to the singleton-factory form

_Source: `docs/builder/bld-slice-1-extensions_singleton_factory.md`_

- [x] **Existing patterns reused.**
  - The migration target form is *already shipped in three places* and is the single canonical shape to copy verbatim — do not invent a variant. The TODO scaffolds the maintainer placed are the authoritative templates:
    - Module-level singleton + factory: `examples/fakeshop/config/schema.py:33-40` (TODO pseudo), `docs/README.md:52-56` (TODO pseudo), `docs/GLOSSARY.md:358-364` (TODO pseudo). All three spell `_optimizer = DjangoOptimizerExtension()` then `extensions=[lambda: _optimizer]`.
    - Function-local form (test files): `tests/optimizer/test_extension.py:34-39` (module-top TODO), `tests/optimizer/test_field_meta.py:10`, `tests/optimizer/test_relay_id_projection.py:14`, `tests/test_list_field.py:41`, `tests/types/test_generic_foreign_key.py:13`, `examples/fakeshop/test_query/test_multi_db.py:32` — all spell `schema = strawberry.Schema(..., extensions=[lambda: ext])` keeping the function-local `ext`.
  - The behavior-preserving guarantee (one shared instance per process) means the existing optimizer suite (`tests/optimizer/test_extension.py` etc.) IS the regression guard; no new behavioral test is added beyond the one no-warning assertion (spec Test plan "Slice 1").
  - **Investigation (Worker 1):** Already addressed. Current source/docs already use the canonical singleton/factory forms: module singleton in `examples/fakeshop/config/schema.py`, `docs/README.md`, and `docs/GLOSSARY.md`, plus function-local `extensions=[lambda: ext]` / `extensions=[lambda: optimizer]` across the cited tests; a focused scan found no remaining Python `extensions=[DjangoOptimizerExtension(...)]`, `extensions=[DjangoOptimizerExtension]`, `extensions=[ext]`, or `extensions=[_CaptureExt()]` forms.
- [x] **New helpers justified.** None. Slice 1 is a mechanical per-construction-site rewrite of an existing call expression (`extensions=[<instance-or-class>]` → `extensions=[lambda: <instance>]`); it adds no function, class, module, or constant. Extracting any shared helper (e.g. a `make_optimizer_factory()`) would be premature and would obscure the per-site lifetime/strictness semantics the spec deliberately keeps explicit per site (Decision 3 "Granularity is per construction site, not per file"). The condition that would later justify a helper: if a consumer-facing API were added to construct the factory (out of scope; the spec rejects it).
  - **Pre-triage (Worker 0):** Skipped — finding states "None" and says Slice 1 adds no function, class, module, or constant.
- [x] **Duplication risk avoided.** The naive implementation could (a) introduce a single module-level `_optimizer` shared across the ~41 schema builds in `tests/optimizer/test_extension.py`, which would *pollute the per-test `cache_info()` counters* and break the `strictness="raise"` site in `test_relay_id_projection.py`; the plan prevents this by keeping each test's **function-local** `ext` and wrapping only the call site (`extensions=[lambda: ext]`). It could also (b) collapse the two distinct directions of the same lambda body into a copy-paste constant; that risk does not arise because each `lambda` closes over a *different* per-site instance, so there is no literal to extract. The repeated literal `extensions=[lambda: ext]` across test sites is the intended, readable, per-site shape — not a DRY defect (it is the upstream-recommended callable form applied uniformly).
  - **Investigation (Worker 1):** Not real. Current source preserves the intended lifetime split: cited tests keep function-local `ext` / `capture_ext` / `optimizer` closures for per-test cache counters and strictness values, while example/docs use module-level `_optimizer`, so extracting a shared factory would erase the per-site ownership the slice deliberately makes explicit.

---

## Build: Slice 2 — `inspect_django_type` diagnostic command

_Source: `docs/builder/bld-slice-2-inspect_django_type.md`_

- [x] **Existing patterns reused.**
  - **`export_schema.py` `Command` shape** (`django_strawberry_framework/management/commands/export_schema.py:11-56`) is the verbatim structural template: `class Command(BaseCommand)` with `help`, `add_arguments(self, parser: CommandParser)`, `handle(self, *args, **options)`, and the `try: import_module_symbol(...) except (ImportError, AttributeError) as e: raise CommandError(str(e)) from e` discipline (`export_schema.py:34-40`). Slice 2's `--schema` loader is the **same call** — `import_module_symbol(options["schema"], default_symbol_name="schema")` — so the schema-import-and-finalize step is a literal reuse, not a near-copy.
  - **`registry.iter_types()`** (`django_strawberry_framework/registry.py:225-235`) yields `(model, type_cls)` once per registered type; the bare-name lookup iterates it collecting `type_cls.__name__ == arg` matches. **`registry.model_for_type(type_cls)`** (`registry.py:211-223`) gives the Django model for the ambiguity-candidate listing (`module.qualname` + model). No new registry surface needed.
  - **`DjangoTypeDefinition`** (`django_strawberry_framework/types/definition.py:73-106`): the command reads `definition.origin` (the `DjangoType` class), `definition.selected_fields` (Django field instances in selection order, `definition.py:79`), `definition.field_map` (`dict[str, FieldMeta]`, `definition.py:80`), and `definition.finalized` (`definition.py:95`). All already-public attributes.
  - **`FieldMeta`** (`django_strawberry_framework/optimizer/field_meta.py:50-189`): the command reads `name`, `is_relation`, `nullable`, `related_model`, the cardinality flags, plus `relation_kind` / `is_many_side` properties (`field_meta.py:107-115`) to classify the relation-converter row (forward FK / M2M / reverse FK). This is the Django-side metadata + converter-classification source.
  - **`scalar_for_field(field)` / `SCALAR_MAP`** (`django_strawberry_framework/types/converters.py:54-139`): the MRO walk that names *which* `SCALAR_MAP` row fired. The command re-runs this lookup **only to NAME the row** (Decision 4: "to NAME which row fired … not to determine nullability"). It MUST NOT re-run `convert_scalar(...)` for nullability — that would reproduce column-native `field.null` widening and miss a Slice-3 override or a consumer-authored annotation.
  - **`origin.__annotations__`** is the authoritative resolved-GraphQL-type + nullability record (written at `types/base.py:341` `cls.__annotations__ = {**synthesized, **consumer_annotations}` and rewritten per relation by the finalizer). The command reads nullability from here, NOT from a `convert_scalar` re-run.
  - **Test reload fixture**: `examples/fakeshop/test_query/test_library_api.py::_reload_library_project_schema` (lines 33-58) is the exact `registry.clear()` + `importlib.reload("apps.library.schema")` + `importlib.reload("config.schema")` (+ URLconf `clear_url_caches`) pattern the bare-name happy-path tests mirror, per `examples/fakeshop/test_query/README.md`.
  - **Failure-mode test scaffolding**: `tests/management/test_export_schema.py::_make_test_module` (lines 21-25) — the `types.ModuleType` + `monkeypatch.setitem(sys.modules, "test_module", module)` helper — is the template for the non-`DjangoType`-symbol and bad-dotted-path failure tests.
  - **Investigation (Worker 1):** Already addressed. Current `django_strawberry_framework/management/commands/inspect_django_type.py` reuses the cited `export_schema.py` `import_module_symbol(..., default_symbol_name="schema")`/`CommandError` shape for `--schema`, resolves bare names through `registry.iter_types()` with ambiguity candidates from `registry.model_for_type()`, reads `DjangoTypeDefinition` / `FieldMeta` / `SCALAR_MAP` metadata rather than adding new registry or definition surface, and renders resolved type/nullability from finalized annotations/Strawberry field metadata rather than re-running `convert_scalar`; `examples/fakeshop/tests/test_inspect_django_type.py` mirrors the schema-reload pattern and `tests/management/test_inspect_django_type.py` mirrors the `_make_test_module` scaffolding.

- [x] **New helpers justified.** Within `inspect_django_type.py`, decompose `handle()` into small private methods/functions so each branch stays under the BUILD.md hotspot thresholds (40 lines / 8 branches) and each is independently legible. Suggested (names at Worker 2's discretion):
  - `_resolve_type(arg)` — the dot-dispatch resolver: dotted → `import_string` (raise-on-failure, original error), bare → unique-`__name__` registry scan.
  - `_row_for_field(definition, field)` — produce one printed row (Django field name, Django field type, resolved GraphQL type + nullability from `origin.__annotations__`, converter classification), including the Relay-suppressed-pk special case.
  These are command-internal, single-responsibility, and serve only this one command — they are NOT new package-level public helpers (the spec scopes Slice 2 to "no new public API"). Do NOT add a method to `DjangoTypeDefinition` or `registry` for the bare-name scan; iterating `registry.iter_types()` inline in the resolver is the right altitude (one call site).
  - **Investigation (Worker 1):** Already addressed. Current `django_strawberry_framework/management/commands/inspect_django_type.py` has `Command.handle()` limited to schema import, type resolution, validation, finalized-state checks, and table printing, with command-internal helpers including `_resolve_type`, `_resolve_bare_name`, `_print_table`, `_resolve_row`, `_relation_row`, `_scalar_row`, `_consumer_authored_row`, `_is_suppressed_relay_pk`, and rendering/label helpers; no new `DjangoTypeDefinition` or registry API was added, and the cited tests/docs already exercise and document the decomposed command.

- [x] **Duplication risk avoided.** The naive implementation would call `convert_scalar(field, type_name)` to recompute the GraphQL type/nullability — duplicating the construction-time logic AND producing a wrong (column-native) answer for override/consumer-authored fields. The plan forbids that: nullability/type comes from `origin.__annotations__`; `scalar_for_field`/`SCALAR_MAP` is re-walked **only** for the converter-row *name*. Also avoid duplicating `export_schema`'s `import_module_symbol` try/except shape with a divergent message — reuse the same `except (ImportError, AttributeError) as e: raise CommandError(str(e)) from e` form for the `--schema` load so the two commands read identically.
  - **Investigation (Worker 1):** Already addressed. Current `django_strawberry_framework/management/commands/inspect_django_type.py` loads `--schema` with the same `import_module_symbol(..., default_symbol_name="schema")` plus `except (ImportError, AttributeError) as e: raise CommandError(str(e)) from e` shape as `export_schema.py`, and renders scalar / consumer-authored rows from `origin.__annotations__` or finalized Strawberry field metadata while only naming the `SCALAR_MAP` row via `_matched_scalar_key`; `examples/fakeshop/tests/test_inspect_django_type.py::test_inspect_reads_resolved_annotation_not_field_null` now pins the Slice-3 override case that a `convert_scalar` re-run would misreport.

---

## Build: Slice 3 — `Meta.nullable_overrides` / `Meta.required_overrides`

_Source: `docs/builder/bld-slice-3-nullability_overrides.md`_

- [x] **Existing patterns reused.**
  - **Validator-helper shape.** `_validate_nullability_override_targets(...)` mirrors `django_strawberry_framework/types/base.py::_validate_optimizer_hints` (the closest structural template — it also takes the selected `fields` tuple + `model`, derives a model-wide name set and a selected-subset name set, and raises `ConfigurationError` with the shared `_format_unknown_fields_error` for the unknown path). The `_validate_filterset_class`/`_validate_orderset_class` pair (base.py:72-123, cited by spec as the structural template) is the template for the *`_validate_meta`-resident shape check* — both return a normalized value (or `None`) and raise `ConfigurationError` for a bad shape; the new shape-check returns the normalized `frozenset` pair onto `_ValidatedMeta`.
  - **Shape-normalize helper.** `_normalize_sequence_spec` (base.py:403-409) already implements the exact "non-string sequence of field names, else `ConfigurationError`" guard the spec demands (Edge case lines 567, mirroring the `Meta.exclude` guard `base.py #"Meta.exclude must be a non-string sequence"`). Reuse it verbatim for both override keys — do NOT hand-roll a second sequence guard. (Note: its error message hardcodes "Meta.exclude"; see Implementation discretion item 1.)
  - **Unknown-field error formatting.** `_format_unknown_fields_error` (base.py:494-508) is the shared "names unknown fields … Available: …" shape used by `Meta.fields`/`Meta.exclude`/`Meta.optimizer_hints`. The *unknown* error path of the target-validator should route through it so the consumer-visible shape stays consistent.
  - **Consumer-authored / relation / selected-set inputs already computed.** `__init_subclass__` already computes `consumer_authored_fields` (the four-corner union, base.py:260-267), the selected `fields` tuple (`_select_fields`, used for the field_map), and `relay_shaped` (base.py:268). `field.is_relation` is the relation flag (used at `_build_annotations` line 947). The validator consumes these as inputs — it derives NONE of them itself, so no parallel computation.
  - **The Relay-pk suppression state.** `_build_annotations` computes `pk_name = source_model._meta.pk.name if suppress_pk_annotation else None` (base.py:945) and `suppress_pk_annotation = _is_relay_shaped(cls, interfaces)`. The validator's *Relay-pk reject* (rule e) uses the SAME `_is_relay_shaped(cls, validated.interfaces)` result (already bound as `relay_shaped` at base.py:268) plus `meta.model._meta.pk.name` — one source of truth for "is this the suppressed pk."
  - **Tri-state widening seam.** `convert_scalar`'s three existing `if field.null:` widening sites become the three `if effective_null:` sites — one computed value applied uniformly. No new conversion entry point (Decision 7 rejects a `convert_scalar_with_override` wrapper).
  - **Acceptance type / reload fixture / SDL introspection.** The live HTTP test reuses `examples/fakeshop/test_query/test_library_api.py`'s autouse `_reload_library_project_schema` fixture (lines 33-64), `_seed_library_graph` (line 67), `_post_graphql`/`_assert_graphql_data`/`_field_type` helpers (lines 90-107), and the exact `__type(name: ...) { fields { name type { kind name ofType {...} } } }` introspection shape already used by `test_library_choice_enum_and_nullable_subtitle_are_deliberate_http_contracts` (lines 368-393). The new acceptance type mirrors the scaffolded `NullabilityOverrideBookType` pseudo at `apps/library/schema.py #"TODO(spec-029 Slice 3): Add the nullability-override acceptance type"`.
  - **Converter test doubles.** `tests/types/test_converters.py` already ships `_FakeArrayField`/`_FakeHStoreField` doubles + a `monkeypatch.setattr(converters, "_ARRAY_FIELD_CLS"/"_HSTORE_FIELD_CLS", ...)` pattern, non-null/nullable `TextField` subclass fixtures, and choice fixtures — the `force_nullable` tri-state tests extend these, not new fixtures.
  - **Investigation (Worker 1):** Already addressed. Current source implements the prescribed reuse shape: `django_strawberry_framework/types/base.py` routes override shape checks through `_normalize_sequence_spec`, unknown-target errors through `_format_unknown_fields_error`, and target validation through `_validate_nullability_override_targets(...)` using the selected fields, consumer-authored set, and Relay-shape state computed by `DjangoType.__init_subclass__`; `django_strawberry_framework/types/converters.py::convert_scalar` computes one `effective_null` and applies it at the ArrayField, HStoreField, and shared scalar/choice widening sites without a wrapper; `examples/fakeshop/apps/library/schema.py` provides `NullabilityOverrideBookType` and its resolver; the cited converter/base/live HTTP/inspect tests and `docs/GLOSSARY.md` entries are present.

- [x] **New helpers justified.**
  - **`_validate_nullability_override_targets(...)`** — single responsibility: given the normalized override frozensets + the selected fields + `consumer_authored_fields` + the model + the Relay-pk identity, raise `ConfigurationError` for every unknown / excluded / consumer-authored / relation / Relay-suppressed-pk target. Single call site (`__init_subclass__`, after `_select_fields`/`consumer_authored_fields`/Relay-shape). Justified because inlining into the 130-line `__init_subclass__` hotspot would push it past complexity thresholds (review_inspect finding) and because the five error paths read cleanly as one cohesive guard. It does NOT recompute the selected set, the consumer set, or the Relay state — those are passed in.
  - **No second new helper.** The `_validate_meta` shape/normalize/collision step and the `_build_annotations` `force_nullable` computation are small enough to live inline at their existing sites (3-5 lines each), reusing `_normalize_sequence_spec`. Extracting them would be premature; the condition that would justify a `_validate_override_shapes` helper later is a third override-key family appearing, at which point the normalize+collision logic could be hoisted.
  - **Investigation (Worker 1):** Already addressed. Current `django_strawberry_framework/types/base.py` has the planned private module-level `_validate_nullability_override_targets(...)` with one call from `DjangoType.__init_subclass__` after `_select_fields`, `consumer_authored_fields`, and `relay_shaped` are computed; `_validate_meta` keeps only the inline `_normalize_sequence_spec` shape/collision step, and `_build_annotations` keeps only the inline per-field `force_nullable` selection before calling `convert_scalar(..., force_nullable=force_nullable)`.

- [x] **Duplication risk avoided.**
  - **Risk: re-deriving the selected-field name set inside the validator.** Avoided — pass the already-computed `fields` tuple and derive the two name sets (`model._meta.get_fields()` for *unknown*, `{f.name for f in fields}` for *excluded*) locally as read-only sets, exactly as `_validate_optimizer_hints` does, not re-running `_select_fields`.
  - **Risk: collapsing *unknown* and *excluded* into one path** (which would weaken the `Meta.exclude` contract — spec Decision 8 explicitly forbids). Avoided — two distinct name sets, two distinct error messages, checked unknown-first then excluded.
  - **Risk: forwarding the outer `force_nullable` into the recursive `ArrayField.base_field` call** (`convert_scalar(field.base_field, type_name)` at converters.py:213). Avoided — the recursion stays `force_nullable`-unset (default `None`), so the inner element nullability follows `base_field.null` and only the OUTER `list[inner]` honors `effective_null` (spec Edge case lines 563 — "the inner element nullability follows base_field.null and is NOT affected by the outer override").
  - **Risk: a parallel nullability-stripping path at the `_build_annotations` call site** (unwrap `T | None`). Avoided by Decision 7 — the tri-state computes widening *before* it happens; nothing is unwrapped.
  - **Risk: persisting the override sets on `DjangoTypeDefinition`** (a new slot). Avoided per Non-goals (lines 167) / Decision 7 — the resolved annotation on `cls.__annotations__` IS the authoritative record; no definition slot is added.
  - **Investigation (Worker 1):** Already addressed. Current `django_strawberry_framework/types/base.py::_validate_nullability_override_targets` receives `selected_fields`, `consumer_authored_fields`, and `relay_shaped` from `DjangoType.__init_subclass__`, derives only `model_field_names` and `selected_by_name` as read-only sets, keeps unknown / excluded / consumer-authored / Relay-pk / relation errors distinct, and does not persist override sets on `DjangoTypeDefinition`; `django_strawberry_framework/types/converters.py::convert_scalar` collapses the tri-state once into `effective_null`, leaves the recursive `convert_scalar(field.base_field, type_name)` call unset, and applies only the outer widening from that single value.

---

## Build: Slice 4 — Card-completion wrap

_Source: `docs/builder/bld-slice-4-card_completion_wrap.md`_

- [x] **Existing patterns reused.** The card-move mechanic is the same one every prior Done card already went through (`DONE-001` … `DONE-028`); the rendered id convention (`DONE-NNN-version`, milestone dropped) is centralized in `examples/fakeshop/apps/kanban/models.py::format_card_id` via `Card.card_id`, and `scripts/build_kanban_md.py::card_key` reads the rendered GraphQL `cardId` — no new id-formatting logic is invented. The stale-ref rewrites reuse the already-correct `Spec:` line in the card header (`KANBAN.md` line 146 already points at `spec-029-consumer_dx_cleanup-0_0_9.md`); only the body's Slice-3 Scope/DoD prose carries the stale `spec-021` / `0.0.8` strings.
  - **Investigation (Worker 1):** Already addressed. The source artifact's final verification records the maintainer-authorized Path A DB-backed closeout: card 29's DB row now renders as `DONE-029-0.0.9`, `KANBAN.md`/`KANBAN.html` were regenerated from `examples/fakeshop/db.sqlite3`, the stale `spec-021` and `## [0.0.8]` card-body references were rewritten, and current source keeps the id convention in `examples/fakeshop/apps/kanban/models.py::format_card_id` / `Card.card_id` with `scripts/build_kanban_md.py::card_key` only consuming `cardId`.
- [x] **New helpers justified.** None. This slice writes no code and adds no helper; it mutates board data + regenerates (Path A) or hand-edits rendered markdown (Path B).
  - **Pre-triage (Worker 0):** Skipped — finding states "None" and says Slice 4 writes no code and adds no helper.
- [x] **Duplication risk avoided.** The chief risk is the rendered/source split itself: a naive hand-edit duplicates the card-state in two places (the `KANBAN.md` text and the DB row) that then disagree. Path A avoids it by keeping the DB as the single source of truth and re-deriving `KANBAN.md`/`KANBAN.html` from it. The plan flags this explicitly rather than letting Worker 2 introduce a silent fork.
  - **Investigation (Worker 1):** Already addressed. The source artifact's final verification records the maintainer-authorized Path A resolution: card 29 was updated in `examples/fakeshop/db.sqlite3`, `KANBAN.md` / `KANBAN.html` were regenerated from that DB-backed source, and the current board states that direct `KANBAN.md` edits are overwritten while `examples/fakeshop/apps/kanban/models.py::format_card_id` / `Card.card_id` remain the single id-format source consumed by `scripts/build_kanban_md.py::card_key`.
