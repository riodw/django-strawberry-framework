# Adversarial review feedback

No open review. This file is the repository's rotating adversarial-review
record: each review overwrites it, and it is cleared when its findings are
fully resolved. Prior reviews (and the finding IDs cited in source
docstrings, e.g. the sealed `get_queryset` visibility-boundary rounds) are
recoverable from this file's git history; the governing records for resolved
work are the numbered specs (`docs/spec-*.md`) and `docs/GLOSSARY.md`.
