"""Frozen base tests for settings, initialization, version, logging, and public exports.

Package-root and ``conf`` internals with no wire shape: version / ``__all__`` /
logger identity, and the ``Settings`` reader. Live siblings for the settings
keys a request can actually turn are named in ``tests/base/test_conf.py``.
"""
