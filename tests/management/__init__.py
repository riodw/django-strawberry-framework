"""Package tests for django_strawberry_framework.management.*."""

# TODO spec-018 Slice 2: empty marker so pytest collects tests as
# `tests.management.<module>` (mirrors `tests/optimizer/__init__.py` and
# `tests/types/__init__.py` per Decision 7 of
# `docs/spec-018-export_schema-0_0_7.md`).
