"""Tests for package Django middleware integrations."""

# TODO(spec-042 Slice 1): Keep middleware-specific tests in this package.
# The debug-toolbar suite belongs here, not in examples/fakeshop/test_query/,
# because fakeshop's shipped settings deliberately do not install the optional
# toolbar app or middleware.
