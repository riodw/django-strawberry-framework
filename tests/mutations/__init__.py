"""Package tests for the mutations subsystem (DjangoMutation + generated inputs)."""

# TODO(spec-036 Slice 1): keep mutation package tests in this mirror package.
# Pseudocode: add one test module per mutation source module so the package
# internal mechanics stay under ``tests/mutations/`` while live GraphQL behavior
# is earned through the fakeshop ``test_query`` suite.
